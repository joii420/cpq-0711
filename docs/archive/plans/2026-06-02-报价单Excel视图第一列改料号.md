# 报价单 Excel 视图第一列改料号 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把报价单 Excel 视图（`LinkedExcelView`）的第一列从「客户料号」(`__label`/customerPartNo) 改为「料号」(`__hfPartNo`/productPartNo，即生产料号)。

**Architecture:** 纯前端单文件改动。第一列是前端硬编码的行头列（与模板 `excel_view_config` 无关）。`__hfPartNo` 在 `useLinkedExcelRows.ts` 已等于 `li.productPartNo`，只需把第一列的 `title` 与 `dataIndex`/`key` 换掉。无后端、无 DB、无迁移。

**Tech Stack:** React + Ant Design Table；TypeScript；Vite；Playwright E2E。

---

### Task 1: 把第一列改为「料号」/`__hfPartNo`

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/LinkedExcelView.tsx`（第一列定义，约 64-68 行）
- Verify (E2E): `cpq-frontend/e2e/quotation-flow.spec.ts`

- [ ] **Step 1: 定位并修改第一列定义**

打开 `cpq-frontend/src/pages/quotation/LinkedExcelView.tsx`，找到 `tableColumns` 数组的第一个元素：

```js
// 改前
{
  title: '客户料号', dataIndex: '__label', key: '__label',
  fixed: 'left' as const, width: 200,
  render: (v: string) => <span style={{ fontFamily: 'monospace' }}>{v}</span>,
},
```

改为：

```js
// 改后
{
  title: '料号', dataIndex: '__hfPartNo', key: '__hfPartNo',
  fixed: 'left' as const, width: 200,
  render: (v: string) => <span style={{ fontFamily: 'monospace' }}>{v}</span>,
},
```

说明：`__hfPartNo` 已在 `useLinkedExcelRows.ts:332` 赋值为 `li.productPartNo`（生产料号），必有值，不做空值兜底（已与用户确认）。`__label` 改后不再被任何代码消费（全工程已 grep 确认仅此一处），无需额外清理。

- [ ] **Step 2: TypeScript 类型检查**

Run:
```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json
```
Expected: 0 错误（无输出即通过）。

- [ ] **Step 3: Vite transform 验证（该文件可正常解析）**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/LinkedExcelView.tsx
```
Expected: `200`

- [ ] **Step 4: 跑 E2E（LinkedExcelView 属报价单渲染链路，强制）**

Run（PowerShell，按 CLAUDE.md）:
```powershell
cd cpq-frontend
npx playwright test --config=e2e/playwright.config.ts e2e/quotation-flow.spec.ts --reporter=list
```
Expected: 所有 test `passed`、控制台 `'加载中' final count = 0`。
人工/截图确认：Excel 视图第一列表头显示「料号」、单元格值为生产料号(productPartNo) 而非客户料号。

- [ ] **Step 5: 追加 RECORD.md 开发记录**

在 `docs/RECORD.md` 追加一行（格式遵循 CLAUDE.md）：
```
[2026-06-02] 报价单Excel视图 - 第一列由客户料号(__label)改为料号(生产料号/__hfPartNo) | cpq-frontend/src/pages/quotation/LinkedExcelView.tsx | 纯前端行头列改动,与模板excel_view_config无关;__hfPartNo=productPartNo必有值不做兜底
```

- [ ] **Step 6: Commit**

```bash
git add cpq-frontend/src/pages/quotation/LinkedExcelView.tsx docs/RECORD.md
git commit -m "feat(quotation): 报价单 Excel 视图第一列改为料号(生产料号)

第一列由硬编码行头 __label(客户料号优先) 改为 __hfPartNo(productPartNo),
标题 客户料号 → 料号。纯前端单文件,不涉及模板/SQL视图/迁移。"
```

---

## Self-Review

- **Spec coverage:** spec 三处要求 —— ①第一列改生产料号 → Step 1 改 `__hfPartNo`；②标题叫「料号」→ Step 1 改 `title`；③客户料号列去掉(只留一个料号列) → 第一列即原客户料号列，换源后整个视图只剩这一个料号列，满足。验证(spec §五)→ Step 2/3/4。
- **Placeholder scan:** 无 TBD/TODO；每个代码步骤含完整改前/改后代码。
- **Type consistency:** `__hfPartNo` 在 `LinkedExcelRow` 接口(`useLinkedExcelRows.ts:23`)已声明为 `__hfPartNo?: string`，与 Step 1 使用一致。
