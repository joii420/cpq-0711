# Excel 卡片引用公式 — 计划一：后端基础 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让报价单 Excel 视图新增 `CARD_FORMULA` 列：每料号用统一公式表达式，引用本料号产品卡片各页签的"结果"（页签内字段行集 + 页签小计）+ 本表其他列，支持四则/SUM_OVER 条件聚合/IF/ROUND/ABS/百分比，后端求值并落 snapshot。

**Architecture:** 复用现有 `TemplateFormulaService`（已具备 `*_OVER` 聚合、`[ref]` 解析、JEXL 含 IF/ROUND/ABS）与 `FormulaEngine`。新增 `CardDataProvider`（按"页签实例"取卡片行+小计）；在聚合取行处加"卡片源"分支；新增百分比字面量预处理；`ExcelViewService.buildRowData` 加 `CARD_FORMULA` 分支 + 列间拓扑排序求值 + 环检测；空值按"规则一"。本计划**只做后端 + 单测**，不含前端 UI（计划二）。

**Tech Stack:** Java 17 / Quarkus / Hibernate Panache；JUnit5 + Mockito（`cpq-backend/src/test`）；JEXL3（既有公式引擎）。

> 设计依据：`docs/superpowers/specs/2026-06-02-Excel视图卡片引用公式配置-design.md`。
> 强约束：本改动属"渲染层协议改动"，须遵守反模式 AP-37（同 componentId 多实例按实例定位）/ AP-44（字段类型联动）/ AP-51（行数权威）。

---

## 文件结构（先锁定边界）

| 文件 | 职责 | 动作 |
|---|---|---|
| `com/cpq/quotation/service/card/CardDataProvider.java` | 按 lineItem + 页签实例(templateComponentId 或 componentId+sortOrder) 返回该页签行集(`List<Map<String,Object>>`)与小计(`BigDecimal`)；进程内无状态 | Create |
| `com/cpq/quotation/service/card/CardRef.java` | 卡片引用值对象：`tab`(页签实例标识) / `field`(field_key 或 `__subtotal__`) / `mode`(FIRST_ROW/ROW_WHERE) / `cond` | Create |
| `com/cpq/formula/PercentLiteral.java` | 纯函数：把表达式里的百分比字面量 `12%` → `(12/100.0)`；不碰取模(本语法无取模) | Create |
| `com/cpq/template/service/CardAggregateSource.java` | 聚合"卡片源"解析：源 token → 是否卡片页签；命中则用 `CardDataProvider` 取行 | Create |
| `com/cpq/quotation/service/CardFormulaEvaluator.java` | 编排：解析一组 `CARD_FORMULA` 列 → 建列依赖图 → 拓扑排序 → 逐列求值（委托 `TemplateFormulaService` + `CardDataProvider`）→ 应用空值规则一 | Create |
| `com/cpq/quotation/service/ExcelViewService.java` | `buildRowData` 增加 `CARD_FORMULA` 分支，调 `CardFormulaEvaluator` | Modify |
| `com/cpq/template/service/TemplateFormulaService.java` | `executeOverFunction` 增加"卡片源"分支（源是页签实例时走 `CardDataProvider` 而非 `dataLoader`） | Modify |
| `src/test/java/com/cpq/quotation/card/*Test.java` | 单测 | Create |

**列配置 JSON（`template.excel_view_config` 中一列，`CARD_FORMULA`）契约：**
```json
{
  "col_key": "B",
  "title": "电镀小计",
  "source_type": "CARD_FORMULA",
  "formula": "=ROUND(([投料.小计] + SUM_OVER([加工] WHERE (c0=='电镀' || c0=='酸洗'), c1)) * 12%, 2)",
  "refs": {
    "投料.小计": { "tab": "<compId:sortOrder>", "field": "__subtotal__" },
    "加工":      { "tab": "<compId:sortOrder>", "cols": { "c0": "工序", "c1": "加工费" } }
  },
  "display_format": { "type": "PERCENT", "decimals": 2 }
}
```
- `formula` 内占位：**含 `.`** 的 `[页签.字段]` = 卡片单值引用（查 `refs`，Java 侧解析成字面量）；**裸 col_key**（无 `.`，如 `[A]`）= 本表列引用；**`[页签]`**（带方括号、无 `.`、且 refs 中该 token 是"聚合源"即有 `cols`/无 `field`）= 聚合源，交引擎 `resolveAggregates`。
- **别名层（见 spec §3）**：聚合源 ref 的 `cols` = `{别名: 中文字段名}`；公式聚合体/WHERE 只用别名 `c0/c1...`（中文不可作 JEXL 标识符）。求值时把该页签行 key 从中文重映射成别名再交 `evalRowExpression`。
- 单值引用 `field` 仍可用中文名（Java `row.get(中文名)`，不进 JEXL）。
- 页签 key 约定 `compId:sortOrder`（实体无 `templateComponentId`，已核实）。
- **WHERE/cond/聚合体存 JEXL 兼容算符**：`==`/`!=`/`>`/`<`/`>=`/`<=`、`&&`、`||`；`IN(...)` 由选择器展开为 `||`。后端 `evalRowExpression`(已有)直接吃 JEXL，**不加 SQL→JEXL 归一化**（避免影响既有 SUM_OVER）。友好"等于/且/或/包含"UI→JEXL 的序列化属**计划二选择器**。

---

### Task 1: 百分比字面量预处理 `PercentLiteral`

**Files:**
- Create: `cpq-backend/src/main/java/com/cpq/formula/PercentLiteral.java`
- Test: `cpq-backend/src/test/java/com/cpq/formula/PercentLiteralTest.java`

- [ ] **Step 1: 写失败测试**

```java
package com.cpq.formula;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

class PercentLiteralTest {
    @Test void rewrites_simple_percent() {
        assertEquals("(12/100.0)", PercentLiteral.rewrite("12%"));
    }
    @Test void rewrites_decimal_percent() {
        assertEquals("(2.5/100.0)", PercentLiteral.rewrite("2.5%"));
    }
    @Test void rewrites_inside_expression() {
        assertEquals("[A] * (12/100.0) + 1", PercentLiteral.rewrite("[A] * 12% + 1"));
    }
    @Test void leaves_plain_numbers_untouched() {
        assertEquals("12 + 3.5", PercentLiteral.rewrite("12 + 3.5"));
    }
    @Test void null_safe() {
        assertEquals(null, PercentLiteral.rewrite(null));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=PercentLiteralTest`
Expected: FAIL（`PercentLiteral` 不存在 / 编译错）

- [ ] **Step 3: 实现**

```java
package com.cpq.formula;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

/** 把百分比字面量 12% / 2.5% 重写为 (12/100.0)。本语法不提供取模，故 % 仅作百分比。 */
public final class PercentLiteral {
    private PercentLiteral() {}
    // 数字（整数或小数）紧跟 %，前面不是标识符字符
    private static final Pattern P = Pattern.compile("(\\d+(?:\\.\\d+)?)%");

    public static String rewrite(String expr) {
        if (expr == null) return null;
        Matcher m = P.matcher(expr);
        StringBuffer sb = new StringBuffer();
        while (m.find()) {
            m.appendReplacement(sb, Matcher.quoteReplacement("(" + m.group(1) + "/100.0)"));
        }
        m.appendTail(sb);
        return sb.toString();
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=PercentLiteralTest`
Expected: PASS（5 tests）

- [ ] **Step 5: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/formula/PercentLiteral.java cpq-backend/src/test/java/com/cpq/formula/PercentLiteralTest.java
git commit -m "feat(formula): 百分比字面量 12% → (12/100.0) 预处理 PercentLiteral"
```

---

### Task 2: 卡片引用值对象 `CardRef`

**Files:**
- Create: `cpq-backend/src/main/java/com/cpq/quotation/service/card/CardRef.java`
- Test: `cpq-backend/src/test/java/com/cpq/quotation/card/CardRefTest.java`

- [ ] **Step 1: 写失败测试**

```java
package com.cpq.quotation.card;

import com.cpq.quotation.service.card.CardRef;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CardRefTest {
    @Test void subtotal_ref() {
        CardRef r = CardRef.fromMap(java.util.Map.of("tab", "TC1", "field", "__subtotal__"));
        assertEquals("TC1", r.tab);
        assertTrue(r.isSubtotal());
        assertFalse(r.isAggregateSource());
    }
    @Test void field_first_row_ref() {
        CardRef r = CardRef.fromMap(java.util.Map.of("tab", "TC1", "field", "proc_fee"));
        assertEquals("proc_fee", r.field);
        assertEquals(CardRef.Mode.FIRST_ROW, r.mode);
    }
    @Test void row_where_ref() {
        CardRef r = CardRef.fromMap(java.util.Map.of(
            "tab", "TC1", "field", "proc_fee", "mode", "ROW_WHERE", "cond", "process='电镀'"));
        assertEquals(CardRef.Mode.ROW_WHERE, r.mode);
        assertEquals("process='电镀'", r.cond);
    }
    @Test void aggregate_source_ref_has_no_field() {
        CardRef r = CardRef.fromMap(java.util.Map.of("tab", "TC1"));
        assertTrue(r.isAggregateSource());
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardRefTest`
Expected: FAIL

- [ ] **Step 3: 实现**

```java
package com.cpq.quotation.service.card;

import java.util.HashMap;
import java.util.Map;

/** Excel 列公式里的卡片引用：指向某页签实例的小计 / 字段(首行或按条件取行) / 或作为聚合源(无 field)。 */
public final class CardRef {
    public enum Mode { FIRST_ROW, ROW_WHERE }
    public static final String SUBTOTAL = "__subtotal__";

    public final String tab;      // 页签实例标识（compId:sortOrder）
    public final String field;    // 中文字段名 / __subtotal__ / null(聚合源)
    public final Mode mode;
    public final String cond;     // ROW_WHERE 行筛选条件（用别名）
    public final Map<String, String> cols; // 别名→中文字段名（ROW_WHERE cond / 聚合体用；单值首行可空）

    private CardRef(String tab, String field, Mode mode, String cond, Map<String, String> cols) {
        this.tab = tab; this.field = field; this.mode = mode; this.cond = cond;
        this.cols = cols != null ? cols : Map.of();
    }

    public boolean isSubtotal() { return SUBTOTAL.equals(field); }
    public boolean isAggregateSource() { return field == null || field.isBlank(); }

    @SuppressWarnings("unchecked")
    public static CardRef fromMap(Map<String, Object> m) {
        if (m == null) return null;
        String tab = str(m.get("tab"));
        String field = str(m.get("field"));
        String modeStr = str(m.get("mode"));
        Mode mode = "ROW_WHERE".equalsIgnoreCase(modeStr) ? Mode.ROW_WHERE : Mode.FIRST_ROW;
        String cond = str(m.get("cond"));
        Map<String, String> cols = new HashMap<>();
        Object colsObj = m.get("cols");
        if (colsObj instanceof Map<?, ?> cm)
            for (Map.Entry<?, ?> e : cm.entrySet()) cols.put(e.getKey().toString(), e.getValue().toString());
        return new CardRef(tab, field, mode, cond, cols);
    }
    private static String str(Object o) { return o == null ? null : o.toString(); }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardRefTest`
Expected: PASS（4 tests）

- [ ] **Step 5: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/quotation/service/card/CardRef.java cpq-backend/src/test/java/com/cpq/quotation/card/CardRefTest.java
git commit -m "feat(quotation): 卡片引用值对象 CardRef(小计/首行/按条件取行/聚合源)"
```

---

### Task 3: 卡片数据提供器 `CardDataProvider`

**Files:**
- Create: `cpq-backend/src/main/java/com/cpq/quotation/service/card/CardDataProvider.java`
- Test: `cpq-backend/src/test/java/com/cpq/quotation/card/CardDataProviderTest.java`

**契约（实现前先读 `QuotationLineComponentData.java` 确认字段：`lineItemId / componentId / tabName / rowData(JSON 数组) / subtotal / sortOrder`，已在 spec §2 列明）：**
- 输入：`List<QuotationLineComponentData> tabsOfLineItem`（同一料号下所有页签，调用方按 `lineItemId` 查好传入，避免本类碰 DB → 可纯函数单测）。
- `rowsOf(String tabId)`：返回该页签 `rowData` 解析出的行集；无该页签 → 空 List。
- `subtotalOf(String tabId)`：返回该页签 `subtotal`；无 → null。
- AP-37/AP-40：`tabId` = `templateComponentId`（即页签实例）。**实现前确认 `QuotationLineComponentData` 是否已有 `templateComponentId` 字段**；若只有 `componentId`，则 tabId 约定为 `componentId + ":" + sortOrder`，由调用方与 refs 生成端一致（计划二选择器生成同款 key）。本类只按传入的 keyer 比对。

- [ ] **Step 1: 写失败测试**

```java
package com.cpq.quotation.card;

import com.cpq.quotation.entity.QuotationLineComponentData;
import com.cpq.quotation.service.card.CardDataProvider;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.util.*;
import static org.junit.jupiter.api.Assertions.*;

class CardDataProviderTest {
    private QuotationLineComponentData tab(String compId, int sort, String rowJson, String sub) {
        QuotationLineComponentData d = new QuotationLineComponentData();
        d.componentId = UUID.fromString(compId);
        d.sortOrder = sort;
        d.rowData = rowJson;
        d.subtotal = new BigDecimal(sub);
        return d;
    }
    @Test void rows_and_subtotal_by_tab_key() {
        var c1 = "11111111-1111-1111-1111-111111111111";
        var tabs = List.of(
            tab(c1, 0, "[{\"proc\":\"电镀\",\"fee\":3},{\"proc\":\"酸洗\",\"fee\":2}]", "5"));
        CardDataProvider p = new CardDataProvider(tabs);
        String key = c1 + ":0";
        assertEquals(2, p.rowsOf(key).size());
        assertEquals("电镀", p.rowsOf(key).get(0).get("proc"));
        assertEquals(new BigDecimal("5"), p.subtotalOf(key));
    }
    @Test void missing_tab_returns_empty_and_null() {
        CardDataProvider p = new CardDataProvider(List.of());
        assertTrue(p.rowsOf("nope:0").isEmpty());
        assertNull(p.subtotalOf("nope:0"));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardDataProviderTest`
Expected: FAIL

- [ ] **Step 3: 实现**

```java
package com.cpq.quotation.service.card;

import com.cpq.quotation.entity.QuotationLineComponentData;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.ObjectMapper;

import java.math.BigDecimal;
import java.util.*;

/**
 * 卡片数据提供器：按"页签实例 key"读该料号某页签的行集 / 小计。
 * 无状态、无 DB —— 调用方查好该料号的页签列表后构造本类（便于单测）。
 * 页签 key 约定：templateComponentId；若实体无此字段则用 componentId + ":" + sortOrder（与 refs 生成端一致）。
 */
public final class CardDataProvider {
    private static final ObjectMapper MAPPER = new ObjectMapper();
    private final Map<String, QuotationLineComponentData> byTab = new HashMap<>();

    public CardDataProvider(List<QuotationLineComponentData> tabs) {
        if (tabs != null) for (QuotationLineComponentData d : tabs) byTab.put(keyOf(d), d);
    }

    /** 与 refs 生成端必须一致的 key 规则。 */
    public static String keyOf(QuotationLineComponentData d) {
        return d.componentId + ":" + (d.sortOrder == null ? 0 : d.sortOrder);
    }

    public List<Map<String, Object>> rowsOf(String tabKey) {
        QuotationLineComponentData d = byTab.get(tabKey);
        if (d == null || d.rowData == null || d.rowData.isBlank()) return List.of();
        try {
            List<Map<String, Object>> rows =
                MAPPER.readValue(d.rowData, new TypeReference<List<Map<String, Object>>>() {});
            return rows != null ? rows : List.of();
        } catch (Exception e) {
            return List.of();
        }
    }

    public BigDecimal subtotalOf(String tabKey) {
        QuotationLineComponentData d = byTab.get(tabKey);
        return d == null ? null : d.subtotal;
    }

    public boolean hasTab(String tabKey) { return byTab.containsKey(tabKey); }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardDataProviderTest`
Expected: PASS（2 tests）

- [ ] **Step 5: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/quotation/service/card/CardDataProvider.java cpq-backend/src/test/java/com/cpq/quotation/card/CardDataProviderTest.java
git commit -m "feat(quotation): CardDataProvider 按页签实例读卡片行集/小计(无DB,可单测)"
```

---

### Task 4: 列依赖图 + 拓扑排序 + 环检测（`CardFormulaEvaluator` 的纯逻辑部分）

**Files:**
- Create: `cpq-backend/src/main/java/com/cpq/quotation/service/CardFormulaEvaluator.java`（先只放静态工具 `topoOrder`）
- Test: `cpq-backend/src/test/java/com/cpq/quotation/card/CardFormulaTopoTest.java`

**契约：** 解析每列 `formula` 里的**裸 col_key 占位**（不含 `.` 的 `[X]`）作为列间依赖；返回拓扑序；有环抛 `BusinessException(400, "Excel 列公式存在循环引用: ...")`。卡片占位（含 `.`）不算列依赖。

- [ ] **Step 1: 写失败测试**

```java
package com.cpq.quotation.card;

import com.cpq.common.exception.BusinessException;
import com.cpq.quotation.service.CardFormulaEvaluator;
import org.junit.jupiter.api.Test;
import java.util.*;
import static org.junit.jupiter.api.Assertions.*;

class CardFormulaTopoTest {
    @Test void orders_b_after_a() {
        Map<String,String> f = new LinkedHashMap<>();
        f.put("B", "=[A] * 1.13");
        f.put("A", "=SUM_OVER(加工 WHERE 工序='电镀', 加工费)");
        List<String> order = CardFormulaEvaluator.topoOrder(f);
        assertTrue(order.indexOf("A") < order.indexOf("B"));
    }
    @Test void card_ref_with_dot_is_not_a_column_dep() {
        Map<String,String> f = new LinkedHashMap<>();
        f.put("A", "=[投料.小计] + 1");   // 含 . → 卡片引用,非列依赖
        List<String> order = CardFormulaEvaluator.topoOrder(f);
        assertEquals(List.of("A"), order);
    }
    @Test void detects_cycle() {
        Map<String,String> f = new LinkedHashMap<>();
        f.put("A", "=[B] + 1");
        f.put("B", "=[A] + 1");
        assertThrows(BusinessException.class, () -> CardFormulaEvaluator.topoOrder(f));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardFormulaTopoTest`
Expected: FAIL

- [ ] **Step 3: 实现（先只放 topoOrder 静态方法 + BRACKET 提取）**

```java
package com.cpq.quotation.service;

import com.cpq.common.exception.BusinessException;
import java.util.*;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

/** Excel CARD_FORMULA 列编排：列依赖拓扑排序 + 环检测（求值编排在后续 Task 注入依赖完成）。 */
public class CardFormulaEvaluator {

    private static final Pattern BRACKET = Pattern.compile("\\[([^\\[\\]]+)]");

    /** 提取公式里"裸 col_key"（不含 . 的占位）作为列间依赖。 */
    static Set<String> columnDeps(String formula, Set<String> allCols) {
        Set<String> deps = new LinkedHashSet<>();
        if (formula == null) return deps;
        Matcher m = BRACKET.matcher(formula);
        while (m.find()) {
            String ref = m.group(1).trim();
            if (!ref.contains(".") && allCols.contains(ref)) deps.add(ref);
        }
        return deps;
    }

    /** Kahn 拓扑排序；存在环抛 BusinessException。入参: col_key → formula。 */
    public static List<String> topoOrder(Map<String, String> formulas) {
        Set<String> cols = formulas.keySet();
        Map<String, Set<String>> deps = new LinkedHashMap<>();
        Map<String, Integer> indeg = new LinkedHashMap<>();
        for (String c : cols) { deps.put(c, columnDeps(formulas.get(c), cols)); indeg.put(c, 0); }
        for (String c : cols) for (String d : deps.get(c)) indeg.merge(c, 0, Integer::sum); // ensure key
        // indegree = 本列依赖数（被依赖者先出）
        for (String c : cols) indeg.put(c, deps.get(c).size());
        Deque<String> q = new ArrayDeque<>();
        for (String c : cols) if (indeg.get(c) == 0) q.add(c);
        List<String> order = new ArrayList<>();
        while (!q.isEmpty()) {
            String c = q.poll();
            order.add(c);
            for (String other : cols) {
                if (deps.get(other).contains(c)) {
                    indeg.put(other, indeg.get(other) - 1);
                    if (indeg.get(other) == 0) q.add(other);
                }
            }
        }
        if (order.size() != cols.size()) {
            Set<String> cyc = new LinkedHashSet<>(cols);
            cyc.removeAll(order);
            throw new BusinessException(400, "Excel 列公式存在循环引用: " + cyc);
        }
        return order;
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardFormulaTopoTest`
Expected: PASS（3 tests）

- [ ] **Step 5: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/quotation/service/CardFormulaEvaluator.java cpq-backend/src/test/java/com/cpq/quotation/card/CardFormulaTopoTest.java
git commit -m "feat(quotation): Excel 列公式列间依赖拓扑排序 + 环检测"
```

---

### Task 5: 聚合"卡片源"分支接入 `TemplateFormulaService.executeOverFunction`

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/template/service/TemplateFormulaService.java`（`executeOverFunction` 取行处，约 696-710 行 `dataLoader.loadByPath` 一段）
- Create: `cpq-backend/src/main/java/com/cpq/template/service/CardAggregateSource.java`（ThreadLocal 持有当前料号卡片 provider + refs，求值期内有效）
- Test: `cpq-backend/src/test/java/com/cpq/quotation/card/CardAggregateTest.java`

**实现前先读** `executeOverFunction`(本 spec 已贴) 与 `evalRowExpression`/`aggregate`/`isTruthy`，确认行内表达式按 `row` map 的裸字段名求值（卡片行同形态可复用）。

**思路（最小侵入）：** 聚合源 token（如 `加工`）经 `CardAggregateSource` ThreadLocal 命中页签实例时，**用卡片行替代 `dataLoader.loadByPath` 结果**，其余（WHERE 过滤 / 行内表达式 / aggregate）完全复用现有逻辑。

- [ ] **Step 1: 写 `CardAggregateSource`（ThreadLocal 上下文）**

```java
package com.cpq.template.service;

import com.cpq.quotation.service.card.CardDataProvider;
import java.util.*;

/** 求值期 ThreadLocal：当前料号卡片 provider + 聚合源token→{页签key + 别名映射}。由 Excel 求值入口 set/clear。 */
public final class CardAggregateSource {
    /** 一个聚合源的绑定：页签 key + 别名→中文字段名（仅该聚合体/WHERE 用到的字段）。 */
    public static final class Binding {
        public final String tabKey;
        public final Map<String, String> aliasToField; // c0 → 工序
        public Binding(String tabKey, Map<String, String> aliasToField) {
            this.tabKey = tabKey; this.aliasToField = aliasToField != null ? aliasToField : Map.of();
        }
    }
    public static final class Ctx {
        public final CardDataProvider provider;
        public final Map<String, Binding> sourceToken; // 如 "加工" → Binding
        public Ctx(CardDataProvider p, Map<String, Binding> map) { this.provider = p; this.sourceToken = map; }
    }
    private static final ThreadLocal<Ctx> TL = new ThreadLocal<>();
    public static void set(Ctx c) { TL.set(c); }
    public static void clear() { TL.remove(); }
    public static Ctx get() { return TL.get(); }

    /**
     * 命中卡片源 → 返回**已按别名重映射 key** 的行集（c0/c1...→值），供 evalRowExpression(JEXL) 用；
     * 否则 null（调用方回退原 dataLoader 路径）。
     */
    public static List<Map<String, Object>> rowsFor(String sourceToken) {
        Ctx c = TL.get();
        if (c == null || sourceToken == null) return null;
        Binding b = c.sourceToken.get(sourceToken.trim());
        if (b == null) return null;
        List<Map<String, Object>> raw = c.provider.rowsOf(b.tabKey);
        List<Map<String, Object>> out = new ArrayList<>(raw.size());
        for (Map<String, Object> row : raw) {
            Map<String, Object> aliased = new HashMap<>();
            for (Map.Entry<String, String> e : b.aliasToField.entrySet()) {
                aliased.put(e.getKey(), row.get(e.getValue())); // 别名 → 中文字段值
            }
            out.add(aliased);
        }
        return out;
    }
}
```

- [ ] **Step 2: 写失败测试（聚合走卡片源 + 条件求和 + 行内表达式）**

```java
package com.cpq.quotation.card;

import com.cpq.quotation.entity.QuotationLineComponentData;
import com.cpq.quotation.service.card.CardDataProvider;
import com.cpq.template.service.CardAggregateSource;
import com.cpq.template.service.TemplateFormulaService;
import io.quarkus.test.junit.QuarkusTest;
import jakarta.inject.Inject;
import org.junit.jupiter.api.*;
import java.math.BigDecimal;
import java.util.*;
import static org.junit.jupiter.api.Assertions.*;

@QuarkusTest
class CardAggregateTest {
    @Inject TemplateFormulaService svc;

    private QuotationLineComponentData tab(String comp, int sort, String json, String sub) {
        var d = new QuotationLineComponentData();
        d.componentId = UUID.fromString(comp); d.sortOrder = sort; d.rowData = json; d.subtotal = new BigDecimal(sub);
        return d;
    }

    @AfterEach void tearDown() { CardAggregateSource.clear(); }

    @Test void sum_over_card_with_condition_and_inline_expr() {
        var comp = "22222222-2222-2222-2222-222222222222";
        var tabs = List.of(tab(comp, 0,
            "[{\"工序\":\"电镀\",\"单价\":3,\"数量\":2},{\"工序\":\"酸洗\",\"单价\":5,\"数量\":1},{\"工序\":\"电镀\",\"单价\":4,\"数量\":1}]",
            "0"));
        // 别名层：c0=工序, c1=单价, c2=数量；公式用别名，源带方括号 [加工]
        CardAggregateSource.set(new CardAggregateSource.Ctx(
            new CardDataProvider(tabs),
            Map.of("加工", new CardAggregateSource.Binding(comp + ":0",
                Map.of("c0", "工序", "c1", "单价", "c2", "数量")))));
        // 电镀行: 3*2 + 4*1 = 10
        Object r = svc.evaluateExpressionPublic(
            "SUM_OVER([加工] WHERE c0=='电镀', c1*c2)",   // JEXL 算符(== 非 =)；选择器负责生成,见 spec §3
            Map.of(), new HashMap<>(), null, "P1", List.of());
        assertEquals(0, new BigDecimal("10").compareTo(new BigDecimal(r.toString())));
    }
}
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardAggregateTest`
Expected: FAIL（聚合仍走 dataLoader，卡片源未接入 → 返回 0）

- [ ] **Step 4: 在 `executeOverFunction` 取行处加卡片源分支**

定位 `executeOverFunction` 中（本 spec 已贴）这段：
```java
            // 查 driver rows
            ...
            List<Map<String, Object>> rows;
            try {
                rows = dataLoader.loadByPath(driverPath, null, partNo, customerId).get();
            } catch ...
```
改为（在 driverPath 解析**之前**先试卡片源）：
```java
            // 卡片源优先：Excel CARD_FORMULA 求值期，源 token 命中页签实例 → 用卡片行
            List<Map<String, Object>> rows = com.cpq.template.service.CardAggregateSource.rowsFor(parsed.source);
            if (rows == null) {
                String driverPath = resolveDriverPath(parsed.source);
                if (driverPath == null || driverPath.isBlank()) {
                    LOG.warnf("[Stage2] %s: cannot resolve driver path for source '%s'", funcName, parsed.source);
                    return BigDecimal.ZERO;
                }
                try {
                    rows = dataLoader.loadByPath(driverPath, null, partNo, customerId).get();
                } catch (InterruptedException | ExecutionException e) {
                    LOG.warnf("[Stage2] %s: driver query failed: %s", funcName, e.getMessage());
                    return BigDecimal.ZERO;
                }
            }
            if (rows == null) rows = List.of();
```
（保留其后的 WHERE 过滤 / `evalRowExpression` / `aggregate` 不变。`aggregate("SUM_OVER", 空)` 须返回 0 —— 验证 §5 规则一 B；若现状返回 null 需在 `aggregate` 内对 SUM 空集补 0。）

- [ ] **Step 5: 跑测试确认通过**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardAggregateTest`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/template/service/CardAggregateSource.java cpq-backend/src/main/java/com/cpq/template/service/TemplateFormulaService.java cpq-backend/src/test/java/com/cpq/quotation/card/CardAggregateTest.java
git commit -m "feat(formula): SUM_OVER 等聚合支持卡片源(页签行)，复用 WHERE/行内表达式/聚合逻辑"
```

---

### Task 6: `CardFormulaEvaluator` 整列编排（卡片单值引用 + 列引列 + 百分比 + 空值规则一）

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/quotation/service/CardFormulaEvaluator.java`（加注入 + `evaluateColumns`）
- Test: `cpq-backend/src/test/java/com/cpq/quotation/card/CardFormulaEvaluatorTest.java`

**契约 `evaluateColumns`：**
- 入参：`columns`(本料号要算的 CARD_FORMULA 列：col_key/formula/refs)、`tabs`(本料号页签列表)。
- 步骤：① 建 `CardDataProvider`；② 拓扑序；③ 逐列：先把卡片单值占位 `[页签.字段]`/`[页签.小计]` 按 refs+provider 解析成字面量或空哨兵；设 `CardAggregateSource`（源 token→tabKey 来自 refs 中 `isAggregateSource` 的项）；`PercentLiteral.rewrite` 处理 `%`；再交 `TemplateFormulaService.evaluateExpressionPublic`（裸 col_key 用 cached 已算列值）；④ 空值规则一：纯单值列空→「—」；公式含引用全空→「—」；部分空→当 0（解析占位时空→"0"，并记录是否全空）。
- 返回：`Map<col_key, Object>`（值或字符串 "—"）。

- [ ] **Step 1: 写失败测试**

```java
package com.cpq.quotation.card;

import com.cpq.quotation.entity.QuotationLineComponentData;
import com.cpq.quotation.service.CardFormulaEvaluator;
import io.quarkus.test.junit.QuarkusTest;
import jakarta.inject.Inject;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.util.*;
import static org.junit.jupiter.api.Assertions.*;

@QuarkusTest
class CardFormulaEvaluatorTest {
    @Inject CardFormulaEvaluator evaluator;

    private QuotationLineComponentData tab(String comp, int sort, String json, String sub) {
        var d = new QuotationLineComponentData();
        d.componentId = UUID.fromString(comp); d.sortOrder = sort; d.rowData = json; d.subtotal = new BigDecimal(sub);
        return d;
    }
    private Map<String,Object> col(String key, String formula, Map<String,Object> refs) {
        return new LinkedHashMap<>(Map.of("col_key", key, "source_type", "CARD_FORMULA",
                "formula", formula, "refs", refs));
    }

    @Test void subtotal_ref_plus_aggregate_then_column_ref() {
        var comp = "33333333-3333-3333-3333-333333333333";
        var tabs = List.of(tab(comp, 0,
            "[{\"工序\":\"电镀\",\"加工费\":3},{\"工序\":\"酸洗\",\"加工费\":2}]", "10"));
        var refsA = Map.<String,Object>of(
            "投料.小计", Map.of("tab", comp+":0", "field", "__subtotal__"),
            "加工", Map.of("tab", comp+":0", "cols", Map.of("c0","工序","c1","加工费")));
        var cols = List.of(
            col("A", "=[投料.小计] + SUM_OVER([加工] WHERE c0=='电镀', c1)", refsA), // 10 + 3 = 13
            col("B", "=[A] * 2", Map.of())                                            // 26
        );
        Map<String,Object> out = evaluator.evaluateColumns(cols, tabs, null, "P1", null);
        assertEquals(0, new BigDecimal("13").compareTo(new BigDecimal(out.get("A").toString())));
        assertEquals(0, new BigDecimal("26").compareTo(new BigDecimal(out.get("B").toString())));
    }

    @Test void single_value_empty_shows_dash() {
        var comp = "44444444-4444-4444-4444-444444444444";
        var tabs = List.of(tab(comp, 0, "[]", "0")); // 0 行
        var refs = Map.<String,Object>of("加工.加工费", Map.of("tab", comp+":0", "field", "加工费"));
        var cols = List.of(col("A", "=[加工.加工费]", refs));
        Map<String,Object> out = evaluator.evaluateColumns(cols, tabs, null, "P1", null);
        assertEquals("—", out.get("A"));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardFormulaEvaluatorTest`
Expected: FAIL

- [ ] **Step 3: 实现 `evaluateColumns`（在 Task4 文件追加）**

```java
    @jakarta.inject.Inject com.cpq.template.service.TemplateFormulaService templateFormulaService;
    private static final com.fasterxml.jackson.databind.ObjectMapper M = new com.fasterxml.jackson.databind.ObjectMapper();
    public static final String DASH = "—";

    /** 算本料号一组 CARD_FORMULA 列。columns: 每项含 col_key/formula/refs。 */
    public java.util.Map<String, Object> evaluateColumns(
            java.util.List<java.util.Map<String, Object>> columns,
            java.util.List<com.cpq.quotation.entity.QuotationLineComponentData> tabs,
            java.util.UUID customerId, String partNo, java.util.UUID quotationId) {

        com.cpq.quotation.service.card.CardDataProvider provider =
                new com.cpq.quotation.service.card.CardDataProvider(tabs);

        java.util.Map<String, String> formulaByCol = new java.util.LinkedHashMap<>();
        java.util.Map<String, java.util.Map<String, Object>> refsByCol = new java.util.HashMap<>();
        for (var c : columns) {
            String key = (String) c.get("col_key");
            formulaByCol.put(key, stripEq((String) c.get("formula")));
            refsByCol.put(key, asRefMap(c.get("refs")));
        }

        java.util.List<String> order = topoOrder(formulaByCol);
        java.util.Map<String, Object> cached = new java.util.LinkedHashMap<>();
        java.util.Map<String, Object> out = new java.util.LinkedHashMap<>();

        for (String col : order) {
            String formula = formulaByCol.get(col);
            java.util.Map<String, Object> refs = refsByCol.get(col);

            // 1) 卡片单值占位 [页签.字段]/[页签.小计] → 字面量；统计是否全空；登记聚合源 Binding
            boolean[] anyNonEmpty = {false}; boolean[] anyRef = {false};
            java.util.Map<String, com.cpq.template.service.CardAggregateSource.Binding> aggBindings = new java.util.HashMap<>();
            String resolved = resolveCardScalars(formula, refs, provider, anyNonEmpty, anyRef, aggBindings);

            // 2) 百分比字面量
            resolved = com.cpq.formula.PercentLiteral.rewrite(resolved);

            // 3) 设聚合卡片源上下文
            com.cpq.template.service.CardAggregateSource.set(
                new com.cpq.template.service.CardAggregateSource.Ctx(provider, aggBindings));
            Object val;
            try {
                val = templateFormulaService.evaluateExpressionPublic(
                        resolved, java.util.Map.of(), cached, customerId, partNo, java.util.List.of());
            } finally {
                com.cpq.template.service.CardAggregateSource.clear();
            }

            // 4) 空值规则一：有卡片单值引用、且全为空、且无聚合 → 「—」；
            //    （聚合空集→0 由引擎处理，故 hasAggregate 时不判 —；部分空→已当 0 续算）
            Object finalVal = (anyRef[0] && !anyNonEmpty[0] && !hasAggregate(formula)) ? DASH : val;

            out.put(col, finalVal);
            cached.put(col, (finalVal == null || DASH.equals(finalVal)) ? null : finalVal);
        }
        return out;
    }

    private static String stripEq(String f) {
        if (f == null) return "";
        f = f.trim();
        return f.startsWith("=") ? f.substring(1).trim() : f;
    }
    @SuppressWarnings("unchecked")
    private static java.util.Map<String, Object> asRefMap(Object o) {
        return (o instanceof java.util.Map) ? (java.util.Map<String, Object>) o : java.util.Map.of();
    }
    private static boolean hasAggregate(String f) {
        return f != null && f.toUpperCase().matches(".*\\b(SUM|COUNT|AVG|MIN|MAX)_OVER\\b.*");
    }

    /**
     * 处理 [..] 占位：
     *  - 聚合源（refs 中 isAggregateSource，对应公式里的 [页签]）→ **保持原样**（交引擎 resolveAggregates），并登记 Binding(tabKey+cols 别名)。
     *  - 卡片单值（含 . 的 [页签.字段] / [页签.小计]）→ 解析成字面量；统计是否全空。
     *  - 其余（裸 col_key 等）→ 保持原样交引擎（列引列走 cached）。
     */
    @SuppressWarnings("unchecked")
    private String resolveCardScalars(String formula, java.util.Map<String, Object> refs,
            com.cpq.quotation.service.card.CardDataProvider provider,
            boolean[] anyNonEmpty, boolean[] anyRef,
            java.util.Map<String, com.cpq.template.service.CardAggregateSource.Binding> aggBindings) {
        // 先登记所有聚合源 Binding（便于占位扫描时跳过）
        java.util.Set<String> aggTokens = new java.util.HashSet<>();
        for (var e : refs.entrySet()) {
            com.cpq.quotation.service.card.CardRef r =
                    com.cpq.quotation.service.card.CardRef.fromMap(asRefMap(e.getValue()));
            if (r != null && r.isAggregateSource()) {
                java.util.Map<String, Object> rm = asRefMap(e.getValue());
                Object colsObj = rm.get("cols");
                java.util.Map<String, String> aliasToField = new java.util.HashMap<>();
                if (colsObj instanceof java.util.Map<?, ?> cm)
                    for (var ce : cm.entrySet()) aliasToField.put(ce.getKey().toString(), ce.getValue().toString());
                aggBindings.put(e.getKey(), new com.cpq.template.service.CardAggregateSource.Binding(r.tab, aliasToField));
                aggTokens.add(e.getKey());
            }
        }
        java.util.regex.Matcher m = java.util.regex.Pattern.compile("\\[([^\\[\\]]+)]").matcher(formula);
        StringBuffer sb = new StringBuffer();
        while (m.find()) {
            String token = m.group(1).trim();
            if (aggTokens.contains(token)) { // 聚合源 [页签] 原样保留
                m.appendReplacement(sb, java.util.regex.Matcher.quoteReplacement("[" + token + "]"));
                continue;
            }
            Object refObj = refs.get(token);
            if (refObj == null) { // 裸 col_key 或未知 → 原样交引擎
                m.appendReplacement(sb, java.util.regex.Matcher.quoteReplacement("[" + token + "]"));
                continue;
            }
            com.cpq.quotation.service.card.CardRef ref =
                    com.cpq.quotation.service.card.CardRef.fromMap(asRefMap(refObj));
            anyRef[0] = true;
            String literal;
            if (ref.isSubtotal()) {
                java.math.BigDecimal s = provider.subtotalOf(ref.tab);
                if (s != null) anyNonEmpty[0] = true;
                literal = s != null ? s.toPlainString() : "0";
            } else {
                Object v = pickFieldValue(provider, ref);
                if (v != null && !"".equals(v)) anyNonEmpty[0] = true;
                literal = toNum(v);
            }
            m.appendReplacement(sb, java.util.regex.Matcher.quoteReplacement(literal));
        }
        m.appendTail(sb);
        return sb.toString();
    }

    private static Object pickFieldValue(com.cpq.quotation.service.card.CardDataProvider provider,
            com.cpq.quotation.service.card.CardRef ref) {
        java.util.List<java.util.Map<String, Object>> rows = provider.rowsOf(ref.tab);
        if (rows.isEmpty()) return null;
        if (ref.mode == com.cpq.quotation.service.card.CardRef.Mode.ROW_WHERE && ref.cond != null) {
            // 复用引擎按行求值条件：交给 TemplateFormulaService 的 evalRowExpression 不可见，这里做等值简版交 Task7 增强
            for (var row : rows) {
                Object cv = row.get(ref.field);
                // 简化：ROW_WHERE 完整条件求值在 Task7 接 evalRowExpression；此处先取首个匹配 field 非空
                if (cv != null) return cv;
            }
            return null;
        }
        return rows.get(0).get(ref.field); // FIRST_ROW
    }
    private static String toNum(Object v) {
        if (v == null) return "0";
        if (v instanceof Number) return v.toString();
        try { return new java.math.BigDecimal(v.toString()).toPlainString(); } catch (Exception e) { return "0"; }
    }
```

> 注：`@QuarkusTest` 需 `CardFormulaEvaluator` 为 CDI bean → 类上加 `@jakarta.enterprise.context.ApplicationScoped`。

- [ ] **Step 4: 给类加 `@ApplicationScoped` 并跑测试**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardFormulaEvaluatorTest`
Expected: PASS（2 tests）

- [ ] **Step 5: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/quotation/service/CardFormulaEvaluator.java cpq-backend/src/test/java/com/cpq/quotation/card/CardFormulaEvaluatorTest.java
git commit -m "feat(quotation): CardFormulaEvaluator 卡片单值/列引列/百分比/空值规则一 编排"
```

---

### Task 7: ROW_WHERE 完整条件取行（接 `evalRowExpression`）

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/template/service/TemplateFormulaService.java`（把 `evalRowExpression` + `isTruthy` 提为 `public`，或新增 `public Map<String,Object> firstRowWhere(rows, cond)`）
- Modify: `cpq-backend/src/main/java/com/cpq/quotation/service/CardFormulaEvaluator.java`（`pickFieldValue` 的 ROW_WHERE 调用引擎条件求值）
- Test: `cpq-backend/src/test/java/com/cpq/quotation/card/CardRowWhereTest.java`

**实现前先读** `evalRowExpression` / `isTruthy` 当前可见性与签名。

- [ ] **Step 1: 写失败测试**

```java
package com.cpq.quotation.card;

import com.cpq.quotation.entity.QuotationLineComponentData;
import com.cpq.quotation.service.CardFormulaEvaluator;
import io.quarkus.test.junit.QuarkusTest;
import jakarta.inject.Inject;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.util.*;
import static org.junit.jupiter.api.Assertions.*;

@QuarkusTest
class CardRowWhereTest {
    @Inject CardFormulaEvaluator evaluator;
    @Test void picks_field_from_row_matching_condition() {
        var comp = "55555555-5555-5555-5555-555555555555";
        var d = new QuotationLineComponentData();
        d.componentId = UUID.fromString(comp); d.sortOrder = 0; d.subtotal = BigDecimal.ZERO;
        d.rowData = "[{\"工序\":\"酸洗\",\"加工费\":2},{\"工序\":\"电镀\",\"加工费\":9}]";
        var refs = Map.<String,Object>of("加工.加工费(工序=电镀)",
            Map.of("tab", comp+":0", "field", "加工费", "mode", "ROW_WHERE",
                   "cond", "c0=='电镀'", "cols", Map.of("c0","工序")));
        var cols = List.of(new LinkedHashMap<String,Object>(Map.of(
            "col_key","A","source_type","CARD_FORMULA","formula","=[加工.加工费(工序=电镀)]","refs",refs)));
        var out = evaluator.evaluateColumns(cols, List.of(d), null, "P1", null);
        assertEquals(0, new BigDecimal("9").compareTo(new BigDecimal(out.get("A").toString())));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**（当前 ROW_WHERE 简版取首个非空 → 返回 2，断言 9 失败）

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardRowWhereTest`
Expected: FAIL（得到 2 ≠ 9）

- [ ] **Step 3: 实现**：`TemplateFormulaService` 暴露"按别名条件求首个命中行下标"（供 Excel 卡片 ROW_WHERE）：

```java
    /** 按别名条件 cond 在 aliasedRows 中找首个命中行下标；无命中返回 -1。cond 为空返回 0(首行)。 */
    public int firstMatchIndex(java.util.List<java.util.Map<String, Object>> aliasedRows, String cond) {
        if (aliasedRows == null || aliasedRows.isEmpty()) return -1;
        if (cond == null || cond.isBlank()) return 0;
        for (int i = 0; i < aliasedRows.size(); i++) {
            if (isTruthy(evalRowExpression(cond, aliasedRows.get(i)))) return i;
        }
        return -1;
    }
```
`CardFormulaEvaluator.pickFieldValue` 的 ROW_WHERE 分支改为（按别名匹配行，取**原始中文字段**值）：
```java
        if (ref.mode == CardRef.Mode.ROW_WHERE && ref.cond != null && !ref.cond.isBlank()) {
            // 用 ref.cols 把行重映射成别名行供条件求值
            java.util.List<java.util.Map<String, Object>> aliased = new java.util.ArrayList<>(rows.size());
            for (var row : rows) {
                java.util.Map<String, Object> a = new java.util.HashMap<>();
                for (var ce : ref.cols.entrySet()) a.put(ce.getKey(), row.get(ce.getValue()));
                aliased.add(a);
            }
            int idx = templateFormulaService.firstMatchIndex(aliased, ref.cond);
            return idx < 0 ? null : rows.get(idx).get(ref.field); // 原始行的中文字段值
        }
```
> 实现前确认 `evalRowExpression` / `isTruthy` 当前可见性；`isTruthy` 为 private 时本方法 `firstMatchIndex` 同类内可直接调，无需改其可见性。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=CardRowWhereTest`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/template/service/TemplateFormulaService.java cpq-backend/src/main/java/com/cpq/quotation/service/CardFormulaEvaluator.java cpq-backend/src/test/java/com/cpq/quotation/card/CardRowWhereTest.java
git commit -m "feat(quotation): 卡片 ROW_WHERE 按完整条件取行(复用 evalRowExpression)"
```

---

### Task 8: `ExcelViewService.buildRowData` 接入 `CARD_FORMULA` 分支

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/quotation/service/ExcelViewService.java`（`buildRowData` switch 增加 `CARD_FORMULA`；本料号页签列表查询）
- Test: `cpq-backend/src/test/java/com/cpq/quotation/card/ExcelViewCardFormulaIT.java`（@QuarkusTest 集成，构造一条 quotation_line_item + 两个页签 + 一个 CARD_FORMULA 模板列，断言 buildLineRowData 输出）

**实现要点：**
- `buildRowData` 当前逐列 switch（本 spec 已贴）。`CARD_FORMULA` 列**不能逐列独立算**（要拓扑+跨列），所以：在 `buildRowData` 入口**先收集所有 `CARD_FORMULA` 列**，一次性调 `cardFormulaEvaluator.evaluateColumns(...)`（传 `QuotationLineComponentData.list("lineItemId=?1 ORDER BY sortOrder", li.id)`），把结果并入 row；switch 里 `CARD_FORMULA` 分支取预算好的值。
- 与旧 `VARIABLE/FORMULA` 列并存：旧列照走原逻辑（Q7/Q9：不动）。

- [ ] **Step 1: 写失败集成测试**（构造数据 → 调 `excelViewService.buildLineRowData(li, templateId, customerId)` → 断言 CARD_FORMULA 列值）

```java
// 见 Task 6/7 模式：@QuarkusTest + @Transactional 造数；断言 row.get("A") 等于预期。
// （完整造数代码在实现时按现有实体字段补全；核心断言：CARD_FORMULA 列出数、列引列正确、空值为 —）
```
> 注：此 IT 需落库造数，实现时参照现有 `*ResourceTest` 的 `@Transactional` 造数范式补全实体必填字段。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=ExcelViewCardFormulaIT`
Expected: FAIL（`CARD_FORMULA` 分支未实现 → 列值 null）

- [ ] **Step 3: 实现 buildRowData 接入**

在 `buildRowData` 内、列循环**之前**加：
```java
        // CARD_FORMULA：批量拓扑求值（不能逐列独立算）
        Map<String, Object> cardFormulaValues = java.util.Collections.emptyMap();
        List<Map<String, Object>> cardCols = new ArrayList<>();
        for (Map<String, Object> col : columns)
            if ("CARD_FORMULA".equals(col.get("source_type"))) cardCols.add(col);
        if (!cardCols.isEmpty()) {
            cardFormulaValues = cardFormulaEvaluator.evaluateColumns(
                cardCols, componentDataList, quotationCustomerId, partNo, null);
        }
```
（`componentDataList` 已在方法内查出；`partNo` 已有。）在列 switch 增加分支：
```java
                case "CARD_FORMULA" -> cardFormulaValues.get(colKey);
```
并 `@Inject CardFormulaEvaluator cardFormulaEvaluator;`。

- [ ] **Step 4: 跑测试确认通过 + 全量后端单测**

Run:
```bash
cd cpq-backend && ./mvnw -q test -Dtest=ExcelViewCardFormulaIT
./mvnw -q test -Dtest='Card*Test,PercentLiteralTest,CardAggregateTest,CardFormulaEvaluatorTest,CardRowWhereTest'
```
Expected: PASS

- [ ] **Step 5: Quarkus 启动自检（触发重启 + 健康）**

Run:
```bash
cd cpq-backend && touch src/main/java/com/cpq/quotation/service/ExcelViewService.java
sleep 7
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/formulas/batch-evaluate   # 期望 401(鉴权正常,非500)
```
Expected: 401（不是 500）

- [ ] **Step 6: 追加 RECORD.md + Commit**

`docs/RECORD.md` 追加：
```
[2026-06-02] Excel卡片引用公式(后端) - 新增 CARD_FORMULA 列来源:引用产品卡片页签结果(行集+小计)+本表列+SUM_OVER条件聚合+IF/ROUND/ABS/百分比,后端拓扑求值+环检测+空值规则一 | CardDataProvider/CardRef/CardFormulaEvaluator/CardAggregateSource/PercentLiteral + TemplateFormulaService.executeOverFunction卡片源分支 + ExcelViewService.buildRowData分支 | 与旧 $view.col 并存(只做今后);AP-37按页签实例定位;前端选择器/渲染见计划二
```
```bash
git add cpq-backend/src/main/java/com/cpq/quotation/service/ExcelViewService.java cpq-backend/src/test/java/com/cpq/quotation/card/ExcelViewCardFormulaIT.java docs/RECORD.md
git commit -m "feat(quotation): ExcelViewService 接入 CARD_FORMULA 列(批量拓扑求值)"
```

---

## Self-Review（计划 vs spec）

**1. Spec 覆盖：**
- §1 D 全粒度：小计(Task6 subtotal)/单值首行(Task6)/按条件取行(Task7)/跨行聚合(Task5) ✅
- §1 条件求和 SUMIF：Task5（WHERE 复用）✅
- §3 可视化选择器 / refs 友好占位：refs 解析在 Task6（选择器 UI 属**计划二**）✅
- §4 函数：四则/聚合(Task5)、IF/ROUND/ABS(FormulaEngine 已有，Task5/6 透传)、百分比(Task1)、列引列(Task4/6)、WHERE AND/OR/IN(复用 evalRowExpression，Task5/7) — ⚠️ **IN/AND/OR 依赖现有 `evalRowExpression` 能力**，Task5 实现前须验证；若不足，需在 Task5 补 `evalRowExpression` 表达式能力（已在风险列出）
- §5 空值规则一：Task6（单值/全空→—，部分空→0，聚合空→0）✅
- §6 后端求值 + 拓扑 + 环检测：Task4/6/8 ✅
- §7 CARD_FORMULA 与旧并存：Task8（旧列不动）✅
- §8 合计行不做：本计划无合计行 ✅
- §9 风险 AP-37(页签实例 key)/AP-51(行数权威，按 provider 实际行)：Task3/5 ✅

**2. 占位符扫描：** Task8 Step1 的 IT 造数代码为"按现有实体补全"——这是**唯一**未给全代码处，因落库造数依赖实体必填字段，已显式标注让实现者参照现有 `*ResourceTest` 范式；其余步骤均含完整代码。

**3. 类型/签名一致：** `CardDataProvider.keyOf`=`componentId:sortOrder` 全计划一致；`CardRef.fromMap`、`CardAggregateSource.Ctx`、`evaluateExpressionPublic(expr,byName,cached,customerId,partNo,viewCols)`(与现有签名一致)、`evaluateColumns(columns,tabs,customerId,partNo,quotationId)` 前后一致 ✅

**4. 待实现期验证的现有内部（计划已标"实现前先读"）：** `executeOverFunction` 取行段行号、`evalRowExpression`/`isTruthy` 可见性与表达式能力(IN/AND/OR)、`aggregate` 空集是否返 0、`QuotationLineComponentData` 是否有 `templateComponentId`。
