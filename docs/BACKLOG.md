# Project Backlog

> 自动维护文件，spec 评审后写入，开发完成后更新。
> 维护规则见 `docs/rules/task-docs.md` §9；写入前必须问用户是否采纳。
> 📌 2026-08-16 由 `CLAUDE.md` 末尾外移至此（原来内嵌在常驻层里，每个会话每个子代理都要加载一遍）。

> 自动维护文件，由 Claude Code 在 spec 评审后写入，开发完成后更新。
> 开始新任务前请先阅读本文件。

## 状态说明
- [ ] 待开发
- [~] 进行中
- [x] 已完成
- [-] 已废弃（需注明原因）

---

## 待开发条目

### 基础资料导入 / pending 生命周期

- [x] **报价单元素单价整列取不到（pending BOM 对取价函数不可见）**
  - 来源：`dev-docs/task-260729-客户价格调整策略和价格版本/repair-260830-元素单价pending不可见/问题说明.md`
  - 状态：**已交付合 master `06d9182c`（2026-08-30）**，待闸门 B 用户验收。方案乙（三参重载 + 改写器补参），15 个视图零改动、前端零改动、无 DROP FUNCTION
  - 根因：`f_material_element_price` 是数据库函数，`QuotePendingRewriter` 文本改写够不到其内部；函数内 `candidate_materials` 写死 `is_current = true` ⇒ pending 影子行的料号不进候选集
  - 影响：报价侧**所有核价未通过**（pending）报价单的元素单价。核价侧 / 已转正数据 / 老客户不受影响
  - 优先级：P0（现网 `QT-20260830-0211` 等单据金额缺失）
  - 前置条件：无
  - 预估规模：S=1-2天


- [ ] **长期未提交报价单的 pending 影子行定期回收机制**
  - 来源：`dev-docs/task-260721-报价升版逻辑/task-260830-重导零堆积/需求文档.md` §② 明确不做什么
  - 推迟原因：原计划随 `task-260830-重导零堆积` 一并处理，**该任务 2026-08-30 已废弃（未实施）** ⇒ 本条独立留存
  - ⚠️ **任务废弃后本条重要性上升**：原设想「首次落地即正式上线后影子行增速会下降」**不再成立** —— 写入逻辑一行未改，影子行仍会按每次导入一整份的速度无限堆积，且**系统内没有任何回收机制**（正常生命周期仅两个出口：核价通过转正 / 删单回收；从未提交的 DRAFT 单永久滞留）
  - 优先级：P2 可选（若库膨胀出现实际影响，应上调）
  - 前置条件：无
  - 预估规模：S=1-2天
  - 背景数据（2026-08-30 实测，用户清库前）：`material_bom` 53,610 行中 99.95% 为 pending 影子行，分属 75 张单，其中 68 张为从未提交的 DRAFT；正常生命周期仅两个出口（核价通过转正 / 删单回收），DRAFT 单不提交则永久滞留

### [报价单保存与渲染性能]

- [ ] **`GET /quotations/{id}` 服务端真分页（lineItems 按页返回）**
  - 来源：`task-260901-保存草稿增量协议与并发保护` §② 明确不做
  - 推迟原因：超出本期范围（用户 2026-09-01 明确「分页暂时先不讨论」）
  - 优先级：P1
  - 前置条件：**依赖 `task-260901` 的 ② 三数组 diff 落地**。现状「保存必须全量发」⇒ 前端必须持有全量 ⇒ 只加载 10 行时保存会把其余 1835 行当成「用户删了」而真删（`QuotationService:673`）。diff 落地后该障碍消除
  - 预估规模：L=1周以上
  - 背景：`task-260825` 已做的是**纯前端分页**（`usePagedSearch.ts` 头注释原文：「服务端零改动，全量数据仍一次性拉取并常驻前端内存」），解决渲染卡顿，未解决 22 秒的传输与组装

- [ ] **`ensure-card-values` 端点内部整跑一次 `getById`，返回 24.6 MB**
  - 来源：`task-260901` 调查发现（`QuotationResource.java:241`）
  - 推迟原因：独立缺陷，与本期改动面不重叠
  - 优先级：P1
  - 前置条件：无
  - 预估规模：S=1-2天
  - 背景数据（2026-09-01 实测）：对 1845 行单调用该端点耗时 **21.85 s**、响应 **24,633,539 字节**，且卡片值已全部非 NULL（不触发重算）的最好情况下亦如此。卡片值缺失的单会更慢。`QuotationWizard.tsx:626/633` 依赖它

- [ ] **核价侧单元格级即时编辑端点（对齐报价侧 `quote-card-edit`）**
  - 来源：`task-260901` 调查发现
  - 推迟原因：超出本期范围
  - 优先级：P2
  - 前置条件：无
  - 预估规模：M=3-5天
  - 背景：报价侧有 `PUT /line-items/{id}/quote-card-edit`（写 editRows + 重算 formulaResults + 物化 row_data + 覆盖派生小计）；核价侧 grep 全工程**无对应端点**，`QuotationStep2.tsx:2224` 注释明写「'COSTING' 维持旧路径」⇒ 核价侧任何改动只能靠整单 saveDraft。补齐可降低核价侧对 saveDraft 的依赖频率，但取代不了它（增删行/工序/单头它管不了）

- [ ] **保存冲突时的行级差异比对与合并 UI**
  - 来源：`task-260901` §② 明确不做
  - 推迟原因：用户 2026-09-01 裁决本期用「强制刷新」；行级合并的工作量约等于本期其余部分之和
  - 优先级：P2
  - 前置条件：**依赖 `task-260901` 的 ③ 版本指纹落地**
  - 预估规模：M=3-5天
  - 背景：本期冲突形态见 `dev-docs/task-260901-.../原型图/冲突提示.html`（单按钮强制刷新）。⚠️ 若将来做成「列出冲突行让用户逐行选」，它就从「简单二次确认」变成内容浏览+决策界面，按 `frontend.md` §1.1 **必须改用 Drawer 而非 Modal**

### 材质与元素主数据

- [ ] **BL-0201 · `material_recipe.symbol`（材质名）加 DB 唯一约束**
  - 来源：`dev-docs/task-260901-材质管理模块定义规则更新/需求文档.md` §2.2「本期明确不做」
  - 推迟原因：本期已把材质身份口径定为**按材质名判**（用户裁决 D2），并在应用层加了 M-6 防御分支（导入遇同名多材质整体跳过 + 报告）与新建/改名时的重名校验。DB 层再加唯一约束是**根治**，但属于加固而非本期功能，且需单独评估存量与并发路径
  - 优先级：P1
  - 前置条件：⚠️ **只有 dev 库就绪，test 库未就绪**（2026-09-02 实测更正，原写「已就绪」是**只查了 dev 库**得出的错误结论）：
    - dev `cpq_db_0724`：`258 | 258`，**零重名** ✅
    - test `cpq_db`：**`263 | 259`，有 3 组同名 ACTIVE 材质** —— `AgNi`×3（AgNi70 / AgNi90 / AgNi95）、`AgCu`×2（AgCu85 / AgCu90）、`Cu`×2（99900 / 99901）
    🚨 **直接加约束会让 test 库的 Flyway 迁移失败 ⇒ `mvnw test` 全挂。** 本条落地前必须先清理 test 库那 3 组（它们是历史用例没回滚的残留，见下方备注），或让迁移带 `NOT VALID` / 先清后建的分步策略
  - 预估规模：S=1-2天
  - 备注：约束应为 `UNIQUE (symbol) WHERE status='ACTIVE'`（部分索引）而非无条件唯一 —— 停用的历史材质允许与在用材质重名，否则停用一条老材质后就无法用同名新建
  - 🔎 **顺带发现（2026-09-02，`task-260901` 测试代理报、主线实测确认）**：test 库 `cpq_db` 的 `material_recipe` 有历史测试残留 —— 除上述 3 组重名外，还有 `99900 / Cu`、`99901 / Cu` 这类五位编号（**这会把「材质编号自增」的基线从 `00262` 顶到 `99901`**）。说明过去有用例没在 `finally` 里回滚。本任务的用例不依赖也不清理它们（不在授权删除面内），**但谁要动 test 库的材质基线，先看这一条**

### 选配（产品配置器）

- [ ] **BL-0202 · 选配生成的报价单行不写 `template_id`，详情/编辑页渲染不出组件结构**
  - 来源：`task-260901-材质管理模块定义规则更新` 的独立验收会话发现（2026-09-02），交付会话核实后转出
  - ⚠️ **与材质模型无关**（核实结论）：本次合并的 150 个文件里 `template_id` / `templateId` **命中 0 处**，`QuotationWizard.tsx` / `QuotationStep2.tsx` **一行未动**；被碰到的 quotation 目录文件只有 `configure/` 下三个（`AddPartSubDrawer.tsx` / `SelDetailTable.tsx` / `configureRequest.ts`），均不处理 `template_id`
  - 现象：`quotation_line_item.template_id IS NULL` ⇒ 该行没有产品卡模板 ⇒ 无页签 / 列 / 公式结构可渲染，`QuotationStep2.tsx:3911` 落到「请通过添加产品选择模板后自动加载组件结构」，小计 ¥0
  - 影响面（2026-09-02 实测 dev 库 `cpq_db_0724`）：`quotation_line_item` 共 **9237 行，NULL 仅 11 行**，且 **11 行全部落在同一张草稿单 `QT-20260901-0234 [DRAFT]`**（09-02 10:08 ~ 12:51）—— 那是 task-260901 验收 spec 反复追加、从不清理的测试残单。**9226 行真实单据零命中**
  - 优先级：P2 —— 真实单据零命中，但「选配路径写不出 `template_id`」是真缺陷，选配重构时必须带上
  - 归属：并入 `dev-docs/task-260902-选配流程重构/`（该任务 2026-09-02 立项、闸门 A0 呈报中，选配落库路径本就在其改造范围内）
  - 连带：`AC-22` 的验法已同步改写为直接断言 `element_bom_item` 逐字不变，不再绕这条坏链路（见 `task-260901/需求文档.md` AC-22 注）
  - 顺带清理：`QT-20260901-0234` 是测试残单，可连同其 11 行一并删除 —— 属 §3.2 红线，需单独批准
  - 预估规模：S（并入 task-260902 后近乎零增量）

### [模块名称]

- [ ] **功能描述**
  - 来源：spec §X.X / [章节名]
  - 推迟原因：
  - 优先级：P0 / P1 / P2
  - 前置条件：
  - 预估规模：S / M / L

---

## 变更记录

| 日期 | 操作 | 条目 | 备注 |
|------|------|------|------|
| 2026-09-02 | 结案 | `task-260901-材质管理模块定义规则更新` **已交付**（闸门 B 通过） | 本任务**无自身 Backlog 条目可勾**（它来自用户直接需求、非 Backlog 派生）；其推迟产物 **BL-0201**（材质名唯一约束）与 **BL-0202**（选配 `template_id`）**仍为待开发**，不随结案关闭 |
| 2026-09-02 | 新增 | **BL-0202** 选配报价单行不写 `template_id` | 由 `task-260901` 独立验收发现、交付会话核实「与材质模型零关联」后转出；用户 2026-09-02 裁决**转 BACKLOG 挂选配侧 + AC-22 改写为直接断言 `element_bom_item`** |
| 2026-09-02 | 更正 | **BL-0201** 前置条件 | 原写「已就绪」是**只查了 dev 库**的错误结论。实测 test 库 `cpq_db` 有 3 组同名 ACTIVE 材质，直接加约束会让 `mvnw test` 全挂 —— 前置条件改为「dev 就绪 / test 未就绪」，并记下需先清理的具体数据 |
| 2026-09-01 | 新增 | **BL-0201** 材质名加 DB 唯一约束 | 来自 `task-260901-材质管理模块定义规则更新` 立项，用户 2026-09-01 裁决**只采纳这一条**；同批提议的「脏数据清理三条」与「旧模板转换工具」**未采纳、不登记**（已写在该任务 `需求文档.md` §2.2，需要时从那里捡） |
| 2026-09-01 | 新增 | 报价单保存与渲染性能 4 条 | 来自 `task-260901-保存草稿增量协议与并发保护` 立项，用户 2026-09-01 裁决采纳；第 5 条「行级版本号」经建议**不登记**（整单级够用时它是没人认领的僵尸条目） |
| 2026-08-30 | 新增 | pending 影子行定期回收机制 | 来自 `task-260830-重导零堆积` 立项，用户裁决登记 P2 |
```
