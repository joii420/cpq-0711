# 方案 — 报价标准模板-Excel基础结构 v1.7 重配方案

> 2026-05-22 起稿 · 评审中(等待用户在本文档上勾 OK/改)
> 一旦本方案确认通过, 下一步生成 Flyway `V216__quotation_std_excel_v1_7.sql` 或调 admin endpoint 落地。

---

## 0. 背景与目标

### 0.1 目标模板(现状)

| 属性 | 值 |
|---|---|
| name | `报价标准模板-Excel基础结构 v1.0` |
| id | `f6dae2b1-8b92-4de3-a93f-e78139af3b35` |
| version | `v1.6` |
| status | `PUBLISHED` (已被存量报价单引用, 不可动) |
| template_kind | `QUOTATION` |
| category_id | `b9576df8` |
| customer_id | `8de8f8b0` (并非通用 — 是某客户专属) |
| is_default | false |
| tabs | 9 个(料件 / 来料 / 元素 / 成品 / 组成件BOM / 组装加工 / 电镀 / 小计1 / 组成件其他费用) |
| fields | 94 个(均为 `BASIC_DATA` + `basic_data_path = v_q_*_merged.<col>`) |
| product_attributes | 3 个(产品规格 / 尺寸 / 图号, FIXED_VALUE) |
| subtotal_formula | `[]`(空) |
| excel_view_config | `NULL`(空) |
| formulas | `[]`(空) |
| 小计1 component | 自身 formulas 引用了**不存在**的 `COMP-0006`/`COMP-0007`(老遗留 bug) |
| template_component.fields_override / data_driver_path_override | 9 行全部 NULL(已符合红线 4) |

### 0.2 用户选择的改造维度(已确认)

- ✅ 可编辑字段改为 `INPUT_NUMBER + default_source`
- ✅ 补 `FORMULA` 计算字段 + 填「小计1」(`component_subtotal` 聚合)
- ✅ 补 `excel_view_config` (Excel 视图 23 列模式)
- ✅ 视图 path 不变(`v_q_*_merged` 继续用, 它本身已是规范的隐式 JOIN 配置)
- ❌ 金额类不改为全局变量(本轮放弃, 维持 BASIC_DATA)

### 0.3 落地方式

1. `POST /api/cpq/templates/{v1.6}/draft` → 创建 v1.7 DRAFT(`createNewDraft`)
2. 修订 v1.7 DRAFT 的 `components_snapshot[]` / `subtotal_formula` / `excel_view_config`
3. 修复「小计1」component 的 `formulas`(运维: 直接更 `component` 表; 走 admin endpoint 还是 SQL 由后续确认)
4. UI 评审 v1.7 → `PUBLISH` → v1.6 自动 `ARCHIVED`

---

## 1. 字段类型升级矩阵 — 逐 Tab 决策

> 取值原则:
> - **B0(不动)**: 保持 BASIC_DATA + 原 `basic_data_path`(标识/分类性字段、视图列已是权威值)
> - **I1(可写)**: 改 `INPUT_NUMBER` + `default_source.BNF_PATH=<原 path>` + `is_amount` 沿用 — 适用于销售/工程师在报价时常调的百分比/量
> - **F2(计算)**: 改 `FORMULA` + `formula_tokens=[...]` — 适用于本可由其他字段算出的派生值
> - **S3(小计标记)**: 在 BASIC_DATA / FORMULA 上加 `is_subtotal=true` — 标记被「小计1」聚合的列(每个 NORMAL 组件最多 1 个)

### Tab 1 「料件」(componentId=f5c00b16, COMP-QX-PART-INFO, v_q_part_info_merged, 10 字段)

| # | 字段名 | 原配置 | 新配置 | 说明 |
|---|---|---|---|---|
| 1 | 宏丰料号 | BASIC `hf_part_no` | B0 不动 | 主键, 不可改 |
| 2 | 客户料号名称 | BASIC `customer_part_name` | B0 不动 | 主数据 |
| 3 | 客户产品编号 | BASIC `customer_product_no` | B0 不动 | 主数据 |
| 4 | 客户图号 | BASIC `customer_drawing_no` | B0 不动 | 主数据 |
| 5 | 付款方式 | BASIC `payment_method` | B0 不动 | 主数据 |
| 6 | 基础货币 | BASIC `base_currency` | B0 不动 | 主数据 |
| 7 | 报价货币 | BASIC `quote_currency` | B0 不动 | 主数据 |
| 8 | **汇率** | BASIC `exchange_rate` | **I1 推荐** | 销售可临时调汇率, `default_source.BNF_PATH=v_q_part_info_merged.exchange_rate`, `is_amount=false`, content="1.0" |
| 9 | 单重(g/pcs) | BASIC `unit_weight` | B0 不动 | 工程参数, 一般不在报价单上手改(改要走主数据) |
| 10 | 重量单位 | BASIC `weight_unit` | B0 不动 | 主数据 |

### Tab 2 「来料」(componentId=e260cdda, COMP-QX-INCOMING, v_q_incoming_merged, 23 字段)

| # | 字段名 | 原配置 | 新配置 | 说明 |
|---|---|---|---|---|
| 1 | 项次 | BASIC `seq_no` | B0 | 排序键 |
| 2 | 项次2 | BASIC `sub_seq_no` | B0 | 子排序键 |
| 3 | 宏丰料号 | BASIC `hf_part_no` | B0 | 主键 |
| 4 | 来源 | BASIC `source_type` | B0 | UNION 来源标识(BOM/INCOMING_FIXED/INCOMING_OTHER/MATERIAL_RECYCLE) |
| 5 | 投入料号 | BASIC `input_material_no` | B0 | 主数据 |
| 6 | 投入料号名称 | BASIC `input_material_name` | B0 | 主数据 |
| 7 | 产出料号类型 | BASIC `output_material_type` | B0 | 主数据 |
| 8 | 要素名称 | BASIC `element_name` | B0 | 主数据 |
| 9 | 材料毛重 | BASIC `gross_qty` | B0 | 工程参数 |
| 10 | 材料净重 | BASIC `net_qty` | B0 | 工程参数 |
| 11 | 重量单位 | BASIC `weight_unit` | B0 | 主数据 |
| 12 | **损耗率(%)** | BASIC `loss_rate` | **I1 推荐** | 销售可改, default_source=BNF, content="0" |
| 13 | **不良率(%)** | BASIC `defect_rate` | **I1 推荐** | 销售可改, default_source=BNF, content="0" |
| 14 | **回收折扣(%)** | BASIC `recycle_pct` | **I1 推荐** | 销售可改, default_source=BNF, content="0" |
| 15 | **比例(%)** | BASIC `fee_ratio` | **I1 推荐** | 销售可改, default_source=BNF, content="100" |
| 16 | **值** | BASIC `fee_value` (is_amount) | **S3 + 保留 BASIC** | `is_subtotal=true` 让小计1可聚合此列 |
| 17 | 货币 | BASIC `currency` | B0 | 主数据 |
| 18 | 计价单位 | BASIC `price_unit` | B0 | 主数据 |
| 19 | 是否随材料价格波动 | BASIC `price_floating` | B0 | 布尔, 不在报价改 |
| 20 | 材料结算涨幅比例(%) | BASIC `settlement_rise_ratio` | **I1 推荐** | 销售可改, default_source=BNF, content="0" |
| 21 | 材料固定涨幅值 | BASIC `fixed_rise_value` (is_amount) | B0 | 金额, 本轮不改全局变量 |
| 22 | 涨幅货币 | BASIC `rise_currency` | B0 | 主数据 |
| 23 | 涨幅单位 | BASIC `rise_unit` | B0 | 主数据 |

### Tab 3 「元素」(componentId=3f1911ef, COMP-QX-ELEMENT, v_q_element_merged, 13 字段)

| # | 字段名 | 原配置 | 新配置 | 说明 |
|---|---|---|---|---|
| 1 | 项次 | BASIC `seq_no` | B0 | 排序 |
| 2 | 宏丰料号 | BASIC `hf_part_no` | B0 | 主键 |
| 3 | 来源 | BASIC `source_type` | B0 | UNION 标识 |
| 4 | 投入料号 | BASIC `input_material_no` | B0 | 主数据 |
| 5 | 投入料号名称 | BASIC `input_material_name` | B0 | 主数据 |
| 6 | 元素 | BASIC `element_name` | B0 | 主数据 |
| 7 | **组成含量(%)** | BASIC `composition_pct` | **I1 推荐** | 工程可调, default_source=BNF, content="0" |
| 8 | 毛用量 | BASIC `gross_qty` | B0 | 计算性, 保留只读 |
| 9 | 毛用量单位 | BASIC `gross_unit` | B0 | 主数据 |
| 10 | 净用量 | BASIC `net_qty` | B0 | 计算性 |
| 11 | 净用量单位 | BASIC `net_unit` | B0 | 主数据 |
| 12 | **损耗率(%)** | BASIC `loss_rate` | **I1 推荐** | 同来料.损耗率 |
| 13 | **回收折扣(%)** | BASIC `recycle_pct` | **I1 推荐** | 同来料.回收折扣 |

### Tab 4 「成品」(componentId=0ed64cd9, COMP-QX-FINISHED, v_q_finished_merged, 8 字段)

| # | 字段名 | 原配置 | 新配置 | 说明 |
|---|---|---|---|---|
| 1 | 项次 | BASIC `seq_no` | B0 | 排序 |
| 2 | 宏丰料号 | BASIC `hf_part_no` | B0 | 主键 |
| 3 | 来源 | BASIC `source_type` | B0 | UNION 标识 |
| 4 | 要素名称 | BASIC `element_name` | B0 | 主数据 |
| 5 | **比例(%)** | BASIC `fee_ratio` | **I1 推荐** | 销售可改 |
| 6 | **值** | BASIC `fee_value` (is_amount) | **S3 + 保留 BASIC** | `is_subtotal=true` |
| 7 | 货币 | BASIC `currency` | B0 | 主数据 |
| 8 | 计价单位 | BASIC `price_unit` | B0 | 主数据 |

### Tab 5 「组成件BOM」(componentId=b21f39e6, COMP-QX-COMPONENT, v_q_component_merged, 9 字段)

| # | 字段名 | 原配置 | 新配置 | 说明 |
|---|---|---|---|---|
| 1 | 项次 | BASIC `seq_no` | B0 | 排序 |
| 2 | 项次2 | BASIC `sub_seq_no` | B0 | 子排序 |
| 3 | 宏丰料号 | BASIC `hf_part_no` | B0 | 主键 |
| 4 | 组成件料号 | BASIC `component_part_no` | B0 | 主数据 |
| 5 | 组成件名称 | BASIC `component_name` | B0 | 主数据 |
| 6 | **组成数量** | BASIC `quantity` | **I1 推荐** | BOM 数量销售可微调, default_source=BNF, content="0" |
| 7 | 组成单位 | BASIC `quantity_unit` | B0 | 主数据 |
| 8 | 组装工序 | BASIC `assembly_process` | B0 | 主数据 |
| 9 | 工序编号 | BASIC `process_code` | B0 | 主数据 |

### Tab 6 「组装加工」(componentId=d2d6e608, COMP-QX-ASSEMBLY, v_q_assembly_merged, 7 字段)

| # | 字段名 | 原配置 | 新配置 | 说明 |
|---|---|---|---|---|
| 1 | 项次 | BASIC `seq_no` | B0 | 排序 |
| 2 | 宏丰料号 | BASIC `hf_part_no` | B0 | 主键 |
| 3 | 组装工序 | BASIC `assembly_process` | B0 | 主数据 |
| 4 | **拒收率/不良率(%)** | BASIC `reject_rate` | **I1 推荐** | 工程可调, default_source=BNF, content="0" |
| 5 | **组装加工费** | BASIC `fee_value` (is_amount) | **S3 + 保留 BASIC** | `is_subtotal=true` |
| 6 | 货币 | BASIC `currency` | B0 | 主数据 |
| 7 | 计价单位 | BASIC `price_unit` | B0 | 主数据 |

### Tab 7 「电镀」(componentId=7c28d2ae, COMP-QX-PLATING, v_q_plating_merged, 14 字段)

| # | 字段名 | 原配置 | 新配置 | 说明 |
|---|---|---|---|---|
| 1 | 项次 | BASIC `seq_no` | B0 | 排序 |
| 2 | 宏丰料号 | BASIC `hf_part_no` | B0 | 主键 |
| 3 | 来源 | BASIC `source_type` | B0 | UNION 标识 |
| 4 | 方案编号 | BASIC `plan_code` | B0 | 主数据 |
| 5 | 版本 | BASIC `plan_version` | B0 | 版本号 |
| 6 | 电镀元素名称 | BASIC `plating_element` | B0 | 主数据 |
| 7 | 电镀要求 | BASIC `plating_requirement` | B0 | 主数据 |
| 8 | **电镀面积(cm2)** | BASIC `plating_area` | **I1 推荐** | 工程可调, default_source=BNF, content="0" |
| 9 | **镀层厚度(um)** | BASIC `coating_thickness` | **I1 推荐** | 工程可调, default_source=BNF, content="0" |
| 10 | **不良率(%)** | BASIC `defect_rate` | **I1 推荐** | 销售可改 |
| 11 | **电镀材料费** | BASIC `plating_material_fee` (is_amount) | **S3 + 保留 BASIC** | `is_subtotal=true`(电镀-材料子项) |
| 12 | **电镀加工费** | BASIC `plating_process_fee` (is_amount) | **F2(新建)+ S3** | `formula_tokens=[field 电镀材料费, operator +, field 电镀加工费_raw]` → ⚠️ 现状是 BASIC 直读, 若拆 F2 需新增 hidden BASIC 列存原值。**建议保守做 S3 不做 F2** |
| 13 | 货币 | BASIC `currency` | B0 | 主数据 |
| 14 | 计价单位 | BASIC `price_unit` | B0 | 主数据 |

> ⚠️ 电镀有 **2 个金额字段**(材料费 + 加工费). 一个组件 `is_subtotal=true` 最多 1 个. 为此本方案在「电镀」上**只标 1 个**(取「电镀加工费」, 因为电镀加工费包含完整电镀成本; 材料费已被加工费间接覆盖). 若用户判断"材料费 + 加工费"应分别聚合, 需要拆成两个 component(`COMP-QX-PLATING-MATERIAL` + `COMP-QX-PLATING-PROCESS`), 这属于结构性改动, 不在本方案范围.

### Tab 8 「小计1」(componentId=0c15bd9c, COMP-0009, SUBTOTAL, fields=0)

**原状**: SUBTOTAL 组件, 自身无 fields. component.formulas 当前引用了**不存在的** `COMP-0006`/`COMP-0007`(老遗留 bug).

**新状**(双层落地, 任选其一或双管齐下):

**方案 A — 修改 component.formulas(组件级)**

```jsonc
[
  {
    "name": "总成本",
    "expression": [
      { "type": "component_subtotal", "component_code": "COMP-QX-INCOMING",       "tab_name": "来料",       "value": "来料·值" },
      { "type": "operator", "value": "+" },
      { "type": "component_subtotal", "component_code": "COMP-QX-FINISHED",       "tab_name": "成品",       "value": "成品·值" },
      { "type": "operator", "value": "+" },
      { "type": "component_subtotal", "component_code": "COMP-QX-ASSEMBLY",       "tab_name": "组装加工",   "value": "组装加工·组装加工费" },
      { "type": "operator", "value": "+" },
      { "type": "component_subtotal", "component_code": "COMP-QX-PLATING",        "tab_name": "电镀",       "value": "电镀·电镀加工费" },
      { "type": "operator", "value": "+" },
      { "type": "component_subtotal", "component_code": "COMP-QX-COMPONENT-FEE",  "tab_name": "组件其他费用","value": "组件其他费用·值" }
    ],
    "result_type": "NUMBER"
  }
]
```

> ⚠️ `component.formulas` 是组件级, 改动**会影响所有引用 0c15bd9c-COMP-0009 的模板**(不仅 v1.7). 若用户希望 v1.7 独享, 应走方案 B.

**方案 B — 填模板根级 subtotal_formula(模板私有)**

直接写到 `template.subtotal_formula`, 模板独享, 不污染 component:

```jsonc
[
  {
    "name": "总成本",
    "expression": [
      { "type": "component_subtotal", "component_code": "COMP-QX-INCOMING",       "tab_name": "来料" },
      { "type": "operator", "value": "+" },
      { "type": "component_subtotal", "component_code": "COMP-QX-FINISHED",       "tab_name": "成品" },
      { "type": "operator", "value": "+" },
      { "type": "component_subtotal", "component_code": "COMP-QX-ASSEMBLY",       "tab_name": "组装加工" },
      { "type": "operator", "value": "+" },
      { "type": "component_subtotal", "component_code": "COMP-QX-PLATING",        "tab_name": "电镀" },
      { "type": "operator", "value": "+" },
      { "type": "component_subtotal", "component_code": "COMP-QX-COMPONENT-FEE",  "tab_name": "组件其他费用" }
    ],
    "result_type": "NUMBER"
  }
]
```

> ✅ **本方案建议:走方案 B**。隔离干净, 不污染 component, 不影响别的模板。组件级旧 bug 公式留待清单中"遗留账务"单独处理。

### Tab 9 「组成件其他费用」(componentId=631f2416, COMP-QX-COMPONENT-FEE, v_q_component_fee_merged, 10 字段)

| # | 字段名 | 原配置 | 新配置 | 说明 |
|---|---|---|---|---|
| 1 | 项次 | BASIC `seq_no` | B0 | 排序 |
| 2 | 组件项次 | BASIC `sub_seq_no` | B0 | 子排序 |
| 3 | 宏丰料号 | BASIC `hf_part_no` | B0 | 主键 |
| 4 | 组成件料号 | BASIC `component_part_no` | B0 | 主数据 |
| 5 | 组成件名称 | BASIC `component_name` | B0 | 主数据 |
| 6 | 组装工序 | BASIC `assembly_process` | B0 | 主数据 |
| 7 | 要素名称 | BASIC `element_name` | B0 | 主数据 |
| 8 | **值** | BASIC `fee_value` (is_amount) | **S3 + 保留 BASIC** | `is_subtotal=true` |
| 9 | 货币 | BASIC `currency` | B0 | 主数据 |
| 10 | 计价单位 | BASIC `price_unit` | B0 | 主数据 |

---

## 2. 改动量小结

| 维度 | 数量 | 备注 |
|---|---|---|
| **B0 不动** | 78 字段 | 维持 BASIC_DATA + `v_q_*_merged.<col>` |
| **I1 改 INPUT_NUMBER** | 11 字段 | 汇率 / 损耗率 / 不良率 / 回收折扣 / 比例 / 组成含量 / 拒收率 / 组成数量 / 电镀面积 / 镀层厚度 / 材料结算涨幅 |
| **S3 加 is_subtotal=true** | 5 字段 | 来料.值 / 成品.值 / 组装加工.组装加工费 / 电镀.电镀加工费 / 组件其他费用.值 |
| **F2 改 FORMULA** | 0 字段 | 本轮不引入计算字段(电镀双金额场景留作后续讨论) |
| 模板根级 subtotal_formula | 1 条 | 「总成本」聚合 5 个 component_subtotal |
| 小计1 组件 fields | 0 → 0 | 不动(SUBTOTAL 组件不需要 fields) |
| **excel_view_config** | 0 → ~14 列 | 见 §3 |

---

## 3. excel_view_config 设计(14 列)

> 模式参照 c965b250 (`选配产品标准报价模板-组合产品 v1.26`):
> - 部分列用 hidden `H_*` 中间列做底层取数(BNF path)
> - 业务列用 FORMULA `=[H_X]` 引用, 便于将来调整列序/拆分而不动 path

| col_key | col_name | source_type | variable_path / formula | visible | comparison_tag | 取自 |
|---|---|---|---|---|---|---|
| `H_HF_NO` | 宏丰料号(中间) | VARIABLE | `{hf_part_no}` | hidden | — | lineItem.productPartNo |
| `H_CUST_NAME` | 客户料号(中间) | VARIABLE | `v_q_part_info_merged.customer_part_name` | hidden | — | 视图 |
| `H_PROD_NAME` | 品名(中间) | VARIABLE | `{product_name}` | hidden | — | lineItem.productName |
| `H_SPEC` | 规格(中间) | VARIABLE | `{specification}` | hidden | — | lineItem.hfPartInfo.specification |
| `H_WEIGHT` | 单重(中间) | VARIABLE | `v_q_part_info_merged.unit_weight` | hidden | — | 视图 |
| `H_WEIGHT_UNIT` | 重量单位(中间) | VARIABLE | `v_q_part_info_merged.weight_unit` | hidden | — | 视图 |
| `H_BASE_CCY` | 基础货币(中间) | VARIABLE | `v_q_part_info_merged.base_currency` | hidden | — | 视图 |
| `H_QUOTE_CCY` | 报价货币(中间) | VARIABLE | `v_q_part_info_merged.quote_currency` | hidden | — | 视图 |
| `H_FX` | 汇率(中间) | VARIABLE | `v_q_part_info_merged.exchange_rate` | hidden | — | 视图 |
| `H_PAY` | 付款方式(中间) | VARIABLE | `v_q_part_info_merged.payment_method` | hidden | — | 视图 |
| `H_SUBTOTAL` | 报价行小计(中间) | VARIABLE | `{subtotal}` | hidden | — | lineItem.subtotal(模板 subtotal_formula 算出) |
| `A` | 宏丰料号 | FORMULA | `=[H_HF_NO]` | ✅ | `HF_PART_NO` | |
| `B` | 客户料号 | FORMULA | `=[H_CUST_NAME]` | ✅ | `CUSTOMER_PART_NAME` | |
| `C` | 品名 | FORMULA | `=[H_PROD_NAME]` | ✅ | `PRODUCT_NAME` | |
| `D` | 规格 | FORMULA | `=[H_SPEC]` | ✅ | — | |
| `E` | 单重(g/pcs) | FORMULA | `=[H_WEIGHT]` | ✅ | `UNIT_WEIGHT` | |
| `F` | 重量单位 | FORMULA | `=[H_WEIGHT_UNIT]` | ✅ | — | |
| `G` | 基础货币 | FORMULA | `=[H_BASE_CCY]` | ✅ | — | |
| `H` | 报价货币 | FORMULA | `=[H_QUOTE_CCY]` | ✅ | — | |
| `I` | 汇率 | FORMULA | `=[H_FX]` | ✅ | `FX_RATE` | |
| `J` | 付款方式 | FORMULA | `=[H_PAY]` | ✅ | — | |
| `K` | 单价 | FORMULA | `=[H_SUBTOTAL]` | ✅ | `UNIT_PRICE` | 模板 subtotal_formula 算出的总成本 |

> ⚠️ 备注:
> 1. 本 Excel 视图按每个 `lineItem`(产品行)输出 1 行, **不展开 Tab 内的子行**(LinkedExcelView 是 lineItem-level)。需要看 Tab 内的明细要用「产品视图」/「Excel 明细视图」切换(若 PRD 有的话, 不在本方案范围)
> 2. 23 列模式是核价 demo, 报价标准只需主报价行字段, 故只配 12 列(11 业务列 + 1 中间 `H_SUBTOTAL`)
> 3. 如果业务需要展示 BOM/工序明细,改用 `excel_view_config` 的 `notes` 字段标记后续阶段补充

---

## 4. 落地步骤(待批准后执行)

### 4.1 步骤序列

```
1. POST /api/cpq/templates/f6dae2b1.../draft           # 创建 v1.7 DRAFT
   响应: { id: <NEW>, name: ..., version: 'v1.7', status: 'DRAFT' }

2. 写 Flyway V216__quotation_std_excel_v1_7.sql:
   - UPDATE template
       SET components_snapshot = jsonb_set(..., 11 字段升级到 INPUT_NUMBER),
           components_snapshot = jsonb_set(..., 5 字段加 is_subtotal=true),
           subtotal_formula = '[ {总成本聚合} ]'::jsonb,
           excel_view_config = '[ 14 列 ]'::jsonb,
           description = 'V216: v1.7 — INPUT_NUMBER + subtotal_formula + excel_view_config'
     WHERE id = '<v1.7 NEW id>';

3. 自检:
   - 重启 Quarkus (touch java)
   - PGPASSWORD=... psql ... -c "SELECT version, success FROM flyway_schema_history WHERE version = '216'"  # success=t
   - GET /api/cpq/templates/<v1.7 NEW id>  → 200, evc/subtotal 字段非空
   - 前端打开 v1.7 模板编辑页 → 字段类型显示「数字输入」(11 字段)、Excel 视图标签页显示 12 列

4. E2E (强制):
   cd cpq-frontend
   npx playwright test --config=e2e/playwright.config.ts e2e/quotation-flow.spec.ts --reporter=list
   # 必须 1 passed + '加载中' final count=0 + 全 8 Tab '加载中'=0
   # ⚠️ 修改了 components_snapshot.fields, 触发 AP-37 / AP-44 的 17 处协议
   # ⚠️ 必须额外验证: 一份 新建报价单 使用 v1.7 → 渲染 8 Tab 全显示, INPUT_NUMBER 字段显示「默认 X · v_q_*_merged.col」 placeholder

5. 评审通过后 PUBLISH v1.7:
   POST /api/cpq/templates/<v1.7 NEW id>/publish
   → v1.6 自动 ARCHIVED (同 series)
   → refreshSnapshotsByComponent 触发
```

### 4.2 Flyway SQL 骨架(伪代码, 实际生成时按 v1.7 真实 id 填)

```sql
-- V216__quotation_std_excel_v1_7.sql
DO $$
DECLARE
  v_template_id UUID := '<v1.7 NEW id, 上一步 createNewDraft 返回>';
  v_evc JSONB := '[
    { "col_key":"H_HF_NO", "col_name":"宏丰料号(中间)", "title":"宏丰料号(中间)",
      "source_type":"VARIABLE", "variable_path":"{hf_part_no}", "visible":false, "hidden":true },
    -- ... 11 其他中间列 ...
    { "col_key":"A", "col_name":"宏丰料号", "title":"宏丰料号", "col_index":"A",
      "source_type":"FORMULA", "formula":"=[H_HF_NO]", "visible":true, "hidden":false,
      "comparison_tag":"HF_PART_NO" }
    -- ... 10 其他业务列 ...
  ]'::jsonb;
  v_subtotal JSONB := '[
    { "name":"总成本",
      "expression":[
        { "type":"component_subtotal","component_code":"COMP-QX-INCOMING","tab_name":"来料" },
        { "type":"operator","value":"+" },
        { "type":"component_subtotal","component_code":"COMP-QX-FINISHED","tab_name":"成品" },
        { "type":"operator","value":"+" },
        { "type":"component_subtotal","component_code":"COMP-QX-ASSEMBLY","tab_name":"组装加工" },
        { "type":"operator","value":"+" },
        { "type":"component_subtotal","component_code":"COMP-QX-PLATING","tab_name":"电镀" },
        { "type":"operator","value":"+" },
        { "type":"component_subtotal","component_code":"COMP-QX-COMPONENT-FEE","tab_name":"组件其他费用" }
      ],
      "result_type":"NUMBER"
    }
  ]'::jsonb;
BEGIN
  -- 1) Excel + subtotal_formula
  UPDATE template
     SET excel_view_config = v_evc,
         subtotal_formula  = v_subtotal,
         description = COALESCE(description,'') || E'\nV216: v1.7 = v1.6 + INPUT_NUMBER 11 + is_subtotal 5 + subtotal_formula + excel_view_config 12 cols',
         updated_at = now()
   WHERE id = v_template_id;

  -- 2) 修订 components_snapshot 内每个 Tab 的 fields[]
  --    使用 JSONB 表达式定位 [tabName=料件].fields[name=汇率] 并改 field_type=INPUT_NUMBER + default_source + content
  --    (实际 SQL 用 jsonb_set 多层或 PL/pgSQL 循环, 模板生成代码后再展开)
  -- ... 11 字段升级 ...
  -- ... 5 字段加 is_subtotal=true ...
END $$;
```

> Flyway 真实文件由本方案确认后生成。结构上是单文件 + PL/pgSQL DO 块。

### 4.3 自检清单(对照 CLAUDE.md「修改后强制自检」)

后端改动:
- [ ] touch java 重启 Quarkus, 等 5-7 秒
- [ ] flyway_schema_history `success=t`
- [ ] 不手动 `psql -f V_xx.sql`(走 Flyway dev 自启)

前端改动: **本方案不动前端代码**(纯 schema/数据改). 但 v1.7 含字段类型升级 → 必跑 E2E:
- [ ] `quotation-flow.spec.ts` 1 passed, `'加载中' final count = 0`, 全 8 Tab `'加载中'=0`
- [ ] **同时手测**:新建一份报价单挂 v1.7 → 编辑器渲染 → 看到 INPUT_NUMBER 字段显示数字输入框 + 「默认 X · v_q_*_merged.col」 placeholder

### 4.4 不在本轮范围

- 金额字段改 GLOBAL_VARIABLE
- FORMULA 计算字段(电镀双金额拆分)
- 把每个 NORMAL Tab 增列「小计」FORMULA
- 修复 `0c15bd9c-COMP-0009` 组件的遗留 formulas(组件级影响别的模板, 独立 ticket 处理)
- 把 `customer_id=8de8f8b0` 改通用(让 v1.7 给所有客户用)

---

## 5. 风险与回滚

### 5.1 主要风险

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| INPUT_NUMBER 11 字段触发 AP-37/AP-44 17 处协议 | 中 | 老报价单 v1.6 不受影响(snapshot 隔离); v1.7 自身可能有 cache key 偏差 | E2E 强制 + 双 spec (quotation-flow + composite-product-flow if applicable) |
| subtotal_formula 公式引用 `tab_name` 拼写错 | 中 | 「总成本」=0 / NaN | 严格按 §1 表中 tab_name(中文) 复刻 |
| excel_view_config 列 `{subtotal}` 解析依赖 lineItem.subtotal | 中 | Excel 视图「单价」=空 | lineItem.subtotal 是后端 quotation 计算后填; 若该字段空, 改用具体 `v_q_*_merged.fee_value` BNF path |
| customer_id=8de8f8b0 限定专属客户 | 低 | v1.7 也只对此客户可见 | 与 v1.6 同范围, 行为一致 |
| createNewDraft 触发 mergeFieldsOverrideForNewDraft | 低 | 老 override 上升风险(AP-39) | 已验证 9 行 override 全 NULL, 不会触发 |

### 5.2 回滚

```sql
-- 若 v1.7 发布后发现问题, 回滚到 v1.6 默认引用
UPDATE quotation
   SET customer_template_id = 'f6dae2b1-8b92-4de3-a93f-e78139af3b35'  -- v1.6
 WHERE customer_template_id = '<v1.7 NEW id>'
   AND status IN ('DRAFT','SUBMITTED');
-- 再 DELETE v1.7 / 或 把 v1.7 置 ARCHIVED, 把 v1.6 状态从 ARCHIVED 改回 PUBLISHED
```

---

## 6. 评审打勾位置

请你在以下问题上勾选(直接回复也可):

- [ ] §1 11 个 INPUT_NUMBER 字段清单 — 是否全部认可? 有要从清单去掉的吗?
- [ ] §1 5 个 `is_subtotal=true` 字段清单 — 是否对应你的成本聚合预期?
- [ ] §1 Tab 7「电镀」材料费 + 加工费 双金额, 本方案选「只标加工费」, 你认可吗? 还是要拆双组件?
- [ ] §1 Tab 8「小计1」是否走「方案 B(模板根级 subtotal_formula)」?
- [ ] §3 Excel 视图 12 列设计 — `K 列 单价 = {subtotal}` 是否符合你期望? 还是要用 `v_q_*_merged.fee_value` 之一?
- [ ] §4 整体落地步骤是否照此推进? Flyway 文件文件名 `V216__quotation_std_excel_v1_7.sql` 命名 OK?

打勾后我就照此生成 Flyway V216 + 调 admin endpoint 跑全流程自检, 完成后回报你 PUBLISH 前的截图证据。
