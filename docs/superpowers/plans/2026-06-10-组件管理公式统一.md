# 组件管理重构 · 公式能力统一 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Excel 视图配置收编为 `component_type=EXCEL` 组件、三类组件公式统一复用 `TabJoinFormulaDrawer` 编辑器、组件管理改 Master-Detail 双栏，并收敛路径来源为 SQL 视图。

**Architecture:** 决策 B（甲）——抽屉只做共享编辑器 UI；页签/小计公式落 `component.formulas`（token，复用已锁定求值器 + 保存前可映射校验），EXCEL 列公式落新 `excel_columns`（字符串，复用 tab-join 求值器）。**不改动**已终态锁定的组件 token 求值器与报价/核价/详情渲染读取链。

**Tech Stack:** Java 17 + Quarkus 3.23 + Hibernate Panache + Flyway + PostgreSQL 16（后端）；React + Ant Design + Vite（前端）；Playwright（E2E）。

**Spec:** `docs/superpowers/specs/2026-06-10-组件管理公式统一-design.md`（v4，三轮评审定稿）
**原型:** `docs/html/组件管理-公式统一-原型.html`

---

## ⚠️ 执行前置约束（CLAUDE.md 强制）

- **必须先建隔离 worktree 分支**（`superpowers:using-git-worktrees`），所有编码在该 worktree 内进行，不直接改 master。
- worktree 只隔离 git 工作区；后端 8081 / 前端 5174 dev server、远程 DB、`node_modules`、`.codegraph/` 共享——**复用主工作区已运行实例**做自检，不另起 server / 重装依赖。
- 提交只 `git add` 本次明确改动文件，**禁止 `git add -A`**。
- 每阶段结束跑「修改后强制自检」；协议级改动跑 E2E 双 spec。
- Flyway 文件放进 `db/migration/` 后 **touch 一个 java 文件触发 Quarkus 重启自动迁移**，禁止手工 `psql -f`。

---

## 文件结构总览（创建/修改）

**后端**
- 创建：`db/migration/V298__component_type_excel_and_excel_columns.sql`（CHECK 约束 + `excel_columns` 列）
- 创建：`com/cpq/component/resource/ComponentTabJoinResource.java`（组件级 tab-defs / sample-cards / dry-run 端点）
- 创建：`com/cpq/component/service/ComponentTabDefService.java`（按 directoryId 聚合兄弟组件 → tabDefs）
- 创建：`com/cpq/component/formula/TokenMappabilityValidator.java`（页签/小计公式可映射校验）
- 修改：`Component.java`（+`excelColumns` 字段）、`ComponentService.java`（validateRequest 加 componentType 枚举 + excel_columns 持久化 + 校验钩子）、`ComponentDTO.java`、`ComponentExportBundle.java`（+excel_columns）
- 修改（component_type 三态）：`ComponentDriverService.java` / `CardSnapshotService.java` / `SnapshotCollectorService.java` / `TemplateService.java#refreshSnapshotsByComponent`
- 修改（Excel 归属迁移读取源）：`ExcelViewService.java` / `TemplateExcelViewResource.java` / `ImportExecutionService.java` / `ImportMappingTemplateService.java` / `TemplateService.java`(克隆/校验) / `TemplateFormulaService.java` / `LegacyPathsResource.java` / `TemplateDTO.java`

**前端**
- 修改：`pages/component/types.ts`（`ComponentType` 加 `'EXCEL'`、字段类型联动）、`ComponentManagement.tsx`（重构 Master-Detail + 移除 FieldPanel + 合并配置行 + 树表隐藏）、`FieldConfigTable.tsx`（去 BNF_PATH 子类型 UI）、`PathPickerDrawer.tsx`（去 manual Tab）、`FormulaBuilder.tsx`（去结果类型列 / 改为列表 + 抽屉入口）
- 修改：`pages/template/TabJoinFormulaDrawer.tsx`（props 从 templateId → directoryId/componentId）、`tabJoinFormulaService.ts`（端点路由）、`ComponentPalette.tsx`（三态分组）、`ExcelViewConfigTab.tsx`（退化为引用 EXCEL 组件）
- 删除：`pages/component/FieldPanel.tsx`
- 新增 E2E：复用 `e2e/quotation-flow.spec.ts` + `composite-product-flow.spec.ts`

---

## 阶段总览与依赖

```
阶段0(门禁: 导入定调 + 可映射spike) ──┬──> 阶段1(DB基座)
                                      │
阶段1 ──> 阶段2(component_type三态) ──┴──> 阶段3(Excel归属迁移)*  ──> 阶段5(前端双页)
阶段2 ──> 阶段4(抽屉重定位+新端点) ───────────────────────────────> 阶段5
阶段5 ──> 阶段6(BNF_PATH UI收敛) ──> 阶段7(E2E+自检)

* 阶段3 的导入链分支由阶段0定调；阶段5 的页签/小计公式落库由阶段0可映射结论定稿。
```

---

# 阶段 0：门禁（spike + 两个决策）— 阻塞后续

**目的**：在写迁移/UI 前，定死两个会改写后续代码的结论。产出是**文档结论 + 一个可映射校验器的测试基线**，不写业务迁移。

### Task 0.1：导入链归属定调（决策，写进 spec 演进史）

**Files:**
- Modify: `docs/superpowers/specs/2026-06-10-组件管理公式统一-design.md`（§3.1 末尾加"导入链定调结论"）

- [ ] **Step 1: 读现状确认分叉点**

Run: `grep -n "import_settings\|excelViewConfig" cpq-backend/src/main/java/com/cpq/importexcel/service/ImportExecutionService.java`
确认 `:374-383` 同一 JSONB 既读 `import_settings` 又读 `columns`。

- [ ] **Step 2: 定调并记录**

决策基线（除非有反证，按此定）：**导入链继续读 `template.excelViewConfig.import_settings`（留在 template）**，仅"列定义"归属迁到 EXCEL 组件。理由：`import_settings`（sheet/header/行偏移/part_no 列）是**模板-导入**绑定，非组件属性；强行迁组件会牵动 `ImportExecutionService`/`ImportMappingTemplateService` 4+ 处且语义不属组件。
→ 故阶段3只切"列定义"读取源，**不动**导入链对 `import_settings` 的读取。把该结论写进 §3.1。

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/specs/2026-06-10-组件管理公式统一-design.md
git commit -m "docs(component): 阶段0 导入链定调—import_settings留template,仅列定义迁组件"
```

### Task 0.2：页签/小计公式 token 可映射校验器（spike + 测试基线）

**Files:**
- Create: `cpq-backend/src/main/java/com/cpq/component/formula/TokenMappabilityValidator.java`
- Test: `cpq-backend/src/test/java/com/cpq/component/formula/TokenMappabilityValidatorTest.java`

校验规则（依据 spec §3.2）：一条页签/小计公式（`FormulaToken[]`）**可映射** ⟺ 其每个跨页签引用都是**单源**（`cross_tab_ref` 单 source、match 锚定本组件当前行），不存在"2+ 外部页签互相 row-key 对齐"的结构。本校验器在保存期对 token 数组判定，给出可映射 / 不可映射 + 原因。

- [ ] **Step 1: 写失败测试（覆盖可映射 + 不可映射两类）**

```java
package com.cpq.component.formula;

import org.junit.jupiter.api.Test;
import java.util.List;
import java.util.Map;
import static org.junit.jupiter.api.Assertions.*;

class TokenMappabilityValidatorTest {

    private final TokenMappabilityValidator v = new TokenMappabilityValidator();

    // 同行字段 + 单源跨页签 → 可映射
    @Test
    void sameRowFieldsPlusSingleSourceCrossTab_isMappable() {
        List<Map<String,Object>> expr = List.of(
            Map.of("type","field","value","单重"),
            Map.of("type","operator","value","*"),
            Map.of("type","field","value","单价"),
            Map.of("type","operator","value","-"),
            Map.of("type","cross_tab_ref","source","回料","agg","SUM",
                   "match", List.of(Map.of("a","料号","b","料号")))
        );
        var r = v.validate(expr);
        assertTrue(r.mappable(), r.reason());
    }

    // 两个不同源 cross_tab_ref 顶层相乘(互相对齐,非锚定本组件) → 不可映射
    @Test
    void twoForeignDetailsMultiplied_isNotMappable() {
        List<Map<String,Object>> expr = List.of(
            Map.of("type","cross_tab_ref","source","投料","agg","NONE",
                   "match", List.of(Map.of("a","料号","b","料号"))),
            Map.of("type","operator","value","*"),
            Map.of("type","cross_tab_ref","source","回料","agg","NONE",
                   "match", List.of(Map.of("a","料号","b","料号")))
        );
        var r = v.validate(expr);
        assertFalse(r.mappable());
        assertNotNull(r.reason());
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=TokenMappabilityValidatorTest`
Expected: FAIL（`TokenMappabilityValidator` 不存在 / 编译错）

- [ ] **Step 3: 实现校验器（最小）**

```java
package com.cpq.component.formula;

import jakarta.enterprise.context.ApplicationScoped;
import java.util.List;
import java.util.Map;

@ApplicationScoped
public class TokenMappabilityValidator {

    public record Result(boolean mappable, String reason) {}

    /**
     * 判定一条页签/小计公式 token 数组是否可落进单源 cross_tab_ref 模型。
     * 规则：不允许出现 ≥2 个 agg=NONE 的 cross_tab_ref（裸明细外部引用）在同一表达式里——
     * 这意味着多外部页签互相 row-key 对齐，token 模型无宿主锚点，不可映射。
     * 单源（含聚合）跨页签 + 任意本组件 field/number/operator 均可映射。
     */
    public Result validate(List<Map<String, Object>> expr) {
        long bareForeignDetails = expr.stream()
            .filter(t -> "cross_tab_ref".equals(t.get("type")))
            .filter(t -> "NONE".equals(String.valueOf(t.getOrDefault("agg", "NONE"))))
            .count();
        if (bareForeignDetails >= 2) {
            return new Result(false,
                "存在 2+ 个未聚合的跨页签明细引用（多外部页签互相对齐），" +
                "token 模型无宿主行锚点。此类复杂多表连表请改用 Excel 组件。");
        }
        return new Result(true, null);
    }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=TokenMappabilityValidatorTest`
Expected: PASS（2 用例绿）

- [ ] **Step 5: 用真实页签公式样本验证覆盖面（spike 结论）**

Run: `grep -rn "cross_tab_ref" cpq-frontend/src/pages --include=*.ts* | head`
人工抽取现有报价/核价模板里页签组件的公式样本（≥5 条），逐条喂 `validate()`，确认"绝大多数可映射、不可映射的确属 Excel 整表场景"。把结论（覆盖率 + 不可映射样本类型）写进 spec §3.2 注脚。
> 若发现某类**常见**页签公式不可映射且属刚需 → 停下，回 architect 评估是否局部上 (乙)，不要硬继续。

- [ ] **Step 6: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/component/formula/TokenMappabilityValidator.java \
        cpq-backend/src/test/java/com/cpq/component/formula/TokenMappabilityValidatorTest.java \
        docs/superpowers/specs/2026-06-10-组件管理公式统一-design.md
git commit -m "feat(component): 阶段0 token可映射校验器 + spike覆盖结论"
```

**🚦 门禁判定**：Task 0.1 导入定调记录 + Task 0.2 spike 覆盖率达标（无常见页签公式落不进 token）→ 放行阶段 1+。否则回 architect。

---

# 阶段 1：DB 基座（CHECK 约束 + excel_columns 列）

### Task 1.1：Flyway 迁移——放开 component_type 枚举 + 加 excel_columns

**Files:**
- Create: `cpq-backend/src/main/resources/db/migration/V298__component_type_excel_and_excel_columns.sql`
- Modify: `cpq-backend/src/main/java/com/cpq/component/entity/Component.java`

- [ ] **Step 1: 写迁移 SQL**

```sql
-- V298: 放开 component_type 支持 EXCEL；新增 excel_columns 承载 EXCEL 组件列定义。
-- 背景: V22 的 chk_component_type 仅允许 NORMAL/SUBTOTAL，会挡 EXCEL 插入。
ALTER TABLE component DROP CONSTRAINT IF EXISTS chk_component_type;
ALTER TABLE component ADD CONSTRAINT chk_component_type
    CHECK (component_type IN ('NORMAL', 'SUBTOTAL', 'EXCEL'));

-- EXCEL 组件列定义（{col_key,title,source_type,hidden,formula,sort} 数组）。
-- 非 EXCEL 组件保持 '[]'。
ALTER TABLE component ADD COLUMN IF NOT EXISTS excel_columns jsonb NOT NULL DEFAULT '[]';
```

- [ ] **Step 2: 实体加字段**

在 `Component.java` 的 `formulas` 字段后加：

```java
    /** EXCEL 组件列定义（component_type=EXCEL 时有效）：{col_key,title,source_type,hidden,formula,sort} 数组。 */
    @JdbcTypeCode(SqlTypes.JSON)
    @Column(name = "excel_columns", columnDefinition = "jsonb", nullable = false)
    public String excelColumns = "[]";
```

- [ ] **Step 3: 触发迁移并验证**

Run: `touch cpq-backend/src/main/java/com/cpq/component/entity/Component.java`（已改即已 touch），等 5-7 秒 Quarkus 重启。
Run: `PGPASSWORD=$DB_PW psql "$DB_URL" -c "SELECT version, success FROM flyway_schema_history WHERE version='298'"`
Expected: `success = t`

- [ ] **Step 4: 验证 EXCEL 可插入**

Run:
```bash
PGPASSWORD=$DB_PW psql "$DB_URL" -c "INSERT INTO component(id,name,code,column_count,fields,formulas,component_type,status,excel_columns) VALUES (gen_random_uuid(),'t_excel','TST-EXCEL-1',0,'[]','[]','EXCEL','ACTIVE','[]') RETURNING id;"
PGPASSWORD=$DB_PW psql "$DB_URL" -c "DELETE FROM component WHERE code='TST-EXCEL-1';"
```
Expected: INSERT 返回 id（不报 check_violation）

- [ ] **Step 5: Commit**

```bash
git add cpq-backend/src/main/resources/db/migration/V298__component_type_excel_and_excel_columns.sql \
        cpq-backend/src/main/java/com/cpq/component/entity/Component.java
git commit -m "feat(db): V298 放开component_type=EXCEL + 加excel_columns列"
```

### Task 1.2：ComponentService 校验 componentType 枚举 + 持久化 excel_columns

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/component/service/ComponentService.java`（`validateRequest` + create/update 持久化）
- Modify: `cpq-backend/.../dto/CreateComponentRequest.java`（+`excelColumns` / `componentType`）
- Modify: `cpq-backend/.../dto/ComponentDTO.java`（+`excelColumns`）
- Test: `cpq-backend/src/test/java/com/cpq/component/service/ComponentTypeValidationTest.java`

- [ ] **Step 1: 写失败测试**

```java
package com.cpq.component.service;

import com.cpq.common.exception.BusinessException;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class ComponentTypeValidationTest {
    @Test
    void rejectsUnknownComponentType() {
        var ex = assertThrows(BusinessException.class,
            () -> ComponentService.assertValidComponentType("WEIRD"));
        assertTrue(ex.getMessage().contains("component_type"));
    }
    @Test
    void acceptsExcel() {
        assertDoesNotThrow(() -> ComponentService.assertValidComponentType("EXCEL"));
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=ComponentTypeValidationTest`
Expected: FAIL（`assertValidComponentType` 不存在）

- [ ] **Step 3: 实现静态校验 + 接进 validateRequest**

在 `ComponentService` 加：

```java
    public static final java.util.Set<String> VALID_COMPONENT_TYPES =
        java.util.Set.of("NORMAL", "SUBTOTAL", "EXCEL");

    public static void assertValidComponentType(String type) {
        String t = type == null ? "NORMAL" : type;
        if (!VALID_COMPONENT_TYPES.contains(t)) {
            throw new BusinessException("Invalid component_type: " + t +
                ". Must be one of: " + VALID_COMPONENT_TYPES);
        }
    }
```

在 `validateRequest(CreateComponentRequest request)` 末尾加：`assertValidComponentType(request.componentType);`

- [ ] **Step 4: create/update 持久化 excel_columns**

在 create/update 把 `request.excelColumns`（缺省 `"[]"`）写入 `entity.excelColumns`；DTO 回填 `dto.excelColumns = entity.excelColumns;`。`CreateComponentRequest` 加 `public String componentType; public String excelColumns;`。

- [ ] **Step 5: 运行测试 + 后端自检**

Run: `cd cpq-backend && ./mvnw -q test -Dtest=ComponentTypeValidationTest`
Expected: PASS
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/q/health` → 200

- [ ] **Step 6: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/component/service/ComponentService.java \
        cpq-backend/src/main/java/com/cpq/component/dto/CreateComponentRequest.java \
        cpq-backend/src/main/java/com/cpq/component/dto/ComponentDTO.java \
        cpq-backend/src/test/java/com/cpq/component/service/ComponentTypeValidationTest.java
git commit -m "feat(component): 校验component_type枚举 + 持久化excel_columns"
```

---

# 阶段 2：component_type=EXCEL 三态传播（前后端）

> 写前必跑 `codegraph_impact componentType` 列全命中清单，逐处对照勾。本阶段每个改点都要确保 EXCEL **不被当 NORMAL** 渲染/求值。

### Task 2.1：前端 ComponentType 联合类型加 'EXCEL'

**Files:**
- Modify: `cpq-frontend/src/pages/component/types.ts:12`

- [ ] **Step 1: 改类型**

```typescript
export type ComponentType = 'NORMAL' | 'SUBTOTAL' | 'EXCEL';
```

- [ ] **Step 2: TS 编译自检**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误（若二元 switch 报 exhaustive，进 Task 2.2~2.4 补分支）

- [ ] **Step 3: Commit**

```bash
git add cpq-frontend/src/pages/component/types.ts
git commit -m "feat(component): ComponentType 加 EXCEL"
```

### Task 2.2：前端渲染二分点显式三态化

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/QuotationStep2.tsx`（行 ~762/908/960/1482 的 `!== 'SUBTOTAL'`）
- Modify: `cpq-frontend/src/pages/quotation/enrichComponentData.ts:279`
- Modify: `cpq-frontend/src/pages/template/ComponentPalette.tsx:96-97`
- Modify: `cpq-frontend/src/components/.../ReadonlyProductCard.tsx`、`ComponentCell.tsx`

- [ ] **Step 1: 逐处把二分改三态**

原则：`!== 'SUBTOTAL'` 的"按普通页签渲染/driver-expand"分支，改为 `=== 'NORMAL'`（EXCEL 不进该分支）；`ComponentPalette` 的 `normalComps` 过滤改为 `=== 'NORMAL'`，新增 `excelComps = componentType === 'EXCEL'`，渲染三组。`ReadonlyProductCard`/`ComponentCell` 加 `=== 'EXCEL'` 分支（AP-50：不加会复用 NORMAL 出僵尸数据；EXCEL 在报价/核价卡里不作为普通页签渲染，按其形态或跳过）。

> 具体每处代码在执行时按该文件上下文写；改完逐文件 `curl` Vite 200。

- [ ] **Step 2: 自检**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json` → 0 错误
对每个改动 `.tsx`：`curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/<相对路径>` → 200

- [ ] **Step 3: Commit**（只 add 本次改动文件）

### Task 2.3：后端三态识别（DriverService / 快照 / 导出）

**Files:**
- Modify: `ComponentDriverService.java`（EXCEL 是否参与 driver-expand：默认**跳过**——EXCEL 列自带取数，不走页签 driver）
- Modify: `CardSnapshotService.java#assembleTabsWithFormulaResults`、`SnapshotCollectorService.java`、`TemplateService.java#refreshSnapshotsByComponent`
- Modify: `ComponentExportBundle.java:33`（Item 加 `excelColumns`，进出 bundle 不丢列）
- Test: `cpq-backend/src/test/java/com/cpq/component/ComponentExportBundleExcelTest.java`

- [ ] **Step 1: 写 export bundle 含 excel_columns 的失败测试**

```java
// 断言：导出一个 EXCEL 组件后再导入，excel_columns 不丢。
// （按现有 ComponentExportBundle 测试风格写 round-trip 断言）
```

- [ ] **Step 2~4: 实现 + 跑测试 + 后端 health 自检**（touch java 重启，curl /q/health 200）

- [ ] **Step 5: Commit**

---

# 阶段 3：Excel 配置归属迁移（11 文件读取源切换）

> **依赖阶段 0 导入定调**：按 Task 0.1 结论——`import_settings` 留 template、仅"列定义"读取源切到 EXCEL 组件。逐处按 spec §3.1 改造表处理。

### Task 3.1：模板 Excel 视图改为"引用 EXCEL 组件"

**Files:**
- Modify: `ExcelViewService.java`（`getExcelViewConfig`/`buildExcelView`/`exportExcelView`/`tabDefsOfTemplate` 列定义读取源 → EXCEL 组件 `excel_columns`）
- Modify: `TemplateExcelViewResource.java`、`ExcelViewConfigTab.tsx`（前端改为"选/引用 EXCEL 组件 + 列覆写"）
- Modify: `TemplateService.java:437`（草稿克隆改克隆引用关系）、`:1043-1050`（$$ 校验随读源）
- Modify: `CardSnapshotService.java:310-338`、`SnapshotCollectorService.java:332-354`（快照读取源）
- Modify: `TemplateFormulaService.java:370,935`（col_key fallback 随读源）、`LegacyPathsResource.java`、`TemplateDTO.java`

- [ ] 每个读取点：定位 → 切到读 EXCEL 组件列定义 → 保留 `import_settings` 对 template 的读取不动 → 单测/集成测试覆盖 → 提交。
- [ ] **隐藏列语义**：渲染/导出/单元格回写快照三侧统一：`hidden=true` 列参与计算、不出现在视图/导出（`updateExcelViewCell` 重建快照时同样过滤）。
- [ ] **数据迁移**：历史 `excelViewConfig` 为测试数据，重建即可；提供一个一次性脚本/迁移把现有模板列定义搬成 EXCEL 组件（或直接重建测试模板）。

> 验收：模板 Excel 视图拖入 EXCEL 组件可渲染、可导出（隐藏列不出现）、**导入功能不回归**（`import_settings` 路径未受影响）。

---

# 阶段 4：TabJoinFormulaDrawer 重定位（目录级新端点）

### Task 4.1：后端组件级 tab-defs 端点

**Files:**
- Create: `com/cpq/component/service/ComponentTabDefService.java`（按 directoryId 取同目录其他组件 → tabDefs，别名用 component `code`，显示用 name）
- Create/Modify: `com/cpq/component/resource/ComponentTabJoinResource.java`（`GET /api/cpq/components/{id}/tab-defs`）
- Test: `ComponentTabDefServiceTest.java`

- [ ] **Step 1**: 写测试——同目录 3 组件，请求其中一个的 tab-defs，断言返回另外两个（含本组件，排除自身或标记本组件），别名=code。
- [ ] **Step 2~4**: 实现（参考 `ExcelViewService.tabDefsOfTemplate:746` 的 tabDefs 形状，数据源换成 `Component.find("directoryId", dirId)`）+ 跑测试 + curl 端点 200/401。
- [ ] **Step 5**: Commit。

### Task 4.2：后端组件级 sample-cards + dry-run 端点

**Files:**
- Modify: `ComponentTabJoinResource.java`（`GET .../sample-cards`、`POST .../dry-run`）
- Modify: 试算服务（复用 tab-join dryRun 内核，上下文换组件）

- [ ] sample-cards 反查：`QuotationLineComponentData.componentId → lineItemId → quotation`（spec §4.2 路径），返回可选样本列表；无样本返回空。
- [ ] dry-run：给定组件 + 表达式 + 样本卡，复用现有 tab-join 求值跑单值；无样本返回"禁用试算"提示。
- [ ] 测试 + curl 自检 + Commit。

### Task 4.3：前端抽屉 props 从 templateId → directoryId/componentId

**Files:**
- Modify: `pages/template/TabJoinFormulaDrawer.tsx`（props + 三处 service 调用）
- Modify: `services/tabJoinFormulaService.ts`（`/templates/{id}/...` → `/components/{id}/...` 路由）
- Modify: `SampleCardPicker.tsx`（数据源换组件级 sample-cards）

- [ ] 抽屉接受 `componentId`（推出 directoryId）；矩阵=同目录组件；保存回调按组件类型分流（页签/小计 → token 序列化 + 可映射校验；EXCEL → 字符串）。
- [ ] **去结果类型列**（已在原型）；标题/占位参数化。
- [ ] TS 编译 + Vite 200 自检 + Commit。

### Task 4.4：token 序列化桥（抽屉表达式 ↔ component.formulas token）

**Files:**
- Create: `cpq-frontend/src/pages/component/formulaSerialize.ts`（+ 单测 `formulaSerialize.test.ts`）

- [ ] 写测试：抽屉表达式字符串 ↔ `FormulaToken[]` 双向；不可映射时抛/标记（前端先 `TokenMappabilityValidator` 同语义校验，后端二次校验）。
- [ ] 实现最小双向序列化（单源 cross_tab_ref + field + number + operator）。
- [ ] `npx vitest run formulaSerialize` 绿 + Commit。

---

# 阶段 5：组件管理双页 UI（Master-Detail）

> 依赖阶段 0 可映射结论 + 阶段 4 抽屉。按原型 `docs/html/组件管理-公式统一-原型.html` 实现。

### Task 5.1：ComponentManagement 改 Master-Detail 布局

**Files:**
- Modify: `pages/component/ComponentManagement.tsx`（左手风琴目录→三段卡片；右内嵌详情；移除 `<FieldPanel>`；树表隐藏；数据驱动路径+核价BOM合并一行）
- Delete: `pages/component/FieldPanel.tsx`

- [ ] 左栏：搜索/新建/批量动作（**走 `SelectableTable` 的 `runBatch` + 危险动作 Modal 列项二次确认**，不用裸 checkbox）；手风琴目录 → 三段卡片（页签/EXCEL/小计），卡片复用 `tm-component-item` 渐变样式。
- [ ] 右栏：选中组件内嵌详情；空态提示。三态详情形态（页签：合并配置行+字段/公式/SQL视图 Tab；EXCEL：Excel 列配置+显示开关；小计：仅公式）。
- [ ] 公式 Tab：列表 → 点编辑/添加弹 `TabJoinFormulaDrawer`（componentId 上下文）；**无结果类型列**。
- [ ] 字段配置：复用 `FieldConfigTable`（金额/小计/行键/备注 不动）。
- [ ] 移除 `<FieldPanel>` 及其 import / 相关回调（`handleFieldClick`/`onGlobalVariableClick` 等）。删 `FieldPanel.tsx`。
- [ ] TS 0 错误 + 每个改动 tsx Vite 200 + Commit。

### Task 5.2：模板配置 ComponentPalette 三组 + Excel 视图拖入 EXCEL 组件

**Files:**
- Modify: `pages/template/ComponentPalette.tsx`（三组）、`ExcelViewConfigTab.tsx`（拖入 EXCEL 组件即用）

- [ ] 调色板渲染页签/EXCEL/小计三组；Excel 视图为单组件容器，拖入 EXCEL 组件、列与公式只读引用。
- [ ] 自检 + Commit。

---

# 阶段 6：BNF_PATH UI 收敛（删1保2）

### Task 6.1：移除 datasource_binding 的 BNF_PATH 子类型 UI + PathPicker manual Tab

**Files:**
- Modify: `pages/component/FieldConfigTable.tsx`（去 `dsType==='BNF_PATH'` 选项 + 配置入口，行 57/153/181/372/548）
- Modify: `pages/component/PathPickerDrawer.tsx`（移除 manual Tab，只留 SQL 视图 Tab）

- [ ] **保留不动**（评审 🔴-5）：`DefaultSourceEditor.tsx` 的 `default_source.type==='BNF_PATH'`、后端 `BnfPathResolver` SPI、渲染层 `$view.列` 求值。
- [ ] 后端 `ComponentService.validateRequest` 的 DATA_SOURCE `case "BNF_PATH"` 可保留（兼容历史），但前端不再产出该子类型。
- [ ] 删除历史含裸表路径的测试组件/模板数据。
- [ ] TS 0 错误 + Vite 200 + 后端 health 200 + Commit。

---

# 阶段 7：E2E + 强制自检

### Task 7.1：E2E 双 spec + 三视图渲染证据

- [ ] 跑 `e2e/quotation-flow.spec.ts`（SIMPLE）+ `composite-product-flow.spec.ts`（COMPOSITE）：
```bash
cd cpq-frontend
npx playwright test --config=e2e/playwright.config.ts e2e/quotation-flow.spec.ts --reporter=list
```
- [ ] 必须：所有 test `passed`、`'加载中' final count = 0`、全 8 Tab `'加载中'=0`。
- [ ] 附 qf-19 + qf-21~28 截图；报价单 / 核价单 / 详情页三视图关键 Tab 截图（EXCEL 组件渲染正确、页签公式渲染正确）。
- [ ] `codegraph_impact componentType` 全清单逐处确认已勾（PR 自检矩阵）。
- [ ] `POST /api/cpq/components/{id}/refresh-template-snapshots` 跑过，各 Tab snapshot.fields 独立保留。

### Task 7.2：RECORD 回写 + 收尾

- [ ] 按格式追加 `docs/RECORD.md`：`[2026-06-10] 组件管理 - 公式统一+Excel收编+Master-Detail | 涉及文件... | 决策B(甲)/import_settings留template/component_type三态`。
- [ ] 用户确认达标后走 `superpowers:finishing-a-development-branch`：切 master → merge 特性分支 → 跑测试 → `git worktree remove` 删 worktree + 删分支。

---

## 自检清单（写计划者自查）

- **Spec 覆盖**：点1(Excel收编→阶段1/3/5)、点2(统一抽屉→阶段0/4)、点3(Master-Detail→阶段5)、点4(移除FieldPanel→5.1)、点5(精简→5.1)、点6(BNF收敛→阶段6)、决策A(同目录页签集→4.1)、🔴DB约束→1.1、决策B甲→0.2/4.4。✅ 全覆盖。
- **门禁**：阶段0 gate 住阶段3(导入定调)、阶段5(可映射结论)。✅
- **类型一致**：`assertValidComponentType` / `excelColumns` / `TokenMappabilityValidator.Result` / `ComponentTabDefService` 命名前后一致。✅
- **YAGNI**：全局变量插入、(乙)完整连表下放、A方案——均排除。✅

> 阶段 3/5 的逐行代码在阶段 0 门禁结论落定后于执行期按文件上下文补全（已标注分支点），不在此预写依赖门禁结论的实现细节。
