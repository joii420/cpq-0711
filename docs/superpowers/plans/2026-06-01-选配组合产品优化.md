# 选配组合产品优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在「新建报价单 → 选配添加 → 组合产品」流程中加入配件数量编辑、简化组合工艺选择、工序/组合工艺带编码模糊搜索、配件进度导航 4 项优化。

**Architecture:** 纯前端交互改造 + 后端一处 `composition_qty` 落库去硬编码。选配只负责数据入库（配件数量写 `material_bom_item.composition_qty`），不做任何系统自动计算。无 Flyway 迁移（列已存在）。

**Tech Stack:** 前端 React + Ant Design + TypeScript（Vite）；后端 Quarkus + Hibernate Panache（原生 SQL）；PostgreSQL 16。

**Spec:** `docs/superpowers/specs/2026-06-01-选配组合产品优化-design.md`

---

## 本仓库的「测试」约定（重要）

本项目**没有前端单元测试框架**。前端改动的验证手段（依据 `CLAUDE.md` 强制自检）是：

1. `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json` → 0 错误
2. 对每个改动/新增 `.tsx` 跑 `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/<相对路径>` → HTTP 200
3. 协议级/向导链路改动跑 Playwright E2E（`composite-product-flow.spec.ts`）

后端改动验证：`touch` 一个 java 文件触发 Quarkus 重启 → `curl /q/health` 200 → 真实选配后用 `psql` 查 `material_bom_item.composition_qty` 落库值。

因此下面每个任务的「验证」步骤即采用上述手段，而非 `pytest`/`jest`。这是刻意遵循现有代码库纪律，不要引入新的单元测试框架。

> 注：Vite dev server 端口以仓库实际为准（`CLAUDE.md` 自检示例用 5174，部分文档用 5173）。执行前先 `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/` 确认哪个端口返回 200，后续 curl 用该端口。

---

## File Structure

### 前端 `cpq-frontend/src/`

| 文件 | 职责 | 动作 |
|------|------|------|
| `types/configure.ts` | 选配请求/响应 TS 类型 | 修改：`PartRequest` 加 `quantity?: number` |
| `pages/quotation/ConfigureProductDrawer.tsx` | 选配抽屉主控（步骤机、状态、提交） | 修改：`PartState` 加 `quantity`；插入「配件数量」步骤；进度导航；提交填默认 participating/params + quantity |
| `pages/quotation/configure/Step0ProductType.tsx` | 产品类型选择 | 修改：把「配件数量」文案改为「配件个数」消歧义 |
| `pages/quotation/configure/StepAccessoryQuantity.tsx` | **新增** 配件清单 + 数量编辑 | 创建 |
| `pages/quotation/configure/AccessoryProgressBar.tsx` | **新增** 顶部配件进度框（可点跳回已完成） | 创建 |
| `pages/quotation/configure/Step3Process.tsx` | 工序选择 | 修改：名称下显示工序编码 + 搜索扩展为 code/name |
| `pages/quotation/configure/Step4CompositeProcess.tsx` | 组合工艺选择 | 修改：去掉参与配件/参数 UI；加搜索框 + 编码显示 |
| `pages/quotation/configure/Step5Summary.tsx` | 汇总确认 | 修改：配件明细显示数量；组合工艺只显示名称 |

### 后端 `cpq-backend/src/main/java/com/cpq/configure/`

| 文件 | 职责 | 动作 |
|------|------|------|
| `dto/PartRequest.java` | 单配件请求 DTO | 修改：加 `public Integer quantity;` |
| `service/ConfigureProductService.java` | 选配落库服务 | 修改：`insertMaterialBomAssemblyV6` 接收数量并 upsert `composition_qty` |

无新增数据库迁移。

---

## Task 1: 后端 — PartRequest 加 quantity 字段

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/configure/dto/PartRequest.java`

- [ ] **Step 1: 在 PartRequest 加 quantity 字段**

在 `quotationLineItemId` 字段之后、类结束 `}` 之前插入：

```java
    /**
     * 配件组成用量（仅 COMPOSITE 子件用）。写入 material_bom_item.composition_qty。
     * 正整数，前端默认 1；null 时后端兜底为 1。SIMPLE 场景忽略。
     */
    public Integer quantity;
```

- [ ] **Step 2: 编译验证**

Run: `touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java`
等待 5-7 秒 Quarkus 重启，然后：
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/q/health`
Expected: `200`（DTO 仅加字段不会破坏启动）

- [ ] **Step 3: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/configure/dto/PartRequest.java
git commit -m "feat(configure): add quantity field to PartRequest DTO"
```

---

## Task 2: 后端 — insertMaterialBomAssemblyV6 写入每配件 composition_qty

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java`

当前 `insertMaterialBomAssemblyV6`（约 635-650 行）把 `composition_qty` 硬编码为 1，且调用处（约 828 行）只传 `childHfPartNos`。本任务让其按配件传入数量，并在指纹复用场景也能更新数量（`ON CONFLICT ... DO UPDATE`，匹配 `uq_material_bom_item` 表达式索引）。

- [ ] **Step 1: 改方法签名与 SQL（去硬编码 + upsert）**

把整个 `insertMaterialBomAssemblyV6` 方法替换为：

```java
    /**
     * V6: 组合子件 → material_bom_item（characteristic='ASSEMBLY'，component_no=子料号）。
     * 让 zcj_bom / composite_child_materials_mirror 渲染子配件清单。
     * composition_qty = 用户在选配「配件数量」步骤填的组成用量（正整数，默认 1）。
     * 指纹复用同一父料号时也更新数量（ON CONFLICT DO UPDATE，匹配 uq_material_bom_item 表达式索引）。
     */
    void insertMaterialBomAssemblyV6(String parentPartNo, String customerCode,
                                     List<String> childPartNos, List<Integer> quantities) {
        if (customerCode == null || customerCode.isBlank()) return;
        int seq = 1;
        for (int i = 0; i < childPartNos.size(); i++) {
            String childPn = childPartNos.get(i);
            int qty = (quantities != null && i < quantities.size() && quantities.get(i) != null
                       && quantities.get(i) >= 1)
                ? quantities.get(i) : 1;
            em.createNativeQuery(
                    "INSERT INTO material_bom_item (system_type, customer_no, material_no, " +
                    "characteristic, seq_no, component_no, composition_qty) " +
                    "VALUES ('QUOTE', :cn, :p, 'ASSEMBLY', :sq, :c, :q) " +
                    "ON CONFLICT (system_type, customer_no, material_no, " +
                    "COALESCE(characteristic, ''), COALESCE(seq_no, 0), " +
                    "COALESCE(component_no, ''), COALESCE(part_no, '')) " +
                    "DO UPDATE SET composition_qty = EXCLUDED.composition_qty")
                .setParameter("cn", customerCode)
                .setParameter("p", parentPartNo)
                .setParameter("sq", seq++)
                .setParameter("c", childPn)
                .setParameter("q", qty)
                .executeUpdate();
        }
    }
```

- [ ] **Step 2: 改调用处传入数量**

在 `configureProductTransactional` 中，定位（约 828 行）：

```java
            insertMaterialBomAssemblyV6(parentHfPartNo, customerCode, childHfPartNos);
```

替换为：

```java
            List<Integer> childQtys = req.parts.stream()
                .map(pr -> (pr.quantity == null || pr.quantity < 1) ? 1 : pr.quantity)
                .collect(java.util.stream.Collectors.toList());
            insertMaterialBomAssemblyV6(parentHfPartNo, customerCode, childHfPartNos, childQtys);
```

> 说明：PASS 1 按 `req.parts` 顺序逐个 `resolvePart` 填 `childHfPartNos`，二者 index 1:1 对齐，故 `childQtys.get(i)` 与 `childPartNos.get(i)` 同指一个配件。

- [ ] **Step 3: 编译/重启验证**

Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/q/health`
Expected: `200`（等 Quarkus 重启完成后）

如果看到 500，`curl -s http://localhost:8081/q/health` 看异常栈，常见是 `ON CONFLICT` 表达式与索引不完全一致——务必逐字符比对 `uq_material_bom_item`（V219 第 99-106 行）。

- [ ] **Step 4: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java
git commit -m "feat(configure): write per-accessory composition_qty in V6 assembly BOM"
```

---

## Task 3: 前端 — types & PartState 加 quantity

**Files:**
- Modify: `cpq-frontend/src/types/configure.ts:12-22`
- Modify: `cpq-frontend/src/pages/quotation/ConfigureProductDrawer.tsx:16-27` 和 `:60-74`

- [ ] **Step 1: PartRequest 加 quantity**

在 `cpq-frontend/src/types/configure.ts` 的 `PartRequest` interface 内，`quotationLineItemId?: string;` 之后加一行：

```typescript
  /** 配件组成用量（仅 COMPOSITE 子件用），写入 material_bom_item.composition_qty。正整数，默认 1。 */
  quantity?: number;
```

- [ ] **Step 2: PartState 加 quantity**

在 `ConfigureProductDrawer.tsx` 的 `PartState` interface 内，`reusedFromExisting: ...` 之后加一行：

```typescript
  quantity: number;
```

- [ ] **Step 3: initParts 初始化 quantity=1**

在 `initParts` 的返回对象里（`reusedFromExisting: null,` 之后）加一行：

```typescript
      quantity: 1,
```

- [ ] **Step 4: 验证编译**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误（此时 quantity 仅声明+初始化，尚未在 UI 使用，不应报错）

- [ ] **Step 5: Commit**

```bash
git add cpq-frontend/src/types/configure.ts cpq-frontend/src/pages/quotation/ConfigureProductDrawer.tsx
git commit -m "feat(configure): add quantity to PartState and PartRequest types"
```

---

## Task 4: 前端 — 新增 StepAccessoryQuantity 配件数量编辑组件

**Files:**
- Create: `cpq-frontend/src/pages/quotation/configure/StepAccessoryQuantity.tsx`

- [ ] **Step 1: 创建组件文件**

```tsx
import React from 'react';
import { Card, InputNumber, Tag, Descriptions, Typography } from 'antd';
import type { PartState } from '../ConfigureProductDrawer';

interface Props {
  parts: PartState[];
  onUpdatePart: (idx: number, patch: Partial<PartState>) => void;
}

/**
 * 配件清单 + 数量编辑（仅 COMPOSITE）。
 * 在进入「组合工艺」前，让用户为每个已选配件设置组成用量（正整数，默认 1）。
 * 数量最终写入 material_bom_item.composition_qty —— 选配只负责入库，不做系统自动计算。
 */
const StepAccessoryQuantity: React.FC<Props> = ({ parts, onUpdatePart }) => (
  <div>
    <Typography.Title level={5} style={{ marginTop: 0 }}>配件清单与数量</Typography.Title>
    <Typography.Paragraph type="secondary" style={{ marginBottom: 16 }}>
      请确认每个配件的组成用量（默认 1，正整数）。该数量用于后续公式计算。
    </Typography.Paragraph>

    {parts.map((p, i) => {
      const partNo = p.selectedHfPartNo
        ?? p.reusedFromExisting?.hfPartNo
        ?? (p.partMode === 'custom' ? '自定义(将新建)' : '—');
      return (
        <Card
          key={i}
          size="small"
          style={{ marginBottom: 12 }}
          title={<><Tag color="purple">{i + 1}</Tag> {p.name}</>}
          extra={
            <span>
              数量:{' '}
              <InputNumber
                min={1}
                step={1}
                precision={0}
                value={p.quantity ?? 1}
                onChange={(v) => onUpdatePart(i, { quantity: v == null || v < 1 ? 1 : Math.floor(v) })}
                style={{ width: 120 }}
              />
            </span>
          }
        >
          <Descriptions size="small" column={1} bordered={false}>
            <Descriptions.Item label="料号">{partNo}</Descriptions.Item>
            <Descriptions.Item label="材质">{p.selectedRecipeSymbol ?? p.selectedRecipeCode ?? '—'}</Descriptions.Item>
          </Descriptions>
        </Card>
      );
    })}
  </div>
);

export default StepAccessoryQuantity;
```

- [ ] **Step 2: 验证编译**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误

- [ ] **Step 3: 验证 Vite transform**

Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/configure/StepAccessoryQuantity.tsx`
Expected: `200`

- [ ] **Step 4: Commit**

```bash
git add cpq-frontend/src/pages/quotation/configure/StepAccessoryQuantity.tsx
git commit -m "feat(configure): add StepAccessoryQuantity component"
```

---

## Task 5: 前端 — 新增 AccessoryProgressBar 配件进度框

配件配置阶段（globalStep 1）顶部显示一排配件 chip：已完成（可点跳回）/ 当前 / 未开始（置灰）。「已完成」= 索引 < `furthestCi`（用户曾经推进到的最远配件之前的都算完成）。

**Files:**
- Create: `cpq-frontend/src/pages/quotation/configure/AccessoryProgressBar.tsx`

- [ ] **Step 1: 创建组件文件**

```tsx
import React from 'react';
import { Tag, Tooltip } from 'antd';
import { CheckCircleFilled, EditOutlined, MinusCircleOutlined } from '@ant-design/icons';
import type { PartState } from '../ConfigureProductDrawer';

interface Props {
  parts: PartState[];
  currentIndex: number;
  /** 用户曾推进到的最远配件下标。index < furthestIndex 视为「已完成」可点跳回。 */
  furthestIndex: number;
  onJump: (idx: number) => void;
}

/**
 * 顶部配件进度框（需求 #4）。
 * - 当前配件：高亮、不可点。
 * - 已完成配件（index < furthestIndex）：绿色对勾、可点跳回修改。
 * - 未开始配件（index >= furthestIndex 且非当前）：置灰、不可点。
 */
const AccessoryProgressBar: React.FC<Props> = ({ parts, currentIndex, furthestIndex, onJump }) => {
  if (parts.length <= 1) return null;
  return (
    <div style={{ display: 'flex', flexWrap: 'wrap', gap: 8, marginBottom: 16 }}>
      {parts.map((p, i) => {
        const isCurrent = i === currentIndex;
        const isDone = i < furthestIndex;
        const clickable = isDone && !isCurrent;
        const icon = isCurrent
          ? <EditOutlined />
          : isDone
            ? <CheckCircleFilled style={{ color: '#52c41a' }} />
            : <MinusCircleOutlined style={{ color: '#bbb' }} />;
        const tip = isCurrent ? '当前配件' : clickable ? '点击跳回此配件修改' : '尚未配置';
        return (
          <Tooltip key={i} title={tip}>
            <Tag
              icon={icon}
              color={isCurrent ? 'processing' : isDone ? 'success' : 'default'}
              onClick={() => { if (clickable) onJump(i); }}
              style={{
                cursor: clickable ? 'pointer' : 'default',
                opacity: !isCurrent && !isDone ? 0.5 : 1,
                padding: '4px 10px',
                fontSize: 13,
              }}
            >
              {p.name}
            </Tag>
          </Tooltip>
        );
      })}
    </div>
  );
};

export default AccessoryProgressBar;
```

- [ ] **Step 2: 验证编译**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误

- [ ] **Step 3: 验证 Vite transform**

Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/configure/AccessoryProgressBar.tsx`
Expected: `200`

- [ ] **Step 4: Commit**

```bash
git add cpq-frontend/src/pages/quotation/configure/AccessoryProgressBar.tsx
git commit -m "feat(configure): add AccessoryProgressBar navigation component"
```

---

## Task 6: 前端 — ConfigureProductDrawer 步骤机改造（插入配件数量步 + 进度导航 + 提交）

这是核心任务：把 globalStep 由 `0|1|2|3` 扩展为 `0|1|2|3|4`，其中新 `2` = 配件数量（COMPOSITE only），`3` = 组合工艺，`4` = 汇总。SIMPLE 流程 `1 → 4`，COMPOSITE 流程 `1 → 2 → 3 → 4`。

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/ConfigureProductDrawer.tsx`

- [ ] **Step 1: 加 import 与 furthestCi 状态**

在文件顶部 import 区，`Step4CompositeProcess` 之后加：

```tsx
import StepAccessoryQuantity from './configure/StepAccessoryQuantity';
import AccessoryProgressBar from './configure/AccessoryProgressBar';
```

把 globalStep 状态类型从 `0 | 1 | 2 | 3` 改为 `0 | 1 | 2 | 3 | 4`：

```tsx
  const [globalStep, setGlobalStep] = useState<0 | 1 | 2 | 3 | 4>(0);
```

在 `const [ci, setCi] = useState(0);` 之后加一行（最远到达的配件下标）：

```tsx
  const [furthestCi, setFurthestCi] = useState(0);
```

- [ ] **Step 2: 替换 goNext（插入配件数量步 + 维护 furthestCi）**

把整个 `goNext` 回调替换为：

```tsx
  const goNext = useCallback(async () => {
    if (globalStep === 0) {
      setParts(initParts(productType, initPartCount));
      setCi(0);
      setFurthestCi(0);
      setSubStep(0);
      setGlobalStep(1);
      return;
    }
    if (globalStep === 1) {
      if (subStep === 0) { setSubStep(1); return; }
      if (subStep === 1) {
        const cur = parts[ci];
        const partLabel = productType === 'COMPOSITE' ? `配件 ${ci + 1}` : '产品';
        const err = cur ? validateCustomPart(cur, partLabel) : null;
        if (err) { message.warning(err); return; }
        const matched = await checkFingerprintAndAdvance();
        if (matched) return;
        setSubStep(2);
        return;
      }
      if (subStep === 2) {
        if (ci < parts.length - 1) {
          const next = ci + 1;
          setCi(next);
          setFurthestCi(prev => Math.max(prev, next));
          setSubStep(0);
          return;
        }
        // 最后一个配件已配完
        setFurthestCi(parts.length); // 全部完成
        if (productType === 'COMPOSITE') { setGlobalStep(2); return; } // → 配件数量
        setGlobalStep(4); // SIMPLE → 汇总
        return;
      }
    }
    if (globalStep === 2) {
      // 配件数量校验：全部 >= 1（InputNumber 已兜底，这里再防一道）
      const bad = parts.findIndex(p => !p.quantity || p.quantity < 1);
      if (bad >= 0) { message.warning(`${parts[bad].name}: 数量必须 ≥ 1`); return; }
      setGlobalStep(3); // → 组合工艺
      return;
    }
    if (globalStep === 3) { setGlobalStep(4); return; } // → 汇总
    if (globalStep === 4) { await submitConfigure(); }
  }, [globalStep, subStep, productType, parts, ci, initPartCount, checkFingerprintAndAdvance, initParts]);
```

- [ ] **Step 3: 替换 goPrev（适配新步骤号）**

把整个 `goPrev` 回调替换为：

```tsx
  const goPrev = useCallback(() => {
    if (globalStep === 4) {
      if (productType === 'COMPOSITE') setGlobalStep(3);
      else { setSubStep(2); setCi(0); setGlobalStep(1); }
      return;
    }
    if (globalStep === 3) { setGlobalStep(2); return; } // 组合工艺 → 配件数量
    if (globalStep === 2) { // 配件数量 → 最后一个配件的工序
      setCi(parts.length - 1);
      setSubStep(2);
      setGlobalStep(1);
      return;
    }
    if (globalStep === 1) {
      if (subStep > 0) { setSubStep((subStep - 1) as 0 | 1); return; }
      if (ci > 0) { setCi(ci - 1); setSubStep(2); return; }
      setGlobalStep(0);
    }
  }, [globalStep, subStep, ci, parts.length, productType]);
```

- [ ] **Step 4: jumpToPart 跳转处理器**

在 `goPrev` 回调之后加一个跳转处理器（点击进度框已完成配件）：

```tsx
  const jumpToPart = useCallback((idx: number) => {
    if (idx === ci) return;
    if (idx >= furthestCi) return; // 只能跳回已完成
    setCi(idx);
    setSubStep(0);
  }, [ci, furthestCi]);
```

- [ ] **Step 5: 提交时填默认 participating/params + 传 quantity**

在 `submitConfigure` 中：

(a) `partsReq` map 内，给每个 part 对象加 `quantity`（在 `quotationLineItemId: ...` 之后加一行）：

```tsx
        quantity: p.quantity ?? 1,
```

(b) 把 `compProcs` 的构造替换为「全配件参与 + 空参数」（需求 #2 —— 不再依赖用户在 Step4 勾选/填参）：

```tsx
      const allPartIdx = parts.map((_, i) => i);
      const compProcs: CompositeProcessRequest[] = addedCProcs.map(a => ({
        defCode: a.defCode,
        participatingPartIndexes: allPartIdx,
        params: {},
      }));
```

- [ ] **Step 6: resetState 重置 furthestCi**

在 `resetState` 内，`setCi(0);` 之后加：

```tsx
    setFurthestCi(0);
```

并把 `setGlobalStep(0);` 保持不变（它已重置 globalStep）。

- [ ] **Step 7: 改 stepLabels / activeIdx**

把 `stepLabels` 与 `activeIdx` 两段替换为：

```tsx
  const stepLabels = productType === 'COMPOSITE'
    ? ['产品类型', `配件选配 ×${parts.length || initPartCount}`, '配件数量', '组合工艺', '完成选配']
    : ['产品类型', '料号匹配', '材质选配', '工序选择', '完成选配'];
  const activeIdx = productType === 'COMPOSITE'
    ? globalStep // COMPOSITE: globalStep 0..4 与 5 个标签一一对应
    : (globalStep === 0 ? 0 : globalStep === 4 ? 4 : subStep + 1);
```

- [ ] **Step 8: 改 footer 确认按钮判断 + render 区**

footer 里把确认按钮文案判断从 `globalStep === 3` 改为 `globalStep === 4`：

```tsx
            <Button type="primary" onClick={goNext} loading={submitting}>
              {globalStep === 4 ? '确认添加' : '下一步'}
            </Button>
```

把 `globalStep === 1` 渲染块替换为（顶部加进度条）：

```tsx
      {globalStep === 1 && parts[ci] && (
        <>
          <AccessoryProgressBar
            parts={parts}
            currentIndex={ci}
            furthestIndex={furthestCi}
            onJump={jumpToPart}
          />
          {subStep === 0 && <Step1SearchPart part={parts[ci]} onUpdate={updateCurrentPart} />}
          {subStep === 1 && <Step2Material part={parts[ci]} onUpdate={updateCurrentPart} />}
          {subStep === 2 && <Step3Process part={parts[ci]} onUpdate={updateCurrentPart} />}
        </>
      )}
```

把 `globalStep === 2`（原组合工艺）与 `globalStep === 3`（原汇总）两块替换为新的三块：

```tsx
      {globalStep === 2 && (
        <StepAccessoryQuantity parts={parts} onUpdatePart={updatePart} />
      )}

      {globalStep === 3 && (
        <Step4CompositeProcess
          parts={parts}
          addedCProcs={addedCProcs}
          onChangeAdded={setAddedCProcs}
        />
      )}

      {globalStep === 4 && (
        <Step5Summary
          productType={productType}
          parts={parts}
          addedCProcs={addedCProcs}
          onUpdatePart={updatePart}
        />
      )}
```

- [ ] **Step 9: 验证编译**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误

- [ ] **Step 10: 验证 Vite transform**

Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/ConfigureProductDrawer.tsx`
Expected: `200`

- [ ] **Step 11: Commit**

```bash
git add cpq-frontend/src/pages/quotation/ConfigureProductDrawer.tsx
git commit -m "feat(configure): wire accessory quantity step + progress nav + default composite submit"
```

---

## Task 7: 前端 — Step3Process 工序编码显示 + 模糊搜索

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/configure/Step3Process.tsx:58-60` 和 `:98-101`、`:130-133`

- [ ] **Step 1: 搜索过滤扩展为 code/name/category（不区分大小写）**

把 `filtered` 定义（约 58-60 行）替换为：

```tsx
  const filtered = allProcs.filter(p => {
    const kw = q.trim().toLowerCase();
    if (!kw) return true;
    return p.name.toLowerCase().includes(kw)
      || p.code.toLowerCase().includes(kw)
      || (p.categoryName ?? '').toLowerCase().includes(kw);
  });
```

- [ ] **Step 2: 搜索框 placeholder 提示可搜编码**

把搜索 `Input` 的 placeholder（约 81 行）`"搜索工序…"` 改为：

```tsx
          placeholder="搜索工序（编码 / 名称）…"
```

- [ ] **Step 3: 左侧列表项名称下显示工序编码**

把左侧列表 `List.Item.Meta`（约 98-101 行）替换为：

```tsx
              <List.Item.Meta
                title={p.name}
                description={
                  <span>
                    <Tag color="blue" style={{ marginRight: 4 }}>{p.code}</Tag>
                    <Tag>{p.categoryName ?? '—'}</Tag>
                  </span>
                }
              />
```

- [ ] **Step 4: 右侧「已选工序」也显示编码**

把右侧 `List.Item.Meta`（约 130-133 行）替换为：

```tsx
              <List.Item.Meta
                avatar={<Tag color="purple">{i + 1}</Tag>}
                title={p.name}
                description={
                  <span>
                    <Tag color="blue" style={{ marginRight: 4 }}>{p.code}</Tag>
                    <Tag>{p.categoryName ?? '—'}</Tag>
                  </span>
                }
              />
```

- [ ] **Step 5: 验证编译 + Vite**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/configure/Step3Process.tsx`
Expected: `200`

- [ ] **Step 6: Commit**

```bash
git add cpq-frontend/src/pages/quotation/configure/Step3Process.tsx
git commit -m "feat(configure): show process code + fuzzy search by code/name in Step3"
```

---

## Task 8: 前端 — Step4CompositeProcess 去参与配件/参数 + 编码 + 搜索

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/configure/Step4CompositeProcess.tsx`（整体重写）

- [ ] **Step 1: 整体替换组件**

把整个文件替换为（移除 `togglePart` / `setParam` / `getParamSchema` / 参与配件区 / 参数区；保留挑选与移除；加搜索 + 编码显示。仍用 `parts` 在 `add()` 时把 `participatingPartIndexes` 预置为全部——后端契约不变，最终提交时 Drawer 又会统一覆盖为全配件 + 空参数）：

```tsx
import React, { useEffect, useState } from 'react';
import { Card, Button, Tag, Input, Empty } from 'antd';
import { SearchOutlined } from '@ant-design/icons';
import {
  compositeProcessService,
  type CompositeProcessDef,
} from '../../../services/compositeProcessService';
import type { PartState, CompositeProcessAdded } from '../ConfigureProductDrawer';

interface Props {
  parts: PartState[];
  addedCProcs: CompositeProcessAdded[];
  onChangeAdded: (next: CompositeProcessAdded[]) => void;
}

/**
 * 组合工艺选择（需求 #2 简化版）：
 * 用户只挑选「使用哪些组合工艺」，不再勾选参与配件、不再填参数。
 * - participatingPartIndexes 预置为全部配件（提交时 Drawer 仍统一覆盖为全配件）。
 * - params 恒为空对象 {}。
 * 需求 #3：组合工艺库卡片显示编码，并支持按编码/名称模糊搜索。
 */
const Step4CompositeProcess: React.FC<Props> = ({ parts, addedCProcs, onChangeAdded }) => {
  const [defs, setDefs] = useState<CompositeProcessDef[]>([]);
  const [q, setQ] = useState('');

  useEffect(() => {
    compositeProcessService.list()
      .then(setDefs)
      .catch(() => setDefs([]));
  }, []);

  const isAdded = (code: string) => addedCProcs.some(a => a.defCode === code);

  const add = (def: CompositeProcessDef) => {
    if (isAdded(def.code)) return;
    const all = parts.map((_, i) => i);
    onChangeAdded([
      ...addedCProcs,
      { defCode: def.code, participatingPartIndexes: all, params: {} },
    ]);
  };

  const remove = (code: string) =>
    onChangeAdded(addedCProcs.filter(a => a.defCode !== code));

  const filtered = defs.filter(d => {
    const kw = q.trim().toLowerCase();
    if (!kw) return true;
    return d.name.toLowerCase().includes(kw) || d.code.toLowerCase().includes(kw);
  });

  return (
    <div style={{ display: 'flex', gap: 16, height: 480 }}>
      {/* 左:工艺库 */}
      <div style={{ width: 300, borderRight: '0.5px solid #eee', paddingRight: 12, display: 'flex', flexDirection: 'column' }}>
        <h4 style={{ marginTop: 0 }}>组合工艺库</h4>
        <Input
          prefix={<SearchOutlined />}
          placeholder="搜索组合工艺（编码 / 名称）…"
          value={q}
          onChange={(e) => setQ(e.target.value)}
          style={{ marginBottom: 8 }}
        />
        <div style={{ flex: 1, overflow: 'auto' }}>
          {filtered.length === 0
            ? <Empty description="无匹配工艺" />
            : filtered.map(def => (
                <Card
                  key={def.id}
                  size="small"
                  style={{
                    marginBottom: 8,
                    cursor: isAdded(def.code) ? 'not-allowed' : 'pointer',
                    background: isAdded(def.code) ? '#f0effe' : undefined,
                  }}
                  onClick={() => add(def)}
                >
                  <div>
                    <span style={{ fontSize: 18 }}>{def.icon}</span> <b>{def.name}</b>
                  </div>
                  <div style={{ marginTop: 4 }}>
                    <Tag color="blue">{def.code}</Tag>
                  </div>
                  <div style={{ color: '#888', fontSize: 11, marginTop: 4 }}>{def.description}</div>
                  {isAdded(def.code) && <Tag color="green" style={{ marginTop: 6 }}>已添加</Tag>}
                </Card>
              ))}
        </div>
      </div>

      {/* 右:已选组合工艺（仅名称 + 移除） */}
      <div style={{ flex: 1, overflow: 'auto' }}>
        <h3 style={{ marginTop: 0 }}>已选组合工艺 ({addedCProcs.length})</h3>
        <div style={{ color: '#aaa', fontSize: 12, marginBottom: 12 }}>默认全部配件参与</div>
        {addedCProcs.length === 0 ? (
          <Empty description="从左侧选择组合工艺" />
        ) : (
          addedCProcs.map(a => {
            const def = defs.find(d => d.code === a.defCode);
            return (
              <Card
                key={a.defCode}
                size="small"
                style={{ marginBottom: 12, background: '#f9f8ff' }}
                title={
                  <>
                    <span style={{ fontSize: 16 }}>{def?.icon}</span> {def?.name ?? a.defCode}{' '}
                    {def && <Tag color="blue">{def.code}</Tag>}
                  </>
                }
                extra={
                  <Button type="text" danger onClick={() => remove(a.defCode)}>
                    移除
                  </Button>
                }
              />
            );
          })
        )}
      </div>
    </div>
  );
};

export default Step4CompositeProcess;
```

- [ ] **Step 2: 验证编译 + Vite**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误（注意：移除了 `InputNumber`/`CompositeProcessParamDef` import，若残留未用 import 会触发 `noUnusedLocals` 报错——已在新文件去掉）
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/configure/Step4CompositeProcess.tsx`
Expected: `200`

- [ ] **Step 3: Commit**

```bash
git add cpq-frontend/src/pages/quotation/configure/Step4CompositeProcess.tsx
git commit -m "feat(configure): simplify composite process selection + code search"
```

---

## Task 9: 前端 — Step5Summary 显示配件数量 + 简化组合工艺展示

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/configure/Step5Summary.tsx:79-94`（数量）和 `:101-117`（组合工艺）

- [ ] **Step 1: 配件明细加「数量」行**

在「单重」`Descriptions.Item`（约 79-94 行）之前插入一个数量项（仅 COMPOSITE 有意义，但展示无害；统一显示）：

```tsx
              {productType === 'COMPOSITE' && (
                <Descriptions.Item label="数量">
                  <b>{p.quantity ?? 1}</b>
                </Descriptions.Item>
              )}
```

- [ ] **Step 2: 组合工艺展示简化为仅名称（去参与配件/参数）**

把 `{productType === 'COMPOSITE' && addedCProcs.length > 0 && (...)}` 整块（约 101-117 行）替换为：

```tsx
    {productType === 'COMPOSITE' && addedCProcs.length > 0 && (
      <Card title="组合工艺" size="small">
        {addedCProcs.map((a, i) => (
          <div key={i} style={{ borderBottom: '0.5px solid #f0f0f0', padding: '8px 0' }}>
            <b>{defNameByCode(a.defCode)}</b>
            <span style={{ marginLeft: 8, color: '#888' }}>全部配件参与</span>
          </div>
        ))}
      </Card>
    )}
```

- [ ] **Step 3: 验证编译 + Vite**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/configure/Step5Summary.tsx`
Expected: `200`

- [ ] **Step 4: Commit**

```bash
git add cpq-frontend/src/pages/quotation/configure/Step5Summary.tsx
git commit -m "feat(configure): show accessory quantity + simplify composite summary"
```

---

## Task 10: 前端 — Step0ProductType 文案消歧义（配件数量 → 配件个数）

Step0 现有「配件数量」实为「配件个数」（N 个配件），与新引入的「配件数量（组成用量）」同名易混淆。改文案。

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/configure/Step0ProductType.tsx:46` 和 `:53`

- [ ] **Step 1: 改两处文案**

把第 46 行 `<span>配件数量:</span>` 改为：

```tsx
          <span>配件个数:</span>
```

把第 53 行 `个配件（最少 2，最多 8）` 所在的 `Typography.Text` 文案保持语义即可（已是「个配件」），无需改。仅确认第 46 行已改。

- [ ] **Step 2: 验证编译 + Vite**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/configure/Step0ProductType.tsx`
Expected: `200`

- [ ] **Step 3: Commit**

```bash
git add cpq-frontend/src/pages/quotation/configure/Step0ProductType.tsx
git commit -m "refactor(configure): rename Step0 '配件数量' label to '配件个数' to avoid ambiguity"
```

---

## Task 11: 端到端验证 + 数据落库核对

**Files:**（无代码改动，仅验证）
- Reference: `cpq-frontend/e2e/composite-product-flow.spec.ts`

- [ ] **Step 1: 跑组合产品 E2E**

```bash
cd cpq-frontend
npx playwright test --config=e2e/playwright.config.ts e2e/composite-product-flow.spec.ts --reporter=list
```

Expected: 全部 test `passed`；若 spec 内有「加载中」断言，`'加载中' final count = 0`。

> 若该 spec 因为本次步骤机改造（新增「配件数量」步、按钮文案、Steps 标签）而失败：定位失败断言，按新流程更新 spec 的步骤导航（点击「下一步」次数、`配件数量` 步骤、`配件个数` 文案）。这是预期内的 spec 维护，不是回归。更新后重跑直到 `passed`。

- [ ] **Step 2: 手工跑一遍 COMPOSITE 选配并核对数量落库**

通过 UI（或 E2E 录制）新建报价单 → 选配 → 组合产品 → 配 2 个配件 → 在「配件数量」步把配件 1 数量改为 `3` → 选一个组合工艺 → 确认添加。

然后查库（customer_no 用该报价单客户 code；material_no 用生成的 COMBO 父料号）：

```bash
PGPASSWORD=<pwd> psql -h <host> -U <user> -d <db> -c \
"SELECT material_no, component_no, seq_no, composition_qty \
 FROM material_bom_item \
 WHERE characteristic='ASSEMBLY' AND system_type='QUOTE' \
 ORDER BY material_no, seq_no LIMIT 20;"
```

Expected: 对应配件 1 的行 `composition_qty = 3`，配件 2 的行 `composition_qty = 1`。

> 连接参数（host/user/db/pwd）以 `cpq-backend/src/main/resources/application.properties` 的 `quarkus.datasource.*` 为准。

- [ ] **Step 3: 核对组合工艺契约（全配件 + 空参数）**

```bash
PGPASSWORD=<pwd> psql -h <host> -U <user> -d <db> -c \
"SELECT def_code, participating_parts, param_values \
 FROM quotation_line_composite_process ORDER BY created_at DESC LIMIT 5;"
```

Expected: 最新记录 `participating_parts` 含全部配件，`param_values` 为 `{}` / 空。

- [ ] **Step 4: 完成宣告自检声明**

按 `CLAUDE.md` 要求，给出一行已自检声明，例如：
> "TS 0 错误 ✅；ConfigureProductDrawer/StepAccessoryQuantity/AccessoryProgressBar/Step3/Step4/Step5/Step0 → Vite 200 ✅；后端 /q/health 200 ✅；material_bom_item.composition_qty=3 落库 ✅；composite_product_flow E2E passed ✅"

- [ ] **Step 5: 最终提交（如 E2E spec 有更新）**

```bash
git add cpq-frontend/e2e/composite-product-flow.spec.ts
git commit -m "test(configure): update composite-product E2E for accessory quantity step"
```

---

## Task 12: 文档回写（RECORD.md + 反模式速查若适用）

**Files:**
- Modify: `docs/RECORD.md`

- [ ] **Step 1: 追加开发记录**

在 `docs/RECORD.md` 末尾按规定格式追加一行（日期 2026-06-01）：

```
[2026-06-01] 选配-组合产品 - 加配件数量编辑(写 material_bom_item.composition_qty)/组合工艺简化(全配件+空参数)/工序&组合工艺带编码模糊搜索/配件进度导航 | ConfigureProductDrawer.tsx + StepAccessoryQuantity.tsx + AccessoryProgressBar.tsx + Step3/Step4/Step5/Step0 + PartRequest.java + ConfigureProductService.insertMaterialBomAssemblyV6 | 选配只入库不算价;composition_qty 用 ON CONFLICT DO UPDATE 匹配 uq_material_bom_item 表达式索引;globalStep 扩 0..4 插入配件数量步
```

- [ ] **Step 2: Commit**

```bash
git add docs/RECORD.md
git commit -m "docs: record composite-product configure optimization (2026-06-01)"
```

---

## Self-Review

**1. Spec coverage:**

| Spec 需求 | 对应任务 |
|-----------|----------|
| #1 配件数量编辑（默认 1，正整数，写 composition_qty） | Task 1/2（后端落库）+ Task 3（类型）+ Task 4（编辑 UI）+ Task 6 Step5（提交传值）+ Task 9（汇总展示） |
| #2 组合工艺只选用哪些（全配件参与 + 空参数） | Task 6 Step5(b)（提交默认）+ Task 8（UI 去参与/参数） |
| #3 工序/组合工艺带编码 + 模糊搜索 | Task 7（工序）+ Task 8（组合工艺） |
| #4 顶部配件框（仅可跳回已完成） | Task 5（组件）+ Task 6（接线 + jumpToPart + furthestCi） |
| SIMPLE 流程不受影响 | Task 6（SIMPLE 仍 1→4，activeIdx 走 subStep 分支） |
| 仅入库不自动计算 | Task 2（仅写列，无乘法）+ 各组件无计算逻辑 |
| 无 Flyway 迁移 | 全程未新增 V*.sql |

无遗漏需求。

**2. Placeholder scan:** 无 TBD/TODO；每个改代码步骤均含完整代码块；psql 连接参数标注「以 application.properties 为准」属于环境值而非占位逻辑。

**3. Type consistency:**
- `PartState.quantity: number`（Task 3）↔ `StepAccessoryQuantity` 读 `p.quantity ?? 1`（Task 4）↔ 提交 `quantity: p.quantity ?? 1`（Task 6）↔ `PartRequest.quantity?: number`（Task 3 types）↔ 后端 `PartRequest.quantity: Integer`（Task 1）↔ `insertMaterialBomAssemblyV6(..., List<Integer> quantities)`（Task 2）— 全链一致。
- `furthestCi`/`furthestIndex`/`currentIndex`/`onJump` 在 `AccessoryProgressBar` props（Task 5）与 Drawer 传参（Task 6 Step8）命名一致。
- `globalStep` 类型 `0|1|2|3|4` 在状态声明（Task 6 Step1）、goNext/goPrev/activeIdx/render/footer 全部按 0..4 语义对齐（0 类型,1 配件,2 数量,3 组合工艺,4 汇总）。
- `Step4CompositeProcess` 移除了 `InputNumber`/`CompositeProcessParamDef` import 与 `togglePart`/`setParam`/`getParamSchema`（Task 8 整体重写），无悬挂引用。

无类型不一致。
