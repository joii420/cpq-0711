# 报价料号统一 Spec 1 实现计划（数据基座 + 发号服务）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **执行前**：本计划属较大改动，须先用 `superpowers:using-git-worktrees` 建隔离 worktree（从本地 HEAD，非 fresh —— 见记忆 `cpq-worktree-must-base-on-head-not-fresh`）。dev server(8081/5174)/DB/node_modules 复用主工作区，不另起。后端测试在 **worktree 的 `cpq-backend/`** 里跑 `./mvnw`。
>
> **权威规格**：`docs/superpowers/specs/2026-07-06-报价料号统一-design.md`（v4）。逐字契约以 spec 为准；本计划把它拆成可执行任务。

**Goal:** 把报价侧料号统一为「报价料号」`XXXX-YYMMNNNNNN`，建好数据基座（`material_customer_map` 加 `system_type`/`production_no`）+ 发号服务 + 报价侧 8 个 handler 改发报价料号（取消 9 字头），核价侧不受影响。

**Architecture:** `material_customer_map` 加 `system_type`(QUOTE/PRICING) 按行隔离两侧、加 `production_no`(生产料号待补)；QUOTE 行 `material_no`=报价料号（全局唯一、↔客户料号 1:1）。发号收敛进单一 `QuoteMaterialNoAllocator`（客户四位码表 + 每(码,YYMM)流水表 + `INSERT..ON CONFLICT DO UPDATE RETURNING` 原子发号），经 `MaterialNoResolver` 一处改、8 handler 受益。map 即"全部报价料号统一登记表"，候选查询靠 `customer_product_no IS NOT NULL` 隔离出真客户产品。

**Tech Stack:** Java 17 + Quarkus 3.34 + Hibernate Panache + Flyway + PostgreSQL 16；JUnit5 + `@QuarkusTest`（连远程 DB，用独特测试料号 + `@BeforeEach/@AfterEach` 清理）。

---

## 文件结构（改动清单）

**新建**
- `cpq-backend/src/main/resources/db/migration/V308__quote_material_no_structure.sql` — 加列 + DROP NOT NULL + 发号表/序列（无 NOT NULL、无新索引）
- `cpq-backend/src/main/resources/db/migration/V309__quote_material_no_backfill_constraints.sql` — 存量 `system_type` 回填 + 建 NOT NULL + 建索引（**前提：Task 0 校验通过**）
- `cpq-backend/src/main/java/com/cpq/basicdata/v6/service/QuoteMaterialNoAllocator.java` — 发号服务
- `cpq-backend/src/test/java/com/cpq/basicdata/v6/service/QuoteMaterialNoAllocatorTest.java`
- `cpq-backend/src/test/java/com/cpq/basicdata/v6/quote/QuoteMaterialNoIntegrationTest.java` — 8 handler / Q02 / 跨客户守卫集成

**修改**
- `MaterialNoResolver.java`（`.../v6/service/`）— `BatchState` 加 `customerNo/yyMm`；`resolve()` 生成分支改调 allocator；停用 9 字头 `generateNextMaterialNo`
- `MaterialCustomerMapRepository.java`（`.../v6/repository/`）— `MapRow`+2 字段、upsert 冲突键+SET+客户守卫、`coalesceOver` dedup 键、`upsertChunk` 13 列、`deleteByCustomerNo` 收窄
- `Q02CustomerMapHandler.java`（`.../v6/quote/`）— relabel 列、`system_type='QUOTE'`、per-row 异常
- `P05CustomerMapHandler.java`（`.../v6/pricing/`）— `system_type='PRICING'`
- 8 个 handler 构造 `BatchState` 传 `customerNo/yyMm`：`MaterialBomMergeHandler` + `Q04ElementBomHandler`/`Q06FixedProcessFeeHandler`/`Q07IncomingOtherFeeHandler`/`Q08IncomingAnnualDiscountHandler`/`Q09IncomingRecoveryHandler`/`Q10SelfProcessFeeHandler`/`Q13ComponentOtherFeeHandler`
- `CustomerPartCandidateService.java`（`.../quotation/service/`）— `:76` 子查询、`:71`/`:165` JOIN 加过滤
- `V6QuotationCommitService.java`（`.../v6/service/`）— `:53` 加 `system_type='QUOTE'`
- `V255__v12_create_component_sql_views.sql` 的等价视图 —— **不改历史 V255**，用**新迁移 V310** DROP+重建 `v12_part_mapping` 视图（Flyway 不改已应用文件）

> ⚠️ 行号（`:76` 等）仅供定位，实现时以 `codegraph`/`grep` 复核真实行号为准。

---

## Task 0：存量数据核对（前置决策，gating）

**目的**：spec §6.2/§6.3 —— 用真实 DB 数据决定 ①存量 QUOTE/PRICING 分类、②两个 QUOTE 唯一索引能否直接建。**此任务不写代码，产出结论写进本计划 Task 1/9 的注释。**

**Files:** 无（只跑 SQL）。

- [ ] **Step 1: 连库跑核对 SQL**

用 CLAUDE.md 的连库方式（`PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db`）依次跑：

```sql
-- (A) 疑似双源合并行（§6.2 (1)）
SELECT count(*) FROM material_customer_map
WHERE (customer_material_name IS NOT NULL OR base_currency IS NOT NULL) AND seq_no IS NOT NULL;

-- (B) 歧义行：6 强信号列全空（§6.2 (2)）
SELECT count(*) FROM material_customer_map
WHERE customer_material_name IS NULL AND customer_drawing_no IS NULL AND payment_method IS NULL
  AND base_currency IS NULL AND quote_currency IS NULL AND exchange_rate IS NULL;

-- (C) material_no 在 QUOTE 语义下是否已有跨行重复（建 uq_mcm_quote_no 前，§6.3 ①）
--     先按强信号预标 QUOTE 再看重复：
WITH q AS (SELECT * FROM material_customer_map
  WHERE customer_material_name IS NOT NULL OR customer_drawing_no IS NOT NULL OR payment_method IS NOT NULL
     OR base_currency IS NOT NULL OR quote_currency IS NOT NULL OR exchange_rate IS NOT NULL)
SELECT material_no, count(*) FROM q GROUP BY material_no HAVING count(*)>1;

-- (D) (customer_no, customer_product_no) 1:1 冲突（建 uq_mcm_quote_cust_prod 前，§6.3 ②）
WITH q AS (SELECT * FROM material_customer_map
  WHERE customer_material_name IS NOT NULL OR customer_drawing_no IS NOT NULL OR payment_method IS NOT NULL
     OR base_currency IS NOT NULL OR quote_currency IS NOT NULL OR exchange_rate IS NOT NULL)
SELECT customer_no, customer_product_no, count(*) FROM q
WHERE customer_product_no IS NOT NULL GROUP BY customer_no, customer_product_no HAVING count(*)>1;
```

- [ ] **Step 2: 记录结论 + 决策**

把 (A)(B)(C)(D) 的行数与样例贴进本计划下方"Task 0 结论"占位。判定：
- (A)/(B) 为 0 → §6.1 强信号回填即安全，V309 直接用强信号 SQL。
- (B) 非 0 → V309 的歧义行用 §6.1 强判据 SQL（`element_bom`/`material_bom`/`unit_price` 的 `system_type='QUOTE'` 引用集）分类；仍无法判者产人工清单。
- (C)/(D) 非 0 → **必须先做数据 1:1 收敛**（与用户确认保留哪个报价料号），否则 V309 建索引崩。收敛动作单列为 Task 0.5（若需要）。

- [ ] **Step 3: 若 (C)/(D) 非 0，与用户确认收敛策略**（阻塞后续 migration 任务）

> **Task 0 结论（执行时填写）：** (A)=___ (B)=___ (C)=___ (D)=___ ；分类策略：___；1:1 收敛：___

---

## Task 1：V308 结构迁移（加列 + 发号表，无约束）

**Files:**
- Create: `cpq-backend/src/main/resources/db/migration/V308__quote_material_no_structure.sql`
- Test: 靠 Quarkus 启动 Flyway migrate + `flyway_schema_history`

- [ ] **Step 1: 写 V308**

```sql
-- 报价料号统一 Spec 1 · 结构（无 NOT NULL、无新索引；约束在 V309 回填后建）
ALTER TABLE material_customer_map ADD COLUMN IF NOT EXISTS system_type   VARCHAR(20);
ALTER TABLE material_customer_map ADD COLUMN IF NOT EXISTS production_no  VARCHAR(20);
ALTER TABLE material_customer_map ALTER COLUMN customer_product_no DROP NOT NULL;   -- 通道②组件登记行无客户料号

-- 客户四位码
CREATE TABLE IF NOT EXISTS quote_customer_code (
  customer_no VARCHAR(20) PRIMARY KEY,
  code        CHAR(4)     NOT NULL UNIQUE,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE SEQUENCE IF NOT EXISTS quote_customer_code_seq START 1;

-- 每(客户码,YYMM)流水
CREATE TABLE IF NOT EXISTS quote_material_no_seq (
  customer_code CHAR(4) NOT NULL,
  year_month    CHAR(4) NOT NULL,
  last_serial   INTEGER NOT NULL DEFAULT 0,
  PRIMARY KEY (customer_code, year_month)
);
```

- [ ] **Step 2: 触发 Quarkus 迁移并自检**

Run（worktree 的 cpq-backend 里 `touch` 一个 java 文件让主工作区 dev server 热重载跑 Flyway，或直接跑测试触发）：
```bash
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -c \
 "SELECT version,success FROM flyway_schema_history WHERE version='308';"
```
Expected: `308 | t`

- [ ] **Step 3: Commit**

```bash
git add cpq-backend/src/main/resources/db/migration/V308__quote_material_no_structure.sql
git commit -m "feat(quote-no): V308 加 system_type/production_no + 发号表(无约束)"
```

---

## Task 2：QuoteMaterialNoAllocator 发号服务

**Files:**
- Create: `.../v6/service/QuoteMaterialNoAllocator.java`
- Test: `.../v6/service/QuoteMaterialNoAllocatorTest.java`

- [ ] **Step 1: 写失败测试**

```java
package com.cpq.basicdata.v6.service;

import io.quarkus.test.junit.QuarkusTest;
import jakarta.inject.Inject;
import jakarta.persistence.EntityManager;
import jakarta.transaction.Transactional;
import org.junit.jupiter.api.*;
import static org.junit.jupiter.api.Assertions.*;

@QuarkusTest
class QuoteMaterialNoAllocatorTest {
    @Inject QuoteMaterialNoAllocator alloc;
    @Inject EntityManager em;
    static final String C1 = "QMNT-CUST-1", C2 = "QMNT-CUST-2";

    @Transactional void cleanup() {
        em.createNativeQuery("DELETE FROM material_customer_map WHERE customer_no IN (:a,:b)").setParameter("a",C1).setParameter("b",C2).executeUpdate();
        em.createNativeQuery("DELETE FROM quote_material_no_seq WHERE customer_code IN (SELECT code FROM quote_customer_code WHERE customer_no IN (:a,:b))").setParameter("a",C1).setParameter("b",C2).executeUpdate();
        em.createNativeQuery("DELETE FROM quote_customer_code WHERE customer_no IN (:a,:b)").setParameter("a",C1).setParameter("b",C2).executeUpdate();
    }
    @BeforeEach void b(){cleanup();} @AfterEach void a(){cleanup();}

    @Test void customerCode_allocatesAndReuses() {
        String code = alloc.getOrAllocateCustomerCode(C1);
        assertTrue(code.matches("\\d{4}"), "四位数字码");
        assertEquals(code, alloc.getOrAllocateCustomerCode(C1), "同客户复用同码");
        assertNotEquals(code, alloc.getOrAllocateCustomerCode(C2), "不同客户不同码");
    }
    @Test void mint_formatAndSerialPerMonth() {
        String r1 = alloc.mintAndRegister(C1, "2607");
        String r2 = alloc.mintAndRegister(C1, "2607");
        String code = alloc.getOrAllocateCustomerCode(C1);
        assertEquals(code+"-2607000001", r1);
        assertEquals(code+"-2607000002", r2);
        assertEquals(code+"-2608000001", alloc.mintAndRegister(C1,"2608"), "跨月流水归1");
        // 登记落 material_customer_map QUOTE 行，customer_product_no/production_no 为 NULL
        Object[] row = (Object[]) em.createNativeQuery(
          "SELECT system_type, customer_product_no, production_no FROM material_customer_map WHERE material_no=:m")
          .setParameter("m", r1).getSingleResult();
        assertEquals("QUOTE", row[0]); assertNull(row[1]); assertNull(row[2]);
    }
    @Test void ensureRegistered_idempotent_sameCustomer() {
        String r = alloc.mintAndRegister(C1, "2607");
        alloc.ensureRegistered(C1, r);   // 重入不抛错、不新增
        long n = ((Number) em.createNativeQuery("SELECT count(*) FROM material_customer_map WHERE material_no=:m").setParameter("m", r).getSingleResult()).longValue();
        assertEquals(1L, n);
    }
    @Test void ensureRegistered_crossCustomer_throws() {
        String r = alloc.mintAndRegister(C1, "2607");   // 属 C1
        assertThrows(QuoteMaterialNoAllocator.CrossCustomerQuoteNoException.class,
            () -> alloc.ensureRegistered(C2, r), "别客户复用同报价料号→报错");
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-backend && ./mvnw -q -o test -Dtest=QuoteMaterialNoAllocatorTest`
Expected: 编译失败（类不存在）。

- [ ] **Step 3: 写实现**

```java
package com.cpq.basicdata.v6.service;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.inject.Inject;
import jakarta.persistence.EntityManager;

@ApplicationScoped
public class QuoteMaterialNoAllocator {

    @Inject EntityManager em;

    public static class CrossCustomerQuoteNoException extends RuntimeException {
        public CrossCustomerQuoteNoException(String m){ super(m); }
    }

    /** 取或分配客户四位码（首见分配、之后固定）。 */
    public String getOrAllocateCustomerCode(String customerNo) {
        var found = em.createNativeQuery("SELECT code FROM quote_customer_code WHERE customer_no=:c")
            .setParameter("c", customerNo).getResultList();
        if (!found.isEmpty()) return (String) found.get(0);

        String code = String.format("%04d", ((Number) em.createNativeQuery("SELECT nextval('quote_customer_code_seq')").getSingleResult()).longValue());
        if (code.compareTo("9999") > 0) throw new IllegalStateException("客户四位码枯竭(>9999): " + code);
        em.createNativeQuery("INSERT INTO quote_customer_code(customer_no, code) VALUES(:c,:code) ON CONFLICT (customer_no) DO NOTHING")
            .setParameter("c", customerNo).setParameter("code", code).executeUpdate();
        // 竞态败者再查
        return (String) em.createNativeQuery("SELECT code FROM quote_customer_code WHERE customer_no=:c")
            .setParameter("c", customerNo).getSingleResult();
    }

    /** 铸新报价料号 + 登记 material_customer_map QUOTE 行（组件行，customer_product_no/production_no=NULL）。 */
    public String mintAndRegister(String customerNo, String yyMm) {
        String code = getOrAllocateCustomerCode(customerNo);
        int serial = ((Number) em.createNativeQuery(
            "INSERT INTO quote_material_no_seq(customer_code, year_month, last_serial) VALUES(:code,:ym,1) " +
            "ON CONFLICT (customer_code, year_month) DO UPDATE SET last_serial = quote_material_no_seq.last_serial + 1 " +
            "RETURNING last_serial").setParameter("code", code).setParameter("ym", yyMm).getSingleResult()).intValue();
        if (serial > 999999) throw new IllegalStateException("单客户单月流水溢出(>999999): " + code + "-" + yyMm);
        String report = code + "-" + yyMm + String.format("%06d", serial);
        em.createNativeQuery(
            "INSERT INTO material_customer_map(system_type, material_no, customer_no, customer_product_no, production_no, created_at, updated_at) " +
            "VALUES ('QUOTE', :m, :c, NULL, NULL, NOW(), NOW()) ON CONFLICT (material_no) WHERE system_type='QUOTE' DO NOTHING")
            .setParameter("m", report).setParameter("c", customerNo).executeUpdate();
        return report;
    }

    /** 料号有文件值：确保 QUOTE 行存在，幂等；命中别客户→抛 CrossCustomerQuoteNoException。 */
    public void ensureRegistered(String customerNo, String quoteNo) {
        int rows = em.createNativeQuery(
            "INSERT INTO material_customer_map(system_type, material_no, customer_no, customer_product_no, production_no, created_at, updated_at) " +
            "VALUES ('QUOTE', :m, :c, NULL, NULL, NOW(), NOW()) ON CONFLICT (material_no) WHERE system_type='QUOTE' DO NOTHING")
            .setParameter("m", quoteNo).setParameter("c", customerNo).executeUpdate();
        if (rows == 0) {
            String owner = (String) em.createNativeQuery(
                "SELECT customer_no FROM material_customer_map WHERE material_no=:m AND system_type='QUOTE'")
                .setParameter("m", quoteNo).getSingleResult();
            if (!customerNo.equals(owner))
                throw new CrossCustomerQuoteNoException("报价料号跨客户串号: " + quoteNo + " 属 " + owner + " 却在 " + customerNo + " 导入中出现");
        }
    }
}
```

> 注：`ON CONFLICT (material_no) WHERE system_type='QUOTE'` 需要 V309 建好部分唯一索引 `uq_mcm_quote_no` 才能作为 arbiter。**因此本 Task 的测试须在 Task 9（V309）之后跑**，或在测试类里先手工建该索引。为解耦，**调整执行顺序**：Task 9 的索引 DDL 提前——见 Task 9 备注"索引可独立于回填先建"。实现时若 Task 2 测试因缺索引失败，先执行 Task 9 的 `CREATE UNIQUE INDEX uq_mcm_quote_no ...` 再回跑。

- [ ] **Step 4: 跑测试确认通过**（前提：`uq_mcm_quote_no` 已存在）

Run: `cd cpq-backend && ./mvnw -q -o test -Dtest=QuoteMaterialNoAllocatorTest`
Expected: 4 tests PASS。

- [ ] **Step 5: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/basicdata/v6/service/QuoteMaterialNoAllocator.java \
        cpq-backend/src/test/java/com/cpq/basicdata/v6/service/QuoteMaterialNoAllocatorTest.java
git commit -m "feat(quote-no): QuoteMaterialNoAllocator 发号服务(四位码+流水+登记+跨客户守卫)"
```

---

## Task 3：MaterialCustomerMapRepository 改造

**Files:**
- Modify: `.../v6/repository/MaterialCustomerMapRepository.java`（`MapRow`、`upsert`、`upsertChunk`、`coalesceOver`、`deleteByCustomerNo`）
- Test: 复用 `QuoteMaterialNoIntegrationTest`（Task 5 建）覆盖；本任务先加 repo 级单测方法

- [ ] **Step 1: 写失败测试**（加进 `QuoteMaterialNoAllocatorTest` 同包新测试类 `MaterialCustomerMapRepositoryTest`）

```java
// 关键断言：
// 1. deleteByCustomerNo(c, QUOTE-only) 只删 system_type='QUOTE' AND customer_product_no IS NOT NULL 的行；
//    PRICING 行 + customer_product_no IS NULL 的组件行存活。
// 2. upsertQuote(report, cust, prodNo=X) 命中已登记的 (report, NULL) 行 → customer_product_no 被回填成 X。
// 3. upsertQuote 命中别客户的 report 行 → 返回 0 行（客户守卫），不改客户。
```
（完整测试体在实现时按 §3 契约展开；至少三条断言各一 @Test。）

- [ ] **Step 2: 改 `MapRow`**：增 `String systemType; String productionNo;`（构造与调用点同步）。

- [ ] **Step 3: 改 `deleteByCustomerNo`**：签名加 scope，QUOTE replace 专用：
```java
/** Q02 replace-per-customer 专用：只删该客户 QUOTE 侧「客户料号映射行」(customer_product_no 非空)，
 *  不动 PRICING 行、也不动组件登记行(customer_product_no IS NULL)。 */
public int deleteQuoteMappingsByCustomerNo(String customerNo) {
    return em.createNativeQuery(
        "DELETE FROM material_customer_map WHERE customer_no=:c AND system_type='QUOTE' AND customer_product_no IS NOT NULL")
        .setParameter("c", customerNo).executeUpdate();
}
```
（保留原 `deleteByCustomerNo` 或标记 @Deprecated；确认无其它 caller —— `grep`/`codegraph_callers` 显示唯一 caller 是 Q02。）

- [ ] **Step 4: 改 QUOTE 专用 upsert**（客户守卫 + `customer_product_no` 进 SET）：
```java
/** QUOTE 行 upsert：冲突键 material_no(部分索引)，客户守卫 WHERE customer_no=EXCLUDED。
 *  返回受影响行数；0 = 跨客户命中(串号)。撞 uq_mcm_quote_cust_prod 会抛 unique_violation，由调用方 per-row 捕获。 */
public int upsertQuote(MapRow r, java.util.UUID updatedBy) {
    return em.createNativeQuery(
      "INSERT INTO material_customer_map(system_type, material_no, customer_no, customer_name, customer_material_name, " +
      "  customer_product_no, customer_drawing_no, seq_no, payment_method, base_currency, quote_currency, exchange_rate, " +
      "  production_no, created_at, updated_at, updated_by) " +
      "VALUES ('QUOTE', :mn,:cn,:cnm,:cmn,:cpn,:cdn,:seq,:pm,:bc,:qc,:er,:pno,NOW(),NOW(),:by) " +
      "ON CONFLICT (material_no) WHERE system_type='QUOTE' DO UPDATE SET " +
      "  customer_product_no = EXCLUDED.customer_product_no, " +   // ★ 回填组件行的 NULL
      "  customer_name = COALESCE(EXCLUDED.customer_name, material_customer_map.customer_name), " +
      "  customer_material_name = COALESCE(EXCLUDED.customer_material_name, material_customer_map.customer_material_name), " +
      "  customer_drawing_no = COALESCE(EXCLUDED.customer_drawing_no, material_customer_map.customer_drawing_no), " +
      "  seq_no = COALESCE(EXCLUDED.seq_no, material_customer_map.seq_no), " +
      "  payment_method = COALESCE(EXCLUDED.payment_method, material_customer_map.payment_method), " +
      "  base_currency = COALESCE(EXCLUDED.base_currency, material_customer_map.base_currency), " +
      "  quote_currency = COALESCE(EXCLUDED.quote_currency, material_customer_map.quote_currency), " +
      "  exchange_rate = COALESCE(EXCLUDED.exchange_rate, material_customer_map.exchange_rate), " +
      "  production_no = COALESCE(EXCLUDED.production_no, material_customer_map.production_no), " +
      "  updated_at = NOW(), updated_by = EXCLUDED.updated_by " +
      "WHERE material_customer_map.customer_no = EXCLUDED.customer_no")   // ★ 客户守卫
      .setParameter("mn", r.materialNo)./* ...绑定其余参数... */setParameter("by", updatedBy).executeUpdate();
}
```

- [ ] **Step 5: 改 PRICING 路径 + `coalesceOver` dedup 键 + `upsertChunk`**：
  - PRICING 侧保留原复合键 upsert，写入时 `system_type='PRICING'`。
  - `coalesceOver` 折叠 dedup 键 `List.of(materialNo, customerNo, customerProductNo)` → 加 `systemType`。
  - `upsertChunk` 若仍用于 PRICING 批量：11 列 → 13 列（加 `system_type`/`production_no`），`b=i*11` 步长改 `i*13`，VALUES 占位与绑定同步。

- [ ] **Step 6: 跑测试确认通过**

Run: `cd cpq-backend && ./mvnw -q -o test -Dtest=MaterialCustomerMapRepositoryTest`
Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git commit -am "feat(quote-no): MaterialCustomerMapRepository system_type+客户守卫upsert+delete收窄"
```

---

## Task 4：MaterialNoResolver 改造（发号收敛，取消 9 字头）

**Files:**
- Modify: `.../v6/service/MaterialNoResolver.java`
- Test: `.../v6/service/MaterialNoResolverTest.java`（若无则建）

- [ ] **Step 1: 写失败测试**

断言：`BatchState` 带 `customerNo/yyMm` 时，`resolve(null, "新料件名", batch)` 返回 `XXXX-YYMMNNNNNN`（非 9 字头）；同批同名复用同号；`resolve("已有报价料号","名",batch)` 直接返回该值并确保登记；`resolve(null,null,batch)` 抛 `MaterialNoUnresolvableException`。

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 改实现**

```java
@Inject QuoteMaterialNoAllocator allocator;   // 新增注入

public static final class BatchState {
    final Map<String,String> nameToNo = new HashMap<>();
    public String customerNo;   // 新增：调用方从 ctx.customerNo 注入
    public String yyMm;         // 新增：导入当时年月（如 java.time.YearMonth.now() → "yyMM"）
    // 删除 batchMaxGenerated / dbMax（9字头专用）
}

public String resolve(String materialNo, String materialName, BatchState state) {
    String no = trimToNull(materialNo);
    if (no != null) { allocator.ensureRegistered(state.customerNo, no); return no; }   // 通道②查不到即登记
    String name = trimToNull(materialName);
    if (name == null) throw new MaterialNoUnresolvableException("料号与名称均为空，无法解析/生成料号");
    String cached = state.nameToNo.get(name);
    if (cached != null) return cached;
    var existing = repo.findFirstByMaterialName(name);   // 按名复用既有报价料号
    if (existing.isPresent()) { String e = existing.get().materialNo; state.nameToNo.put(name, e); return e; }
    String minted = allocator.mintAndRegister(state.customerNo, state.yyMm);   // 取代 9 字头
    state.nameToNo.put(name, minted);
    return minted;
}
// 删除 generateNextMaterialNo（9字头）。resolveMatchOnly 保留不动（Q05 用）。
```

> `maxNineLeadingMaterialNo`/`lockForMaterialNoGeneration`（在 `MaterialMasterRepository`）：先 `codegraph_callers` 确认报价侧外无引用，无则删；有则保留仅停报价侧调用。

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: Commit**
```bash
git commit -am "feat(quote-no): MaterialNoResolver 发号改走allocator,取消9字头生成路径"
```

---

## Task 5：Q02 + 8 handler 接线 + 集成测试

**Files:**
- Modify: `Q02CustomerMapHandler.java`、`MaterialBomMergeHandler.java`、`Q04/Q06/Q07/Q08/Q09/Q10/Q13*Handler.java`、`P05CustomerMapHandler.java`
- Test: `.../v6/quote/QuoteMaterialNoIntegrationTest.java`

- [ ] **Step 1: 写失败集成测试**

覆盖：① Q02 写入行 `system_type='QUOTE'`、relabel 列（「报价料号」或回退「宏丰料号」）；② Q02 先删后插只删 `customer_product_no IS NOT NULL` 的 QUOTE 行、组件登记行与 PRICING 行存活；③ `MaterialBomMergeHandler` 无号有名组件 mint 报价料号（非 9 字头）并登记；④ 一个费用 handler（如 `Q07`）同样 mint；⑤ 跨客户：把 C1 的报价料号放进 C2 的 BOM → `recordError` 含"跨客户串号"；⑥ P05 写入 `system_type='PRICING'`。

- [ ] **Step 2: 改 8 个 handler 的 `BatchState` 构造**

每个 handler 在 `new MaterialNoResolver.BatchState()` 后补：
```java
batch.customerNo = ctx.customerNo;
batch.yyMm = java.time.YearMonth.now().format(java.time.format.DateTimeFormatter.ofPattern("yyMM"));
```
（`ctx.customerNo` 在 `QuoteImportService:87` 已设，8 handler 均在其下运行，非空。）

- [ ] **Step 3: 改 Q02**：relabel 读列（`row.getStr("报价料号","宏丰料号")`）；改调 `repo.upsertQuote(...)`（`system_type='QUOTE'`）；`repo.deleteByCustomerNo`→`deleteQuoteMappingsByCustomerNo`；upsert 外套 **per-row try-catch**：0 行 → `recordError("报价料号跨客户串号")`；捕 `unique_violation`（客户料号 1:1 冲突）→ `recordError("客户料号1:1冲突")`，不毒死整 sheet。

- [ ] **Step 4: 改 P05**：写入行 `system_type='PRICING'`（走 PRICING 复合键 upsert，不受报价料号影响）。

- [ ] **Step 5: 跑测试 + E2E 前置**

Run: `cd cpq-backend && ./mvnw -q -o test -Dtest=QuoteMaterialNoIntegrationTest`
Expected: 全 PASS。

- [ ] **Step 6: Commit**
```bash
git commit -am "feat(quote-no): Q02/P05/8handler 接入报价料号发号+system_type"
```

---

## Task 6：读点/视图过滤（§5 交付项）

**Files:**
- Modify: `CustomerPartCandidateService.java`、`V6QuotationCommitService.java`
- Create: `V310__v12_part_mapping_system_type.sql`（DROP+重建 `v12_part_mapping` 视图）

- [ ] **Step 1: 用 codegraph/grep 定位真实行号**（`material_customer_map` 在这两个 service 的读点 + V255 视图定义）。

- [ ] **Step 2: 改 `CustomerPartCandidateService`**（按 §5 Chain-1 更正后）：
  - 回退路径裸子查询 `WHERE p.material_no IN (SELECT material_no FROM material_customer_map)` → `... WHERE system_type='QUOTE' AND customer_product_no IS NOT NULL`。
  - 回退路径 `LEFT JOIN material_customer_map m ...` → 加 `AND m.system_type='QUOTE'`。
  - V6 路径 `LEFT JOIN material_customer_map m ...` → 加 `AND m.system_type='QUOTE'`（Chain-1 遗漏点）。
  - V6 路径 `IN :hfs` 不改（上游已过滤）。

- [ ] **Step 3: 改 `V6QuotationCommitService:53`** → 加 `AND system_type='QUOTE'`（已含 `customer_product_no IS NOT NULL`）。

- [ ] **Step 4: 写 V310 重建 v12_part_mapping 视图**：复制 V255 中该视图定义，`LEFT JOIN material_customer_map mcm ... AND mcm.system_type='QUOTE' AND mcm.customer_product_no IS NOT NULL`。`DROP VIEW IF EXISTS v12_part_mapping CASCADE;` + 重建（含被 CASCADE 掉的依赖视图）。

- [ ] **Step 5: 迁移 + 重启自检**：Flyway 跑 V310 → `touch` java 强制 Quarkus 重启清 BNF 缓存（CLAUDE.md 视图重建纪律）→ 用一个含 BNF 路径的 endpoint 验证返单值不"共N项"。

- [ ] **Step 6: Commit**
```bash
git commit -am "fix(quote-no): 候选/commit/v12视图加 system_type='QUOTE'+customer_product_no隔离组件行"
```

---

## Task 7：V309 回填 + 约束（前提 Task 0 通过）

**Files:**
- Create: `V309__quote_material_no_backfill_constraints.sql`

- [ ] **Step 1: 写 V309**（按 Task 0 结论选强信号/强判据分类）：

```sql
-- ① 强信号回填 QUOTE
UPDATE material_customer_map SET system_type='QUOTE'
WHERE system_type IS NULL AND (customer_material_name IS NOT NULL OR customer_drawing_no IS NOT NULL
  OR payment_method IS NOT NULL OR base_currency IS NOT NULL OR quote_currency IS NOT NULL OR exchange_rate IS NOT NULL);

-- ② 歧义行强判据（Task 0 (B) 非 0 时启用；被报价侧 BOM/单价引用→QUOTE）
UPDATE material_customer_map m SET system_type='QUOTE'
WHERE system_type IS NULL AND m.material_no IN (
  SELECT material_no FROM element_bom  WHERE system_type='QUOTE'
  UNION SELECT material_no FROM material_bom WHERE system_type='QUOTE'
  UNION SELECT material_no FROM unit_price   WHERE system_type='QUOTE');

-- ③ 其余归 PRICING
UPDATE material_customer_map SET system_type='PRICING' WHERE system_type IS NULL;

-- ④ NOT NULL
ALTER TABLE material_customer_map ALTER COLUMN system_type SET NOT NULL;

-- ⑤ 索引（Task 0 (C)(D) 已校验为 0 / 已收敛）
DROP INDEX IF EXISTS uq_material_customer_map;
CREATE UNIQUE INDEX uq_mcm_composite ON material_customer_map(system_type, material_no, customer_no, customer_product_no) NULLS NOT DISTINCT;
CREATE UNIQUE INDEX uq_mcm_quote_no  ON material_customer_map(material_no) WHERE system_type='QUOTE';
CREATE UNIQUE INDEX uq_mcm_quote_cust_prod ON material_customer_map(system_type, customer_no, customer_product_no) WHERE system_type='QUOTE' AND customer_product_no IS NOT NULL;
```

> **备注（Task 2 解耦）**：`uq_mcm_quote_no` 是 allocator/Task 2 测试的前提。若执行顺序需 Task 2 先跑，可把 `CREATE UNIQUE INDEX uq_mcm_quote_no` 提到 V308 尾部（它不依赖回填——空表/新表也能建，存量校验由 Task 0 (C) 保证）。`uq_mcm_composite`/`uq_mcm_quote_cust_prod` 依赖回填与 (C)(D) 校验，留在 V309。

- [ ] **Step 2: 迁移 + 校验**：Flyway 跑 V309 → `flyway_schema_history` version 309 success=t；`SELECT count(*) FROM material_customer_map WHERE system_type IS NULL` = 0。

- [ ] **Step 3: Commit**
```bash
git commit -m "feat(quote-no): V309 存量system_type回填+NOT NULL+三索引"
```

---

## Task 8：全链路自检 + E2E

- [ ] **Step 1: 后端全量测试**：`cd cpq-backend && ./mvnw -q -o test`（至少新增测试全绿 + 无回归）。
- [ ] **Step 2: 报价 E2E**（改了 `MaterialBomMergeHandler` + V255/V310 视图，协议级）：按 `docs/E2E测试方法.md` 跑 `quotation-flow.spec.ts`，`1 passed` + `'加载中' final=0`。
- [ ] **Step 3: 自检声明**：TS/编译 0 错 ✅；V308/V309/V310 success=t ✅；后端 endpoint 401 ✅；E2E passed ✅；候选查询不冒组件行 ✅。
- [ ] **Step 4: 更新 RECORD.md + BACKLOG.md**（Spec 2/3 写入 Backlog 待开发）。

---

## 自检（写完计划回读 spec v4 的覆盖）

- §1.1 结构 → Task 1 + Task 7；§1.2 发号表 → Task 1；§2 allocator → Task 2；§3 repo/Q02 → Task 3+5；§4 8 handler → Task 4+5；§5 读点/视图 → Task 6；§6 存量 → Task 0+7；§7 边界（跨客户/1:1 冲突）→ Task 2(ensure)+3(upsert 守卫)+5(per-row 捕获)；§8 测试 → 各任务 TDD + Task 8。
- 无 TBD/占位（Task 0 结论占位是执行期数据，非设计缺口）。
- 类型一致：`mintAndRegister`/`ensureRegistered`/`getOrAllocateCustomerCode`/`CrossCustomerQuoteNoException`/`upsertQuote`/`deleteQuoteMappingsByCustomerNo`/`BatchState.customerNo/yyMm` 全计划一致。
