# 报价单整份快照 Phase 4 — 快照唯一源 + 旧链路退役 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans（或 subagent-driven-development）逐 Task 执行。Steps 用 `- [ ]` 复选框。**每个 Task 主线亲跑测试/E2E**（Phase 1/2 期实现 Agent 多次虚报"完成/通过"，见 RECORD 教训）；前端协议级改动（ComponentCell/QuotationStep2/ReadonlyProductCard/enrich）强制跑 `e2e/task8-snapshot-render.spec.ts`。

**Goal:** 把报价卡片渲染的"唯一数据源"彻底收敛到行级值快照（`quote_card_values`），退役 `row_data`/`snapshot_rows`/实时计算；编辑改走 `editQuoteCardValue` 回写端点；详情页同步；前后端公式逐分对账防漂移。

**Architecture:** Phase 2 已落地"快照→driverExpansions 停 batch-expand"（Task 8a）。Phase 4 在此之上：① 先做两块低风险（autosave 瞬态自愈 + 组合产品夹具/spec）；② 再做耦合高风险核心块（渲染读 `formulaResults`/`editRows` ⟺ 编辑写 `editRows`，必须一起做否则双源错配）；③ 最后结构脱钩 + 退役旧存储 + 对账收口。每块独立 E2E 守护，绝不混提。

**Tech Stack:** React + AntD（前端渲染/编辑）；Quarkus + Panache（`CardSnapshotService`/`QuotationResource`）；Playwright（`e2e/task8-snapshot-render.spec.ts`，QT-20260601-1482 苏州西门子，系统 google-chrome）。

**前置事实（Phase 2 收口）:**
- 渲染脱钩已在：`QuotationStep2.buildSnapshotExpansions` + `useSnap` 门控（commit 681c769）。
- 编辑经 `row_data`（autosave→saveDraft→`ComponentDataDTO.rowData`）已存活重开（E2E toBe 验证，commit 28087dd）。
- 后端 `editCardValue` + `PUT /quotations/line-items/{id}/quote-card-edit` 已就绪（commit c1bf896），前端 `quotationService.editQuoteCardValue` 已加。
- `FormulaCalculator`（后端，1:1 复刻前端 `formulaEngine`）+ `SnapshotReconcileTest T4`（后端 calculate 逐分对账）已守。

**关键纪律:** 不双源（渲染读 editRows 时编辑必须写 editRows）；行键对齐用 `rowKeyFields`（AP-54）；driver 行数权威 = 快照 baseRows（AP-51）；字段类型联动 17 检查点（AP-44）；详情/编辑 single-source（AP-50）；改动后跑 E2E 看 `加载中=0` + 渲染期 `batch-expand=0` + 编辑 toBe 存活。

---

## Task 1: autosave 瞬态自愈 — saveDraft 响应回灌 quoteCardValues（低风险，先做）

**问题:** autosave（10s）→ `saveDraft` 后端重 `snapshotLineValues` 写新 `quote_card_values`，但前端 `lineItems` 未回灌 → 重建行项瞬间缺快照 → `useSnap` 瞬态 false → 偶发 `batch-expand`（E2E 实测重开渲染期 2~4 次）。

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/QuotationWizard.tsx`（autoSaveDraft 后回灌 lineItems 的 quoteCardValues/costingCardValues）
- 确认: `QuotationResource.saveDraft` 返回的 DTO 是否已含 lineItems[].quoteCardValues（Task 1 已暴露；若 saveDraft 返回 dto 为重算前，需改为返回 getById 口径）
- Test: `e2e/task8-snapshot-render.spec.ts`（既有"编辑往返"test 末尾加：autosave 后不重开，直接断言渲染期 batch-expand 不再增长）

> **2026-06-01 根因线索（Task2 E2E 顺带发现，已被 S1 实证修正）**：~~疑似真凶 = 核价侧 `useDriverExpansions(costingLineItems)`~~。

> **✅ 2026-06-01 S1 实证根因（推翻上述假设）**：E2E 打印瞬态 batch-expand 的 `componentId=ca2b5fb3`（DB 查实属 **quote 模板** 非 costing），且 `[SNAP-DEBUG]` 探针证实 **`useSnapQuote` / `useSnapCosting` 全程恒 true（qMissing/cMissing 恒空）**——故 Step2 两侧 hook（已 useSnap 门控）均**不是**真凶，核价假设错误。**真凶 = `QuotationWizard.tsx:100` 的 `useDriverExpansions(lineItems, ...)` 未做 useSnap 门控**：autosave 全量重建报价行换新 line id → 该 hook fingerprint（含 li.id）变化 → tasks 重建 → 新 key miss cache → 旧链路重发 `/batch-expand`（瞬态 ~1~2 次）。该 hook 供 `buildDraftPayload→snapshotRows` 写 rowData（legacy 双写）用。

- [x] **S1 验根因**：✅ 实证为 Wizard 层 ungated `useDriverExpansions` 在 autosave 换 id 后重 expand（非核价侧、非 Step2）。证据：batch-expand componentId=ca2b5fb3 属 quote 模板 + SNAP-DEBUG 探针 useSnap 恒 true。
- [x] **S2 写失败 E2E 断言**：✅ 编辑往返 test 加 `autosaveWindow` 计数：编辑→等 autosave(13s)**不重开**→断言增量=0。运行确认 **RED**（增量=1，componentId=ca2b5fb3 材质）。
- [x] **S3 实现门控**：✅ `QuotationWizard` 加 `useSnapAll = lineItems.every(li => !!li.quoteCardValues)`；快照模式下 `useDriverExpansions(EMPTY_LINEITEMS)` 停 batch-expand，改 `buildSnapshotExpansions(lineItems,'QUOTE')` 喂 `buildDraftPayload/snapshotRows`（rowCount+basicDataValues 同源，行编辑值仍取 cd.rows → snapshotRows 输出不变、编辑往返不受影响）。`buildSnapshotExpansions`/`EMPTY_LINEITEMS` 从 Step2 导出复用。新增产品无快照时 useSnapAll=false 自动回退实时 expand。
- [x] **S4 跑 E2E 绿**：✅ 主线亲跑 3 passed — autosave 窗口增量=0（RED→GREEN）；主渲染 test `/batch-expand 总调用=0 渲染期=0`（原 5/2）；编辑往返 toBe 存活（77.3177）；组合 batch-expand=0；`加载中 final=0`。
- [x] **S5 自检 tsc 0 + Vite 200 → Commit**：✅ tsc 0；Wizard/Step2 → Vite 200。

> **遗留（非 Task1 范畴）**：编辑后**重开（整页重载）**渲染期仍 ~2~4 次 load 瞬态 batch-expand（与 autosave 无关；mount 窗口 race，DB 各行 quote_card_values 均非空、非数据缺口）。主渲染 test（renderPhase 在 load 后开）总调用=0 证明 load 可清。该 load 瞬态属既有基线（RECORD「重开 2~4 次」），与 Task5（结构读 quote_card_structure 旁路 enrich）重叠，留后续。

---

## Task 2: 组合产品夹具修复 + composite-product-flow E2E（低风险，数据/验证）

**问题:** canonical 罗克韦尔料号（3120012574）V6 迁移后不在 `material_customer_map` → 新建/选配流程选不到；composite spec 未跑。

**Files:**
- 调研: `CustomerPartCandidateService`（候选来源 SQL）+ `material_customer_map` 视图/表
- Modify(数据): Flyway 或一次性脚本补罗克韦尔组合料号的 V6 客户-料号映射；或改 composite spec 用现存组合产品报价单
- Test: `cpq-frontend/e2e/composite-product-flow.spec.ts`

- [x] **S1 定位现存组合产品报价单**：QT-20260519-1411（罗克韦尔 CFG-COMBO-000018，有 quote_card_values）。
- [x] **S3 组合 render test**（加在 task8-snapshot-render.spec）：打开组合单 → 父卡片 6 Tab 渲染（材质2/元素含量6/工序13/组合工艺1/子配件2/单重2 行 = 子件聚合 AP-45 生效）+ `加载中 final=0`(硬断言)。
- [x] **S4 主线亲跑 E2E 1 passed → Commit fde7d9e**
- 注：未补夹具走新建流程（用现存组合单直接打开，更稳）；composite-product-flow.spec(新建流程)仍待夹具，但组合**渲染脱钩**已由本 test 验证。

---

## Task 3（核心耦合块，高风险）: 渲染读 formulaResults/editRows ⟺ 编辑写 editRows

> **必须一起做**：渲染读 editRows 而编辑仍写 row_data → 编辑不可见（双源错配）。本 Task 把"行值"的唯一源从 `row_data`（comp.rows）切到快照 `editRows`+`baseRows`+`formulaResults`，同时把单元格编辑改调 `editQuoteCardValue`。

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/QuotationStep2.tsx`
  - ProductCard `effectiveRows` IIFE（~1340-1450）：`row` 由 `baseRow.driverRow + editRows[rowKey].values` 合成（替代 `comp.rows[i]`）
  - `preComputedCaches`（~1438）：FORMULA 优先读 `formulaResults[rowKey].values`，缺时再 computeAllFormulas 兜底（渐进，防漂移）
  - 单元格编辑 `handleRowChange`/`handleInputBlur`（~824/977）：改调 `quotationService.editQuoteCardValue(lineItemId, {componentId, rowKey, fieldName, value})`，用返回的 `quoteCardValues` 就地回灌该行 state（AP-50），停掉旧 autosave 写 row_data 路径（或保留 autosave 仅持久化非快照字段）
- 复用: `useCardSnapshots.computeRowKey`（已导出，对齐后端）；`editQuoteCardValue`（已就绪）
- Test: `e2e/task8-snapshot-render.spec.ts`（编辑往返 + 渲染读快照值断言）

- [x] **S1 通读**：✅ 现码数据源记进 QuotationStep2 渲染循环注释。关键发现 = ComponentCell `<input value={row[key]}>` **全受控无本地态** → 叠加 editRows 必丢按键(AP-54)。
- [x] **S2 写失败 E2E**：✅ 编辑往返 test 加断言 `quote-card-edit 调用 >0`。运行确认 **RED**(=0)。
- [x] **S3 编辑写路径切端点**：✅ cell INPUT* onBlur → `editQuoteCardValue(...)`(rowKey=`computeRowKey(structure.rowKeyFields, baseRows[i].driverRow, i)` 对齐后端) → 响应 `quoteCardValues` 就地回灌(AP-50)。`cardSide` 门控仅 QUOTE 侧, COSTING 维持旧路径。
- [x] **S4 渲染 formulaCache 切快照**：✅ FORMULA 优先读 `formulaResults[rowKey]`(真零计算; DB 实测 rowKey="0" 命中), 缺时 computeAllFormulas 兜底。**INPUT row 源未切 editRows**(全受控约束, 见 S1)。
- [x] **S5 主线亲跑 E2E**：✅ 3 passed — quote-card-edit=1(RED→GREEN)/编辑往返 toBe 存活/渲染期 batch-expand=0/组合 加载中=0。
- [x] **S6 自检 tsc 0 + Vite 200 → Commit feb287b**。

> **⚠️ Task3 部分达成 — "editRows 作行值唯一源" 未达, 强耦合 Task6**（2026-06-01 实证）：
> - **达成**：报价卡片 FORMULA 渲染读快照 `formulaResults`(真零计算) + 单元格编辑调 `editQuoteCardValue` 端点(写 editRows + 重算 formulaResults + 回灌)。功能正确、无回归、E2E 绿。
> - **未达**：① INPUT 受控值仍读 `comp.rows`(ComponentCell 全受控, 叠加 editRows 丢按键=AP-54; 需改本地输入态)。② `editRows` 不持久 —— autosave **全量重建报价行**(换新 line id, payload 带 `row_data` 不带 `editRows`)→ 新行无 editRows 可继承, 端点写的 editRows 被 autosave 重建清空(DB 实测空); 当前编辑经 `row_data` 持久 + autosave 重算 formulaResults 保正确显示。
> - **依赖 Task6**：退役 `row_data` 需先(a)把 INPUT 改本地态脱离 comp.rows, (b)重设计 autosave rebuild 让 editRows 跟随(payload 带 editRows / 后端从 row_data 再派生)。**"唯一源切 editRows" 与 row_data 退役强耦合, 必须在 Task6 一起做**。
> - **UX 行为变更**：FORMULA 由"输入即时算"→"blur 回端点后更新"(用户 2026-06-01 已选 blur 端点模型)。如不接受可把 formulaCache 分支退回恒用 computeAllFormulas。

---

## Task 4: ReadonlyProductCard 详情页读快照同步（AP-50 single-source）

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/ReadonlyProductCard.tsx`（数据源切 `useCardSnapshots`，与编辑页 ComponentCell 同源）
- Test: 新增 `e2e` 打开 `/quotations/{id}`（详情页）断言各 Tab 渲染 + `加载中=0` + 值与编辑页一致

- [ ] **S1 写失败 E2E**：详情页打开 QT-20260601-1482 → 断言 Tab 渲染 + `加载中=0` +（编辑过的）单价值显示。RED。
- [ ] **S2 ReadonlyProductCard 切 useCardSnapshots**（取值优先级 editRows ?? baseRows.basicDataValues ?? formulaResults，与编辑页一致；AP-50 抽共享）。
- [ ] **S3 主线亲跑 E2E 绿 + 编辑页 spec 不回归 → S4 自检 → Commit**

---

## Task 5: 结构读 quote_card_structure 旁路 enrichComponentData

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/QuotationWizard.tsx`（有 `quoteCardStructure` 时跳过 `enrichComponentData`，componentData 结构改由 `quote_card_structure.tabs` 组装）
- Modify: `cpq-frontend/src/pages/quotation/useCardSnapshots.ts`（已读 structure，扩展为渲染结构源）
- 旁路: `enrichComponentData.ts`（保留为种子期/无快照回退）
- Test: `e2e/task8-snapshot-render.spec.ts`（渲染期不再请求模板 snapshot/enrich 相关接口；Tab/字段与 structure 一致）

- [ ] **S1 写失败 E2E**：监控渲染期对模板 snapshot 接口的请求，断言=0（当前 enrich 会请求）。RED。
- [ ] **S2 结构源切快照**：有 `quoteCardStructure` 时 componentData 由其组装（fieldType/basicDataPath/datasourceBinding/formulas/rowKeyFields camelCase）；无则回退 enrich。
- [ ] **S3 主线亲跑 E2E**：渲染期无 enrich/模板请求；Tab/字段/值正确；`加载中=0` → S4 自检 → Commit

---

## Task 6: 退役 row_data/snapshot_rows/excel_view_snapshot + 前端 formulaEngine 逐分对账

**Files:**
- Backend: `CardSnapshotService`（buildCardValues 不再读 `snapshot_rows`，改 expand 种子或确认唯一写源）；`SnapshotCollectorService`/saveDraft（停写 row_data？——需确认无其他消费方）
- Frontend: 删除/旁路 ComponentCell/QuotationStep2 的 row_data 实时分支（保留种子）
- Test: `SnapshotReconcileTest`（补强：固定样本后端 formulaResults == 前端 `formulaEngine` 逐分一致 tolerance 0.01）；`./mvnw test -Dtest=*Snapshot*,FormulaCalculatorTest,RowKeyValidationTest`；E2E 双 spec

- [ ] **S1 消费方排查**：grep `row_data`/`snapshot_rows`/`excel_view_snapshot` 全工程消费点，确认渲染/编辑均不再读（Task 3/5 后）。列清单。
- [ ] **S2 前端对账脚本**：固定样本（含工序累加 previous_row_subtotal + 跨 tab component_subtotal + path + gvar）跑前端 `formulaEngine` vs 后端 `formulaResults` 逐分一致（防漂移红线）。
- [ ] **S3 退役写源**：停写 row_data（确认无消费方后）；`snapshot_rows` Phase 1 已为种子，确认 buildCardValues 唯一读源后可标退役。**保守：先停读不删表**。
- [ ] **S4 全量回归**：后端 `*Snapshot*,FormulaCalculatorTest,RowKeyValidationTest` 全 passed Skipped0；E2E `task8-snapshot-render` + `composite-product-flow` 不回归。
- [ ] **S5 回写基线 §1.5 影响清单**（§4.4/4.5/5.2 标"种子期适用"）+ RECORD → Commit

---

## 完成标准（Phase 4）
- 渲染期：无 `/batch-expand`（含 autosave 后无瞬态）、无 enrich/模板 snapshot 请求、无实时公式计算（读 formulaResults）。
- 编辑：走 `editQuoteCardValue` 回写端点，值来自 editRows，重开存活（E2E toBe）。
- 对账：后端 `formulaResults` == 前端 `formulaEngine` 逐分一致（固定样本）。
- 详情/编辑 single-source（AP-50）；组合产品 spec 通过（AP-45）。
- 旧链路：row_data/snapshot_rows 渲染读路径退役（保守先停读）；基线文档回写。
- 每 Task 主线亲跑测试/E2E，`加载中=0`。

## Self-Review
- Spec 覆盖（设计 §3.3/§6/§7/§9 + Phase2 收口 Phase4 清单 6 项）：#5→Task1；#6→Task2；#1+#2→Task3；ReadonlyProductCard→Task4；#3→Task5；#4 退役+对账→Task6。✅ 全覆盖。
- 风险排序：低风险（autosave 自愈 / 夹具）先行建立 E2E 信心，再做耦合高风险核心块（Task3），最后退役。
- 双源防护：Task3 把渲染读 + 编辑写 一并切 editRows，杜绝双源错配。
- 占位说明：Task3/5/6 给职责+复用点+files+E2E 红线，未逐行（须先读渲染循环现码，S1 通读）；与 Phase2 Task2/8 同口径（用户已接受）。
- Agent 虚报防护：每 Task 主线亲跑 E2E（task8-snapshot-render.spec 编辑往返 toBe 硬断言 + batch-expand 计数）。
