# 选配 V44 → V6 单写迁移 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把报价单「选配」写路从 V44+V6 双写切到 V6 单写（停写 `mat_part`/`mat_bom`/`mat_process`/`mat_composite_process`），并把 3 个仍读 V44 的下游改读 V6，使选配产品端到端可用。

**Architecture:** V6 基建大半已就位（`material_master` 全局表已带 `config_fingerprint`，选配已双写 material_master/element_bom_item/material_bom_item/unit_price/capacity，工序渲染已读 V6）。本计划=「停 V44 半边 + 指纹权威切 material_master + 补简单料号工序 V6 回显源 + 3 下游读路切 V6」。干净大切换，无开关。**不删 V44 表**（导入侧仍用，删表是后续）。

**Tech Stack:** Java 17 + Quarkus 3 + Hibernate Panache + 原生 SQL（`em.createNativeQuery`）+ PostgreSQL 16 + Flyway + Playwright E2E。

**关联 spec:** `docs/superpowers/specs/2026-06-02-选配V44到V6单写迁移-design.md`

**全部后端改动集中在:**
- `cpq-backend/.../configure/service/ConfigureProductService.java`（主）
- `cpq-backend/.../configure/service/MaterialRecipeService.java`（已局部改，无新增）
- `cpq-backend/.../quotation/snapshot/SnapshotCollectorService.java`
- `cpq-backend/.../quotation/service/DriftDetectionService.java`
- `cpq-backend/.../quotation/service/QuotationService.java`
- `cpq-backend/src/main/resources/db/migration/V284__material_master_fingerprint_unique.sql`（新；V282/V283 已被 COMBO 渲染修复占用）

**通用自检命令（每个后端任务结束跑）:**
```bash
# 触发 Quarkus dev 热重载并验证未 500
touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java
sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components   # 期望 401（非 500）
# 后端单测
cd cpq-backend && ./mvnw test -Dtest='ConfigureProductFlowTest,FormulaCalculatorTest' 2>&1 | grep -E "Tests run:|BUILD"
```
DB 连接（远程 env-2）：`PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db`

---

## Phase 0 — 设计前置调查（解决缺口 B + 锁定列映射）

> 这一阶段不改生产代码，产出 3 个明确决策写进本计划末尾「§决策记录」。**后续 Phase 依赖这些决策。**

### Task 0.1: 锁定简单料号工序的 V6 回显源

**Files:** 仅读 + 文档

- [ ] **Step 1: 读复用回显当前读 mat_process 的逻辑**

Run: `sed -n '128,170p' cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java`
看 `buildSnapshot` 如何用 `mat_process`（:142）+ `mat_composite_process`（:156）构造 reuse 回显的 processes 列表（字段：process_code / assembly_process / seq_no）。

- [ ] **Step 2: 读 V6 unit_price 与 quotation_line_process 的工序表征**

Run:
```bash
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c "\d unit_price" | grep -iE "operation_no|finished_material|code|customer|version|seq"
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c "\d quotation_line_process"
```
确认：unit_price 用 `operation_no`（=process code）+ group key(code/finished_material_no/customer_no) 表征"某料号的工序集"。

- [ ] **Step 3: 决策并记录**

回答并写入 §决策记录：
- D1：buildSnapshot 复用回显的 processes，改读 **unit_price.operation_no**（按 finished_material_no=hfPartNo + customer_no 过滤，ORDER BY seq_no）。理由：unit_price 是 V6 里"料号→工序"的权威落点，组合已在用。
- D2：简单料号是否需要写 unit_price？**需要** —— 否则 buildSnapshot 复用回显（改读 unit_price 后）对简单料号会空。故 Phase 2 给简单料号补 unit_price 写入（镜像 `insertProcessUnitPriceV6` 单料号版）。
- D3：`quotation_line_process`（per-quote 工序，:1225）已是 V6，**不动**。

### Task 0.2: 锁定 3 下游读路的 V6 列映射

**Files:** 仅读 + 文档

- [ ] **Step 1: 读 3 处读 V44 的现状 SQL + 周边用法**

Run:
```bash
sed -n '245,280p' cpq-backend/src/main/java/com/cpq/quotation/snapshot/SnapshotCollectorService.java
sed -n '348,362p' cpq-backend/src/main/java/com/cpq/quotation/service/DriftDetectionService.java
sed -n '1625,1672p' cpq-backend/src/main/java/com/cpq/quotation/service/QuotationService.java
```

- [ ] **Step 2: 查 V6 等价列**

Run:
```bash
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c "SELECT column_name FROM information_schema.columns WHERE table_name='material_master';"
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c "SELECT column_name FROM information_schema.columns WHERE table_name='element_bom_item';"
```

- [ ] **Step 3: 决策并记录（§决策记录 D4）**

写出每处的 V44列→V6列映射表：
- SnapshotCollector:251 `mat_part(hf_part_no,part_name,material,unit_weight,unit)` → `material_master(material_no, material_type, unit_weight)` + 缺失列（part_name/unit）的兜底来源（material_master 无则取 NULL 或 material_no）。
- SnapshotCollector:269 `mat_bom(hf_part_no,bom_type,element_name,quantity)` → `element_bom_item(hf_part_no/material_no, component_no AS element_name, content AS quantity)` + is_current/MAX(characteristic) 规范过滤（对齐 ys_view）。
- DriftDetection:355 `mat_bom.element_name` → `element_bom_item.component_no`（+ is_current 过滤）。
- loadLineItems:1633/1666 `mat_part(part_no,product_type)` → `material_master(material_no, '——' 或 material_type)`（product_type 在 V6 无对应，按 D4 决定兜底值）。

---

## Phase 1 — 指纹权威切 V6（独立、可先上）

### Task 1.1: material_master.config_fingerprint 加唯一索引

**Files:**
- Create: `cpq-backend/src/main/resources/db/migration/V284__material_master_fingerprint_unique.sql`（V282/V283 已被占用）

- [ ] **Step 1: 先查存量是否有重复 fingerprint（建索引前置）**

Run:
```bash
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c \
"SELECT config_fingerprint, COUNT(*) c FROM material_master WHERE config_fingerprint IS NOT NULL GROUP BY config_fingerprint HAVING COUNT(*)>1;"
```
Expected: 空（无重复）。**若非空**：先人工清理重复行（保留 created_at 最早的一条），否则下一步建索引失败。

- [ ] **Step 2: 写 Flyway 迁移（partial unique index，忽略 NULL）**

```sql
-- V284: 全局指纹去重约束迁到 V6（替代 V44 uq_mat_part_fingerprint）。
-- partial：仅对非 NULL config_fingerprint 唯一（导入的 V6 料号 fingerprint 为 NULL，不受约束）。
CREATE UNIQUE INDEX IF NOT EXISTS uq_material_master_fingerprint
    ON material_master (config_fingerprint)
    WHERE config_fingerprint IS NOT NULL;
```

- [ ] **Step 3: 让 Quarkus dev 自动跑迁移（勿手工 psql -f）**

Run:
```bash
touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java
sleep 10
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c \
"SELECT version, success FROM flyway_schema_history WHERE version='284';"
```
Expected: `284|t`

- [ ] **Step 4: 提交**

```bash
git add cpq-backend/src/main/resources/db/migration/V284__material_master_fingerprint_unique.sql
git commit -m "feat(selopt-v6): material_master.config_fingerprint 加全局唯一索引（指纹权威切 V6 前置）"
```

### Task 1.2: lookupHfByFingerprint 改查 material_master

**Files:**
- Modify: `cpq-backend/.../configure/service/ConfigureProductService.java:115-127`（lookupHfByFingerprint）

- [ ] **Step 1: 读当前实现确认返回处理**

Run: `sed -n '115,127p' cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java`

- [ ] **Step 2: 改 SQL：mat_part → material_master**

把 :121 的查询
```java
"SELECT part_no FROM mat_part WHERE config_fingerprint = :fp"
```
改为
```java
// 2026-06-02 指纹权威切 V6：material_master.config_fingerprint 全局唯一（uq_material_master_fingerprint）
"SELECT material_no FROM material_master WHERE config_fingerprint = :fp"
```
（其余 setParameter("fp", fp) / 取 first 列逻辑不变。）

- [ ] **Step 3: 自检（热重载 + 直查复用判定）**

Run（用一个已知 fp 验证仍能命中）:
```bash
touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components   # 401
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c \
"SELECT material_no, config_fingerprint FROM material_master WHERE config_fingerprint IS NOT NULL LIMIT 3;"
```
Expected: 401；material_master 有带 fingerprint 的行。

- [ ] **Step 4: 提交**

```bash
git add cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java
git commit -m "feat(selopt-v6): lookupHfByFingerprint 指纹查重切 material_master"
```

---

## Phase 2 — 补简单料号工序 V6 写入（缺口 B，依赖 D2）

### Task 2.1: 新增简单料号 unit_price 工序写入方法

**Files:**
- Modify: `cpq-backend/.../configure/service/ConfigureProductService.java`（新增方法，紧邻 `insertProcessUnitPriceV6` :765 之后）

- [ ] **Step 1: 复读组合版 insertProcessUnitPriceV6 作模板**

Run: `sed -n '765,810p' cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java`

- [ ] **Step 2: 新增单料号版（group key 的 code 与 finished_material_no 都用 hfPartNo）**

```java
/**
 * 2026-06-02 缺口 B：简单料号工序 → V6 unit_price（镜像组合版 insertProcessUnitPriceV6）。
 * 简单料号无父子，group key 的 code = finished_material_no = hfPartNo。
 */
void insertProcessSimpleUnitPriceV6(String hfPartNo, List<UUID> processIds, String customerCode) {
    if (customerCode == null || customerCode.isBlank()) return;
    if (processIds == null || processIds.isEmpty()) return;
    List<Map<String, Object>> rows = new ArrayList<>();
    int seq = 1;
    for (UUID processId : processIds) {
        List<Object> codes = em.createNativeQuery(
                "SELECT code FROM process WHERE id = :id")
            .setParameter("id", processId).getResultList();
        if (codes.isEmpty() || codes.get(0) == null) {
            throw new IllegalArgumentException("工艺不存在: " + processId);
        }
        String opNo = codes.get(0).toString();
        String currency = "CNY";
        String unit = "KG";
        List<Object[]> pm = em.createNativeQuery(
                "SELECT standard_currency, standard_unit FROM process_master WHERE process_no = :c")
            .setParameter("c", opNo).getResultList();
        if (!pm.isEmpty()) {
            Object[] m = pm.get(0);
            if (m[0] != null && !m[0].toString().isBlank()) currency = m[0].toString();
            if (m[1] != null && !m[1].toString().isBlank()) unit = m[1].toString();
        }
        Map<String, Object> r = new LinkedHashMap<>();
        r.put("operation_no", opNo);
        r.put("seq_no", seq++);
        r.put("currency", currency);
        r.put("unit", unit);
        rows.add(r);
    }
    Map<String, Object> gk = new LinkedHashMap<>();
    gk.put("system_type", "QUOTE");
    gk.put("price_type", "MATERIAL");
    gk.put("cost_type", "自制加工费");
    gk.put("customer_no", customerCode);
    gk.put("code", hfPartNo);
    gk.put("finished_material_no", hfPartNo);
    versionedWriter.writeVersionedGroup(new VersionedGroupSpec(
        "unit_price", "version_no", gk,
        List.of("operation_no", "seq_no", "currency", "unit"), rows));
}
```

- [ ] **Step 3: 自检（编译）**

Run: `touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java; sleep 9; grep -iE "error|cannot find" /tmp/cpq-backend.log | tail -3 || echo "无编译错"`
Expected: 无编译错；components=401。

- [ ] **Step 4: 提交**

```bash
git add cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java
git commit -m "feat(selopt-v6): 新增简单料号工序 V6 unit_price 写入（缺口 B）"
```

### Task 2.2: resolvePart 简单路径改调 V6 工序写入

**Files:**
- Modify: `ConfigureProductService.java`（resolvePart 简单路径 + existing 路径的 insertProcesses 调用点：:259, :301，需 customerCode 入参）

- [ ] **Step 1: 读 resolvePart 签名与两处 insertProcesses 调用上下文**

Run: `sed -n '186,322p' cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java | grep -nE "resolvePart|insertProcesses|customerCode|customerId"`
确认 resolvePart 已有 `customerCode` 入参（spec §3.3 确认有）。

- [ ] **Step 2: 把 :301（新建路径）的 V44 工序写入替换为 V6**

```java
// 原：insertProcesses(hfPartNo, pr.processIds, customerId);  // 写 mat_process(V44)
insertProcessSimpleUnitPriceV6(hfPartNo, pr.processIds, customerCode);  // 写 unit_price(V6)
```

- [ ] **Step 3: 把 :259（existing+processIds 覆盖路径）同样替换**

```java
// 原：insertProcesses(pr.existingHfPartNo, pr.processIds, customerId);
insertProcessSimpleUnitPriceV6(pr.existingHfPartNo, pr.processIds, customerCode);
```
（注：原 existing 路径前有 `DELETE mat_process`（:243/:254）—— versionedWriter.writeVersionedGroup 自带"按 group key 升版覆盖"，无需手工 DELETE。Phase 3 Task 3.1 会删那两条 DELETE。）

- [ ] **Step 4: 自检 + 提交**

```bash
touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components
git add -A && git commit -m "feat(selopt-v6): resolvePart 简单料号工序改写 V6 unit_price"
```

---

## Phase 3 — 停所有 V44 写入 + 删死代码

### Task 3.1: 删 resolvePart 内 V44 写入/删除调用

**Files:**
- Modify: `ConfigureProductService.java`（resolvePart :202/:243/:254/:293/:294 + composite :1009/:1010/:1012）

- [ ] **Step 1: 删简单路径 V44 写入（:293 insertMatPart, :294 insertElementBom）**

删除这两行调用（V6 的 insertMaterialMasterV6/insertElementBomV6 在 :308/:309 已写）。

- [ ] **Step 2: 删 existing 路径的 DELETE mat_process（:243/:254）**

删除这两条 `DELETE FROM mat_process ...`（unit_price 升版自带覆盖）。

- [ ] **Step 3: 删 :202 的 mat_part 存在性校验**

把 `SELECT 1 FROM mat_part WHERE part_no=:p` 改为查 material_master：
```java
"SELECT 1 FROM material_master WHERE material_no = :p"
```
（这是"存在性校验"读，不是写；切 V6 而非删除。）

- [ ] **Step 4: 删组合路径 V44 写入（:1009 insertMatPart, :1010 insertAssemblyBom, :1012 insertCompositeProcesses）**

删除这三行（V6 的 insertMaterialMasterV6/insertMaterialBomAssemblyV6/insertCompositeProcessCapacityV6 在 :1020/:1027/:1028 已写）。

- [ ] **Step 5: 自检**

Run:
```bash
touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components   # 401
grep -iE "error|cannot find symbol" /tmp/cpq-backend.log | tail -3 || echo "无编译错"
```

- [ ] **Step 6: 提交**

```bash
git add -A && git commit -m "feat(selopt-v6): resolvePart/composite 停写 V44 mat_part/mat_bom/mat_process"
```

### Task 3.2: 删双写桥 + 死代码方法

**Files:**
- Modify: `ConfigureProductService.java`（删方法 backfillV44FromV6:593 / backfillV6FromV44:614 / copyElementBom:445 / readElementsFromMatBom:429 + 其调用点 :209/:214）

- [ ] **Step 1: 删 backfillV6FromV44 / backfillV44FromV6 调用点（:209/:214）**

读 `sed -n '205,220p'` 确认上下文，删除这两个 backfill 调用（V6 单写后不需要 V44↔V6 桥）。

- [ ] **Step 2: 删 4 个方法体**

删除方法定义：`backfillV44FromV6`（593-613）、`backfillV6FromV44`（614-642）、`copyElementBom`（445-455）、`readElementsFromMatBom`（429-444）。
（copyElementBom/readElementsFromMatBom 已确认全工程无调用=死代码。）

- [ ] **Step 3: 确认无残留引用**

Run:
```bash
grep -nE "backfillV44FromV6|backfillV6FromV44|copyElementBom|readElementsFromMatBom" cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java
```
Expected: 空。

- [ ] **Step 4: 自检 + 提交**

```bash
touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components
git add -A && git commit -m "refactor(selopt-v6): 删 V44↔V6 双写桥 + copyElementBom/readElementsFromMatBom 死代码"
```

### Task 3.3: 删 insertProcesses / insertMatPart / insertElementBom / insertAssemblyBom / insertCompositeProcesses 方法体

**Files:**
- Modify: `ConfigureProductService.java`（删方法体 + 残留调用：insertProcessesWithLineItemId、backfillProcessesForNewCustomer 见 Task 4.2）

- [ ] **Step 1: 确认这些方法已无调用（Phase 3.1 删调用后）**

Run:
```bash
for m in insertMatPart insertElementBom insertProcesses insertAssemblyBom insertCompositeProcesses; do echo "$m: $(grep -c "$m(" cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java)"; done
```
Expected: 各为 1（仅定义）。**若 insertProcesses >1**：还有 insertProcessesWithLineItemId / backfillProcessesForNewCustomer 在引用，留到 Task 4.2 处理后再删。

- [ ] **Step 2: 删确认无调用的方法体**

删除 `insertMatPart`(374)/`insertElementBom`(456)/`insertProcesses`(888)/`insertAssemblyBom`(1102)/`insertCompositeProcesses`(1118) 中**计数=1** 的那些方法体。

- [ ] **Step 3: 自检 + 提交**

```bash
touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components
git add -A && git commit -m "refactor(selopt-v6): 删无调用的 V44 写入方法体"
```

---

## Phase 4 — 选配内 V44 读改 V6

### Task 4.1: buildSnapshot 复用回显改读 V6（依赖 D1）

**Files:**
- Modify: `ConfigureProductService.java:128-170`（buildSnapshot）

- [ ] **Step 1: 读 buildSnapshot 全貌**

Run: `sed -n '128,170p' cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java`

- [ ] **Step 2: :133 单重 mat_part → material_master**

```java
// 原：SELECT unit_weight FROM mat_part WHERE part_no = :p
"SELECT unit_weight FROM material_master WHERE material_no = :p"
```

- [ ] **Step 3: :142 工序 mat_process → unit_price（按 D1）**

把读 `mat_process`（process_code/assembly_process/seq_no）的查询改为读 `unit_price`：
```java
// 复用回显工序：读 V6 unit_price 的 operation_no（=工序码），按本料号+客户当前版本
"SELECT operation_no, seq_no FROM unit_price " +
"WHERE finished_material_no = :p AND customer_no = :c AND cost_type = '自制加工费' " +
"  AND is_current = true ORDER BY seq_no"
```
（回显 DTO 的 processCode 取 operation_no；assembly_process 字段 V6 无 → 置空/默认。具体 DTO 字段对照 Step 1 读到的结构调整。）

- [ ] **Step 4: :156 组合工艺 mat_composite_process → capacity**

```java
// 组合工艺回显：读 V6 capacity 的 process_no
"SELECT process_no, seq_no FROM capacity " +
"WHERE material_no = :p AND resource_group_no = 'QUOTE_ASSEMBLY' AND is_current = true ORDER BY seq_no"
```

- [ ] **Step 5: 自检（复用回显往返）**

Run（用一个 material_master + unit_price 都有的料号，验回显非空）:
```bash
touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components
```
（E2E 在 Phase 6 统一验。）

- [ ] **Step 6: 提交**

```bash
git add -A && git commit -m "feat(selopt-v6): buildSnapshot 复用回显改读 V6（material_master/unit_price/capacity）"
```

### Task 4.2: backfillProcessesForNewCustomer 改 V6（或评估删除）

**Files:**
- Modify: `ConfigureProductService.java:398-428`（backfillProcessesForNewCustomer）

- [ ] **Step 1: 读该方法 + 调用点，判断 V6 下是否仍需要**

Run: `grep -n "backfillProcessesForNewCustomer(" cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java; sed -n '398,428p' cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java`
判断：它做"复用料号到新客户时，把 mat_process 复制到新 customer_id"。V6 下工序在 unit_price（按 customer_no）。

- [ ] **Step 2: 决策（写入 §决策记录 D5）**

- 若 unit_price 已按 customer_no 隔离且复用路径会经 insertProcessSimpleUnitPriceV6 重写 → **删除** backfillProcessesForNewCustomer（不再需要 V44 复制）。
- 否则改为 unit_price 复制（SELECT operation_no FROM unit_price WHERE finished_material_no=:p AND customer_no=源 → insertProcessSimpleUnitPriceV6 到新 customer）。

- [ ] **Step 3: 按决策改/删 + 删 insertProcesses/insertProcessesWithLineItemId 剩余方法体（Task 3.3 留的）**

按 D5 执行；执行后回到 Task 3.3 Step 1 重查计数，删掉现在=1 的 insertProcesses/insertProcessesWithLineItemId 方法体。

- [ ] **Step 4: 确认 ConfigureProductService 内已无 V44 写/读残留**

Run:
```bash
grep -nE "(FROM|JOIN|INTO|UPDATE|DELETE FROM)\s+(mat_part|mat_bom|mat_process|mat_composite_process)" cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java
```
Expected: 空（注释除外）。

- [ ] **Step 5: 自检 + 提交**

```bash
touch cpq-backend/src/main/java/com/cpq/configure/service/ConfigureProductService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components
git add -A && git commit -m "feat(selopt-v6): backfillProcessesForNewCustomer 切 V6 + 清空 ConfigureProductService 内 V44"
```

---

## Phase 5 — 3 下游读路切 V6（依赖 D4）

### Task 5.1: SnapshotCollectorService 改读 V6

**Files:**
- Modify: `cpq-backend/.../quotation/snapshot/SnapshotCollectorService.java:251, :269`

- [ ] **Step 1: 读两处查询 + 结果消费方式**

Run: `sed -n '245,285p' cpq-backend/src/main/java/com/cpq/quotation/snapshot/SnapshotCollectorService.java`

- [ ] **Step 2: :251 mat_part → material_master（按 D4 映射）**

```java
// 原：SELECT hf_part_no, part_name, material, unit_weight, unit FROM mat_part WHERE ...
"SELECT material_no AS hf_part_no, material_no AS part_name, material_type AS material, " +
"       unit_weight, NULL AS unit FROM material_master WHERE material_no IN (:pns)"
```
（part_name/unit 在 material_master 无对应列，按 D4 用 material_no / NULL 兜底；若 D4 决定别的兜底则按 D4。）

- [ ] **Step 3: :269 mat_bom → element_bom_item（含 is_current 规范过滤）**

```java
// 原：SELECT hf_part_no, bom_type, element_name, quantity FROM mat_bom WHERE ...
"SELECT hf_part_no, 'ELEMENT' AS bom_type, component_no AS element_name, content AS quantity " +
"FROM element_bom_item ebi WHERE ebi.system_type='QUOTE' AND ebi.is_current = true " +
"  AND ebi.hf_part_no IN (:pns) " +
"  AND ebi.characteristic = (SELECT MAX(c.characteristic) FROM element_bom_item c " +
"     WHERE c.is_current=true AND c.customer_no=ebi.customer_no AND c.material_no=ebi.material_no)"
```

- [ ] **Step 4: 自检 + 提交**

```bash
touch cpq-backend/src/main/java/com/cpq/quotation/snapshot/SnapshotCollectorService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components
git add -A && git commit -m "feat(selopt-v6): SnapshotCollector 提交快照改读 V6 material_master/element_bom_item"
```

### Task 5.2: DriftDetectionService 改读 V6

**Files:**
- Modify: `cpq-backend/.../quotation/service/DriftDetectionService.java:355`

- [ ] **Step 1: 读上下文**

Run: `sed -n '348,365p' cpq-backend/src/main/java/com/cpq/quotation/service/DriftDetectionService.java`

- [ ] **Step 2: mat_bom → element_bom_item**

```java
// 原：SELECT DISTINCT element_name FROM mat_bom WHERE ...
"SELECT DISTINCT component_no AS element_name FROM element_bom_item " +
"WHERE system_type='QUOTE' AND is_current = true AND hf_part_no = :p"
```
（保持原 WHERE 的 part 维度参数名；若原按 hf_part_no 则同。）

- [ ] **Step 3: 自检 + 提交**

```bash
touch cpq-backend/src/main/java/com/cpq/quotation/service/DriftDetectionService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components
git add -A && git commit -m "feat(selopt-v6): DriftDetection 元素对账改读 element_bom_item"
```

### Task 5.3: QuotationService.loadLineItems 改读 V6

**Files:**
- Modify: `cpq-backend/.../quotation/service/QuotationService.java:1633, :1666`

- [ ] **Step 1: 读两处 + 消费方式（注意 :1779 mat_part_version_log 是否同改）**

Run: `sed -n '1625,1680p' cpq-backend/src/main/java/com/cpq/quotation/service/QuotationService.java; sed -n '1775,1785p' cpq-backend/src/main/java/com/cpq/quotation/service/QuotationService.java`

- [ ] **Step 2: :1633 / :1666 mat_part → material_master（按 D4）**

```java
// 原：SELECT part_no, product_type FROM mat_part WHERE part_no IN (:pns)
"SELECT material_no AS part_no, '——' AS product_type FROM material_master WHERE material_no IN (:pns)"
```
（product_type 在 material_master 无 → 按 D4 用占位；:1666 的列按 D4 映射同样处理。）

- [ ] **Step 3: 评估 :1779 mat_part_version_log**

读上下文判断该读是否在选配产品路径上 + 是否有 V6 等价。**若不在本次范围（属版本日志子系统）→ 不动，记入 §决策记录 D6 留后续。**

- [ ] **Step 4: 自检 + 提交**

```bash
touch cpq-backend/src/main/java/com/cpq/quotation/service/QuotationService.java; sleep 9
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/api/cpq/components
git add -A && git commit -m "feat(selopt-v6): loadLineItems 改读 material_master"
```

---

## Phase 6 — 端到端验证

### Task 6.1: 写入对账（V6 齐全 + V44 零新增）

**Files:** 仅验证

- [ ] **Step 1: 记录基线 V44 行数 + created_at**

```bash
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c \
"SELECT 'mat_part',COUNT(*),MAX(created_at) FROM mat_part UNION ALL SELECT 'mat_process',COUNT(*),MAX(created_at) FROM mat_process UNION ALL SELECT 'mat_bom',COUNT(*),MAX(created_at) FROM mat_bom;"
```

- [ ] **Step 2: E2E 跑选配简单 + 组合「确认产品」**

Run:
```bash
cd cpq-frontend
npx playwright test --config=e2e/playwright.config.ts e2e/composite-product-flow.spec.ts --reporter=list 2>&1 | grep -E "passed|failed"
```
Expected: passed（若夹具缺，按 `docs/E2E测试方法.md` 修夹具后再跑）。

- [ ] **Step 3: 验 V44 零新增 + V6 写入**

```bash
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c \
"SELECT 'mat_part',MAX(created_at) FROM mat_part UNION ALL SELECT 'mat_process',MAX(created_at) FROM mat_process;"
# 期望：MAX(created_at) 不晚于 Step1 基线（无新增）
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c \
"SELECT material_no, config_fingerprint FROM material_master ORDER BY material_no DESC LIMIT 5;"
# 期望：新配置料号在 material_master，带 fingerprint
```

### Task 6.2: 指纹复用 + 工序渲染 + 提交快照验证

- [ ] **Step 1: 指纹复用**：同配置再选一次 → 复用同一全局料号（material_master.config_fingerprint 命中，不新建）。手动走前端选配 2 次或直接 POST `/configure-product/lookup-fingerprint`，验 hfPartNo 一致。

- [ ] **Step 2: 简单料号工序渲染（验缺口 B）**：选配简单料号配 ≥1 工序 → 进报价向导，工序 Tab/v12 视图显示该工序。

- [ ] **Step 3: 提交快照（验 Phase 5）**：提交一张选配产品报价单 → `SELECT submission_snapshot IS NOT NULL` 且含元素/材质字段。
```bash
PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db -t -A -c \
"SELECT length(submission_snapshot::text) FROM quotation WHERE id='<提交的单>';"
```

- [ ] **Step 4: 最终 V44 残留扫描**

```bash
grep -rnE "(FROM|JOIN|INTO|UPDATE|DELETE FROM)\s+(mat_part|mat_bom|mat_process|mat_composite_process)" \
  cpq-backend/src/main/java/com/cpq/configure/ \
  cpq-backend/src/main/java/com/cpq/quotation/snapshot/SnapshotCollectorService.java \
  cpq-backend/src/main/java/com/cpq/quotation/service/DriftDetectionService.java | grep -v "//\|\*"
```
Expected: 空（loadLineItems 的 mat_part_version_log 若按 D6 保留则单独豁免说明）。

- [ ] **Step 5: RECORD 回写 + 收口提交**

把交付内容追加到 `docs/RECORD.md`，commit。

---

## §决策记录（Phase 0 / 执行期填）

- **D1**（buildSnapshot 工序回显源）：________
- **D2**（简单料号是否写 unit_price）：________
- **D3**（quotation_line_process 不动）：确认
- **D4**（3 下游 V44列→V6列映射表）：________
- **D5**（backfillProcessesForNewCustomer 改/删）：________
- **D6**（QuotationService:1779 mat_part_version_log 处置）：________

---

## 风险与回滚

- **无开关大切换**：合并前必须 Phase 6 全绿。回滚=revert 本系列 commit（迁移 V284 可保留，唯一索引不影响旧逻辑）。
- **V284 唯一索引建失败**：Task 1.1 Step 1 必须先清存量重复 fingerprint。
- **unit_price 列不够承载工序语义**：Phase 0 D1/D2 若发现 unit_price 无法表征简单工序回显，升级为「buildSnapshot 回显改读 quotation_line_process（按最近一次该料号的 line process）」备选，记入 D1。
- **V44 表本次不删**：导入侧仍用；本计划交付边界=选配写路 V6 单写，不含 DROP。
