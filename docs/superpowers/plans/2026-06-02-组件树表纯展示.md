# 组件树表(纯展示)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让组件可配置为「树表」——指定 ID 列(料号)与父 ID 列(父料号)两列,报价单/核价单/详情三视图渲染时把 driver 展开的平铺行按邻接表重排成树并支持缩进 + 折叠,纯展示不改任何数值。

**Architecture:** 组件级 `treeConfig`(独立列 `tree_config jsonb`)随 `dataDriverPath` 现有传播链进模板快照;渲染采用方案 A——在「行已构建、即将 map 成 JSX」的边界调用共享纯函数 `layoutTreeRows` 重排 + 标注缩进,折叠态用顶层 `useTreeCollapse` hook。**绝不改 `rowData` / `rowCount` / 行序权威源**(规避 AP-37/40/51 + 不破坏 Excel `CardDataProvider`)。

**Tech Stack:** 前端 React + TypeScript + Ant Design(`Switch`/`Select`);后端 Quarkus + Hibernate Panache + Flyway(PostgreSQL JSONB);测试 Vitest(纯函数单测)+ Playwright E2E。

**关联设计:** `docs/superpowers/specs/2026-06-02-组件树表纯展示-design.md`(尤其 §7 六条边界规则、§9.5 与 Excel 卡片公式的不变量)。

**并发约束(与实施中的 Excel 卡片公式那条线):**
- Flyway 版本号 **树表占 V289**;若 Excel 也在加迁移需先对齐避免抢号。
- 共享单 DB / 单 Quarkus dev / 单 Vite:两条线的 E2E 与 Flyway 重启动作要串行,别并发触发。
- 提交时**只 `git add` 本计划列出的文件**,勿扫入 Excel 未提交工作。
- 共用文件仅 `QuotationStep2.tsx`,代码区不相交(Excel 在 import/2213/2272 渲染 `LinkedExcelView`;树表在卡片行渲染循环 ~1582)。

---

## 文件结构(改动地图)

**新增**
- `cpq-frontend/src/pages/quotation/treeTable.ts` — 纯逻辑:`buildTreeRows` / `layoutTreeRows` / `resolveTreeKey` / `isTreeRowHidden`(无 React 依赖,可单测)
- `cpq-frontend/src/pages/quotation/useTreeCollapse.ts` — 折叠态 hook(会话内,不持久化)
- `cpq-frontend/src/pages/quotation/treeTable.test.ts` — 纯函数单测
- `cpq-backend/src/main/resources/db/migration/V289__component_add_tree_config.sql` — 加列

**修改**
- 后端:`Component.java`(entity)、`ComponentDTO.java`、`CreateComponentRequest.java`、`ComponentService.java`(create/update 持久化 + 校验)、`TemplateService.java`(componentsSnapshot 两处写 `tree_config`)、`CardSnapshotService.java`(结构 tab 透传 `tree_config`)
- 前端类型:`pages/component/types.ts`(`ComponentItem.treeConfig`)、`pages/quotation/QuotationStep2.tsx`(`ComponentDataItem.treeConfig` + 编辑渲染)、`services/quotationService.ts`(`CardStructureTab.treeConfig`)
- 前端传播:`pages/quotation/enrichComponentData.ts`(模板路径 + `buildComponentDataFromStructure`)
- 前端配置 UI:`pages/component/ComponentManagement.tsx`(开关 + 双下拉 + save/load)
- 前端渲染:`pages/quotation/QuotationStep2.tsx`(编辑态)、`pages/quotation/ReadonlyProductCard.tsx`(只读/详情/核价态)

---

## Task 1: 树表核心纯函数 `buildTreeRows`(TDD)

**Files:**
- Create: `cpq-frontend/src/pages/quotation/treeTable.ts`
- Test: `cpq-frontend/src/pages/quotation/treeTable.test.ts`

实现 spec §7 全部六条边界规则。`buildTreeRows` 只吃「每行的 id/parent 归一化键」,不关心值从哪来(值解析在 Task 4 的 `resolveTreeKey`)。

- [ ] **Step 1: 写失败测试**

```ts
// cpq-frontend/src/pages/quotation/treeTable.test.ts
import { describe, it, expect, vi } from 'vitest';
import { buildTreeRows } from './treeTable';

describe('buildTreeRows', () => {
  it('单根多层:子行紧跟父行 + depth 正确 + hasChildren 正确', () => {
    // 行: 0=A(root) 1=B(parent A) 2=C(parent B) 3=D(parent A)
    const r = buildTreeRows([
      { id: 'A', parent: null },
      { id: 'B', parent: 'A' },
      { id: 'C', parent: 'B' },
      { id: 'D', parent: 'A' },
    ]);
    expect(r.order).toEqual([0, 1, 2, 3]);
    expect(r.depthByIndex).toEqual({ 0: 0, 1: 1, 2: 2, 3: 1 });
    expect([...r.hasChildren].sort()).toEqual([0, 1]);
    expect(r.parentIndexByIndex).toEqual({ 0: null, 1: 0, 2: 1, 3: 0 });
  });

  it('父为空 → 根', () => {
    const r = buildTreeRows([{ id: 'A', parent: '' }, { id: 'B', parent: null }]);
    expect(r.parentIndexByIndex).toEqual({ 0: null, 1: null });
    expect(r.order).toEqual([0, 1]);
  });

  it('父值在批次内缺失 → 升为根(不丢行)', () => {
    const r = buildTreeRows([{ id: 'A', parent: 'GHOST' }, { id: 'B', parent: 'A' }]);
    expect(r.parentIndexByIndex[0]).toBeNull();
    expect(r.parentIndexByIndex[1]).toBe(0);
    expect(r.order.length).toBe(2);
  });

  it('多根并列', () => {
    const r = buildTreeRows([{ id: 'A', parent: null }, { id: 'B', parent: null }]);
    expect(r.order).toEqual([0, 1]);
    expect(r.depthByIndex).toEqual({ 0: 0, 1: 0 });
  });

  it('成环 → 环上行降级为根 + console.warn + 不死循环 + 不丢行', () => {
    const warn = vi.spyOn(console, 'warn').mockImplementation(() => {});
    // A.parent=B, B.parent=A (互引)
    const r = buildTreeRows([{ id: 'A', parent: 'B' }, { id: 'B', parent: 'A' }]);
    expect(r.order.length).toBe(2);            // 不丢行
    expect(r.parentIndexByIndex[0]).toBeNull(); // 环上降级为根
    expect(r.parentIndexByIndex[1]).toBeNull();
    expect(warn).toHaveBeenCalled();
    warn.mockRestore();
  });

  it('自环(parent==自身) → 根', () => {
    const r = buildTreeRows([{ id: 'A', parent: 'A' }]);
    expect(r.parentIndexByIndex[0]).toBeNull();
    expect(r.order).toEqual([0]);
  });

  it('料号重复 → 父按第一条匹配 + 重复行全部保留', () => {
    // 0=A 1=A(重复) 2=child(parent A) → 挂到第一条 A(index 0)
    const r = buildTreeRows([
      { id: 'A', parent: null },
      { id: 'A', parent: null },
      { id: 'X', parent: 'A' },
    ]);
    expect(r.order.length).toBe(3);          // 不丢行
    expect(r.parentIndexByIndex[2]).toBe(0); // 第一条匹配
  });

  it('同级保持原始顺序(不重排序)', () => {
    // 同父 A 下的子行原序 [2,1] → 保持 [2,1]
    const r = buildTreeRows([
      { id: 'A', parent: null },
      { id: 'C', parent: 'A' },
      { id: 'B', parent: 'A' },
    ]);
    expect(r.order).toEqual([0, 1, 2]); // C(1) 在 B(2) 前,保持原序
  });

  it('空数组 → 空布局', () => {
    const r = buildTreeRows([]);
    expect(r.order).toEqual([]);
    expect(r.hasChildren.size).toBe(0);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-frontend && npx vitest run src/pages/quotation/treeTable.test.ts`
Expected: FAIL — `buildTreeRows` is not exported / not defined.

- [ ] **Step 3: 写实现**

```ts
// cpq-frontend/src/pages/quotation/treeTable.ts

/** 单行的邻接表输入:id=本行标识(料号),parent=父标识(父料号);空串/null 视为无。 */
export interface TreeNodeInput {
  id: string | null;
  parent: string | null;
}

export interface TreeLayout {
  /** 重排后的原始行下标序列(DFS,子行紧跟父行;同级保持原序) */
  order: number[];
  /** 原始行下标 → 缩进层级(根=0) */
  depthByIndex: Record<number, number>;
  /** 含子节点的原始行下标(显示折叠箭头) */
  hasChildren: Set<number>;
  /** 原始行下标 → 父行下标(根=null);折叠隐藏判定用 */
  parentIndexByIndex: Record<number, number | null>;
}

/**
 * 把平铺行的 (id, parent) 邻接表重排成树布局(纯展示,见 spec §7 六条规则)。
 * 不改变行集合:order.length === nodes.length(永不丢行)。
 */
export function buildTreeRows(nodes: TreeNodeInput[]): TreeLayout {
  const n = nodes.length;

  // 规则4:同 id 多行 → 第一条声明者胜
  const idToIndex = new Map<string, number>();
  for (let i = 0; i < n; i++) {
    const id = nodes[i].id;
    if (id != null && id !== '' && !idToIndex.has(id)) idToIndex.set(id, i);
  }

  // 规则1:parent 空、或 parent 匹配不到任何行的 id、或指向自身 → 根(null)
  const parentIndexByIndex: Record<number, number | null> = {};
  for (let i = 0; i < n; i++) {
    const p = nodes[i].parent;
    if (p == null || p === '') { parentIndexByIndex[i] = null; continue; }
    const pi = idToIndex.get(p);
    parentIndexByIndex[i] = (pi == null || pi === i) ? null : pi;
  }

  // 规则3:环检测 — 把任意环上的行全部降级为根 + warn(不死循环)
  const onCycle = new Set<number>();
  for (let i = 0; i < n; i++) {
    if (onCycle.has(i)) continue;
    const pathPos = new Map<number, number>();
    const path: number[] = [];
    let cur: number | null = i;
    while (cur != null) {
      if (onCycle.has(cur)) break;
      if (pathPos.has(cur)) {
        for (let k = pathPos.get(cur)!; k < path.length; k++) onCycle.add(path[k]);
        break;
      }
      pathPos.set(cur, path.length);
      path.push(cur);
      cur = parentIndexByIndex[cur];
    }
  }
  if (onCycle.size > 0) {
    // eslint-disable-next-line no-console
    console.warn(`[treeTable] 检测到父子成环,${onCycle.size} 行降级为根平铺显示`);
    for (const i of onCycle) parentIndexByIndex[i] = null;
  }

  // 规则2/6:按 parent 聚子,子行保持原始下标顺序
  const childrenByParent = new Map<number | null, number[]>();
  for (let i = 0; i < n; i++) {
    const p = parentIndexByIndex[i];
    if (!childrenByParent.has(p)) childrenByParent.set(p, []);
    childrenByParent.get(p)!.push(i);
  }

  const order: number[] = [];
  const depthByIndex: Record<number, number> = {};
  const hasChildren = new Set<number>();
  const visit = (idx: number, depth: number): void => {
    order.push(idx);
    depthByIndex[idx] = depth;
    const kids = childrenByParent.get(idx);
    if (kids && kids.length > 0) {
      hasChildren.add(idx);
      for (const k of kids) visit(k, depth + 1);
    }
  };
  for (const r of (childrenByParent.get(null) ?? [])) visit(r, 0);

  return { order, depthByIndex, hasChildren, parentIndexByIndex };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd cpq-frontend && npx vitest run src/pages/quotation/treeTable.test.ts`
Expected: PASS(9 个 test 全绿)。

- [ ] **Step 5: 提交**

```bash
git add cpq-frontend/src/pages/quotation/treeTable.ts cpq-frontend/src/pages/quotation/treeTable.test.ts
git commit -m "feat(tree): buildTreeRows 邻接表→树布局纯函数(6规则+单测)"
```

---

## Task 2: 折叠态 hook + 行隐藏判定 `isTreeRowHidden`(TDD)

**Files:**
- Create: `cpq-frontend/src/pages/quotation/useTreeCollapse.ts`
- Modify: `cpq-frontend/src/pages/quotation/treeTable.ts`(追加 `isTreeRowHidden`)
- Test: `cpq-frontend/src/pages/quotation/treeTable.test.ts`(追加用例)

折叠态用「显式翻转集 + 每组件 defaultExpanded」模型:无需在顶层预知 id 即可表达「默认全折叠」。

- [ ] **Step 1: 追加失败测试(isTreeRowHidden)**

```ts
// 追加到 treeTable.test.ts
import { buildTreeRows, isTreeRowHidden } from './treeTable';

describe('isTreeRowHidden', () => {
  // 树: 0=A(root,has child) 1=B(child of A, has child) 2=C(child of B)
  const layout = buildTreeRows([
    { id: 'A', parent: null },
    { id: 'B', parent: 'A' },
    { id: 'C', parent: 'B' },
  ]);
  const nodeKeyByIndex = { 0: 'k0', 1: 'k1', 2: 'k2' };

  it('祖先折叠 → 后代隐藏', () => {
    const collapsed = new Set(['k0']); // A 折叠
    const hidden = (i: number) =>
      isTreeRowHidden(i, layout.parentIndexByIndex, nodeKeyByIndex, collapsed);
    expect(hidden(0)).toBe(false); // A 自己不隐藏
    expect(hidden(1)).toBe(true);  // B 被 A 折叠
    expect(hidden(2)).toBe(true);  // C 被祖先 A 折叠
  });

  it('仅中间节点折叠 → 只隐藏其后代', () => {
    const collapsed = new Set(['k1']); // B 折叠
    const hidden = (i: number) =>
      isTreeRowHidden(i, layout.parentIndexByIndex, nodeKeyByIndex, collapsed);
    expect(hidden(0)).toBe(false);
    expect(hidden(1)).toBe(false); // B 自己可见
    expect(hidden(2)).toBe(true);  // C 被 B 折叠
  });

  it('无折叠 → 全可见', () => {
    const collapsed = new Set<string>();
    expect(isTreeRowHidden(2, layout.parentIndexByIndex, nodeKeyByIndex, collapsed)).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-frontend && npx vitest run src/pages/quotation/treeTable.test.ts`
Expected: FAIL — `isTreeRowHidden` 未定义。

- [ ] **Step 3: 实现 isTreeRowHidden(追加到 treeTable.ts)**

```ts
// 追加到 cpq-frontend/src/pages/quotation/treeTable.ts

/**
 * 行是否因祖先折叠而隐藏:沿 parentIndex 链上溯,任一祖先的 nodeKey ∈ collapsed → 隐藏。
 * collapsed = 当前“有效折叠”的 nodeKey 集合(由 useTreeCollapse 按 defaultExpanded 算出)。
 */
export function isTreeRowHidden(
  index: number,
  parentIndexByIndex: Record<number, number | null>,
  nodeKeyByIndex: Record<number, string>,
  collapsed: Set<string>,
): boolean {
  let p = parentIndexByIndex[index];
  while (p != null) {
    const key = nodeKeyByIndex[p];
    if (key != null && collapsed.has(key)) return true;
    p = parentIndexByIndex[p];
  }
  return false;
}
```

- [ ] **Step 4: 实现 useTreeCollapse hook**

```ts
// cpq-frontend/src/pages/quotation/useTreeCollapse.ts
import { useCallback, useState } from 'react';

/**
 * 树表折叠态(会话内,不持久化;刷新/重进恢复默认)。
 *
 * 模型:只存“用户显式翻转过的 nodeKey”。某节点的“有效折叠”=
 *   defaultExpanded=true  → 翻转过即折叠
 *   defaultExpanded=false → 未翻转即折叠(默认全折叠)
 * 这样顶层无需预知任何 id 即可表达两种默认,且 nodeKey 含 componentId 前缀全局唯一。
 */
export function useTreeCollapse() {
  const [toggled, setToggled] = useState<Set<string>>(() => new Set());

  const isCollapsed = useCallback(
    (nodeKey: string, defaultExpanded: boolean) =>
      defaultExpanded ? toggled.has(nodeKey) : !toggled.has(nodeKey),
    [toggled],
  );

  const toggle = useCallback((nodeKey: string) => {
    setToggled((prev) => {
      const next = new Set(prev);
      if (next.has(nodeKey)) next.delete(nodeKey);
      else next.add(nodeKey);
      return next;
    });
  }, []);

  /** 给定可见行的 defaultExpanded,算出当前“有效折叠”的 nodeKey 集合(供 isTreeRowHidden 用)。 */
  const collapsedSet = useCallback(
    (nodeKeys: string[], defaultExpanded: boolean): Set<string> => {
      const s = new Set<string>();
      for (const k of nodeKeys) if (isCollapsed(k, defaultExpanded)) s.add(k);
      return s;
    },
    [isCollapsed],
  );

  return { isCollapsed, toggle, collapsedSet };
}
```

- [ ] **Step 5: 跑测试确认通过 + 类型检查**

Run: `cd cpq-frontend && npx vitest run src/pages/quotation/treeTable.test.ts && npx tsc --noEmit -p tsconfig.json`
Expected: 测试全绿;tsc 0 错误。

- [ ] **Step 6: 提交**

```bash
git add cpq-frontend/src/pages/quotation/treeTable.ts cpq-frontend/src/pages/quotation/treeTable.test.ts cpq-frontend/src/pages/quotation/useTreeCollapse.ts
git commit -m "feat(tree): isTreeRowHidden 折叠隐藏判定 + useTreeCollapse 会话态hook(单测)"
```

---

## Task 3: 前端类型 — 三处加 `treeConfig`

**Files:**
- Modify: `cpq-frontend/src/pages/component/types.ts`
- Modify: `cpq-frontend/src/pages/quotation/QuotationStep2.tsx`(`ComponentDataItem` 定义处)
- Modify: `cpq-frontend/src/services/quotationService.ts`(`CardStructureTab`)

- [ ] **Step 1: 加共享类型 `TreeConfig` 到 component/types.ts**

在 `cpq-frontend/src/pages/component/types.ts` 的 `ComponentItem` 接口内(`rowKeyFields?` 之后)新增,并在文件顶部附近导出 `TreeConfig`:

```ts
// 放在 ComponentItem 接口之前
export interface TreeConfig {
  /** 本行 ID 列字段 key(= 字段 name,如「料号」) */
  idField: string;
  /** 父 ID 列字段 key(如「父料号」) */
  parentField: string;
  /** 默认全展开(true,缺省)/全折叠(false) */
  defaultExpanded?: boolean;
}
```

在 `ComponentItem` 内追加:

```ts
  /**
   * 树表配置(可选)。非空 → 该组件渲染时按邻接表(idField/parentField)重排成树。
   * 纯展示:不改 rowData/rowCount/行序;见 docs/.../组件树表纯展示-design.md。
   */
  treeConfig?: TreeConfig;
```

- [ ] **Step 2: `ComponentDataItem` 加 treeConfig(QuotationStep2.tsx)**

`ComponentDataItem` 接口定义在 `QuotationStep2.tsx`。先定位:

Run: `grep -n "interface ComponentDataItem\|dataDriverPath" cpq-frontend/src/pages/quotation/QuotationStep2.tsx | head`

在该接口内 `dataDriverPath?: string;` 同级追加(若 `ComponentField`/`ComponentDataItem` 从别处 import,则改对应定义文件):

```ts
  treeConfig?: import('../component/types').TreeConfig;
```

- [ ] **Step 3: `CardStructureTab` 加 treeConfig(quotationService.ts)**

`cpq-frontend/src/services/quotationService.ts` 的 `CardStructureTab` 已有 `dataDriverPath?` / `rowKeyFields?`(line ~36-37)。追加:

```ts
  treeConfig?: { idField: string; parentField: string; defaultExpanded?: boolean };
```

- [ ] **Step 4: 类型检查**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误(纯加可选字段,不破坏现有)。

- [ ] **Step 5: 提交**

```bash
git add cpq-frontend/src/pages/component/types.ts cpq-frontend/src/pages/quotation/QuotationStep2.tsx cpq-frontend/src/services/quotationService.ts
git commit -m "feat(tree): 前端三处类型加 treeConfig(ComponentItem/ComponentDataItem/CardStructureTab)"
```

---

## Task 4: 后端存储 — Flyway V289 + 实体/DTO/Request + 持久化 + 校验

**Files:**
- Create: `cpq-backend/src/main/resources/db/migration/V289__component_add_tree_config.sql`
- Modify: `cpq-backend/src/main/java/com/cpq/component/entity/Component.java`
- Modify: `cpq-backend/src/main/java/com/cpq/component/dto/ComponentDTO.java`
- Modify: `cpq-backend/src/main/java/com/cpq/component/dto/CreateComponentRequest.java`
- Modify: `cpq-backend/src/main/java/com/cpq/component/service/ComponentService.java`

- [ ] **Step 1: 写 Flyway 迁移 V289**

```sql
-- cpq-backend/src/main/resources/db/migration/V289__component_add_tree_config.sql
-- 组件树表配置(纯展示):{ "idField": "...", "parentField": "...", "defaultExpanded": true }
-- NULL = 非树表(平铺渲染,行为不变)。
ALTER TABLE component ADD COLUMN IF NOT EXISTS tree_config jsonb;

COMMENT ON COLUMN component.tree_config IS
  '树表配置(纯展示):idField/parentField/defaultExpanded;NULL=非树表';
```

- [ ] **Step 2: Component 实体加字段**

在 `Component.java` 的 `rowKeyFields` 字段之后追加:

```java
    /**
     * 树表配置(纯展示,可选)。JSON 对象 {idField,parentField,defaultExpanded}。
     * 非空 → 报价/核价/详情渲染时按邻接表重排成树;不改行集合/行序。
     */
    @JdbcTypeCode(SqlTypes.JSON)
    @Column(name = "tree_config")
    public String treeConfig;
```

- [ ] **Step 3: ComponentDTO 加字段 + from() 映射**

在 `ComponentDTO.java` 字段区(`rowKeyFields` 之后)加:

```java
    /** 树表配置(纯展示);entity 存 JSON 字符串,DTO 透传为 Map(null=非树表) */
    public Map<String, Object> treeConfig;
```

在 `from()` 内(`dto.rowKeyFields = ...` 之后)加:

```java
        dto.treeConfig = parseJsonObject(component.treeConfig);
```

在类内新增辅助方法(紧邻 `parseStringList`):

```java
    /** 解析 tree_config JSON 对象为 Map;null/空/非法 → null。 */
    @SuppressWarnings("unchecked")
    private static Map<String, Object> parseJsonObject(String json) {
        if (json == null || json.isBlank()) return null;
        try {
            return MAPPER.readValue(json, new TypeReference<Map<String, Object>>() {});
        } catch (Exception e) {
            return null;
        }
    }
```

- [ ] **Step 4: CreateComponentRequest 加字段**

在 `CreateComponentRequest.java`(`rowKeyFields` 之后)加:

```java
    /** 树表配置(纯展示,可选):{idField, parentField, defaultExpanded} */
    public Map<String, Object> treeConfig;
```

确保文件已 `import java.util.Map;`(若无则补)。

- [ ] **Step 5: ComponentService create/update 持久化 + 校验**

先看现有 create 持久化区(~line 102-115)与 update 区(~line 159-183),按 `rowKeyFields` 同款加 `treeConfig`。

**create()**(在 `validateRowKeyConfig(...)` 调用之前):

```java
        // 树表配置:校验后存 JSON(null=非树表)
        validateTreeConfig(request.treeConfig, request.fields);
        component.treeConfig = request.treeConfig != null ? toJsonRaw(request.treeConfig) : null;
```

**update()**(在 `validateRowKeyConfig(...)` 调用之前;沿用「null=不变」语义):

```java
        // 树表配置更新(null=不变;传值=覆盖,空对象/缺字段=清空)
        if (request.treeConfig != null) {
            validateTreeConfig(request.treeConfig, component.fields);
            boolean hasBoth = request.treeConfig.get("idField") != null
                    && request.treeConfig.get("parentField") != null;
            component.treeConfig = hasBoth ? toJsonRaw(request.treeConfig) : null;
        }
```

新增校验方法(放在 `validateRowKeyConfig` 附近):

```java
    /**
     * 树表配置软校验(spec §3):开启时 idField/parentField 均必填且不同列,
     * 且两列须存在于组件字段名集合中。不满足 → IllegalArgumentException(保存阻断)。
     * 传 null 或空对象(关闭树表)→ 直接通过。
     */
    public void validateTreeConfig(Map<String, Object> treeConfig, Object fieldsJsonOrList) {
        if (treeConfig == null) return;
        Object idF = treeConfig.get("idField");
        Object pF = treeConfig.get("parentField");
        boolean idEmpty = idF == null || idF.toString().isBlank();
        boolean pEmpty = pF == null || pF.toString().isBlank();
        if (idEmpty && pEmpty) return; // 视为关闭树表
        if (idEmpty || pEmpty) {
            throw new IllegalArgumentException("树表配置:ID 列与父 ID 列均必填");
        }
        if (idF.toString().equals(pF.toString())) {
            throw new IllegalArgumentException("树表配置:ID 列与父 ID 列不能为同一列");
        }
        // 字段存在性:fields 可能是 JSON 字符串(entity)或 List(request);提取字段 name 集合
        java.util.Set<String> names = extractFieldNames(fieldsJsonOrList);
        if (!names.contains(idF.toString()) || !names.contains(pF.toString())) {
            throw new IllegalArgumentException(
                "树表配置:idField/parentField 必须是组件已配置字段名");
        }
    }

    @SuppressWarnings("unchecked")
    private java.util.Set<String> extractFieldNames(Object fieldsJsonOrList) {
        java.util.Set<String> names = new java.util.HashSet<>();
        try {
            java.util.List<Map<String, Object>> list;
            if (fieldsJsonOrList instanceof String s) {
                if (s.isBlank()) return names;
                list = MAPPER().readValue(s, new com.fasterxml.jackson.core.type.TypeReference<>() {});
            } else if (fieldsJsonOrList instanceof java.util.List<?> l) {
                list = (java.util.List<Map<String, Object>>) l;
            } else return names;
            for (Map<String, Object> f : list) {
                Object nm = f.get("name");
                if (nm != null) names.add(nm.toString());
            }
        } catch (Exception ignore) { /* 软处理:解析失败按空集 */ }
        return names;
    }
```

> 注:`MAPPER()` / `toJsonRaw` —— 用 ComponentService 内既有的 ObjectMapper 与 `toJsonRaw`(Task 探查已确认 `toJsonRaw` 存在,用于 rowKeyFields)。若 service 内 ObjectMapper 是字段名(如 `private static final ObjectMapper MAPPER = ...`),把上面 `MAPPER()` 改成该字段名;若没有则复用 `objectMapper`。实现时按文件实际命名对齐。

- [ ] **Step 6: 触发 Quarkus 重启跑迁移 + 验证**

```bash
touch cpq-backend/src/main/java/com/cpq/component/entity/Component.java
sleep 7
```

验证迁移成功(用项目惯用的 PG 连接;占位按本机环境替换):

Run: `PGPASSWORD=$PGPASSWORD psql -h localhost -U $PGUSER -d $PGDB -c "SELECT version, success FROM flyway_schema_history WHERE version='289'"`
Expected: `289 | t`

Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/q/health`
Expected: 200。

- [ ] **Step 7: 提交**

```bash
git add cpq-backend/src/main/resources/db/migration/V289__component_add_tree_config.sql cpq-backend/src/main/java/com/cpq/component/entity/Component.java cpq-backend/src/main/java/com/cpq/component/dto/ComponentDTO.java cpq-backend/src/main/java/com/cpq/component/dto/CreateComponentRequest.java cpq-backend/src/main/java/com/cpq/component/service/ComponentService.java
git commit -m "feat(tree): 后端 treeConfig 存储(V289+实体/DTO/Request+持久化+校验)"
```

---

## Task 5: 后端快照传播 — TemplateService + CardSnapshotService

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/template/service/TemplateService.java`
- Modify: `cpq-backend/src/main/java/com/cpq/quotation/service/CardSnapshotService.java`

`treeConfig` 跟随 `data_driver_path` 进 ① 模板 `components_snapshot` ② 报价单整份结构快照。

- [ ] **Step 1: TemplateService 初次构建 snapshot 写 tree_config**

定位写 `data_driver_path` 的两处(~line 243 初始构建、~line 351-353 refreshSnapshotsByComponent)。

在 ~line 243 `entry.put("data_driver_path", effectiveDriverPath);` 之后加:

```java
                // 树表配置随组件冻进 snapshot(纯展示;无 template 级 override)
                if (comp.treeConfig != null && !comp.treeConfig.isBlank()) {
                    entry.set("tree_config", MAPPER.readTree(comp.treeConfig));
                } else {
                    entry.putNull("tree_config");
                }
```

> `MAPPER` 用该文件既有的 ObjectMapper 字段名;`entry` 为 ObjectNode。若该方法签名未声明抛 `JsonProcessingException`,用 try/catch 包住(参考同文件 rowKeyFields/snapshot 既有写法)。

- [ ] **Step 2: TemplateService refreshSnapshotsByComponent 写 tree_config**

在 ~line 353 覆盖 `data_driver_path` 的同段(`effectiveDriverPath` 之后,覆盖 fields/data_driver_path 处)加同款 `tree_config` 覆盖,保证平台级组件改树表配置后已发布模板 snapshot 同步:

```java
                if (comp.treeConfig != null && !comp.treeConfig.isBlank()) {
                    snapshotEntry.set("tree_config", MAPPER.readTree(comp.treeConfig));
                } else {
                    snapshotEntry.putNull("tree_config");
                }
```

> `snapshotEntry` 为该处覆盖 snapshot 数组项的 ObjectNode(按实际变量名对齐,如同段写 `data_driver_path` 用的那个 node)。

- [ ] **Step 3: CardSnapshotService 结构 tab 透传 tree_config**

在 `CardSnapshotService.java` ~line 200 `tabNode.put("dataDriverPath", tab.path("data_driver_path").asText(""));` 之后加(注意结构快照用 camelCase 键 `treeConfig`,与前端 `CardStructureTab.treeConfig` 对齐;源 snapshot 是 snake_case `tree_config`):

```java
                // 树表配置透传(snapshot snake_case → 结构 camelCase;缺失/NULL 不写,前端按非树表处理)
                JsonNode treeCfg = tab.path("tree_config");
                if (treeCfg != null && treeCfg.isObject()) {
                    tabNode.set("treeConfig", treeCfg.deepCopy());
                }
```

同时更新该方法上方注释(~line 147-148 的结构说明)把 `tree_config`/`treeConfig` 列进字段清单。

- [ ] **Step 4: 重启 + 健康检查**

```bash
touch cpq-backend/src/main/java/com/cpq/template/service/TemplateService.java
sleep 7
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/q/health
```
Expected: 200。

- [ ] **Step 5: 提交**

```bash
git add cpq-backend/src/main/java/com/cpq/template/service/TemplateService.java cpq-backend/src/main/java/com/cpq/quotation/service/CardSnapshotService.java
git commit -m "feat(tree): treeConfig 进模板snapshot+报价整份结构快照(随data_driver_path传播)"
```

---

## Task 6: 前端传播 — enrichComponentData 两条路径回填 treeConfig

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/enrichComponentData.ts`

模板路径(读 `GET /templates` componentsSnapshot)与整份快照结构 v2 路径(`buildComponentDataFromStructure`)都要把 `treeConfig` 挂上 `ComponentDataItem`,渲染层才拿得到。

- [ ] **Step 1: 模板路径回填 treeConfig**

定位 enrich 主路径返回 `ComponentDataItem` 处(~line 161-180 取 `dataDriverPath` + ~line 266 整 tab 组装)。在组装返回对象、设置 `dataDriverPath` 的同处追加 `treeConfig`。模板 snapshot 里 component 项的键是 snake_case `tree_config`(见 Task 5 Step1),需归一为前端 `TreeConfig`:

在文件顶部辅助区新增归一函数:

```ts
/** snapshot/结构里的 tree_config(snake)或 treeConfig(camel)→ 前端 TreeConfig(null=非树表) */
function normalizeTreeConfig(raw: any): import('../component/types').TreeConfig | undefined {
  const o = raw?.tree_config ?? raw?.treeConfig ?? raw;
  if (!o || typeof o !== 'object') return undefined;
  const idField = o.idField ?? o.id_field;
  const parentField = o.parentField ?? o.parent_field;
  if (!idField || !parentField) return undefined;
  return {
    idField: String(idField),
    parentField: String(parentField),
    defaultExpanded: o.defaultExpanded ?? o.default_expanded ?? true,
  };
}
```

在模板路径组装 `ComponentDataItem` 处(设置 `dataDriverPath` 的对象字面量内)追加:

```ts
        treeConfig: normalizeTreeConfig(snapshotComp),
```

> `snapshotComp` 为该处读取 component snapshot 的变量名(同 `dataDriverPath` 取值来源);按实际变量名对齐。

- [ ] **Step 2: 整份快照路径回填(buildComponentDataFromStructure)**

在 `buildComponentDataFromStructure` 返回对象处(~line 266,`dataDriverPath: tab.dataDriverPath || ...` 同级)追加:

```ts
      treeConfig: normalizeTreeConfig(tab),
```

(`tab` 含 Task 5 Step3 写入的 camelCase `treeConfig`,`normalizeTreeConfig` 会命中 `raw.treeConfig`。)

- [ ] **Step 3: 类型检查**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误。

- [ ] **Step 4: 提交**

```bash
git add cpq-frontend/src/pages/quotation/enrichComponentData.ts
git commit -m "feat(tree): enrich 两路径(模板/整份快照)回填 treeConfig 到 ComponentDataItem"
```

---

## Task 7: 组件管理配置 UI — 树表开关 + 双下拉

**Files:**
- Modify: `cpq-frontend/src/pages/component/ComponentManagement.tsx`

在「数据驱动路径」配置块之后(~line 711 `</PathPickerDrawer>` 后、`<HeaderPreview>` 前)加树表设置区;state + load + save 接线。

- [ ] **Step 1: 加 state**

在组件 state 区(~line 324-325 `dataDriverPath`/`rowKeyFields` 附近)加:

```tsx
  const [treeConfig, setTreeConfig] = useState<import('./types').TreeConfig | null>(null);
```

确保文件已 import `Switch`、`Select`(从 `antd`)。

- [ ] **Step 2: load 时回填(handleSelectComponent)**

在 ~line 427-428 `setDataDriverPath(...)` / `setRowKeyFields(...)` 之后加:

```tsx
      setTreeConfig((loaded as any).treeConfig
        ? {
            idField: (loaded as any).treeConfig.idField,
            parentField: (loaded as any).treeConfig.parentField,
            defaultExpanded: (loaded as any).treeConfig.defaultExpanded ?? true,
          }
        : null);
```

- [ ] **Step 3: save 时携带(handleSave)**

在 ~line 459-467 `componentService.update(...)` 的入参对象内,`rowKeyFields` 之后加:

```tsx
        // 树表配置:开启传对象,关闭传空对象(后端按缺字段=清空)
        treeConfig: treeConfig
          ? { idField: treeConfig.idField, parentField: treeConfig.parentField, defaultExpanded: treeConfig.defaultExpanded ?? true }
          : {},
```

- [ ] **Step 4: 渲染设置区 UI(数据驱动路径块之后)**

在 `<PathPickerDrawer ... />`(~line 711)之后插入:

```tsx
                  {/* 树表配置(纯展示):指定 ID 列/父 ID 列 → 渲染时按父子关系折叠 */}
                  <div
                    style={{
                      background: '#f6ffed', border: '1px solid #b7eb8f', borderRadius: 6,
                      padding: '10px 12px', marginBottom: 12, display: 'flex',
                      alignItems: 'center', gap: 8, flexWrap: 'wrap',
                    }}
                  >
                    <span style={{ fontSize: 13, color: '#237804', fontWeight: 500, whiteSpace: 'nowrap' }}>
                      树表(纯展示):
                    </span>
                    <Switch
                      size="small"
                      checked={!!treeConfig}
                      onChange={(on) =>
                        setTreeConfig(on ? { idField: '', parentField: '', defaultExpanded: true } : null)
                      }
                    />
                    {treeConfig && (
                      <>
                        <span style={{ fontSize: 12, color: '#555' }}>ID 列:</span>
                        <Select
                          size="small" style={{ minWidth: 140 }} placeholder="选 ID 列(料号)"
                          value={treeConfig.idField || undefined}
                          options={fields.filter(f => f.name).map(f => ({ label: f.name, value: f.name }))}
                          onChange={(v) => setTreeConfig({ ...treeConfig, idField: v })}
                        />
                        <span style={{ fontSize: 12, color: '#555' }}>父 ID 列:</span>
                        <Select
                          size="small" style={{ minWidth: 140 }} placeholder="选父 ID 列(父料号)"
                          value={treeConfig.parentField || undefined}
                          options={fields.filter(f => f.name).map(f => ({ label: f.name, value: f.name }))}
                          onChange={(v) => setTreeConfig({ ...treeConfig, parentField: v })}
                        />
                        <span style={{ fontSize: 12, color: '#555' }}>默认展开:</span>
                        <Switch
                          size="small"
                          checked={treeConfig.defaultExpanded ?? true}
                          onChange={(v) => setTreeConfig({ ...treeConfig, defaultExpanded: v })}
                        />
                        {treeConfig.idField && treeConfig.parentField && treeConfig.idField === treeConfig.parentField && (
                          <span style={{ color: '#cf1322', fontSize: 12 }}>ID 列与父 ID 列不能相同</span>
                        )}
                      </>
                    )}
                  </div>
```

- [ ] **Step 5: 保存前防呆(handleSave 开头)**

在 `handleSave` 设置 `setSaving(true)` 之前加前端校验,避免提交非法树表配置:

```tsx
    if (treeConfig && (!treeConfig.idField || !treeConfig.parentField)) {
      message.error('树表已开启:请选择 ID 列与父 ID 列'); return;
    }
    if (treeConfig && treeConfig.idField === treeConfig.parentField) {
      message.error('树表:ID 列与父 ID 列不能相同'); return;
    }
```

- [ ] **Step 6: 自检(tsc + Vite transform)**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误。

Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/component/ComponentManagement.tsx`
Expected: 200。

- [ ] **Step 7: 端到端配置验证(手动 curl 保存往返)**

挑一个有 driver 的多行组件(组件管理 UI 操作或直接调 API),开启树表选两列保存,再 GET 验证回读:

Run: `curl -s http://localhost:8081/api/cpq/components/<componentId> | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('treeConfig'))"`
Expected: 打印 `{'idField': '...', 'parentField': '...', 'defaultExpanded': True}`。

- [ ] **Step 8: 提交**

```bash
git add cpq-frontend/src/pages/component/ComponentManagement.tsx
git commit -m "feat(tree): 组件管理树表配置UI(开关+ID/父ID双下拉+默认展开+防呆+存取)"
```

---

## Task 8: 渲染集成 — QuotationStep2 编辑态

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/treeTable.ts`(追加 `resolveTreeKey` + `layoutTreeRows`)
- Modify: `cpq-frontend/src/pages/quotation/QuotationStep2.tsx`

把 effectiveRows 数组(~line 1581-1582 处)按 treeConfig 重排,折叠隐藏 + 首列缩进/箭头。

- [ ] **Step 1: treeTable.ts 追加 resolveTreeKey(单测先行)**

在 `treeTable.test.ts` 追加:

```ts
import { resolveTreeKey } from './treeTable';

describe('resolveTreeKey', () => {
  it('driver BASIC_DATA:优先取 basicDataValues[lookupKey]', () => {
    const field = { name: '料号', field_type: 'BASIC_DATA', basic_data_path: 'mat_part.part_no' } as any;
    const v = resolveTreeKey(field, { 料号: 'OLD' }, { 'mat_part.part_no': 'P001' }, (p: string) => p);
    expect(v).toBe('P001');
  });
  it('basicDataValues 缺键 → 回退 row[name]', () => {
    const field = { name: '料号', field_type: 'BASIC_DATA', basic_data_path: 'mat_part.part_no' } as any;
    const v = resolveTreeKey(field, { 料号: 'P002' }, {}, (p: string) => p);
    expect(v).toBe('P002');
  });
  it('数组值 → 取首元素', () => {
    const field = { name: '料号', field_type: 'BASIC_DATA', basic_data_path: 'x' } as any;
    const v = resolveTreeKey(field, {}, { x: ['A', 'B'] }, (p: string) => p);
    expect(v).toBe('A');
  });
  it('空 → null', () => {
    const field = { name: '料号', field_type: 'INPUT_TEXT' } as any;
    expect(resolveTreeKey(field, {}, undefined, (p: string) => p)).toBeNull();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-frontend && npx vitest run src/pages/quotation/treeTable.test.ts`
Expected: FAIL — `resolveTreeKey` 未定义。

- [ ] **Step 3: 实现 resolveTreeKey + layoutTreeRows**

追加到 `treeTable.ts`:

```ts
import type { TreeNodeInput } from './treeTable'; // 同文件内无需 import,占位说明

/**
 * 解析某行某字段用于建树的归一化键(字符串)。
 * 优先级对齐 ComponentCell 取值:driver 行级 basicDataValues[lookupKey] → row[name]。
 * idField/parentField 据 spec 来自 driver/SQL 视图(BASIC_DATA / DATA_SOURCE.BNF_PATH)。
 * @param bnfLookup bnfDriverLookupKey 函数(从 useDriverExpansions 传入,避免本模块依赖 React 链)
 */
export function resolveTreeKey(
  field: { name?: string; field_type?: string; basic_data_path?: string; datasource_binding?: any },
  row: Record<string, any>,
  basicDataValues: Record<string, any> | undefined,
  bnfLookup: (path: string) => string,
): string | null {
  const norm = (v: any): string | null => {
    if (v == null || v === '') return null;
    if (Array.isArray(v)) return v.length > 0 ? norm(v[0]) : null;
    if (typeof v === 'object') return null; // 复杂对象不作树键
    const s = String(v).trim();
    return s === '' ? null : s;
  };
  let path: string | undefined;
  if (field.field_type === 'BASIC_DATA') path = field.basic_data_path;
  else if (field.field_type === 'DATA_SOURCE' && field.datasource_binding?.type === 'BNF_PATH') {
    path = field.datasource_binding.bnf_path;
  }
  if (path && basicDataValues) {
    const lk = bnfLookup(path);
    if (Object.prototype.hasOwnProperty.call(basicDataValues, lk)) {
      const got = norm(basicDataValues[lk]);
      if (got != null) return got;
    }
  }
  // 兜底:row[name]
  return norm(field.name ? row[field.name] : null);
}

export interface TreeRenderRow<T> {
  item: T;
  originalIndex: number;
  depth: number;
  hasChildren: boolean;
  nodeKey: string;
  parentIndex: number | null;
}

export interface TreeLayoutResult<T> {
  rows: TreeRenderRow<T>[];                  // 已按树序重排(全部行,未做折叠过滤)
  parentIndexByIndex: Record<number, number | null>;
  nodeKeyByIndex: Record<number, string>;
}

/**
 * 把渲染描述符数组按 treeConfig 重排成树渲染行。
 * @param items 渲染描述符(每项含足以解析 id/parent 的数据)
 * @param idOf/parentOf 从描述符取归一化键(用 resolveTreeKey)
 * @param keyPrefix nodeKey 前缀(用 componentId,保证全局唯一 + tab 切换稳定)
 */
export function layoutTreeRows<T>(
  items: T[],
  idOf: (item: T, index: number) => string | null,
  parentOf: (item: T, index: number) => string | null,
  keyPrefix: string,
): TreeLayoutResult<T> {
  const nodes: TreeNodeInput[] = items.map((it, i) => ({ id: idOf(it, i), parent: parentOf(it, i) }));
  const layout = buildTreeRows(nodes);
  const nodeKeyByIndex: Record<number, string> = {};
  for (let i = 0; i < items.length; i++) {
    const id = nodes[i].id;
    nodeKeyByIndex[i] = `${keyPrefix}::${id != null ? id : `#${i}`}`;
  }
  const rows: TreeRenderRow<T>[] = layout.order.map((origIdx) => ({
    item: items[origIdx],
    originalIndex: origIdx,
    depth: layout.depthByIndex[origIdx] ?? 0,
    hasChildren: layout.hasChildren.has(origIdx),
    nodeKey: nodeKeyByIndex[origIdx],
    parentIndex: layout.parentIndexByIndex[origIdx] ?? null,
  }));
  return { rows, parentIndexByIndex: layout.parentIndexByIndex, nodeKeyByIndex };
}
```

> `TreeNodeInput` / `buildTreeRows` 已在同文件(Task1)定义,直接引用,**删除上面占位 import 行**(写实现时不要保留 `import type { TreeNodeInput } ...`,它只是阅读提示)。

- [ ] **Step 4: 跑 resolveTreeKey 测试确认通过**

Run: `cd cpq-frontend && npx vitest run src/pages/quotation/treeTable.test.ts`
Expected: 全绿。

- [ ] **Step 5: QuotationStep2 顶层引入折叠 hook + import**

在 `QuotationStep2.tsx` 顶部 import:

```tsx
import { layoutTreeRows, isTreeRowHidden } from './treeTable';
import { resolveTreeKey } from './treeTable';
import { useTreeCollapse } from './useTreeCollapse';
import { bnfDriverLookupKey } from './useDriverExpansions';
```

在渲染单个 ProductCard 的函数组件顶层(`effectiveRows` 所在组件,与 `activeComponent` 同作用域的组件函数体内)调用:

```tsx
  const treeCollapse = useTreeCollapse();
```

> 该 hook 必须在组件函数顶层无条件调用。若 effectiveRows 在某子组件内,把 `useTreeCollapse()` 放到该子组件顶层。

- [ ] **Step 6: 重排 + 渲染改造(~line 1581-1688)**

把 `(() => { ... return effectiveRows.map((er, idx) => ({ ...er, formulaCache: preComputedCaches[idx] })); })()` 的结果改为树序遍历。具体:在 IIFE `return` 之前(已得 `effectiveRows` + `preComputedCaches`)插入:

```tsx
                    // ── 树表重排(纯展示):有 treeConfig 时按 id/parent 重排 + 折叠 ──
                    const withCache = effectiveRows.map((er, idx) => ({ ...er, formulaCache: preComputedCaches[idx] }));
                    const treeCfg = activeComponent.treeConfig;
                    if (!treeCfg?.idField || !treeCfg?.parentField) {
                      // 非树表:原样平铺(行为零变化),depth=0/无箭头
                      return withCache.map((r) => ({ ...r, _depth: 0, _hasChildren: false, _nodeKey: '', _origIndex: r.rowIndex }));
                    }
                    const idFieldDef = activeComponent.fields.find(f => (f.name || f.key) === treeCfg.idField);
                    const parentFieldDef = activeComponent.fields.find(f => (f.name || f.key) === treeCfg.parentField);
                    const keyPrefix = activeComponent.componentId || activeComponent.tabName || 'tree';
                    const laid = layoutTreeRows(
                      withCache,
                      (it) => idFieldDef ? resolveTreeKey(idFieldDef, it.row, it.basicDataValues, bnfDriverLookupKey) : null,
                      (it) => parentFieldDef ? resolveTreeKey(parentFieldDef, it.row, it.basicDataValues, bnfDriverLookupKey) : null,
                      keyPrefix,
                    );
                    const defExp = treeCfg.defaultExpanded ?? true;
                    const collapsed = treeCollapse.collapsedSet(Object.values(laid.nodeKeyByIndex), defExp);
                    return laid.rows
                      .filter(r => !isTreeRowHidden(r.originalIndex, laid.parentIndexByIndex, laid.nodeKeyByIndex, collapsed))
                      .map(r => ({
                        ...r.item,
                        _depth: r.depth,
                        _hasChildren: r.hasChildren,
                        _nodeKey: r.nodeKey,
                        _origIndex: r.originalIndex,
                      }));
                  })().map(({ row, rowIndex, rowKey, basicDataValues, isDriverBound, isListFormulaBound, formulaCache, listFormulaItem, listFormulaField, _depth, _hasChildren, _nodeKey }) => {
```

> 注意:把原 `.map(({ row, rowIndex, ... }) => {` 的解构参数补上 `_depth, _hasChildren, _nodeKey`(如上)。`_origIndex` 不解构也可,`rowIndex` 仍是原始下标(用于编辑写路径,保持不变 → 不破坏 AP-54 下标纪律)。

- [ ] **Step 7: 首列注入缩进 + 折叠箭头**

在 `activeComponent.fields.map(field => { ... })` 内,把首个字段单元格加缩进/箭头。最简做法:在该 map 回调起始处判断是否首列,渲染 `<ComponentCell>` 外包一层带 paddingLeft 的容器 + 箭头按钮。定位 `return ( <td key={key} ...>` 块,改为:

```tsx
                        const isFirstField = activeComponent.fields[0] === field;
                        const treeIndent = (activeComponent.treeConfig?.idField && activeComponent.treeConfig?.parentField) ? (_depth ?? 0) : 0;
                        return (
                          <td key={key} className={/* 原有 className 逻辑保持 */ [
                              field.field_type === 'FORMULA' ? 'qt-formula-cell' : '',
                              field.field_type === 'LIST_FORMULA' ? 'qt-formula-cell' : '',
                              isRequiredEmpty ? 'qt-ds-required-empty' : '',
                            ].filter(Boolean).join(' ') || undefined}>
                            {isFirstField && treeIndent >= 0 && activeComponent.treeConfig?.idField ? (
                              <span style={{ display: 'inline-flex', alignItems: 'center', gap: 4 }}>
                                <span style={{ display: 'inline-block', width: treeIndent * 16 }} />
                                {_hasChildren ? (
                                  <button
                                    type="button"
                                    onClick={() => treeCollapse.toggle(_nodeKey)}
                                    style={{ border: 'none', background: 'none', cursor: 'pointer', fontSize: 10, width: 14, padding: 0, color: '#888' }}
                                    title="展开/折叠"
                                  >
                                    {treeCollapse.isCollapsed(_nodeKey, activeComponent.treeConfig.defaultExpanded ?? true) ? '▶' : '▼'}
                                  </button>
                                ) : (
                                  <span style={{ display: 'inline-block', width: 14 }} />
                                )}
                                {/* 原 cell 内容 */}
                                {cellInner}
                              </span>
                            ) : (
                              cellInner
                            )}
                          </td>
                        );
```

把原本 `<td>` 内的 `showElementHint ? (...) : (<ComponentCell .../>)` 整段抽成一个 `const cellInner = showElementHint ? (...) : (<ComponentCell .../>);`(在 return 之前定义),供上面两分支复用,避免重复。

- [ ] **Step 8: 自检(tsc + Vite + E2E 前置)**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误。

Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/QuotationStep2.tsx`
Expected: 200。

- [ ] **Step 9: 提交**

```bash
git add cpq-frontend/src/pages/quotation/treeTable.ts cpq-frontend/src/pages/quotation/treeTable.test.ts cpq-frontend/src/pages/quotation/QuotationStep2.tsx
git commit -m "feat(tree): 报价单编辑态树表渲染(layoutTreeRows重排+缩进+折叠箭头;rowIndex写路径不变)"
```

---

## Task 9: 渲染集成 — ReadonlyProductCard 只读/详情/核价态

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/ReadonlyProductCard.tsx`

只读态渲染循环在 ~line 461-515(`Array.from({length: effectiveCount}, (_, ri) => <tr>)`)。同款树化:构建描述符 → layoutTreeRows → 折叠过滤 → 首列缩进。

- [ ] **Step 1: 顶层引入 hook + import**

顶部 import:

```tsx
import { layoutTreeRows, isTreeRowHidden, resolveTreeKey } from './treeTable';
import { useTreeCollapse } from './useTreeCollapse';
// bnfDriverLookupKey 已在本文件 import(line 6 useDriverExpansions),复用
```

在 ReadonlyProductCard 组件函数顶层加:

```tsx
  const treeCollapse = useTreeCollapse();
```

- [ ] **Step 2: 构建描述符数组并树化(改 ~line 461)**

把 `{Array.from({ length: effectiveCount }, (_, ri) => { const rawRow = ...; ... return (<tr key={ri}>...) })}` 改为先构建描述符再树化:

```tsx
                        {(() => {
                          const descriptors = Array.from({ length: effectiveCount }, (_, ri) => ({
                            ri,
                            rawRow: activeComp.rows[ri] ?? {},
                            rowBdv: useDriver ? activeDriverExpansion!.rows[ri]?.basicDataValues : undefined,
                            formulaCache: preComputedCaches[ri] ?? {},
                          }));
                          const treeCfg = activeComp.treeConfig;
                          let ordered = descriptors.map(d => ({ ...d, _depth: 0, _hasChildren: false, _nodeKey: '', _origIndex: d.ri }));
                          if (treeCfg?.idField && treeCfg?.parentField) {
                            const idFieldDef = activeComp.fields.find(f => (f.name) === treeCfg.idField);
                            const parentFieldDef = activeComp.fields.find(f => (f.name) === treeCfg.parentField);
                            const keyPrefix = activeComp.componentId || activeComp.tabName || 'tree';
                            const laid = layoutTreeRows(
                              descriptors,
                              (it) => idFieldDef ? resolveTreeKey(idFieldDef, it.rawRow, it.rowBdv, bnfDriverLookupKey) : null,
                              (it) => parentFieldDef ? resolveTreeKey(parentFieldDef, it.rawRow, it.rowBdv, bnfDriverLookupKey) : null,
                              keyPrefix,
                            );
                            const defExp = treeCfg.defaultExpanded ?? true;
                            const collapsed = treeCollapse.collapsedSet(Object.values(laid.nodeKeyByIndex), defExp);
                            ordered = laid.rows
                              .filter(r => !isTreeRowHidden(r.originalIndex, laid.parentIndexByIndex, laid.nodeKeyByIndex, collapsed))
                              .map(r => ({ ...r.item, _depth: r.depth, _hasChildren: r.hasChildren, _nodeKey: r.nodeKey, _origIndex: r.originalIndex }));
                          }
                          return ordered.map(({ ri, rawRow, rowBdv, formulaCache, _depth, _hasChildren, _nodeKey }) => (
                          <tr key={ri}>
                            {activeComp.fields.map((field) => {
                              // …原有 cellCtx 构建保持不变…
                              const isFirstField = activeComp.fields[0] === field;
                              const treeOn = !!(activeComp.treeConfig?.idField && activeComp.treeConfig?.parentField);
                              // 原有 <td> 内 <ComponentCell> 抽成 cellInner 复用
                              return (
                                <td key={key} className={/* 原 className */ undefined}>
                                  {isFirstField && treeOn ? (
                                    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 4 }}>
                                      <span style={{ display: 'inline-block', width: (_depth ?? 0) * 16 }} />
                                      {_hasChildren ? (
                                        <button type="button" onClick={() => treeCollapse.toggle(_nodeKey)}
                                          style={{ border: 'none', background: 'none', cursor: 'pointer', fontSize: 10, width: 14, padding: 0, color: '#888' }} title="展开/折叠">
                                          {treeCollapse.isCollapsed(_nodeKey, activeComp.treeConfig!.defaultExpanded ?? true) ? '▶' : '▼'}
                                        </button>
                                      ) : (<span style={{ display: 'inline-block', width: 14 }} />)}
                                      {cellInner}
                                    </span>
                                  ) : cellInner}
                                </td>
                              );
                            })}
                          </tr>
                          ));
                        })()}
```

> 保留原有 `cellCtx` / `showTrace` / `fieldPath` / `<FieldTraceIcon>` 逻辑;把原 `<ComponentCell .../>`(含 `<FieldTraceIcon>` 的 `<span>`)整体抽为 `const cellInner = (...)` 在字段 map 回调内、return 之前定义,供首列/非首列两分支复用。`ri` 仍是原始下标(rawRow / trace fieldPath 用,保持正确)。

- [ ] **Step 3: 自检**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误。

Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/ReadonlyProductCard.tsx`
Expected: 200。

- [ ] **Step 4: 确认无第三处 ComponentCell 行渲染遗漏(核价单)**

Run: `grep -rn "ComponentCell" cpq-frontend/src/pages --include=*.tsx | grep -v "QuotationStep2\|ReadonlyProductCard\|ComponentCell.tsx"`
Expected: 无输出(确认报价/核价/详情三视图的组件行渲染只经 QuotationStep2 + ReadonlyProductCard 两条路径;若有其它 callsite,补同款树化并在本任务追加 step)。

- [ ] **Step 5: 提交**

```bash
git add cpq-frontend/src/pages/quotation/ReadonlyProductCard.tsx
git commit -m "feat(tree): 只读/详情/核价态树表渲染(ReadonlyProductCard 同款重排+缩进+折叠)"
```

---

## Task 10: E2E + 全链路自检

**Files:**
- 验证为主;如需新增树表专项 E2E,Modify: `cpq-frontend/e2e/quotation-flow.spec.ts`

协议级改动(改了 enrich / QuotationStep2 / ReadonlyProductCard / TemplateService snapshot / CardSnapshotService)→ 强制 E2E。

- [ ] **Step 1: 全量单测**

Run: `cd cpq-frontend && npx vitest run src/pages/quotation/treeTable.test.ts`
Expected: 全绿(buildTreeRows 9 + isTreeRowHidden 3 + resolveTreeKey 4)。

- [ ] **Step 2: 跑既有报价流 E2E(回归,确认非树表组件零行为变化)**

```bash
cd cpq-frontend
rm -f e2e/screenshots/qf-*.png 2>/dev/null || true
npx playwright test --config=e2e/playwright.config.ts e2e/quotation-flow.spec.ts --reporter=list
```
Expected: 所有 test `passed`;`'加载中' final count = 0`;8 Tab `'加载中'=0`;行数刷新 3 次稳定(AP-51 未累加)。

- [ ] **Step 3: 树表渲染手动验证(挑一个配了树表的多行组件)**

在报价单填写视图 + 详情页打开含父子料号的组件 Tab,确认:
- 子行紧跟父行、按层级缩进;
- 父行有 ▼/▶ 箭头,点击折叠后子行隐藏、再点恢复;
- 折叠状态下 Tab 小计金额不变(纯展示,折叠不改数值);
- 刷新页面后折叠态回到 `defaultExpanded` 默认。

截图留证(报价单 + 详情页各一张树表 Tab)。

- [ ] **Step 4: snapshot 传播验证**

对配了树表的组件跑 refresh,确认模板 snapshot 带 `tree_config`:

Run: `curl -s -X POST http://localhost:8081/api/cpq/components/<componentId>/refresh-template-snapshots; echo`
Expected: 200/成功。

Run: 取一个引用该组件的模板,`curl -s http://localhost:8081/api/cpq/templates/<templateId>` 并确认其 componentsSnapshot 对应 tab 含 `tree_config`(或前端进报价单后 `ComponentDataItem.treeConfig` 非空)。

- [ ] **Step 5: 后端健康 + Flyway**

Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/q/health`(期望 200)
Run: `PGPASSWORD=$PGPASSWORD psql -h localhost -U $PGUSER -d $PGDB -c "SELECT version, success FROM flyway_schema_history WHERE version='289'"`(期望 `289 | t`)

- [ ] **Step 6: 自检声明 + 提交(若新增 E2E/截图)**

写一行自检声明,例如:
> "vitest treeTable 16 passed ✅;tsc 0 错误 ✅;QuotationStep2/ReadonlyProductCard → Vite 200 ✅;quotation-flow E2E 1 passed, '加载中'=0 ✅;V289 success=t ✅;component.treeConfig 回读正常 ✅"

```bash
git add cpq-frontend/e2e/quotation-flow.spec.ts cpq-frontend/e2e/screenshots/ 2>/dev/null || true
git commit -m "test(tree): 树表渲染 E2E/手验留证 + 全链路自检通过" || echo "无 E2E 文件变更则跳过本提交"
```

---

## Self-Review(写计划后自查结论)

**Spec 覆盖核对(逐节)**:
- §2 邻接表模型 → Task 1 `buildTreeRows` ✅
- §3 存储 treeConfig + 独立列 tree_config + V289 + 校验 → Task 4 ✅
- §4 配置 UI 开关 + 双下拉 + 默认展开 → Task 7 ✅
- §5 传播链(后端 entity/DTO/TemplateService/CardSnapshotService + 前端 enrich 双路径) → Task 4/5/6 ✅
- §6 渲染方案 A(`buildTreeRows`/`useTreeCollapse`/三视图共享、不动数据层) → Task 1/2/8/9 ✅
- §7 六条边界规则 → Task 1 单测逐条覆盖 ✅
- §8 单测 + E2E → Task 1/2/8(单测)+ Task 10(E2E) ✅
- §9.5 不动 rowData 行序(rowIndex 写路径不变;只重排渲染序) → Task 8 Step6 注释 + 设计约束 ✅

**Placeholder 扫描**:无 TBD/TODO;每个代码步骤含完整代码。少数「按文件实际变量名对齐」注解(MAPPER 字段名 / snapshotEntry node 名 / snapshotComp 变量名)是「读现场代码确认命名」的精确指令,非占位。

**类型一致性**:`TreeConfig`(idField/parentField/defaultExpanded)三处类型一致;`buildTreeRows`→`TreeLayout`,`layoutTreeRows`→`TreeLayoutResult<T>`/`TreeRenderRow<T>`,`isTreeRowHidden`/`resolveTreeKey` 签名跨 Task 8/9 一致;hook `useTreeCollapse` 暴露 `isCollapsed/toggle/collapsedSet` 三处调用一致。

**风险点**:渲染集成(Task 8/9)是改密集 JSX,最易出错;两任务都要求抽 `cellInner` 复用以避免重复,并保留 `rowIndex/ri` 作为原始下标(不破坏编辑写路径 AP-54 与 trace fieldPath)。
