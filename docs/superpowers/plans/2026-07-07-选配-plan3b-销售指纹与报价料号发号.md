# 选配模板方案 · Plan 3b：销售侧指纹 + 去重 + 报价料号发号 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development。Steps 用 `- [ ]`。
> **🔴 本 Plan 触碰核心基线 `ConfigureProductService`（CLAUDE.md「修改后强制自检」§5 点名文件）→ 强制 Playwright E2E 双 spec。**

**Goal:** 选配 custom/COMPOSITE 分支：用**销售侧客户维度指纹**（落 `sel_part_signature`）判复用，取代生产侧全局指纹判复用；发号从 `CFG-` 换成统一报价料号 `QuoteMaterialNoAllocator.mintAndRegister`；写 `material_master` 时 `config_fingerprint` 置 **NULL**（避免跨客户同物质撞全局唯一索引）。

**Architecture / 基线依据：** 严格按《选配运行时接入-集成设计》(`docs/superpowers/specs/2026-07-07-选配运行时接入-集成设计.md`) §2 + §4。改动集中在 `ConfigureProductService.resolvePart`（SIMPLE custom）与 `configure`（COMPOSITE 父级）两处发号 + 一处 fp 实参。新增 `sel_part_signature` 表 + `SalesFingerprintCalculator` + `SalesSignatureRepository`，均与现役生产侧 `FingerprintCalculator` **并列、不改后者**。

**Tech Stack:** Java 17 + Quarkus + Panache + Flyway；测试 `@QuarkusTest`+RestAssured + Playwright E2E。

**依赖（硬前置，均已在 master）：** Spec1 `QuoteMaterialNoAllocator` + `material_customer_map.system_type`；Plan 1/2（`sel_param_type`）；`V311` 已加 `sales_part_no` 列。**Plan 3a**（EnabledParam 投影运行时）为**同批依赖**——若 3a 未合，本 Plan T3 先定 `EnabledParam` record 契约 + 内部最小投影桩，与 3a 对齐同一 record。

**新迁移从 V314 起**（执行前按 Plan 2「动态算号」复核 max，撞则顺延）。

---

## Task 0: 前置校验 + 调用面复核（不改代码）

- [ ] **Step 1**：`codegraph_callers isCfg`（应仅 `MaterialBomMergeHandler` 2 处）；`codegraph_callers partNoProvider.apply`（确认选配外是否还有别的调用点——保留 PartNoProvider 供它们）。
- [ ] **Step 2**：确认前置在目标分支：`grep -l system_type cpq-backend/src/main/resources/db/migration/V308*.sql`（Spec1）、`ls .../V311__pricing_sales_part_no.sql`（sales_part_no 列）、`grep sel_param_type .../V313*.sql`。
- [ ] **Step 3**：Read `ConfigureProductService.java` 的 `:180-274`（resolvePart custom）、`:771-813`（configure/COMPOSITE）、`:390-453`（insert 方法）、`:850`（getCustomerCodeFromCustomerId）；`FingerprintCalculator.java:42-100`；`QuoteMaterialNoAllocator.java:18-63`。**记录真实当前代码，后续改造以真实行为准（集成设计给的行号可能因基线演进偏移）。**
- [ ] 无提交（纯勘查）。

---

## Task 1: `V314` + `SelPartSignature` 实体 + `SalesSignatureRepository`

**Files:** Create migration + `com.cpq.configure.entity.SelPartSignature` + `com.cpq.configure.service.SalesSignatureRepository`

- [ ] **Step 1: 迁移**（号按动态算法确定，预计 V314）

```sql
-- V314__sel_part_signature.sql —— 销售侧客户维度指纹去重登记(独立于 material_master.config_fingerprint 生产侧全局)
CREATE TABLE sel_part_signature (
    id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    customer_no           VARCHAR(50)  NOT NULL,
    structure_version     VARCHAR(10)  NOT NULL,
    config_fingerprint    CHAR(64)     NOT NULL,
    config_signature_text TEXT         NOT NULL,
    quote_part_no         VARCHAR(32)  NOT NULL,
    product_type          VARCHAR(16)  NOT NULL DEFAULT 'SIMPLE',
    created_at            TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    CONSTRAINT uq_sel_part_signature UNIQUE (customer_no, structure_version, config_fingerprint)
);
CREATE INDEX idx_sel_part_signature_quote ON sel_part_signature(quote_part_no);
```

- [ ] **Step 2: 实体 `SelPartSignature`**（Panache，字段映射上表）—— 参照现役 Panache 实体写法（`@Id UUID`、`@Column(name=...)`）。
- [ ] **Step 3: `SalesSignatureRepository`**：

```java
@ApplicationScoped
public class SalesSignatureRepository {
    @Inject EntityManager em;

    /** 命中返回 quote_part_no，否则 null。 */
    public String lookup(String customerNo, String structureVersion, String fingerprint) {
        List<?> r = em.createNativeQuery(
            "SELECT quote_part_no FROM sel_part_signature WHERE customer_no=:c AND structure_version=:v AND config_fingerprint=:f")
            .setParameter("c", customerNo).setParameter("v", structureVersion).setParameter("f", fingerprint)
            .setMaxResults(1).getResultList();
        return r.isEmpty() ? null : (String) r.get(0);
    }

    /** 登记；并发败者(ON CONFLICT DO NOTHING 返回0)时返回既有 quote_part_no(回读)，否则返回传入的 quotePartNo。 */
    @Transactional
    public String insertOrReadExisting(String customerNo, String structureVersion, String fp, String text,
                                       String quotePartNo, String productType) {
        int n = em.createNativeQuery(
            "INSERT INTO sel_part_signature (customer_no,structure_version,config_fingerprint,config_signature_text,quote_part_no,product_type) " +
            "VALUES (:c,:v,:f,:t,:q,:pt) ON CONFLICT (customer_no,structure_version,config_fingerprint) DO NOTHING")
            .setParameter("c",customerNo).setParameter("v",structureVersion).setParameter("f",fp)
            .setParameter("t",text).setParameter("q",quotePartNo).setParameter("pt",productType)
            .executeUpdate();
        if (n == 1) return quotePartNo;              // 先赢者
        return lookup(customerNo, structureVersion, fp);  // 败者回读先赢者号
    }
}
```

- [ ] **Step 4: TDD 测试** `SalesSignatureRepositoryTest`：唯一键拦重复；`insertOrReadExisting` 第二次相同指纹返回**首次的** quotePartNo（模拟并发败者回读）。跑 `./mvnw test -Dtest=SalesSignatureRepositoryTest ...` 绿。
- [ ] **Step 5: 提交**（迁移+实体+repo+测试，逐 commit 或合并）。

---

## Task 2: `SalesFingerprintCalculator`

**Files:** Create `com.cpq.configure.SalesFingerprintCalculator`（`@ApplicationScoped`）

> 按集成设计 §2.2。与生产侧 `FingerprintCalculator` **并列不改后者**；`normalize` 复用其去尾零口径（`stripTrailingZeros().toPlainString()`）。

- [ ] **Step 1: 先写测试**（TDD）`SalesFingerprintCalculatorTest`：
  - 空哨兵：模板启用 PROCESS 但未选 → 串含 `PRC=∅`，与「未启用 PROCESS」的串不同。
  - 工序排序集合：`[电镀,抛光]` 与 `[抛光,电镀]` → 同指纹。
  - 元素去尾零：`Cr=18.0000` 与 `Cr=18` → 同指纹。
  - 客户维度：同参数不同 `customerNo` → 不同指纹。
  - 结构版本前缀：text 以 `v1|CUST=` 开头。
  - COMPOSITE：childQuotePartNos 排序集合 → 顺序无关同指纹。
- [ ] **Step 2: 实现**（`EnabledParam` record + `computeSimple`/`computeComposite` + `sha256`）——按集成设计 §2.2 伪代码逐条落地：
  - token：MATERIAL→`MAT=code`/`MAT=∅`；ELEMENT→`ELE=`元素码升序`码:normalize(含量)`/`ELE=∅`；PROCESS→`PRC=`工序码升序join","/`PRC=∅`。
  - `text = "v1|CUST="+customerNo+"|"+tokens.sorted(by paramTypeCode).join("|")`；`hash=sha256hex(text)`。
  - **`EnabledParam` record 契约必须与 Plan 3a 产出的投影一致**（同一 record 定义，放 `com.cpq.configure` 供两边引用）。
- [ ] **Step 3: 测试绿 → 提交**。

---

## Task 3: `SalesConfigContext` + `configure` 入口装配

**Files:** Create `com.cpq.configure.dto.SalesConfigContext` + Modify `ConfigureProductService.configure(:771)`

> 集成设计 §2.6。`configure` 入口按 `customerId → customer.industry_code → SelTemplateService.getByIndustry(industryCode) ?? getByIndustry("__DEFAULT__")`，把每个 `PartRequest` 投影成 `EnabledParam[]`（启用未选=空哨兵），组装 `SalesConfigContext{customerNo=customerCode, yyMm=YearMonth.now(), structureVersion="v1", enabledParamsPerPart, ...}`。

- [ ] **Step 1: Read** `configure(:771-813)` 当前代码 + `SelTemplateService.getByIndustry` 返回结构（Plan 2）。
- [ ] **Step 2: 写 `SalesConfigContext`**（record 或 class：customerNo/yyMm/structureVersion + `Map<partIndex, List<EnabledParam>>` 或每 PartRequest 挂）。
- [ ] **Step 3: `configure` 入口装配**：查 industry_code（native 或 Customer 实体）→ 解析模板（回退 __DEFAULT__，无则空参数集但不报错）→ 每 PartRequest 投影 EnabledParam（依 template enabled 项 + pr 的 recipeCode/elements/processIds；启用未选=空哨兵）。
  - **依赖 3a**：若 3a 已合，复用其投影逻辑/DTO；若未合，本 Task 内实现最小投影（与 3a 对齐 EnabledParam record）。
- [ ] **Step 4: 编译无错 + 现役 configure 单测（若有）不回归 → 提交**。

---

## Task 4: `resolvePart` SIMPLE custom 发号 swap + fp=NULL

**Files:** Modify `ConfigureProductService.resolvePart(:180-274)`

> 集成设计 §2.3 改造表（**以 Task 0 记录的真实当前代码为准应用**）。

- [ ] **Step 1: 注入** `@Inject QuoteMaterialNoAllocator quoteAllocator; @Inject SalesFingerprintCalculator salesFp; @Inject SalesSignatureRepository sigRepo;`。`resolvePart` 签名追加 `SalesConfigContext salesCtx`（调用点 `configure` 同步传入）。
- [ ] **Step 2: 按 §2.3 表改 custom 分支**：
  - 复用判定：`var sig = salesFp.computeSimple(salesCtx.customerNo(), salesCtx.enabledParamsFor(pr)); String hit = sigRepo.lookup(salesCtx.customerNo(), "v1", sig.hash());`
  - 命中：`if (hit != null){ reused.add(hit); return hit; }` —— **在任何落库(`:262`)之前 return**（R3：命中跳过全部落库）。
  - 未命中发号：`String hfPartNo = quoteAllocator.mintAndRegister(salesCtx.customerNo(), salesCtx.yyMm());` 然后 `sigRepo.insertOrReadExisting(salesCtx.customerNo(),"v1",sig.hash(),sig.text(),hfPartNo,"SIMPLE")`——**若返回值≠hfPartNo（并发败者回读到别人的号），改用返回号并放弃自己 mint 的号（孤儿可接受，R4），return 该号**。
  - `insertMaterialMasterV6(...)` 的 fp 实参 → **`null`**（R1）。
  - 删除 `lookupHfByFingerprint(fp)` 对复用的驱动（生产侧 `fingerprintCalc.simpleFingerprint` 可保留为审计变量或一并移除；不再驱动复用）。
  - **customerCode 空**：custom 选配强制校验 `customerCode` 非空（报价料号号内嵌客户码），空则 `recordError`/抛错（R6）。
- [ ] **Step 3: 编译 + 后端单测**（见 Task 7）不回归 → 提交。

---

## Task 5: COMPOSITE 父级 swap

**Files:** Modify `ConfigureProductService.configure(:792-813)`

> 集成设计 §2.4 表。父组合体对 `childQuotePartNos` 排序集合 + customerCode 算 `computeComposite` → 命中复用 / 未命中 `mintAndRegister` + `insertOrReadExisting(...,"COMPOSITE")`；`insertMaterialMasterV6(parent,...,null)` fp=NULL；命中复用时整体跳过父级落库。

- [ ] **Step 1: 按 §2.4 表改**（以 Task 0 真实代码为准）。
- [ ] **Step 2: 编译 + 单测不回归 → 提交**。

---

## Task 6: `lookupFingerprint` 端点决策

**Files:** Modify `ConfigureProductService.lookupFingerprint(:78-112)`（或标 TODO）

> 集成设计 §4.3。选配号 config_fingerprint 改 NULL 后该端点对新号恒 `matched=false`。**推荐 (a)**：改查 `sel_part_signature`（需前端 P2 传客户上下文，依赖 3a）；若 3a 未就绪 → **(b) 过渡**：保留查 material_master（仅命中历史 CFG- 号），代码里标 `// TODO(3a): 改查 sel_part_signature 客户维度实时复用`。

- [ ] **Step 1: 按 3a 就绪情况选 (a)/(b)，实现或标 TODO。**
- [ ] **Step 2: 提交**。

---

## Task 7: E2E + 单测（强制）

**Files:** `ConfigureProductServiceSalesFingerprintTest`（后端）+ 跑 Playwright 双 spec

- [ ] **Step 1: 后端单测**（RestAssured 走 `/api/cpq/configure` 或 service 级，`@BeforeEach/@AfterEach` 清 `sel_part_signature`/选配产出的 `material_master`/`material_customer_map` QUOTE 行，用独特测试料号）：
  - **R1**：两个不同客户各自对同一 recipe+elements 选配一次 → 各得**不同** quotePartNo；两行 `material_master.config_fingerprint` 均 **NULL**；无 `uq_material_master_fingerprint` 冲突。
  - **R3**：同客户同选值选配两次 → 第二次命中复用**同** quotePartNo；`material_master`/`element_bom_item` 行数**不增**（刷新稳定，守 AP-51）。
  - **发号格式**：产出料号匹配 `^\d{4}-\d{6,}$`（报价料号，非 CFG-）。
- [ ] **Step 2: Playwright E2E**（CLAUDE.md 强制；测试数据见记忆 `cpq-e2e-quotation-flow-test-data`）——**需在主分支/主树 dev server 跑**（worktree 跑不了 E2E，见记忆）：
  ```
  npx playwright test --config=e2e/playwright.config.ts e2e/quotation-flow.spec.ts --reporter=list
  npx playwright test --config=e2e/playwright.config.ts e2e/composite-product-flow.spec.ts --reporter=list
  ```
  必须：所有 test `passed`、`'加载中' final count = 0`、8 Tab `'加载中'=0`。**E2E 依赖主树 dev server + DB**——worktree 内只能跑后端单测，E2E 在合回主分支后由主树 server 跑（如实标注，别跳过）。
- [ ] **Step 3: BACKLOG**：把 `BL-0017`（报价料号统一 Spec2·选配发号统一）标记「由选配运行时接入 3b/3c 覆盖」（集成设计 §6）。
- [ ] **Step 4: 提交 + 一行「已自检」声明**（含 R1/R3 后端断言 + E2E 双 spec 结果）。

---

## Self-Review（对照集成设计 + 需求 spec）
- §2.1 sel_part_signature DDL / 唯一键 → T1 ✅
- §2.2 SalesFingerprintCalculator（客户维度/空哨兵/工序集合/去尾零/结构版本）→ T2 ✅
- §2.3 resolvePart swap + fp=NULL + 命中 return → T4 ✅（R1/R3）
- §2.4 COMPOSITE → T5 ✅
- §2.7 并发败者回读 → T1 `insertOrReadExisting` + T4 败者放弃己号 ✅（R4）
- §2.8 isCfg 保持原样 → T0 复核 + 不动 ✅
- §4.3 lookupFingerprint → T6 ✅
- §4.2 E2E 双 spec → T7 ✅（强制）
- 不含：3a 前端渲染、3c 按 sales_part_no 落库（各自计划）——本 Plan 只到「发号+指纹去重」，落库补 sales_part_no 在 3c
- **风险显式**：R1（fp=NULL）是最易漏的致命点，T4 + T7 R1 断言双重把关
