# 报价单详情页 只读快照视图切换 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在报价单详情页（`QuotationDetail.tsx`，只读）加「报价/核价/比对 × 卡片/Excel」两级视图切换，全部只读、读已落库快照、零 batch-expand / 零 ensure / 零阻塞重算。

**Architecture:** 选项 A —— 薄只读快照渲染器 + 复用 `ReadonlyProductCard`。值与四份结构快照后端已全暴露；唯一后端改动 = `getById` 捎回带 `display_format` 的有效 Excel 列定义。Excel 行复用现成 `useExcelSnapshotRows`（含核价 BOM 树），比对复用纯函数 `buildComparisonModel`，格式化抽共享 `renderCellValue/formatNumber`（仅统一显示、不统一值来源）。

**Tech Stack:** 前端 React + Ant Design + Vite + vitest + Playwright；后端 Quarkus + Panache + JUnit。

**Spec:** `docs/superpowers/specs/2026-06-27-报价单详情页只读快照视图切换-design.md`（v2）

**关键纪律（CLAUDE.md / AP）：**
- worktree 已建（`worktree-quote-detail-readonly-views`）；**不另起 dev server / 不重装依赖**，复用主工作区 8081/5174 + 共享 node_modules。
- 后端测试在 **worktree 的 `cpq-backend`** 跑（`./mvnw`，见 [[cpq-worktree-maven-test-tree]]）；`@TestTransaction` 防污染。
- 改 `ReadonlyProductCard` / `QuotationStep2` 导出 / 后端 getById = **协议级**，强制双 spec E2E（`quotation-flow.spec.ts` + `composite-product-flow.spec.ts`）。
- 抽共享函数后**必跑编辑态**确认 `LinkedExcelView`/`ComparisonView` 无回归。
- 每个实现 agent 自检不可信，主线必须亲验测试/DB/E2E（[[cpq-deliver-agents-overreport]]）。
- 提交只 `git add` 本任务明确改动文件，**严禁 `git add -A`**。

---

## 文件结构（决策锁定）

**后端**
- `cpq-backend/.../quotation/dto/QuotationDTO.java` —— 新增两字段 `quoteExcelColumns` / `costingExcelColumns`（`List<Map<String,Object>>`，承载有效列）。
- `cpq-backend/.../quotation/service/QuotationService.java#getById` —— 新增 `populateEffectiveExcelColumns(dto, q)`，用 `ExcelViewService.getEffectiveColumns` 填两侧有效列。
- 测试：`cpq-backend/.../quotation/QuotationDetailColumnsTest.java`（新）+ submit 冻结守卫 `QuotationSubmitFreezeTest.java`（新）。

**前端**
- `cpq-frontend/src/pages/quotation/excelCellFormat.ts`（新）—— 从 `LinkedExcelView.tsx` 抽出 `renderCellValue` / `formatNumber` / `isComputedExcelColumn`。
- `cpq-frontend/src/pages/quotation/excelCellFormat.test.ts`（新，vitest）。
- `cpq-frontend/src/pages/quotation/LinkedExcelView.tsx`（改：改为 import 共享格式化）。
- `cpq-frontend/src/pages/quotation/ReadonlyExcelView.tsx`（新）—— 复用 `useExcelSnapshotRows` + 有效列 + 共享格式化 + 核价树形布局。
- `cpq-frontend/src/pages/quotation/ReadonlyComparison.tsx`（新）—— 两份 Excel 快照 + tag → `buildComparisonModel`。
- `cpq-frontend/src/pages/quotation/comparisonTable.tsx`（新）—— 从 `ComparisonView.tsx` 抽出比对列/行组装（供编辑态与只读共用）。
- `cpq-frontend/src/pages/quotation/ComparisonView.tsx`（改：改用 `comparisonTable` 共享组装）。
- `cpq-frontend/src/pages/quotation/ReadonlyProductCard.tsx`（改：加结构驱动 COSTING 路径 + `side` 等新 props）。
- `cpq-frontend/src/pages/quotation/QuotationDetail.tsx`（改：两级 Segmented 容器 + 空态 + 线程化核价模板 id/结构）。
- `cpq-frontend/src/services/quotationService.ts`（改：补 `ExcelEffectiveColumn` 类型 + getById 响应说明）。
- `cpq-frontend/e2e/quotation-detail-readonly-views.spec.ts`（新，Playwright）。

---

## Task 1: 后端 —— getById 捎回带 display_format 的有效 Excel 列

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/quotation/dto/QuotationDTO.java`
- Modify: `cpq-backend/src/main/java/com/cpq/quotation/service/QuotationService.java`（`getById` 附近，`:140-178`）
- Test: `cpq-backend/src/test/java/com/cpq/quotation/QuotationDetailColumnsTest.java`（新）

**背景（已核验）：** `getById` 已在 `:156` 调 `populateViewStructures` 填四份结构；`ExcelViewService.getEffectiveColumns(Template t)`（`ExcelViewService.java:67`）返回 `List<Map<String,Object>>`（snake_case `col_key`/`source_type`/`display_format`/`comparison_tag`，零值计算）。报价模板 = `q.customerTemplateId`，核价模板 = `q.costingCardTemplateId`。

- [ ] **Step 1: 写失败测试**

`QuotationDetailColumnsTest.java`：
```java
package com.cpq.quotation;

import io.quarkus.test.junit.QuarkusTest;
import io.quarkus.test.TestTransaction;
import jakarta.inject.Inject;
import org.junit.jupiter.api.Test;
import java.util.UUID;
import static org.junit.jupiter.api.Assertions.*;

@QuarkusTest
class QuotationDetailColumnsTest {

    @Inject com.cpq.quotation.service.QuotationService quotationService;

    /** 选一个已提交、报价+核价模板齐备的报价单（夹具）。实现期用 §E2E 同一条数据。 */
    private static final UUID SUBMITTED_QUOTE_ID =
        UUID.fromString("REPLACE_WITH_FIXTURE"); // 实现期填：一条 SUBMITTED 且双模板齐备的报价单

    @Test
    @TestTransaction
    void getById_exposes_effective_excel_columns_with_display_format() {
        var dto = quotationService.getById(SUBMITTED_QUOTE_ID);
        assertNotNull(dto.quoteExcelColumns, "报价有效列应被暴露");
        assertFalse(dto.quoteExcelColumns.isEmpty(), "报价有效列非空");
        // 至少一列带 snake_case col_key
        assertTrue(dto.quoteExcelColumns.stream().anyMatch(c -> c.containsKey("col_key")),
            "列定义应为 snake_case col_key");
        // 核价侧（若该单绑核价模板）
        assertNotNull(dto.costingExcelColumns, "核价有效列字段应存在（可空 list 但非 null）");
    }
}
```
> 注：`REPLACE_WITH_FIXTURE` 在 Step 2 之前用现有数据库选一条满足条件的报价单 id 填入（`SELECT id FROM quotation WHERE status<>'DRAFT' AND customer_template_id IS NOT NULL AND costing_card_template_id IS NOT NULL LIMIT 1;`）。

- [ ] **Step 2: 跑测试确认失败**

Run（worktree 内）：`cd cpq-backend && ./mvnw -q test -Dtest=QuotationDetailColumnsTest`
Expected: FAIL —— `dto.quoteExcelColumns` 字段不存在（编译错）或为 null。

- [ ] **Step 3: DTO 加字段**

`QuotationDTO.java` 顶层字段区（紧邻 `quoteExcelStructure` 等结构字段）加：
```java
    /** 详情页只读 Excel/比对所需：带 display_format 的有效列（snake_case），零值计算。 */
    public java.util.List<java.util.Map<String, Object>> quoteExcelColumns;
    public java.util.List<java.util.Map<String, Object>> costingExcelColumns;
```

- [ ] **Step 4: getById 填充有效列**

`QuotationService.java`：在 `getById` 的 `populateViewStructures(dto, id);`（`:156`）下一行加：
```java
        // 详情页只读 Excel/比对：捎回带 display_format 的有效列（零值计算，仅列定义解析）
        populateEffectiveExcelColumns(dto, q);
```
并新增方法（紧邻 `populateViewStructures`）：
```java
    @jakarta.inject.Inject
    com.cpq.quotation.service.ExcelViewService excelViewService;

    /** 用 getEffectiveColumns 取报价/核价两侧有效列（含 display_format），随 getById 捎回。 */
    private void populateEffectiveExcelColumns(QuotationDTO dto, Quotation q) {
        try {
            if (q.customerTemplateId != null) {
                com.cpq.template.entity.Template qt = com.cpq.template.entity.Template.findById(q.customerTemplateId);
                if (qt != null) dto.quoteExcelColumns = excelViewService.getEffectiveColumns(qt);
            }
        } catch (Exception ignore) { /* 列定义缺 → null，详情页显示"暂无数据" */ }
        try {
            if (q.costingCardTemplateId != null) {
                com.cpq.template.entity.Template ct = com.cpq.template.entity.Template.findById(q.costingCardTemplateId);
                if (ct != null) dto.costingExcelColumns = excelViewService.getEffectiveColumns(ct);
            }
        } catch (Exception ignore) { /* 同上 */ }
    }
```
> 实现期核验：`Template` 实体的真实包路径（用 codegraph `codegraph_search Template`）；`getEffectiveColumns` 入参类型（应为 `Template`，见 `ExcelViewService.java:67`）。

- [ ] **Step 5: 跑测试确认通过**

Run：`cd cpq-backend && ./mvnw -q test -Dtest=QuotationDetailColumnsTest`
Expected: PASS。

- [ ] **Step 6: 触发 Quarkus 重启 + 端点自检**

Run：`touch cpq-backend/src/main/java/com/cpq/quotation/service/QuotationService.java && sleep 7 && curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/q/health`
Expected: 200。再 `curl -s "http://localhost:8081/api/cpq/quotations/<SUBMITTED_ID>" | python3 -c "import sys,json;d=json.load(sys.stdin)['data'];print('quoteExcelColumns', len(d.get('quoteExcelColumns') or []));print('costingExcelColumns', len(d.get('costingExcelColumns') or []))"`（带 auth 头则用 401 也可，关键不 500）。

- [ ] **Step 7: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/quotation/dto/QuotationDTO.java \
        cpq-backend/src/main/java/com/cpq/quotation/service/QuotationService.java \
        cpq-backend/src/test/java/com/cpq/quotation/QuotationDetailColumnsTest.java
git commit -m "feat(backend): getById 捎回带 display_format 的报价/核价有效 Excel 列（详情页只读用）"
```

---

## Task 2: 后端 —— submit 冻结四值守卫测试（仅测试，逻辑已存在）

**Files:**
- Test: `cpq-backend/src/test/java/com/cpq/quotation/QuotationSubmitFreezeTest.java`（新）

**背景（已核验）：** submit 内 `QuotationResource.java:365` 已调 `ensureExcelValues`，覆盖 `quoteExcelValues` + `costingExcelValues`（`CardSnapshotService.java:607-616`，`== null` 才算）。本任务只加回归守卫，防未来回退。

- [ ] **Step 1: 写测试**

```java
package com.cpq.quotation;

import io.quarkus.test.junit.QuarkusTest;
import io.quarkus.test.TestTransaction;
import jakarta.inject.Inject;
import org.junit.jupiter.api.Test;
import java.util.UUID;
import static org.junit.jupiter.api.Assertions.*;

@QuarkusTest
class QuotationSubmitFreezeTest {

    @Inject com.cpq.quotation.service.CardSnapshotService cardSnapshotService;

    private static final UUID DRAFT_WITH_BOTH_TEMPLATES =
        UUID.fromString("REPLACE_WITH_DRAFT_FIXTURE"); // 实现期填：DRAFT + 双模板 + 已加产品

    @Test
    @TestTransaction
    void ensureExcelValues_populates_both_sides() {
        int n = cardSnapshotService.ensureExcelValues(DRAFT_WITH_BOTH_TEMPLATES);
        // 再读行项，四值列非 null
        var lines = com.cpq.quotation.entity.QuotationLineItem
            .<com.cpq.quotation.entity.QuotationLineItem>list("quotationId", DRAFT_WITH_BOTH_TEMPLATES);
        assertFalse(lines.isEmpty(), "夹具应有行项");
        for (var li : lines) {
            assertNotNull(li.quoteExcelValues, "报价 Excel 值应已落库");
            assertNotNull(li.costingExcelValues, "核价 Excel 值应已落库");
        }
    }
}
```
> 实现期填 `REPLACE_WITH_DRAFT_FIXTURE`（`SELECT id FROM quotation WHERE status='DRAFT' AND customer_template_id IS NOT NULL AND costing_card_template_id IS NOT NULL LIMIT 1;`，并确认其有行项）。

- [ ] **Step 2: 跑测试**

Run：`cd cpq-backend && ./mvnw -q test -Dtest=QuotationSubmitFreezeTest`
Expected: PASS（逻辑已存在）。若 FAIL（核价无 Excel 值），说明夹具核价模板无 Excel 列定义 → 换夹具或在测试注释标注该单不绑核价 Excel。

- [ ] **Step 3: Commit**

```bash
git add cpq-backend/src/test/java/com/cpq/quotation/QuotationSubmitFreezeTest.java
git commit -m "test(backend): submit ensureExcelValues 两侧四值冻结守卫"
```

---

## Task 3: 前端 —— 抽共享 Excel 单元格格式化（excelCellFormat）

**Files:**
- Create: `cpq-frontend/src/pages/quotation/excelCellFormat.tsx`
- Create: `cpq-frontend/src/pages/quotation/excelCellFormat.test.ts`
- Modify: `cpq-frontend/src/pages/quotation/LinkedExcelView.tsx`（`:55-82` 区，改为 import 共享）

**背景（已核验，`LinkedExcelView.tsx:61-82`）：** 现有 `isComputedExcelColumn` + `renderCellValue(val, col: CostingTemplateColumn)` 用 `formatNumber`（来自 `../../utils/...` 或同文件）。抽出后**行为不变**，编辑态与只读共用。

- [ ] **Step 1: 写失败测试**

`excelCellFormat.test.ts`：
```ts
import { describe, it, expect } from 'vitest';
import { renderCellValue, isComputedExcelColumn } from './excelCellFormat';
import type { CostingTemplateColumn } from '../../services/costingTemplateService';

const col = (over: Partial<CostingTemplateColumn> = {}): CostingTemplateColumn =>
  ({ col_key: 'c', title: 'C', source_type: 'COMPONENT_FIELD', ...over } as CostingTemplateColumn);

describe('excelCellFormat', () => {
  it('空值渲染占位 —', () => {
    // renderCellValue 返回 ReactNode；这里断言它对空值不抛、返回真值
    expect(renderCellValue(null, col())).toBeTruthy();
    expect(renderCellValue('', col())).toBeTruthy();
  });
  it('计算列识别', () => {
    expect(isComputedExcelColumn('EXCEL_FORMULA')).toBe(true);
    expect(isComputedExcelColumn('CARD_FORMULA')).toBe(true);
    expect(isComputedExcelColumn('COMPONENT_FIELD')).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run：`cd cpq-frontend && npx vitest run src/pages/quotation/excelCellFormat.test.ts`
Expected: FAIL —— 模块不存在。

- [ ] **Step 3: 抽出共享模块**

读 `LinkedExcelView.tsx:55-82` 当前实现，把 `isComputedExcelColumn`、`renderCellValue`、以及它依赖的 `formatNumber` import，原样迁到 `excelCellFormat.tsx`：
```tsx
import React from 'react';
import { formatNumber } from '../../utils/unitConversion'; // ← 实现期核对 formatNumber 真实出处（grep formatNumber import in LinkedExcelView.tsx）
import type { CostingTemplateColumn } from '../../services/costingTemplateService';

export function isComputedExcelColumn(sourceType: string | undefined): boolean {
  return ['FORMULA', 'CARD_FORMULA', 'TAB_JOIN_FORMULA', 'EXCEL_FORMULA'].includes(sourceType ?? '');
}

export function renderCellValue(val: any, col: CostingTemplateColumn): React.ReactNode {
  // ← 原样从 LinkedExcelView.tsx:64-82 迁入，不改逻辑
}
```
> 必须**逐字迁移**，保持 PERCENT / decimals / computed 灰底 / "—" 占位行为不变。

- [ ] **Step 4: LinkedExcelView 改 import**

`LinkedExcelView.tsx`：删除本地 `isComputedExcelColumn`/`renderCellValue` 定义，改为：
```tsx
import { renderCellValue, isComputedExcelColumn } from './excelCellFormat';
```
（若 `formatNumber` 在 LinkedExcelView 已不再直接用，移除其孤儿 import。）

- [ ] **Step 5: 跑测试 + tsc + Vite 自检**

Run：
```bash
cd cpq-frontend && npx vitest run src/pages/quotation/excelCellFormat.test.ts \
  && npx tsc --noEmit -p tsconfig.json \
  && curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/LinkedExcelView.tsx
```
Expected: vitest PASS；tsc 0 错误；Vite 200。

- [ ] **Step 6: Commit**

```bash
git add cpq-frontend/src/pages/quotation/excelCellFormat.tsx \
        cpq-frontend/src/pages/quotation/excelCellFormat.test.ts \
        cpq-frontend/src/pages/quotation/LinkedExcelView.tsx
git commit -m "refactor(excel): 抽共享 renderCellValue/formatNumber 至 excelCellFormat（编辑态与只读共用）"
```

---

## Task 4: 前端 —— ReadonlyExcelView（复用 useExcelSnapshotRows + 有效列 + 核价树）

**Files:**
- Create: `cpq-frontend/src/pages/quotation/ReadonlyExcelView.tsx`
- Modify: `cpq-frontend/src/services/quotationService.ts`（补 `ExcelEffectiveColumn` 类型）

**背景（已核验）：** `useExcelSnapshotRows({lineItems, side, parsedColumns})`（`useExcelSnapshotRows.ts:23`）已解析 `*ExcelValues`、铺平、识别核价树（`__nodeId/__hfPartNo/__parentNo/__lvl`），返回 `{rows: LinkedExcelRow[]}`。`parsedColumns` 用 getById 的 `quoteExcelColumns/costingExcelColumns`（snake_case + display_format，结构同 `CostingTemplateColumn`）。

- [ ] **Step 1: quotationService.ts 补类型**

在 `ExcelStructure` 附近加：
```ts
/** getById 捎回的有效 Excel 列（snake_case + display_format），可直接当 CostingTemplateColumn 用。 */
export type ExcelEffectiveColumn = import('../../services/costingTemplateService').CostingTemplateColumn;
```
> 实现期核对 `costingTemplateService.ts` 的 `CostingTemplateColumn` 字段（`:3-19`：`col_key`/`title`/`source_type`/`display_format`/`comparison_tag` 等）。

- [ ] **Step 2: 写 ReadonlyExcelView**

```tsx
/**
 * ReadonlyExcelView —— 详情页只读 Excel 视图。纯读快照：
 * 行值来自 useExcelSnapshotRows(quoteExcelValues/costingExcelValues)，列定义来自 getById 捎回的有效列。
 * 零后端值计算、零 ensure。核价侧为 BOM 树（按 __lvl 缩进）。
 */
import React from 'react';
import { Empty, Table } from 'antd';
import { useExcelSnapshotRows } from './useExcelSnapshotRows';
import { renderCellValue } from './excelCellFormat';
import type { CostingTemplateColumn } from '../../services/costingTemplateService';
import type { LineItem } from './QuotationStep2';

interface Props {
  lineItems: LineItem[];
  side: 'QUOTE' | 'COSTING';
  columns: CostingTemplateColumn[] | null | undefined;
}

const ReadonlyExcelView: React.FC<Props> = ({ lineItems, side, columns }) => {
  const parsedColumns = (columns ?? []).filter(c => !(c as any).hidden);
  const { rows } = useExcelSnapshotRows({ lineItems, side, parsedColumns });

  // 任一侧快照全空（草稿/旧单）→ 暂无数据
  const hasAnyValue = rows.some(r => !r.__noData);
  if (!parsedColumns.length || !hasAnyValue) {
    return <Empty description="暂无数据" style={{ padding: 48 }} />;
  }

  const tableColumns = [
    {
      title: '产品/节点', dataIndex: '__label', key: '__label', fixed: 'left' as const, width: 220,
      render: (v: any, rec: any) => (
        <span style={{ paddingLeft: (rec.__lvl ?? 0) * 16, fontFamily: 'monospace' }}>{v}</span>
      ),
    },
    ...parsedColumns.map(col => ({
      title: col.title || col.col_key,
      dataIndex: col.col_key,
      key: col.col_key,
      render: (v: any) => renderCellValue(v, col),
    })),
  ];

  return (
    <Table
      rowKey="__key" size="small" bordered
      columns={tableColumns} dataSource={rows}
      pagination={false} scroll={{ x: 'max-content' }}
    />
  );
};

export default ReadonlyExcelView;
```
> 树形布局：核价侧 `useExcelSnapshotRows` 已逐 BOM 节点出行并带 `__lvl/__parentNo`，故按 `__lvl` 缩进即得树观感（无需自建 expandable 树，与编辑态平铺一致）。实现期对照编辑态核价 Excel 渲染（`LinkedExcelView` COSTING 分支）确认列顺序/缩进口径一致。

- [ ] **Step 3: tsc + Vite 自检**

Run：
```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json \
  && curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/ReadonlyExcelView.tsx
```
Expected: tsc 0 错误；Vite 200。

- [ ] **Step 4: Commit**

```bash
git add cpq-frontend/src/pages/quotation/ReadonlyExcelView.tsx \
        cpq-frontend/src/services/quotationService.ts
git commit -m "feat(detail): ReadonlyExcelView —— 只读 Excel 视图复用 useExcelSnapshotRows + 有效列（含核价树）"
```

---

## Task 5: 前端 —— 抽比对表组装 + ReadonlyComparison

**Files:**
- Create: `cpq-frontend/src/pages/quotation/comparisonTable.tsx`（抽 `ComparisonView` 的列/行组装）
- Modify: `cpq-frontend/src/pages/quotation/ComparisonView.tsx`（改用共享组装）
- Create: `cpq-frontend/src/pages/quotation/ReadonlyComparison.tsx`

**背景（已核验，`ComparisonView.tsx`）：** `buildComparisonModel(quoteCols, quoteRows, costingCols, costingRows, tagMetas)`（`comparisonModel.ts:91`）纯函数。`ComparisonView` 把 model → AntD `columns`/`dataSource`（`:66-115`）。把这段组装 + 渲染抽成共享 `ComparisonTable`，编辑态与只读都喂 model。

- [ ] **Step 1: 抽 comparisonTable.tsx**

把 `ComparisonView.tsx:66-164` 的 `dataSource`/`columns`/`<Table>` + "仅看差异"开关 + 导出按钮，抽成：
```tsx
import React, { useMemo, useState } from 'react';
import { Table, Tag, Button, Switch, Space, message } from 'antd';
import { DownloadOutlined } from '@ant-design/icons';
import type { ComparisonModel } from './comparisonModel';
import { comparisonExportService } from '../../services/comparisonExportService';

const HIGHLIGHT_BG = '#fff1f0';

export const ComparisonTable: React.FC<{ model: ComparisonModel; quotationId: string }> = ({ model, quotationId }) => {
  const [onlyDiff, setOnlyDiff] = useState(false);
  const [exporting, setExporting] = useState(false);
  // ← 原样迁入 ComparisonView 的 visibleRows/dataSource/columns/handleExport/<Table>（不改逻辑）
  return (/* … 原 ComparisonView return 的 <div>…</div> … */);
};
```
> 逐字迁移，保持 rowSpan 料号合并、HIGHLIGHT_BG、"仅看差异"、导出行为不变。

- [ ] **Step 2: ComparisonView 改用共享**

`ComparisonView.tsx`：保留 `useLinkedExcelRows` 取数 + `buildComparisonModel`，把渲染换成 `<ComparisonTable model={model} quotationId={quotationId} />`。

- [ ] **Step 3: 写 ReadonlyComparison**

```tsx
/**
 * ReadonlyComparison —— 详情页只读比对。零计算：吃两份已冻结 Excel 快照行值 + tag → buildComparisonModel。
 * 不走 useLinkedExcelRows（那会实时求值）。任一侧快照空 → 暂无数据。
 */
import React, { useEffect, useMemo, useState } from 'react';
import { Empty } from 'antd';
import { useExcelSnapshotRows } from './useExcelSnapshotRows';
import { buildComparisonModel } from './comparisonModel';
import type { TagMeta } from './comparisonModel';
import { ComparisonTable } from './comparisonTable';
import { comparisonTagService } from '../../services/comparisonTagService';
import type { CostingTemplateColumn } from '../../services/costingTemplateService';
import type { LineItem } from './QuotationStep2';

interface Props {
  quotationId: string;
  lineItems: LineItem[];
  quoteColumns: CostingTemplateColumn[] | null | undefined;
  costingColumns: CostingTemplateColumn[] | null | undefined;
}

const ReadonlyComparison: React.FC<Props> = ({ quotationId, lineItems, quoteColumns, costingColumns }) => {
  const [tagMetas, setTagMetas] = useState<TagMeta[]>([]);
  useEffect(() => {
    comparisonTagService.list('ACTIVE')
      .then(r => setTagMetas((r.data || []).map((t: any) => ({
        code: t.code, label: t.label, groupName: t.groupName,
        groupSortOrder: t.groupSortOrder, tagSortOrder: t.tagSortOrder,
      }))))
      .catch(() => setTagMetas([]));
  }, []);

  const quoteCols = (quoteColumns ?? []);
  const costingCols = (costingColumns ?? []);
  const { rows: quoteRows } = useExcelSnapshotRows({ lineItems, side: 'QUOTE', parsedColumns: quoteCols });
  const { rows: costingRows } = useExcelSnapshotRows({ lineItems, side: 'COSTING', parsedColumns: costingCols });

  const bothHaveSnapshot = quoteRows.some(r => !r.__noData) && costingRows.some(r => !r.__noData);

  const model = useMemo(
    () => buildComparisonModel(quoteCols, quoteRows, costingCols, costingRows, tagMetas),
    [quoteCols, quoteRows, costingCols, costingRows, tagMetas],
  );

  if (!bothHaveSnapshot || model.columns.length === 0) {
    return <Empty description="暂无数据" style={{ padding: 48 }} />;
  }
  return <ComparisonTable model={model} quotationId={quotationId} />;
};

export default ReadonlyComparison;
```
> 实现期核对 `buildComparisonModel` 入参顺序/类型（`comparisonModel.ts:91`）与 `TagMeta` 字段。

- [ ] **Step 4: tsc + Vite 自检**

Run：
```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json \
  && for f in comparisonTable ComparisonView ReadonlyComparison; do \
       curl -s -o /dev/null -w "$f %{http_code}\n" http://localhost:5174/src/pages/quotation/$f.tsx; done
```
Expected: tsc 0 错误；三个文件 Vite 200。

- [ ] **Step 5: Commit**

```bash
git add cpq-frontend/src/pages/quotation/comparisonTable.tsx \
        cpq-frontend/src/pages/quotation/ComparisonView.tsx \
        cpq-frontend/src/pages/quotation/ReadonlyComparison.tsx
git commit -m "feat(detail): ReadonlyComparison + 抽 comparisonTable 共享组装（只读比对吃冻结快照，零计算）"
```

---

## Task 6: 前端 —— ReadonlyProductCard 加结构驱动 COSTING 路径

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/ReadonlyProductCard.tsx`（`:44-57` props、`:161-256` 数据装配）

**背景（已核验）：** 现有 QUOTE 路径 = enrich `lineItem.componentData`(报价模板) + `useSnap` 读 `quoteCardValues` + `buildSnapshotExpansions(...,'QUOTE',...)`。核价侧**无核价 componentData**，需结构驱动：`buildComponentDataFromStructure(costingCardStructure)`（`enrichComponentData.ts`，`QuotationWizard.tsx:29/426` 在用）+ 读 `costingCardValues` + `buildSnapshotExpansions(...,'COSTING',...)`。1:1 参照编辑态 `QuotationStep2.tsx:3070`(useSnapCosting)/`:3324`/`:3370`(side=COSTING + costingCardStructure)。

- [ ] **Step 1: props 扩展**

`ReadonlyProductCardProps` 加：
```tsx
  /** 视图侧：缺省 'QUOTE'（报价卡片，现状）；'COSTING' 走核价结构驱动快照 */
  side?: 'QUOTE' | 'COSTING';
  /** 核价卡片结构快照（顶层，提供 rowKeyFields + tabs 结构，用于结构驱动组装 componentData） */
  costingCardStructure?: CardStructure | null;
```

- [ ] **Step 2: 数据源按 side 分流**

读 `ReadonlyProductCard.tsx:161-256` 现状，把"取 components / 取 cardValues / driver side / structure"四处按 `side` 分流（不破坏 QUOTE 现状）：
```tsx
const side = props.side ?? 'QUOTE';
const isCosting = side === 'COSTING';

// 1) components：QUOTE = 现状 enrich(lineItem.componentData)；COSTING = buildComponentDataFromStructure(costingCardStructure)
//    （参照 QuotationWizard.tsx:426 用法；buildComponentDataFromStructure 来自 ./enrichComponentData）

// 2) cardValues：QUOTE = lineItem.quoteCardValues；COSTING = lineItem.costingCardValues
const cardValuesJson = isCosting ? lineItem.costingCardValues : lineItem.quoteCardValues;
const useSnap = !!cardValuesJson && components.length > 0;

// 3) structure：QUOTE = quoteCardStructure；COSTING = costingCardStructure
const activeStructure = isCosting ? props.costingCardStructure : props.quoteCardStructure;

// 4) buildSnapshotExpansions(..., isCosting ? 'COSTING' : 'QUOTE', ...)
```
> 注意 AP-50/AP-54：只读无受控 input，不涉 AP-54；但渲染分支按 `cardValuesJson`/`activeStructure` 全程用 side 变量，避免 QUOTE/COSTING 串源。无快照回退实时仅作兜底（详情页严格期望读快照）。

- [ ] **Step 3: tsc + Vite 自检**

Run：
```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json \
  && curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/ReadonlyProductCard.tsx
```
Expected: tsc 0 错误；Vite 200。

- [ ] **Step 4: Commit**

```bash
git add cpq-frontend/src/pages/quotation/ReadonlyProductCard.tsx
git commit -m "feat(detail): ReadonlyProductCard 加结构驱动 COSTING 路径（读 costingCardValues，移植编辑态核价快照渲染）"
```

---

## Task 7: 前端 —— QuotationDetail 两级 Segmented 容器 + 空态 + 线程化

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/QuotationDetail.tsx`（`:438-472` "产品明细" 卡片区）

**背景（已核验）：** 现"产品明细"渲染 `ReadonlyProductCard` 列表（`:446-457`），过滤 `compositeType !== 'PART'`（`:441`）。getById 现已带 `quotation.quoteCardStructure`/`costingCardStructure`/`quoteExcelColumns`/`costingExcelColumns`/各 lineItem 四值。

- [ ] **Step 1: 加视图状态**

`QuotationDetail` 组件内加：
```tsx
const [mainTab, setMainTab] = useState<'quote' | 'costing' | 'comparison'>('quote');
const [viewType, setViewType] = useState<'card' | 'excel'>('card');
```
import：`Segmented`（已从 antd 导入则复用）；`ReadonlyExcelView`/`ReadonlyComparison`。

- [ ] **Step 2: 替换"产品明细"卡片内容**

把 `:439-460`「Card title="产品明细" {…ReadonlyProductCard 列表…}」改为「两级 Segmented + 按选择渲染」：
```tsx
<Card title="产品明细" style={{ marginBottom: 16 }}>
  <div style={{ display: 'flex', justifyContent: 'space-between', flexWrap: 'wrap', gap: 12, marginBottom: 12 }}>
    <Segmented
      size="small"
      options={[
        { label: '📝 报价单', value: 'quote' },
        { label: '📊 核价单', value: 'costing' },
        { label: '📈 比对视图', value: 'comparison' },
      ]}
      value={mainTab}
      onChange={v => setMainTab(v as any)}
    />
    {mainTab !== 'comparison' && (
      <Segmented
        size="small"
        options={[
          { label: '📋 产品卡片', value: 'card' },
          { label: '📑 Excel 视图', value: 'excel' },
        ]}
        value={viewType}
        onChange={v => setViewType(v as any)}
      />
    )}
  </div>
  {(() => {
    const visible = (quotation.lineItems || []).filter((li: any) => li.compositeType !== 'PART');
    if (visible.length === 0) return <div style={{ textAlign: 'center', padding: 32, color: '#999' }}>暂无产品</div>;

    // 比对
    if (mainTab === 'comparison') {
      return (
        <ReadonlyComparison
          quotationId={quotation.id}
          lineItems={visible}
          quoteColumns={quotation.quoteExcelColumns}
          costingColumns={quotation.costingExcelColumns}
        />
      );
    }
    // Excel
    if (viewType === 'excel') {
      return (
        <ReadonlyExcelView
          lineItems={visible}
          side={mainTab === 'costing' ? 'COSTING' : 'QUOTE'}
          columns={mainTab === 'costing' ? quotation.costingExcelColumns : quotation.quoteExcelColumns}
        />
      );
    }
    // 卡片（报价/核价）
    return (
      <div className="qt-products-list">
        {visible.map((li: any, idx: number) => (
          <ReadonlyProductCard
            key={li.id || idx}
            lineItem={li}
            index={idx}
            quotationId={quotation.id}
            quotationStatus={quotation.status}
            customerId={quotation.customerId}
            globalVariableDefs={gvDefs}
            side={mainTab === 'costing' ? 'COSTING' : 'QUOTE'}
            quoteCardStructure={quotation.quoteCardStructure ?? null}
            costingCardStructure={quotation.costingCardStructure ?? null}
          />
        ))}
      </div>
    );
  })()}
</Card>
```
> 合计行（`:461-471`）保持在 Card 内 Segmented 区之后、或移到 Card 外——实现期保持原视觉（建议合计行仅"报价卡片"视图显示，其余视图它无意义；用 `{mainTab==='quote' && viewType==='card' && (<合计行/>)}` 包裹）。

- [ ] **Step 3: tsc + Vite 自检**

Run：
```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json \
  && curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/QuotationDetail.tsx \
  && curl -s -o /dev/null -w "/quotations %{http_code}\n" http://localhost:5174/
```
Expected: tsc 0 错误；Vite 200。

- [ ] **Step 4: Commit**

```bash
git add cpq-frontend/src/pages/quotation/QuotationDetail.tsx
git commit -m "feat(detail): 产品明细区两级视图切换（报价/核价/比对 × 卡片/Excel），只读读快照 + 空态"
```

---

## Task 8: E2E —— 详情页 5 视图只读渲染 + 编辑态防回归

**Files:**
- Create: `cpq-frontend/e2e/quotation-detail-readonly-views.spec.ts`
- 复跑：`cpq-frontend/e2e/quotation-flow.spec.ts` + `composite-product-flow.spec.ts`

**背景：** 选一条 **SUBMITTED+**、报价+核价模板齐备、已落四值的报价单（详情页 url `/quotations/{id}`）。参照 `docs/E2E测试方法.md` 选择器约定 + UTF-8 编码。

- [ ] **Step 1: 写 E2E spec**

```ts
import { test, expect } from '@playwright/test';

const DETAIL_URL = '/quotations/REPLACE_WITH_SUBMITTED_ID'; // 实现期填 SUBMITTED + 双模板齐备

test('详情页 5 视图只读渲染、零 batch-expand、零加载中', async ({ page }) => {
  const batchExpandCalls: string[] = [];
  page.on('request', r => { if (r.url().includes('/batch-expand')) batchExpandCalls.push(r.url()); });

  await page.goto(DETAIL_URL);
  await expect(page.getByText('产品明细')).toBeVisible();

  // 报价·卡片（默认）
  await expect(page.getByText('📝 报价单')).toBeVisible();
  // 报价·Excel
  await page.getByText('📑 Excel 视图').click();
  await expect(page.locator('table')).toBeVisible();
  // 核价·卡片
  await page.getByText('📊 核价单').click();
  await page.getByText('📋 产品卡片').click();
  // 核价·Excel
  await page.getByText('📑 Excel 视图').click();
  await expect(page.locator('table')).toBeVisible();
  // 比对
  await page.getByText('📈 比对视图').click();
  await expect(page.locator('table')).toBeVisible();

  // 断言：全程零 batch-expand
  expect(batchExpandCalls, `batch-expand 应为 0，实得 ${batchExpandCalls.length}`).toHaveLength(0);
  // 断言：无"加载中"/"__loading__"残留
  await expect(page.getByText('加载中')).toHaveCount(0);
  await expect(page.getByText('__loading__')).toHaveCount(0);
});
```
> 选择器以实现期实际 DOM 为准（Segmented label 文案）。若需更稳，给 Segmented 加 `data-testid`。

- [ ] **Step 2: 跑详情页 E2E**

Run：
```bash
cd cpq-frontend && npx playwright test --config=e2e/playwright.config.ts e2e/quotation-detail-readonly-views.spec.ts --reporter=list
```
Expected: 1 passed；batch-expand=0；加载中=0。

- [ ] **Step 3: 跑编辑态防回归（强制双 spec）**

Run：
```bash
cd cpq-frontend && npx playwright test --config=e2e/playwright.config.ts \
  e2e/quotation-flow.spec.ts e2e/composite-product-flow.spec.ts --reporter=list
```
Expected: 全 passed；`'加载中' final count = 0`。（验证 Task3/Task5 抽共享未破坏编辑态 `LinkedExcelView`/`ComparisonView`。）
> 若 `composite-product-flow.spec.ts` 因预存夹具损坏失败（记忆已知技术债），记录为预存问题、不阻断，但 `quotation-flow.spec.ts` 必 pass。

- [ ] **Step 4: 截图证据 + Commit**

收集详情页 5 视图截图（提交后）+ 草稿态"暂无数据"截图，放 `e2e/screenshots/`。
```bash
git add cpq-frontend/e2e/quotation-detail-readonly-views.spec.ts cpq-frontend/e2e/screenshots/*.png
git commit -m "test(e2e): 详情页 5 视图只读渲染 + batch-expand=0 + 编辑态防回归"
```

---

## Task 9: 收尾自检 + 文档回写

**Files:**
- Modify: `docs/RECORD.md`（追加开发记录）
- 可选：`docs/反模式.md`（若发现新坑）

- [ ] **Step 1: 全量自检声明**

逐条确认并写出"已自检"声明：
- `cd cpq-backend && ./mvnw -q test -Dtest='QuotationDetailColumnsTest,QuotationSubmitFreezeTest'` → PASS
- `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json` → 0 错误
- `npx vitest run src/pages/quotation/excelCellFormat.test.ts` → PASS
- 详情页 E2E → 1 passed / batch-expand=0 / 加载中=0
- 编辑态 `quotation-flow.spec.ts` → passed（防回归）

- [ ] **Step 2: RECORD.md 追加**

格式：`[2026-06-27] 报价单详情页 - 加报价/核价/比对×卡片/Excel 只读快照视图切换 | QuotationDetail/ReadonlyProductCard/ReadonlyExcelView/ReadonlyComparison/QuotationService.getById | 零计算读快照、唯一后端改动=getById捎回有效列、停止跨引擎对账`

- [ ] **Step 3: Commit**

```bash
git add docs/RECORD.md
git commit -m "docs(record): 报价单详情页只读快照视图切换 开发记录"
```

---

## 自检（plan 对 spec 覆盖）

- spec §6 B1（getById 捎回有效列）→ Task 1 ✅
- spec §6 B2（submit 冻结守卫）→ Task 2 ✅
- spec §4/§7 F5（抽共享格式化，反分叉）→ Task 3（Excel 格式）+ Task 5（比对组装）✅
- spec §7 F3（ReadonlyExcelView 复用 useExcelSnapshotRows + 核价树）→ Task 4 ✅
- spec §7 F4（ReadonlyComparison 零计算派生）→ Task 5 ✅
- spec §7 F2（ReadonlyProductCard 结构驱动 COSTING）→ Task 6 ✅
- spec §7 F1（QuotationDetail 两级切换 + 空态）→ Task 7 ✅
- spec §10（5 视图 E2E + batch-expand=0 + 编辑态防回归 + 与导出对账口径）→ Task 8 ✅
- spec §8 行为矩阵（草稿空态 / PART 核验）→ Task 7 空态 + Task 8 截图；PART 核验在 Task 6 Step2 注 + Task 8 ✅

**类型一致性**：`CostingTemplateColumn`（snake_case `col_key`）贯穿 Task1(后端 getEffectiveColumns 产出)→Task4/5(前端 columns prop)→useExcelSnapshotRows(`parsedColumns`)；`side: 'QUOTE'|'COSTING'` 贯穿 Task4/6/7。`quoteExcelColumns`/`costingExcelColumns` 字段名贯穿 Task1(DTO)→Task7(读 `quotation.*`)。

**占位扫描**：仅余 `REPLACE_WITH_*` 夹具 id（实现期第一步用 SQL 选定，已给查询语句），非逻辑占位。
