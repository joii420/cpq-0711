# 公式可视化构建器 P1.2 — CardFormulaDrawer 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Excel 列「卡片公式」抽屉 `CardFormulaDrawer` 升级为更友好的引导式构建器——简单/高级分层 + 友好操作标签 + 简单模式降术语(隐藏 JEXL/别名 token、隐藏高级聚合)+ 简单模式 aggExpr 点选 chip 构建器,让不会写语法的用户也能配出 Excel 列的取值/查找/求和公式。

**Architecture:** 纯前端 UX 改造,**底层 `{formula, refs}` token 输出零变更**(向后兼容,无 AP-44 协议传播)。

> **关键模型差异(与 P1 CrossTabRefDrawer 不同)**:CardFormulaDrawer 的 `formula` 文本框是**始终常驻的组装画布**(用户在此拼整条公式,插入面板把 ref token 追加进去),不是 P1 那种"隐藏的高级原始文本逃生口"。因此:① P1 的"双模式原始文本"在此 **moot**(原始文本本就常驻);② 简单模式**不能隐藏 formula 文本框**(那是画布),P1.2 的"简单"= 把**插入面板**做友好 + 降术语,formula 画布保留。CardFormulaDrawer 已具备的条件构建器(含动态 RHS=SUMIF)、试算预览、row_where 编辑回填、显示格式、内联帮助一律保留不动。

**Tech Stack:** React + TypeScript(Vite)+ Ant Design(Segmented/Select/Radio/Input)+ vitest(纯函数单测)+ Playwright(E2E)。

**已确认事实(实现依据,来自 CardFormulaDrawer 测绘):**
- 文件 `cpq-frontend/src/pages/template/CardFormulaDrawer.tsx`(1013 行);模型/helper `cpq-frontend/src/pages/template/cardFormula.ts`。
- `RefType = 'subtotal' | 'first_row' | 'row_where' | 'aggregate'`(L58),现状用户标签(Radio.Button,约 L744-748):`页签小计 / 字段·首行 / 字段·按条件取行 / 聚合（SUM/AVG…）`。
- `AggFunc = 'SUM'|'AVG'|'COUNT'|'MIN'|'MAX'`(L60),`aggFunc` Select(约 L253-259);`aggExpr` 自由文本 Input(用户敲中文字段名表达式如 `单价*数量`,插入时 `buildAliasMap`+`replaceFieldsWithAlias` 转 c0/c1 别名)。
- 条件构建器 `conds: CondRow[]`(L289),`needCondBuilder = refType==='row_where' || refType==='aggregate'`(约 L536),被 row_where 与 aggregate 共用;支持 literal/product/column 三种 RHS;有 JEXL/动态条件预览。
- `formula` 文本框 `Input.TextArea`(约 L581,`value={formula}`)始终可见;插入面板在光标处追加 `[placeholder]`;`refs` map 累积。
- 抽屉标题(L540):`编辑卡片公式 — 列 ${col_key}（${title}）`,无 Segmented。
- `buildInsertResult`(L162-238,定义在组件文件内,纯函数无副作用)产出 `{placeholder, refKey, ref}`;`onSave({formula, refs, display_format})`。
- 唯一调用点:`cpq-frontend/src/pages/template/ExcelViewConfigTab.tsx`(约 L558-567)。
- 试算预览(`dryRunQuotationId` 提供时):POST `dryRunExcelView` 显示每行计算值;CrossTabRefDrawer 没有此特性。
- token 不变量:`CardRefSpec` 形状、`[placeholder]` 方括号语法、`SUM_OVER(...)` 聚合调用语法 —— 全在 `cardFormula.ts` 类型 + `buildInsertResult` 内;P1.2 不改它们 → 输出不变。

**数据约定(贯穿全计划):**
- 「操作」是面向用户的业务措辞,映射到内部 `RefType`(+ aggregate 的 `AggFunc`):`引用页签小计=subtotal / 取某字段值=first_row / 按条件查找取值=row_where / 按条件求和=aggregate+SUM / 求平均·计数·最大·最小=aggregate+AVG·COUNT·MAX·MIN`。
- 简单模式只露 `simple:true` 操作(小计 / 取值 / 查找 / 求和)+ 隐藏高级聚合(AVG/COUNT/MAX/MIN)+ 隐藏 JEXL/别名 token 预览 + aggExpr 用 chip 构建器;高级模式 100% 暴露现状全部能力(原 Radio、aggFunc 全集、aggExpr 自由文本、JEXL 预览)。
- `formula` 文本画布两模式都保留(简单模式辅助文案更友好,但不隐藏)。

**范围(P1.2 不含):** 全局变量插入(现状两抽屉都未实现,留后续);条件 IF 分支(P3 引擎函数化);后端/token 协议任何变更。

---

## Task 1: 新建 `cardFormulaOps.ts` — 友好操作 ↔ RefType/AggFunc 映射

**Files:**
- Create: `cpq-frontend/src/pages/template/cardFormulaOps.ts`
- Test: `cpq-frontend/src/pages/template/cardFormulaOps.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// cardFormulaOps.test.ts
import { describe, it, expect } from 'vitest';
import { CARD_OPERATIONS, opToRefType, refTypeToOp } from './cardFormulaOps';

describe('card operation 映射', () => {
  it('CARD_OPERATIONS: 8 项, simple 标记正确', () => {
    expect(CARD_OPERATIONS.map((o) => o.key)).toEqual(
      ['subtotal', 'lookup_first', 'lookup_where', 'sum', 'avg', 'count', 'max', 'min']
    );
    expect(CARD_OPERATIONS.filter((o) => o.simple).map((o) => o.key)).toEqual(
      ['subtotal', 'lookup_first', 'lookup_where', 'sum']
    );
  });
  it('opToRefType: 操作 key → {refType, aggFunc}', () => {
    expect(opToRefType('subtotal')).toEqual({ refType: 'subtotal', aggFunc: undefined });
    expect(opToRefType('lookup_where')).toEqual({ refType: 'row_where', aggFunc: undefined });
    expect(opToRefType('sum')).toEqual({ refType: 'aggregate', aggFunc: 'SUM' });
    expect(opToRefType('count')).toEqual({ refType: 'aggregate', aggFunc: 'COUNT' });
  });
  it('refTypeToOp: (refType, aggFunc) → 操作 key, 非聚合忽略 aggFunc', () => {
    expect(refTypeToOp('subtotal')).toBe('subtotal');
    expect(refTypeToOp('first_row')).toBe('lookup_first');
    expect(refTypeToOp('row_where')).toBe('lookup_where');
    expect(refTypeToOp('aggregate', 'SUM')).toBe('sum');
    expect(refTypeToOp('aggregate', 'MAX')).toBe('max');
    expect(refTypeToOp('aggregate')).toBe('sum'); // 缺 aggFunc 默认 sum
    expect(refTypeToOp('unknown' as any)).toBe('subtotal'); // 未知回退
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-frontend && npx vitest run src/pages/template/cardFormulaOps.test.ts`
Expected: FAIL（模块不存在）。若项目无 vitest 跳过单测改由 E2E 覆盖并 PR 注明（项目有 vitest，见 `crossTabText.test.ts`）。

- [ ] **Step 3: 实现映射**

```typescript
// cardFormulaOps.ts
// 友好操作 ↔ CardFormulaDrawer 内部 RefType/AggFunc 映射（松类型,避免跨文件 type 耦合;drawer 侧 cast）。
export interface CardOperationDef {
  key: string;
  label: string;
  refType: string;          // 'subtotal' | 'first_row' | 'row_where' | 'aggregate'
  aggFunc?: string;         // 仅 aggregate: 'SUM'|'AVG'|'COUNT'|'MAX'|'MIN'
  /** 简单模式是否露出 */
  simple: boolean;
}

export const CARD_OPERATIONS: CardOperationDef[] = [
  { key: 'subtotal',     label: '引用页签小计',   refType: 'subtotal',  simple: true },
  { key: 'lookup_first', label: '取某字段的值',   refType: 'first_row', simple: true },
  { key: 'lookup_where', label: '按条件查找取值', refType: 'row_where', simple: true },
  { key: 'sum',          label: '按条件求和',     refType: 'aggregate', aggFunc: 'SUM',   simple: true },
  { key: 'avg',          label: '求平均',         refType: 'aggregate', aggFunc: 'AVG',   simple: false },
  { key: 'count',        label: '计数',           refType: 'aggregate', aggFunc: 'COUNT', simple: false },
  { key: 'max',          label: '求最大',         refType: 'aggregate', aggFunc: 'MAX',   simple: false },
  { key: 'min',          label: '求最小',         refType: 'aggregate', aggFunc: 'MIN',   simple: false },
];

export function opToRefType(key: string): { refType: string; aggFunc: string | undefined } {
  const o = CARD_OPERATIONS.find((x) => x.key === key);
  return { refType: o?.refType ?? 'subtotal', aggFunc: o?.aggFunc };
}

export function refTypeToOp(refType: string, aggFunc?: string): string {
  if (refType === 'aggregate') {
    return CARD_OPERATIONS.find((o) => o.refType === 'aggregate' && o.aggFunc === aggFunc)?.key ?? 'sum';
  }
  return CARD_OPERATIONS.find((o) => o.refType === refType)?.key ?? 'subtotal';
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd cpq-frontend && npx vitest run src/pages/template/cardFormulaOps.test.ts`
Expected: PASS（3 用例）

- [ ] **Step 5: 提交**

```bash
git add cpq-frontend/src/pages/template/cardFormulaOps.ts cpq-frontend/src/pages/template/cardFormulaOps.test.ts
git commit -m "feat(formula-builder): cardFormulaOps friendly-op<->refType mapping (P1.2 Task 1)"
```

---

## Task 2: CardFormulaDrawer — 简单/高级 Segmented + mode 状态

**Files:**
- Modify: `cpq-frontend/src/pages/template/CardFormulaDrawer.tsx`

- [ ] **Step 1: import + mode 状态**

顶部 import 加 `Segmented`（并入已有 antd 解构）+ `import { CARD_OPERATIONS, opToRefType, refTypeToOp } from './cardFormulaOps';`（本任务先只 import `CARD_OPERATIONS, opToRefType, refTypeToOp` 中 Task 用到的；Task 3 用全部，故此处全 import 可接受，但若 lint 报未用，先只 import 本任务用到的，Task 3 再补）。

在 state 区加：
```typescript
  const [mode, setMode] = useState<'simple' | 'advanced'>('simple');
```

- [ ] **Step 2: 标题加 Segmented**

把抽屉 `title`（现 `编辑卡片公式 — 列 ...` 字符串）改为 JSX，右侧加简单/高级 Segmented：
```tsx
      title={
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 12 }}>
          <span>{`编辑卡片公式 — 列 ${value.col_key}（${value.title}）`}</span>
          <Segmented
            size="small"
            value={mode}
            onChange={(v) => setMode(v as 'simple' | 'advanced')}
            options={[{ label: '简单', value: 'simple' }, { label: '高级', value: 'advanced' }]}
          />
        </div>
      }
```

- [ ] **Step 3: 编译 + transform**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json` → 0 错误
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/template/CardFormulaDrawer.tsx` → 200

- [ ] **Step 4: 提交**

```bash
git add cpq-frontend/src/pages/template/CardFormulaDrawer.tsx
git commit -m "feat(formula-builder): CardFormulaDrawer simple/advanced Segmented (P1.2 Task 2)"
```

---

## Task 3: 友好操作选择器（简单模式替代 RefType Radio + 隐藏高级聚合）

**Files:**
- Modify: `cpq-frontend/src/pages/template/CardFormulaDrawer.tsx`

- [ ] **Step 1: 读现状 RefType Radio + aggFunc Select 区**

先 Read 插入面板里 `refType` 的 `Radio.Group`（约 L744-748）与 `aggFunc` 的 Select（约 L253-259 选项常量 + 渲染处）。

- [ ] **Step 2: 简单模式渲染友好操作 Select,驱动 refType + aggFunc**

在 RefType Radio.Group 外层按 mode 分支:`mode === 'advanced'` 渲染**现状 Radio 原样**(不动);`mode === 'simple'` 改渲染友好操作 Select:
```tsx
        {mode === 'simple' ? (
          <Select
            style={{ width: 280 }}
            value={refTypeToOp(refType, aggFunc)}
            onChange={(opKey) => {
              const { refType: rt, aggFunc: af } = opToRefType(opKey);
              setRefType(rt as RefType);
              if (af) setAggFunc(af as AggFunc);
            }}
            options={CARD_OPERATIONS.filter((o) => o.simple).map((o) => ({ label: o.label, value: o.key }))}
          />
        ) : (
          /* ……现状 RefType Radio.Group 原样…… */
        )}
```

- [ ] **Step 3: 简单模式隐藏 aggFunc Select（已由操作选择器决定）**

aggregate 分支里的 `aggFunc` Select 包 `{mode === 'advanced' && (...)}`——简单模式 aggFunc 已由操作("按条件求和"=SUM)决定,不再单独露;高级模式保留全集可改。

- [ ] **Step 4: 编译 + transform**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json` → 0 错误
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/template/CardFormulaDrawer.tsx` → 200

- [ ] **Step 5: 提交**

```bash
git add cpq-frontend/src/pages/template/CardFormulaDrawer.tsx
git commit -m "feat(formula-builder): friendly operation selector in simple mode (P1.2 Task 3)"
```

---

## Task 4: 简单模式降术语（隐藏 JEXL/别名 token 预览 + 语法提示）

**Files:**
- Modify: `cpq-frontend/src/pages/template/CardFormulaDrawer.tsx`

- [ ] **Step 1: 读现状 JEXL 预览 + 语法提示块**

先 Read:① 条件构建器的「JEXL 预览」/「动态条件预览」渲染处(约 L793-937 内);② formula 文本框下的「JEXL 算符」语法提示(约 L594);③ aggExpr 标签里「别名将自动替换」tooltip。

- [ ] **Step 2: 简单模式隐藏原始 JEXL/别名 token 展示,保留友好预览**

- 条件预览:`mode === 'advanced'` 才显示原始 JEXL 串(`c0=='镀铜' && c1>0`)与别名脚注;`mode === 'simple'` 只显示已有的「动态条件预览」结构化中文(若现状 literal 分支只有 JEXL 串无中文预览,则简单模式显示一句"满足:<字段> <算符> <值> …"的中文摘要,复用条件行已有数据拼,不新引擎)。
- formula 文本框下「JEXL 算符」语法提示行 + aggExpr「别名自动替换」tooltip:包 `{mode === 'advanced' && (...)}`,简单模式不露术语。

> 不动 formula 文本框本身(画布两模式都在);只隐藏**术语性辅助文案**。

- [ ] **Step 3: 编译 + transform**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json` → 0 错误
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/template/CardFormulaDrawer.tsx` → 200

- [ ] **Step 4: 提交**

```bash
git add cpq-frontend/src/pages/template/CardFormulaDrawer.tsx
git commit -m "feat(formula-builder): hide JEXL/alias jargon in simple mode (P1.2 Task 4)"
```

---

## Task 5: 简单模式 aggExpr 点选 chip 构建器（替代自由文本）

**Files:**
- Modify: `cpq-frontend/src/pages/template/CardFormulaDrawer.tsx`

- [ ] **Step 1: 读现状 aggExpr 自由文本 Input + selTab.fields 来源**

先 Read aggExpr `Input`(用户敲 `单价*数量`)与 `selTab`(L369 `tabs.find(...)`)的 `fields` 列表来源,确认简单模式可列出当前源页签字段供点选。

- [ ] **Step 2: 简单模式渲染 chip 构建器(点选字段/运算符/数字),拼出 aggExpr 字符串**

`mode === 'advanced'` 保留现状 aggExpr 自由文本 Input;`mode === 'simple'` 改渲染 chip 构建器:加本地 state `const [aggExprChips, setAggExprChips] = useState<string[]>([]);`(每个 chip 是一个片段字符串:字段名 / `*` `/` `+` `-` `(` `)` / 数字),点选追加 chip;chip 序列 `join(' ')` 写回 `setAggExpr(...)`(保持 aggExpr 仍是最终字符串,`buildInsertResult` 不变)。控件:
```tsx
        {mode === 'simple' ? (
          <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
            <div style={{ /* chip 回显框,复用 CrossTabRefDrawer 的样式风格 */ }}>
              {aggExprChips.length === 0 ? <span style={{ color: '#c0c4cc', fontSize: 12 }}>点选下方字段/运算符拼出要汇总的算式</span>
                : aggExprChips.map((c, i) => (<Tag key={i} closable onClose={(e) => { e.preventDefault(); setAggExprChips((p) => p.filter((_, j) => j !== i)); }}>{c}</Tag>))}
            </div>
            <Space wrap>
              <Select size="small" style={{ width: 140 }} placeholder="字段" value={undefined}
                options={(selTab?.fields ?? []).map((f: any) => ({ label: f.label || f.name, value: f.name }))}
                onChange={(v) => setAggExprChips((p) => [...p, v])} />
              {(['+', '-', '×', '÷', '(', ')'] as const).map((sym) => (
                <Button key={sym} size="small" onClick={() => setAggExprChips((p) => [...p, sym === '×' ? '*' : sym === '÷' ? '/' : sym])}>{sym}</Button>
              ))}
              {/* 数字: InputNumber + 添加按钮,同 CrossTabRefDrawer Task 7 模式 */}
            </Space>
          </div>
        ) : (
          /* ……现状 aggExpr 自由文本 Input 原样…… */
        )}
```
并加 effect 把 chips 同步进 aggExpr:`useEffect(() => { if (mode === 'simple') setAggExpr(aggExprChips.join(' ')); }, [aggExprChips, mode]);`。`open`/refType 切换或插入成功后重置 `aggExprChips`(在现有重置点补 `setAggExprChips([])`)。

> 关键:最终仍走 `aggExpr` 字符串 → `buildInsertResult` 的 `replaceFieldsWithAlias` 不变 → 产出 token 不变。chip 只是简单模式的录入方式。

- [ ] **Step 3: 编译 + transform**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json` → 0 错误
Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/template/CardFormulaDrawer.tsx` → 200

- [ ] **Step 4: 提交**

```bash
git add cpq-frontend/src/pages/template/CardFormulaDrawer.tsx
git commit -m "feat(formula-builder): simple-mode chip builder for aggExpr (P1.2 Task 5)"
```

---

## Task 6: E2E — 简单/高级 + 友好操作 + chip 构建器(只读)

**Files:**
- Create: `cpq-frontend/e2e/card-formula-builder.spec.ts`

- [ ] **Step 1: 找到打开 CardFormulaDrawer 的导航路径**

先 grep/Read 现有 E2E 看是否已有进 Excel 视图配置(`ExcelViewConfigTab`)→ 打开卡片公式抽屉的路径可复用;若无,按 `docs/E2E测试方法.md` 选择器约定,从模板管理 → Excel 视图配置 → 某 FORMULA 列「编辑公式」打开抽屉(标题含「编辑卡片公式」)。全程只读、不点保存。

- [ ] **Step 2: 写专项 E2E**

断言(只读):① 抽屉标题区有简单/高级 Segmented;② 简单模式「操作」Select 只含 `引用页签小计/取某字段的值/按条件查找取值/按条件求和`(不含 求平均/计数/最大/最小);③ 切「高级」→ 操作回到原 Radio(`页签小计/字段·首行/字段·按条件取行/聚合（SUM/AVG…）`)且 aggFunc 全集可见;④ 简单模式选「按条件求和」→ 出现 aggExpr chip 构建器(点字段 + `×` → chip 出现);⑤ 加载中=0 / 无 console error。

- [ ] **Step 3: 跑专项 + 回归**

```bash
cd cpq-frontend
npx playwright test --config=e2e/playwright.config.ts e2e/card-formula-builder.spec.ts --reporter=list
npx playwright test --config=e2e/playwright.config.ts e2e/quotation-flow.spec.ts --reporter=list
```
Expected: 专项 passed;quotation-flow passed 加载中=0(卡片公式 token 未变,渲染无回归)。

- [ ] **Step 4: 提交**

```bash
git add cpq-frontend/e2e/card-formula-builder.spec.ts
git commit -m "test(formula-builder): E2E CardFormulaDrawer simple/advanced + chip builder (P1.2 Task 6)"
```

---

## Task 7: 文档回写

**Files:**
- Modify: `docs/RECORD.md`（追加 P1.2 条目,插在归档 footer 之前）
- Modify: `docs/superpowers/specs/2026-06-09-公式可视化构建器-design.md`（§9 标注 P1.2 已实施）

- [ ] **Step 1: RECORD 追加**

```
### [2026-06-09] 公式可视化构建器 P1.2 — CardFormulaDrawer 简单/高级 + 友好操作 + chip 构建器 | cardFormulaOps.ts(新)+test + CardFormulaDrawer.tsx | 纯前端 UX,{formula,refs} token 零变更;CARD_OPERATIONS 友好操作↔RefType/AggFunc 映射 + 简单/高级 Segmented;简单模式降术语(隐藏 JEXL/别名 token 预览+语法提示,隐藏高级聚合 AVG/COUNT/MAX/MIN)+ aggExpr 自由文本换点选 chip 构建器;formula 文本画布两模式保留(模型差异:本就常驻非逃生口)。条件构建器/试算/编辑回填/显示格式/帮助一律保留不动。单测 3 + E2E card-formula-builder 只读 + quotation-flow 加载中=0 回归
```

- [ ] **Step 2: 设计文档 §9 标注**

在 §9 追加:`> 2026-06-09：P1.2(CardFormulaDrawer 简单/高级+友好操作+chip 构建器)已落地;计划 docs/superpowers/plans/2026-06-09-公式可视化构建器-P1.2-CardFormulaDrawer.md。`

- [ ] **Step 3: 提交**

```bash
git add docs/RECORD.md docs/superpowers/specs/2026-06-09-公式可视化构建器-design.md
git commit -m "docs(formula-builder): record P1.2 CardFormulaDrawer + design status"
```

---

## Self-Review（已执行）

- **Spec 覆盖**:简单/高级分层→T2;友好操作标签→T1+T3;简单模式降术语(JEXL/别名/高级聚合隐藏)→T3+T4;aggExpr chip 构建器→T5;E2E→T6;文档→T7。✅
- **token 不变量**:全程不改 `buildInsertResult` / `cardFormula.ts` / `CardRefSpec` / `SUM_OVER` 语法 / `onSave` 产出;chip/友好标签只改"怎么录",不改"录出来是什么"。✅
- **模型差异已显式处理**:formula 文本画布两模式保留(不套用 P1 的隐藏式 dual-mode);简单模式只友好化插入面板。✅
- **占位扫描**:无 TBD;T3/T4/T5 标「先 Read 现状再按 mode 分支」是定位指引(UI 改动常规),非占位;chip 构建器复用 CrossTabRefDrawer 既有 targetExpr 模式。
- **类型一致**:`CARD_OPERATIONS`/`opToRefType`/`refTypeToOp`(松 string 类型避免跨文件耦合,drawer 侧 cast `as RefType/as AggFunc`)在 T1 定义,T3 一致引用;`mode`/`aggExprChips` 状态命名跨 T2/T5 一致。

## P1.2 边界（不含,留后续）
- 全局变量插入(两抽屉均未实现)。
- IF 分支 / 卡片引擎函数化(P3)。
- subtotal/first_row/aggregate 的编辑回填(现状仅 row_where 可回填;非本期目标)。
