# Excel 卡片引用公式 — 计划二：前端（选择器/编辑器/渲染） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 前端支持 `CARD_FORMULA` 列：在模板 Excel 列配置界面提供"可视化选择器 + 公式编辑器"（点选页签→字段→模式/条件，自动生成 ASCII 别名与稳定 refs，公式显示友好占位），并让报价单 Excel 视图渲染后端算好的 CARD_FORMULA 值（含百分比显示格式、空值「—」）。

**Architecture:** 复用后端契约（计划一）：列存于 `template.excel_view_config`，`source_type='CARD_FORMULA'`，含 `formula`(友好占位+JEXL算符) / `refs`(占位→{tab,field/cols,mode,cond}) / `display_format`。配置端扩 `ExcelViewConfigTab.tsx`；渲染端 `LinkedExcelView.tsx` 对"新模型模板"改用后端 `getExcelView` 返回值（已含 CARD_FORMULA），旧模板仍走 `useLinkedExcelRows`。纯逻辑（别名生成、IN→||、占位↔refs 序列化）抽成可 vitest 的纯函数。

**Tech Stack:** React + Ant Design（Drawer/Form/Select/Radio）；TypeScript；Vite；Vitest（纯逻辑）；Playwright E2E（集成）。

> 设计依据：`docs/superpowers/specs/2026-06-02-Excel视图卡片引用公式配置-design.md`（§3 别名层、§4 函数、§5 空值规则一、§7 落点）。
> 后端契约：计划一已实现 `CARD_FORMULA` 求值（`ExcelViewService.buildRowData` + `CardFormulaEvaluator`）。
> 数据源：`templateService.listComponents(templateId)`(页签实例: tcId/componentId/sortOrder/fields) + `quotationService.getExcelView(id)`(返回 `{columns, rows:[{<col_key>:value, _lineItemId}]}`)。

---

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `cpq-frontend/src/pages/template/cardFormula.ts` | 纯逻辑：refs/别名/占位 序列化模型 + 校验（无 React，便于 vitest） | Create |
| `cpq-frontend/src/pages/template/cardFormula.test.ts` | vitest 单测 | Create |
| `cpq-frontend/src/pages/template/CardFormulaDrawer.tsx` | 卡片引用选择器 + 公式编辑器（Drawer） | Create |
| `cpq-frontend/src/pages/template/ExcelViewConfigTab.tsx` | 加 `CARD_FORMULA` 列类型入口 + 接 CardFormulaDrawer | Modify |
| `cpq-frontend/src/services/templateService.ts` | 确认/补 `listComponents` 返回页签+字段（如缺字段则补端点） | Modify(可能) |
| `cpq-frontend/src/pages/quotation/LinkedExcelView.tsx` | CARD_FORMULA 模板改读后端 `getExcelView` 值；百分比显示；空值「—」 | Modify |
| `cpq-frontend/src/pages/quotation/useBackendExcelRows.ts` | 新 hook：调 `getExcelView`，产出 `{rows, columns}`（与 useLinkedExcelRows 输出形态对齐） | Create |
| `cpq-frontend/e2e/card-formula-flow.spec.ts` | E2E：配 CARD_FORMULA 列 → 报价视图出值 | Create |

**列对象（与计划一后端契约一致）：**
```ts
interface CardRefSpec { tab: string; field?: string; mode?: 'FIRST_ROW'|'ROW_WHERE'; cond?: string; cols?: Record<string,string>; }
interface ExcelViewColumn {
  col_key: string; title: string;
  source_type: 'CARD_FORMULA' | /*旧*/ 'VARIABLE'|'FORMULA'|...;
  formula?: string;                       // "=ROUND(([投料.小计]+SUM_OVER([加工] WHERE c0=='电镀', c1))*12%,2)"
  refs?: Record<string, CardRefSpec>;     // 占位token → ref
  display_format?: { type?: 'PERCENT'|'NUMBER'; decimals?: number };
  // ...旧字段保留
}
```

---

### Task 1: 纯逻辑 `cardFormula.ts`（别名/占位/序列化/校验）

**Files:**
- Create: `cpq-frontend/src/pages/template/cardFormula.ts`
- Test: `cpq-frontend/src/pages/template/cardFormula.test.ts`

**职责（纯函数，无 React）：**
- `genAlias(index): string` → `c0/c1/...`。
- `expandIn(token, values[]): string` → `(c0=='a' || c0=='b')`（IN→||）。
- `cmpToJexl(op): string` → `等于→==、不等→!=、大于→>...`，且 `且→&&、或→||`。
- `buildAggregateRef(tabKey, fieldAliasMap): CardRefSpec` → `{tab, cols}`。
- `extractColKeyDeps(formula, allColKeys): string[]` → 抓裸 `[X]`（无 `.`）列依赖（与后端 `columnDeps` 同义，前端用于保存前环检测提示）。
- `validateCardFormula(col, allColKeys, refs): string[]` → 返回错误清单：占位 ref 缺失、聚合源缺 cols、列引列成环、JEXL 非法字符（仅允许 `[]()。数字 别名 运算符 字符串字面量 函数名` 白名单）。

- [ ] **Step 1: 写失败测试**
```ts
import { describe, it, expect } from 'vitest';
import { genAlias, expandIn, extractColKeyDeps, validateCardFormula } from './cardFormula';

describe('cardFormula pure logic', () => {
  it('genAlias', () => { expect(genAlias(0)).toBe('c0'); expect(genAlias(3)).toBe('c3'); });
  it('expandIn → ||', () => {
    expect(expandIn('c0', ['电镀','酸洗'])).toBe("(c0=='电镀' || c0=='酸洗')");
  });
  it('extractColKeyDeps 只抓裸 col_key, 跳过含点占位', () => {
    expect(extractColKeyDeps('[投料.小计] + [A] * 2', ['A','B'])).toEqual(['A']);
  });
  it('validateCardFormula 报占位缺失', () => {
    const errs = validateCardFormula(
      { col_key:'B', title:'x', source_type:'CARD_FORMULA', formula:'=[加工.加工费]', refs:{} } as any,
      ['B'], {});
    expect(errs.length).toBeGreaterThan(0);
  });
  it('validateCardFormula 报列引列环', () => {
    const errs = validateCardFormula(
      { col_key:'A', title:'x', source_type:'CARD_FORMULA', formula:'=[B]+1', refs:{} } as any,
      ['A','B'], { A:'=[B]+1', B:'=[A]+1' });
    expect(errs.some(e => e.includes('循环'))).toBe(true);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd cpq-frontend && npx vitest run src/pages/template/cardFormula.test.ts`
Expected: FAIL（模块未实现）

- [ ] **Step 3: 实现 `cardFormula.ts`**
```ts
export interface CardRefSpec { tab: string; field?: string; mode?: 'FIRST_ROW'|'ROW_WHERE'; cond?: string; cols?: Record<string,string>; }

export const genAlias = (i: number): string => `c${i}`;

export const expandIn = (alias: string, values: string[]): string =>
  '(' + values.map(v => `${alias}=='${v}'`).join(' || ') + ')';

const BRACKET = /\[([^\[\]]+)]/g;
export function extractColKeyDeps(formula: string, allColKeys: string[]): string[] {
  const out: string[] = []; let m: RegExpExecArray | null;
  const re = new RegExp(BRACKET);
  while ((m = re.exec(formula)) !== null) {
    const ref = m[1].trim();
    if (!ref.includes('.') && allColKeys.includes(ref) && !out.includes(ref)) out.push(ref);
  }
  return out;
}

/** 列依赖环检测（Kahn）。formulas: col_key→formula。有环返回环成员，否则 []。 */
export function detectCycle(formulas: Record<string,string>): string[] {
  const cols = Object.keys(formulas);
  const deps: Record<string,string[]> = {};
  const indeg: Record<string,number> = {};
  for (const c of cols) { deps[c] = extractColKeyDeps(formulas[c] || '', cols); indeg[c] = deps[c].length; }
  const q = cols.filter(c => indeg[c] === 0); const order: string[] = [];
  while (q.length) {
    const c = q.shift()!; order.push(c);
    for (const o of cols) if (deps[o].includes(c) && --indeg[o] === 0) q.push(o);
  }
  return order.length === cols.length ? [] : cols.filter(c => !order.includes(c));
}

const ALLOWED = /^[\sA-Za-z0-9_+\-*/().,%<>=!&|'一-龥\[\]]*$/; // 含中文(字符串字面量/占位内)

export function validateCardFormula(
  col: { col_key: string; formula?: string; refs?: Record<string, CardRefSpec> },
  allColKeys: string[],
  allFormulas: Record<string,string>,
): string[] {
  const errs: string[] = [];
  const f = (col.formula || '').replace(/^=/, '');
  const refs = col.refs || {};
  // 1) 占位 ref 完整性：含 . 的占位必须在 refs；聚合源 [页签] 必须有 cols
  let m: RegExpExecArray | null; const re = new RegExp(BRACKET);
  while ((m = re.exec(f)) !== null) {
    const tok = m[1].trim();
    if (tok.includes('.')) { if (!refs[tok]) errs.push(`占位 [${tok}] 缺少 ref 定义`); }
    else if (!allColKeys.includes(tok) && refs[tok]) {
      if (!refs[tok].cols || Object.keys(refs[tok].cols!).length === 0) errs.push(`聚合源 [${tok}] 缺少 cols 别名映射`);
    }
  }
  // 2) 列引列环
  const cyc = detectCycle(allFormulas);
  if (cyc.includes(col.col_key)) errs.push(`列公式存在循环引用: ${cyc.join(',')}`);
  // 3) 字符白名单
  if (!ALLOWED.test(f)) errs.push('公式含非法字符');
  return errs;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd cpq-frontend && npx vitest run src/pages/template/cardFormula.test.ts`
Expected: PASS（5 tests）

- [ ] **Step 5: Commit**
```bash
git add cpq-frontend/src/pages/template/cardFormula.ts cpq-frontend/src/pages/template/cardFormula.test.ts
git commit -m "feat(template): Excel 卡片公式纯逻辑(别名/IN展开/列依赖环检测/校验)"
```

---

### Task 2: `CardFormulaDrawer.tsx` 可视化选择器 + 公式编辑器

**Files:**
- Create: `cpq-frontend/src/pages/template/CardFormulaDrawer.tsx`
- Modify: `cpq-frontend/src/services/templateService.ts`（如需补"页签+字段"获取）

**UI（遵循 CLAUDE.md：Drawer，placement=right，宽 720/960）：**
- 顶部：公式 `Input.TextArea`（显示友好占位）。
- "插入引用"区（按 §3）：
  - 选页签（下拉，来源 `listComponents(templateId)`：显示 `name`，值 = `componentId:sortOrder` 作 tabKey）。
  - 选引用类型：`页签小计` / `字段-首行` / `字段-按条件取行` / `聚合(SUM/AVG/COUNT/MIN/MAX)`。
  - 字段下拉（来源所选页签的 `fields[].name`，中文显示）。
  - 条件构建器（按条件取行 / 聚合 WHERE）：行集 = (字段下拉 + 运算符下拉[等于/不等/大于/...] + 值输入) 多行 + 且/或；点"确定"→ 用 `cardFormula.ts` 生成别名 cols + JEXL cond（IN 用 `expandIn`）。
  - "插入函数"按钮：`IF()` `ROUND(,2)` `ABS()` `12%`。
- 插入逻辑：在光标处插入占位文本（`[页签.字段]` / `[页签.小计]` / `SUM_OVER([页签] WHERE <jexl>, <别名表达式>)`），同时把对应 `CardRefSpec` 写入 `refs`（聚合源含 `cols` 别名映射；ROW_WHERE 单值含 `cols`+`cond`）。
- 底部：`display_format`（百分比开关 + 小数位）。
- 保存：调 `validateCardFormula`，有错 message.error 列出；无错 `onSave({formula, refs, display_format})`。

**实现要点（先 Read `ExcelViewConfigTab.tsx` 现有 FORMULA 编辑 Drawer 与光标插入函数 `insertAtCursor`，复用同款交互）。** props：`{ open, templateId, allColKeys, allFormulas, value: ExcelViewColumn, onSave, onClose }`。

- [ ] **Step 1: 实现 CardFormulaDrawer.tsx**（按上结构；引用 `cardFormula.ts` 的 `genAlias/expandIn/validateCardFormula`；页签数据 `templateService.listComponents`；字段取每个组件的 `fields`。若 `listComponents` 不含 fields，则在 templateService 加 `getComponentFields(componentId)` 或复用 component 详情接口——先 Read 确认。）

> 代码量较大，按 ExcelViewConfigTab 既有 Drawer/Form/Select 范式书写；关键纯逻辑已在 Task1 抽出并测过，本组件主要是装配 + 状态。

- [ ] **Step 2: 自检（编译 + Vite transform）**

Run:
```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/template/CardFormulaDrawer.tsx
```
Expected: tsc 0 错误；Vite 200。

- [ ] **Step 3: Commit**
```bash
git add cpq-frontend/src/pages/template/CardFormulaDrawer.tsx cpq-frontend/src/services/templateService.ts
git commit -m "feat(template): CardFormulaDrawer 卡片引用可视化选择器+公式编辑器"
```

---

### Task 3: `ExcelViewConfigTab.tsx` 接入 CARD_FORMULA 列类型

**Files:**
- Modify: `cpq-frontend/src/pages/template/ExcelViewConfigTab.tsx`

**改动：**
- `SourceType` 增加 `'CARD_FORMULA'`；`ExcelViewColumn` 增加 `refs?`、`display_format?`。
- "新增列"下拉菜单加一项「卡片公式 CARD_FORMULA」→ `addColumn('CARD_FORMULA')`（base 设 `source_type:'CARD_FORMULA', formula:'=', refs:{}`）。
- `renderValueCell`：`CARD_FORMULA` 列显示公式摘要 + Tag「CARD」+「编辑」按钮 → 打开 `CardFormulaDrawer`（传 `templateId / allColKeys=所有列col_key / allFormulas=所有CARD_FORMULA列formula映射 / value=本列`）。
- Drawer `onSave` → `updateColumn(index, { formula, refs, display_format, source_type:'CARD_FORMULA' })`。
- 保存模板前（已有保存入口）对所有 CARD_FORMULA 列跑 `validateCardFormula` 汇总报错（阻止保存）。

- [ ] **Step 1: 实现改动**（先 Read 现有 `addColumn` / `renderValueCell` / 保存逻辑，按既有范式接入；勿动旧 VARIABLE/FORMULA 分支）

- [ ] **Step 2: 自检**
```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/template/ExcelViewConfigTab.tsx
```
Expected: tsc 0 错误；Vite 200。

- [ ] **Step 3: Commit**
```bash
git add cpq-frontend/src/pages/template/ExcelViewConfigTab.tsx
git commit -m "feat(template): Excel 列配置接入 CARD_FORMULA 类型(选择器入口+校验)"
```

---

### Task 4: 渲染 — `useBackendExcelRows` + `LinkedExcelView` 走后端值

**Files:**
- Create: `cpq-frontend/src/pages/quotation/useBackendExcelRows.ts`
- Modify: `cpq-frontend/src/pages/quotation/LinkedExcelView.tsx`

**思路：** CARD_FORMULA 列由后端 `getExcelView` 算好。LinkedExcelView 判断：若模板 excel_view_config 含 CARD_FORMULA 列（新模型）→ 用 `useBackendExcelRows`(调 `quotationService.getExcelView(quotationId)`) 取 `{columns, rows}`，每行按 `col_key` 取后端值；否则（旧模型）→ 仍用 `useLinkedExcelRows`（不动）。

**`useBackendExcelRows.ts`：** 入参 `{ quotationId, enabled }`；调 `getExcelView`，输出 `{ rows: Array<{__lineItemId, [col_key]:value}>, columns, loading, error }`。第一列「料号」：后端 row 无 `__hfPartNo`，由前端用 lineItems 的 productPartNo 按 `_lineItemId` 关联补上（或后端 row 已含某料号列——先 Read getExcelView row 内容确认；缺则前端 join lineItems）。

**LinkedExcelView 渲染：**
- 第一列「料号」保持（计划已改 `__hfPartNo`）。
- 数据列：`display_format.type==='PERCENT'` → 值 `*100` 后加 `%`、按 decimals 截断；空值/`null`/`'—'` → 显示「—」。
- `CARD_FORMULA` 走后端值；旧列保持原渲染。

- [ ] **Step 1: 实现 useBackendExcelRows.ts + 改 LinkedExcelView**（先 Read `LinkedExcelView.tsx` 全文 + `quotationService.getExcelView` 返回；按"含 CARD_FORMULA 列则切后端值"分支）

- [ ] **Step 2: 自检（tsc + Vite + 协议级 E2E 前置）**
```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/LinkedExcelView.tsx
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/useBackendExcelRows.ts
```
Expected: tsc 0 错误；两个 200。

- [ ] **Step 3: Commit**
```bash
git add cpq-frontend/src/pages/quotation/useBackendExcelRows.ts cpq-frontend/src/pages/quotation/LinkedExcelView.tsx
git commit -m "feat(quotation): LinkedExcelView 对 CARD_FORMULA 模板改读后端 getExcelView 值(百分比/空值—)"
```

---

### Task 5: E2E 集成验证（CLAUDE.md 强制 — 改了 LinkedExcelView）

**Files:**
- Create: `cpq-frontend/e2e/card-formula-flow.spec.ts`

**场景：** 选一个（或新建）模板配 1 个 CARD_FORMULA 列（如 `=[投料.小计]`，或引用某页签字段）→ 报价单选该模板产品 → 打开 Excel 视图 → 断言该列出现非「—」的预期值、且第一列为「料号」、`'加载中' final count = 0`。

- [ ] **Step 1: 写 spec**（参照 `e2e/quotation-flow.spec.ts` 选择器约定 + UTF-8 中文坑；用一个已知含 CARD_FORMULA 配置的模板/料号）
- [ ] **Step 2: 跑 E2E**
```bash
cd cpq-frontend
npx playwright test --config=e2e/playwright.config.ts e2e/card-formula-flow.spec.ts --reporter=list
```
Expected: passed；CARD_FORMULA 列出值；`'加载中'=0`。若夹具数据缺失导致无法跑到视图，如实记录并改用可用料号/模板。
- [ ] **Step 3: RECORD.md 追加 + Commit**
```bash
git add cpq-frontend/e2e/card-formula-flow.spec.ts docs/RECORD.md
git commit -m "test(quotation): CARD_FORMULA Excel 视图渲染 E2E"
```

---

## Self-Review（计划 vs spec）

**1. Spec 覆盖：**
- §3 可视化选择器(页签→字段→模式/条件) + 友好占位 + 别名层 → Task1(纯逻辑)+Task2(Drawer) ✅
- §4 函数(四则/聚合/IF/ROUND/ABS/百分比) + IN→|| + 列引列 → Task1(expandIn/校验)+Task2(插入函数按钮)+Task4(百分比显示) ✅
- §5 空值规则一显示 → Task4(「—」) ✅（后端已按规则一算，前端只显示）
- §7 CARD_FORMULA 列类型与旧并存 → Task3(新增类型,旧分支不动)+Task4(新模型走后端/旧走前端) ✅
- §3 同组件多实例按 `componentId:sortOrder` 定位 → Task2(tabKey 生成) ✅

**2. 占位符扫描：** Task2/3/4 的 React 组件未逐行给 JSX（依赖 Read 现有 Drawer/渲染范式后装配），但**关键纯逻辑(别名/IN/环检测/校验)已在 Task1 给全代码并测试**；UI 装配步骤给了明确结构 + props + 数据源 + 自检命令。属"UI 按既有范式装配"的合理粒度（与计划一落库 IT 同处理原则）。

**3. 类型一致：** `CardRefSpec`(tab/field/mode/cond/cols)、列对象 `formula/refs/display_format`、tabKey=`componentId:sortOrder`、占位区分(含`.`=卡片/裸=列引)——与计划一后端契约逐项对齐 ✅

**4. 待实现期确认（已标"先 Read"）：** `listComponents` 是否含 `fields`、`getExcelView` row 是否含料号列、`ExcelViewConfigTab` 的 `addColumn/renderValueCell/insertAtCursor/保存` 范式、`LinkedExcelView` 全文。
