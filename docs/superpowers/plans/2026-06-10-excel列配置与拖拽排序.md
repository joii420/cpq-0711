# EXCEL 列配置收敛 + 字段/列拖拽排序 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** EXCEL 组件列配置「来源」收敛为「固定值 + 页签连表公式」两种（固定值给纯文本输入），并给 EXCEL 列行与字段配置表字段行加上拖拽排序。

**Architecture:** 纯前端 2 文件改动，后端不动（FIXED_VALUE/TAB_JOIN_FORMULA 已能渲染）。新增一个可复用 `SortableTable`（AntD Table + `@dnd-kit/sortable` 包装，带拖拽手柄列），ExcelColumnPanel 与 FieldConfigTable 共用，DRY。

**Tech Stack:** React + Ant Design `Table` + `@dnd-kit/{core,sortable,utilities}`（均已装）+ Vite + Playwright（E2E）。

**Spec:** `docs/superpowers/specs/2026-06-10-excel列配置与拖拽排序-design.md`

---

## 执行前置约束（CLAUDE.md）

- **必须先建隔离 worktree 分支**（`superpowers:using-git-worktrees`），所有编码在该 worktree 内进行，不直接改 master。
- worktree 只隔离 git 工作区；Vite(5174)/Quarkus(8081)/DB 绑主目录 → **worktree 内只能静态检查**（`tsc --noEmit`、`vitest`）；运行时自检（Vite 200 / E2E）合回主分支后由主目录 server 跑。worktree 无 node_modules，跑 tsc 前 `ln -s 主目录/cpq-frontend/node_modules cpq-frontend/node_modules`。
- 提交只 `git add` 本次明确改动文件，**禁止 `git add -A`**。

## 文件结构总览

- **创建**：`cpq-frontend/src/components/SortableTable.tsx`（AntD Table + dnd-kit 可排序行包装 + `DragHandle` + `SortableRow`）
- **修改**：`cpq-frontend/src/pages/component/ComponentManagement.tsx`（`ExcelColumnPanel`：来源收敛 + 固定值输入 + 默认值 + 接 SortableTable 拖拽）
- **修改**：`cpq-frontend/src/pages/component/FieldConfigTable.tsx`（字段行接 SortableTable 拖拽 + `sort_order` 同步）

---

# Task 1：可复用 SortableTable（AntD Table + dnd-kit 拖拽行）

**Files:**
- Create: `cpq-frontend/src/components/SortableTable.tsx`

- [ ] **Step 1: 写组件（完整代码）**

```tsx
import React, { createContext, useContext, useMemo } from 'react';
import { Table } from 'antd';
import type { TableProps } from 'antd';
import { HolderOutlined } from '@ant-design/icons';
import {
  DndContext, PointerSensor, KeyboardSensor, useSensor, useSensors,
  closestCenter, type DragEndEvent,
} from '@dnd-kit/core';
import {
  SortableContext, useSortable, verticalListSortingStrategy, arrayMove,
  sortableKeyboardCoordinates,
} from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';

interface RowContextProps {
  setActivatorNodeRef?: (el: HTMLElement | null) => void;
  listeners?: Record<string, unknown>;
}
const RowContext = createContext<RowContextProps>({});

/** 放进某一列的 render 里作为拖拽手柄。 */
export const DragHandle: React.FC = () => {
  const { setActivatorNodeRef, listeners } = useContext(RowContext);
  return (
    <HolderOutlined
      ref={setActivatorNodeRef as unknown as React.Ref<HTMLSpanElement>}
      {...listeners}
      style={{ cursor: 'move', color: '#999', touchAction: 'none' }}
    />
  );
};

const SortableRow: React.FC<React.HTMLAttributes<HTMLTableRowElement> & { 'data-row-key': string }> = (props) => {
  const { attributes, listeners, setNodeRef, setActivatorNodeRef, transform, transition, isDragging } =
    useSortable({ id: props['data-row-key'] });
  const style: React.CSSProperties = {
    ...props.style,
    transform: CSS.Translate.toString(transform),
    transition,
    ...(isDragging ? { position: 'relative', zIndex: 999, background: '#fafafa' } : {}),
  };
  const ctx = useMemo<RowContextProps>(() => ({ setActivatorNodeRef, listeners }), [setActivatorNodeRef, listeners]);
  return (
    <RowContext.Provider value={ctx}>
      <tr {...props} ref={setNodeRef} style={style} {...attributes} />
    </RowContext.Provider>
  );
};

export interface SortableTableProps<T> extends Omit<TableProps<T>, 'components'> {
  /** 稳定且唯一的行 id 字段名（如字段行用 'key'，EXCEL 列用 'col_key'）。 */
  rowKey: string;
  /** 拖拽结束后的新数组顺序。 */
  onReorder: (next: T[]) => void;
}

/** AntD Table + dnd-kit 垂直拖拽排序。消费方需在 columns 里放一列 render={() => <DragHandle />}。 */
export function SortableTable<T extends Record<string, unknown>>(
  { rowKey, onReorder, dataSource, ...rest }: SortableTableProps<T>,
) {
  const sensors = useSensors(
    useSensor(PointerSensor, { activationConstraint: { distance: 5 } }),
    useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }),
  );
  const list = (dataSource ?? []) as T[];
  const items = list.map((d) => String(d[rowKey]));
  const onDragEnd = ({ active, over }: DragEndEvent) => {
    if (!over || active.id === over.id) return;
    const from = list.findIndex((d) => String(d[rowKey]) === active.id);
    const to = list.findIndex((d) => String(d[rowKey]) === over.id);
    if (from < 0 || to < 0) return;
    onReorder(arrayMove(list, from, to));
  };
  return (
    <DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={onDragEnd}>
      <SortableContext items={items} strategy={verticalListSortingStrategy}>
        <Table<T>
          {...rest}
          rowKey={rowKey}
          dataSource={dataSource}
          components={{ body: { row: SortableRow as never } }}
        />
      </SortableContext>
    </DndContext>
  );
}
```

- [ ] **Step 2: tsc 自检**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误。

- [ ] **Step 3: Commit**

```bash
git add cpq-frontend/src/components/SortableTable.tsx
git commit -m "feat(component): 可复用 SortableTable(AntD Table + dnd-kit 拖拽行)"
```

---

# Task 2：ExcelColumnPanel 来源收敛 + 固定值输入

**Files:**
- Modify: `cpq-frontend/src/pages/component/ComponentManagement.tsx`（`ExcelColumnPanel`，约 :404-460）

> 先读 `ExcelColumnPanel` 现状：来源 `Select` 5 选项（:446-450）、`isFormula = source_type==='TAB_JOIN_FORMULA' || !!col.formula`（:439）、新增列默认 `source_type:'COMPONENT_FIELD'`（:416）、`value={col.source_type ?? 'COMPONENT_FIELD'}`（:443）。

- [ ] **Step 1: 来源 Select 选项收敛为 2 + 默认改 FIXED_VALUE**

把来源 `Select` 的 `options` 改为仅两项；`addColumn` 新增列默认与 `value ??` 兜底都改 `'FIXED_VALUE'`：

```tsx
// options:
options={[
  { label: '固定值', value: 'FIXED_VALUE' },
  { label: '页签连表公式', value: 'TAB_JOIN_FORMULA' },
]}
value={col.source_type ?? 'FIXED_VALUE'}
onChange={(v) => update(idx, {
  source_type: v,
  // 切换来源清理互斥字段,避免脏数据
  ...(v === 'FIXED_VALUE' ? { formula: undefined } : { fixed_value: undefined }),
})}
```

新增列默认（原 :416）：
```tsx
onChange([...columns, { col_key: `col_${columns.length + 1}`, title: '', source_type: 'FIXED_VALUE', hidden: false }]);
```

- [ ] **Step 2: 配置区按 source_type 切换（固定值文本框 / 公式按钮）**

把「来源/公式」单元格 render 改为：来源 Select 旁边按 `col.source_type` 条件渲染配置控件。`FIXED_VALUE` → `Input` 绑 `fixed_value`；`TAB_JOIN_FORMULA` → 保留现有"配置公式"按钮 + 表达式预览（不动其开抽屉逻辑）：

```tsx
{col.source_type === 'FIXED_VALUE' && (
  <Input
    size="small"
    style={{ width: 180, marginLeft: 8 }}
    placeholder="固定值(留空则空白)"
    value={col.fixed_value ?? ''}
    onChange={(e) => update(idx, { fixed_value: e.target.value })}
  />
)}
{col.source_type === 'TAB_JOIN_FORMULA' && (
  /* 保留原有的"配置公式"按钮 + 表达式预览,原样不动 */
)}
```

> 确认 `ExcelColumn` 类型（约 :330）含 `fixed_value?: string`；缺则补上。`Input` 从 antd 引入（文件多半已 import，缺则加）。

- [ ] **Step 3: tsc 自检**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误。

- [ ] **Step 4: Commit**

```bash
git add cpq-frontend/src/pages/component/ComponentManagement.tsx
git commit -m "feat(component): EXCEL列来源收敛为固定值/页签公式 + 固定值文本输入"
```

---

# Task 3：ExcelColumnPanel 接 SortableTable 拖拽排序

**Files:**
- Modify: `cpq-frontend/src/pages/component/ComponentManagement.tsx`（`ExcelColumnPanel`）

- [ ] **Step 1: 引入 SortableTable + DragHandle，加手柄列**

顶部 import：
```tsx
import { SortableTable, DragHandle } from '../../components/SortableTable';
```
在 `ExcelColumnPanel` 的 `columns` 数组**最前面**插入手柄列：
```tsx
{ key: 'drag', width: 40, align: 'center' as const, render: () => <DragHandle /> },
```

- [ ] **Step 2: 把 `<Table>` 换成 `<SortableTable>`**

```tsx
<SortableTable
  rowKey="col_key"
  size="small"
  pagination={false}
  dataSource={columns}
  columns={columns_def}              // 现有列定义(已含手柄列)
  onReorder={(next) => onChange(next)} // next = 重排后的列数组
/>
```
> `rowKey="col_key"`：实现期确认 `col_key` 在面板内唯一（新增用 `col_${n}`）。若担心用户编辑 col_key 致重复，可改用稳定内部 id，但本期按 col_key（新增保证唯一即可）。

- [ ] **Step 3: tsc 自检**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误。

- [ ] **Step 4: Commit**

```bash
git add cpq-frontend/src/pages/component/ComponentManagement.tsx
git commit -m "feat(component): EXCEL列行拖拽排序(SortableTable)"
```

---

# Task 4：FieldConfigTable 字段行拖拽排序 + sort_order 同步

**Files:**
- Modify: `cpq-frontend/src/pages/component/FieldConfigTable.tsx`（`<Table dataSource={fields} columns={columns}>`，约 :508-510）

> 先读：`fields` 元素含稳定 `key`（`FieldItem.key`）；现 `<Table>` 通过 `onChange(newFields)` 回传父组件保存。

- [ ] **Step 1: 引入 + 手柄列**

```tsx
import { SortableTable, DragHandle } from '../../components/SortableTable';
```
在 `columns` 最前插入手柄列：
```tsx
{ key: 'drag', width: 40, align: 'center' as const, render: () => <DragHandle /> },
```

- [ ] **Step 2: `<Table>` → `<SortableTable>` + 重排回调（同步 sort_order）**

```tsx
<SortableTable
  rowKey="key"
  /* ...保留原 Table 的 size/pagination/scroll 等 props... */
  dataSource={fields}
  columns={columns}
  onReorder={(next) => onChange(next.map((f, i) => ({ ...f, sort_order: i })))}
/>
```
> `onReorder` 重排后把 `sort_order` 重写为新下标（spec §3.2/§4 核实点：快照/后端若按 `sort_order` 排序则此同步必要；若按数组下标则同步无害）。`onChange` 即现有保存路径。

- [ ] **Step 3: tsc 自检**

Run: `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json`
Expected: 0 错误。

- [ ] **Step 4: Commit**

```bash
git add cpq-frontend/src/pages/component/FieldConfigTable.tsx
git commit -m "feat(component): 字段配置表字段行拖拽排序 + sort_order 同步"
```

---

# Task 5：合回主分支 + 运行时自检 + E2E（AP-44）

> worktree 内跑不了运行时；合回主分支后由主目录 server 验证。

- [ ] **Step 1: 实现期核实点（合回前/后任一处确认）**

- 后端 Excel 视图渲染**按 `excel_columns` 数组顺序**出列（查 `ExcelViewService` 列迭代是否依赖单独 `sort` 字段；若依赖，则 Task 3 的 `onReorder` 需同步重写各列 `sort` = 下标，仿 Task 4）。
- FieldConfigTable 保存确实按 `fields` 数组顺序 / `sort_order` 持久化（决定 `sort_order` 同步是否生效）。

- [ ] **Step 2: 合回主分支 + 触发重载**

```bash
# 切回主工作区 → git merge <特性分支>
# 前端 Vite 自动 HMR;无需重启后端(纯前端改动)
```

- [ ] **Step 3: 前端运行时自检**

Run（主工作区）:
- `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/` → 200
- `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/component/ComponentManagement.tsx` → 200
- `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/component/FieldConfigTable.tsx` → 200
- `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/components/SortableTable.tsx` → 200

- [ ] **Step 4: E2E 双 spec（FieldConfigTable 是 AP-44 协议敏感文件，强制）**

Run（主工作区 `cpq-frontend`）:
```bash
rm -f e2e/screenshots/qf-*.png
npx playwright test --config=e2e/playwright.config.ts e2e/quotation-flow.spec.ts --reporter=list
npx playwright test --config=e2e/playwright.config.ts e2e/composite-product-flow.spec.ts --reporter=list
```
Expected:
- `quotation-flow`：**1 passed**、`'加载中' final=0`、全 8 Tab `加载中=0`（字段拖拽改动不退步报价渲染）。
- `composite-product-flow`：仍受**既有** `composite_child_elements_mirror.unit_weight` 缺列 bug 影响（与本改动无关，见 RECORD 2026-06-10）——按既定结论看 SIMPLE 通过 + 截图，不因本改动新增失败。

- [ ] **Step 5: 手工验收（headless 无法，列清单交用户点）**

- 组件管理选一个 EXCEL 组件：来源下拉只剩「固定值/页签连表公式」；选固定值出现文本框、填值保存重开仍在；列行可拖拽调序、保存后顺序保留并反映到 Excel 视图列序。
- 选一个普通组件：字段配置表字段行可拖拽调序、保存后该页签渲染列序随之变化。

- [ ] **Step 6: RECORD 回写 + 收尾**

```
[2026-06-10] 组件管理 - EXCEL列来源收敛(固定值+页签公式)+固定值文本输入+字段/列拖拽排序 | SortableTable(新)/ComponentManagement(ExcelColumnPanel)/FieldConfigTable | 纯前端;后端不动;砍掉的3种来源留后端死分支;拖拽用@dnd-kit/sortable
```
用户确认达标后走 `superpowers:finishing-a-development-branch`：切 master → merge → `git worktree remove` + 删分支。

---

## 自检清单（写计划者自查）

- **Spec 覆盖**：决策1(来源收敛→T2)、2(固定值文本→T2)、3(默认FIXED_VALUE→T2)、4(宽松不校验→T2 留空允许)、5(拖拽→T1+T3+T4)、6(A+B 范围→T3 EXCEL列 / T4 字段)、7(后端不动→全程未改后端)。✅ 全覆盖。
- **类型一致**：`SortableTable`/`DragHandle`/`onReorder`/`rowKey` 命名 T1 定义、T3/T4 引用一致；`fixed_value` 与后端 `ExcelViewService:390 col.get("fixed_value")` 字段名一致。✅
- **No placeholder**：T2「保留原有配置公式按钮原样不动」指向现有代码（非新代码,无需贴）；其余代码块完整。✅
- **YAGNI**：后端清理、C/D/E 排序、固定值格式化均排除。✅
- **AP-44**：FieldConfigTable 改动 → T5 强制 E2E 双 spec。✅
