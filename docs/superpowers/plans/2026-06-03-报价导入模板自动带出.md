# 报价导入向导 · 模板自动带出 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 报价导入向导进入「选模板」步骤时,自动带出该客户上次使用的报价模板线最新版本 + 当前可用的最新核价模板 + 上次使用的产品分类,三项预填可改,消灭重复手选。

**Architecture:** 新增后端只读端点 `GET /api/cpq/templates/auto-defaults?customerId`,把「最近报价单 → 模板线 → 最新发布版本」推导集中在 `TemplateService.computeAutoDefaults`(兜底复用现有 `matchCustomerQuoteTemplate`)。前端在进 Step 2 时调一次、预填 `createForm` 三个 ID + 来源提示文案;`QuotationCreateForm` 现有 effect 的 "preserve if present" 逻辑天然保留预填值,不触碰字段类型联动协议。

**Tech Stack:** 后端 Quarkus + Hibernate Panache(`Template`/`Quotation`/`ProductCategory` 实体)+ JUnit5 `@QuarkusTest`/`@TestTransaction`;前端 React + Ant Design + axios。

参考设计:`docs/superpowers/specs/2026-06-03-报价导入模板自动带出-design.md`

---

## 文件结构

| 文件 | 职责 | 动作 |
| --- | --- | --- |
| `cpq-backend/.../template/dto/QuoteImportAutoDefaults.java` | 自动默认结果 DTO(纯数据) | 新建 |
| `cpq-backend/.../template/service/TemplateService.java` | 新增 `computeAutoDefaults(UUID)` 推导逻辑 | 修改 |
| `cpq-backend/.../template/resource/TemplateResource.java` | 新增 `GET /auto-defaults` 端点 | 修改 |
| `cpq-backend/.../test/.../template/AutoDefaultsServiceTest.java` | 6 分支单测 | 新建 |
| `cpq-frontend/src/pages/quotation/QuotationCreateForm.tsx` | 新增两个可选 hint props + 渲染提示 | 修改 |
| `cpq-frontend/src/pages/quotation/QuoteBasicDataImportV6Drawer.tsx` | 进 Step 2 拉 auto-defaults 预填 + 传 hint | 修改 |

---

## Task 1: 后端 DTO `QuoteImportAutoDefaults`

**Files:**
- Create: `cpq-backend/src/main/java/com/cpq/template/dto/QuoteImportAutoDefaults.java`

- [ ] **Step 1: 创建 DTO(纯数据持有类,无逻辑,无需单测)**

```java
package com.cpq.template.dto;

import java.util.UUID;

/**
 * 报价导入向导「选模板」步骤的自动默认值。
 *
 * <p>customerTemplateSource:
 * <ul>
 *   <li>LAST_USED — 跟随该客户上次使用的报价模板线的最新发布版本</li>
 *   <li>CUSTOMER_SPECIFIC_FALLBACK — 无历史,退客户专属最新发布</li>
 *   <li>GENERAL_FALLBACK — 无历史且无客户专属,退通用最新发布</li>
 *   <li>NONE — 无任何可用报价模板</li>
 * </ul>
 * costingTemplateSource: CUSTOMER_SPECIFIC | GENERAL | NONE(核价不做记忆)。
 */
public class QuoteImportAutoDefaults {

    public UUID categoryId;
    public String categoryName;

    public UUID customerTemplateId;
    public UUID customerTemplateSeriesId;
    public String customerTemplateName;
    public String customerTemplateVersion;
    public String customerTemplateSource;

    public UUID costingTemplateId;
    public String costingTemplateName;
    public String costingTemplateVersion;
    public String costingTemplateSource;
}
```

- [ ] **Step 2: 编译确认**

Run: `cd cpq-backend && ./mvnw -q -o compile`
Expected: BUILD SUCCESS(无报错)

- [ ] **Step 3: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/template/dto/QuoteImportAutoDefaults.java
git commit -m "feat(template): 新增报价导入自动默认 DTO QuoteImportAutoDefaults"
```

---

## Task 2: `TemplateService.computeAutoDefaults` + 单测(TDD)

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/template/service/TemplateService.java`
- Test: `cpq-backend/src/test/java/com/cpq/template/AutoDefaultsServiceTest.java`

- [ ] **Step 1: 先加桩方法让测试可编译并失败**

在 `TemplateService` 类中加入(先返回 null,使测试编译通过但断言失败):

```java
public com.cpq.template.dto.QuoteImportAutoDefaults computeAutoDefaults(UUID customerId) {
    return null; // TDD 桩,Step 4 实现
}
```

同时在文件顶部 import 区补充(实现时会用到,提前加避免二次编辑):

```java
import com.cpq.basicdata.entity.ProductCategory;
import com.cpq.quotation.entity.Quotation;
import com.cpq.template.dto.QuoteImportAutoDefaults;
```

- [ ] **Step 2: 写失败测试(6 分支)**

创建 `cpq-backend/src/test/java/com/cpq/template/AutoDefaultsServiceTest.java`:

```java
package com.cpq.template;

import com.cpq.template.dto.QuoteImportAutoDefaults;
import com.cpq.template.service.TemplateService;
import io.quarkus.test.TestTransaction;
import io.quarkus.test.junit.QuarkusTest;
import jakarta.inject.Inject;
import jakarta.persistence.EntityManager;
import org.junit.jupiter.api.Test;

import java.util.UUID;

import static org.junit.jupiter.api.Assertions.*;

/**
 * computeAutoDefaults 6 分支验收。每个用例自建隔离的 category / customer / template /
 * quotation 数据,@TestTransaction 跑完回滚,互不污染。
 */
@QuarkusTest
class AutoDefaultsServiceTest {

    @Inject TemplateService templateService;
    @Inject EntityManager em;

    // ---- fixture helpers(native SQL,避免依赖各实体构造细节) ----

    private UUID newCategory(String name) {
        UUID id = UUID.randomUUID();
        em.createNativeQuery(
                "INSERT INTO product_category(id, code, name, status, sort_order, created_at, updated_at) " +
                "VALUES (:id, :code, :name, 'ACTIVE', 0, NOW(), NOW())")
                .setParameter("id", id)
                .setParameter("code", "AD_" + id.toString().substring(0, 8))
                .setParameter("name", name)
                .executeUpdate();
        return id;
    }

    private UUID newCustomer() {
        UUID id = UUID.randomUUID();
        String suffix = id.toString().substring(0, 8);
        // customer NOT NULL 列: name/code/level/accumulated_amount/status/version(@Version)/时间戳
        em.createNativeQuery(
                "INSERT INTO customer(id, name, code, level, accumulated_amount, status, version, created_at, updated_at) " +
                "VALUES (:id, :name, :code, 'STANDARD', 0, 'ACTIVE', 0, NOW(), NOW())")
                .setParameter("id", id)
                .setParameter("name", "AD客户_" + suffix)
                .setParameter("code", "ADC_" + suffix)
                .executeUpdate();
        return id;
    }

    /**
     * 无历史分支依赖服务端 `name='默认分类'` 的解析,必须保证测试模板挂在「服务会解析到的那一行」上。
     * select-or-create:复用已存在的默认分类(seed 通常已有一行),没有才新建,避免出现两行同名导致 firstResult() 歧义。
     */
    private UUID defaultCategoryId() {
        var rs = em.createNativeQuery(
                "SELECT id FROM product_category WHERE name = '默认分类' LIMIT 1").getResultList();
        return rs.isEmpty() ? newCategory("默认分类") : (UUID) rs.get(0);
    }

    /** quotation.sales_rep_id 是 NOT NULL FK → "user"(id),需预置一个销售。 */
    private UUID newSalesRep() {
        UUID id = UUID.randomUUID();
        String suffix = id.toString().substring(0, 8);
        em.createNativeQuery(
                "INSERT INTO \"user\"(id, username, full_name, email, password_hash, role, status, " +
                "is_first_login, failed_login_attempts, created_at, updated_at) " +
                "VALUES (:id, :u, '自动测试用户', :e, 'x', 'SALES_REP', 'ACTIVE', false, 0, NOW(), NOW())")
                .setParameter("id", id)
                .setParameter("u", "ad_" + suffix)
                .setParameter("e", "ad_" + suffix + "@test.local")
                .executeUpdate();
        return id;
    }

    /** 插一条模板版本;customerId 传 null = 通用;publishedOffsetSec 控制 published_at 先后。 */
    private UUID newTemplate(UUID seriesId, UUID categoryId, UUID customerId,
                            String kind, String status, String version, int publishedOffsetSec) {
        UUID id = UUID.randomUUID();
        em.createNativeQuery(
                "INSERT INTO template(id, template_series_id, name, version, category_id, customer_id, " +
                "template_kind, status, template_sql_views_snapshot, formulas, product_attributes, subtotal_formula, " +
                "published_at, created_at, updated_at) " +
                "VALUES (:id, :sid, :name, :ver, :cat, :cust, :kind, :status, '{}', '[]', '[]', '[]', " +
                "NOW() + (:off || ' seconds')::interval, NOW(), NOW())")
                .setParameter("id", id)
                .setParameter("sid", seriesId)
                .setParameter("name", "T_" + version)
                .setParameter("ver", version)
                .setParameter("cat", categoryId)
                .setParameter("cust", customerId)
                .setParameter("kind", kind)
                .setParameter("status", status)
                .setParameter("off", publishedOffsetSec)
                .executeUpdate();
        return id;
    }

    private void newQuotation(UUID customerId, UUID customerTemplateId, int createdOffsetSec) {
        UUID rep = newSalesRep();
        UUID id = UUID.randomUUID();
        // quotation NOT NULL 列: quotation_number(unique)/customer_id/name/sales_rep_id/status/tax_rate/tax_amount/时间戳
        em.createNativeQuery(
                "INSERT INTO quotation(id, quotation_number, customer_id, name, sales_rep_id, customer_template_id, " +
                "status, tax_rate, tax_amount, created_at, updated_at) " +
                "VALUES (:id, :qn, :cust, 'AD报价单', :rep, :tpl, 'DRAFT', 13, 0, " +
                "NOW() + (:off || ' seconds')::interval, NOW())")
                .setParameter("id", id)
                .setParameter("qn", "ADQ_" + id.toString().substring(0, 8))
                .setParameter("cust", customerId)
                .setParameter("rep", rep)
                .setParameter("tpl", customerTemplateId)
                .setParameter("off", createdOffsetSec)
                .executeUpdate();
    }

    // ---- 6 分支 ----

    @Test
    @TestTransaction
    void branch1_hasHistory_picksLatestPublishedOfUsedSeries() {
        UUID cat = newCategory("AD分类1");
        UUID cust = newCustomer();
        UUID series = UUID.randomUUID();
        UUID v1 = newTemplate(series, cat, cust, "QUOTATION", "PUBLISHED", "v1", 0);
        UUID v2 = newTemplate(series, cat, cust, "QUOTATION", "PUBLISHED", "v2", 100); // 同线更新版本
        newQuotation(cust, v1, 0); // 上次用 v1
        em.flush();

        QuoteImportAutoDefaults d = templateService.computeAutoDefaults(cust);

        assertEquals("LAST_USED", d.customerTemplateSource);
        assertEquals(v2, d.customerTemplateId, "应取该线最新发布版本 v2,而非历史所用 v1");
        assertEquals(cat, d.categoryId);
    }

    // 设计:branch2~4 用「历史钉到隔离分类 + 上次线归档」走 fallback 路径,categoryId 完全受控,
    // 不依赖共享「默认分类」内容,避免 seed/其他数据污染断言。branch5 专门验证「无历史→解析默认分类」。

    @Test
    @TestTransaction
    void branch2_archivedSeries_fallbackCustomerSpecific() {
        UUID cat = newCategory("AD分类2");
        UUID cust = newCustomer();
        UUID archived = newTemplate(UUID.randomUUID(), cat, cust, "QUOTATION", "ARCHIVED", "v1", 0);
        newQuotation(cust, archived, 0); // 上次用的线现已整条归档
        UUID specific = newTemplate(UUID.randomUUID(), cat, cust, "QUOTATION", "PUBLISHED", "v1", 50);
        em.flush();

        QuoteImportAutoDefaults d = templateService.computeAutoDefaults(cust);

        assertEquals("CUSTOMER_SPECIFIC_FALLBACK", d.customerTemplateSource);
        assertEquals(specific, d.customerTemplateId);
        assertEquals(cat, d.categoryId, "分类仍从历史模板反推");
    }

    @Test
    @TestTransaction
    void branch3_archivedSeries_onlyGeneral_fallbackGeneral() {
        UUID cat = newCategory("AD分类3");
        UUID cust = newCustomer();
        UUID archived = newTemplate(UUID.randomUUID(), cat, cust, "QUOTATION", "ARCHIVED", "v1", 0);
        newQuotation(cust, archived, 0);
        UUID general = newTemplate(UUID.randomUUID(), cat, null, "QUOTATION", "PUBLISHED", "G1", 50);
        em.flush();

        QuoteImportAutoDefaults d = templateService.computeAutoDefaults(cust);

        assertEquals("GENERAL_FALLBACK", d.customerTemplateSource);
        assertEquals(general, d.customerTemplateId);
        assertEquals(cat, d.categoryId);
    }

    @Test
    @TestTransaction
    void branch4_archivedSeries_nothingElse_none() {
        UUID cat = newCategory("AD分类4");
        UUID cust = newCustomer();
        UUID archived = newTemplate(UUID.randomUUID(), cat, cust, "QUOTATION", "ARCHIVED", "v1", 0);
        newQuotation(cust, archived, 0);
        em.flush();

        QuoteImportAutoDefaults d = templateService.computeAutoDefaults(cust);

        assertEquals("NONE", d.customerTemplateSource);
        assertNull(d.customerTemplateId);
        assertEquals(cat, d.categoryId, "分类仍从历史模板反推");
    }

    @Test
    @TestTransaction
    void branch5_noHistory_resolvesDefaultCategory_multipleSpecific_picksLatest() {
        UUID cat = defaultCategoryId(); // 服务在无历史时会解析到这一行
        UUID cust = newCustomer();       // 全新客户:其唯一的客户专属就是下面两条, 不会被其他数据遮蔽
        newTemplate(UUID.randomUUID(), cat, cust, "QUOTATION", "PUBLISHED", "A", 0);
        UUID newer = newTemplate(UUID.randomUUID(), cat, cust, "QUOTATION", "PUBLISHED", "B", 100);
        em.flush();

        QuoteImportAutoDefaults d = templateService.computeAutoDefaults(cust);

        assertEquals(cat, d.categoryId, "无历史应解析到默认分类");
        assertEquals("CUSTOMER_SPECIFIC_FALLBACK", d.customerTemplateSource);
        assertEquals(newer, d.customerTemplateId, "无历史多条客户专属取最新发布");
    }

    @Test
    @TestTransaction
    void branch6_costing_specificBeatsGeneral_andNone() {
        // 用历史把 categoryId 钉到隔离分类 cat, quote 侧给个已发布模板让其有历史
        UUID cat = newCategory("AD分类6");
        UUID cust = newCustomer();
        UUID q = newTemplate(UUID.randomUUID(), cat, cust, "QUOTATION", "PUBLISHED", "Q", 0);
        newQuotation(cust, q, 0);
        newTemplate(UUID.randomUUID(), cat, null, "COSTING", "PUBLISHED", "CG", 0);          // 通用核价
        UUID custCosting = newTemplate(UUID.randomUUID(), cat, cust, "COSTING", "PUBLISHED", "CC", 10); // 客户专属核价
        em.flush();

        QuoteImportAutoDefaults d = templateService.computeAutoDefaults(cust);

        assertEquals(cat, d.categoryId);
        assertEquals("CUSTOMER_SPECIFIC", d.costingTemplateSource);
        assertEquals(custCosting, d.costingTemplateId, "客户专属核价优先于通用");

        // 另一客户:历史钉到隔离分类 cat2(无核价模板)→ 核价 NONE
        UUID cat2 = newCategory("AD分类6b");
        UUID cust2 = newCustomer();
        UUID q2 = newTemplate(UUID.randomUUID(), cat2, cust2, "QUOTATION", "PUBLISHED", "Q2", 0);
        newQuotation(cust2, q2, 0);
        em.flush();

        QuoteImportAutoDefaults d2 = templateService.computeAutoDefaults(cust2);
        assertEquals("NONE", d2.costingTemplateSource);
        assertNull(d2.costingTemplateId);
    }
}
```

> **fixture 已核对的约束**(基于 V1/V7/V11 建表 + 实体):
> - `quotation`:`quotation_number`(unique NOT NULL)、`customer_id`→FK customer、`sales_rep_id`→**FK "user" NOT NULL**(故 `newSalesRep()` 预置)、`name`、`status`、`tax_rate`、`tax_amount` 均 NOT NULL,已补全。
> - `customer`:`code`(unique)、`level`、`accumulated_amount`、`status`、`version`(@Version)NOT NULL,已补全。
> - `template`:`template_series_id` / `name` / `status` NOT NULL;`template_sql_views_snapshot`='{}'、`formulas`/`product_attributes`/`subtotal_formula`='[]';`status` CHECK 仅允许 DRAFT/PUBLISHED/ARCHIVED(fixture 用 PUBLISHED/ARCHIVED ✓);legacy `category` 列留 NULL(CHECK 允许)。
> - 实现时若 Quarkus 启动后报某列 NOT NULL 缺失(后续 Flyway 给 template/quotation 加过无默认列),按报错列名补进对应 helper 的 INSERT 即可。

- [ ] **Step 3: 跑测试确认失败**

Run: `cd cpq-backend && ./mvnw -o test -Dtest=AutoDefaultsServiceTest`
Expected: 6 个测试 FAIL(桩返回 null → NPE / 断言失败)

- [ ] **Step 4: 实现 `computeAutoDefaults`**

用以下实现替换 Step 1 的桩方法:

```java
public com.cpq.template.dto.QuoteImportAutoDefaults computeAutoDefaults(UUID customerId) {
    if (customerId == null) {
        throw new BusinessException("customerId 不能为空");
    }
    QuoteImportAutoDefaults out = new QuoteImportAutoDefaults();

    // 1. 最近一张报价单(customerTemplateId 非空),其模板仍存在者优先,createdAt DESC
    List<Quotation> quotes = Quotation.list(
            "customerId = ?1 AND customerTemplateId IS NOT NULL ORDER BY createdAt DESC",
            customerId);
    Template lastTemplate = null;
    for (Quotation q : quotes) {
        Template t = Template.findById(q.customerTemplateId);
        if (t != null) { lastTemplate = t; break; }
    }

    // 2. 分类:有历史从模板反推,无历史退「默认分类」
    UUID categoryId;
    if (lastTemplate != null) {
        categoryId = lastTemplate.categoryId;
    } else {
        ProductCategory def = ProductCategory.find("name = '默认分类'").firstResult();
        categoryId = def != null ? def.id : null;
    }
    out.categoryId = categoryId;
    if (categoryId != null) {
        ProductCategory cat = ProductCategory.findById(categoryId);
        out.categoryName = cat != null ? cat.name : null;
    }

    // 3. 有历史 → 取上次使用线的最新 PUBLISHED 版本(LAST_USED)
    if (lastTemplate != null) {
        Template latest = Template.find(
                "templateSeriesId = ?1 AND status = 'PUBLISHED' ORDER BY publishedAt DESC NULLS LAST",
                lastTemplate.templateSeriesId).firstResult();
        if (latest != null) {
            out.customerTemplateId = latest.id;
            out.customerTemplateSeriesId = latest.templateSeriesId;
            out.customerTemplateName = latest.name;
            out.customerTemplateVersion = latest.version;
            out.customerTemplateSource = "LAST_USED";
        }
        // latest == null(整条归档)→ 落入第 4 步兜底
    }

    // 4. 兜底:无历史 / 线失效 → 复用现有匹配(客户专属优先 + publishedAt DESC)
    if (out.customerTemplateId == null) {
        if (categoryId != null) {
            com.cpq.template.dto.TemplateMatchResult match =
                    matchCustomerQuoteTemplate(customerId, categoryId);
            if (match.matchType != com.cpq.template.dto.TemplateMatchResult.MatchType.NONE
                    && match.templates != null && !match.templates.isEmpty()) {
                TemplateDTO first = match.templates.get(0); // specific 优先, 再 publishedAt DESC
                out.customerTemplateId = first.id;
                out.customerTemplateSeriesId = first.templateSeriesId;
                out.customerTemplateName = first.name;
                out.customerTemplateVersion = first.version;
                boolean specific = first.customerId != null && first.customerId.equals(customerId);
                out.customerTemplateSource = specific ? "CUSTOMER_SPECIFIC_FALLBACK" : "GENERAL_FALLBACK";
            } else {
                out.customerTemplateSource = "NONE";
            }
        } else {
            out.customerTemplateSource = "NONE";
        }
    }

    // 5. 核价模板(独立,不记忆):客户专属优先 → publishedAt DESC
    if (categoryId != null) {
        Template costing = Template.find(
                "categoryId = ?1 AND templateKind = 'COSTING' AND status = 'PUBLISHED' "
                        + "AND (customerId = ?2 OR customerId IS NULL) "
                        + "ORDER BY CASE WHEN customerId = ?2 THEN 0 ELSE 1 END, publishedAt DESC NULLS LAST",
                categoryId, customerId).firstResult();
        if (costing != null) {
            out.costingTemplateId = costing.id;
            out.costingTemplateName = costing.name;
            out.costingTemplateVersion = costing.version;
            boolean specific = costing.customerId != null && costing.customerId.equals(customerId);
            out.costingTemplateSource = specific ? "CUSTOMER_SPECIFIC" : "GENERAL";
        } else {
            out.costingTemplateSource = "NONE";
        }
    } else {
        out.costingTemplateSource = "NONE";
    }

    return out;
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd cpq-backend && ./mvnw -o test -Dtest=AutoDefaultsServiceTest`
Expected: 6 个测试全部 PASS

- [ ] **Step 6: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/template/service/TemplateService.java \
        cpq-backend/src/test/java/com/cpq/template/AutoDefaultsServiceTest.java
git commit -m "feat(template): computeAutoDefaults 推导导入默认模板(上次线最新版+核价最新)"
```

---

## Task 3: 后端端点 `GET /auto-defaults`

**Files:**
- Modify: `cpq-backend/src/main/java/com/cpq/template/resource/TemplateResource.java`

- [ ] **Step 1: 加端点**

在 `match-customer-quote` 端点之后插入:

```java
    @GET
    @Path("/auto-defaults")
    public ApiResponse<com.cpq.template.dto.QuoteImportAutoDefaults> autoDefaults(
            @QueryParam("customerId") UUID customerId) {
        return ApiResponse.success(templateService.computeAutoDefaults(customerId));
    }
```

- [ ] **Step 2: 触发 Quarkus 重启并验证端点存活**

```bash
touch cpq-backend/src/main/java/com/cpq/template/resource/TemplateResource.java
sleep 7
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8081/api/cpq/templates/auto-defaults?customerId=00000000-0000-0000-0000-000000000000"
```
Expected: `200`(空客户返各 source=NONE 的 DTO,不报 500)或 `401`(鉴权拦截,均非 500)

- [ ] **Step 3: 用真实客户验证返回结构**

```bash
# 取一个已有报价单的客户 id 替换 <CID>,带上有效 token(参考其他 curl 调试)
curl -s "http://localhost:8081/api/cpq/templates/auto-defaults?customerId=<CID>" | head -c 800
```
Expected: JSON 含 `categoryId / customerTemplateId / customerTemplateSource / costingTemplateId / costingTemplateSource` 字段

- [ ] **Step 4: Commit**

```bash
git add cpq-backend/src/main/java/com/cpq/template/resource/TemplateResource.java
git commit -m "feat(template): 新增 GET /templates/auto-defaults 端点"
```

---

## Task 4: 前端 `QuotationCreateForm` 增加 hint props

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/QuotationCreateForm.tsx`

- [ ] **Step 1: Props 接口增加两个可选字段**

在 `interface Props`(约 51 行)内补两行:

```ts
  /** 报价模板自动带出来源提示(如「上次使用 · 最新版 v3」);readOnly 时不显示 */
  customerTemplateHint?: string;
  /** 核价模板自动带出来源提示 */
  costingTemplateHint?: string;
```

并在组件解构(约 65 行)加入:

```ts
  customerTemplateHint,
  costingTemplateHint,
```

- [ ] **Step 2: 报价模板 Form.Item 标签旁渲染提示**

在报价模板 `Form.Item` 的 `label` 的 `<span>` 内,版本/匹配 Tag 之后追加(约 304 行 `</span>` 之前):

```tsx
              {!readOnly && customerTemplateHint && (
                <span style={{ marginLeft: 8, fontWeight: 400, fontSize: 12, color: '#999' }}>
                  {customerTemplateHint}
                </span>
              )}
```

- [ ] **Step 3: 核价模板 Form.Item 标签旁渲染提示**

在核价模板 `Form.Item` 的 `label` 的 `<span>` 内,`{(() => {...})()}` 之后追加(约 354 行):

```tsx
              {!readOnly && costingTemplateHint && (
                <span style={{ marginLeft: 8, fontWeight: 400, fontSize: 12, color: '#999' }}>
                  {costingTemplateHint}
                </span>
              )}
```

- [ ] **Step 4: TS 检查 + Vite transform 自检**

```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/QuotationCreateForm.tsx
```
Expected: tsc 0 错误;curl `200`

- [ ] **Step 5: Commit**

```bash
git add cpq-frontend/src/pages/quotation/QuotationCreateForm.tsx
git commit -m "feat(quotation): QuotationCreateForm 增加模板自动带出来源提示 props"
```

---

## Task 5: 前端 Drawer 进 Step 2 拉 auto-defaults 预填

**Files:**
- Modify: `cpq-frontend/src/pages/quotation/QuoteBasicDataImportV6Drawer.tsx`

- [ ] **Step 1: 新增 hint 状态 + source→文案映射**

在组件 state 区(约 68 行 `committing` 之后)加入:

```ts
  const [autoHints, setAutoHints] = useState<{ customer?: string; costing?: string }>({});
  const [enteringStep2, setEnteringStep2] = useState(false);
```

在组件函数体内(`handleUpload` 之前)加入纯函数:

```ts
  const quoteHintOf = (source?: string, version?: string): string | undefined => {
    switch (source) {
      case 'LAST_USED': return `上次使用 · 最新版${version ? ' ' + version : ''}`;
      case 'CUSTOMER_SPECIFIC_FALLBACK': return '无历史 · 客户专属最新';
      case 'GENERAL_FALLBACK': return '无历史 · 通用最新';
      default: return undefined; // NONE 不显示,沿用表单内「未找到模板」Alert
    }
  };
  const costingHintOf = (source?: string, version?: string): string | undefined => {
    switch (source) {
      case 'CUSTOMER_SPECIFIC': return `客户专属 · 最新${version ? ' ' + version : ''}`;
      case 'GENERAL': return `通用 · 最新${version ? ' ' + version : ''}`;
      default: return undefined;
    }
  };
```

- [ ] **Step 2: 进 Step 2 的处理函数(拉默认值再切步骤)**

在 `handleUpload` 之后加入:

```ts
  const enterStep2 = async () => {
    if (!customerId) return;
    setEnteringStep2(true);
    try {
      const resp: any = await api.get('/templates/auto-defaults', { params: { customerId } });
      const d = resp?.data ?? resp; // axios 拦截器已返回 ApiResponse body, payload 在 .data
      setCreateForm({
        name: createForm.name || `${customerName} 报价单`,
        categoryId: d?.categoryId ?? undefined,
        customerTemplateId: d?.customerTemplateId ?? undefined,
        costingTemplateId: d?.costingTemplateId ?? undefined,
      });
      setAutoHints({
        customer: quoteHintOf(d?.customerTemplateSource, d?.customerTemplateVersion),
        costing: costingHintOf(d?.costingTemplateSource, d?.costingTemplateVersion),
      });
    } catch {
      // 静默降级:不预填, 表单走现状默认分类 + 手选(不阻断流程)
      setAutoHints({});
    } finally {
      setEnteringStep2(false);
      setStep(2);
    }
  };
```

- [ ] **Step 3: 重置时清空 hint**

在 `useEffect(open)` 重置块(约 76 行,`setCreateForm({...})` 之后)加入:

```ts
    setAutoHints({});
```

- [ ] **Step 4: 「下一步」按钮改调 `enterStep2`**

将 footer 中「下一步:选模板 + 建报价单」按钮(约 194 行)的:

```tsx
              onClick={() => setStep(2)}
```
改为:
```tsx
              loading={enteringStep2}
              onClick={enterStep2}
```

- [ ] **Step 5: 把 hint 传给表单**

`QuotationCreateForm`(约 310 行)的 props 追加两行:

```tsx
            customerTemplateHint={autoHints.customer}
            costingTemplateHint={autoHints.costing}
```

- [ ] **Step 6: TS 检查 + Vite transform 自检**

```bash
cd cpq-frontend && npx tsc --noEmit -p tsconfig.json
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:5174/src/pages/quotation/QuoteBasicDataImportV6Drawer.tsx
```
Expected: tsc 0 错误;curl `200`

- [ ] **Step 7: Commit**

```bash
git add cpq-frontend/src/pages/quotation/QuoteBasicDataImportV6Drawer.tsx
git commit -m "feat(quotation): 导入向导进 Step 2 自动带出报价/核价模板 + 分类"
```

---

## Task 6: 端到端手动流程验证

**Files:** 无(验证步骤)

- [ ] **Step 1: 真实导入一次**

打开报价单列表 → 进入「基础数据导入」抽屉 → 选一个**有历史报价单**的客户 → 上传一个 .xlsx → 开始导入 → 成功后点「下一步」。

- [ ] **Step 2: 核对自动带出**

进入 Step 2 时,确认:
- 产品分类、报价模板、核价模板三项**均已自动填好**;
- 报价模板旁显示「上次使用 · 最新版 …」;核价旁显示「客户专属/通用 · 最新」;
- 报价模板与该客户**最近一张报价单所用的模板线**一致(取该线最新发布版本)。

- [ ] **Step 3: 改分类回归**

手动把产品分类切到另一个分类 → 确认报价模板/核价模板按新分类**正确重匹配**(不卡死、不残留旧值)。切回原分类 → 确认仍能选回。

- [ ] **Step 4: 无历史客户回归**

换一个**全新无报价单**客户重复导入 → Step 2 应退「默认分类」+(若有)客户专属最新/通用最新;若完全无模板 → 报价模板留空 + 现有「未找到模板」Alert 正常显示。

- [ ] **Step 5: 编辑态回归**

打开任一已生成报价单的编辑页 → 确认 `QuotationCreateForm` 在 `readOnly` 下三项锁定、**不显示**自动带出提示文案、行为与改动前一致。

- [ ] **Step 6: 记录开发记录**

向 `docs/RECORD.md` 追加一行:
`[2026-06-03] 报价导入 - 选模板步骤自动带出(上次使用报价线最新版+核价最新+上次分类) | QuoteImportAutoDefaults/TemplateService.computeAutoDefaults/TemplateResource/QuotationCreateForm/QuoteBasicDataImportV6Drawer | 主规则=有历史跟随上次模板线,客户专属优先仅无历史兜底;核价不记忆`

```bash
git add docs/RECORD.md
git commit -m "docs(record): 报价导入选模板自动带出开发记录"
```

---

## 自检对照(实现完成后逐项确认)

- [ ] 后端:`AutoDefaultsServiceTest` 6 测试全 PASS
- [ ] 后端:`/q/health` 200 + `/templates/auto-defaults` 非 500
- [ ] 前端:`tsc --noEmit` 0 错误
- [ ] 前端:两个改动 `.tsx` 各 Vite 200
- [ ] 手动:有历史客户三项自动带出且与历史一致;改分类正确重匹配;无历史退默认分类兜底;编辑态不受影响
- [ ] **完成宣告必须含一行「已自检」声明**(见 CLAUDE.md)
