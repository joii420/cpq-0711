# 导入前置清单（待用户操作）

> 生成于 2026-09-03，全部结论均为**实测**（Excel 逐 cell 解析 + 共享库 `cpq_db_0724` 逐值查询），不是推断。
> 依据裁决 **D-20「主数据存在性校验全部保持严格，先补主数据」** 与 **D-23「占位行按现规则报错拒收」**。
> 🚩 **这份清单不做完，`AC-11` / `AC-36` 等「导入成功」类验收跑不通** —— 模板会被自己的校验拒收。

---

## A. 三份 Excel 要改的 5 处

### A-1 🔴 `核价1 - 数据导入与表格建表.xlsx` —— 文件损坏

```
file 判定：data（不是 xlsx）
文件头：87 7d 1c bc      （zip 应为 50 4b 03 04）
openpyxl：BadZipFile: File is not a zip file
```

**动作**：在 Excel / WPS 里打开原件，**重新另存覆盖**。
**影响**：详细核价 19 张表的字段矩阵目前取自 2026-09-02 23:18 的成功读取 + **人工转录**，尚未机器复核；
`AC-26`（详细核价 17 个 tab）与 `AC-34`（sheet「产能」不属于基础核价）两条验收因此**阻塞**。
修好后我会重跑 `gen_matrix.py` 机器复核。

### A-2 🔴 `报价 - …xlsx` · 「客户料号」sheet —— 补一列「客户编号」

**动作**：新增一列，**红底必填**，建议放在「客户产品编号」**之前**。
**依据**：D-18。现有真正在用的 `material_customer_map`（1877 行）有 `customer_no`，且唯一约束
`uq_mcm_quote_cust_prod (system_type, customer_no, customer_product_no)` 把客户编号当身份的一部分。
不补这一列，新表既回答不了「这条属于哪个客户」，唯一约束也比现有系统弱（森萨塔跨客户串号同型风险）。

### A-3 🔴 `报价 - …xlsx` · 「来料回收折扣」第 3 行 —— 残行

```
R3: [None, None, '外购件', None, None, None, None]
     ↑ 销售料号（轴）为空，只有「投入料号」写着「外购件」
```

**动作**：**删掉这一行**，或把销售料号等必填列补全。
**影响**：按 D-7「轴列一律必填」，这一行会让**整份报价文件 400 拒收**。

### A-4 🔴 `核价2 - …xlsx` · 「物料与元素BOM」第 10~16 行 —— 占位行（共 7 行）

```
第10行 轴=3120014539    第11、12行 轴=2120011658
第13、14行 轴=3110520789  第15、16行 轴=2120011659
（元素代码 / 组成含量 等全空）
```

**动作**：**删掉这 7 行**。
**依据**：D-23 你已裁决「按现规则报错拒收」，后端正在移除跳过分支。留着会报 **28 处「必填项为空」整份拒收**。

### A-5 🔴 三份文件的「物料」sheet 都没登记齐轴值（**2026-09-03 重写**，取代原「S- 前缀口径」条目）

> ⚠️ **本条原先写成「`S-3120014539` 与 `3120014539` 口径不一致」是我判断错了** —— 用户指出那就是两个不同的料号，
> 不该由我判断是否相似。撤回该描述。真正的问题是下面这个，且**三份文件都有**。

**判据（裁决 D-24）**：`「物料」sheet 的料号集合  ⊇  全部带版本 sheet 的轴值集合`
不满足 → 导入报 `轴值未在物料表登记`、**整份拒收**。

**为什么必须拦**：维护页列表的数据源就是物料表（`api.md §3`）。轴值没登记的话，数据能落库，
但该料号在界面上**完全不可见** —— 列表里没有它 → 抽屉打不开 → 9/17 个 tab 一个都看不到。

#### 逐份实测缺口

| 文件 | 「物料」sheet 现登记 | 带版本表轴值 | 需补进物料 sheet |
|---|---|---|---|
| **报价** | **0 个（完全空）** | 4 个 | `202601011226`（物料BOM 等 6 处）· `5550C1649001`（自制加工费）· `S-3120014539`（成品其他费用等 7 处）· `S-80011`（电镀费用） |
| **核价2（基础）** | 1 个：`S-3120014539` | 4 个 | `3120014539`（29 处）· `2120011658`（2 处）· `2120011659`（2 处）· `3110520789`（2 处） |
| **核价1（详细）** | 1 个：`S-3120014539` | 4 个 | 同核价2（`3120014539` 出现 **52** 处） |

**动作**：在每份文件的「物料」sheet 里把上表右列的料号各补一行（品名等必填列一并填）。
核价两份现有的 `S-3120014539` 那行**保留或删除都可以** —— 它只是一条没有任何带版本数据的孤立料号。

#### 📋 可直接照抄的补录内容（品名等已从库中查实，2026-09-03）

**报价「物料」sheet** —— 现在**完全空**（只有表头），需补 **4 行**。必填列：`销售料号`🔴 `品名`🔴

| 销售料号 | 品名 | 规格 | 尺寸 | 生产料号 |
|---|---|---|---|---|
| `202601011226` | ⚠️ **库中无，请自行填写** | | | |
| `5550C1649001` | ⚠️ **库中无，请自行填写** | | | |
| `S-3120014539` | `主料1` | | `3.5×3.5×0.6` | `3120014539` |
| `S-80011` | ⚠️ **库中无，请自行填写** | | | |

> 三个「库中无」的是模板自造的示例料号，`material_master` 里查不到。品名随便填一个可辨识的即可
> （如 `示例产品-202601011226`），它不参与任何校验，只是红底必填不能为空。

**核价2 与 核价1 的「物料」sheet** —— 现各只有 `S-3120014539` 一行，需补 **4 行**。必填列：`生产料号`🔴 `品名`🔴

| 生产料号 | 品名 | 尺寸 |
|---|---|---|
| `3120014539` | `主料1` | `3.5×3.5×0.6` |
| `2120011658` | `AgNi10/Cu-Cu/301/Cu接触桥` | |
| `2120011659` | `AgNi10/Cu-Cu/301/Cu接触桥` | |
| `3110520789` | `AgNi10/Cu动触头` | |

> 这 4 个品名**都是从 `material_master` 查实的真实值**，直接抄即可。
> 原有的 `S-3120014539` 那行留着不影响（它只是一条没有任何带版本数据的孤立料号）。

**已隔离验证**：后端用「原始模板 + 仅在物料 sheet 补登记这 4 个轴值」做了单变量实验 →
`HTTP 200`、`物料 inserted=4 updated=1`、10 个 sheet 全通、耗时 665ms。
⇒ **只要补齐这一处，整份模板立即导得进；剩余阻塞 100% 在 Excel 内容，不在代码。**

---

## B. 主数据要补的 10 条

> 严格校验下，模板引用但库里查不到的值会导致**整份拒收**。以下为逐值实测结果。

### B-1 🔴 工序 —— `process_master` 现有 **4 行**，模板引用 **5 个全部缺失**

| 工序编号 | 模板中出现的位置 | 库中 |
|---|---|---|
| `Z002` | 核价2 物料BOM / 包装 | ❌ |
| `Z008` | 核价2 | ❌ |
| `Z053` | 核价2 产能/折旧/能耗/模具/耗材/加工费 等 | ❌ |
| `Z490` | 核价2 | ❌ |
| `Z611` | 核价2 其他外加工成本（无缝焊接） | ❌ |

`process_master` 现有全部 4 行：`Z100 焊接` / `Z101 铆接` / `TP10 测试工序10` / `TP20 测试工序20`
（后两条是测试数据）。

⚠️ **这是最严重的一条**：详细核价有 **8 张 sheet** 带工序编号，不补则**一行都导不进**。

### B-2 🔴 元素 —— `element` 现有 45 行，缺 **4 个**

| 值 | 库中 | 说明 |
|---|---|---|
| `电解铜` / `线材` / `钢板` / `锌锭` | ❌ 全缺 | 报价「物料与元素BOM」的「元素」列 |
| `Cu` / `Ag` / `Ni` / `301` / `白银` | ✅ | 核价2 用代码、报价的「白银」恰好命中 |

📌 **一处语义提示**（你已裁决仍严格，此处仅备查）：`element` 表的中文名是**元素名**（铜/银/铝/铍/碳），
而报价这一列装的是**投入材料的商品名与形态**。现有 `Q04ElementBomHandler` 把它读作 `componentNo`、**从不校验**。
把「线材」「钢板」录入元素表在语义上是扩张了该表的定义 —— 按你的裁决执行，但建议给它们一个可辨识的分类标记。

### B-3 🔴 材质 —— `material_recipe` 现有 263 行，缺 **1 个**

| 值 | 库中 |
|---|---|
| `991` | ❌ 缺失 |
| `00006`（AgNi10）/ `00168`（301/Cu/301）/ `992`（AgNi11#-Ⅰ） | ✅ |

### B-4 🔴 客户 —— `customer` 现有 40 行，现网 19 个客户编号中 **16 个未登记**

| 客户编号 | 库中 |
|---|---|
| `CUST-0004`（正泰）/ `CUST-0001`（罗克韦尔）/ `CUST-0002`（测试客户） | ✅ |
| `8000142` ~ `8000155`（芜湖德力西，共 14 个）、`Q13CUST0617`、`C1` | ❌ 全缺 |

⚠️ A-2 补上「客户编号」列后，填进去的值必须在 `customer.code` 中存在，否则整份拒收（D-19）。

---

## C. 完成后我会做什么

1. 重跑 `gen_matrix.py` 机器复核 `字段矩阵.md`（解除 A-1 阻塞）
2. 通知测试代理解除 `AC-26 / 34`（核价1）与 `AC-26/27/28/30`（`S-` 前缀）两组阻塞项
3. 在主线亲验里跑通 `quote` 与 `cost-detail` 两条完整导入路径（当前只有 `cost-basic` 全链路验通）

---

## D. 不阻塞本清单的已完成项（备查）

- ✅ 84 张表建表 + 三套 Registry + 启动期自检（含 6 处漂移注入的负向对照）
- ✅ 行指纹 + 通用升版写入器 + 两阶段导入器（升版序列 9 步全绿，含行序对调/改项次/尾零三条不升版）
- ✅ 7 个维护端点 + 乐观锁（两会话争锁实测）+ 权限
- ✅ 性能：2000 行 / 200 料号 **1053ms**；SQL 条数与料号数无关（UNCHANGED 路径 20 与 200 料号同为 3 条）
- ✅ 51 条 AC 的用例已写全，双向覆盖无缺行
