# 回归执行手册 · 产品管理页重做

> 任务：`task-260903-产品管理页重做`　|　编写：`cpq-tester`　|　日期：2026-09-03
> 覆盖 `test.md §5` 的 **RG-1~RG-6** 与 `backtask.md §3` 的 **G-1~G-5**。
> 本文只写**怎么执行 + 期望值哪来的**。执行结果写 `test-report.md`，不写这里。

---

## 0. 执行前提（不满足就别开跑，否则结论不可采信）

```bash
# 1) 后端存活（⚠️ /q/health 返 404 是正常的，它不是健康探针）
curl -s --noproxy '*' -o /dev/null -w '%{http_code}\n' http://localhost:8081/api/cpq/components   # 期望 401
# 2) 前端存活
curl -s --noproxy '*' -o /dev/null -w '%{http_code}\n' http://localhost:5174/                     # 期望 200
```

⚠️ 本机 shell 常设 `http_proxy=127.0.0.1:7890`，探本机服务**必须加 `--noproxy '*'`**，否则走代理返 502
——那会被误读成「服务挂了」。

🚨 **跑 E2E 一律显式带 `PW_BASE_URL` / `PW_BACKEND_URL`**：
不带时 Playwright 的 `webServer` 与 `global-setup` 会去动共享的 5174，并对 `admin`/`alice`/`bob`
各发一次登录（**吃登录限流额度 30 次/分/IP**）。2026-09-03 实测踩到。

⚠️ **多个 vite 共用软链的 `node_modules/.vite` 会互相踢掉依赖预构建缓存**，表现为加载超时 / 白屏。
遇到诡异 timeout **先怀疑它，再怀疑实现**（主线 2026-09-03 通报，对方已实证这是真因不是产品 bug）。

---

## 1. 后端零改动回归 G-1 ~ G-5

后端本任务零改动，但摘除了两个页签的 UI 入口，需确认后端未受牵连。
**鉴权是 Cookie 不是 Bearer token** —— 登录响应体里没有 token，必须用 cookie jar：

```bash
J=/tmp/cpq-rg-cookies.txt; rm -f $J
curl -s --noproxy '*' -c $J -X POST http://localhost:8081/api/cpq/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"Admin@2026"}' -o /dev/null
```

| # | 命令 | 期望 | 2026-09-03 改动前实测基线 |
|---|---|---|---|
| **G-1** | `curl -s --noproxy '*' -b $J "http://localhost:8081/api/cpq/products?page=1&size=1"` | `code=200`，`total=3` | ✅ **`code=200 total=3`** |
| **G-2** | `curl -s --noproxy '*' -b $J "http://localhost:8081/api/cpq/material-masters?page=0&size=1"` | 见下方 🚨 | ✅ `code=200` **`total=42`** |
| **G-3** | 见下方 SQL | 与改动前一致 | `product=3` / `qli_with_product=0` / `ptb=1` / `pp=0` / `material_master=1889` |
| **G-4** | `curl -s --noproxy '*' -b $J http://localhost:8081/api/cpq/pricing-basic-data/parts?page=0&size=1` | 与改动前逐字节一致 | **未取基线**（见 §4 未验证项） |
| **G-5** | `curl -s --noproxy '*' -o /dev/null -w '%{http_code}' http://localhost:8081/api/cpq/components` | **401** | ✅ **401** |

### 🚨 G-2 的期望值：`backtask.md` 写错了，按本文修正

`backtask.md §3` 的 G-2 写「期望 `total` = **1889**」。**实测是 42。**

原因：`material_master` 表**总行数**确实是 1889，但其中 **1847 行是未提交报价单的 pending 占号影子行**，
`GET /api/cpq/material-masters` 已经在服务端过滤掉了 pending，只回落地的 42 行。

⇒ **照 `backtask.md` 的 1889 去比，会报出一个不存在的回归。** 期望值以本表为准（`total=42`）。
> 🚩 已报主线，待回写 `backtask.md`。

### G-3 SQL（只读）

```sql
SELECT (SELECT count(*) FROM product)                                          AS product_rows,
       (SELECT count(*) FROM quotation_line_item WHERE product_id IS NOT NULL) AS qli_with_product,
       (SELECT count(*) FROM product_template_binding)                         AS ptb,
       (SELECT count(*) FROM product_process)                                  AS pp,
       (SELECT count(*) FROM material_master)                                  AS mm_all;
```

连库：`PGPASSWORD=joii5231 psql -h 10.177.152.12 -U postgres -d cpq_db_0724`

> 🚩 **顺带更正一处依据**：`需求文档.md` ④ R-2 / 裁决 11 说「`product` 被
> `quotation_line_item` / `product_template_binding` / `product_process` **三张表外键引用**」——
> **外键约束确实是 3 条**（已核 `information_schema`），但**实际有引用行的只有 `product_template_binding` 1 行**，
> 另两张表的 `product_id` 引用行数是 **0**。
> ⇒ 结论（不删表）不变，但「删表会立刻断链」的实际量级是 **1 行**，不是三张表。写 `RECORD.md` 时别把量级说大。

---

## 2. 前端回归 RG-1 ~ RG-5（手工逐屏）

四个都是「本任务不该碰的地方，确认没碰到」。**每项都要留改动前/改动后两张截图**，
截图归档到 `dev-docs/task-260903-产品管理页重做/证据/`，🚫 不要留在 `test-results/`（下一轮开跑会被清空 ⇒ 等于没证据）。

| # | 回归项 | 执行路径 | 判据 |
|---|---|---|---|
| **RG-1** | 核价侧「料号核价」**仍可编辑** | `/master-data-hub` →「料号核价」→ 搜 `S-3120014539` → 点行开抽屉 | 抽屉内**有**「保存」按钮、单元格**有** `input`。🚨 这是 AC-9 的反向要求，**已在 `E2E-09` 里自动化**，手工只做二次确认 |
| **RG-2** | `/master-data-hub` 全部页签 | 逐个点开 6 个页签 | 均正常渲染，无红色遮罩、无「加载中…」永久占位 |
| **RG-3** | 报价单管理「从基础数据导入」 | 报价单管理 → 导入 | 行为零变化（写旧 V6 表，喂 107 个组件视图）。**本任务不调新导入端点** |
| **RG-4** | 报价单渲染主链路 | 打开一张既有报价单 | 组件与金额与改动前一致 |
| **RG-5** | 「产品分类管理」抽屉 | `/products-hub` → 右上角按钮 | 仍能打开、增删改分类正常。**已在 `E2E-01` 里断言了「能打开」**，增删改需手工 |

> 🚩 **RG-1 与 RG-5 已被 E2E 覆盖一部分**，但 E2E 只验「按钮在 / 抽屉能开」，
> **「保存后真的存进去了」「分类真的删掉了」这类写入行为仍须手工** —— 本套 E2E 是只读的，一行都不写库。

---

## 3. RG-6 既有 E2E —— 🚨 **必须 A/B 同型对比，不许拿绝对失败数下结论**

### 已知噪声

`quotation-flow.spec.ts` 在**干净 master 上恒 3 条失败**（夹具单缺产品分类 → Step1「下一步」禁用，
记忆条目 `task0712-update071501-category-axis`）。

⇒ **「跑出 3 个 failed」既可能是无回归，也可能是掩盖了一个新回归**（旧的 3 条修好了、新坏了 3 条，总数一样）。
**绝对失败数没有判别力，必须比对「失败的是不是同一批用例」。**

### 执行方法（两轮，同一时刻，只差分支）

```bash
# ── A 轮：对照组（主仓 master，不含本任务改动）──
cd /home/joii/project/cpq/cpq-frontend
PW_BASE_URL=<临时前端端口> PW_BACKEND_URL=<临时后端端口> \
  npx playwright test quotation-flow composite-product-flow \
  --reporter=json > /tmp/rg6-A-master.json 2>&1

# ── B 轮：实验组（本任务 worktree）──
cd /home/joii/project/cpq/.claude/worktrees/task-260903-product-hub/cpq-frontend
PW_BASE_URL=<临时前端端口> PW_BACKEND_URL=<临时后端端口> \
  npx playwright test quotation-flow composite-product-flow \
  --reporter=json > /tmp/rg6-B-task.json 2>&1

# ── 比对：只看「失败用例集合」的差集，不看数量 ──
python3 - <<'PY'
import json
def failed(p):
    d=json.load(open(p))
    out=set()
    def walk(s):
        for sp in s.get('suites',[]): walk(sp)
        for sp in s.get('specs',[]):
            if not sp.get('ok'): out.add(sp['title'])
    for s in d.get('suites',[]): walk(s)
    return out
A,B=failed('/tmp/rg6-A-master.json'),failed('/tmp/rg6-B-task.json')
print(f"A(master) 失败 {len(A)} 条")
print(f"B(task)   失败 {len(B)} 条")
print("\n🚨 B 独有（= 本任务引入的回归）：");  [print('  ',x) for x in sorted(B-A)] or print('   (无)')
print("\n✅ A 独有（= 本任务顺带修好的）：");  [print('  ',x) for x in sorted(A-B)] or print('   (无)')
print("\n⚪ 共同失败（= 既有噪声，与本任务无关）："); [print('  ',x) for x in sorted(A&B)]
PY
```

**判据（只有这一条）**：
- `B - A` 为空 ⇒ **无回归**（哪怕两边都失败 3 条）
- `B - A` 非空 ⇒ **每一条都要归因**，不许以「偶发，重跑一下就好」结案（`testing.md §4.1`）

### 两个执行纪律

1. **A/B 两轮必须在同一时刻附近跑**。`cpq_db_0724` 是共享库，`task-260902` 的
   `@QuarkusTest` 正在同库跑，隔几小时跑的两轮**数据基线已经不同**，差集会混入漂移噪声。
2. 🚫 **不要用 5174 / 8081 跑**（保留给主线亲验），且两轮要用**各自独立**的临时端口 ——
   共用一个端口的两轮会因 `node_modules/.vite` 预构建缓存互踢而随机超时。

---

## 4. 本任务新增 E2E 的执行方法

```bash
cd /home/joii/project/cpq/.claude/worktrees/task-260903-product-hub/cpq-frontend
PW_BASE_URL=http://localhost:<临时前端> \
PW_BACKEND_URL=http://localhost:<临时后端> \
PW_USER_PRICING=<PRICING_MANAGER 用户名> PW_PWD_PRICING=<口令> \
PW_USER_SALES=<SALES_REP 用户名>       PW_PWD_SALES=<口令> \
  npx playwright test product-hub-readonly --reporter=line
```

**开跑前它自己会挡三道**（都已做过证伪实验，确认会**硬失败**而不是静默跳过）：

| 守卫 | 触发条件 | 2026-09-03 证伪实测 |
|---|---|---|
| `assertIsolatedEnv()` | 没给 `PW_BASE_URL` / 指向 5174·8081 | ✅ 实测硬失败并打印原因 |
| `assertSampleDataLoaded()` | `ds_quote_material` 表不存在 or 0 行 | ✅ 实测硬失败：`前置数据为空（material=0 customerPart=0）` |
| `credOf()` | 缺角色测试账号 | 抛「测试环境缺陷」并点名**不许改成 skip** |

**跑完自动做的事**：`afterAll` 对 16 张 `ds_quote_*` 逐表比对「行数 + 内容 md5」，
变了即硬失败 —— 这是 AC-8 / AC-9「全只读」的**数据层证据**（界面上没有保存按钮 ≠ 后台没在写）。

### 🚨 共享库纪律（本套用例已遵守，改用例时不要破坏）

- 本套 **一行都不写库**，`product-hub.helpers.ts` 里**没有任何** `DELETE` / `UPDATE` / `TRUNCATE` 入口
- 列表总数断言走**不变量**（页面总数 == 同一时刻库中 `count(*)`），🚫 不写死绝对行数 ——
  `task-260902` 的夹具（`TEST-DS-` 前缀）与我们的真实料号在**同一批表**里，绝对值会漂移
- 主角料号相关计数按 `material_no='S-3120014539'` 过滤，与 `TEST-DS-` 前缀不重叠，天然不受漂移影响
- 🚫 清理数据时**绝不能用反向条件**（如 `WHERE material_no NOT LIKE 'TEST-DS-%'`）——
  那会把别人的数据一起带走，属 `CLAUDE.md §3.2` 数据销毁红线

---

## 5. `test.md §4` 四条证伪实验的执行前提

| # | 实验 | 现在能不能做 | 说明 |
|---|---|---|---|
| **FS-1** | 只读是真的吗 | ⏸ 待页面可跑 | 🚩 **做法需重述**：`test.md` 写的是「把 `editable={false}` 改成 `true`」，该做法预设复用 `EditableSheetTable`。实际前端交付了 `ReadonlySheetTable.tsx`（仅从文件名可知，未读实现）⇒ 干预点可能不同，**执行前须与前端确认改哪一处**，否则「改了没变红」会被误判成用例失效 |
| **FS-2** | 数字是真的吗 | ⏸ 待导入通道就绪 | 删 1 行重导 → `E2E-07` 须从 9 变 8 并失败 |
| **FS-3** | pending 过滤是真的吗 | ⏸ 待导入通道就绪 | 往 `ds_quote_material` 插 1 行 → 总数须变。⚠️ **这是写库**，须主线批准并登记清理条件（`WHERE material_no = <我们插的那个>`） |
| **FS-4** | 空态是真的吗 | ⏸ 待页面可跑 | 已被 `E2E-12` 覆盖（断言 `暂无数据` 且 **not** `加载中`） |

> 🔬 **还原实验纪律**：自己写的验证脚本**首次 PASS 也可能是空验证**。
> 本套的三道守卫已按此做过证伪（见 §4 表格），🚫 但 FS-1~FS-3 **尚未做**，不得在报告里写成「已验证」。
