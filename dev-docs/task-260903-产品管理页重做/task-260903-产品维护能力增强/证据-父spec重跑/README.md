# 父任务 spec 重跑产生的证据（2026-09-04）

> **这些不是父任务的交付证据**，是子任务做 A/B 回归时重跑父任务
> `product-hub-readonly.spec.ts` 产生的新结果，因该 spec 的 `shot()`/`evidence()`
> 写死了父任务的证据目录而误入其中，现移到此处。

## 为什么必须移走

父任务 `task-260903-产品管理页重做` 已于 2026-09-03 **闸门 B 验收通过并结案**，
其 `证据/` 目录里的文件是**交付当时的事实记录**，不是「这些断言现在仍成立」。

重跑结果与当时已不同，且差异都有正当来源：
- `E2E-AC14-cust0004.txt`：`ui=11 db=11` → `ui=12 db=12`
  —— `task-260902` 的导入给 `CUST-0004` 增加了 1 行客户料号
- 销售产品列数：7 → 8 —— 子任务 AC-8 新增产品分类列（见需求文档 R-0）

⇒ 用新结果覆盖旧证据，等于**篡改一份已验收的交付记录**。已 `git checkout` 恢复原状。

## 📌 遗留：spec 的证据落盘路径写死了父任务目录

父任务 spec 的 `shot()`/`evidence()` 仍指向父任务 `证据/`。
**下次任何人重跑它，还会再覆盖一次。** 根治需要把证据目录参数化（如读环境变量），
本次未做——那要改父任务已交付的测试件，属独立改动。
