====================================================================
AC-13 性能实测（dev 库 cpq_db_0724，只读，各 3 次取中位数）
====================================================================
A｜改动前：现网两参版函数 SELECT count(*) FROM f_material_element_price('CUST-0004','2026-08-30')
   23.275 ms / 18.730 ms / 15.154 ms   → 中位数 18.730 ms

B｜改动后（V397 三参版函数体逐字内联，p_pq = 本单 bbcb566f）：
   22.901 ms / 32.786 ms / 31.554 ms   → 中位数 31.554 ms

Δ 函数单次调用：+12.8 ms 绝对 / +68% 相对
结果集规模：候选料号 16 → 1861；函数返回行 80 → 9305（1861 × 5 个有价元素）

⚠️ 未验证：AC-13 的断言口径是「该单**整单渲染**耗时劣化 < 20%」，不是函数单次耗时。
   整单渲染需要 V397 已应用到 dev 库 + 跑起服务走完整链路，worktree 里做不到（迁移未进 dev 库、
   共享 8081 服务的是主工作区代码）。这一条留给主线亲验。
   风险提示：若 mc_view 在整单渲染中按 line_item 逐次执行（本单 1845 行），+12.8ms/次 会被放大；
   若走 expand/DataLoader 缓存则只付一次。该链路的调用次数本人未测，请主线在亲验时一并看。
