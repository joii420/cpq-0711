# fronttask · repair-260830 元素单价 pending 不可见

## 结论：前端零改动

**不派前端子代理。**

## 判定依据

| # | 依据 |
|---|---|
| 1 | 根因在**数据库函数的候选集过滤** + **后端 SQL 改写器**，前端只是如实渲染后端下发的 `snapshot_rows` |
| 2 | 「元素单价」列**已有完整渲染分支**且工作正常 —— 同页签白银行取到值后即显示，无需新增渲染逻辑。证据：同一份 `snapshot_rows` 里 `_元素`/`_料件`/`_毛重` 等列**全部正常显示**，走的是同一套渲染路径 |
| 3 | 后端返回值形态**不变**：仍是 `driverRow.元素单价` / `basicDataValues["{$mc_view.元素单价}"]` 两个 key，只是值从 `null` 变成有值。**无字段增删、无类型变化** |
| 4 | **不触发 `AP-44`**（字段类型联动协议）：本次不新增 / 不修改任何 `field_type`，`元素单价` 列的字段类型定义一字不改 |
| 5 | **不触发 `AP-31` / `AP-38`**（"加载中…"永久占位族）：不改 driver expand 链路、不改 fingerprint 维度、不改缓存 key |
| 6 | 无 API 契约变更（见 `api.md`） |

## 回归确认清单（由测试与主线亲验执行，非前端开发项）

| # | 确认项 | 对应 AC |
|---|---|---|
| 1 | 「材料成本」页签白银行单价显示 `12345.0000`，**数值格式与小数位**符合现行显示口径（9 位显示规则） | AC-1 |
| 2 | 未建档的 11 个元素对应行显示为**空**，🚫 **不得出现「加载中…」占位**（`AP-31`/`AP-38` 族症状） | AC-12 |
| 3 | 同页签内白银行与未建档元素行**混排时互不影响** | AC-12 |
| 4 | 页签小计 / 产品小计 / 单据总额随单价变化**正常重算**，不出现 `—` 或 0 | AC-1, AC-13 |
| 5 | 详情页（只读）与编辑页显示**一致**（`AP-50` 族：ReadonlyProductCard 与 ProductCard 分支对齐） | AC-1 |
| 6 | 报价单导出 Excel 中该列取值与页面一致 | AC-1 |

> ⚠️ 第 4、5 项虽是前端表现，但**根因侧零改动**，属回归确认而非开发任务。
> 若确认中发现异常，说明存在**本次未预见的耦合**，回流主线重新评估，不要就地改前端。

## 二期触发条件

出现以下任一情况才需要前端介入，届时另立任务：

- 用户要求对**取不到价的元素**做显式提示（如「未配置价格」文案替代空白）——
  当前裁定为空白，因为空白与「值确实为 0」可区分，加文案属需求变更
- 用户要求在页面上直接跳转到元素建档页（配置引导）
