# task-0729 B8.2 · 《SUBTOTAL 双端对拍清单》（正式报告）

> 产出日期：2026-08-01 · 后端工程师（技术总监已并行复核，结论一致）
> 生成方式：`GET /api/cpq/admin/quotations/task0729-subtotal-reconcile`（只读，只 SELECT + 内存重算，不写任何字段）
> 后端新算口径：`CardSnapshotService.buildCardValues`（含 B8.1 三处口径补齐，见 §0）→ `CostingSubtotalUtil.extractUnitSubtotal` 取 SUBTOTAL tab 的 `subtotal`
> 🔒 **本报告确认前，S6（写回 `li.subtotal`）未动一行代码，也未执行。**

---

## 0. 本次实际修复的三处口径差（backtask 原文列了 2 处，深挖过程中多发现 1 处）

| # | 口径差 | 位置 | 状态 |
|---|--------|------|------|
| 1 | `productAttributes` 传空 map → `product_attribute` token 恒取 0 | `CardSnapshotService`（4 处 `assembleTabsWithFormulaResults` 调用点 + SUBTOTAL 公式求值 `subCtx`） | 已补齐（新增 7 参重载 + `buildProductAttributesContext`，向后兼容，当前库 0 处实际引用，纯防御） |
| 2 | `${key}#__amount_total__` 哨兵键未登记 | `CardSnapshotService.backfillSubtotalsFromResolved`（BL-0017 遗漏的第三条路径） | 已补齐，照抄 `ComponentDataEffectiveRows:180-199` 口径 |
| 3 🆕 | `ComponentDataEffectiveRows.amountColsFromFields` 只认 snake_case（`is_amount`/`is_subtotal`），**冻结结构**（`quotation_view_structure.structure`，`CardSnapshotService` 走的唯一输入源）里字段是 camelCase（`isAmount`/`isSubtotal`，由 `CardSnapshotService.buildCardStructure:292` 冻结时显式转写）| 用真实数据验证 #2 时发现：只补登记不够，`amountCols` 对冻结结构恒返回空集，`__amount_total__` 恒为 0，#2 的修复对生产路径完全不生效 | 已补齐（两种键名都查，对齐 `FormulaCalculator.findSubtotalFieldNames` 已有的双形态兼容写法） |

**技术总监已实证复核第 3 处**：查 `QT-20260726-0016` 冻结结构，「材料成本」「外购件成本」「加工费」三页签各有 1 个 camelCase 金额列，snake_case 全为 0 —— 与本报告结论一致。

单测：`CardSnapshotAmountTotalTest`（新增，2/2 passed）+ `ComponentDataEffectiveRowsTest`（6/6，无回归）+ `ComponentDataEffectiveRowsDiscountTest`（2/2，无回归）。

---

## 1. 对拍结果全量表（25 条 line item，`cpq_db_0724`，料号 `S-3120014539`）

| quotationNo | status | li.subtotal（已落库） | 后端新算 SUBTOTAL | 差异 | 定性 |
|---|---|---|---|---|---|
| QT-20260726-0002 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260726-0003 | DRAFT | 14.0 | 14.0 | 0.0 | 一致 |
| QT-20260726-0004 | DRAFT | 14.0 | 14.0 | 0.0 | 一致 |
| QT-20260726-0005 | DRAFT | 14.0 | 14.0 | 0.0 | 一致 |
| QT-20260726-0006 | DRAFT | 14.0 | 14.0 | 0.0 | 一致 |
| QT-20260726-0007 | DRAFT | 14.0 | 14.0 | 0.0 | 一致 |
| **QT-20260726-0008** | DRAFT | 21.3704 | 21.0 | **0.3704** | **历史时点漂移（§2.1）** |
| QT-20260726-0015 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| **QT-20260726-0016** | APPROVED | 26.0 | 14.0 | **12.0** | **历史时点漂移（§2.2）** |
| QT-20260726-0018 | SUBMITTED | 14.0 | 14.0 | 0.0 | 一致 |
| QT-20260727-0019 | DRAFT | 14.0 | 14.0 | 0.0 | 一致 |
| QT-20260727-0020 | DRAFT | 0.0 | — | — | 跳过（无 `customerTemplateId`，疑选配/异常单，非本清单口径范围） |
| QT-20260727-0021 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260728-0022 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260728-0023 | DRAFT | 214.0 | 214.0 | 0.0 | 一致 |
| QT-20260728-0024 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260728-0025 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260728-0026 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260729-0027 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260729-0028 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260729-0029 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260729-0031 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260731-0035 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| QT-20260731-0036 | DRAFT | 0.0 | 0.0 | 0.0 | 一致 |
| **QT-20260731-0037** | DRAFT | 755.9252 | 214.0 | **541.9252** | **历史时点漂移（§2.3）** |

**统计**：25 条 line item → 1 条跳过（无模板）+ 21 条一致（diff=0） + 3 条历史时点漂移（本报告全部定性完毕，无「其他/待查」遗留项）。

---

## 2. 三条差异行逐条定性（手算 SUBTOTAL 公式，验证后端新算值内部自洽）

### 2.1 QT-20260726-0008（diff = 0.3704）

`quote_card_values` 各页签实测（`li.id=3941020f-e87e-4eab-9c33-1373af0f80d2`）：

| 页签 | componentCode | subtotal | subtotalByColumn |
|---|---|---|---|
| 材料成本 | COMP-0027 | 0.0 | （无 is_subtotal 列） |
| 外购件成本 | COMP-0028 | 0.0 | `{成本: 0.0}` |
| 加工费 | COMP-0029 | 21.0 | `{加工费: 21.0}` |

该单 SUBTOTAL 公式（componentId `b97417f1-7cb9-4f0e-a554-b98296337e30`）：
```
材料成本(COMP-0027#__amount_total__) + 外购件成本(COMP-0028#__amount_total__) + 加工费(COMP-0029#__amount_total__)
= 0 + 0 + 21 = 21.0
```
**与后端新算值 21.0 完全吻合**（内部自洽，非计算 bug）。`li.subtotal=21.3704` 比 21.0 多出的 0.3704，与「外购件成本 → 成本」列当前值 0.0 高度吻合方向——该列是 `is_amount=true/is_subtotal=true` 的金额列，**当前值 0，历史上大概率非 0**（即 `li.subtotal` 保存时「成本」列曾有 0.3704 的值，此后底层数据变化归零）。

**定性：历史时点漂移**。`card_snapshot_at=2026-08-01 09:11:57`（与本次对拍同一天，落在本分支并发测试活跃窗口内），`li.subtotal` 反映的是某次前端保存时刻的底层数据，与当前重算时刻的底层数据不同源。

### 2.2 QT-20260726-0016（diff = 12.0）——技术总监已复核

`quote_card_values` 各页签实测（componentCode COMP-0090/0091/0092）：

| 页签 | subtotal | subtotalByColumn |
|---|---|---|
| 材料成本 | 0.0 | `{材料成本: 0.0}`（该 FORMULA 列因驱动行 `_组成含量` 现为 NULL，现算恒 0） |
| 外购件成本 | 0.0 | `{单价: 0.0}`（手填列，未填） |
| 加工费 | 14.0 | `{加工费: 14.0}` |

SUBTOTAL 公式 = `0 + 14 + 0 = 14.0`，**与后端新算值 14.0 完全吻合**。

**定性：历史时点漂移**。证据链：`quote_values_at=2026-07-27 06:41:29` 早于 `q.updated_at=2026-07-27 06:42:06`（状态流转到 APPROVED 前那一刻的保存值）；`li.subtotal=26` 是当时前端算出并保存的值，当时「材料成本」驱动行的 `_组成含量` 应有真实值（现已变 NULL，可能是后续 V6 基础资料被清理/重导时连带丢失）。**后端算的 14 是对当前底层数据的正确反映，26 是历史快照，两者本就该不同**。

### 2.3 QT-20260731-0037（diff = 541.9252）

`quote_card_values` 各页签实测（`li.id=51b5699b-679a-4a8a-a976-dd5c8777704e`）：

| 页签 | subtotal | subtotalByColumn |
|---|---|---|
| 材料成本 | 200.0 | `{材料成本: 200.0}` |
| 外购件成本 | 0.0 | `{单价: 0.0}` |
| 加工费 | 14.0 | `{加工费: 14.0}` |

SUBTOTAL 公式 = `200 + 14 + 0 = 214.0`，**与后端新算值 214.0 完全吻合**（内部自洽）。

**定性：历史时点漂移，且大概率是并发测试活动产物**。`li_created=2026-08-01 05:32:40`、`quote_values_at=2026-08-01 05:33:11`——**当天凌晨新建**，落在本分支前端/测试工程师同步开发价格调整功能的活跃窗口内。`li.subtotal=755.9252` 与当前重算的 214 差 541.93，量级远大于本单「材料成本」当前 200 的贡献，暗示 `li.subtotal` 保存时的材料成本构成（元素单价/组成含量/BOM 行数）与当前已有较大差异（很可能是当日测试过程中多次编辑 BOM/元素数据所致）。因单据是 DRAFT 且创建-保存间隔仅 31 秒，属正常编辑过程中的瞬时状态，不视为口径 bug。

---

## 3. S6 影响面预告（🔒 关键，业务方须知情）

**S6（`li.subtotal` 写回）尚未实现、未启用**。本节回答：**若/当某料号触发升版重算（S6 执行），以上 25 条中哪些单的行金额会变、变多少**：

| quotationNo | 当前 `li.subtotal` | S6 执行后（若被选中升版）会写回的值 | 变化量 | 方向 |
|---|---|---|---|---|
| 21 条「一致」的单 | = 后端新算值 | 不变（S6 写回同一个值，等价 no-op） | 0 | — |
| QT-20260726-0008 | 21.3704 | 21.0 | **−0.3704** | 缩小 |
| QT-20260726-0016 | 26.0 | 14.0 | **−12.0**（约 −46%） | 缩小 |
| QT-20260731-0037 | 755.9252 | 214.0 | **−541.9252**（约 −71.7%） | 缩小 |

**关键提醒**：
1. **S6 不是批量重算**——按 backtask 硬约束 1（"只对被升版的料号行执行重算，哪怕输入没变也不做整单重算"），上表"会变"的前提是**该料号真的经历一次价格版本升版**（通过审核流程触发），而不是 S6 一上线就自动把这 25 条全部改写。21 条"一致"的单即使被升版，写回值也不变（无感知）。
2. **3 条"历史时点漂移"的单如果被升版，行金额会显著下降**（尤其 QT-20260731-0037，降约 72%）——这是"修正陈旧值"，方向正确（反映当前真实成本），但金额降幅大，**必须让销售/财务知情**，避免被误解为"系统突然把报价改低了"。
3. 这 3 条能否被升版取决于 backtask B4 的「待办池」判定逻辑（是否在活单范围内、是否命中调价料号清单等），本报告不代表它们一定会被升版，只说明**如果**被升版会发生什么。

---

## 4. 影响面提醒（task-0717 比对视图）

补齐后端 SUBTOTAL 口径会让 **task-0717 比对视图**报价侧「产品总价」列与核价侧显示的数值**朝正确方向变化**（对使用 `[页签(总计)]` 公式的模板生效，backtask 估计涉及「5 个报价模板 + 核价模板1」共 26 张单；本次实测覆盖到的是 `S-3120014539` 这一料号系下的 25 张单，同一模板系列）。

---

## 5. 结论

- **21 / 25 条完全一致**（diff = 0.0000），证明 B8.1（含追加发现的 camelCase 修复）补齐后计算路径对绝大多数存量数据正确、稳定。
- **3 条差异行全部定性为「历史时点漂移」**，逐条手算 SUBTOTAL 公式验证后端新算值内部自洽（非计算 bug），与技术总监独立复核的结论一致。
- **S6 影响面已预告**（§3）：若这 3 条单未来被升版，行金额会显著下降，需业务方知情。
- **本报告确认前，S6 写回逻辑未动一行代码。**
