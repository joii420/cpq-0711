# task-0729 · 70 条验收状态存档

> 由测试工程师在 P1 收官时输出的**逐条原始状态**，交付验收报告的依据。
> 生成时间：2026-08-05（P1 结束、P2 开始前）

## 统计

| 状态 | 条数 | 编号 |
|---|---|---|
| **PASS** | **63** | 见下表 |
| **部分** | 4 | `#16` `#26` `#52` `#56` |
| **环境不可验** | 3 | `#30` `#31` `#33` |
| **未跑** | **0** | —— 全部收口 |
| 依赖已知缺口 | 1 | `#43`（依赖 A-1 `quote-card-edit` 未接归位，并入该缺口不拆成独立 FAIL） |

⚠️ **`#10` `#27` `#57` `#66` 原不在技术总监列的 P2 清单里**（测试工程师交接时主动标出）—— `#10` `#66` 已 PASS，`#27` `#57` 在第二位手上。

> **最终版**　更新时间：2026-08-06（全部 70 条收口）
> 验收期共发现并修复 **13 个缺陷**，另登记 BACKLOG `BL-0120`~`BL-0126` 共 7 条

---

## 逐条状态

```
#1  PASS | 顺延规则JUnit 10/10 + API四周期PUT/GET回读一致
#2  PASS | 原FAIL(跨页勾选静默丢失)→前端 a9529c2d 加 preserveSelectedRowKeys（delete循环保留，两处互相点名注释）→复验三来源混合路径(seed回填+筛选态勾+跨页勾)落库 array_agg 精确集合匹配
#3  部分/证据不完整 | 无N+1(扫描增量+24 vs +20，N+1应~740)/10版截断/两种空值DOM不同 全PASS；停用元素仅API层验证，缺「已停用」Tag的DOM断言；Network"恰好1次"实为2~6次(常量，非N+1)
#4  PASS | 正向(slot=原定非补跑时刻)/幂等/反向(WEEKLY非当天不触发)三向live验证，后端bug已修复验证
#5  PASS | 同日流水递增+trigger_type正确+代码走查确认手动/定时同一方法
#6  PASS | 最初FAIL(0-diff仍进池)；后端修复后4/4单测+live确认保守兜底分支+#17轮自然撞上正面佐证
#7  PASS | 最初FAIL(VOIDED未转换)；后端修复后正反两向live确认(PENDING→VOIDED，APPROVED不变+指针不推进)
#8  PASS | 一料号一行+currentVersionNo/targetVersionNo精确匹配(#7修复后重判)
#9  PASS | 依据单锁定，版本生成后新建单C不影响basis
#10 PASS | 两侧共用 ComparisonViewService#extractSide，同单同料号 14.0==14.0；另发现 CostingSubtotalUtil 是第二份实现（结果一致，构成跨实现三角验证）
#11 PASS | breachedMaterials精确命中+approve对超阈值仍202不阻断
#12 PASS | 空原因400+正常驳回200落库+驳回后仍可查
#13 PASS | 通过确认弹窗数据(第二批)
#14 PASS | 隔离性正反双向(第一批,技术总监亲验)
#15 PASS | 预算quote_adjusted=实际重算subtotal逐位相同；24/24 job成功含无价元素不卡住整体
#16 部分/真实gap | 4条编辑路径部分不bump row_version(第二批)
#17 PASS | 同V版2料号先后approve合并进同一R，upgraded_material_nos累积，last_updated_at刷新
#18 PASS | GET price-revisions真实数据200完整返回
#19 PASS | materialVersions含state字段确认(NOT_PARTICIPATING等)
#20 PASS | 原「代码层面全对、运行时未验」→复验建真实 SALES_REP 实证：19/19 price-adjust 端点返403 且【拦在副作用之前】(settings md5/strategy字段/版本数/items.updated_at/审计日志五维证明无副作用)；反向屏7返200；PRICING_MANAGER 同resource GET200/PUT403 证明分级；7个resource注解覆盖率 0 漏网
#21 PASS | 核价侧联动+costing_order frozen逐字节不变(第一批,技术总监亲验)
#22 PASS | 预算costing_adjusted/quote_adjusted与实际升版后subtotal逐位相同
#23 PASS | 比对列配置round-trip精确
#24 PASS | 跨客户/跨系列隔离confirmed
#25 PASS | 行标红判定正反两向三层证据(第二批)
#26 部分 | affectedReviewCount精确scoping+APPROVED的Q完全隔离confirmed；P反映新配置子项受环境debris阻塞
#27 PASS | 屏3汇总列↔屏4明细 status 分布逐位一致；全库40条review不变量普查 breached=RED+MISSING 40/40、amber/stale/column_count 各40/40；唯一写点同事务预聚合，列表直读冗余列无实时COUNT()
#28 PASS | 升版后saveDraft不回滚(第一批,技术总监亲验)
#29 PASS | 手改值清除(driverRow权威值=本版价)+其余字段保留；含S4b自我纠错
#30 环境不可验 | 真实export-excel-view调用200+未GET详情页，但该组件excelColumns未配置导出恒空表，与价格无关
#31 环境不可验 | S-80011当前0条真实活单+0条子件element_bom_item，从零构造超出快速测试范围
#32 PASS | ①②③④全部子项(含null/空串语义自我纠错，COMP-0049/0021/0027/0102对照排除假实现)
#33 环境不可验 | 同#31(依赖S-80011子件BOM)
#34 PASS | 手动行Cu(∈清单)：元素单价=171.368000(本版价)，元素字段未清空
#35 PASS | 手动行银/Ni/301三条对照组：元素单价原样保留100.0未被误伤
#36 PASS | D5正反两案精确对照：P(无活单)不进池+指针推进；Q(曾驳回)不进池+指针不推进
#37 PASS | submission_snapshot md5升版前后完全一致
#38 PASS | 系列甲配置不泄漏系列乙；Y正确MISSING非STALE；MISSING计入breached_count未被静默判健康
#39 PASS | 模板多版本共享series配置代码走查证实
#40 PASS | 陈旧页面driver行不退价(第一批,技术总监亲验)
#41 PASS | 陈旧页面手动行不退价(第一批,技术总监亲验)
#42 PASS | ①③手动行priceLocked+详情页徽标修复，代码+数据独立核验(前端另有端到端实测)
#43 依赖已知缺口 | 依赖 A-1（quote-card-edit 未接归位，已核实：PriceReconciler 只在 saveDraft 路径调用）。①③④ 均为其下游表现，并入 A-1 缺口，不拆成 4 张指向同一未实现功能的单
#44 PASS | 归位不误伤：毛重 99.9 精确保留；∉清单行 md5 逐字节相同且两个锁标记键都不存在（不是值碰巧一样，是根本没被访问）
#45 PASS | 归位幂等：3 次保存 md5 全同、行数恒 5；rowsChanged=8 + row_version=1 证明不是空转
#46 PASS | 活单范围：job_item=5（非 7 非 9），9 状态逐一表态且两组拒绝理由字符串不同，4 张非活单逐字节不变
#47 PASS | 原场景②FAIL(从无历史价→row_data删值不删锁=死格)→031a2dfa 清值时一并撤锁→复验15个自造探针+嵌套结构6次数组重写md5恒定
#48 PASS | AMBER/RED阈值判据单测新增通过(第二批)
#49 PASS | MISSING计入breached且rowRed=true，STALE不计；反向核心：同review另一列NORMAL时breached未增。另发现 missingSide=BOTH 前端归因错（已派修）
#50 PASS | Round A/B受控对照钉死「锚点是指针不是上一批」（A：本期价与上一批逐字节相同但指针版不同→仍进池）；MZ跨4次生成指针不推进。含一次假绿自纠（保守兜底分支≠有差异分支）
#51 PASS | 元素停用后仍在清单、屏1可见并标停用、照常参与调价、__priceLocked 仍 true（未静默解锁）
#52 部分/阻塞 | job/job_item重试机制确认OK；"尚未更新"子项待重测(端点已解除BLOCKED，未回补)
#53 PASS | 初版定型全链路(第二批)
#54 PASS | 原FAIL(COMPARISON_COLUMN 的 changed_by_name 全库5/5 NULL)→7663a363 下沉为实体方法 stampActor（与strategy侧共用，物理上无法只落一半）→复验双actor判别+与STRATEGY类型 name_md5 逐字节相同
#55 PASS | R1快照隔离性DB层验证(第二批，绕开缺失preview端点)
#56 部分 | ①接口200不报错(真实撞上R间材料完全不同场景)PASS；②③④逐料号趋势标记契约未实现(与前端注释一致,非新发现)
#57 PASS | 四轮改配置全部触发重算(比对列/预警线/元素清单/料号范围)；防空转证据=review_column行id md5四轮全变(delete+insert)；APPROVED对照组四轮逐字节不变
#58 PASS | 撤锁PASS(Ag行两键消失、Cu行保留、Zn行未误动)；原「业务值非逐字节」(2200.000000→2200.0，探针实证真丢精度)→4f069534 MAPPER三项配置(缺一不可：只加第一项会从丢精度变成2.2E+3)→复验15探针+嵌套+6次重写md5恒定+不反向污染
#59 PASS | ①②③+反向 全PASS(G范围外手改值经4次归位+1次真实升版md5逐字节不变)；原⑤FAIL(策略停用不撤锁)→4f069534 unlockOnly模式(连带修复「元素清单清空」第二入口，撤锁族4入口全收口)→复验撤锁↔重锁循环可逆、md5与「从未停用过」路径完全相同
#60 PASS | 归位价=P_0720(1000.0000/60.0000)≠P_today(5000.0000/90.0000)，两次归位恒定且rowsChanged=2证明真跑了；代码依据 baseDate=q.createdAt.toLocalDate() 不读 LocalDate.now()
#61 PASS | 原FAIL(旧批次FAILED/CONFLICT不转STALE、retry返202)→7663a363 状态集合扩为(WAITING,RUNNING,FAILED,CONFLICT)+875331de 同事务重算job计数器→复验5种状态全表态(含WAITING/RUNNING回归)+三层拦截+计数器逐项对齐+留痕完整保留
#62 PASS | CHECK约束+partial unique index+grep三层结构性证据
#63 PASS(核心断言) | 两料号先后升版后 X/Y 都是新价（X未停在自己升版那刻的旧快照）→ 证实整单覆写非追加；步骤3逐位比对未完成、步骤4环境不可验
#64 PASS | 两侧同一套元素价(第一批,技术总监亲验)
#65 PASS | 409/202+耗时84ms+隔离性代码确认
#66 PASS | E14-7红线：干净单增量53ms（漂移控制51ms、最坏界87ms），端到端增量已低于噪声底；5类元数据查询各=1（归位8行无N+1）
#67 PASS | 二次确认409精确响应+取消/确认场景
#68 PASS | 空清单/全无价/补勾有价三步骤精确验证
#69 PASS | 双端黄金用例31/33+2条已知留红(RECORD/README/交付报告三源交叉验证)
#70 PASS | 含step4完整approve→FAIL→改阈值→retry→SUCCESS(技术总监亲验)
```

---

## 🔴 验收期间被真实升版的 24 张单（交付备案）

**全部为 `CUST-0001` / 料号 `S-3120014539` / 升至版本 `V26080505`**（`Ag=43358.750000` / `Cu=171.368000`）

> ⚠️ 这些升版**在业务上是合规的**（按版本价正确计算），但由**验收测试触发**、非业务发起。业务方需知情。
> `li.subtotal` 未被触碰——价格调整只写组件层 `driverRow` 单价，不改写该缓存字段。

```
QT-20260726-0002(0)      QT-20260726-0004(14)     QT-20260726-0005(14)
QT-20260726-0006(14)     QT-20260726-0007(14)     QT-20260726-0015(0)
QT-20260726-0018(14)     QT-20260727-0019(14)     QT-20260727-0020(0)
QT-20260727-0021(0)      QT-20260728-0022(0)      QT-20260728-0023(214)
QT-20260728-0024(0)      QT-20260728-0025(0)      QT-20260728-0026(0)
QT-20260729-0027(0)      QT-20260729-0028(0)      QT-20260729-0029(0)
QT-20260729-0031(0)      QT-20260731-0035(0)      QT-20260731-0036(0)
QT-20260802-0050(14)     QT-20260803-0052(14)     QT-20260803-0053(14)
```
（括号内为 `li.subtotal`，升版前后同值）

另有 3 张单（`QT-20260803-0055` / `0056` / `QT-20260726-0016`）曾被临时置 `CANCELLED` 以绕过 L3 拦截，**测完已逐字节复原**为原状态，未被本次升版触碰内容。
