# 报价编辑链路优化与前后端对账

- 立项日期：2026-08-06
- 类型：task
- 状态：**阶段⓪① 已合并**（merge `2942a4d8`，实现 `286def1c`）｜阶段②③④⑤ **未开工**
- 关联：`BL-0137`；分支 `feat/task-0806-edit-reconcile`（已合并并删除）；前置已交付 `BL-0127`（`dev-docs/repair-0803-BL0098-公式绑定改绑ID/repair-0805-行内公式列不随编辑重算/`，合 master `872113c0`/`d06fd90e`/`caf720ba`/`2ca1dec0`）

### 阶段进度

| 阶段 | 状态 | 备注 |
|---|---|---|
| ⓪ 白名单收窄 | ✅ **已合并** `2942a4d8` | AC-18 已亲验（合并前 8097 临时实例 + 合并后 8081 真实环境各验一次） |
| ① 分流+对账+闸门 | ✅ **已合并** `2942a4d8` | AC-4 / AC-15 已亲验；**AC-1 / AC-2 / AC-3 / AC-13 / AC-18b 属 UI 级，待闸门 B 用户在已合并环境真机验收** |
| ② 异步 | ⬜ 未开工 | — |
| ③ 懒物化 | ⬜ 未开工 | 前置调研已完成（§5.3） |
| ④ 缓存 | ⬜ 未开工 | 前置：需先收敛结构写入口 |
| ⑤ Decimal 统一 | ⬜ 未开工 | 必须在 ① 的对账跑出基线之后 |
- 相关但**不互为前置**：`dev-docs/task-0806-模板发布全量冻结/`（模板不可变治理。本任务主路径读的是冻结结构 `quotation_view_structure`，不是 `template.components_snapshot`，两者解耦）

---

## 1. 背景与目标

### 1.1 起点：BL-0127 修完，值对了，但结构问题还在

用户报 `QT-20260805-0080`「改了材料占比，只有小计变、行内公式列不动」。根因是三段闭环：

1. **行内 FORMULA 单元格优先读后端快照**（`QuotationStep2.tsx:3322-3338`），而**列小计/产品小计一律前端实时重算**（`buildCrossTabRows`，`:2855` 写在 ProductCard 渲染体里、非 `useMemo`）→ **同一列两个引擎**；
2. `saveDraft` 重建行时 `li.quoteCardValues = null`（D-1 失效），`editRows` 随之被抹掉；
3. 懒重建 `buildCardValues` 对 `editRows` **硬编码 null**，只读纯 driver 的 `snapshot_rows`。

实证：**全库 34 行有卡片值、非空 `editRows` = 0 行** —— 这条通道从未通电。已修复合并。

### 1.2 不做会怎样（三个仍在的问题）

| # | 问题 | 证据 |
|---|---|---|
| **P1** | **每编辑一格等 ~900ms** | 实测中位 900ms（附录 A） |
| **P2** | **前后端两套引擎无对账，分歧完全静默** | 历史断过三次：rowKey 对不上（全库 497 行只对上 1 行）/ `saveDraft` 清空 editRows / 重建不读 `row_data`。**每次都靠线上故障发现，没有一次是监控发现的** |
| **P3** | **快照是 4 条链路的数据权威，却零自动化覆盖** | 比对视图（`ComparisonViewService:286,292`）/ 价格升版（`MaterialVersionUpgradeService:263-276`，**重算后写回 `li.subtotal`**）/ 复制单（`QuotationService:1548`）/ 树删除重算（`QuotationTreeService`） |

### 1.3 做完达到什么状态

**把后端从「显示权威」降级为「校验器」**：

- DRAFT 下行内值走前端引擎 → 与列小计天然同源、零等待；
- 后端异步照算，两边**对账** → 不一致亮标记 + **禁止提交**；
- 在此安全网之上再叠异步、懒物化、缓存三级性能优化。

**目标量化**：编辑一格的用户感知 900ms → **0**；后端单次编辑耗时 900ms → **~450ms**（阶段③④后，须在生产态复测）。

---

## 2. 范围

### 2.1 本期做（六阶段，可独立上线）

| 阶段 | 内容 | 解决 | 性质 |
|---|---|---|---|
| **⓪** | 字段类型白名单收窄 8 → 6（剔 `DATA_SOURCE` / `INPUT`），**代码分支全保留** | 收窄协议面，不再长新分歧源 | 治理，零风险 |
| **①** | 分流 + 对账 + 差异可见 + 提交闸门（**五件必须一起上**） | P2、显示不同源 | **安全网** |
| **②** | W1 异步：前端不 `await` 编辑请求 | P1 | 性能 |
| **③** | W3 懒物化 `row_data`（抄 `lazy-excel` 现成模式） | 后端 45% 负载 | 性能 |
| **④** | 缓存冻结结构 + 行键 | 计算段 74% | 性能 |
| **⑤** | **求值管线载体 number → Decimal，前后端数值模型统一** | 消灭「聚合精度」这一类分歧源 | **一致性**，会改值 |

> **顺序约束**：
> - **① 必须最先**（除 ⓪ 外）—— 它是后面所有步骤的安全网。②③④⑤ 的任何错误最终都表现为「前后端对不上」，而对账正好盯这个。先优化后补网 = 中间裸奔。
> - **⑤ 必须在 ① 之后** —— 对账上线后会把 SUM/AVG 那些列的实际差异**量化出来**，那正是 ⑤ 的验收基准（改完这些告警应当消失）。没有基准就改，无法判定改对没有。
> - **⓪ 随时可做**，与其余阶段无耦合。

### 2.2 本期明确不做

| 不做 | 原因 | 推迟去向 |
|---|---|---|
| 把公式引擎合并成一套 | 全搬后端 = 每格等往返；全搬前端 = 导出/比对/升版无浏览器可用。现实路径是两份实现 + 持续对账 | 不推迟，**永久不做** |
| 后端 `BigDecimal` 降级 `double` 求速度 | 会变快也会与前端更一致，但**金额精度退化**。项目选的是反方向（前端引入 decimal 向后端靠），方向正确 | 不推迟，**永久不做** |
| 缓存 `row_data` / `snapshot_rows` | 每次编辑就变，失效判据写不干净，有踩坑史 | 不推迟，**永久不做** |
| 缓存 `template.components_snapshot` | 失效面扇出（一个组件 → N 个模板），且主路径读的是冻结结构 | 归 `task-0806-模板发布全量冻结` |
| **W4 按依赖图增量重算 / 增量写** | **D14 已裁定不做**：收益仅 120ms（且已被 ①④ 覆盖），风险最高（漏算下游 = 静默不一致，正是本次故障同族） | **登记 BACKLOG 长期观察**，不排期 |
| **真删 `DATA_SOURCE` / `INPUT` 的代码分支** | ⓪ 只收窄白名单（禁新配），删分支等于 AP-44 反向操作 17 个检查点 | **登记 BACKLOG**（P2），待 ⓪ 稳定运行一段后评估 |
| **删 `LIST_FORMULA`** | **D13 裁定保留** —— 它是 V203/Phase B「配置模板」能力的字段入口（`ConfigTemplate` = "LIST_FORMULA 数据源"），删它 = 砍功能，属**产品决策**不是技术治理 | **登记 BACKLOG**：「配置模板能力去留」待产品拍板 |
| 已冻结/已提交单据跟随 ⑤ 精度改造重算 | **D12 裁定不回溯** —— 提交即冻结、逐字节不变是既有语义 | 永久不做 |
| Redis 分布式失效 | D8 裁定先按单实例 | 转多实例部署时再做 |
| AC-3/5/6 三条自动化回归（BL-0127 遗留） | 覆盖对账够不着的入口（比对/升版/导出） | 保留在 `BL-0127` 条目下，本任务 ④ 后接续 |

---

## 3. 澄清结论与决策记录 ★

| # | 决策项 | 结论 | 理由 | 被否决的方案及原因 |
|---|---|---|---|---|
| **D1** | 对账不一致时怎么办 | **只标记 + 埋点，不自动改数**（用户拍板） | 谁对谁错无法自动判定；本次故障的教训就是「静默」比「错」更危险 | ❌ 自动以后端为准回灌 = 退回今天的行为（慢且会抹掉用户编辑）；❌ 自动采信前端 = 后端白算，对账退化成告警装饰 |
| **D2** | 告警内容 | **必须带两边的输入快照**，不能只报「结果不同」 | 一眼分清是「输入不同」（去查数据流）还是「同输入不同结果」（真引擎分歧）。见 §5.4 | ❌ 只报结果差值：排查仍要人肉挖，等于没减负 |
| **D3** | 异步竞态 | 带请求序号 / `row_version`，**过期响应直接丢** | 异步返回时用户可能已改了几格 | ❌ 后到即覆盖：会用旧快照结果盖掉新编辑（lost update） |
| **D4** | 差异阈值 | 按现行 `DISPLAY_SCALE = 6` 比对，差值 < 阈值视为一致 | 否则浮点末位天天误报，噪音淹没信号 | ❌ 逐字节比对：`BigDecimal` vs IEEE754 必然末位不同，恒报警 |
| **D5** | 对账粒度 | **编辑防抖后整卡对一次**，不是每格一次 | 跨页签公式必须整卡才有意义；每格一次请求量爆炸 | ❌ 每格失焦对一次：与 ② 的防抖合并冲突，且跨页签依赖看不全 |
| **D6** | 冻结单据 | 非 DRAFT **显示快照、前端算只用于对账** | 方向对称，且不破坏「提交即冻结、逐字节不变」语义 | ❌ 一律前端实时算：公式引擎/精度口径任何改动都会让历史单据显示漂移 |
| **D7** | 不一致能否提交 | **禁止提交**，并列出差异清单（用户拍板） | 自相矛盾的单子一旦冻结，比对/升版/导出各读各的，且**不可改** | ❌ 仅警告放行：等于知道有问题还放行，把矛盾永久钉进历史 |
| **D8** | 部署形态 | **先按单实例做**（用户拍板），缓存用本地 evict + TTL | 省掉一整块分布式复杂度；`redis-client` 扩展已装，转多实例时再补广播 | ❌ 一上来就上 Redis pub/sub：当前无多实例需求，为假想需求付复杂度 |
| **D9** | 判谁对的默认倾向 | **默认怀疑后端** | 金额链路（`li.subtotal`、Excel 导出）实际跟的是**前端**，前端错了直接反映在钱上、易被业务发现；后端错了藏得深 | ❌ 默认怀疑前端：与金额实际来源相反，会把排查引向错误方向。**注：本条是经验判断，非定论** |
| **D10** | 阶段① 五件事能否拆开上 | **不能，必须一起上** | 只做分流不做对账 = 把唯一的人肉探针（用户肉眼）也拆了 | ❌ 先分流后补对账：中间窗口期完全裸奔 |
| **D11** | 阶段③ 懒物化的粒度 | **只推迟公式列，INPUT 值即时写** | INPUT 是用户敲的、不需要「算」；贵的只是上千次公式求值。这样 ensure 挂载点少一半 | ❌ 整个 `row_data` 都推迟：行键判重等只看 INPUT 的读取方也被迫挂 ensure |
| **D12** | 求值管线是否全面 Decimal | **做（阶段⑤）**，前后端数值模型统一；**存量数据不回溯**（用户拍板） | 现状：前端 `evaluateArithmetic` 内部 Decimal 但**出口 `.toNumber()` 转回 number**（`formulaEngine.ts:792/905`），聚合拿到的已是 number；后端聚合是 `PrecisionPolicy.sum`（BigDecimal）→ **SUM/AVG 是确定的分歧源**。统一是治本 | ❌ 只改 `aggregateTreeNums` 一个函数：它入参就是 `number[]`，无原料，改了没用；❌ 后端降级 `double` 对齐前端：会变快也会更一致，但**金额精度退化**，方向错；❌ 存量重算：破坏「提交即冻结、逐字节不变」 |
| **D13** | 未用字段类型如何处置 | **白名单 8 → 6**：剔 `DATA_SOURCE` / `INPUT`（禁新配），**代码分支全部保留**；`FIXED_VALUE` 保留（用户指定）；**`LIST_FORMULA` 保留** | 三个载体（组件表含 DISABLED / 43~50 张单的冻结结构 / 模板快照）实测**只有 4 种在用**，剔除两种零风险。`LIST_FORMULA` 是 V203/Phase B「配置模板」能力的字段入口，删它 = 砍功能 | ❌ 直接删 4 种的代码分支：等于 AP-44 反向操作 17 个检查点；❌ **原判「LIST_FORMULA 是选配核心、两个 worktree 依赖它」——已实测证伪**：那两个分支命中的文件集与 master **完全相同**（分支还少一个），引用全部继承自 master，一行新依赖没加；且两分支已搁置（落后 master 604 / 713 提交） |
| **D14** | 编辑同步的重算粒度 | **维持整卡重算**，不做依赖图增量（用户拍板） | 整卡重算实测仅 120ms，且已被 ①④ 的优化覆盖；增量重算的收益不足以抵消「漏算下游 = 静默不一致」的风险 | ❌ 按依赖图增量重算（W4）：依赖图代码现成（`buildCrossTabRows` 的 `deps` + 拓扑序），技术可行，但在对账未跑稳前漏算无人知晓 |
| **D15** | 阶段① 单独上线时 `assertLineSettled` 的「在飞写」怎么表现 | **阶段① 该条件恒 `false`，只有 `RECONCILE_PENDING` 生效**；`WRITE_IN_FLIGHT` 随阶段② 一起启用。另定：**前端提交前必须先完成一次对账上报再发 submit**（前端串行保证），后端以**最后一次成功上报**为准 | 阶段① 前端仍 `await` 编辑请求（异步化是阶段②），物理上不存在「在飞写」。不定义清楚会让实现者凭空造一个恒空的队列 | ❌ 阶段① 就把队列建起来：为一个当前不可能发生的状态引入并发状态；❌ 不定义、留给实现者猜：`test.md` 已因此产生不可判定用例（TC-134/TC-201） |
| **D16** | 白名单收窄的校验粒度 | **保持「整份 `fields` 逐个校验」**（现状不改）。「已有数据零影响」的准确表述是：**实测三载体 0 命中所以无影响**，而非设计上豁免存量 | 改成「只校验本次变更字段」要 diff 新旧 fields，引入新复杂度去解决一个**实测不存在**的问题 | ❌ 改成只校验变更字段：为 0 个存量数据付架构复杂度；❌ 沿用「零影响」这个含糊措辞：`test.md` TC-164 已据此写出**与实际行为相反**的期望值（写了「保存成功」，实际会 400） |

---

## 4. 功能需求

### 阶段⓪ · 白名单收窄（治理，零风险）

- **FR-0** `ComponentService.VALID_FIELD_TYPES` 由 8 种收窄为 **6 种**：保留 `BASIC_DATA` / `INPUT_TEXT` / `INPUT_NUMBER` / `FORMULA` / `FIXED_VALUE` / `LIST_FORMULA`，剔除 **`DATA_SOURCE`** 与 **`INPUT`**（裸）。
  - **只改校验白名单**，`DATA_SOURCE` / `INPUT` 的**所有既有代码分支保持原样**（前后端渲染、解析、序列化一处不删）
  - 🔧 **校验语义（2026-08-07 查实后修正措辞）**：`validateFields` 是**遍历整份 `fields` 逐个校验**，不是只校验本次改动的字段。因此**存量若含被剔类型，下次保存会被 400 拒绝** —— 「已有数据零影响」这句话**只成立于「实测三个载体均 0 命中」这个前提**，不是设计上豁免存量。见 D16
  - 错误信息（**代码实际文案，勿写错**）：`Invalid field_type: DATA_SOURCE. Must be one of: [...]`（注意是 `field_type` 带下划线）。`VALID_FIELD_TYPES` 是 `Set.of()`，**遍历顺序不保证，断言时只能比集合不能比顺序**
- **FR-0b** **前端组件管理表单的字段类型下拉同步收窄为 6 种**。否则出现「后端拒绝、前端还展示 8 个选项」的体验缺口 —— 用户能选中一个必然失败的值。

### 阶段① · 分流 + 对账 + 提交闸门

- **FR-1** DRAFT 单据的行内 FORMULA 单元格取值改走前端引擎（`computeAllFormulas` / `computeTabFormulasTree`）；非 DRAFT 仍读快照 `formulaResults`。
- **FR-2** `ProductCardProps` 新增 `quotationStatus`，**报价侧 / 核价侧 / `ReadonlyProductCard` 三处调用点全部透传**（AP-41）。
- **FR-3** 保留「快照缺该 rowKey / 该列时本地兜底」现有行为，**不得删除**（新行 / LIST_FORMULA 字符串公式依赖它）。
- **FR-4** 后端异步返回结果后，前端逐格与本地值比对；差异按 D4 阈值判定，超阈值写入 `out.errors` 旁路 → `ComponentCell` 显示 ⚠ + tooltip，tooltip 内容按 D2（两边值 + 两边输入）。
- **FR-5** 差异上报埋点（API-5），含 lineItemId / componentId / rowKey / 字段名 / 两边值 / 两边输入摘要。
- **FR-6** 提交前置校验 `assertLineSettled(lineItemId)`：存在**未落定的对账差异**或**在飞的编辑写**→ 拒绝提交，返回差异清单（API-3 新增错误码）。

### 阶段② · 异步

- **FR-7** 编辑请求改为不阻塞渲染：`handleSnapshotCellEdit` 不再 `await` 后回灌，仅把响应交给对账。
- **FR-8** 防丢更新：同一 `lineItemId` 的编辑请求服务端串行化（或用现有 `row_version` 乐观锁 + 失败重放）。
- **FR-9** 编辑请求防抖合并：连改 N 格合并为 ≤2 次请求（D5 同批做整卡对账）。
- **FR-10** 失败可见：自动重试 N 次，仍失败则在卡片上显示可见失败态（不得静默）。

### 阶段③ · 懒物化

- **FR-11** `row_data` 的**公式列**改为懒物化：编辑期只标脏，不逐组件 `REQUIRES_NEW` 落库；**INPUT 值仍即时写**（D11）。
- **FR-12** 新增 `ensure-row-data` 端点（API-2）；Excel 视图 / 导出 / 折扣基数 / 提交冻结采集 / 回填采集 / 价格归位 / 价格升版**调用前先 ensure**（清单见 §5.3）。
- **FR-13** 脏标记**持久化**（进程重启 / 多实例不得丢）。

### 阶段④ · 缓存

- **FR-14** 缓存冻结结构，key = `quotationId + view_kind`。
- **FR-15** 缓存 `row_key_fields`，key = `componentId`。
- **FR-16** 失效：写入口事务提交后同步 `evict`；配 TTL 兜底与手工清缓存诊断端点（API-4）。
- **FR-17** **禁止 negative caching**：查不到时不缓存 `null`。

### 阶段⑤ · 求值管线 Decimal 统一（D12）

- **FR-18** 前端求值管线的数值载体由 `number` 换为 `Decimal`：`evaluateExpression` / `evalTreeRefToken` / `aggregateTreeNums` / `cross_tab_ref` 聚合**全链路保持 Decimal**，**只在最终出口（写 `formulaCache` / 落 payload / 显示）转 number**。
  - 现状病灶：`formulaEngine.ts:792` `:905` 的 `result.toNumber()` 把精度在**每条表达式出口**就丢了，聚合拿到的已是 number。
- **FR-19** 聚合口径与后端对齐：`SUM` / `AVG` 用 Decimal 精确累加与除法（对齐后端 `PrecisionPolicy.sum` / `divide`）；`MAX` / `MIN` / `COUNT` 无精度问题，实现可不变但**不得再经 number 中转**。
- **FR-20** **存量不回溯**（D12）：改造只影响改造后新算的值。已冻结/已提交单据的 `quote_card_values` / `submissionSnapshot` **一律不重算、不迁移**。
- **FR-21** `GoldenCardValuesEquiv` 的 `amt-002` / `amt-003` **期望值重新标定**（不得沿用旧常量；标定过程与依据写进 `test-report.md`）。

---

## 5. 数据与规则

### 5.1 无表结构变更（阶段①②④）

阶段③ 需要一个持久化脏标记，二选一（实现期定，写进 `backtask.md`）：
- **方案 a**：`quotation_line_component_data` 增列 `row_data_dirty_at timestamptz`（Flyway `V<待分配>`）
- **方案 b**：复用现有 `snapshot_at` / `row_version` 语义，零迁移

> Flyway 版本号**开工时才分配**（共享 `cpq_db` 的迁移号是移动靶，见 §8 K5）。

### 5.2 缓存失效表

| 缓存 | key | 何时变 | 失效机制 | 失效时间 | 兜底 TTL | 容量 |
|---|---|---|---|---|---|---|
| **冻结结构** `quotation_view_structure` | `quotationId + view_kind` | 仅显式重建结构 | 写入口**事务提交后** `evict(quotationId)` | 同步立即 | **30 分钟** | LRU 200（全库 51 条 `QUOTE_CARD`） |
| **`row_key_fields`** | `componentId` | 组件保存 | 组件保存**提交后** `evict(componentId)` | 同步立即 | **10 分钟** | 全量常驻（几百个短数组） |

> TTL 的作用**不是**「内容会变」，而是**防某条写路径漏挂 evict** —— 它是保险丝，不是主机制。

### 5.3 `row_data` 读取方（2026-08-06 穷举完成，共 12 处）

**必须挂 ensure（3 个）**：

| 位置 | 用途 | 现状 |
|---|---|---|
| `ExcelViewService.buildRowData:403`（+`:732/:742`） | Excel 视图 / 导出数据源 | **活跃** —— 整行物化存在的唯一理由 |
| `LineDiscountService:148` → `discountBaseAmount:118` | **折扣基数 → 金额** | ⚠️ **当前零命中**：`discount_source` 全库 55 行全 NULL、`pricing_strategy` 0 行。代码活着，配了策略即激活 |
| `SnapshotCollectorService:172-191` | 提交冻结采集 `element_actual_*` | ⚠️ **当前零数据**：全库 `row_data` 含该前缀 0 条 |

**顺手挂（3 个，读+写，加一行调用）**：`QuoteBackfillCollector:138`、`PriceReconciler:553`、`MaterialVersionUpgradeService:459/:633`。

**已定性豁免（6 个）**：

| 位置 | 豁免理由 |
|---|---|
| `RowKeyUniquenessService:88` | 判重看**行键字段（INPUT）**，不看公式值 → D11 下天然不受影响 |
| `QuotationService.migrateAndCreateComponentData:1686` | 复制/升版**只搬不算** |
| `QuotationService.getFieldTrace:998` | 读的是 `q.submissionSnapshot`（提交冻结 JSON），**不是活 `row_data`** |
| `QuotationExportService:247` | 把 `rowData` 原串塞进 Excel 单元格，调试/原始数据 sheet |
| `ConfigureSnapshotService.loadRowDataByComp:1058` | **它本身就是物化器** |
| `CardSnapshotService:1265/:1517/:3612` | prefetch + BL-0127 回种，自己人 |

### 5.4 对账告警内容规格（D2 落地）

> 🚨 **下面的数字是「格式示例」，不是断言基准，禁止拿去写死在用例里。**
> 该单元格在 24 小时内实测出现过**三个不同的值**（`608.775811` → `6.087758` → `0.526759`），
> 原因是共享开发库上有并发会话在改价格调整策略（K8）。
> **凡涉及具体数值的用例，一律执行时动态取基线 + 自洽性断言（如「列小计 = 各行之和」「篡改前后对比」）。**

```
不一致：物料 / Ag粉 / 材料成本
  前端 6.087758    输入：材料占比=0.25  组成数量=2  元素单价=28892.5
  后端 608.775811  输入：材料占比=25    组成数量=2  元素单价=28892.5
                         ^^^^^^^^^^^^ 输入就不同
```

排查顺序（按本项目真实发生频率）：
**① 输入不是同一份 → ② 字段解析口径（camel/snake、driver 列名 vs 字段名）→ ③ 公式没被解析到 → ④ 行集/行序 → ⑤ 真算术差异**。

⑤ 最罕见：`GoldenCardValuesEquiv` 的 `amt-002/003` 曾被归因为精度分歧，后有提交（`39982ce0`）专门更正 —— **真实根因不是精度**。

### 5.5 精度口径

沿用 `DISPLAY_SCALE = 6`（task-0801）。**本任务不改任何求值口径**，AC-11 值中性每阶段必须过。

---

## 6. 验收标准

| # | 判据 | 对应 FR | 阶段 |
|---|---|---|---|
| **AC-1** | DRAFT 下改一格：行内值**立刻**变，且与列小计逐值一致，**F12 Network 无阻塞请求** | FR-1,2,3 | ① |
| **AC-2** | 非 DRAFT 打开：行内值**逐字节**等于提交时快照 | FR-1 | ① |
| **AC-3** | 人为制造前后端分歧（临时改一侧口径）→ 对账**必须报警**，且 tooltip 含两边值 + 两边输入 | FR-4 | ① |
| **AC-4** | 存在未落定差异时**提交被拒**，响应含差异清单（lineItemId/组件/行/列/两边值） | FR-6 | ① |
| **AC-5** | 快速连改 10 格 → 后端最终状态与前端逐值一致（**无丢更新**） | FR-8 | ② |
| **AC-6** | 连改 10 格的编辑请求数 **≤ 2** | FR-9 | ② |
| **AC-7** | 编辑请求人为失败 → 卡片出现可见失败态，且重试后自愈 | FR-10 | ② |
| **AC-8** | 懒物化后：Excel 视图 / 导出取到的仍是**最新值**（改一格 → 直接开 Excel 视图核对） | FR-11,12 | ③ |
| **AC-9** | 进程重启后脏标记不丢：标脏 → 重启 → 开 Excel 视图仍能补算出最新值 | FR-13 | ③ |
| **AC-10** | 组件保存后**立即**（不等 TTL）生效，缓存不返旧行键 | FR-15,16 | ④ |
| **AC-11** | 结构重建后**立即**生效 | FR-14,16 | ④ |
| **AC-12** | `row_key_fields` 查不到时**不缓存 null**（连查两次，第二次仍打到库） | FR-17 | ④ |
| **AC-13** | **值中性 A/B**：对**无编辑**单据，改动前后 `quote_card_values` **逐值不变** | 全部 | **⓪①②③④**（⑤ 除外，见 AC-16） |
| **AC-16** | **阶段⑤ 专属判据**（值中性在此不适用）：改造前后**每一处数值变化都必须可解释为「精度提升」，且变化幅度 < 显示精度 6 位**。任一处超出即按缺陷处理，不得以「精度改了嘛」搪塞 | FR-18,19 | ⑤ |
| **AC-17** | **存量不回溯**：改造后，随机抽 5 张已提交单据，`quote_card_values` 与 `submissionSnapshot` **逐字节**与改造前一致 | FR-20 | ⑤ |
| **AC-18** | 白名单收窄后：新建/编辑组件选 `DATA_SOURCE` 或 `INPUT` → **400 拒绝**，错误信息含被拒类型名且列出 6 种合法值（**比集合，不比顺序**）。<br>🔧 **存量语义（D16）**：`validateFields` 遍历整份 `fields`，故人工构造的含被剔类型的存量组件，**保存会被 400 拒绝——这是预期行为**。实测三载体 0 命中，故对真实数据无影响 | FR-0 | ⓪ |
| **AC-19** | 阶段⑤ 上线后，① 对账里 `SUM`/`AVG` 列的差异告警**归零**（以 ① 上线后累积的告警为基准） | FR-18,19 | ⑤ |
| **AC-14** | 端到端耗时：① 后感知 ≈ 0；③ 后后端 900 → ~550ms；④ 后 → ~450ms。**须用 `target/quarkus-app/quarkus-run.jar` 在生产态复测**，dev + 远程库的数字不作数 | 全部 | ②③④ |
| **AC-15** | `tsc --noEmit` 0 错误 + 前端单测与基线一致 + 后端 `mvnw test` **按失败测试方法名清单逐条比对**与基线相同（master 存量 159F/393E，见 §8 K1）。<br>🔧 **口径澄清**：不是比总数——总数相同存在「此消彼长」的假阴性（修好 1 个、新坏 1 个，总数不变）。**必须比对具体的失败方法名集合** | 全部 | 每阶段 |
| **AC-18b** | 前端组件管理表单的字段类型下拉**只展示 6 种**，无法选中 `DATA_SOURCE` / `INPUT` | FR-0b | ⓪ |

> 每条 AC 在 `test.md` 至少对应一条用例（开发期由 `cpq-tester` 编写，主线审核）。

---

## 7. 影响面与风险

### 7.1 命中的反模式（必须点名）

| AP | 为什么命中 | 本任务的处理 |
|---|---|---|
| **AP-41**（prop drilling 漏传 → 报价单 vs 核价单不对齐） | FR-2 要给 `ProductCardProps` 加 `quotationStatus` | **三处调用点全传**，并在 `fronttask.md` 列成可勾选项逐个确认 |
| **AP-50**（详情页/编辑页渲染层 single-source） | FR-1 改取值分支，`QuotationStep2` 与 `ReadonlyProductCard` 必须同步 | 两端同一套判定，AC-2 + 三视图人工核对 |
| **AP-31 / AP-37 / AP-38**（"加载中…"族、cache key 维度） | 触碰渲染取值链与缓存 key | 不改 `driverExpansionKey` 维度；阶段④ 缓存 key 与它们无交集（一个是 driver 展开、一个是结构/行键） |
| **AP-51**（`snapshotRows` 行数权威） | 阶段③ 动 `row_data` 物化 | 不碰行数判定逻辑，只推迟公式列写入 |
| **AP-54**（过滤后下标当底层下标） | 触碰 `QuotationStep2` 渲染/写路径 | 不新增下标映射；沿用现有 `realRowIndex` |
| **AP-62**（换个身份重新取数 = 静默丢数据） | 对账依赖行身份（rowKey）两端一致 | 沿用 BL-0127 已归一的 `buildRawRowKeys` 单一口径，**禁止再造第四份** |

**AP-44 不触发**：本任务不新增 / 不修改任何 `field_type`。

### 7.2 主战场文件

| 文件 | 改什么 | 危险度 |
|---|---|---|
| `QuotationStep2.tsx` | FR-1~FR-5、FR-7~FR-10 | 🔥 **全仓最危险**：24 个任务在它身上叠协议，属 `CLAUDE.md` E2E 强制清单 |
| `ReadonlyProductCard.tsx` | FR-1、FR-2（AP-50 同步） | 高 |
| `ComponentCell.tsx` | FR-4 的 ⚠ 渲染 | 中 |
| `CardSnapshotService.java` | FR-8、FR-11~FR-13、FR-14~FR-17 | 高（13 个任务叠协议） |
| `ConfigureSnapshotService.java` | FR-11（物化器本体） | 中 |
| `ExcelViewService.java` / `LineDiscountService.java` / `SnapshotCollectorService.java` | FR-12 挂 ensure | 中 |

### 7.3 残留风险（只能缓解，不能消除）

| 风险 | 说明 | 缓解 |
|---|---|---|
| **懒算雪崩** | 多行同时脏 → 打开 Excel 时集中算。**成本是转移不是消失** | 分批 / 后台预热 / 进度提示；`ensure-excel-values` 已在承受同款 |
| **缓存失效点漏挂** | 依赖「以后每个新写入口都记得挂 evict」这条人的纪律，机制上消不掉 | 三道防线：**先收敛写入口为唯一入口**（阶段④前置）+ TTL + 诊断端点 |

---

## 8. 已知坑位

| # | 坑 | 出处 | 本任务必须怎么做 |
|---|---|---|---|
| **K1** | **master 全量测试本来就是红的**：159 failures + 393 errors，全指向共享测试库 `element_price_version`（`customer_no='TEST-B5-…'`）脏数据导致的事务毒化 | 2026-08-06 本会话实测 | 判回归**只能做 A/B**（改动前后失败数逐个对比），**不能看绝对值**。AC-15 即此口径 |
| **K2** | **共享测试库的 Flyway 是移动靶**：并发会话把未提交的 `V380` 应用到 `cpq_db`，导致基于旧 HEAD 的分支启动即 `FlywayValidateException` | 同上 + 记忆 `cpq-shared-flyway-history-churn` | 迁移号**开工时才分配**；worktree 里的迁移要 copy 到主仓验证、合并前删副本 |
| **K3** | **worktree 的 dev server 是共享的**：8081/5174 跑的是主工作区代码，worktree 里改的后端**看不到** | `CLAUDE.md` worktree 共享约束 | 后端自检用主仓或临时端口；`mvnw` 必须在 **worktree 的 `cpq-backend/`** 里跑（跑主仓 = 测错树 = 假绿） |
| **K4** | **`REQUIRES_NEW` 自锁**：外层持锁 + 内层另开事务 UPDATE 同一批行 → JTA 60s 超时。现有代码顺序「赋卡片值 → 跑物化 → flush」**不能调换** | `CardSnapshotService` 注释 + `applySubtotalsFromCardValues` | 阶段③ 动物化时序时必须复核这条约束 |
| **K5** | **`grep` 是 ugrep**：中文多的大文件会被误判二进制**静默返空** | 记忆 `cpq-grep-ugrep-binary-pitfall` | 一律用 `/usr/bin/grep -a` |
| **K6** | **前端聚合仍是纯 number**：主干四则运算已用 decimal.js，但 **C\* 族与 `cross_tab_ref` 聚合是 `nums.reduce((s,x)=>s+x,0)`**（`formulaEngine.ts:189-197`），后端是 `PrecisionPolicy.sum`（BigDecimal）。根因不是「没想到」，而是 `evaluateArithmetic` **出口 `.toNumber()`**（`:792` `:905`）把精度在每条表达式边界就丢了 | 2026-08-06/07 本会话实测 | **已升级为阶段⑤ 的正式范围**（D12），不再是"不修" |
| **K7** | **子代理会虚报完成** | 记忆 `cpq-deliver-agents-overreport` | 主线必须按 §6 亲验，不采信「已完成」 |
| **K8** | **并发会话正在同库改价格策略**：本会话实测期间 `CUST-0002` 策略被另一会话打开，导致同一单据两次测量结果不同 | 2026-08-06 实测 | 做值对比实验前先确认无并发写；异常波动优先怀疑外部干扰而非本次改动 |
| **K9** | **「分支引用了 X」≠「分支依赖 X」**：本会话曾据「两个 worktree 命中 `LIST_FORMULA`」断言它不能删，实测后发现那些引用**全部继承自 master**（命中文件集与 master 完全相同，分支还少一个），两分支一行新依赖都没加 | 2026-08-07 本会话自证伪 | 判定「谁依赖谁」必须**与 base 比对增量**（`diff <(git grep -l X master) <(git grep -l X 分支)`），只看「命中了」必然高估依赖 |
| **K10** | **`git grep <rev> -- <path>` 的 pathspec 是相对仓库根**，在子目录里跑会静默返空 | 2026-08-07 本会话踩到 | 跨分支检索一律 `cd` 到仓库根再跑，或用绝对 pathspec |

---

## 附录 A · 实测基线（本方案全部依据，非估算）

### A.1 一格编辑 ≈ 900ms（端点），方法内 795ms 分布

| 阶段 | 中位 | 占比 |
|---|---|---|
| 整行物化 `row_data`（**8 次 `REQUIRES_NEW` 独立事务**） | **356 ms** | **45%** |
| 整卡重算 assemble（PASS1+PASS2） | 120 ms | 15% |
| 小计覆盖 + **整单表头重算** | 78 ms | 10% |
| 加载实体 + 冻结结构 + 抽 baseRows/editRows | 53 ms | 7% |
| flush / clear / 重读实体 | 39 ms | 5% |
| 应用编辑 + 查墓碑 | 12 ms | 1.5% |
| 序列化 cardValues | 0.3 ms | ~0 |

> 写的数据量只有 **27 KB**（`quote_card_values` 23KB + 8 组件 `row_data` 3.8KB）。
> **贵在 9 次独立事务提交 × 远程往返，不是数据量。**

### A.2 缓存收益（用现成 `precomputeCardValuesPrefetch` 实测）

```
无缓存（每次重查重解析）  均值 229.5 ms
有缓存（复用预取）        均值  60.0 ms     → 计算段省 74%
建缓存一次性              282.3 ms          → 必须「建一次命中多次」才划算
```

放回全链路：900ms → ~730ms（端到端 19%）。

### A.3 冻结结构体量（决定缓存内存代价）

| 视图侧 | 份数 | 平均 | 最大 | 全库合计 |
|---|---|---|---|---|
| `QUOTE_CARD` | 51 | 22 KB | 38 KB | 1.1 MB |
| `COSTING_CARD` | 43 | 47 KB | 48 KB | 2.0 MB |

**全库全缓存原始 JSON 才 3.1 MB** → 内存不是约束。

### A.4 测量环境与可信度

全部数字来自 **dev 模式 + 远程共享库（`10.177.152.12`）**，且测量期间有并发会话在同库活动。生产是打包 jar + 可能同机房 DB，**SQL 往返快很多时缓存与懒物化的收益都会缩水**。

动阶段③④ 之前**必须**用 `target/quarkus-app/quarkus-run.jar` 在生产态复测基线 —— 本项目在性能上栽过这个跟头（优化「首存慢」时，实测发现真正卡点不在当初以为的地方）。
