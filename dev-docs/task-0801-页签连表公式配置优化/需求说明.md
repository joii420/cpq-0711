# 需求立项说明 · 页签连表公式配置抽屉 · 布局优化

> 定稿基线 = 同目录 `原型-连表公式抽屉-v1.html`（2026-08-01 用户确认「原型没有问题」）。
> 本文与原型冲突时**以原型为准**；原型未覆盖的技术细节见 §9.2「1:1 可行性核查」。

---

## 1. 基本信息

| 项 | 内容 |
|----|------|
| ⭐ 需求名称 | 页签连表公式配置优化（配置抽屉布局改版） |
| 提出人 / 日期 | 用户 / 2026-08-01 |
| ⭐ 优先级 | P1 重要 |
| 期望上线时间 | 未指定 |
| 需求类型 | 功能增强（前端 UI/UX 改版） |

---

## 2. 需求背景与目标 ⭐

**要解决什么问题 / 痛点**

「配置页签连表公式」抽屉（`TabJoinFormulaDrawer.tsx`，width 1100）当前是**纯上下堆叠**布局，从上到下依次为：试算条 → 公式表达式框 → 运算符/函数工具条 → 规则提示 → SUMIF 构造器 → 页签字段矩阵。由此带来四个具体不便：

1. **可选字段在最底部**，而公式框在最上方 —— 用户「看一眼有哪些字段 → 插进公式」要反复上下滚动，页签多时尤其明显。
2. **公式框只有 `min-height: 52px`**（约两行），稍长的公式就要在窄条里滚动，看不全。
3. **括号错配只有一句文字报错**（`checkParenBalance` 返回「缺少 N 个右括号」），看不出是**哪一对**出问题，长公式里定位靠人数。
4. 试算功能占据顶部约 60~200px（含结果表），实际使用价值低于它占的版面。

**本次要达成的目标**

- 抽屉改为**左右分栏**：左侧常驻「页签组件与可选字段」，右侧「公式配置」，两侧各自滚动，插字段不再需要滚动找。
- 公式框高度从 52px 提到 **min 170 / max 340**，并随内容自动增高。
- 括号**配对可视化**：按嵌套深度着色 + 光标处高亮同一对 + 未闭合标红，替代纯文字报错。
- 移除试算功能，腾出版面。

**不做什么（明确边界）**

- ❌ 不改公式语义、不改 token 序列化/反序列化（`formulaSerialize.ts` 的 `parseFormulaSegments` / `tokensToDrawerExpression` 等**只读复用**）。
- ❌ 不改后端：`TabJoinPlanEvaluator` 及 dry-run 端点**保留不动**（仅前端不再调用）。
- ❌ 不改 `[引用]` 块现有的 5 色语义（蓝/黄/绿/紫/红，见 §4.2.4）。
- ❌ 不改 SUMIF 构造器的**内部表单结构**（仅调整它在版面中的位置：右栏底部折叠）。
- ❌ 不引入第三方编辑器库（CodeMirror/Monaco 等）—— 现有 contentEditable 方案已够用。

---

## 3. 涉及模块 ⭐

- [x] 组件管理（页签组件 / 字段 / 公式）—— 本抽屉是「模板页签连表公式」的配置入口
- [x] 模板管理（拖拽组装 / Excel 视图 / 快照）—— 抽屉挂在模板配置链路下
- [ ] 其余模块无涉及

> **不涉及**三大核心模块的架构基线改动，**不涉及** `field_type` 变动 → **不触发 AP-44**（17 检查点协议不启动）。
> 改动文件均不在 `CLAUDE.md`「协议级改动必跑 E2E」清单内（该清单为 `useDriverExpansions.ts` / `QuotationStep2.tsx` / `QuotationWizard.tsx` / `ReadonlyProductCard.tsx` / `component/types.ts` / `component/FieldConfigTable.tsx` 等）。

---

## 4. 功能详述 ⭐

### 4.1 用户场景 / 操作流程

模板配置人员在「模板管理 → 页签列 → 配置连表公式」打开抽屉，为某个列配置跨页签公式：

1. 打开抽屉 → **左栏**看到本目录所有页签组件及其可选字段（明细 / 小计列 / 页签总计），行键不可比的明细自动置灰；
2. 点左栏字段 chip → token 插入**右栏**公式框光标处（左右并排，无需滚动）；
3. 在右栏用运算符 / 函数 / 条件聚合按钮补全表达式；公式框随内容变高；
4. 输入括号时实时看到配对着色；把光标停在某个括号上，与它配对的另一半同时高亮；漏了右括号则该括号标红波浪线；
5. 括号平衡后「保存」按钮可用 → 保存。

### 4.2 界面与交互（前端）· 逐条规格

> 编号对应用户提的 4 点。所有数值以原型为准。
> 原型路径：`dev-docs/task-0801-页签连表公式配置优化/原型-连表公式抽屉-v1.html`（可交互）

#### 4.2.1 左右分栏 + 加宽（第 1 点）

| 项 | 规格 |
|---|---|
| Drawer 宽度 | `1100` → **`1520`** |
| 布局 | 抽屉 body 内两栏：**左 42% : 右 58%**（原型 `minmax(430px,42fr) / minmax(520px,58fr)`） |
| 滚动 | 两栏**各自独立滚动**（`overflow:auto`，`max-height` 约 78vh），不再整体滚 |
| 左栏内容 | 页签组件与可选字段（现 `TabFieldMatrix`），含标题「页签组件与可选字段 · 点击插入到右侧公式 · 行键不可比的明细置灰」 |
| 右栏内容 | 公式表达式框 → 图例 → 运算符/函数/条件聚合工具条 → 规则提示（黄）→ SUMIF 折叠区（顺序同原型，自上而下） |
| 分隔 | 左右栏之间 1px 分隔线 |

> ⚠️ **左栏内 `TabFieldMatrix` 需改内部排布**：现状是「左侧 160px 固定宽页签名列 + 右侧 chip 区」的**左右结构**，塞进 42%（≈600px）后 chip 区仅剩约 400px，长字段名会被挤成多行难读。原型改为**上下结构**：卡片头部放「页签名 + 行键徽标」，卡片体放 chip 换行。**这是本次真实改动点，不是纯外层布局调整。**

#### 4.2.2 移除试算（第 2 点）

整条试算区移除，包括：

- 顶部蓝底试算条容器（`background:#f0f5ff` 那块）
- `SampleCardPicker` 样本卡选择器
- 「试算」按钮及 `dryRunLoading` 态
- EXCEL 单值结果 `试算结果：xxx`
- NORMAL/SUBTOTAL 逐行结果小表（`试算值` 列）与「试算无行」提示
- 配套状态与函数：`sampleLi` / `dryRunValue` / `dryRunRows` / `dryRunErrors` / `dryRunLoading` / `runDryRun()`

**保留不动**：后端 dry-run 端点、`TabJoinPlanEvaluator` 及其测试。
**`SampleCardPicker.tsx`**：全工程仅此一处引用 → 随之成为死代码，**本次一并删除该文件**（避免留悬空组件；后续要恢复试算可从 git 历史取回）。

#### 4.2.3 公式框加高 + 自动扩张（第 3 点）

`FormulaRichInput` 的容器样式：

| 属性 | 现状 | 改为 |
|---|---|---|
| `minHeight` | `52` | **`170`** |
| `maxHeight` | 无 | **`340`** |
| `overflowY` | 无 | **`auto`**（超过 max 才滚） |

> contentEditable 本身即随内容增高，无需额外 JS 测高。该组件全工程**仅被本抽屉使用**，改死值无跨页面影响（若日后复用，再抽成 props）。

#### 4.2.4 括号配对着色（第 4 点）

**先明确不冲突的前提**：`[引用]` 块的底色**已承载合法性语义**，本次**不动**：

| 块颜色 | 含义 | 判定来源 |
|---|---|---|
| 蓝 | 普通跨页签明细引用 | `classifyRefSegment` 默认合法路径 |
| 黄 | 小计列 | 字段 ∈ `subtotalCols` |
| 绿 | 页签总计 `[X(总计)]` | `tab-total` |
| 紫 | 宿主自身字段 | `tabDef.self` |
| 红 | **非法**：页签/字段查不到、行键不可比、KSUM 内引宿主列 | 校验失败 |

**着色只作用于分组圆括号字符本身**，与块底色分属两个视觉通道（块 = 有边框底色方块，括号 = 无背景粗体字符）：

| 规格 | 内容 |
|---|---|
| 深度配色 | `(` `)` 按嵌套深度轮转 4 色并加粗（原型：`#d4820a` / `#7d3ac1` / `#0a9396` / `#c2185b`），第 5 层起循环 |
| 配对高亮 | 光标紧邻某括号（左侧或右侧）时，它与配对的另一半同时加黄底 `#fff3cd` |
| 错误标记 | 未闭合的 `(`、无匹配的 `)` → 红色 `#cf1322` + 波浪下划线 |
| 状态行 | 公式框右上角显示「● 括号匹配 / ● 括号不匹配」 |
| 图例 | 右栏公式框下方两行图例：第一行「块底色 = 引用语义」，第二行「括号字色 = 配对深度」 |

**保存按钮禁用逻辑不变**：仍由 `checkParenBalance` 决定（着色是可视化增强，不改校验口径）。

### 4.3 业务规则与计算逻辑（后端相关）

**本次无后端逻辑改动。** 公式的解析、行键对齐、聚合语义、保存格式全部沿用现状。

### 4.4 数据字段

**无新增或改动字段**（纯前端展示层改版）。

---

## 5. 接口相关

**无后端改动，无接口新增/变更。**

- 前端不再调用 dry-run 试算端点（端点本身保留）。
- 其余端点（`tab-defs` 页签定义、保存公式）调用方式不变。

---

## 6. 数据与迁移

- 数据库表结构变更：**无**
- 历史数据迁移 / 刷数据：**无**
- 存量兼容：**无影响** —— 本次不改公式串格式与 token 序列化，已保存的公式打开后渲染结果与改版前逐字一致（仅视觉样式变化）。

---

## 7. 权限与角色

不涉及权限改动（沿用模板配置页现有权限）。

---

## 8. 验收标准 ⭐

| # | 验收点 | 判定方式 |
|---|---|---|
| AC-1 | 抽屉宽 1520，左右两栏 42:58，两栏各自独立滚动 | 打开抽屉目测 + 左栏滚动时右栏不动 |
| AC-2 | 左栏页签卡为上下结构（头部页签名+行键徽标 / 体 chip 换行），长字段名不被挤压 | 用字段名 ≥6 字的页签验证 |
| AC-3 | 点左栏任一 chip → token 正确插入右栏公式框**光标处**（非末尾） | 光标置于公式中部后点 chip |
| AC-4 | 行键不可比页签的明细 chip 仍置灰不可点，hover 提示原文保留 | 用行键不同的页签验证 |
| AC-5 | 抽屉内**不存在**任何试算相关 UI | 全抽屉目测 + `grep -n "试算" TabJoinFormulaDrawer.tsx` 应 0 命中 |
| AC-6 | 公式框空态高 ≈170px；内容超出自动增高；超 340px 出现内部滚动条 | 逐行粘贴长公式观察 |
| AC-7 | 括号按深度着色，4 层以上循环 | 输入 `((((1))))` |
| AC-8 | 光标停在括号旁 → 该括号与配对的另一半同时黄底 | 方向键移动光标验证 |
| AC-9 | 未闭合 `(` 或多余 `)` → 红色 + 波浪线，且保存按钮禁用 | 输入 `SUM([投料.金额]` |
| AC-10 | 🔑 **`[X(总计)]` 内的 `(总计)` 不参与括号配对计数** | 输入 `[回料(总计)] + (1)` → 应显示括号匹配 |
| AC-11 | 🔑 **`{...}` 路径块内的圆括号不参与计数** | 构造含 `{}` 块的公式验证 |
| AC-12 | 🔑 **光标不错位**：在括号后连续输入 10 个字符，字符按序落在正确位置 | 手动连打验证（见 §9.2 风险 R1） |
| AC-13 | 中文输入法在公式框内输入不被打断（composition 期间不重渲染） | 切中文输入法打字 |
| AC-14 | 打开既有公式（含 SUMIF token）→ 渲染与改版前一致，保存后公式串逐字节不变 | 取一条存量公式，改版前后各保存一次比对 DB 值 |
| AC-15 | `npx tsc --noEmit` 0 错误；改动的 `.tsx` 经 Vite transform 返 200 | 见 CLAUDE.md「修改后强制自检」 |

---

## 9. 依赖与风险

### 9.1 依赖

- 无新增依赖包（不引入编辑器库）。
- 复用现有：`formulaSerialize.parseFormulaSegments`（块切分与颜色）、`formulaBracketCheck.checkParenBalance`（平衡校验）。
- 无前置模块依赖，可独立开工。

### 9.2 🔑 1:1 可行性核查（回答「能否按原型 1:1 实现」）

**结论：视觉与交互可以 1:1 实现。** 改动全部收敛在 4 个前端文件内，无后端、无数据迁移、无跨页面回归面。

**波及面（已核查）**：`FormulaRichInput` / `SampleCardPicker` / `formulaBracketCheck` **全工程仅被 `TabJoinFormulaDrawer` 引用**，`TabFieldMatrix` 亦仅被它 + 单测引用 → 改动不外溢。

**但原型有 3 处是「演示级简化」，实现时必须补齐**，否则会出现原型上看不出来的 bug：

| 风险 | 说明 | 必须的处理 |
|---|---|---|
| **R1 光标错位（高·已实证）** | `FormulaRichInput.restoreCaret` 遍历时假设「元素节点 = 引用块（带 `data-raw`）」，用 `getAttribute('data-raw') ?? ''` 计长度。新增的**括号 span 没有 `data-raw` → 长度按 0 计**，光标偏移算错。**原型里同样存在这个简化。**<br><br>**实证**（按现状算法逐节点模拟，公式 `SUM([投料.金额])`，DOM = `文本"SUM"` + `span"("` + `块[投料.金额]` + `span")"`）：<br>`offset=4`（应落在 `(` 之后、引用块之前）→ 现状算法返回 **「`)` 之前」**，即光标被放到整个块的**后面**。用户表现为：在 `(` 后打字，字符跑到块后面去。 | 二选一：① 给括号 span 也加 `data-raw`（值为该括号字符）；② 把 `restoreCaret` 改为**递归**下降到元素内部文本节点（`readBack` 本已递归，两者需对齐）。**推荐 ②**，语义更干净且不与 R3 打架。**AC-12 专门验它。** |
| **R2 括号扫描范围（高）** | 原型的括号扫描只跳过了 `[...]` 块，**没跳过 `{...}` 路径块**。而 `formulaBracketCheck.ts` 注释明确：`(总计)` 后缀总在 `[...]` 内、`{...}` 是路径块，二者内部的圆括号**都不是分组括号**。不跳过会误报错配。 | 着色用的扫描必须与 `checkParenBalance` **同口径**：遇 `[` 跳到 `]`、遇 `{` 跳到 `}`。建议把跳过逻辑从 `formulaBracketCheck.ts` 抽成共享函数，**一处维护、两处使用**（校验 + 着色），避免两套规则漂移。**AC-10 / AC-11 专门验它。** |
| **R3 括号 span 可编辑性（中）** | 引用块是 `contenteditable=false`（原子、不可进入）。括号 span **绝不能**照抄该属性，否则用户无法删除/编辑括号。 | 括号 span 不设 `contenteditable=false`，仅加 class 着色；退格删块的 `handleKeyDown` 需确认只对带 `data-raw` 的块生效（若 R1 选方案①给括号加了 `data-raw`，此处必须额外区分 —— 这正是推荐方案②的原因）。 |
| R4 重渲染性能（低） | 每次 `input` 全量重建 DOM，括号 span 使节点数增加。公式长度量级（百字符内）无实际影响。 | 不处理，观察即可。 |
| R5 窄屏（低） | 1520 在 1366 宽笔记本上会挤压。 | 原型已含 `@media (max-width:1100px)` 单栏降级，实现时保留（或按需改 `90vw`）。 |

### 9.3 涉及文件（预估）

| 文件 | 改动性质 |
|---|---|
| `cpq-frontend/src/pages/template/TabJoinFormulaDrawer.tsx` | 主改：宽度、两栏布局、删试算（约 -80 行） |
| `cpq-frontend/src/pages/template/tabjoin/TabFieldMatrix.tsx` | 改内部排布：左右结构 → 上下卡片结构 |
| `cpq-frontend/src/pages/template/tabjoin/FormulaRichInput.tsx` | 高度；括号着色渲染；`restoreCaret` 修正（R1） |
| `cpq-frontend/src/pages/template/tabjoin/formulaBracketCheck.ts` | 抽出「跳过 `[]`/`{}`」共享扫描（R2）+ 新增配对信息计算 |
| `cpq-frontend/src/pages/template/tabjoin/SampleCardPicker.tsx` | **删除**（随试算移除成为死代码） |
| `formulaBracketCheck.test.ts` | 补配对/跳过规则用例 |

### 9.4 关联

- 无关联 Backlog 条目（本需求为新提出，不在 BACKLOG.md 95 条内）。
- 无 PRD 章节变更（纯 UI 改版，不改功能语义）。

---

## 10. 补充说明

- 定稿基线原型：`原型-连表公式抽屉-v1.html`（可交互，含 4 点前后对照与第 4 点评估）。
- 用户对原型 6 个待定稿问题的处理：回复「原型没有问题」→ **按原型取值全部采纳**（42:58 / 1520 / 规则提示常驻右栏底部 / SUMIF 留右栏折叠 / 第 4 点 = 括号配对着色 / 试算整条移除）。
- 第 4 点曾评估另一种理解（同页签引用染同色）：**已排除** —— 会覆盖「红 = 非法引用」这条最重要的提示，代价远大于收益。理由见原型页底评估区。
