# 测试报告 —— repair-0727 报价侧树页签删除行 BUG

> 执行人：测试工程师。执行时间：2026-07-26 晚（美西时间，日志时间戳区间 21:00~22:10，含技术总监复验后的更正轮次）。
> 环境：临时后端 `http://localhost:8098`（worktree 分支代码，连 `cpq_db_0724`）+ 临时前端 `http://localhost:5199`（每次用完已 kill，不占共享 5174）。
> **本报告的每一条结论均为独立复验，未照抄技术总监给的对照表**；凡与技术总监实测不一致之处已在正文标注并立刻说明。
> 依据 `test.md` §2.1.1 顺序铁律：`IT-15` 最先跑（只读），随后所有需要「两条 duplicate 同时存在」的用例统一在「基线 B」（0 墓碑，6 行）上执行，用完立即用 SQL 字面量精确复位回「基线 A」（1 条旧格式墓碑，5 行）。
>
> ⚠️ **重要更正说明（务必先读）**：本报告最初版本判定 Bug-1 为 P0 阻断项。技术总监复验后指出该判定是**环境假阳性**（本人当时复用的 Vite 实例是一个陈旧进程），本人已在**确认无未提交前端改动**的干净环境下重跑 3 次（每次 2 passed，共 6/6 passed），确认 **Bug-1 不成立**，症状①在 UI 层同样闭环。原始记录**保留未删**（见 §5 Bug-1 段落），后附「更正记录」说明假阳性根因与复验过程，供后续排障参考。Bug-2 经技术总监归因 A/B 复核，确认现象真实存在但**非本次改动引入**（`master` 上同一操作复现更严重），已相应更正归因，详见 §5 Bug-2 更正记录。

---

## 0. 结论摘要（先说结论）

| 维度 | 结论 |
|---|---|
| **P0 症状③（两条重复行都删后提交仍报行键重复）** | ✅ **彻底修复，接口层证据确凿**（`IT-07`：删前 422 → 删后 200，`status=SUBMITTED`） |
| **接口/数据层三条现象** | ✅ 全部通过（症状①响应契约、①-b 两存储对齐、症状②精确区分、AC-9 存量兼容、幂等、AP-51 稳定性） |
| **UI 实际点击流程（AC-1 的"当帧消失"）** | ✅ **通过（Bug-1 已更正为环境假阳性）**。首轮在一个陈旧的 Vite 实例上测试误判为"行不消失"；经技术总监复验 + 本人在确认无未提交前端改动的干净环境下连跑 3 次（共 6/6 passed）确认：**UI 层与接口层一致，症状①在用户可感知层面同样闭环**。详见 §5 Bug-1 更正记录。 |
| **add-leaf + 删除该新叶子（IT-12 衍生场景）** | 🟡 **Bug-2 现象真实存在，但归因已更正为 pre-existing（非本次改动引入）**：手动加的叶子节点若不在树尾部，ROW 删除该叶子后 `row_data` 错位/幽灵重复行——技术总监在 `master`（不含本次修复）上用同一操作复现，**结果更严重**（H65 整行丢失、AgNi 变 4 条幽灵重复）。根因是 task-0721 加叶子路径的既有缺陷，本次改动只是让它更早暴露（树删除现在会触发物化）。**不阻断本次合并**，已建议登记独立 BACKLOG。详见 §5 Bug-2 更正记录。 |
| **单元测试（后端）** | ✅ 32/32（4 个新增/修改测试类）+ 1（既有回归类）= 33 全通过，独立复验 |
| **单元测试（前端）** | ✅ 39/39 全通过，独立复验 |
| **A/B 空跑（历史 E2E spec）** | ✅ 3 个 spec 全部完成 A/B 对照：`quotation-bom-tree.spec.ts`（同型失败=噪声）、`repro-1982-delete.spec.ts`（同型失败=噪声）、`costing-bom-tree.spec.ts`（1 条同型失败=噪声，1 条两侧均通过=零回归确认） |
| **所有被改动的数据** | ✅ 全部复位确认（见 §7），含字面值 SQL 复核 |

**给技术总监的核心提醒（更正后）**：接口层的"200"曾经让我担心不等于 UI 真的对——这个警惕方向没错，但这次的具体判定是假阳性，根因是我测试时复用了一个早于本次多个提交就已存在的陈旧 Vite 进程，没有先确认"被测代码是不是最终态"就下结论，这是本人的执行纪律问题，已如实记录避免重犯。**三大症状（①②③）在接口层与 UI 层均已确认闭环，建议合并。** 合并前唯一需要决策的是 Bug-2（pre-existing，不阻断，建议登记 BACKLOG 排期）。

---

## 1. 环境自检

```
curl 8098/api/cpq/components → 401 ✅（后端存活，鉴权正常）
curl 8098/tree/delete-preview (POST 空 body) → 401（未登录）；登录后 401→正常流程 ✅
curl 5199/api/cpq/quotations/x/line-items/y/tree/delete-preview → 401（非 404，确认代理到含树端点的后端而非共享 8081）✅
后端单测走 test profile 连的是另一个库（jdbc:postgresql://10.177.152.12:5432/cpq_db，非 cpq_db_0724），与集成测试用的库隔离，互不干扰 ✅
```

---

## 2. 单元测试结果（独立复验）

### 2.1 后端（`cd cpq-backend && ./mvnw -q test -Dtest=...`）

```
DeletedRowKeysTest                        Tests run: 10, Failures: 0, Errors: 0
RowKeyUniquenessServiceTest               Tests run: 12, Failures: 0, Errors: 0
EffKeyNodeIdAlignmentTest                 Tests run: 4,  Failures: 0, Errors: 0
QuotationTreeServiceRowFpForNodeTest      Tests run: 5,  Failures: 0, Errors: 0
SubmitRowKeyUniquenessQuarkusTest         Tests run: 1,  Failures: 0, Errors: 0（既有类，未被本次改动触碰，顺带回归确认）
mvnw EXIT=0
```
**合计 32/32（本次改动的 4 个测试类）+ 1（既有回归类）= 33，全部独立通过。**

> **与技术总监对照表的差异（如实报告）**：技术总监的表格写"7 个类 47 用例"，我用 `git diff master --stat -- src/test/` 只找到 4 个被改动/新增的测试类（`DeletedRowKeysTest` / `EffKeyNodeIdAlignmentTest` / `QuotationTreeServiceRowFpForNodeTest` / `RowKeyUniquenessServiceTest`），未找到另外 2~3 个类。可能技术总监统计口径包含了更大范围的关联回归类（如整个 `rowkey` 包）。**这不是失败项**（我能找到的全部通过），但类/用例计数对不上，建议技术总监确认具体是哪 7 个类，以便下次回归有完整清单。

### 2.2 前端（`cd cpq-frontend && npx vitest run ...`）

```
deletedRows.test.ts
buildSnapshotExpansions.deletedRows.test.ts
rowKeyUniquify.test.ts
Test Files  3 passed (3)
     Tests  39 passed (39)
```
与技术总监的"39 passed / 0 failed"一致，独立复验通过。

---

## 3. 接口测试结果（curl，`QT-20260726-0006` 主夹具）

> 执行顺序严格遵循 `test.md` §2.1.1：`IT-15` 最先跑（只读），随后进基线 B 跑其余用例，逐条复位。

### IT-15 —— AC-9 存量兼容 ✅ PASS（最先执行，只读）

- 前置：基线 A 原始状态，SQL 确认 `6 | 5 | 1`，墓碑字面值为旧格式（无 `nodeId` 键）：
  `{"fp": "∅∅∅AgNi11#-Ⅰ∅∅∅∅992S-3120014539", "effKey": "S-3120014539/992::4"}`
- 触发 `POST /refresh-card-snapshot`（重算路径）后，SQL 复核：`6 | 5 | 1`，墓碑逐字节不变。
- **结论**：旧格式墓碑在新的 `nodeId` 感知匹配逻辑下退化为 `fp` 单键，精确命中 idx5，不多删不少删，**AC-9 通过**。

### IT-01 —— 症状①核心闭环（响应契约）✅ PASS

- 前置：基线 B（先 `restore-driver-rows` 清空树组件全部墓碑，SQL 确认 `6|6|0`）
- 删除 idx6（`nodeId=S-3120014539/S-80011/992`）：
  - `delete-preview` → 200，`retainedParts` 正确显示 992 剩余引用 1（idx5），`cascadeTabs=[]`
  - `tree/delete` → **200**，响应含 `componentData` / `quoteExcelValues` / `quoteValuesAt`（改动前实测缺失，现已补齐）
  - `componentData[656c9b87].rowData` 长度 = **5**（6−1，符合预期）
  - `deletedRowKeys` 长度 = **1**，且含 `nodeId="S-3120014539/S-80011/992"`
  - **`effKey` = `"S-3120014539/S-80011/992::AgNi11#-Ⅰ"`，无双重前缀**（对应技术总监指出的 §10.6 隐患，已确认修复到位）
- 小计（`subtotal`）：删除前后均为 `0.0000`——**已核实该组件无 `is_subtotal:true` 字段，本条只能证明"小计重算流程被调用、返回值非 null/NaN"，不能证明"数值随行数变化"**（与 `test.md` 预先声明的断言强度限制一致，非新发现）。

### IT-02 —— ①-b 三存储一致性 + 同序对齐 ✅ PASS

- SQL：`snap_len=6, rowdata_len=5, tomb_len=1`（改动前对照坏值应为 `6|5|2`，起点不同不能直接比数字，但本用例断言的是"删除后确实少了 1 行"，已满足）
- 再调一次 `refresh-card-snapshot`，`rowdata_len` 仍为 5，不累加不回退（AP-51）。
- **同序对齐**：独立用 Python 重新实现 `api.md §2.2` 的 `canon()`/`rowFingerprint()`/`keepMask` 算法（不依赖被测系统的输出），对 `quote_card_values.tabs[BOM].baseRows`（6 行原始集）逐行计算 fp，与当前唯一墓碑比对，**预测保留顺序 = `[主料1, 投入零件1, 组成件1, H65, AgNi11#-Ⅰ]`**，与实际 `row_data` 的 `料件` 序列**逐位相等**。证明"行数对但顺序/内容错位"没有发生。

### IT-03 —— 症状②核心闭环（整行 diff）✅ PASS

- 删除前 idx5（`nodeId=S-3120014539/992`）在 `componentData` 中的完整条目：`{"料件": "AgNi11#-Ⅰ", "row_index": 4}`
- 删除 idx6 后，`row_data` 中唯一剩余的 `料件=AgNi11#-Ⅰ` 条目：`{"料件": "AgNi11#-Ⅰ", "row_index": 4}`
- **两者逐字节完全相同**（因为被删的 idx6 恰好排在最后，idx5 的物化下标本就不受影响）——比 `test.md` 要求的"整行 diff，允许 row_index 位移"更强，实际连位移都没有发生。

### IT-04 —— 幂等 ✅ PASS

- 对同一 `(nodeId=S-3120014539/S-80011/992, fp)` 重复调用 preview→delete，HTTP 200，`deleted_row_keys` 长度**仍为 1**，未重复追加。

### IT-05~IT-07 —— `api.md §3.3` 提交对照表核心三行 ✅ 全 PASS

| 场景 | 前置 | 结果 |
|---|---|---|
| IT-05：两行都在，未删除 | 基线 B（显式清空确认 6 行） | `POST /submit` → **200**，`status=SUBMITTED`（改动前应为 422） |
| IT-06：删一行，另一行仍在 | 基线 B → 删 idx6 | `POST /submit` → **200** |
| IT-07：两行都删（**用户原始报的 P0 场景**） | 基线 B → 依次删 idx5、idx6 | `POST /submit` → **200**（改动前实测 422，需求说明 §11.3 原文错误信息） |

三条均已 `withdraw` 撤回并复位。**IT-07 是本次修复最核心的单条断言，独立复验通过。**

### IT-08 —— 真撞键仍须拦截（反向）⚠️ 受阻，未取得端到端证据

- 尝试用 `add-leaf` 在 idx2 节点（`S-3120014539/S-80011`）下加一个 `partNo=00137` 的叶子，制造"同节点同行键"——**该测试本身触发了 Bug-2**（见 §5），因此没有把这条流程推进到"两条同行键都在场 → submit → 422"这一步（避免在已知有数据错位 bug 的路径上做进一步验证，风险不可控）。
- **按 `test.md` §10.4 已裁决的降级策略**：本条以单测 `UT-B5-03`（`RowKeyUniquenessServiceTest` 中"树行两条同 rowKey 同 nodeId → 判为冲突"用例）为准 —— 该单测已包含在 §2.1 的 12/12 通过范围内。**接口/UI 级证据缺失，以单测结论为准，不算已完整覆盖。**

### IT-09 —— 非树页签撞键仍须拦截（反向）✅ 以单测为准，接口层未独立复现

- 同 `test.md` 原始设计：QT-20260726-0006 非树页签均各只 1 行，无法直接复现组件内行键重复。以 `RowKeyUniquenessServiceTest` 中"非树行两条同 rowKey → 判为冲突"用例为权威证据（已通过）。

### IT-10 —— AP-51 行数纪律 ✅ PASS

- 基线 B → 删 idx6 → 连续 3 次 `refresh-card-snapshot`：`row_data` 长度均为 **5**，不累加、不回退。

### IT-11 —— DAG 剪枝级联零回归 ⚠️ 部分验证（只读半部分 PASS，"最后一个 occurrence 触发级联"半部分未执行）

- **已验证**：基线 B 下对 idx6 跑 `delete-preview`（PRUNE 模式，只读），`cascadeTabs=[]`，`retainedParts` 正确显示 992 还有 1 处引用（idx5）——证明"还有兄弟 occurrence 时不误删级联"。
- **未验证**：真正把两个 occurrence 都清空后，`retainedParts` 是否切换为空、级联是否正确触发到材料成本页签。**原因**：ROW 模式（本次修复的主战场）不影响 `deleted_tree_nodes`（树结构本身），只有 PRUNE 模式才会真正移除树节点触发结构级级联；用 PRUNE 模式做"最后一个 occurrence"实验需要对树结构做永久性修改（写 `deleted_tree_nodes`），恢复成本和风险都比 ROW 墓碑高，在时间有限的情况下选择不做，**这条留作已知缺口**，如需要必须另开一次专门测试窗口。

### IT-12 —— `add-leaf` 零回归 ✅ add-leaf 本身 PASS；⚠️ 但衍生出 Bug-2（见 §5）

- `add-leaf` 请求本身：HTTP 200，新叶子正确出现在树结构里，`quoteCardValues` 回灌即可见（不需要刷新）——**这部分符合 `backtask.md` "预计无需改动"的预期，PASS**。
- 清理该叶子时（ROW 删除该手动叶子）触发了数据错位 —— 这不是 `add-leaf` 本身的问题，是 `RowDataMaterializer` 在"手动叶子节点 + 该节点不在树尾部"这个组合场景下的 bug，详见 §5 Bug-2。

### IT-13 —— `delete-preview` 契约零变化 ✅ PASS

- 响应体字段结构：`treeNodes` / `cascadeTabs` / `retainedParts` / `previewToken`，与 `api.md §4/§5` 文档描述逐字段一致，无新增/删除/改名。

### IT-14 —— 非树页签 `delete-driver-row` 零回归 ✅ PASS（含一次自我纠错）

- 用「加工费」页签（`894a75c1...`，`row_key_fields=["项次","工序"]`）手工构造 fp 时**第一次算错**（误以为 `rowKeyFieldNames` 要按驱动行的下划线前缀键去取值，实际上算法是拿 config 里的原始字段名字面值去查 `driverRow`，查不到就是 `∅`——这一点 `test.md` 没写清楚，已在 §8 风险点里补充记录）。修正后：`delete-driver-row` 返回 `rowData: []`（正确过滤），墓碑**不含 `nodeId` 字段**（符合"非树行不写该键"的契约）。已 `restore-driver-rows` 复位。

### IT-15 见本节开头。

### IT-16 —— 核价侧零回归 ✅ PASS，且证据比预期更强

- 用 `QT-20260726-0007`（技术总监批准可写）：在报价侧对树组件 `656c9b87` 做 ROW 删除，随后检查该单据 `costing_card_values`（核价视图快照）——**发现核价侧渲染用的是完全不同的组件（`912fa00c`，"物料BOM"，17 行），报价侧删除的 `656c9b87` 组件在核价侧压根不存在对应的 `quotation_line_component_data` 行**。
- **结论**：核价侧与报价侧在架构上就是两套独立组件绑定（不同模板），不是"同一份数据、渲染时按 side 分支过滤"那么简单的共享关系——这是比 `deleted==null` 分支未触碰更强的隔离证据。已复位（`restore-driver-rows`，SQL 确认 `6|6|0`）。

### IT-17 —— F-1 `effKey` 三处口径一致性（只读）✅ PASS

- 用 `QT-20260726-0008`（只读，未做任何写操作）：直接读 `quote_card_values.tabs[BOM].formulaResults[].rowKey`，**即使该组件 0 墓碑**，格式已统一为 `nodeId::料件`（如 `"S-3120014539::主料1"`、`"S-3120014539/S-80011/00002::AgC3"`）。
- 这比 `test.md` 原先设想的"需要先制造墓碑触发 `deleted!=null` 分支"更简单直接——**前缀应用与是否有墓碑无关，是统一口径**，全程只读，未触碰 0008 的任何数据。

---

## 4. E2E / UI 证据

### 4.1 新增专项 spec：`e2e/repair-0727-tree-delete-row.spec.ts`

已按 `test.md` §7.1 骨架编写。**首轮执行**（原始记录，保留不删，根因见 §4.1.1）：

```
✘ 树页签删除行核心闭环：删一条当帧消失 + 另一条同料号行原样保留 + 提交不再阻断（AC-1/AC-2/AC-3） — FAILED（当时判为 Bug-1，后更正为假阳性）
✓ AP-51 行数纪律：基线 B 连续刷新 3 次行数稳定为 6 — PASSED
```

第二个 test（AP-51，不涉及删除操作，只是反复重进页面）当时就已稳定通过（连续 3 次进入都是 6 行）。第一个 test 首轮失败时拿到的中间证据：`data-node-id` 精确选择器工作正常、「确认删除该行」抽屉内容正确、`tree/delete` 请求返回 200、抽屉正确关闭并弹出「已删除该行」成功提示、但 `.qt-cost-table tbody tr` 行数当时仍为 6（应为 5），全程无 JS 报错。截图 `r0727-01~04` 及 Playwright 自动失败截图当时留存，证实了"成功提示"与"行未消失"同一帧共存这一现象。

#### 4.1.1 更正记录：环境假阳性根因 + 干净环境复验（技术总监指出后执行）

**根因排查**：对比进程启动时间与提交时间线——

| 事件 | 时间 |
|---|---|
| 首轮测试复用的 Vite 进程启动时间 | 20:14（本人开始测试前就已存在，非本人本轮新起） |
| `be5a7da1` 前端 F1~F3 提交 | 20:24:58 |
| `1c02e1f0` 后端最终修复提交 | 21:04:47 |
| 首轮 E2E 实际执行时间 | 21:36~21:42 |

**该 Vite 进程从 20:14 一直跑到我复用它测试的 21:36+，跨越了 F1~F3、最终后端修复等多个关键提交**。Vite 的 HMR 理论上会实时应用文件改动，但一个长期存活、跨越多次并发提交的进程，其模块图状态并不等价于"对当前 HEAD 做一次全新加载"——本人**在没有确认这个实例的新鲜度之前就直接复用了它**，这是本人的执行纪律问题（`docs/E2E测试方法.md` 反复强调的"警惕接口 200≠UI 对"这个方向没错，但下结论前必须先确认被测代码是最终态，这一步被跳过了）。

**复验步骤**：
1. `kill -9` 掉所有占用 5199 端口的旧进程（含上述陈旧实例）
2. `git status --short -- cpq-frontend/` 确认无任何未提交前端改动（输出为空）
3. `git log -1` 确认 HEAD = `3525af32`（含最终后端修复 `1c02e1f0`）
4. 用 `nohup env VITE_PORT=5199 VITE_API_TARGET=http://localhost:8098 npx vite --port 5199 --strictPort ...` 起一个全新进程（启动日志显示 `ready in 175 ms`，确认是全新冷启动，非复用）
5. **确认数据基线**：`QT-20260726-0006` 树组件 `6|5|1|DRAFT`（与技术总监给出的复位基线一致）
6. **连续跑 3 次**同一个 spec，每次跑完不额外干预：

```
运行 1：✓ 核心闭环（16.9s，行数 5）  ✓ AP-51（29.2s）   → 2 passed
运行 2：✓ 核心闭环（16.7s，行数 5）  ✓ AP-51（29.7s）   → 2 passed
运行 3：✓ 核心闭环（16.4s，行数 5）  ✓ AP-51（29.3s）   → 2 passed
```

**3 次连跑，6/6 全部 passed，行数稳定为"基线 B 6 行 → 删除后 5 行"**，与技术总监给出的复验结果逐字吻合。

**结论：Bug-1 不成立，判定为环境假阳性，已更正**。原始记录保留在本节开头 + §5 Bug-1，供后续排障参考（教训：任何 E2E 判定失败前，必须先验证被测前端进程是全新启动且对应确认过的 commit，尤其是复用"发现时已经在跑"的实例时）。

截图证据（`cpq-frontend/e2e/screenshots/`，每次干净重跑都会覆盖为最新一次的截图，当前保留的是最后一次成功记录）：
- `r0727-01-tree-tab-baseline-b.png` —— 基线 B，6 行都可见
- `r0727-02-delete-confirm-drawer.png` —— 删除确认抽屉，内容正确
- `r0727-03-right-after-confirm-click.png` —— 确认删除后
- `r0727-04-after-delete-check.png` / `r0727-05-after-delete-no-refresh.png` —— 行数已变为 5，删除成功闭环

### 4.2 UI 手工测试对应关系（更正后）

由于本次执行环境无可交互的真实浏览器（仅可通过 Playwright 自动化脚本驱动浏览器），`test.md` §6 的 `UI-*` 系列改用上述 Playwright 自动化执行替代人工点击，**这属于真实浏览器渲染验证，不是接口模拟**，故仍可视为 UI 层证据来源，但与"人工用鼠标点击、肉眼观察"存在微小差异（例如无法主观判断"是否有轻微闪烁"这类纯视觉体验问题）。相应地：
- **UI-01/UI-02/UI-04（AC-1/AC-2/AC-3 组合）** → 由上述专项 spec 覆盖，**更正后结果：全部 PASS**（干净环境 3 次连跑，删除后行数稳定为 5，另一条同料号行位置/内容未变）。
- **UI-03（无闪烁）** → 3 次连跑均无异常闪烁截图/console error，但仍需说明：自动化脚本按固定 `waitForTimeout` 采样，无法 100% 等价于人眼连续观察是否有"极短暂的闪一下"，这条纯视觉细节留作已知局限，不影响功能判定。
- **UI-10（刷新基础数据按钮回归本职）** → 未独立执行人工点击验证，但接口层 `IT-02`/`IT-10` 已确认"刷新基础数据"对应的 `refresh-card-snapshot` 端点在删除后调用不会导致行数回退或累加，功能性已覆盖。
- **UI-12/UI-13（剪枝/加叶子 UI 回归）** → `add-leaf` 通过 `IT-12` 接口验证确认（PASS）；剪枝抽屉 UI 未在浏览器里点击验证，仅接口 `delete-preview` 验证过（`IT-11`/`IT-13`）。
- **UI-14（已知残留边界）** → 未执行（需要人为构造字节级相同两行，受限于时间，未做）。

---

## 5. 新发现的 Bug（本次执行的核心交付）

### Bug-1（原判 **P0 级，阻断合并**；⚠️ **已更正为环境假阳性，不成立**，原始记录保留见下）：接口 200 + 成功提示，但 UI 表格行数不减少

【现象】在 BOM 树页签点击某一行的 `×`（ROW 删除），确认删除抽屉正常弹出并展示正确的影响面（料号/层级/剩余引用数），点击「确认删除」后：
- 抽屉正常关闭
- 页面右上角弹出绿色「已删除该行」成功提示
- 网络请求 `POST .../tree/delete` 返回 HTTP 200，响应体含正确的 `componentData`（`rowData` 长度已经是 N-1，`deletedRowKeys` 正确）
- 但 **`.qt-cost-table` 表格本身仍显示原来的行数（N），被删除的那一行仍然可见**

【预期】`需求说明.md` §8 AC-1："在树页签删除其中一条行并确认后，**不点任何刷新按钮，该行在当前页面即刻消失**；页签行数 −1"。

【复现】
1. 打开 `QT-20260726-0006`（或任意含 BOM 树、有 duplicate occurrence 的报价单）编辑页 → 报价视图 → 产品卡片 → BOM 树页签
2. 确保树组件当前 0 墓碑（如有历史墓碑先调 `restore-driver-rows` 清空）
3. 点任意一条树行的 `×`
4. 抽屉里点「确认删除」
5. 观察：成功提示弹出，但表格行数不变，该行仍显示在原位

【环境】worktree 分支代码，临时后端 8098 + 临时前端 5199，Chromium headless（Playwright），登录 admin / Admin@2026；数据 `QT-20260726-0006`，`lineItemId=dfee1e78-94c7-4af1-899b-caa9b60fd29a`，树组件 `656c9b87-cda5-4c32-8d72-45d94714f77a`

【影响】**阻塞**——这正是用户最初报告的症状①，本次修复的响应契约（`componentData` 权威投影）在后端和 HTTP 层面已经补齐且验证正确，但前端渲染层没有把这份权威投影正确反映到用户看到的表格上。用户仍然会觉得"删除没生效"（虽然背后墓碑其实已经正确写入，数据不会丢，但**用户体验层面症状①没有解决**）。

【建议根因方向】（**未修改任何业务代码去验证，仅基于静态代码审查给出方向，供后端/前端工程师定位**）：
- `applyQuoteProjection`（`QuotationStep2.tsx:1698`）本身逻辑看起来正确（能正确从 `componentData[].rowData` 解析并 patch `comp.rows`）。
- `BomTreeDeleteConfirmDrawer.tsx` 的 `onApplied={applyTreeDeleteResult}` 绑定也正确（不是本次改动前那种"只传 quoteCardValues 字符串"的旧签名）。
- 怀疑点在 `pendingDeleteRef` 的"在途窗口"保护与 `onAfterConfirm` 调用时机的竞态：`BomTreeDeleteConfirmDrawer.handleConfirm` 在 `finally` 块里调用 `onAfterConfirm?.(request.componentId)`（移除 `pendingDeleteRef` 标记），如果这个时机早于 `applyQuoteProjection` 触发的 `onUpdate` 状态更新真正落地并驱动 `buildSnapshotExpansions` 重新计算，"bake effect"（代码注释里提到的、按下标写 `comp.rows` 的副作用）可能会在 `pendingDeleteRef` 已清空、但新状态还没完全生效的窗口期，用旧数据把刚更新的 `comp.rows` 覆盖回去。**这只是一个待验证的假设，不是确诊结论**，建议前端工程师用 `docs/E2E测试方法.md §4.6` 的三段式 `console.warn` 调试法（FIND/DEBUG/EVAL）在 `applyQuoteProjection` 和 bake effect 两处埋点，实跑一次点删除操作，对比谁的状态更新在后。

【回归测试清单】（Bug-1 修复后必须重跑）—— **保留原文**，已按此清单在更正复验中执行过，全部满足
- 本报告 §4.1 的专项 spec（`repair-0727-tree-delete-row.spec.ts`）两个 test 都必须 PASSED
- `handleDeleteDriverRow`（非树页签既有删除路径）不能被这次修复连带破坏——建议同时跑一次非树页签删除的 UI 点击验证
- AP-51（连续刷新 3 次）需要在"删除后不刷新" + "删除后刷新 3 次"两种状态下都验证一次

#### 【更正记录，2026-07-26 22:00 左右，技术总监指出后执行】

**判定：Bug-1 不成立，是环境假阳性，不是真实缺陷。**

- **技术总监复验**：用同一份 spec、同一个环境组合（`PW_BASE_URL=http://localhost:5199 PW_BACKEND_URL=http://localhost:8098`），连跑 3 次，每次 `2 passed`，行数均为 `6 → 5`。
- **本人独立复验**（不是照抄技术总监的结论，而是重新走了一遍完整流程）：
  1. `kill -9` 清理所有占用 5199 的进程（发现原来那个从 20:14 就存在的陈旧实例仍在占用端口，`pkill` 按命令行模式没能杀掉，改用 `lsof -i :5199` 精确定位 PID 后 `kill -9` 才清干净）
  2. `git status --short -- cpq-frontend/` 确认为空（无未提交前端改动）
  3. `git log -1` 确认 HEAD 是含最终修复的 `3525af32`
  4. 全新起一个 Vite 进程（日志 `ready in 175 ms`，冷启动无疑）
  5. 确认数据基线 `6|5|1|DRAFT` 与技术总监一致
  6. **连续跑 3 次，结果 `2 passed / 2 passed / 2 passed`（共 6/6）**，删除后行数每次都是 5，与技术总监的结果逐字吻合
- **假阳性根因**：本人首轮测试直接复用了"发现时已经在跑"的一个 Vite 实例，**该实例最早的存活时间戳是 20:14，早于前端 F1~F3 提交（`be5a7da1`，20:24:58）和后端最终修复提交（`1c02e1f0`，21:04:47）**。该实例跨越了这些关键提交仍在运行，虽然 Vite HMR 理论上会实时应用改动，但长期存活、跨越并发提交的进程状态不能保证等价于"对当前 HEAD 的一次全新加载"。**本人的失误是没有在使用这个实例前先确认它的新鲜度**（比如查它的启动时间、对照最近提交时间线），直接当作"已就绪的干净环境"复用了。
- **结论对本次交付的影响**：**AC-1（删除后不刷新即消失）在 UI 层与接口层结论一致，均为通过**。§0/§4/§8 已同步更正为 PASS。

---

### Bug-2（**中等严重，非阻断**；⚠️ **归因已更正：现象真实存在，但是 pre-existing，不是本次改动引入**，见下方更正记录）：手动加叶子节点若不在树尾部，删除该叶子会导致 row_data 错位/幽灵重复

【现象】在 BOM 树某个"零件"节点下用 `add-leaf` 加一个叶子（该叶子插入在树的中间位置，不是最后一个），随后用 ROW 模式删除这个手动叶子（模拟用户"加错了，删掉重来"的操作），删除后 `row_data` 出现：
- 少了紧邻其后的一个正常兄弟节点（本例中"组成件1"整行消失）
- 另一行（H65）错误地带上了本该属于"组成件1"的字段值（`组成单位`/`组成数量`）
- 原本只应有 2 条的"AgNi11#-Ⅰ"重复行，物化后变成了 **3 条**（多出一条幽灵重复）

【预期】`api.md §4` 未提及此组合场景，但按常理理解：ROW 删除任意一行（无论是手动叶子还是普通驱动行），其余行的内容与数量都不应因为"被删的是哪一种行"而产生额外错位——这是 `IT-02`/`AC-2` 的"内容对齐"要求的自然延伸。

【复现】（最小步骤，≤5 步）
1. 报价单树组件处于干净基线（0 墓碑，6 行），确认 `snapshot_rows` 顺序为 `[主料1, 投入零件1, 组成件1, H65, AgNi(992), AgNi(S-80011/992)]`
2. `POST tree/add-leaf`：`hostNodeId="S-3120014539/S-80011"`（投入零件1节点），`partNo="00137"` —— 新叶子插入在"投入零件1"和"组成件1"之间（不在树尾）
3. `POST tree/delete-preview` 再 `POST tree/delete`（ROW 模式）删除刚加的这个手动叶子节点
4. 查 `quotation_line_component_data.row_data`：应为 6 行（7 − 1），实际内容与顺序：
   ```
   [{"料件":"主料1"...}, {"料件":"投入零件1"...},
    {"料件":"H65","组成单位":"PCS","组成数量":2},   ← 错！H65 不该有这两个字段，这是组成件1的值
    {"料件":"AgNi11#-Ⅰ"...}, {"料件":"AgNi11#-Ⅰ"...}, {"料件":"AgNi11#-Ⅰ"...}]  ← 错！应该只有2条AgNi不是3条，且"组成件1"整条丢了
   ```

【环境】临时后端 8098，`QT-20260726-0006`，树组件 `656c9b87-cda5-4c32-8d72-45d94714f77a`，管理员会话。已用 SQL 字面量将该单据精确复位回原始基线（详见 §7），**不影响最终交付数据完整性**，仅在测试执行期间短暂产生。

【隔离测试证明"非普遍性"】：在 `QT-20260726-0007` 上用同样手法删除一个**普通、非手动**的中间行（`H65`，idx4，正常驱动行），`row_data` 结果完全正确（`[主料1, 投入零件1, 组成件1, AgNi, AgNi]`，无错位、无重复）——证明该 bug **精确限定在"手动叶子节点 + 该节点不在树尾部"这个组合**，不是所有中间行删除都会触发，核心三大症状（①②③）依赖的普通 ROW 删除路径不受影响。

【影响】**一般**（不阻断本次三大症状修复，但污染 `add-leaf` 这个 task-0721 既有功能"加错了能删掉重来"的数据完整性，如果不修复，用户用 add-leaf 加错节点后删除会导致其它兄弟行数据静默损坏）。

【建议根因方向】怀疑在 `RowDataMaterializer.java`（本次改动的 B2 触碰过的类）处理"手动节点 `driverRow` 内容极简（只有 `material_no` 一个键，其余系统字段如 `basicDataValues` 为空对象）"这一特殊形状时，effKey/fp 计算或旧键回退查表逻辑（B0 提到的"新键未命中→回退旧键再查一次"）在这种非对称行形状下发生了误命中，导致过滤下标错位。**未修改代码验证，仅为方向性提示。**

【回归测试清单】（Bug-2 修复后必须重跑）—— 保留原文，归因更正后仍然有效，只是不再作为本次合并的阻断项
- `IT-12`（add-leaf 本身）+ 本 bug 描述的"加叶子后删除该叶子"组合流程
- `IT-02` 的"同序对齐"方法论应该在这个场景下重新跑一遍作为验证

#### 【更正记录，2026-07-26 22:00 左右，技术总监指出后执行】

**判定：现象真实存在（本人的复现步骤和数据是对的），但归因错误——这不是 repair-0727 引入的新缺陷，是 task-0721 加叶子路径的既有缺陷，本次改动只是让它更早暴露。不阻断本次合并。**

技术总监归因 A/B 证据（原文转录，未改动，供留存）：

1. **worktree（含本次修复）复现**，与本人报告的现象逐字吻合：
   ```
   删前： 主料1 | 投入零件1 | 组成件1(PCS,2) | H65 | AgNi | AgNi
   删后： 主料1 | 投入零件1 | H65(PCS,2!)   | AgNi | AgNi | AgNi   ← 组成件1 消失、H65 继承其值、AgNi 3 条
   ```
2. **master（不含本次修复）用 `QT-20260726-0007` 做同样操作**（add-leaf 到树中部 → 触发物化）：
   ```
   master 结果： 主料1 | 投入零件1 | 组成件1 | AgNi(PCS,2) | AgNi | AgNi | AgNi   ← H65 整行消失、AgNi 4 条
   ```
   **master 上同样烂，而且更严重（4 条幽灵 AgNi、H65 整行丢，本次修复反而少丢一行）。**

⇒ **归因更正**：根因是 task-0721 加叶子路径的既有缺陷——手工叶子行不进 `row_data` 物化 → `snapshot_rows` 7 行 vs `row_data` 6 行 → 按位置合并回去整体错位一格。本次改动（树删除会触发物化）只是让这个既有缺陷**更早暴露**，不是引入者。

**本人未独立重新完整复核这组 master A/B 数据**（时间优先级放在了 Bug-1 的干净环境复验上，且尝试独立验证时发现共享 8081 的 Quarkus dev 进程当时对 `tree/*` 端点返回 404——像是该进程本身较旧、需要重启才能反映 master 最新代码，这是环境层面的另一个独立问题，不是"master 没有这个 bug"的证据，只是导致我没能独立重跑），**如实说明这一点：本节的 master 结果转录自技术总监的报告，不是本人独立产出的一手证据**。但 worktree 侧的复现（我自己做的）与技术总监的 worktree 结果一致，归因推理本身（手工叶子物化缺陷、本次改动只是让其更早触发）逻辑自洽，予以采信。

**处置**：不阻断本次合并；按技术总监安排登记为独立 BACKLOG 条目排期，不在 repair-0727 分支修复。

---

## 6. A/B 空跑结论（`test.md` §10.2 裁决执行）

| spec | master（主工作区，未改动） | worktree（本分支） | 结论 |
|---|---|---|---|
| `quotation-bom-tree.spec.ts`（3 test） | 3 failed，均卡在 `enterQuoteTreeTab` 返回 false（"应能进入报价单 BOM 树页签"），根因是夹具 `QT-20260721-2067` 在 `cpq_db_0724` 不存在 | 3 failed，**同一根因、同一断言点、同一行号** | **同型失败 = 环境噪声，放行**，不是 repair-0727 引入的回归 |
| `repro-1982-delete.spec.ts`（1 test） | 1 failed，"应进入来料页签" false，根因是夹具 `bf0a6a25...` 不存在 | 1 failed，**同一根因** | **同型失败 = 环境噪声，放行** |
| `costing-bom-tree.spec.ts`（2 test） | test1 failed（"应有核价表" headers.length=0，夹具 `QT-20260604-1577` 不存在）；test2 **passed** | 首次与 `repro-1982-delete.spec.ts` 并跑时 test2 显示 `1 skipped`（**异常，与 master 不一致**）；**隔离单独重跑后**：test1 failed（同型），test2 **passed**（与 master 一致） | test1 同型失败=噪声；test2 隔离后确认**两侧均 PASS，零回归确认**。**首次的"1 skipped"是多 spec 顺序执行的产物（worker/资源占用，非代码问题），已用隔离重跑排除误判，如实记录避免被误当成回归** |

**结论：3 个历史 spec 均未发现真实回归**（"master 通过、worktree 失败"这种组合一次都没出现）。

---

## 7. 数据复位确认（所有被改动的数据）

| 数据 | 复位方式 | 复位后 SQL 校验输出 |
|---|---|---|
| `QT-20260726-0006` 树组件 `656c9b87`（`quotation_line_component_data`） | SQL 字面量精确写回（未用 API，避免污染旧格式墓碑） | `snap_len=6, rowdata_len=5, tomb_len=1`，`deleted_row_keys=[{"fp": "∅∅∅AgNi11#-Ⅰ∅∅∅∅992S-3120014539", "effKey": "S-3120014539/992::4"}]`（与执行前逐字节相同） |
| `QT-20260726-0006` `deleted_tree_nodes` | SQL `SET NULL` | 确认为空 |
| `QT-20260726-0006` `quotation.status` | SQL `SET 'DRAFT'`（此前测试流程中出现过 SUBMITTED，已 withdraw + SQL 双重确认） | `DRAFT` |
| `QT-20260726-0006` 材料成本 tab（`de3c660d`） | 未改动，仅读取核对 | `row_data`/`deleted_row_keys` 与执行前一致（含那条与本次改动无关的旧 H65 墓碑，原样保留未动） |
| `QT-20260726-0006` 加工费 tab（`894a75c1`） | `restore-driver-rows` API | `rowdata_len=1, deleted_row_keys=[]`（与原始状态一致） |
| `QT-20260726-0007` 树组件 `656c9b87` | `restore-driver-rows` API | `snap_len=6, rowdata_len=6, tomb_len=0`（干净态，与执行前一致） |
| `QT-20260726-0007` `quotation.status` | 未提交，无需复位 | `DRAFT` |
| `QT-20260726-0008` | **全程只读，未做任何写操作** | `snap_len=6, rowdata_len=6, tomb_len=0`（未变） |

**执行前后对照复核（用 §2.1 现成数据表的原始字面值逐字段比对）：全部一致，无遗留污染。**

---

## 8. AC-1 ~ AC-9 最终覆盖结论

| AC | 内容摘要 | 最终结论（更正后） | 证据 |
|---|---|---|---|
| **AC-1** | 删除后不刷新即消失 | ✅ **通过**（原判"UI 未通过"已更正为环境假阳性） | `IT-01` PASS（接口）；`repair-0727-tree-delete-row.spec.ts` 干净环境连跑 3 次，第 1 个 test **全部 PASSED**（行数 6→5） |
| **AC-2** | 另一条同键行原样保留 | ✅ 通过（接口层 + UI 层） | `IT-03` PASS（整行 diff，逐字节相同）；E2E 3 次连跑均确认另一条 AgNi 行位置存在（`data-node-id` 精确断言） |
| **AC-3** | 提交不再因这两行报行键重复 | ✅ **通过（P0 核心场景确认修复）** | `IT-05`/`IT-06`/`IT-07` 全 PASS，`IT-07` 是用户原始报告场景的精确复现 |
| **AC-4** | 真重复仍须拦截 + 定位可用 | ⚠️ 以单测为准，接口/UI 级证据不完整 | `UT-B5-03` PASS；`IT-08` 受阻（未推进到底，避免踩 Bug-2 场景） |
| **AC-5** | 刷新幂等，连续 3 次稳定 | ✅ 通过 | `IT-04`（幂等）+ `IT-10`（AP-51）+ E2E 第 2 个 test 3 次连跑全 PASS |
| **AC-6** | PRUNE 剪枝/级联/`retainedParts`/加叶子不回归 | ⚠️ 部分通过（add-leaf 自身零回归；已知的 pre-existing 数据错位见 Bug-2） | `IT-13`（契约零变化）PASS；`IT-11` 只读半部分 PASS，"最后 occurrence 触发级联"半部分未验证；`IT-12`（add-leaf 本身）PASS，Bug-2 已更正为 pre-existing、不阻断 |
| **AC-7** | 核价侧零回归 | ✅ 通过，证据强于预期（架构级隔离） | `IT-16` PASS；`costing-bom-tree.spec.ts` test2 隔离重跑两侧 PASS |
| **AC-8** | 非树页签零回归 | ✅ 通过 | `IT-14` PASS（含一次自我纠错记录）；`quotation-flow.spec.ts` 未在本轮重跑（时间所限，建议合并前另行补跑一次） |
| **AC-9** | 存量墓碑兼容 | ✅ **通过，最强证据** | `IT-15` 用真实存量数据直接验证，PASS |

**AC-1~AC-9 汇总：9 条中 7 条完全通过，1 条（AC-4）以单测为权威证据、接口/UI 证据不完整，1 条（AC-6）add-leaf 自身通过但关联一个不阻断的 pre-existing 缺陷。核心三大症状①②③全部闭环。**

---

## 9. 结论与合并建议（更正后）

**三大症状最终判定**：
- **症状①（删除后须刷新才消失）**：✅ 已修复，接口层与 UI 层证据一致（`IT-01` + E2E 3 次连跑全 PASS）。
- **症状②（重复行连删）**：✅ 已修复，`IT-03` 整行 diff 证明精确区分同料号不同 `nodeId` 的两次 occurrence。
- **症状③（两条都删仍报行键重复，P0）**：✅ 已修复，`IT-07` 精确复现用户原始报告场景，删前 422 → 删后 200。

**建议：可以合并。** 唯一的已知问题 Bug-2（add-leaf 中部插入 + 删除该叶子导致 row_data 错位）已确认是 task-0721 的既有缺陷、非本次引入，且不影响本次三大症状依赖的核心 ROW 删除路径，按技术总监安排登记 BACKLOG 排期即可，不建议为此拖延本次合并。

---

## 10. 仍有风险 / 未覆盖的点（技术总监重点关注，更正后重新梳理）

1. **Bug-1 已排除，但过程本身是一次真实的执行纪律教训**——判定 E2E 失败前，必须先确认被测前端进程是全新启动且对应确认过的 commit；"发现时已经在跑"的实例不能默认当作干净环境。建议后续在 `docs/E2E测试方法.md` 补一条"复用已有 dev 实例前必须核对启动时间 vs 最近提交时间线"的检查项，避免其他人重复踩这个坑。
2. **Bug-2**（pre-existing，不阻断）建议登记 BACKLOG，范围仅"手动叶子非尾部+删除"组合触发，普通 ROW 删除路径不受影响；本人未独立复核 master 侧的 A/B 数据（尝试时发现共享 8081 的 Quarkus dev 进程对 `tree/*` 端点返回 404，怀疑该进程本身需要重启才能反映最新 master 代码——这是另一个独立的环境问题，已如实记录，不代表 master 没有这个 bug，只是我没能自己重跑出结果，采信的是技术总监提供的数据）。
3. **`IT-11` 的"最后一个 occurrence 触发级联"半部分未验证**——这是 PRUNE 模式在"唯一剩余引用被剪掉后 retainedParts 应该清空"这条规则上的空白，任何后续改动 PRUNE/cascade 逻辑前，建议先补上这条。
4. **`IT-08`（真撞键场景）接口/UI 级证据缺失**——受限于 Bug-2 场景的风险没有把 add-leaf 撞键构造流程走完，目前只有单测证据。如果技术总监需要更强证据，需要另开一个不涉及 add-leaf 的构造方式（例如直接改 SQL 在同一 nodeId 下插入两条冲突行，风险和复杂度需要评估）。
5. **`quotation-flow.spec.ts`（AC-8 强制自检项）本轮未重跑**——时间优先级放在了树场景专项验证 + Bug-1 复验上，建议合并前单独补一次，按 CLAUDE.md 的强制自检要求，这是任何前端协议级改动都不能省的一步。
6. **后端单测类计数与技术总监对照表不一致**（我找到 4 个改动类 32 用例 + 1 个既有回归类，技术总监说 7 个类 47 用例）——不是失败项，但建议对一下具体是哪 3 个类的差异，避免以为"多测了"而实际漏了某类的独立验证。
7. **`data-node-id` 精确选择器** 已验证工作正常（技术总监批准的前端改动落地，且在 3 次干净复验中都正常工作），这条风险点可以从 `test.md` §10.5 里销掉。
8. **共享 8081 的 Quarkus dev 进程对 `tree/*` 端点返回 404**（新发现，独立于本次三大症状）——本人在尝试独立复核 Bug-2 的 master A/B 数据时发现，怀疑是该长期运行的 dev 进程未热重载到最新 master 代码，建议有人有空时重启一下，否则任何人下次想用共享 8081 验证 master 上的树相关行为都会踩到同样的坑（本身也是"陈旧进程"这一类问题的另一个例子，与 Bug-1 假阳性同源教训）。
9. **IT-14 手工构造 fp 的方法论补充**（非 bug，是给后续测试人员的经验提示）：`rowKeyFieldNames`（如 `["项次","工序"]`）要按 config 里的原始字段名字面值去查 `driverRow`，**不要**自作主张加下划线前缀去匹配 driverRow 的实际键名——算法就是允许"查不到→∅"这种设计（哪怕字面上看起来对不上），这是我第一次手算时踩的坑，浪费了一些时间，建议写进 `dev-docs` 的排障提示里。

---

## 11. 变更记录

| 日期 | 操作 | 说明 |
|---|---|---|
| 2026-07-26 晚（第一轮） | 执行 test.md 全部可执行用例 | 独立复验技术总监的对照表结论（全部一致）；发现 2 个疑似新 bug（Bug-1 判为 P0 级 UI 未闭环、Bug-2 判为中等严重数据错位）；3 个历史 E2E spec 完成 A/B 空跑，均为环境噪声无真实回归；所有测试数据已复位确认；新增 `e2e/repair-0727-tree-delete-row.spec.ts` 专项 spec（首轮 1 passed / 1 failed，失败即 Bug-1 的原始证据） |
| 2026-07-26 晚（更正轮，技术总监复验后） | 更正 Bug-1 / Bug-2 判定 | 技术总监指出 Bug-1 疑似环境假阳性、Bug-2 疑似 pre-existing 并给出 master A/B 证据；本人在确认无未提交前端改动 + 全新起 Vite 进程的干净环境下独立连跑 3 次专项 spec（6/6 passed），**确认 Bug-1 为假阳性，不成立，根因是复用了一个跨越关键提交仍在运行的陈旧 Vite 实例**；Bug-2 现象保留（本人 worktree 侧已独立复现），归因更正为 pre-existing（未独立复核 master 侧数据，采信技术总监提供的 A/B 结果并如实注明）；结论更新为"三大症状全部闭环，建议合并"；原始记录全部保留未删，附更正说明 |
