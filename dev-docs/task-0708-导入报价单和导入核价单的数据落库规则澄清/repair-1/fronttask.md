# 前端任务 · repair-1 生产料号落库补全

> 结论:**本次前端零代码改动。**

## 为什么不改前端

本修复只补全后端 production_no 的落库(material_master 加列 + 各表主行/映射表写值)+ 组成料号 calc_type 语义,全部在 DB 与后端 handler 层:
1. 导入文件驱动,前端只上传 Excel,不做列映射;
2. 接口契约不变(见 api.md),无新增前端要传/要渲染的字段;
3. 验收在 DB 层(生产料号是否落到各表主行),不含页面展示要求。

## 下游影响(本次不做,已在 BACKLOG)

`material_master` 新增 `production_no`、V6 表 material_no 值语义,若将来要在前端 V6 基础资料查询页展示生产料号/校正标签,归 **[BL-0037] V6 基础资料查询页料号列标签校正 + 可选展示新列**,择期处理,不在本修复范围。

## 交付物

- ✅ 无代码改动。
- ✅ 本文档作为"前端已评估、无需改动"的留痕。
