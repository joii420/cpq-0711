# 前端任务 · task-0708 导入报价单/核价单落库料号语义纠偏

> 负责人:cpq-frontend｜优先级:P0｜**本次前端零代码改动**

---

## 1. 结论:本次前端无需改动

技术总监已做前端影响面评估,结论为**零改动**,依据三条(全部成立):

1. **导入是文件驱动**:前端只上传 Excel 文件,列名解析→落哪个字段全在后端 handler,前端不做任何列映射 UI。
2. **接口契约不变**(见 api.md):`POST /v6/quote`、`POST /v6/pricing` 的请求(`customerId+file` / `file`)与响应(`ImportResultDTO`)结构均不变,**不新增前端要传或要渲染的字段**。
3. **验收在 DB 层**:本任务验收标准是"数据正确落库",不含任何页面展示要求。

因此本次不新建/不修改任何前端页面、组件、service。

---

## 2. 已做的影响面核查(留痕,便于验收方复核)

技术总监已 grep 全前端料号引用,分三类,均确认本次不受影响或不在范围:

| 前端位置 | 数据链 | 本次结论 |
|----------|--------|----------|
| 核价工作台 `CostingPartDataPage`、核价汇总、`PartVersionDrawer`(宏丰料号/`hfPartNo`) | V76 `costing_part_*` + `hf_part_no`(**另一套表**,非本次导入的 V6 表) | 不受影响 |
| 报价卡片 `QuotationStep2`/`ReadonlyProductCard`(生产料号/`productPartNo`) | 走 `internal_material` + V6 表(候选 API),**非**本次落库主链 | 不受影响 |
| 基础资料查询 `V6BomQueryTab`/`V6BomItemDetailDrawer`、`CustomerMaterialMappingTab`、物料主数据页 | 直接读本次要改的 V6 表 `material_no` | ⚠️ 潜在标签语义错位 → 已转 **BACKLOG BL-0037**,不在本次范围 |

---

## 3. 需前端知悉的下游影响(本次不做,已登记 BACKLOG)

本任务后,V6 表 `material_no` 的**值语义翻转为销售料号**。第三类页面(直接读 V6 表)将来可能出现:
- 列标签仍写旧名(宏丰料号/生产料号),与实际显示的销售料号**错位**;
- 用户按生产料号搜索会搜不到(数据已按销售料号建键);
- 新增的 `production_no`/`material_part_no` 默认不展示。

以上均为**语义一致性优化**,不属本次验收范围,已登记 **[BL-0037] V6 基础资料查询页料号列标签校正 + 可选展示新列**(P2,规模 S),待择期。届时若排到,前端需:校正标签为「销售料号」、按需加列展示 `production_no`/`material_part_no`、对齐搜索键。

---

## 4. 本次前端交付物

- ✅ 无代码改动。
- ✅ 本文档作为"前端已评估、无需改动"的留痕,供验收方复核第 2 节影响面结论。
