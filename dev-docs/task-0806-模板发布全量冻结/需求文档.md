# task-0806 模板发布全量冻结

- **立项日期**：2026-08-06
- **类型**：task（架构级缺陷修复 + 不变量补强）
- **状态**：**待确认**（🚦 闸门 A —— 需求文档 + `backtask.md` / `fronttask.md` / `api.md` 已齐备，等待用户确认后方可建分支写代码）
- **优先级**：P0 ｜ **BACKLOG**：`BL-0133`
- **关联**：worktree 分支 `feat/task-0806-template-freeze`（基于 `HEAD`）｜ 合并 commit：待填
- **范围**：`Template`（`templateKind` = QUOTATION + COSTING）
- **配套文档**：`backtask.md` / `fronttask.md` / `api.md` / `test.md`（测试期产出）/ `test-report.md`（测试期产出）
- **原型**：不需要（无新页面 / 无新交互，前端仅一处文案与告警等级调整）

> **唯一权威口径**。本文档与代码或其他文档冲突时以本文档为准。
> §6 的 `AC-N` 是交付验收依据，每条必须在 `test.md` 有对应用例。

---

## 1. 背景与目标

### 1.0 目标

模板发布（PUBLISHED）后，**其渲染所依赖的全部配置冻结成一份不可变快照**。达成后：

- 改任何组件配置，**已发布模板与其报价单的渲染结果一字节不变**
- 想让新配置生效，**只能** `createNewDraft` → 改 → `publish` 出新版本
- 快照读不到时**显式报错**，绝不静默回落活表

### 1.1 一句话

模板发布（PUBLISHED）之后，**页签内部的字段定义、组件公式、驱动路径这些「内容层」仍然是活的** —— 在「组件管理」里保存一次组件，所有引用该组件的已发布模板快照会被**自动、静默、无提示**地改写，连带改变在途报价单的渲染结果。

### 1.2 这是代码漂移，不是既定设计

`docs/三大核心模块基线.md:178` 写明了 snapshot 的存在理由：

> 为什么需要 snapshot：组件管理改字段后，老模板的历史报价单**不能反向受影响**（否则历史报价改写）。

同文档 `:182` 定的契约是「组件管理改字段 **+ 调 H1 端点时**同步刷新所有 PUBLISHED 模板 snapshot（**但只在用户明确触发时**）」。
同文档 §10.2 第 4 条：「模板新版本通过 `createNewDraft` 创建，**不直接动 PUBLISHED**」。

而代码实现：`ComponentService.update:733` **每次保存组件就无条件自动调用** `refreshSnapshotsByComponent`。「只在用户明确触发时」这个限定词在实现里根本不存在，§10.2 第 4 条也被同一条路径绕过。

`TemplateService.refreshSnapshotsByComponent:333-334` 的注释坦承了这一点：

> 语义说明: 这刻意破坏 PUBLISHED 模板的 "版本快照不可变" 约定，因为配置层变更必须对所有视图同步生效

**本任务不是推翻架构，是把实现拉回它自己写明的基线。**

### 1.3 三类活穿透（本次要全部堵死）

| # | 活穿透形态 | 位置 |
|---|---|---|
| ① | `refreshSnapshotsByComponent` 改写已发布模板 `components_snapshot` | `TemplateService:340-414`，4 个调用点 |
| ② | `component` 实体 23 个字段中 **8 个从未进过任何 snapshot** | 见 §1.4 |
| ③ | 渲染期 **10 处**直接读活 `component` / `template_component` 表 | 见 §1.5 |

### 1.4 ② 从未被冻结的 8 个字段

`rowKeyFields`、`sortField`、`elementCodeField`、`elementPriceField`、`elementCurrencyField`、`columnCount`、`status`、`directoryId`。

**字段账（口径统一，全文以此为准）**：`Component` 实体共 **23** 个字段 = `id` + `createdAt` / `updatedAt`（3 个非配置）+ **18 个渲染配置字段** + `status` / `directoryId`（2 个刻意不冻，见 §5.1.3）。18 个需冻字段中，**旧 snapshot 只冻了 12 个**，本次补齐剩余 **6 个**（上列前 6 个）。

上列 8 个中，前 5 个直接改变渲染结果：

- `rowKeyFields` —— driver 行的业务标识；改了会让已发布模板的快照**命中不了**（历史坑参见行键唯一化消歧）
- `sortField` —— 「快照组装层按该字段对 driver 行做数字感知升序排列」，直接决定行序
- `element*Field` —— 决定元素单价是否接价格策略（task-0729 B7）

**根因**：这三类都是「组件级角色字段」。`Component.java:120` 的注释自己写着 ——

> 组件级角色字段，**不进 `fields` 的 config JSON（绕开 AP-44 17 点联动）**

它们当初为了绕开 AP-44 那 17 处协议传播的成本被挂在 `component` 表列上，而 snapshot 冻的恰恰只有 `fields`。**绕开 AP-44 的同时顺带绕开了冻结** —— 这是无意的副作用，不是有意的设计。

### 1.5 ③ 渲染期 10 处活表读

| 文件 | 行 | 读取内容 |
|---|---|---|
| `CardSnapshotService.java` | 1072 | `JOIN component c` 取 `c.data_driver_path` 筛 driver 组件 |
| `CardSnapshotService.java` | 1299 | `c.id, c.bom_recursive_expand` |
| `CardSnapshotService.java` | 1640 | `c.bom_recursive_expand = true` 计数 |
| `CardSnapshotService.java` | 2858 | `c.id, c.bom_recursive_expand, c.tab_type` |
| `CardSnapshotService.java` | 2929 | `c.tab_type = 'BOM'` |
| `ConfigureSnapshotService.java` | 856 | `c.data_driver_path` |
| `ExcelViewService.java` | 519 / 565 / 1094 / 1119 | `Component.findById` / `TemplateComponent.list` |

`publish:244-248` 的注释已明确承认这个缺口：「当前运行期渲染逻辑（`ConfigureSnapshotService`/`BomTreeRenderService`/`overlayTreeTabsFromFrozenSnapshot`）均直接查活的 component 表，不读这里」。

### 1.6 爆炸半径（实测，2026-08-06，`cpq_db_0724`）

| 指标 | 值 |
|---|---|
| PUBLISHED 模板 | **17**（QUOTATION 15 + COSTING 2），DRAFT 0，ARCHIVED 0 |
| `template_component` 行 | **149** |
| 被引用的组件 | **53** |
| 一个组件被几个已发布模板共享 | 平均 **2.8**，**最多 6** |
| 引用 PUBLISHED 模板的报价单 | **54** |
| `fields_override` / `data_driver_path_override` 非空 | **0 / 0**（与基线 §3.3「已废弃全清空」一致） |

**即：改一个组件，最多同时改写 6 张已发布模板 + 其下所有在途报价单。**

#### 1.6.1 补充实测（2026-08-07，测试工程师编用例时暴露 → 主线核实）：**被改写的是整条版本历史**

全库 **零个组件跨 `template_series_id` 共享** —— 所以上面「6 个模板」必然是**同一系列的 6 个历史版本**。以 `COMP-0045` 所在系列为例：

| 版本 | 状态 | 发布时间 | 挂着的报价单 |
|---|---|---|---|
| v1.0 | PUBLISHED | 08-05 02:28 | 1 |
| v1.1 | PUBLISHED | 08-06 00:26 | 1 |
| v1.2 | PUBLISHED | 08-06 00:29 | 1 |
| v1.3 | PUBLISHED | 08-06 00:34 | 1 |
| v1.4 | PUBLISHED | 08-06 00:39 | 2 |
| v1.5 | PUBLISHED | 08-06 11:09 | **12** |

**结论比 §1.6 的表述更严重**：这不是「6 个不相干的模板被改」，而是「**一条版本历史被整体改写**」。挂在 v1.0 上的报价单，用户以为它冻结在 8-05 的配置；但只要今天有人保存一次 `COMP-0045`，v1.0 的快照立刻变成今天的配置。

**版本号当前是假的** —— v1.0 到 v1.5 全都显示同一份（最新的）配置，版本之间没有任何实质差异被保留。叠加 D6（不自动归档，旧版停留 PUBLISHED）+ `matchCustomerQuoteTemplate:534` 返回整个版本列表供用户挑选，用户挑 v1.0 想要旧配置，拿到的其实是今天的配置。

> 这条同时说明：本任务修复后，**`frozen-drift`（FR-9）的价值比原先设想的更高** —— 它是第一次让「这个历史版本到底冻的是什么」变得可查。

---

## 2. 范围

### 2.1 本期做

| # | 事项 | 对应 FR |
|---|---|---|
| 1 | 新建 `template_component_snapshot` 关系表，`publish()` 时冻结全部渲染配置 | FR-1 |
| 2 | `components_snapshot` jsonb 改为由新表**派生**（前端契约不变） | FR-2 |
| 3 | `refreshSnapshotsByComponent` **整体退役**，4 个调用点逐个处置 | FR-3 |
| 4 | 补齐 6 个从未进快照的渲染配置字段 | FR-4 |
| 5 | 渲染期 10 处活表读收口到 `PublishedTemplateReader` | FR-5 |
| 6 | 快照 miss 显式报错，禁止回落活表（含 SQL 视图 fallback） | FR-6 |
| 7 | 保留的 admin 后门加「预览 + 审计 + 告警」 | FR-7 |
| 8 | ~~存量一次性对齐~~ → **改为「不迁移存量，由用户手工重新发布」**（D16）+ 过渡期「未冻结」提示（D17）+ 归档自动补冻（D18） | FR-8 |
| 9 | `frozen-drift` 版本差异视图（仅 API） | FR-9 |
| 10 | 换模板护栏：修正 `CopyQuotationDrawer` 的误导性文案 | FR-10 |
| 11 | 文档固化：PRD + 三大基线 + 方案制定前必读 + 反模式 | FR-11 |

### 2.2 本期明确不做

| 不做 | 原因 / 去向 |
|---|---|
| `ConfigTemplate`（选配配置模板）守卫补全 | 问题性质不同（守卫缺失非快照穿透），登记 BACKLOG 单独修 |
| `ConfiguratorTemplate`（3D 选配）守卫补全 | 同上；且 `feat/task-0712-selection-config` 分支未合，会撞车 |
| 堵死 admin 后门端点 | 用户决策：保留，改为「预览 + 审计 + 可观测」（§5.2.3） |
| 报价单层再冻一份快照（纵深防御） | 模板层冻死后已足够；`BL-0136` |
| 发新版时自动归档老 PUBLISHED | **D6 维持现状**。只修文档错误表述，不改行为 |
| 批量派生草稿 / 批量发版 | **D7**，登 BACKLOG。先用 `frozen-drift` 看痛感 |
| 修 `BL-0129`（换模板丢数据） | **D8** 本期只加护栏。但严格版本化会把这条已坏路径变成高频路径 → `BL-0129` **提优先级到 P1** 并关联本任务 |
| `frozen-drift` 前端 UI | **D10**，登 BACKLOG |

---

## 3. 澄清结论与决策记录

> 与用户对话中拍板的每一条都落在这里。**口头说过但没落文档 = 不存在** —— 后续 Agent 只认文档不认历史对话。

### 3.1 决策表（用户拍板，不再重议）

| # | 决策项 | 结论 | 理由 | 被否决的方案及原因 |
|---|---|---|---|---|
| D1 | 改组件后如何让已发布模板生效 | **严格版本化**：只能 `createNewDraft` → 改 → `publish` 出新版本 | 快照真不可变才有意义；版本号才能对应确定的渲染结果 | ❌「默认冻结 + 显式同步按钮」——「不可变」退化成「未经批准不可变」，仍留改写通道；❌「发新版 + 存量批量迁移工具」——日常流程被运维工具绕过 |
| D2 | 冻结做到哪一步 | ①+②+③ **一步做到位**；admin 后门本次保留 | 半冻等于没冻：②冻了但③不读、或③读了但②没冻，都留活穿透 | ❌「先 ①+② 二期做 ③」——一期结束时"不可变"仍不成立；❌「连 admin 后门一起堵死」——用户要保留紧急通道 |
| ~~D3~~ | ~~存量已发布模板处置~~ | ~~一次性对齐后冻死~~ | — | **🔄 2026-08-07 已被 D16 推翻** |
| **D16** | **存量处置（取代 D3）** | **不迁移存量。业务逻辑修正后由用户手工重新发布模板** | 用户口径「都是测试数据，重要的是机制」。重新发布产生的每份快照都经人工触发，不用纠结「承认现状即基线」，也省掉迁移脚本自身的正确性验证负担 | ❌ D3 一次性对齐——把未经审阅的现状固化成基线；❌ 体检后再对齐——人工成本高 |
| **D17** | 过渡期（PUBLISHED 但尚未重新发布）渲染行为 | **显式提示「请重新发布」**：不报错、不读活表，返回可识别的「未冻结」状态 | 用户一眼知道该干什么，不会当成系统故障 | ❌ 直接报 500——17 模板 / 54 张单在重发完前全打不开；❌ 暂时回落活表 + 告警——把刚拆掉的「静默回落」又装回去，而且没人看日志 |
| **D18** | ARCHIVED 模板的快照 | **归档时自动补冻**：`archive()` 若无快照则按当时活配置补一份再归档 | 归档是终态、无法重新发布，不补则其历史报价单永久打不开 | ❌ 归档前强制先重发——多一道阻断；❌ 不管——将来必然出现「永久打不开的历史单」 |
| **D19** | 「零快照行」vs「部分缺行」 | **两分**：零行 = 未冻结（正常过渡态，不报错）；有行但缺某 sortOrder = **快照损坏，抛 500** | 混为一谈会让「数据被后门/裸 SQL 删了」被误判成「还没发布」，正好丢掉本任务最想要的那个报警 | ❌ 一律抛错（过渡期不可用）；❌ 一律当未冻结（丢失损坏检测） |
| D4 | 模板范围 | 只做 `Template`（QUOTATION + COSTING） | 另两套模板的病是「守卫缺失」，修法完全不同，混入会撑大回归面 | ❌「+ ConfigTemplate」「+ ConfiguratorTemplate」——见 §2.2，各自登 BACKLOG |
| D5 | `component.status` 语义 | 停用 = **新模板不能再选它**；已发布模板与已生成报价单**不受影响**。故 status **不进快照、也不进渲染路径** | 停用是生命周期开关，不是渲染配置 | ❌「连 status 一起冻死」——冻了反而让"停用"对老模板产生歧义；❌「渲染期实时读 status 并隐藏页签」——会让停用影响已发布模板，违背用户语义 |
| D6 | 发新版后老 PUBLISHED 是否自动归档 | **维持现状：不自动归档**。只修正基线 §3.4 的错误表述（见 §3.3） | 自动归档会撞 `checkNoInProgressQuotations` 阻断（在途单会挡住归档），引入新行为与新回归面 | ❌「自动归档」——阻断路径未想清；❌「UI 标记旧版 + 默认选最新」——要动前端，破坏本期最小前端改动面 |
| D7 | 批量派生草稿 / 批量发版 | **本期不做** → `BL-0138` | 先用 `frozen-drift` 当发版决策入口，确认痛感真实存在再造工具 | ❌「本期一并做」——在不知实际频率时提前造工具；批量 publish 的部分失败处理会显著撑大本期 |
| D8 | 在途 DRAFT 报价单升级路径 | **本期不做功能，加一道护栏**（见 §5.5） | 根因 `BL-0129` 在另一条链路上；但现有文案在骗用户，必须先说实话 | ❌「本期一并修 BL-0129」——根因不在本任务链路，会失控；❌「什么都不做」——严格版本化会把这条已坏路径变成主干道 |
| D9 | admin 后门审计落表 | **`operation_log`** + 加法式新增 `details jsonb` | 语义完全匹配（谁在何时动了哪个对象），且 `CustomerService` 已是真实写入方 | ❌`basic_data_change_log`——该表语义是「基础资料变更」，模板快照不是基础资料，为复用而扭曲语义会污染 `BL-0100` 设计意图；❌ 新建专用审计表——后门本身低频，多一张表不划算 |
| D10 | `frozen-drift` 前端 UI | **本期只做 API** → UI 进 `BL-0139` | 保持前端改动面最小（仅 §5.5 一处文案），vitest 全绿即渲染契约未破坏的证据 | ❌「顺手做模板详情页抽屉」——前端回归面扩大，与本期"证明没改坏"的验收策略冲突 |
| D11 | `refresh-template-snapshots` 下线方式 | **直接删路由** | 用户裁定，代码最干净 | ❌「返 410 + 指引文案」——用户不采纳 |
| D12 | 体检 A 发现快照 ≠ 活配置时以谁为准 | **以活配置为准**（D3 的自然推论）；差异清单人工过目；若活配置本身是错的 → **先修活配置再跑迁移** | 迁移脚本里开特例 = 把一次性判断固化成永久的隐藏逻辑 | ❌「迁移里对差异项做特殊处理」——不可追溯 |
| D13 | 体检 B 发现 SQL 视图闭包 miss | **跑完体检看数据再定边界**，三档处置已写死（见 §5.4.4） | 闭包算法可能有历史欠账，盲切报错会打挂原本能跑的模板 | ❌「无论多少都本期修完」——工期不可控；❌「直接整块拆出去」——多数情况可能只是零星 miss，拆出去反而留缺口 |
| D14 | `docs/PRD-v3.md` 同步 | **要**，本期同步 | 本改动改变产品语义（改配置只能通过发新版生效），CLAUDE.md 强制要求 | ❌「只改基线文档」——PRD 是功能交付的唯一标准，语义变更必须落 PRD |
| D15 | worktree 分支 | `feat/task-0806-template-freeze`，基于 `HEAD` | 本地 master 远领先 origin，fresh 会丢提交 | ❌ fresh 建 worktree |

> **并发核对**：在途另一条 0806 主线碰的是 `QuotationStep2.tsx` / `formulaEngine.ts`，与本任务（`CardSnapshotService` / `ComponentService` / `ConfigureSnapshotService` / `ExcelViewService` / `CopyQuotationDrawer.tsx`）**交集为零**，可并行。

<!-- 旧表保留在下方供追溯，内容已并入上表 -->
<details>
<summary>决策速查（精简版）</summary>

| # | 决策 | 内容 |
|---|---|---|
| D1 | **生效模型** | **严格版本化** —— 改组件只能通过发新模板版本生效；已发布模板永不变；在途报价单停留在旧版本，除非显式换模板 |
| D2 | **冻结范围** | ①+②+③ **一步做到位**；admin 后门本次保留 |
| ~~D3~~ | ~~**存量处置**~~ | ~~一次性对齐后冻死~~ → **已被 D16 推翻（2026-08-07）：不迁移存量，改由用户手工重新发布** |
| D4 | **模板范围** | 只做 `Template`（QUOTATION + COSTING） |
| D5 | **`component.status` 语义** | 停用 = **新模板不能再选它**；已发布模板与已生成报价单**不受影响**。故 status **不进快照、也不进渲染路径** |

#### 第二轮决策（2026-08-06 逐条确认）

| # | 议题 | 决策 |
|---|---|---|
| D6 | 发新版后老 PUBLISHED 是否自动归档 | **维持现状：不自动归档**。只修正 `docs/三大核心模块基线.md` §3.4 的错误表述（见 §3.3） |
| D7 | 批量派生草稿（一个组件最多牵动 6 个模板） | **本期不做**，登 BACKLOG。先用 `frozen-drift` 当发版决策入口，看真实痛感 |
| D8 | 在途 DRAFT 报价单升级到新模板版本 | **本期不做功能，但加一道护栏**：换模板入口加明确警告（见 §5.5） |
| D9 | admin 后门审计落表 | **`operation_log`** + 加法式新增 `details jsonb` 列（**不用** `basic_data_change_log`，见 §5.2.3） |
| D10 | `frozen-drift` 前端 UI | **本期只做 API**，UI 登 BACKLOG |
| D11 | `refresh-template-snapshots` 下线方式 | **直接删路由** |
| D12 | 体检 A 发现快照 ≠ 活配置时以谁为准 | **以活配置为准**（D3 的自然推论）；差异清单人工过目；若活配置本身错 → **先修活配置再跑迁移**，不在迁移脚本开特例 |
| D13 | 体检 B 发现 SQL 视图闭包 miss | **看体检结果再定**（本期唯一浮动项，见 §5.3.4） |
| D14 | `docs/PRD-v3.md` 同步 | **要**，本期同步（见 §7.3） |
| D15 | worktree 分支 | `feat/task-0806-template-freeze`，从 `HEAD` 建（本地 master 领先 origin，禁用 fresh）。与在途另一条 0806 主线（碰 `QuotationStep2.tsx` / `formulaEngine.ts`）**交集为零**，可并行 |

</details>

### 3.2 已核实的现状：D5 的 `component.status` 语义（本期不改代码）

用户定的 status 语义**当前代码已经做到**，本期只做「固化 + 文档化」，不改代码：

- **后端渲染链路零处读 `component.status`**（全量 grep：命中的 `status='ACTIVE'` 全在别的表 —— `comparison_tag` / `MaterialRecipe` / `DerivedAttribute` / SQL 视图 / 元素等；`component` 表唯一命中是 `TemplateService:804` 的 admin 端点，不在渲染路径）
- **组件选择器已过滤**：`ComponentPalette.tsx:106`、`TemplateConfiguration.tsx:129` 均 `filter(c.status === 'ACTIVE')`
- **实测硬证据**：库中 121 个组件里 **51 个已 `DISABLED`**，其中 **12 个 tc 行分布在 2 个已发布模板上，且仍在正常渲染**

> 值域是 `ACTIVE` / `DISABLED`（`ComponentService.toggleStatus:846-850`），**不是** `INACTIVE`。

**交付物**：把这条写成带实测证据的不变量，写入 `docs/三大核心模块基线.md`，防止将来有人「顺手」在渲染层加 `status='ACTIVE'` 过滤把它破坏掉。

### 3.3 附带发现：基线 §3.4「老版本自动归档」是错的（D6）

立项勘察中发现的**第二处文档与代码不一致**，与主线问题独立，但在严格版本化下会咬人：

| 出处 | 说法 |
|---|---|
| `docs/三大核心模块基线.md:218` | 「发布为新 PUBLISHED → 老 PUBLISHED **自动 ARCHIVED**（同 series 版本）」 |
| `TemplateService.publish:313-314` | 「同 series 升级时，旧 PUBLISHED **保持原状态**，由用户后续主动归档。V62 已撤销 V28 partial unique index，同 (customer_id, category_id) 允许多个 PUBLISHED 共存」 |

**实测：代码是对的，文档是错的。** 6 个 series 中 **4 个存在多 PUBLISHED 版本共存**，最多一个 series 挂着 6 个已发布版本（v1.0~v1.5）。

且 `matchCustomerQuoteTemplate:534` 返回的是**整个列表**（`ORDER BY publishedAt DESC`）供用户挑选 —— 严格版本化后每改一次配置就发一版，列表会持续膨胀，选到老版本的风险随之上升。

**D6 决策：维持代码现状（不自动归档），本期只修正基线 §3.4 的错误表述**，并在其中写明「多 PUBLISHED 共存是设计允许的，选择列表按 `publishedAt DESC` 排序，最新版在最前」。版本膨胀的体感问题由 D7（批量发版工具）在后续期评估。

---

## 4. 功能需求

> 每条 FR 在 §5 有对应的设计细节，在 §6 有对应的 `AC-N`。

| FR | 需求 | 设计 | 验收 |
|---|---|---|---|
| **FR-1** | 新建 `template_component_snapshot` 表；`TemplateService.publish()` 在同一事务内为每个 `template_component` 落一行完整生效配置 | §5.1.1 / §5.2.1 | AC-1 |
| **FR-2** | `template.components_snapshot` jsonb 改为**由 FR-1 的新表派生**，字段形状与现状完全一致，前端消费方无感 | §5.2.1 | AC-2 |
| **FR-3** | `refreshSnapshotsByComponent` 整体退役：删 `ComponentService.update:733` 与 `setDriverView:792` 两处自动传导；删 `ComponentResource:145` 路由；`ConfigCenterResource:128` 保留但改造为 FR-7 | §5.2.2 | AC-3 / AC-4 |
| **FR-4** | 补齐 6 个从未进快照的渲染配置字段：`rowKeyFields` / `sortField` / `elementCodeField` / `elementPriceField` / `elementCurrencyField` / `columnCount` | §5.1.1 | AC-1 |
| **FR-5** | 渲染期 10 处活表读全部改走新增的 `PublishedTemplateReader`；接口按模板批量取，SQL 条数与页签数无关 | §5.3.1 / §5.3.2 | AC-5 / AC-11 |
| **FR-6** | PUBLISHED / ARCHIVED 模板快照 miss → 抛错含 `templateId` + `sortOrder`，**禁止回落活表**；`ComponentSqlViewService.lookupForResolver:400` 的 SQL 视图 fallback 同口径（受 D13 三档处置约束） | §5.3.3 / §5.3.4 | AC-6 / AC-7 |
| **FR-7** | 保留的 3 个 admin 后门（`ConfigCenterResource:128`、`TemplateResource:170` delete-tcs、`:205` promote-override）统一加：前置影响面预览 + 二次确认、写 `operation_log` 审计、`LOG.warn` 告警。`TemplateResource:143` migrate-to-unified-view 直接删除 | §5.2.3 | AC-8 |
| **FR-8** | **（2026-08-07 按 D16~D19 重写）** 不做存量迁移：V382 只建表/加列，不回填。① PUBLISHED 但零快照行 → 返回可识别的「未冻结」态，**不报错、不读活表**，前端提示「请重新发布」（D17）；② 有行但缺某 sortOrder → **抛 500**（快照损坏，D19）；③ `archive()` 若无快照则按当时活配置**自动补冻**再归档（D18） | §5.4.1 | AC-9 / AC-12 |
| **FR-9** | `GET /api/cpq/templates/{id}/frozen-drift`（SYSTEM_ADMIN）返回该已发布模板快照 vs 当前活组件配置的逐字段差异。**仅 API，无 UI** | §5.4.3 | AC-10 |
| **FR-10** | 修正 `CopyQuotationDrawer.tsx:70/:89` 的误导性文案（现承诺「迁移输入值 + 公式重算」，实际均未发生），`info` → `warning` | §5.5 | AC-13 |
| **FR-11** | 文档固化：`PRD-v3.md`（发布后不可变 + 版本演进）、三大基线 §3.2/§3.4/§3.5/§10、方案制定前必读 §改动 5、反模式新增 AP | §7.3 | AC-14 |

---

## 5. 数据与规则

### 5.1 数据模型

#### 5.1.1 新表 `template_component_snapshot`

一行 = 一个已发布模板的一个页签的**完整生效配置**。发布时写一次，此后只读。

```
主键 / 溯源
  id, template_id (FK → template ON DELETE CASCADE), sort_order
  template_component_id, component_id     -- 仅溯源，不建 FK
  frozen_at

来自 template_component（模板级，现已冻结）
  tab_name, preset_rows(jsonb), formula_assignments(jsonb)

来自 component（内容层，本次要冻的主体）
  component_name, component_code, component_type, column_count
  fields(jsonb), formulas(jsonb), excel_columns(jsonb)
  data_driver_path(text), tree_config(jsonb), bom_recursive_expand(bool)
  tab_type, part_no_field, part_name_field
  row_key_fields(jsonb)   ← ② 新补
  sort_field              ← ② 新补
  element_code_field      ← ② 新补
  element_price_field     ← ② 新补
  element_currency_field  ← ② 新补
```

**索引**（按 §1.5 那 6 处 SQL 的谓词开）：

- `(template_id)`
- `(template_id, tab_type)`
- `(template_id) WHERE data_driver_path IS NOT NULL`
- `(component_id)` —— 供 §4.4.3 版本差异视图使用

**唯一约束 `(template_id, sort_order)`** —— 正好把 AP-40 那个「同 componentId 多实例必须按 sortOrder 精确匹配」的历史坑从 schema 层面变成不可能。

**不建 FK 到 `component` / `template_component` 是刻意的**：快照的全部意义就是与活表脱钩，组件被停用或被删都不该动摇已发布模板。

#### 5.1.2 三条冻结边界

| 边界 | 规则 | 依据 |
|---|---|---|
| **DRAFT 模板不写快照** | 草稿期一律走活表 | 现状即如此：`create:130` 不设、`createNewDraft:468` 显式 NULL、`publish:256` 才写 |
| **ARCHIVED 模板同样冻死** | `archive()` 只改 status 不动快照，新表同理 | 历史报价单仍在读它们 |
| ~~**`sql_views_snapshot` / `template_sql_views_snapshot` 不动**~~ | ~~本来就在 publish 冻结且 refresh 不碰~~ | ❌ **2026-08-07 更正：这个判断是错的**，见 §5.1.4 |

#### 5.1.4 更正：SQL 视图的「双层冻结」是**冻了但从来没人读**（B10 实施中发现）

**我原判断错在哪**：只读了 `ComponentSqlViewService.lookupForResolver` 的三层结构，**没检查它的输入是否曾被填充过**，就断言「已经符合要求」。

**实测真相** —— 渲染主链路唯一入口 `ComponentDriverService:369` / `:674`：

```java
SqlViewRuntimeContext.setNested(componentId, null, _pq, null);
//                                   templateId↑          ↑quotationStatus
```

| 层 | 触发条件 | 实际 |
|---|---|---|
| ① 报价单冻结快照 `quotation_component_sql_snapshot` | `isQuotationFrozen()` | `quotationStatus` 恒 `null` → 恒 false → **休眠** |
| ② 模板冻结快照 `template.sql_views_snapshot` | `ctx.templateId != null` | 恒 `null` → **休眠** |
| ③ 兜底实时读 `component_sql_view` | 兜底 | **永远走这层** |

且是**前人刻意留的休眠**（`:360-366` 注释）：怕传真实 status 让「已提交单的视图 SQL 来源静默切换」，属超范围变更。

**后果**：`publish()` 确实冻结了 `sql_views_snapshot`，但**没人读它** —— 已发布模板渲染用的 SQL 视图定义取自活表当前值。这是同一种病的**第四条路径**，也是又一处「文档描述了不存在的机制」（`docs/三大核心模块基线.md` §3.2.1 至今仍写着那套三层优先级）。

**决策（用户 2026-08-07 拍板）：本期接上，把冻结做真。** 追加 **B17**：

- **B17-a（必做）**：仿 `QuotePendingScope` 引入 ThreadLocal scope，由 `CardSnapshotService` / `ConfigureSnapshotService` 在驱动展开前设 templateId，`ComponentDriverService` 读取后填进 `SqlViewRuntimeContext` → 点亮第②层。**不加 `expand()` 参数**（重载链调用点太多）。批量路径 `expandMulti` / `expandForPartSet` / `expandFlatDriverBaseRows` 一并覆盖
- **B17-b（先分析后实施）**：第①层涉及与 task-0725 pending 改写机制的交互（`_pq` 只在非冻结时非空、`SqlViewExecutor:555` 门槛），**须先出分析结论交主线确认**，不得擅自实施

**验收补充（B10 的教训）**：B10 的守卫逻辑正确、单测三分支全过，但**任何 HTTP 路径都触达不到**。故 B17 验收**必须端到端证明第②层被真实命中**，不接受「代码看起来对」；并须做接通前后**值中性对比** —— 值若变化 = 活表定义已与快照漂移，须上报差异明细，**不由实现者判断哪个对**。

#### 5.1.3 不冻的字段

- `status` —— 见 §3（不进快照也不进渲染路径）
- `directory_id` —— 纯组织维度，不影响渲染

### 5.2 写入路径

#### 5.2.1 唯一写入点：`TemplateService.publish()`

发布事务里一次性落三样，全部同事务：

1. `template_component_snapshot` —— N 行（每个 tc 一行）【新增】
2. `template.components_snapshot` jsonb —— **改为从 ① 派生**，不再各自拼装
3. `sql_views_snapshot` / `template_sql_views_snapshot` —— 维持现状

**派生而非并行拼装是关键**：两份数据同源同事务生成、之后都不再变，结构上不可能不一致。

**前端契约不变**：`components_snapshot` 的字段形状保持一致，`BulkImportPartsDrawer.tsx:123`、`QuotationStep2.tsx:3844`、`ExcelViewConfigTab`、`enrichComponentData` 路径全部无感。

#### 5.2.2 `refreshSnapshotsByComponent` 整体退役

DRAFT 模板恒无快照 → 该方法的作用面**从来只有 PUBLISHED + ARCHIVED** → 冻结之后它没有任何合法用途。**不是限制它只刷 DRAFT，是整个下线。**

| 调用点 | 处置 |
|---|---|
| `ComponentService.update:733` | **删除**（灾难源头：保存组件即改写已发布模板） |
| `ComponentService.setDriverView:792` | **删除** |
| `ComponentResource:145` `POST /components/{id}/refresh-template-snapshots` | **直接删路由**（D11） |
| `ConfigCenterResource:128` 批量刷新 | **保留**（admin 后门），改造见 §5.2.3 |

#### 5.2.3 保留的 admin 后门：从「静默改写」变「留痕改写」

适用端点：`ConfigCenterResource:128`、`TemplateResource:170`（delete-tcs）、`TemplateResource:205`（promote-override-to-component）。三者统一加：

1. **前置影响面预览** —— 先返回「将改动 N 个模板 / M 张在途报价单」，二次确认才执行（对齐 `docs/列表操作规范.md` 危险动作二次确认）
2. **写审计**（D9）—— 目标表 **`operation_log`**：
   - 现有列 `operator_id / operation_type / target_type / target_id / summary / created_at` 语义完全匹配「谁在什么时候动了哪个对象」，且 `CustomerService` 已是真实写入方
   - **加法式新增 `details jsonb` 列**存结构化 diff（改前改后），可空，不影响现有写入方
   - `operation_type` 取值示例：`TEMPLATE_SNAPSHOT_FORCE_REFRESH` / `TEMPLATE_TC_DELETE` / `TEMPLATE_OVERRIDE_PROMOTE`；`target_type = 'TEMPLATE'`
   - ❌ **不用** `basic_data_change_log`：那张表语义是「基础资料变更」，模板快照不是基础资料，为复用而扭曲语义会污染 `BL-0100` 的设计意图
3. **日志告警** —— `LOG.warn` 明确标注「已破坏模板不可变性」

`TemplateResource:143` `migrate-to-unified-view` 是一次性历史迁移（基线 §3.5 标注「已跑过」）→ **直接删除**。

#### 5.2.4 CI 级约束

Flyway 迁移不得再直接 `UPDATE template SET components_snapshot = ...`。写入 `docs/方案制定前必读.md` §改动 5（该节现有内容是老契约「必须同时改 3 个层」，需一并改写）。

### 5.3 读取路径

#### 5.3.1 收口到 `PublishedTemplateReader`

10 处各写各的 SQL 是本 bug 潜伏至今的原因之一。新增读取服务，渲染期一切「这个模板有哪些页签、每个页签什么配置」的问题都问它。

接口按现有调用形态给**批量**方法（`driverCompsOf(templateId)` / `treeTabsOf(templateId)` / `hasRecursiveExpand(templateId)` / `allTabsOf(templateId)` …），一次一个模板返全集 —— 把 CLAUDE.md 「单个业务操作 SQL 条数与 N 无关」的铁律焊在接口形状里。

#### 5.3.2 6 处 native SQL：只换表，谓词不动

```sql
-- 改前
JOIN component c ON c.id = tc.component_id  WHERE tc.template_id = :tid AND c.tab_type = 'BOM'
-- 改后
FROM template_component_snapshot s          WHERE s.template_id = :tid AND s.tab_type = 'BOM'
```

**有益副作用**：改完后 `template_component` 活表也退出渲染路径 —— `delete-tcs` 后门连渲染都影响不到了。

`ExcelViewService` 那 4 处需注意 `:435` 的性能守卫注释（「避免对每行做无谓的 `Component.findById`」）：换成按 templateId 一次性预载 Map，比现状更快。

#### 5.3.3 快照缺失 = 显式报错，绝不回落活表

PUBLISHED / ARCHIVED 模板读不到快照行 → 抛错，带 `templateId` + 缺失的 `sortOrder`。

**绝不能写成「没有就查活表兜底」** —— 那是活穿透原地复辟，而且是静默的。

渲染只发生在报价单上，而报价单绑定已被 task-0729 强制要求 PUBLISHED，故渲染期不存在 DRAFT 分支，无需兜底路径。

#### 5.3.4 堵上 SQL 视图 fallback 缺口

`ComponentSqlViewService.lookupForResolver:400` 第 3 步「兜底实时读 `component_sql_view`」是 `sql_views_snapshot` 线上唯一的活穿透口，性质与主线问题相同。

改为同 §4.3.3 口径（miss 即报错）。**但不得盲改** —— `snapshotForComponents` 冻的是「含 GLOBAL scope 闭包」的视图集，若闭包算法本有漏算、现靠 fallback 悄悄兜着，一改成报错会把原本能跑的模板打挂。

**前置门槛（D13，本期唯一浮动项）**：§5.4.2 体检 B 跑完看数据再定边界 ——

| 体检 B 结果 | 处置 |
|---|---|
| 零 miss | 直接切报错，本节完成 |
| miss 少（闭包算法小漏，可定点补） | 补闭包算法 + 回填存量 snapshot → 再切报错，仍在本期 |
| miss 多（翻出闭包算法的历史欠账） | **本节整体拆出单独立项**，主线先上；此时 SQL 视图那条线暂时保留 fallback，并在 spec / BACKLOG 显式记为已知缺口（**不得默默留着当没事**） |

体检 B 跑完须把数据给用户过目再定，不由实现者自行决断。

### 5.4 存量对齐与可观测

#### 5.4.1 一次性对齐 = 一条 Flyway 迁移

```sql
INSERT INTO template_component_snapshot (...)
SELECT tc.id, tc.template_id, tc.component_id, tc.sort_order, tc.tab_name,
       tc.preset_rows, tc.formula_assignments,
       c.name, c.code, c.component_type, c.column_count,
       COALESCE(tc.fields_override::jsonb, c.fields), c.formulas, c.excel_columns,
       COALESCE(tc.data_driver_path_override, c.data_driver_path),
       c.tree_config, c.bom_recursive_expand, c.tab_type, c.part_no_field, c.part_name_field,
       c.row_key_fields, c.sort_field,
       c.element_code_field, c.element_price_field, c.element_currency_field, now()
FROM template_component tc
JOIN component c ON c.id = tc.component_id
JOIN template  t ON t.id = tc.template_id
WHERE t.status IN ('PUBLISHED', 'ARCHIVED');
```

- `COALESCE` 保留 override 优先语义 —— 眼下两列全 NULL 用不上，但语义须对，否则将来谁用后门写了一个就静默丢
- `ARCHIVED` 一并覆盖（当前 0 个，规则先立住）
- 同一迁移里按新表重生成一次 `components_snapshot` jsonb，两份从第一天起同源
- **迁移幂等**：按 `template_id` 先删后插

#### 5.4.2 迁移前两份体检，迁移后一份铁证

| 项 | 内容 | 门槛 |
|---|---|---|
| **前置体检 A** | 「现 `components_snapshot` vs 当前活配置」逐字段 diff，**存档留证** | 理论应近乎为零；非零处 = 历史某次传导失败留下的疤，冻死前须人工看一眼 |
| **前置体检 B** | 17 个模板 × 每处视图引用，验证在 `sql_views_snapshot` 闭包内 | **零 miss** 才允许执行 §5.3.4 |
| **迁移后铁证** | **值中性验证**：迁移前后对同一批报价单纯读渲染，卡片值**逐位对比** | **必须逐位一致**。本次改的是「数据从哪读」不是「读出什么值」，任一位不同即 bug。沿用 `GoldenCardValuesEquiv` 背靠背纯读手法 |

#### 5.4.3 版本差异视图（不是告警器）

`GET /api/cpq/templates/{id}/frozen-drift`（`SYSTEM_ADMIN`）：对比该已发布模板快照 vs 当前活组件配置，输出逐字段差异。

**D10：本期只做 API，不做前端 UI**（UI 登 BACKLOG）。管理员先用 curl / DB 查。

**语义定位很重要**：迁移当天应全零；随组件继续迭代，差异会自然增长 —— **这不是故障，正是严格发版模型下的正常状态**。它回答的是「这个已发布模板比当前组件配置落后多少，值不值得发新版」，是发版决策依据。同时天然兼任 admin 后门的安全网：谁偷改了快照，一眼可见。

#### 5.4.4 回滚

新表可直接 `DROP`；`components_snapshot` jsonb 形状向后兼容，代码回滚即恢复旧行为。

### 5.5 换模板护栏（D8）—— 本期唯一的前端改动

**背景**：严格版本化（D1）落地后，「想用上新配置」的唯一路径变成「发新版 → 已有报价单换模板」。而这条路径**当前是坏的**（`BL-0129`）：`QuotationService:1670` 换模板时**显式清空 `snapshot_rows`**（既有设计语义「留空待重建」），结果是产品行无数据、总价恒 0，且实测三条恢复路径（重算 / 刷新卡片快照 / force 重建）**均无法纠正**。

**本期不修 `BL-0129`**（根因在另一条链路），但必须让用户知情 —— 因为现有文案在骗人：

| 位置 | 现状 | 实际行为 |
|---|---|---|
| `CopyQuotationDrawer.tsx:70` | 普通 `<p>`，**零视觉警示**，恒显示。文案：「换模板后：页签相同的**迁移用户输入值**，不同的留空，**公式/数据由新模板重算**」 | `snapshot_rows` 被清空，**没有数据可迁移、可重算** |
| `CopyQuotationDrawer.tsx:84-91` | `Alert type="warning"`（**样式已对**），仅 `changed === true` 时显示。文案：「已更换模板：仅页签字段相同的**输入值会被迁移**，其余留空」 | 同上 |

> 📌 **更正**：`:89` 的 Alert 本来就是 `warning`，不是 `info`。问题**不在告警等级而在文案内容** —— 两处都在承诺一件根本没发生的事。真正缺视觉警示的是 `:70` 那个裸 `<p>`。

**交付**：

1. `:70` 的裸 `<p>` → 改为**恒显示**的 `Alert type="warning" showIcon`，文案明确三层信息：**会清空已填数据** / **当前无法恢复（`BL-0129`）** / **请先导出留档**
2. `:84-91` 的换模板 Alert 保留 `warning`，**文案改成实话**（删掉「输入值会被迁移」的承诺）
3. 两处均**不得**再出现「迁移用户输入值」「公式/数据由新模板重算」等与实际不符的表述

**同时**：`BL-0129` 优先级从 P2 **提到 P1**，并在条目里关联本任务，写明「严格版本化把这条已坏路径变成了高频路径」。

> ⚠️ 这是本期**唯一**的前端改动，只改文案与告警等级，**不触碰任何渲染契约**。`CopyQuotationDrawer.tsx` 与在途另一条 0806 主线（`QuotationStep2.tsx` / `formulaEngine.ts`）无交集。

---

## 6. 验收标准

> 每条 AC 必须**可观测、可复现、有明确期望值**；每条至少对应 `test.md` 一条用例。

### 6.1 AC 清单

| AC | 对应 | 断言（期望值明确） |
|---|---|---|
| **AC-1** | FR-1 / FR-4 | `publish()` 后 `template_component_snapshot` 行数 **== 该模板 `template_component` 行数**；**18 个冻结字段**（§1.4 字段账）逐个等于当时活配置（`fields_override` 非空时取 override）；`(template_id, sort_order)` 唯一约束生效 |
| **AC-2** | FR-2 | `publish()` 生成的 `components_snapshot` jsonb 与改造前**同输入同输出**（键集合、键顺序、值逐字段一致）；前端 vitest 全绿 |
| **AC-3** ⭐ | FR-3 | **核心验收**。取被 6 个已发布模板共享的组件（`COMP-0045` / `COMP-0047` / `COMP-0046` / `COMP-0181` / `COMP-0182` 任一，实测均为 6 模板共享且 `ACTIVE`），改其 `fields` / `formulas` / `dataDriverPath` 并保存 → ① 6 个模板的 `template_component_snapshot` 行 **`md5(row::text)` 前后一致**；② 6 个模板的 `components_snapshot` jsonb **前后一致**；③ 引用这些模板的报价单渲染值**逐位一致** |
| **AC-4** | FR-3 | `ComponentService.update` / `setDriverView` 内**不存在**对 `refreshSnapshotsByComponent` 的调用（grep 零命中）；`POST /api/cpq/components/{id}/refresh-template-snapshots` 返 **404**（路由已删）；`POST /api/cpq/templates/admin/migrate-to-unified-view` 返 **404** |
| **AC-5** | FR-5 | 渲染期 10 处读取点在 **PUBLISHED / ARCHIVED 路径上全部经 `PublishedTemplateReader`**，对 `component` / `template_component` 活表零直查。<br>📌 **2026-08-07 修订**：原判据写「三个文件内 grep 零命中」，**过严且与 §5.1.2 自相矛盾** —— 冻结边界明确规定「DRAFT 模板不写快照，草稿期一律走活表」，所以 DRAFT 分支**必须**保留活表查询。修订判据：<br>① `CardSnapshotService` / `ExcelViewService` 活表直查 = **0**（这两处无 DRAFT 分支）<br>② `ConfigureSnapshotService` 允许**恰好 1 处**活表查询，且必须位于 `status` 判定为非 PUBLISHED/ARCHIVED 的分支内（人工核对分支条件，不只数行数）<br>③ 任何「PUBLISHED 但快照 miss → 查活表」的写法一律不合格（那是 AC-6 的禁止项，与本条的 DRAFT 分支性质完全不同） |
| **AC-6** | FR-6 | 人为删除某 PUBLISHED 模板一行快照 → 渲染该模板**抛错**，错误信息含 `templateId` 与缺失的 `sortOrder`；**不回落活表**（活表数据完好时仍报错，不静默出数） |
| **AC-7** | FR-6 / D13 | 已发布模板上下文下 `sql_views_snapshot` miss → **抛错**，不读 `component_sql_view`。**若 D13 判定为「整块拆出」，本条降级为「体检 B 报告已产出且缺口已登记 BACKLOG」** |
| **AC-8** | FR-7 | 3 个 admin 后门端点：① 不带确认参数调用 → 返回影响面预览（`{templates:[...], quotations:N}`）**且不落任何写入**；② 带确认参数调用 → 写入成功 **且** `operation_log` 新增一行（`target_type='TEMPLATE'`，`details` 含改前改后 diff）；③ 后端日志出现 `WARN` 且含「不可变性」字样 |
| **AC-9** | FR-8 | **（按 D16~D19 重写）** ① V382 执行后 `template_component_snapshot` **行数 = 0**（不回填）；② 对一个未重新发布的 PUBLISHED 模板：`frozen-drift` 明确返回**未冻结**（不得用 `hasDrift:false` 表示），渲染**不报 500、不读活表**，透出「请重新发布」信号；③ 重新发布该模板后快照生成、渲染恢复、`frozen-drift` 回到 `frozen:true / hasDrift:false`；④ 有行但删掉某个 sortOrder → 走 `findTab` 的路径**仍抛 500**；⑤ 对无快照的 PUBLISHED 模板执行 `archive()` → 快照被**自动补冻** |
| **AC-10** | FR-9 | `GET /api/cpq/templates/{id}/frozen-drift` 以 SYSTEM_ADMIN 调用返 **200**；迁移刚完成时对全部 17 个模板返回**差异为空**；人为改一个组件字段后，引用它的模板返回**恰好该字段**的差异（快照值 vs 活配置值） |
| **AC-11** | FR-5 | **N+1**：渲染 SQL 条数与页签数**无关**。**两点法**：取页签数差 ≥3 倍的两个真实模板（实测可用 6 页签 vs 18 页签，18 是库内现存最大），SQL 条数**必须相同**（`[perf]` 日志佐证）。<br>📌 **2026-08-07 修订**：原文写「5 增至 20」，但库内无天然夹具（最大 18），专门构造 5/20 对结论无增量价值 —— N+1 会随页签数线性增长，3 倍差距足以判定。**以自然数据两点法为主证据**，构造法降为可选补充 |
| **AC-12** | FR-8 | **（按 D16 调整）** 原「迁移前后值中性」因不再有存量迁移而失去对象。改为：**同一模板重新发布前后**，对同一批报价单纯读渲染，卡片值**逐位一致**（沿用 `GoldenCardValuesEquiv` 背靠背手法）——证明「冻结的内容 == 冻结前活配置渲染出的内容」。任一位不同即不通过 |
| **AC-13** | FR-10 | `CopyQuotationDrawer` 打开时**恒显示**一个 `Alert type="warning" showIcon`，文案含「会清空已填数据」「当前无法恢复」「请先导出留档」三层信息；换模板时的第二个 Alert 文案同步改为实话；全组件**不再出现**「迁移用户输入值」「公式/数据由新模板重算」等与实际不符的表述（grep 零命中） |
| **AC-14** | FR-11 | `PRD-v3.md` / `三大核心模块基线.md`（§3.2 / §3.4 / §3.5 / §10）/ `方案制定前必读.md`（§改动 5）/ `反模式.md`（新 AP）均已更新，内容与本文档一致 |
| **AC-15** | D5 | 用现存夹具（2 个已发布模板 / 12 个 `DISABLED` tc）：组件停用后这 2 个模板**照常渲染**（页签数与渲染值不变）；同时新建草稿模板时组件候选列表**不出现**该组件 |
| **AC-16** | D1 | `createNewDraft` → 改组件 → `publish` 新版本：**新模板**快照含新配置，**老模板**快照 `md5` 与操作前一致 |
| **AC-17** | §5.1.2 | 模板 `archive()` 后其快照行**不变**；DRAFT 模板**无**快照行（`count = 0`） |
| **AC-18** | 协议级 | `quotation-flow.spec.ts` + `composite-product-flow.spec.ts` **双 spec 全 passed**，`'加载中'` final count **= 0**，附 8 Tab 截图 |

### 6.2 自检声明（PR 必含，缺一不可）

- `cd cpq-frontend && npx tsc --noEmit -p tsconfig.json` → **0 错误**；`CopyQuotationDrawer.tsx` → Vite **200**
- 后端目标端点 `curl` → **200/401**（非 500）
- Flyway：`SELECT version, success FROM flyway_schema_history WHERE version='NNN'` → **`success=t`**
- **N+1 自检声明**：逐个检查本次新增/改动的 `for` / `forEach` / `stream()` 循环体，确认无 repository 调用 / `SqlViewExecutor.execute` / 懒加载 getter（格式见 CLAUDE.md）
- **值中性验证**逐位一致的原始输出

---

## 7. 影响面与风险

### 7.0 风险与缓解

| 风险 | 缓解 |
|---|---|
| §5.3.4 切报错可能打挂原本靠 fallback 兜着的模板 | 前置体检 B 零 miss 才切；有 miss 先补闭包 |
| 高危文件改动（3 个协议级文件） | 双 spec E2E + 值中性验证 + 逐位对比 |
| **Flyway 版本号是移动靶** | 多会话并发；本次迁移版本号合并前必须复查；已应用迁移禁止改名改号 |
| **worktree 里的迁移共享 dev server 看不见** | 需 copy 到主仓跑验证，合并前删副本 |
| ~~D3 承认「当前即基线」可能把错配置冻进去~~ | **风险随 D16 消失** —— 不再有一次性对齐，每份快照都由人工重新发布产生 |
| D17 过渡期「未冻结」状态被误当成系统故障 | 显式文案「该模板尚未冻结，请重新发布」；`frozen-drift` 区分 `frozen:false` 与 `hasDrift:false` |

---

### 7.1 影响面清单

#### 7.1.1 后端

| 文件 | 改动 |
|---|---|
| `template/entity/TemplateComponentSnapshot.java` | **新增**实体 |
| `template/service/PublishedTemplateReader.java` | **新增**读取服务（§4.3.1） |
| `template/service/TemplateService.java` | `publish()` 落新表 + jsonb 改派生；`refreshSnapshotsByComponent` 下线；`migrate-to-unified-view` 删除 |
| `component/service/ComponentService.java` | 删 `:733` / `:792` 两处自动传导 |
| `component/resource/ComponentResource.java` | 下线 `:145` refresh 端点 |
| `configcenter/ConfigCenterResource.java` | `:128` 加预览 + 审计 + 告警 |
| `template/resource/TemplateResource.java` | `:143` 删；`:170` / `:205` 加预览 + 审计；新增 `frozen-drift` 端点 |
| `quotation/service/CardSnapshotService.java` | 5 处读取点改走 Reader |
| `configure/service/ConfigureSnapshotService.java` | 1 处读取点改走 Reader |
| `quotation/service/ExcelViewService.java` | 4 处读取点改走 Reader |
| `component/service/ComponentSqlViewService.java` | `:400` fallback 改报错（体检 B 通过后） |
| Flyway 迁移（版本号合并前定，见 §6） | 建表 + 存量对齐 + jsonb 重生成 |

#### 7.1.2 前端

| 文件 | 改动 |
|---|---|
| `pages/quotation/CopyQuotationDrawer.tsx` | `:70` / `:89` 两处误导性文案改为实话 + `info` → `warning`（§5.5） |

其余**零改动**（`componentsSnapshot` 契约不变）。

#### 7.1.3 文档

| 文档 | 改动 |
|---|---|
| `docs/PRD-v3.md` | **D14**：模板管理章节新增「发布后不可变 + 版本演进」一节（改配置只能通过发新版生效）；第 9 章演进史记一笔 |
| `docs/三大核心模块基线.md` | §3.2 改写（快照现在真的不可变）；**§3.4 纠错**（老 PUBLISHED **不**自动归档，多版本共存是设计允许的，见 §3.3）；§3.5 admin 端点清单更新；§10 红线新增 3 条：「渲染层禁止读活 `component` / `template_component` 表」「渲染层禁止按 `component.status` 过滤」「快照 miss 禁止回落活表」 |
| `docs/方案制定前必读.md` | §改动 5 改写（老契约「必须同时改 3 个层」作废，改为「已发布模板快照只能由 `publish()` 写」） |
| `docs/反模式.md` | 新增 AP：「发布后活穿透」族（3 类形态 + 「绕开 AP-44 顺带绕开冻结」这一根因模式） |
| `docs/RECORD.md` | 交付后回写 |
| `dev-docs/INDEX.md` | 新增本任务条目 + §0 症状反查 2 行 + §0.5 文件反查表（**已做**） |
| `BACKLOG.md` | **已做**：本任务 `BL-0133`(P0) + 衍生 `BL-0134`(P1 ConfigTemplate 守卫) / `BL-0135`(P2 ConfiguratorTemplate 守卫) / `BL-0136`(P2 报价单层纵深防御) / `BL-0138`(P2 批量发版 D7) / `BL-0139`(P2 drift UI D10)；`BL-0129` 已**提 P1** 并写明提级理由 |

---

## 8. 已知坑位（本任务必须避开的历史教训）

> 引用 `docs/反模式.md` 条目号；开工前**逐条读原文**，不要只看这里的一句话摘要。

| 坑 | 与本任务的关系 | 必须做什么 |
|---|---|---|
| **AP-40** 同 cid 多 tc 实例 `firstResult()` 反向污染 | 直接命中。`refreshSnapshotsByComponent:377-383` 就是为修它才加的 sortOrder 精确匹配 | 新表 `(template_id, sort_order)` 唯一约束把它变成 schema 层不可能；迁移 SQL 必须按 tc 逐行插入，**不得**按 componentId 聚合 |
| **AP-44** 字段类型改动跨 17 个协议检查点 | **间接命中且是 ② 的根因**：`rowKeyFields` / `sortField` / `element*Field` 当初正是为绕开 AP-44 才挂在表列上，顺带绕开了冻结 | 本任务**不改 `field_type` 枚举**，不触发 AP-44 主流程；但新表加的 6 个字段必须同步进 `PublishedTemplateReader` 的读取契约，否则重演同款遗漏 |
| **AP-39** PUBLISHED 模板 snapshot 残留老散字段 | 同族问题（Flyway 迁移只改一层、漏改引用方） | 本次迁移必须**同时**写新表 + 重生成 jsonb，两者同事务；`docs/方案制定前必读.md` §改动 5 的老契约「必须同时改 3 个层」要一并改写 |
| **AP-51** `snapshotRows` Math.max 持久化累加 | 邻近区域（`CardSnapshotService` 行迭代） | 本任务改的是"配置从哪读"，**不得顺手动行数逻辑**；driver 行数权威纪律保持不变 |
| **AP-53** 老表禁用导致渲染断链 | 同类形态（读到已停止维护的数据源） | 新表是"冻结副本"不是"废弃表"，但 `frozen-drift` 的存在正是为了让"快照 vs 活配置"的偏离可见，避免重演「读到冻结数据却不自知」 |
| `grep` 是 ugrep，中文多的大源文件会被**静默判为二进制返空** | 本任务大量依赖 grep 做「零命中」验收（AC-4 / AC-5） | 所有验收用 grep **必须** `/usr/bin/grep -a`；据空结果下「无调用」结论前先用 `-a` 复核 |
| 共享 Flyway 历史 + 多会话并发 | 本次有 Flyway 迁移 | 版本号是移动靶，合并前复查；**已应用的迁移禁止改名改号**；worktree 里的迁移共享 dev server 看不见，需 copy 到主仓验证、合并前删副本 |
| 子代理会虚报完成 | 本任务派前后端 + 测试子代理 | 主线**必须亲验**（`任务平台规则.md` §6 清单）：亲跑 E2E、亲查 DB、亲跑 worktree 内 `./mvnw test`（`mvnw` 在 `cpq-backend/` 不在仓库根） |

---

## 9. 附录：实测数据（2026-08-06，`10.177.152.12:5432/cpq_db_0724`）

```
模板状态分布
  PUBLISHED / QUOTATION : 15   (含 snapshot 15, 含 sql_views_snapshot 15)
  PUBLISHED / COSTING   :  2   (含 snapshot  2, 含 sql_views_snapshot  2)
  DRAFT / ARCHIVED      :  0

template_component
  行数 149 ｜ 模板 17 ｜ 组件 53
  fields_override 非空 0 ｜ data_driver_path_override 非空 0
  单组件被引用模板数：max 6 ｜ avg 2.8

报价单
  引用 PUBLISHED 模板：54 张

component.status
  ACTIVE 70 ｜ DISABLED 51
  已发布模板引用 DISABLED 组件：2 个模板 / 12 个 tc 行（且仍正常渲染 → D5 语义现状即成立）
```
