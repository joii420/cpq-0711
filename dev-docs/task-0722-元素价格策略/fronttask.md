# 前端任务文档 · 元素单价维护与价格策略（task-0722）

> **权威依据**：`需求说明.md` §11（尤其 §11.24 交付物对照矩阵）、`api.md`（前后端契约）、`元素价格策略-原型图.html`（v9 定稿，**逐屏 1:1 还原**）。
> **配套**：`backtask.md`。
> **纪律**：契约以 `api.md` 为准，需要改字段先改 `api.md` 并知会后端，不得单边改。

---

## 0. 开工前必读

| # | 事项 | 依据 |
|---|------|------|
| 1 | **一律用 Drawer，禁止 Modal** 做表单/详情/向导。唯一例外：危险动作的二次确认弹窗、`message`/`notification` | CLAUDE.md UI 规范 |
| 2 | **列表走 `SelectableTable` + 顶部工具栏动作**，行内不放动作按钮；动作用 `enabledWhen` 返回 `true`/原因字符串，**禁止 `if(...) return null` 隐藏按钮**；批量失败用 `runBatch` 聚合 | `docs/列表操作规范.md` |
| 3 | 抽屉宽度按内容取 480 / 720 / 960 / 1200，本任务各屏宽度已在原型标注，照抄 | UI 规范 |
| 4 | 响应是**裸 DTO，无 `{code,data}` 信封**，不要写解包逻辑 | `api.md` §0 |
| 5 | worktree 只隔离 git 工作区，**5174 dev server 是主工作区共享的**。worktree 内自检需软链 `node_modules` + 另起临时端口 vite，或直接用 `tsc` + 内容比对 | `cpq-worktree-frontend-selfcheck` |
| 6 | 只 `git add` 本次明确改动的文件，**严禁 `git add -A`** | CLAUDE.md |
| 7 | **本期报价单 / 核价单渲染层零改动**：不得修改 `QuotationStep2.tsx` / `ReadonlyProductCard.tsx` / `useDriverExpansions.ts` 等任何报价渲染文件。验收要点 #13 会检查它们的 `git diff` 为空 | §11.14D 连带结论 |

### 遗留文件处置
`src/pages/element-price/ElementPriceCenterPage.tsx` 与 `ManualPriceEntryDrawer.tsx` 是 v1 遗留（未挂菜单，路由仍在）。
**本期不复用、不删除、不修改**——新功能全部新建文件，避免范围蔓延。

---

## 1. 任务总览

| 任务 | 内容 | 涉及文件 | 规模 |
|------|------|---------|------|
| F1 | 元素管理页：3 个入口按钮 + 时间列合并 | 改 `pages/config/ElementManagement.tsx` | S |
| F2 | 元素抽屉：各源最新价区块 | 改 `pages/config/ElementEditDrawer.tsx` | S |
| F3 | 价格源管理抽屉 | 新建 | M |
| F4 | 价格导入抽屉 | 新建 | M |
| F5 | 元素价格表抽屉（明细 + 矩阵） | 新建 | M |
| F6 | 定价策略页新 Tab + `_GLOBAL_` 入口 | 改 `pages/pricing/PricingStrategy.tsx` + 新建 | L |
| F7 | 策略试算抽屉 | 新建 | S |
| F8 | 策略变更历史抽屉 | 新建 | M |
| F9 | service 层 + 类型定义 | 新建 | S |
| F10 | 自检 | — | S |

**建议目录**：新增文件统一放 `src/pages/element-price/`（沿用既有目录），策略相关放 `src/pages/pricing/element-strategy/`。

---

## F9 · service 层与类型（先做，其余任务依赖）

**新建** `src/services/elementPriceStrategyService.ts` 与 `src/types/element-price-strategy.ts`。
**扩展** 既有 `src/services/elementPriceService.ts`（追加新端点，勿动既有方法——v1 遗留页面还在引用）。

按 `api.md` 逐个端点封装。类型定义要点：

```ts
export type PriceMethod = 'LATEST' | 'AVG' | 'MAX' | 'MIN';
export type WindowUnit  = 'DAY' | 'WEEK' | 'MONTH' | 'YEAR';

export const METHOD_LABEL: Record<PriceMethod, string> = {
  LATEST: '最新一条价', AVG: '窗口内平均值', MAX: '窗口内最高值', MIN: '窗口内最低值',
};
export const UNIT_LABEL: Record<WindowUnit, string> = {
  DAY: '天', WEEK: '周', MONTH: '月', YEAR: '年',
};

/** 全局策略（核价成本口径）的客户标识 —— 与后端字面量一致，全链路字符串 */
export const GLOBAL_CUSTOMER_NO = '_GLOBAL_';
```

> **🔒 `customerNo` 全链路用 `string`**，`_GLOBAL_` 原样传递，任何一层都不要转 UUID（§11.11.4）。

---

## F1 · 元素管理页（改 `pages/config/ElementManagement.tsx`）

**原型**：屏 1

### 改动 1：右上角新增 3 个按钮
在既有「搜索框 + 新建元素」之间插入，顺序与原型一致：

```
[搜索框] [🔗 价格源管理] [📥 价格导入] [📊 元素价格表] [＋ 新建元素(primary)]
```

三个按钮分别打开 F3 / F4 / F5 的抽屉。可见性沿用本页现有权限，**不新增权限判断**（§11.17）。

### 改动 2：时间列合并
- 删除「创建时间」「修改时间」两列，**合并为一列「最后修改时间」**，`dataIndex: 'lastModifiedAt'`；
- 该列显示排序箭头（默认倒序）；**排序由后端定，前端不本地 sort**（沿用文件顶部既有注释的约定）；
- 格式化沿用既有 `fmtTime`（`YYYY-MM-DD HH:mm`，空值 `—`）。

**🚫 不做**（§11.14D）：不加「最新价」列、不加变更类型/操作人小字。

### 其余不动
表格其它列、工具栏「编辑」「停用」动作、搜索防抖逻辑**保持原样**。

---

## F2 · 元素抽屉各源最新价（改 `pages/config/ElementEditDrawer.tsx`）

**原型**：屏 1-B

- 抽屉宽度 `520` → **`640`**；
- 在既有表单**下方**加分隔 + 「各源最新价格」区块，**仅 `editing` 非空（编辑态）时渲染**，新建态不出现；
- 打开抽屉时调 `GET /element-price/latest-by-source?elementCode={editing.elementCode}`；
- 表格 5 列（只读）：`价格源 / 最新价 / 货币 / 计价单位 / 价格日期`；
- `sourceStatus === 'DISABLED'` 的行**整行置灰** + 源名后加 `<Tag>源已停用</Tag>`；
- 返回 `[]` 时显示空态文案：**「该元素暂无任何价格记录，请通过『价格导入』录入」**；
- 表格下方小字：「该元素在 N 个源下有价格记录。价格录入统一走「价格导入」，此处只读。」

**🚫 不做**（§11.14D）：不加「距今天数」标记、不加「查看历史」按钮、**不在此录价**。

---

## F3 · 价格源管理抽屉（新建）

**原型**：屏 2　**宽度**：720

- `SelectableTable` + 顶部工具栏：`[＋ 新建源(primary)] [✎ 编辑] [⊘ 停用]`
  - 编辑：`enabledWhen: rows => rows.length === 1 ? true : '编辑一次只能选一行'`
  - 停用：`enabledWhen` 需排除已停用行 → 返回 `'仅启用状态可停用'`；`needsConfirm` + 列出所选项二次确认
- 列：`源名称（链接，点击进编辑）/ 网址 / 类型 / 备注 / 状态`
  - 状态用小圆点 + 文字（启用绿点 / 停用灰点），停用行整行置灰
- **无删除动作**（§11.13.1 不提供物理删除）
- 新建/编辑走**第二层抽屉**（480）：`源名称*（必填）/ 网址 / 备注 / 状态`
  - 网址下方提示：「仅作参考记录，系统不会自动访问」
  - `409` 错误直接 `message.error(e.message)` 显示后端文案（"源名称 + 网址 已存在"）

**🚫 不做**：不加「价格条数」列。

---

## F4 · 价格导入抽屉（新建）

**原型**：屏 3　**宽度**：720

### 表单区
- `价格源*`：Select，**只列启用源**（调 `GET /sources?status=ACTIVE`）；提示「只列出「启用」状态的源」
- `价格日期*`：DatePicker，默认今天；提示「本次文件里所有价格都记为这一天」
- `[⬇ 下载导入模板]` 按钮：`GET /import-template`，浏览器直接下载
- 上传区：`Upload.Dragger`，限 `.xlsx/.xls`、≤ 5MB；选中后显示文件行 + 「移除」
- 模板列提示（虚线框）：`元素符号*` `单价*` `货币` `计价单位` + 「元素符号须在「元素管理」里已存在且为启用状态」

### 结果区（导入后展示）
- 顶部 `Alert type="success"`：`导入完成：新增 N 条，覆盖 M 条，失败 K 条` + 第二行小字（源 / 日期 / 操作人 / 耗时）
- 明细表 7 列：`行号 / 元素符号 / 单价 / 货币 / 计价单位 / 结果 / 说明`
  - `结果` 用 Tag：新增=绿、覆盖=橙、失败=红
  - 失败行**整行浅红底**，说明列显示后端返回的具体原因
- 表格下方小字：「失败行不影响其他行入库；修正后可重新导入（会走覆盖逻辑）。」

### Footer
`[关闭] [开始导入(primary)]`　**🚫 不做**：不加「导出失败明细」按钮。

---

## F5 · 元素价格表抽屉（新建）

**原型**：屏 4　**宽度**：1200

`Tabs`：`明细` / `矩阵`

### Tab 1 明细
- 筛选行：`价格源`（Select，含"全部"）/ `日期区间`（RangePicker，默认最近 30 天）/ `元素`（Input，符号或中文名）+ `[查询][重置]`，右侧 `[⬇ 导出 Excel]`
- 表格 9 列：`元素符号 / 中文名 / 价格日期 / 价格源 / 单价 / 货币 / 计价单位 / 录入人 / 录入时间`
- 分页（每页 20），走后端分页参数

### Tab 2 矩阵
- 顶部 `Alert type="warning"` 固定说明：跨度默认 30 天最长 90 天；**必须先选定单一价格源**
- 筛选：`价格源*`（**必填、单选，无"全部"选项**）/ `日期区间` / `元素` + `[查询]`，右侧 `[⬇ 导出 Excel]`
- 表格：首列「元素」**左侧固定**（`fixed: 'left'`），其余列由响应 `dates` 动态生成
- `prices[i]` 为 `null` → 渲染 **「—」**（灰色），**绝不渲染 0**
- 表格下方小字：「「—」= 当天该源无价格记录（周末 / 未录入）。」
- 后端返 `400`（缺源 / 跨度超限）→ `message.warning(e.message)`，不清空已有结果

---

## F6 · 定价策略页新 Tab + `_GLOBAL_` 入口

**原型**：屏 5　**改**：`pages/pricing/PricingStrategy.tsx` + 新建子组件

### 6.1 页面骨架
- 左侧客户列表、右侧内容区的**既有布局保持不动**；
- 右侧内容区加 `Tabs`：**`折扣策略`（原有内容原样搬入）** / **`元素价格策略`（新）**。

### 6.2 客户列表顶部固定项（§11.11E）
在搜索框下方、客户列表**之上**插入一个固定项：

```
🌐 全局（核价成本口径）
[_GLOBAL_]  核价单取用
```

- 紫色底 + 下方分隔线，与真实客户视觉区分；
- **不参与客户搜索与分页**（搜索时它始终在顶部）；
- 选中它 → `selectedCustomerNo = '_GLOBAL_'`，右侧内容与真实客户**完全同构**；
- **选中它时「折扣策略」Tab 不显示**（折扣是客户维度概念）——用条件渲染 Tab items，非 disabled。

### 6.3 「元素价格策略」Tab 内容

**顶部操作条**：
```
取值优先级：元素例外 > 客户默认 > 留空（销售手填）      [🕘 变更历史] [🧮 策略试算]
```

**卡片 1 · 客户级默认策略**
- 卡片头：标题 + `已生效` Tag + **`最后变更 2026-07-22 10:15 · 张三`**（小字）+ 右侧 `[保存]`
- 表单 5 项：`价格源*`（Select，仅启用源）/ `取值方式*`（4 选 1）/ `窗口*`（数字 + 单位 Select）/ `系数`（默认 1）/ `加价`（默认 0）
- **联动**：`取值方式 === 'LATEST'` 时**窗口两个控件灰置（disabled）并清空值**；提交时**不要传** `windowNum`/`windowUnit`（传了后端返 400）
- 底部 `Alert type="info"`：「最终单价 = 取值结果 × 系数 + 加价（先乘后加）。基准日 = 报价单的创建日期，不是今天 —— 同一张单以后重开，价格不会自己变。」

**卡片 2 · 元素级例外**
- 卡片头：标题 + 小字说明 + 右侧 `[＋ 新增例外]`
- `SelectableTable` + 工具栏 `[✎ 编辑] [🗑 删除(danger)]`（删除 `needsConfirm` 并列出所选项）
- 列：`元素 / 价格源 / 取值方式 / 窗口 / 系数 / 加价 / 最后变更时间 / 变更用户`
- 新增/编辑走抽屉，字段 = 默认策略 5 项 + `元素*`（Select，选 ACTIVE 元素）
- 表格下方小字：「未列在此处的元素，一律走上方「客户级默认策略」。」

**底部** `Alert type="warning"`：「没配策略的客户：报价单元素单价留空，由销售手填。系统不设全局默认策略。」

---

## F7 · 策略试算抽屉（新建）

**原型**：屏 6　**宽度**：720

- 入口：F6 顶部操作条的 `[🧮 策略试算]`
- 顶部：`基准日`（DatePicker，默认今天）+ `[🧮 试算]`；日期下方提示「实际报价时 = 报价单创建日期」
- **请求带 `draft`**：把**当前页面上的表单值**（含未保存改动）一并提交，实现"先试算、满意再保存"（§11.14C）
- 结果表 9 列：`元素 / 命中规则 / 源 / 取值方式 / 取值结果 / × 系数 / + 加价 / 最终单价 / 参与天数`
  - `hitRule`：`EXCEPTION` → 紫 Tag「元素例外」；`DEFAULT` → 蓝 Tag「客户默认」
  - `hasPrice === false` → **整行黄底**，`最终单价` 列显示橙色粗体「无价」，数值列显示 `—`
- 底部 `Alert type="warning"`：「标黄行 = 策略窗口内一条价格都没有 → 报价单上该元素单价留空，销售手填。不会往窗口外找旧价顶上。」
- Footer 只有 `[关闭]`　**🚫 不做**：不加「导出试算结果」

---

## F8 · 策略变更历史抽屉（新建）

**原型**：屏 5-B　**宽度**：960

- 入口：F6 顶部操作条的 `[🕘 变更历史]`
- 筛选：`变更对象`（Select：全部 / 客户级默认策略 / 各元素例外，选"客户级默认"时传 `elementCode=__DEFAULT__`）/ `变更时间区间` / `变更用户` + `[查询][重置]`
- 表格 5 列：`变更时间 / 变更用户 / 变更对象 / 操作 / 变更内容`
  - `操作` Tag：新建=蓝 / 修改=橙 / 删除=红
  - **`变更内容` 渲染分三种情况**（差异由后端算好，前端**不做比对逻辑**）：
    - `action==='UPDATE'` → 遍历 `changes[]`，每项一行：`{fieldLabel}：<灰>{oldValue}</灰> → <橙粗>{newValue}</橙粗>`
    - `action==='CREATE'` → `changes` 为空，改用 `snapshot` 展示全量配置
    - `action==='DELETE'` → 显示 `删除前：{snapshot 摘要一行}`
- 分页（每页 20）
- **只读**：抽屉内无任何编辑/回滚入口（§11.24 D 节明确不做回滚）
- Footer 只有 `[关闭]`

---

## F10 · 自检（缺一不可，无此声明 = 未完成）

```bash
cd cpq-frontend
npx tsc --noEmit -p tsconfig.json          # 必须 0 错误
# 对每个改动/新增的 .tsx 逐个验证 Vite transform（必须 200）
curl -s --noproxy '*' -o /dev/null -w '%{http_code}\n' \
  http://localhost:5174/src/pages/config/ElementManagement.tsx
# ... 其余改动文件同样逐个跑
curl -s --noproxy '*' -o /dev/null -w '%{http_code}\n' http://localhost:5174/   # 主入口 200
```

> ⚠️ `tsc --noEmit` **不覆盖** Vite/Rollup 解析阶段错误（字符串嵌套引号、JSX 解析等），两者都要跑。
> ⚠️ worktree 内 5174 是主工作区的，改动可能不生效 —— 按 `cpq-worktree-frontend-selfcheck` 处理（软链 node_modules + 临时端口，或 `tsc` + 内容确证）。

### 功能自检（对应 §11.21）
- [ ] #1 源列表增改停用；停用行置灰；无删除按钮；撞名 409 提示可见
- [ ] #2 导入全流程；结果三色 Tag；失败行红底且原因可读
- [ ] #3 明细筛选/分页/导出；矩阵**源必填**、`null` 显示「—」不显示 0、首列固定
- [ ] #4 默认策略 + 例外增改删；`LATEST` 时窗口灰置且不提交窗口参数
- [ ] #10 `_GLOBAL_` 固定项在列表顶部、不参与搜索、选中后隐藏「折扣策略」Tab
- [ ] #11 历史三种 action 渲染正确（UPDATE 列差异 / CREATE 展示全量 / DELETE 展示删除前）
- [ ] #12 默认策略卡片头与例外表两列显示最后变更信息
- [ ] **#13 前端零改动核验**：`git diff --stat` 中**不得出现**任何报价/核价渲染文件（`QuotationStep2.tsx` / `ReadonlyProductCard.tsx` / `useDriverExpansions.ts` / `usePathFormulaCache.ts` 等）
- [ ] #15 各源最新价：3 源 3 行、停用源置灰标注、无价元素显示空态文案

### 完成声明格式
> "TS 0 错误 ✅；ElementManagement.tsx / ElementEditDrawer.tsx / PricingStrategy.tsx + 6 个新建文件 → Vite 均 200 ✅；5174 主入口 200 ✅；零改动核验：报价渲染文件 diff 为空 ✅"

---

## 附：本期明确不做（§11.24 D 节，实现时不得自行加回）

| 不做项 |
|--------|
| 报价单 `策略` 角标 / `已改` 标记 / 恢复策略价 / 取价明细 Popover / 页签红点 / **无价黄底** |
| 元素抽屉「距今天数」标记、「查看历史」按钮 |
| 元素列表「最新价」列、最后修改时间下的变更类型小字 |
| 价格源「价格条数」列 |
| 导入「导出失败明细」、试算「导出试算结果」 |
| 策略历史**回滚到指定版本** |

> 若实现中发现某控件在 §11.24 矩阵里查不到对应条目 —— 说明它是范围外的加法，**拿掉或先补需求**，不要自行实现。
