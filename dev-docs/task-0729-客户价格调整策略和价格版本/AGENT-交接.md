# AGENT 交接文档 · task-0729 客户价格调整策略与价格版本

> **给接手本任务的任意 Agent（后端 / 前端 / 测试 / 架构）**。
> 你没有需求澄清会的上下文，本文是你的**唯一入口**。按顺序读完再动手。
>
> 👤 **如果你是「人」而不是 Agent，请先读同目录的 `同事交接.md`** —— 那份讲现状、来龙去脉、接手第一天做什么；本文侧重符号级定位与硬约束，动手写代码时再回来查。
> 编写日期：2026-07-29 ｜ 编写人：技术总监（澄清与定稿阶段）

---

## 0. 一句话任务

元素行情波动后，系统按客户约定的节奏把新价固化成**可审核的价格版本**；财务**逐料号**审核，通过即给该料号**升版**并把它在所有活单里的**报价侧 + 核价侧**卡片重算一遍。

---

## 1. 当前交接状态

| 项 | 状态 |
|----|------|
| 需求澄清 | ✅ 完成（44 条裁决 + 4 条补充，三次结构性修正） |
| 原型定稿 | ✅ 完成（`价格调整策略-原型图.html` v10，7 屏） |
| 需求备案 | ✅ 完成（`需求说明.md` 991 行，§11 为权威口径；§11.5.3 = 待办池比对项实现口径；**§11.15 = 技术方案，与业务裁决同权威、冲突时以它为准**） |
| **独立架构评审** | ✅ 完成（无上下文架构师逐条读代码核实，查出 **7 阻断 + 12 重要**缺口；核心裁决 12/32/37/41 原方案落不了地） |
| **技术方案** | ✅ **定稿（§11.15）** —— 双通道分离 + 重算通道 8 步 + 六项框架决策（业务方逐条裁定）+ 修正 5 条既有裁决 + 更正 §11.0 中 4 条实证事实 |
| **开工前置验证** | ⬜ **6 项待做（§11.15.9）**，其中 P1/P5 需连库。**未做完不得出任务书** |
| 前后端任务文档 | ⬜ 未出（`backtask.md` / `fronttask.md` / `api.md`） |
| worktree 分支 | ⬜ 未建 |
| 代码 | ⬜ 未开工 |
| Flyway 迁移 | ⬜ 未写 |

---

## 2. 必读顺序（不要跳）

| # | 文档 | 读什么 | 必要性 |
|---|------|--------|--------|
| 1 | 本文件 | 全部 | 🔴 强制 |
| 2 | `需求说明.md` **§11.15 技术方案** | 🔴 **写代码前第一份要读的东西**。双通道分离架构 / 重算通道 8 步 / 价格字段推导算法 / JOIN 键铁律 / 六项框架决策 / **对 5 条既有裁决的修正** / **§11.0 中 4 条实证事实的更正** / 6 项开工前置验证 | 🔴 强制 |
| 2b | `需求说明.md` **§11 其余** | 44 条裁决 + 4 条补充。⚠️ **§11.0 现状盘点有 4 条已被证伪，必须连 §11.15.8 一起读**，不要单独采信；**§11.5.3** 是待办池比对项实现口径（行标红判定 / 标记格式 / 配置入口 / 预算落库），做屏 3、屏 4 必读 | 🔴 强制 |
| 3 | `价格调整策略-原型图.html` | 浏览器打开，7 屏逐屏看。屏 1 的单选**可点**（料号范围 / 周期类型有真实切换效果） | 🔴 强制 |
| 4 | `../task-0722-元素价格策略/需求说明.md` §11 | 本任务的地基：取价策略怎么算价、`f_customer_element_price` 怎么来的、为什么核价走 `_GLOBAL_` | 🔴 强制 |
| 5 | `/CLAUDE.md`（项目根） | 「修改后强制自检」「开发流程规范」「UI 交互规范」「代码探索规范」 | 🔴 强制 |
| 6 | `docs/列表操作规范.md` | 屏 3 待办池必须按此实现 | 🟡 前端强制 |
| 7 | `../task-0717-比对视图/api.md` §5 | `ColumnDef` schema —— 本期比对列**原样复用** | 🟡 强制（做比对列的人） |
| 8 | `docs/方案制定前必读.md` | 历史教训速查 + 决策清单 | 🟡 建议 |
| 9 | `docs/反模式.md` AP-31 / AP-37 / AP-51 / AP-53 | 涉及快照重算与 driver 时必读 | 🟡 建议 |

> **探索代码用 codegraph，不要 grep 乱翻**（`codegraph_context` 起手 / `codegraph_trace` 追链路 / `codegraph_impact` 评估影响面）。本仓库 `.codegraph/` 已就绪。
> ⚠️ 本环境 `grep` 是 `ugrep -I`，会把中文注释多的大源文件**静默返空** —— 用 `/usr/bin/grep -a` 复核，否则会得出"无引用"的错误结论。

---

## 3. 核心模型速记（背下来）

```
客户调价策略（周期 + 执行时刻 + 料号范围 + 参与元素 + 毛利预警线 + 比对列配置）
   │
   │ 到点触发（定时扫描每 15 分钟 / 或手动「立即生成一次」）
   ▼
元素价格版本 V26080501       ← V + YYMMDD + 两位当日流水
   ├── 报价侧快照：该客户 element_price_strategy 算出的元素价
   └── 核价侧快照：_GLOBAL_ element_price_strategy 算出的元素价    ← 裁决 41，双侧同源
   │
   │ 系统预算：受影响料号两侧各预演重算 → 按客户比对列出差异
   ▼
料号待办池（跨客户）
   │ 财务逐料号审核，依据 = 该料号【最近一张单】的比对列差异
   ├── 通过 → 料号版本指针 V26070501 → V26080501
   │          所有活单中该料号的【报价侧 + 核价侧】卡片重走公式
   │          单据产生/并入 R 版本（R + YYMMDD + NN，一期一版）
   └── 驳回 → 指针不动，两侧原样，下一期重新评估
```

**四个层次不要混**：

| 概念 | 粒度 | 编号 | 谁持有 |
|------|------|------|--------|
| 元素价格版本 | 客户 × 一次调价 | `V26080501` | 客户 |
| 料号版本指针 | 客户 × 料号 | 指向某个 `V` | 料号 |
| 报价单快照版本 | 报价单 × 一期 | `R26080501` | 报价单 |
| 比对列配置 | 客户 | — | 客户 |

**开关只有一个，取价口径只有一条**（2026-07-29 收敛，见 `需求说明.md` §11.4.2）：

```
唯一客户级开关 = 调价策略的「启用状态」
   启用 → 按周期生成版本 → 料号可审核升版 → 指针推进
   停用 → 不生成版本 → 指针冻结（已生效价格不受影响）

唯一取价口径 = 料号版本指针（裁决 35）
   指针有值 → 用该版本价
   指针为空 → task-0722 实时算   （从未升版 / 客户未启用 / 一直被驳回）
   清单外元素 → task-0722 实时算 （行为不变）
```

> 🗑 原「是否优先使用客户价格调整策略」勾选框（屏 2）**已整体废除**，裁决 3 / 4 / 21 作废。
> **元素价格策略 Tab 本期零改动**；数据模型**不得**为该开关预留列。

**取价走两条通道，别混**（2026-07-29 技术方案，`需求说明.md` §11.15.1）：

```
通道 A · 新建单初次取价
  mc_view ──JOIN──► f_material_element_price(customer_no, base_date, side)
                      ├ 指针有值 → 取该版价
                      └ 指针为空 → 回落 task-0722 实时算
  ★ 只迁报价侧视图；核价侧一个都不用改（指针仅 QUOTE 侧）

通道 B · 升版执行 + 审核预算        ← 同一段代码，只差 dryRun 布尔
  element_price_version_item ──直接读──► 字段级写回 snapshot_rows → 重算
  ★ 不走视图 / 不走函数 / 无需版本入参
  ★ 这是裁决 41「预算与实际逐位一致」的结构保证，不是靠纪律
```

> ⚠️ **通道 B 是本期最大的新建工程**（8 步，`需求说明.md` §11.15.2）。`snapshot_rows` 现在**没有任何"按新价刷新"的写入口** —— 不实现 S3，升版会被下一次 saveDraft **静默回滚**。

---

## 4. 🔴 硬约束（违反即返工，不接受"我觉得这样更好"）

### 4.1 隔离纪律（本方案的生命线）

1. **只对被升版的料号行执行重算与 `recompute`**。哪怕输入没变，也**不得**"整单重算一遍" —— 会走引擎兜底路径造成静默漂移。
2. 通过料号 A 后，料号 B 的 `quote_card_values` / `snapshot_rows` / `line_total_amount` / `discountRateApplied` 必须**逐字节不变**。这是验收要点 #14，会被逐字段断言。
3. 单据级聚合（`quotation.total_amount`、税额）随之变化是**预期**，不算破坏隔离。

### 4.2 核价侧纪律

4. **只动 `quotation_line_item.costing_card_values`**（报价单内的核价卡片值）。
5. **核价单 `costing_order` 的 frozen 快照逐字节不动**（task-0713 冻结语义）。验收要点 #21 会断言。
6. **禁止把客户策略价用于核价侧** —— 核价侧只能用 `_GLOBAL_` 口径，否则成本会随客户不同而不同，破坏成本口径。

### 4.3 版本纪律

7. 版本号 `V/R + YYMMDD + 两位当日流水`，流水按**客户 + 日期**（`R` 按**单 + 日期**）独立计数。**不要**用自增整数。
8. **`R` 版本一期一版**：同一 `V` 版内多次料号升版**合并**进同一 `R`（更新「最后更新」+ 追加「已升版料号」），**不新建版本号**。
9. 新版生成即把上一版**整体作废**，未处理料号随之失效；**已通过并执行的更新不回滚**。

### 4.4 取价纪律

10. **新建函数 `f_material_element_price`，不要改 `f_customer_element_price` 的签名** —— 后者被 6 个 `mc_view` 在用，改签名等于强制一次性全量迁移且无法回滚。
11. 6 个 `mc_view` 的 `sql_template` 迁移必须**可逆**（保留原文备份）。
12. `DataLoader.resultCache` 等按 `$view` 的缓存键**必须含客户 + 料号维度**，否则跨单串价（历史教训：`$view 缓存 key 需含 componentId`）。
13. **改视图 / DDL 后必须重启 Quarkus**（`ImplicitJoinRewriter.tableColumnsCache` 等是进程级缓存，见 CLAUDE.md「视图 DROP CASCADE / 重建后必须重启」）。

### 4.5 UI 纪律

14. 屏 3 待办池**严格按 `docs/列表操作规范.md`**：行内零动作按钮、动作上提工具栏、禁用态**置灰 + hover 说明原因**（**禁止** `if (...) return null` 隐藏）。**抽屉入口 = 料号链接，不是整行点击**（曾被提出，已否决：整行点击会与批量审核的多选语义打架，见 `需求说明.md` §11.5.3(6)）。
15. **一律 Drawer，不用 Modal** —— 唯一例外是屏 5 的危险动作二次确认（列表规范明确允许）。
16. 元素矩阵（屏 1）必须**一次 pivot 查完**（元素 × 版本），**不得**逐元素查 10 次。
17. 🚨 **屏 3 行标红 = `breached_count > 0`（任一比对列 `status=RED`），不是「产品总价差异 < 0」** —— 两者**不等价**，写成后者则裁决 44 形同虚设。样例见原型屏 3 第 5 行 `10120240`（产品总价 +4.05 / 毛利 9.2% 都健康，但自定义列已卖穿 → 整行必须红）。验收 #25 专测此点。比对状态标记须**红橙分开计数** `🔴N 🟠M / X列`，禁用旧写法「M/N 列跌破」。
18. **比对列配置唯一写入口 = 屏 1**（价格调整策略 Tab）；屏 4 审核抽屉**只读展示，不得提供修改入口**。列表与抽屉的比对数据**都读 `material_price_review_column` 落库值**，禁止任一端实时算 —— 否则两处对不上，破坏裁决 41。改配置后重算的范围**只含 `待处理` 料号**。详见 `需求说明.md` §11.5.3。

### 4.6 范围纪律

19. **§11.12「明确不做清单」16 条**，一条都不要"顺手"加回。特别是：不做抓价爬虫、不做手工值保护/已改标记、不做编辑锁、不做版本回滚、不硬阻断跌破毛利线的通过、**不做「是否优先使用客户价格调整策略」勾选框或任何"新单是否用版本价"的独立开关**（#15，2026-07-29 废除，见 §11.4.2）。
20. 🚫 **核价单切版本调价（BACKLOG `BL-0006`）不在本期**（2026-07-29 裁定：后续单独立项，不与本期混合）。它切的是**料号 BOM 版本**，与本期的**元素价格版本**是**两条不同的轴**，别混。由此三条硬约束：
    - **裁决 42 与不做清单 #3 保持不变** —— 本期核价侧只动 `quotation_line_item.costing_card_values`，`costing_order` frozen 快照**逐字节不动**，验收 #21 照常逐字段断言；
    - **不要**新建 `costing_order_revision` / `costing_order_line_snapshot`（那是 BL-0006 的范围）；
    - **不要**碰 `:versionFilter` 宏与 `VersionFilterMacro` —— 那是 task-0713 核价单版本切换作用到 BOM 主树的通道（见 `docs/RECORD.md` 2026-07-26 V363 条目），非本期范围。
    > 📌 做 BL-0006 时须先解决命名二义：届时"料号版本"会同时指**价格版本**（本期）与 **BOM 版本**（BL-0006），两者都会出现在核价界面上。本期无此问题（`part_version` 族已由 task-0723 B3 整族下线，该词当前无占用）。

---

## 5. 已实证的技术事实（不用重新考古）

全部在澄清期间用 SQL / 源码查证过。

> 🚨 **2026-07-29 独立架构评审后：本表 #2 / #3 / #5 / #7 / #9 已被证伪或需补前提，切勿直接采信。**
> **必读 `需求说明.md` §11.15.8** 的逐条核实结论。下表已就地标注。

| # | 事实 | 影响 |
|---|------|------|
| 1 | `f_customer_element_price(p_customer_no, p_base_date)` **只有两个入参**，返回 `(element_code, unit_price, currency, price_unit)`，**无料号维度** | 必须新建料号级函数（裁决 31） |
| 2 | ⚠️ **半错** —— 视图内有 `ebi.material_no` 属实，但 ~~它是销售料号~~ **在 BOM 闭包形态下是子件料号**；「6 个」是 DB 状态，**未核实** | ❌ ~~JOIN 条件加得上，不用改数据源~~ → **JOIN 键必须与该视图 `hf_part_no` 输出表达式逐字一致**，两形态并存**禁止统一替换**（§11.15.4.2）；且**只迁报价侧** |
| 3 | `quotation` 表**零个** lock / edit 列，全系统只有 `ProductImportLock` / `DdlOperationLock`（**属实**） | ❌ ~~无编辑锁 → 用乐观时间戳~~ → `quotation.updated_at` **不是可靠水位线**（单元格编辑/删恢复行/树增删叶/选配加产品**全都不 bump 它**）。裁决 32 已改判为**行级 JPA `@Version`**（F1） |
| 4 | 卡片值按 `quotation_line_item` 存（1 line_item = 1 销售料号 = 1 产品卡片） | 料号级隔离天然成立 |
| 5 | `LineDiscountService.recompute(QuotationLineItem li)` **入参就是单行**，无跨行分摊（**属实**） | ⚠️ **漏了致命前提**：其 S0 **优先采信 `li.subtotal`**，而后端**无任何重算写入源**（3 处写入全是搬前端 DTO）→ 不主动写回 `li.subtotal`，**升版后行金额与单据总额都不会变**。解法见 §11.15.2 的 S6 |
| 6 | `discountRateApplied` 的 **4 个写入点全是前端草稿的持久化人工值**，后端无"按总额匹配折扣阶梯"逻辑 | 料号 A 涨价不会让 B 的折扣率跳档 |
| 7 | `ComparisonViewService` 的 `productTotal` = SUBTOTAL 组件页签 `subtotal`；同款算法 `CostingSubtotalUtil#extractUnitSubtotal` | ⚠️ ~~直接复用~~ **只对"取值"成立**。差异/阈值/着色 **100% 前端算**（`comparisonMapping.ts:84-115`），后端**连比对视图的 `ColumnDef` 类型都没有**（`ComparisonConfigDTO.java:20`；`basicdata.v6.maintenance.dto.ColumnDef` 是**同名不同物**）→ §11.5.3(5) 预算逐列落库 = 后端**新写一份 diff/着色算法**，须移植 + 单测对齐黄金用例 |
| 8 | `quotation_comparison_config`（`quotation_id` + `bucket` + `columns` jsonb），`ColumnDef` schema 见 task-0717 api.md §5 | 比对列 schema 原样复用；但存储维度要改成**按客户** |
| 9 | ❌ ~~`ScheduledTaskService` 已有**三个** `@Scheduled`~~ → **实为 7 个**（`:30/:44/:57/:69/:83/:100/:155`），另有 `ProductImportLockService:184` | ⚠️ **全部未配 `concurrentExecution`**。新增任务**必须带 `@Scheduled(concurrentExecution = SKIP)`** + 幂等键 `UNIQUE(customer_no, scheduled_slot)`，否则验收 #4 判不出对错 |
| 10 | `material_customer_map`：客户料号 = `customer_product_no`，名称 = `customer_material_name`，销售料号 = `material_no`；**`customer_product_no` 现网大量为空** | 屏 1 料号矩阵按此取数，空值显示「—」但可勾 |

---

## 6. 分工边界与起手动作

### 6.1 后端工程师

**起手**：读 `需求说明.md` **§11.15 技术方案**（必须先读，它修正了 5 条裁决 + 4 条实证事实）→ 跑完 **§11.15.9 的 6 项开工前置验证**（P1/P5 需连库；**未跑完不要动笔写迁移脚本**）→ 读 `backtask.md`（待产出）→ 建 worktree。

主要工作块：
0. 🔴 **通道 B 重算通道**（§11.15.2 的 8 步）—— **本期最大的新建工程，也是最容易漏的**。`snapshot_rows` 现在没有任何"按新价刷新"的写入口；S3/S4/S6/S7 四个洞在同一条链路上，**必须一次性设计**，分开补会互相打架
1. 表结构（§11.10 的 8 张 + §11.15.6 追加的 job/job_item 两张 + 幂等键/唯一约束/索引）+ Flyway 迁移
2. `f_material_element_price` 函数 + 6 个 `mc_view` 的 `sql_template` 迁移（**可逆**）
3. 定时扫描任务（每 15 分钟，幂等补跑）+ 版本生成（**双侧快照写入同一事务**）
4. 预算计算（受影响料号两侧预演重算，**落库**供审核页秒读）
5. 审核 API（通过 / 驳回）+ 异步更新任务（乐观时间戳 + 逐单事务 + 进度）
6. 比对列配置 CRUD（按客户）+ 比对结果计算（复用 `ComparisonViewService` 算法）

⚠️ **Flyway 版本号是移动靶**：共享库并发迁移，落库前先查 `flyway_schema_history` 最大版本号，**不要**改已应用的迁移文件（历史教训 `cpq-shared-flyway-history-churn`）。

### 6.2 前端工程师

**起手**：读 `fronttask.md`（待产出）→ 对照原型 7 屏 → 先做屏 1（策略 Tab），它是其余屏的数据源头。

主要工作块：
1. 屏 1：策略表单（周期四形态切换）+ 料号矩阵 + 元素矩阵（10 版价格 pivot）+ 比对列配置 + 版本轨迹
2. ~~屏 2：元素价格策略 Tab 顶部加勾选框~~ 🗑 **已废除（2026-07-29），本项取消** —— **元素价格策略 Tab 本期零改动，不要碰它**。依据见 `需求说明.md` §11.4.2（裁决 3/4/21 作废）。工作块编号保留空位，以对齐屏号
3. 屏 3：待办池（`SelectableTable` + 工具栏动作 + 禁用态 tooltip）
4. 屏 4：料号审核抽屉（1200px，三段结构 + 双色分组表头）
5. 屏 5：确认 Modal（唯一允许的 Modal）
6. 屏 6：进度抽屉（轮询 + 三种非成功态）
7. 屏 7：报价单价格版本抽屉（轨迹表 + 料号级版本表 + 预览态禁用一切写操作与导出）

⚠️ **worktree 里不要另起 dev server**：5174 / 8081 是全会话共享的，直接复用（历史教训 `cpq-concurrent-sessions-and-worktree`）。前端自检要软链 `node_modules` 或另起临时端口（`cpq-worktree-frontend-selfcheck`）。

### 6.3 测试工程师

**起手**：读 `需求说明.md` §11.11（**31 条验收要点，全部可测**）+ §11.15 技术方案 + 本文 §4 硬约束。

⚠️ **四条新增的核心验收，专防静默失败**：#28 升版不被 saveDraft 回滚 / #29 手工值按列清除（单价被覆盖、毛重不被误伤）/ #30 不打开报价单直接导出即为新价 / #31 BOM 闭包的子件同样吃版本。
⚠️ **#14 必须双向断言** —— 只写"料号 B 不变"是**无效测试**（`CardSnapshotService` 从不写 `snapshot_rows`，该断言天然成立，即使 A 的价格根本没更新也会绿）。

重点测四件事：
1. **隔离性**（#14）：通过料号 A 后，B 的四个字段逐字节不变 —— 这是最该被验的一条
2. **核价联动 + 冻结隔离**（#21）：核价卡片变了、`costing_order` frozen 没变
3. **双侧同源可复现**（#22）：审核页预算的核价值 = 通过后实际重算值，逐位一致
4. **R 版本合并**（#17）：同期分两次通过两个料号 → 只有一个 R 版本

⚠️ **E2E 夹具可能失效**：BL-0078 记录多个 spec 硬编码 quotationId、在 **`cpq_db_0724`** 中进不了编辑页。**但那是对 `cpq_db_0724` 实测的结论，本开发环境是 `jh` profile → `localhost:5432/cpq_db`，未验证，不要直接照搬**。跑 E2E 前先确认夹具单号在**本库**是否存在；判回归必须 **A/B 同型对比**（在 master 上跑同一 spec 看是否同样失败），否则会把环境噪声当真回归。

---

## 7. 环境与自检

```bash
# 端口探活（⚠️ 本机 http_proxy 会让 curl 走代理返 502，必须加 --noproxy）
curl -s --noproxy '*' -o /dev/null -w '%{http_code}\n' http://localhost:5174/                  # 前端 期望 200
curl -s --noproxy '*' -o /dev/null -w '%{http_code}\n' http://localhost:8081/api/cpq/components # 后端 期望 401
# ⚠️ /q/health 返 404 —— 未装 smallrye-health，它不是健康探针

# 数据库 = 本机 docker PostgreSQL 16
PGPASSWORD=joii5231 psql -h localhost -U postgres -d cpq_db
```

**🚨 启动必须带 `jh` profile，三个 profile 别搞混**（连错库 = 改动看不到效果，还可能污染别人的环境）：

```bash
cd cpq-backend && ./mvnw quarkus:dev -Dquarkus.profile=jh   # ← 当前开发环境
cd cpq-frontend && npm run dev
```

| profile | 主机 / 库 | 用途 |
|---------|----------|------|
| **`jh`** | **`localhost:5432/cpq_db`** | ✅ **当前开发环境，日常开发用这个** |
| 默认（不带 `-Dquarkus.profile`） | `10.177.152.12:5432/`**`cpq_db_0724`** | ⚠️ **另一个开发测试库**，不是本环境 |
| `test` | `10.177.152.12:5432/cpq_db` | 测试 |

> 本文早期版本曾写「2026-07-24 起用 `cpq_db_0724`，不要连旧 `cpq_db`」—— **该说法已更正**：`cpq_db_0724` 是另一套开发测试库，本环境用 `jh` profile 连本机 `cpq_db`。

**完成任何改动前必跑**（CLAUDE.md「修改后强制自检」）：
- 前端：`npx tsc --noEmit` 0 错 + 每个改动的 `.tsx` 走 Vite 返 200
- 后端：touch 一个 java 文件触发重启 → 业务端点返 401（非 500）→ Flyway `success=t`
- 协议级改动：Playwright E2E（本任务涉及快照重算，**建议跑**）

**任何"完成"宣告必须带一行"已自检"声明**，没有 = 未完成。

---

## 8. 本任务特有的坑（预警）

| 坑 | 说明 | 规避 |
|----|------|------|
| **预算与实际不一致** | 若核价侧预算用实时价、执行用版本价，两者会差 | 严守裁决 41：两侧都从**同一版本快照**取，禁止任何一端实时算 |
| **视图迁移后不重启** | 改 `sql_template` 后进程级缓存残留，BNF 路径求值不注入谓词 → 视图返全表 | 改完**必须重启 Quarkus**，并用含料号的端点验证返单值 |
| **缓存串价** | `$view` 缓存键漏料号维度 → A 料号拿到 B 料号的价 | 缓存键含 `customerCode + materialNo`；写完用两个不同料号交叉验证 |
| **跨客户列表列不统一** | 比对列按客户配置，待办池是跨客户的 | 列表固定列 + 汇总标记，抽屉才展开（裁决 43，已在原型体现） |
| **`_GLOBAL_` 无策略** | 某元素没有 `_GLOBAL_` 取价策略 → 核价侧无价 | 版本明细该元素核价侧标「无价」，审核页显式提示，**不要**回退到客户价 |
| **手工值被冲掉** | 裁决 37 明确直接覆盖，但销售会懵 | 完成通知里**必须**带"此前手工调整可能已被覆盖，请复核" |
| **grep 静默返空** | 本环境 `grep` 是 `ugrep -I` | 用 `/usr/bin/grep -a` 复核 |

---

## 9. 三处"明示接受的取舍"（验收不得以此判不通过）

1. **单内同一元素可能有多个价**（§11.1.1）：料号 A 已升版、B 被驳回时，Ag 在两张卡片里价格不同。**是预期行为**，屏 7 料号级版本表逐行可查证。
2. **销售手工值被覆盖**（裁决 37）：系统不记录哪些值被手改过，升版一律覆盖；只在通知里提示复核。
3. **快照永久保留**（裁决 38）：不做清理，需配套库体积监控。

---

## 10. 有疑问时的决策顺序

1. 查 `需求说明.md` §11 有没有对应裁决 → 有则照做，**不要重新设计**
2. 查 §11.12「明确不做清单」→ 在清单里则**不做**
3. 查本文 §5「已实证事实」→ 别重复考古
4. 仍不明确 → **问技术总监 / 需求方，不要自行假设**。本任务经历三次结构性修正，自行假设的代价很高。

---

## 11. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-07-29 | 创建。澄清与原型定稿完成后交接，此时任务文档与代码均未开工 |
| 2026-07-29 | 补录待办池比对项实现口径（`需求说明.md` §11.5.3，**不改裁决**）。本文同步：§4.5 新增硬约束 17/18（行标红判定 + 配置入口唯一性与预算落库），原 17 顺延为 19；§6.3 验收要点 24→27 条；§2 必读顺序加指 §11.5.3 |
| 2026-07-29 | 🔴 **独立架构评审 + 技术方案定稿**（`需求说明.md` **§11.15**）。评审查出 **7 阻断 + 12 重要**缺口，核心裁决 12/32/37/41 原方案落不了地。定稿内容：**双通道分离**架构（通道 B 使"预算与实际一致"成为结构保证）+ **重算通道 8 步** + **价格字段推导算法**（零新增配置）+ **JOIN 键铁律**（只迁报价侧）+ **六项框架决策**（业务方逐条裁定）+ **修正 5 条既有裁决** + **更正 §11.0 中 4 条实证事实** + **6 项开工前置验证**。<br>本文同步：§1 状态表加评审/技术方案/前置验证三行；§2 必读顺序把 **§11.15 提为第 1 位**并给 §11.0 加证伪警告；§3 加双通道图；**§5 实证事实表 #2/#3/#5/#7/#9 就地标注证伪**；§6.1 起手改为"先读 §11.15 + 跑完前置验证"并新增工作块 0（重算通道）；§6.3 验收 27→31 条 + 四条新增核心验收 + #14 双向断言警告 |
| 2026-07-29 | **裁决 3 / 4 / 21 作废 —— 「优先使用勾选框」整体废除**（`需求说明.md` §11.4.2）。本文同步：§3 增「开关只有一个、取价口径只有一条」终态图；§4.6 不做清单 14→15 条并点名该开关；§6.2 前端工作块 2 标废除（**元素价格策略 Tab 本期零改动**）。⚠️ **屏号不重编**，屏 3~7 引用全部有效 |
