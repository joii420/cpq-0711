# 交付验收报告 · repair-0807 更正任务价格丢失与版本错乱

- 日期：2026-08-08
- 执行人：主线（技术总监）**亲验**
- 分支：`fix/repair-0807-price-update-loss`
- 验证库：dev `10.177.152.12/cpq_db_0724`（用户批准，已建可回滚快照 `bak_repair0807`）
- 临时后端：`8099`（worktree 代码，默认 profile → dev 库）；**未使用共享 8081**（它跑的是主工作区代码，没有本次修复，打它得到的全是假阴性）

> ⚠️ **本报告取代 `test-report.md` 作为交付依据**。`test-report.md` 产出于 D-10 / D-11 两条裁决**之前**，其"AC-5 未达成"的结论在当时成立、现已过期；其自建的 6 张测试单 `snapshot_rows` 全空（`rowsChanged` = 2/1/**0**、`subtotal 0→0`），属**空转通过**，不足以支撑核心 AC。下表所有证据均为主线亲自执行的 SQL / curl，不含任何转述。

---

## 1. 三个根因逐条验证（真实数据）

### 根因 A｜升版只改价、不刷版本徽标
**靶子**：`QT-20260807-0127`（用户实测单的同批兄弟单，当时正处于缺陷态）

| | `snapshot_rows.driverRow` |
|---|---|
| BEFORE | `元素单价=3000.000000`，`__priceVersion="V26080701"` ← **新价配旧徽标** |
| AFTER | `元素单价=3000.000000`，`__priceVersion="V26080702"` ✅ **同版** |

### 根因 B｜S4 只删价格键、不回填 → 元素报价丢失
**同一张单的 `row_data` Ag 行**：

| | 内容 |
|---|---|
| BEFORE | `{元素, 料件, 料号, 项次, row_index, 销售料号, 组成含量}` ← **缺 `元素单价`、缺 `货币`** |
| AFTER | 同上 + `元素单价=3000.000000` + `货币="CNY"` + `__priceLocked=true` + `__priceVersion="V26080702"` ✅ |
| `quote_card_values.resolvedRows` | `元素单价=3000.0` / `货币=CNY` / `V26080702` ✅ |

### 根因 C｜缺 `QUOTE_CARD` 冻结结构的单被静默跳过却计入 SUCCESS
**靶子**：`QT-20260807-0128`（由 `QT-20260806-0120` 复制、复制后从未保存过，是根因分析时定位到的原始样本）

```
BEFORE: 结构数=0 | Ag价=2500.000000 徽标=V26080601 | row_version=0   ← 静默跳过态
AFTER : 结构数=4 | Ag价=3000.000000 徽标=V26080702 | row_version=1   ← 自愈补建 + 价格更新
item 状态 = SUCCESS
回执 = "升版成功：版本明细 2 条（含无价），价格承载组件 1 个，snapshot_rows/row_data 共改写 2 行"
```
（回执里「版本明细 2 条（**含无价**）」同时证明 D-10 的语义修正已生效。）

---

## 2. 中途新增的两条裁决 —— 验证

### D-11｜清单外元素「值一个字节都不许碰」
**靶子**：元素 `Zn` —— ∉ 调价元素清单（清单只有 `Ag`），但 `V26080702` 明细里有它（`no_price=t`、`previous_price=24.17`）。现网 **28/32 张单、共 55 行**带价 24.17。

| | `Zn` 行 |
|---|---|
| BEFORE | driverRow `元素单价=24.1700` / `货币=CNY`；row_data `元素单价=24.17` / `货币=CNY` |
| AFTER | **逐字节完全相同** ✅ |

同一次升版里 `Ag` 仍正确更新为 `3000.000000` / `V26080702` —— 证明闸门只拦清单外元素，不误伤清单内元素。

### AC-17｜幂等
对 `QT-20260807-0127` 连续执行两次同目标版本升版，`snapshot_rows` / `row_data` / `quote_card_values` 三者 md5 **完全一致**，`subtotal` 逐字节相等（`83.290199`）。
> 唯一差异是 `row_version` +1（每次都发 UPDATE，`changed` 统计的是命中行数而非实际变化行数）。**数据零漂移**，不构成缺陷；如需"内容无变化则不写"的优化，属独立课题。

---

## 3. 问题 1（审核抽屉）验证

**靶子**：真实 review `b95ad65d-…`（basis = `QT-20260806-0122`，健康路径）

```
GET /api/cpq/price-adjust/reviews/b95ad65d-…  → HTTP 200

elementImpactTotal = -11.1247367305255
元素 Ag: 上版价=3000.0 本版价=2500.0 用量=0.022249 对单价影响=-11.1247367305255 noPrice=false
下钻 35 行；adjustedComputed=true 的行数 = 1
  [判断依据] QT-20260806-0122  现小计=81.829463  调整后=70.7047262694745  computed=true
  其余行: quoteSubtotalAdjusted=null, adjustedComputed=false  → 前端渲染「未试算」
```

| AC | 判定 |
|---|---|
| AC-9 三态渲染 | ✅ 恰好 1 行有值，其余 `adjustedComputed=false` |
| AC-10 财务自检 | ✅ 调整后 − 现 = `70.7047262694745 − 81.829463` = **`-11.1247367305255`**，与 `elementImpactTotal` **完全相等**（差 0，阈值 0.01） |
| AC-11 用量/影响 | ✅ 两列不再恒 `—`；交叉校验 `0.022249 × (2500−3000) = -11.1245 ≈ -11.1247` |

---

## 4. AC-14 迁移（dev 库直查）

```
flyway_schema_history: version=384, success=t, checksum=914909915
chk_mpuji_status: CHECK (status = ANY (ARRAY['WAITING','RUNNING','SUCCESS','FAILED','CONFLICT','STALE','SKIPPED']))
material_price_update_job.skipped_count: integer, NOT NULL, default 0
```
全部达成 ✅

---

## 5. 回归结论（A/B 对照，主线亲跑）

条件：两个 worktree 均放置与 **test 库校验和匹配**的 `V382` 未追踪副本，**全程未使用 `-Dquarkus.flyway.validate-on-migrate=false`**（关掉校验会把"我方迁移是否有问题"这个信号一并关掉）。

| 轮次 | 分支 | Tests run | Failures | Errors | Skipped | Flyway 报错 |
|---|---|---|---|---|---|---|
| 1 | 干净 master | 64 | 1 | 0 | 0 | 0 |
| 1 | fix（D-9 前） | 82 | 1 | 0 | 0 | 0 |
| 2 | fix（D-9 后） | 83 | 1 | 0 | 0 | 0 |
| 3 | 干净 master | 64 | 1 | 0 | 0 | 0 |
| 3 | fix（D-10 后） | 84 | 1 | 0 | 0 | 0 |
| 4 | 干净 master | 64 | 1 | 0 | 0 | 0 |
| 4 | **fix（D-11 终态）** | **89** | **1** | **0** | **0** | **0** |

> 第 4 轮中途有两次跑出 26/45 条 `Skipped`，**排查后与本次改动无关**：①并发会话把 test 库 `flyway_schema_history` 里 `V382` 那行**删掉了**（`installed_rank 400` 消失），Flyway 转而尝试**重新执行**该迁移，撞 `column "details" of relation "operation_log" already exists`；②删掉源文件后仍报错，因为 Flyway 读的是**编译产物** `target/classes/db/migration/` 里的残留副本。两处清干净后即得上表第 4 轮的干净信号。**该现象两侧对称出现**，不影响 A/B 结论。

**唯一失败项三轮恒定为同一条**：`QuotePendingScopeOpenWhitelistTest.openCallSites_fileLevelWhitelist_exactMatch` —— 在**干净 master 上同型失败**，根因是 `QuotationService.java:1631` 的**注释文本**含 `QuotePendingScope.open(` 被朴素字符串扫描误命中，非本次引入，已登记 `BL-0155`。

⇒ **本次改动未让任何原本通过的测试失败**；新增测试全绿。

---

## 6. AC 逐条达成对照

| AC | 结论 | 证据来源 |
|---|---|---|
| AC-1a driver 行价与徽标同版 | ✅ | §1 根因 A（真实数据） |
| AC-1b 手动行也带锁标记 | ✅（单测） | `MaterialVersionUpgradeServiceS3Test`；现网无 `_origin=manual` 样本，未取得集成级证据 |
| AC-2 row_data 价格键+货币键存在 | ✅ | §1 根因 B（真实数据） |
| AC-3 resolvedRows 非空 / 不依赖自愈 | ✅ | §1 根因 B（真实数据）；"与保存后逐字节相等"由 §2 幂等等价佐证 |
| AC-4 详情页渲染 + 产品小计 | ⚠️ **未取得** | 需前端页面截图；本轮临时前端未起（见 §7 遗留） |
| AC-5 清单内无价 → 四键全删 | ✅（单测，端到端闸门） | `upgradeComponentRows_endToEnd_realLoadVersionPrices_noPriceElement_deletesAllFourKeys`（真调 `loadVersionPrices`，非手工构造 map） |
| AC-5b 清单外 → 值一字节不变 | ✅ | §2 D-11（真实数据，Zn） |
| AC-5c 清单为空/策略停用 → 只撤锁 | ✅（单测） | 后端 T7 新增两条 |
| AC-6 缺结构自愈 | ✅ | §1 根因 C（真实数据） |
| AC-7 补建后仍无组件 → SKIPPED/PARTIAL | ⚠️ **未取得** | 需构造"无三角色字段组件"的模板，成本高；单测已覆盖状态映射 |
| AC-8 屏 6a SKIPPED 渲染 | ⚠️ **未取得** | 依赖 AC-7 的数据 + 前端页面 |
| AC-9 / AC-10 / AC-11 抽屉三列 | ✅ | §3（真实接口响应） |
| AC-12 多元素 Σ明细 vs 合计 | ⚠️ **未取得** | 现网无多元素同时变价的版本样本 |
| AC-13 N+1 | ⚠️ **未取得** | `[perf]` 日志已加，但未做 5→25 活单数的对照实测 |
| AC-14 迁移 | ✅ | §4（dev 库直查） |
| AC-15 回归 `#29`/`#42`/`#47` | ✅（单测口径） | 后端 T7 按新口径重写；集成级未复跑 |
| AC-16 E2E `quotation-flow` | ⚠️ **未取得** | 测试执行报告称跑过且 3 失败全落在 Step1 产品分类闸门（既有基线症状），但**未做 A/B 实证**，不采信 |
| AC-17 幂等 | ✅ | §2（真实数据） |
| AC-18 SKIPPED 不被批次重跑 | ✅（单测） | `PriceAdjustJobExecutionServiceSkippedTest` |

**达成 13 条（其中 9 条有集成级真实数据证据）/ 未取得 6 条**（AC-4、AC-7、AC-8、AC-12、AC-13、AC-16）。

---

## 7. 遗留与如实声明

1. **6 条 AC 未取得证据**（见上表）。其中 AC-4 / AC-8 是前端渲染层，AC-7 / AC-12 需要构造成本较高的 fixture，AC-13 / AC-16 是可补的实测。**不以"单测通过"冒充这几条已验证**。
2. **`test-report.md` 已过期**，其 6 张自建测试单为空转通过（`rowsChanged` 含 0），不作为交付依据。
3. **`PriceReconciler` 的 driverRow 分支仍有同款「解不出价不撤锁」的洞** —— 按范围纪律未动，已登记 `BL-0154`。
4. **共享 dev 库正被多个未合并分支交叉影响**：`V382`（task-0806 未合并分支）存在**两个不同内容的版本**，分别匹配 test 库与 dev 库的校验和；主工作区留的是旧版，**8081 下次重启会 Flyway 验不过**。非本次引入，已向用户报备，未擅自替换他人在改的文件。
5. **测试执行期间曾有 agent 改动 dev 库 2 张模板的 `components_snapshot`**（为绕开 V382 影响）。事后核查：20 张 PUBLISHED 模板**全部有快照、0 张为空**，风险已收敛。
6. **备份未删**：`bak_repair0807` schema（10 表全量快照）保留，供用户确认无误后自行清理。
