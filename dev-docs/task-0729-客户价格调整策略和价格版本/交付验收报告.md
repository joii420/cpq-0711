# task-0729 客户价格调整策略和价格版本 · 交付验收报告

> **状态：编写中**（验收进行时累积，结案前定稿）
> 技术总监维护。本文是交付给业务方的**唯一权威说明**：做了什么、没做什么、已知限制是什么、上线前要注意什么。
> 详细需求裁定见 `需求说明.md`，端点契约见 `api.md`，测试用例见 `testcases.md`。

---

## 1. 验收进度

| 批次 | 范围 | 状态 |
|---|---|---|
| 第一批 | 8 条（核心链路） | ✅ 8/8 通过 |
| 第二批 | 9 条（跨端） | ✅ 通过 + 挖出 2 个真问题 |
| 第三批 P0 | 14 条 | 🔄 进行中 |
| 第三批 P1/P2 | 剩余 | ⏳ 未开始 |

**当前：23/70**

---

## 2. 🔴 上线前必读（业务方视角）

### 2.1 成本预警线在生产上不会触发

**现象**：屏 3/屏 4 的红/橙标记，上线后大概率**全绿**。

**原因**：本环境所有已检查的真实报价单，**核价侧总小计恒为 0**（V6 成本数据未导入/未联动）。差异 = 报价侧成本 − 核价侧成本，核价侧恒 0 → 差异恒为正 → **RED 结构上不可达**。

**不是**：功能有 bug（判定逻辑已用构造数据正反双向验证通过）。

**要做什么**：上线前确认 V6 成本数据已导入并与报价单关联，否则预警线形同虚设 —— **全绿不代表没有超支，代表没有可比的核价成本**。

### 2.2 核价卡片内同一元素可能出现两个价

见 `需求说明.md §12.6.4`（业务方 2026-08-04 明示接受）。

**一句话**：某零件若**既是别人的子件、自己又是可独立核价的组件**，它下挂物料的元素价跟随**它自己**的价格版本，而非当前卡片所属成品的版本。

**识别方法**：查 `bom_closure` 该物料的归一根是否等于当前卡片料号（SQL 见 §12.6.4）。

### 2.3 已发出单在屏 7 显示「尚未更新」

见 `需求说明.md §11.7.3`（业务方 2026-08-04 裁定）。

`SENT`/`ACCEPTED` 等单按 §11.6.0 有意排除、不参与自动更新，实际停在旧价。屏 7 显示「尚未更新」而非「已升版」—— **这是刻意的，不是漏更新**。

---

## 3. 已知缺口清单

> 分三类：**A=功能未实现** / **B=实现有瑕疵** / **C=验证受限**

### A · 功能未实现

| # | 缺口 | 影响 | 处置 |
|---|---|---|---|
| A-1 | `quote-card-edit` 未接归位机制 | 单元格失焦不触发价格归位（另两个时机——升版 / saveDraft——已接） | 已知缺口，交付备案（`PriceReconciler.java:56` 已如实标注） |
| A-2 | 存量单看不到元素单价徽标 | V366 之前建的单，冻结结构无「元素单价」字段 | 结构冻结语义使然，非 bug |

### B · 实现有瑕疵

| # | 缺口 | 风险 | 处置 |
|---|---|---|---|
| B-1 | `costing_card_values` 无并发保护，`row_version` 是死列 | 并发写核价卡片值可能互相覆盖 | 待评估 |
| B-2 | V366 未同步 `template.components_snapshot`（AP-39 类），且 `refresh-template-snapshots` 端点返 200 却未生效 | 存量模板 snapshot 残留旧字段 | 待评估 |
| B-3 | `COMP-0151` / `COMP-0154` 仍调旧函数 | 未归一到新取价路径 | 待评估 |
| B-4 | Excel 导出兜底读 `row_data` | 与冻结结构口径可能不一致 | 待评估 |
| B-5 | `snapshot_rows` 实际以 `component_id` 为 key（文档写 `componentDataId`） | AP-37「同 componentId 多实例」下 `ObjectNode.set` 后写覆盖前写，**快照静默丢行** | **现网查重 0 组**，本轮不动 |
| B-6 | `comparison_column_config.removable` 契约说谎 | `PRODUCT_TOTAL` 与其他列共存时回显 `true`，PUT 删除却被 400 拒绝；前端若按它渲染删除按钮，用户必然吃错误 | **待修**（后端 4 端点收工后处理） |
| B-7 | `GlobalExceptionMapper` 盲区 | `q.delete()` 不同步 flush，FK 检查逃逸到 commit 阶段 → 裸 500（BL-0108 已修主路径） | 部分已修 |

### C · 验证受限

| # | 事项 | 说明 |
|---|---|---|
| C-1 | E2E `quotation-flow.spec.ts` 本环境跑不完 | 多方独立确认超时（400s/180s），非本任务引入 |
| C-2 | `switchVersion` 仅 code-level 确认 | 未做运行时验证 |
| C-3 | `amt-002` / `amt-003` 故意留红 | 黄金用例中的已知不一致，非回归 |
| C-4 | 21 条 `sealed=t` 初版 ≠ 升版流程验证过 | 库里 `element_price_version` 为 0 行，说明升版跑过但版本/job 数据后被清；**不可作为升版已验证的证据** |
| C-5 | 调价运行时数据两库全空 | 四态等验证需手工 seed 四张表 |

---

## 4. 结案前置

- [ ] **合并前先 `git merge master`** —— 本分支缺 `V377`（`annual_discount_unify`，master 后落），共享库已跑过该迁移，不 merge 则 Flyway validate 挂
- [ ] 24 张被真实升版的单号写进备案
- [ ] 剩余验收跑完（47 条）
- [ ] B-6 修复
- [ ] 4 个补交端点验收（`#17` `#18` `#8` `#29`）
- [ ] `#42①` 手动行锁价修复验收

---

## 5. 交付过程中的方法论问题（技术总监自陈）

### 5.1 任务书三次同型遗漏

`api.md` 按**端点**组织、`backtask.md` 按**业务流程**组织、`fronttask.md` 按**屏**组织。屏↔端点对应直观 → **前端一处未漏**；业务流程↔端点无天然对应且**从未交叉核对** → 后端漏三批（R 版本快照 / 策略 CRUD / 4 个端点）。

**加剧因素**：`api.md §7`「契约自检清单（前后端联调前逐条对）」**自始至终无人执行**。前端照契约把屏 7 做全（类型 + 纯函数 + 4 条单测 + Drawer 消费），调的是不存在的端点 —— 本该在联调时 5 分钟暴露，拖到验收末期。

**下次的做法**：任务书产出后、开工前，**机械对账一遍 api.md 端点清单 vs 前后端任务块**，逐个打勾。这是纯机械劳动，成本极低，收益是本次三批遗漏全部可提前避免。

### 5.2 设计闸门有效

三次「先出清单再动手」的要求全部拦住了东西：S6 写回前的对拍清单 / 前端改公式引擎前的影响面清单 / 动共享渲染基础设施前的评估 —— **三次里两次证明若直接批准就是改错东西**。
