# task-260903 · 选配主数据迁至 ds_quote_* 新表体系

> **立项日期**：2026-09-03（`date +%y%m%d` 实取）
> **前置任务**：`task-260902-选配流程重构`（已交付，34 条 AC 全部有结论）
> **方案来源**：`落库方案`（artifact `ad594f86`），闸门 A0 已于 2026-09-03 裁决完毕
> **状态**：🟠 立项 · 闸门 A 待过

---

## 1. 为什么做

`task-260902` 把选配升到三层模型后，选配产出的主数据仍写在 **V6 旧表**（`material_master` / `material_bom` / `material_bom_item` / `element_bom` / `element_bom_item`）。

而「新料号数据规则」那条线已建成 `ds_quote_*` 报价侧新表体系（16 张，带 `version_no` + `row_fingerprint` + `_history` 版本化）。**用户裁决：选配改往新表落库，不再写 V6，且不做双写。**

不双写意味着**没有过渡期** —— 停写 V6 的那一刻，读 V6 的渲染链路就断。所以本任务必须同时做两件事，且**顺序不能反**。

---

## 2. 两阶段（B 先行，A 后到）

| 阶段 | 内容 | 为什么这个顺序 |
|---|---|---|
| **B · 渲染读取侧** | 建兼容视图；135 个组件 SQL + 3 个 PG 视图改读它 | 先让渲染层「两边都能读」，此时 V6 仍在写，**系统行为零变化**，可安全验证 |
| **A · 选配写入侧** | 选配改写 4 张新表；停写 V6 五表；退役 `sel_product_no` | B 落地后才切写入，切换瞬间渲染不断 |

🚫 **A 先于 B 上线 = 选配产品在报价单里渲染为空。** 这是本任务最重要的顺序约束。

---

## 3. 实测基线（2026-09-03，共享库 `cpq_db_0724`）

```
component_sql_view 引用 material_master    = 132
component_sql_view 引用 material_bom_item  =  89
component_sql_view 引用 element_bom_item   =  29
三者去重合计（本任务改造面）               = 135
PG 视图层引用                              =   3
  v_composite_child_materials / v_composite_child_processes / v_composite_child_elements
```

⚠️ **共享库会漂移**，开工前须重取。计数不写死进断言。

---

## 4. 验收标准（AC）

### 4.1 阶段 B · 渲染读取侧

| 编号 | 场景 | 操作 | 断言 |
|---|---|---|---|
| **B-AC-1** | 兼容视图建成后，V6 表数据不变 | 查兼容视图 | 返回行数与列值**与直接查 V6 表逐字相同**。<br>🚨 **2026-09-03 实测更正**：立项时我写的「此时新表侧为空」**是错的** —— `ds_quote_material` 已有 **42 行 IMPORT，且 42/42 与 `material_master` 料号重叠**（另一条线的导入写的）。裸 `UNION ALL` 会让这 42 个料号在 132 段 SQL 里**出双份行**。⇒ 兼容视图新表侧**必须带反连接**（`NOT EXISTS V6 同键行`），V6 侧优先、新表侧只补 V6 没有的。 |
| **B-AC-2** | 135 个组件 SQL 完成表名替换 | 快照 diff：改造前后各跑 750 段，逐单元格比对 | 每个页签的**行数**与**每个单元格的值**与改造前逐字相同；**行序允许变化**（见下）。<br>📌 **实测结论（2026-09-03，判通过）**：`逐字相同 596 / 有差异 154`，但逐段验证后 —— **132/132 段排序后内容逐字一致、删除行 0、行数 0 变化 ⇒ 差异 100% 是行序**。<br>🚨 **已知行为变更（用户裁决接受）**：131 段出现**不同内容的行换序**（不只是相同行互换）。根因：兼容视图的 `UNION ALL` + `JOIN` 改变了执行计划，而**全库 41/150 段本就没有 `ORDER BY`** —— SQL 不保证顺序，改造前的稳定顺序只是碰巧。有 `ORDER BY` 的段（`mc_view`/`wg_view`）也变序，那是**排序键 tie**（实测一段 16 行只有 5 个唯一首列值）。<br>⚠️ **用户会看见**：实测 `配 sortField 的组件 = 5`、`配 rowKeyFields = 0`、渲染层不重排 ⇒ 绝大多数组件直接按 SQL 返回顺序渲染，同一张报价单改造前后行的排列会不同（内容一字未变）。<br>📌 **未采纳的两条**：给兼容视图加确定性排序（顺序由实现者定，未必是用户习惯的）；给 41 段无 `ORDER BY` 的 SQL 补上（改用户配置的组件 SQL，面大风险高）。**「行序」在本系统从来不是被保证的契约，为它加约束等于凭空造一个新契约。** |
| **B-AC-3** | 3 个 PG 视图改造 | `SELECT * FROM v_composite_child_materials WHERE ...` | 对同一料号，返回结果与改造前**逐字相同** |
| **B-AC-4** | 列映射正确性 · characteristic | 在新表插一条 `output_material_type='ASSEMBLY'` 的测试行（事务内，用后回滚） | 兼容视图的 `characteristic` 列返回 `ASSEMBLY` |
| **B-AC-5** | 列映射正确性 · 材质名 | 同上，`input_material_no='00006'` | 兼容视图的 `component_usage_type` 返回 **`AgNi10`**（由 JOIN `material_recipe.symbol` 补出） |
| **B-AC-6** | 列映射正确性 · is_current | 新表行（无该列） | 兼容视图的 `is_current` 恒为 `true` |
| **B-AC-7** | 🚨 **性能不退化** | **对 134 段组件 SQL 逐段 `EXPLAIN(ANALYZE)`**，每段 5 次取中位，**按档位分别判** | **生产档（t1）合计劣化 ≤ 500ms**（绝对值口径，2026-09-03 用户裁决）。<br>🚨 **为什么不用百分比**：低基数上的百分比没有意义 —— `0.2ms → 1.4ms` 就是 +600%，但毫无影响。实测 t1 档 98 段 `167.8 → 268.2ms`（+60%），**绝对劣化仅 100ms**，用户感知不到。<br>📌 **实测结论（2026-09-03，判通过）**：t1 `167.8→268.2ms` ✅；t2 灵敏档 `12594→44573ms`（+254%）—— 但 t2 是为提高 diff 鉴别力人为把 `is_current` 置 TRUE、放出全部历史版本（24920 行）的**非生产档位**。<br>按结果集行数分桶：`1-100 行` 劣化 108ms · `1001-10000` 劣化 5.6ms · **`>10000 行` 劣化 31965ms（占 99.7%）**。 |
| **B-AC-8** | 🚨 **N+1 未引入** | **`POST /api/cpq/components/batch-expand`**（真正执行组件 SQL 的端点，🚫 不是「打开报价单」） | 表扫描次数增量与改造前**同量级**（按行循环时扫描数会随 N 涨）。<br>📌 `pg_stat_statements` 未安装（`shared_preload_libraries` 为空），装它要改共享环境全局配置 = §3.2 红线 ⇒ 用 `pg_stat_user_tables` 的扫描增量作代理指标 |

### 4.2 阶段 A · 选配写入侧

| 编号 | 场景 | 操作 | 断言 |
|---|---|---|---|
| **A-AC-1** | 选配新建产品 | 配一个零件（2 材质，占比 70/30）+ 1 外购件，提交 | ① `ds_quote_material` 落料号主档；② `ds_quote_material_bom` 落材质行，`material_ratio` 存满 12 位小数；③ `ds_quote_element_bom` 落元素含量；④ **`version_no` 全部 = 1** |
| **A-AC-2** | 🚨 **V6 五表零新增** | 同 A-AC-1，前后对比 | `material_master` / `material_bom` / `material_bom_item` / `element_bom` / `element_bom_item` **五张表行数不变**。<br>📌 本条是「不双写」的守卫 |
| **A-AC-3** | 客户产品编号 | 同 A-AC-1，输入编号 `T260903-A` | 落 `ds_quote_customer_part`（`customer_no` + `customer_product_no` + `material_no`）；🚫 **`sel_product_no` 零新增**（该表本任务退役） |
| **A-AC-4** | 指纹命中 → 纯引用 | 用**新编号**配一套**完全相同**的配置 | ① 复用原料号；② **`ds_quote_*` 四张表一行不写**（用户裁决：命中即引用）；③ `ds_quote_customer_part` 新增一行（一料号多编号） |
| **A-AC-5** | 🚨 **选配阶段不升版** | 连续三次配置**不同**产品 | 三个料号的 `version_no` **全部 = 1**，`_history` 表零新增。<br>📌 用户裁决：选配阶段不发生版本升级 |
| **A-AC-6** | 投影维度正确 | 配一个 COMPOSITE 产品（零件 + 外购件） | `ds_quote_material_bom` 里该父料号既有 `output_material_type='ASSEMBLY'` 行、也有 `='RECIPE'` 行（对齐 V6 双投影语义） |
| **A-AC-7** | 外购件身份 | 同 A-AC-6 | 外购件料号在 `ds_quote_material.material_type` = **`外购件`**（依赖 V409 落地） |
| **A-AC-8** | 🚨 **端到端渲染** | 完成 A-AC-1 后，打开该报价单 | 产品卡片**正常渲染出材质、占比、外购件**，🚫 不是空白、不是「加载中…」。<br>📌 本条是 A+B 合起来才成立的终局验证 |
| **A-AC-9** | 复用不走写入器 | A-AC-4 的场景 | 后端日志中 `VersionedGroupWriter` **零调用**（命中即早退） |
| **A-AC-10** | 边界 · 并发同编号 | 两个会话同时用同一客户产品编号提交 | 恰好一个成功、另一个返 409；`ds_quote_customer_part` 只有 1 行（`uq_ds_quote_customer_part` 保证） |

### 4.3 回归

| 编号 | 断言 |
|---|---|
| **R-1** | `task-260902` 的 33 条后端测试**全绿**（带真实鉴权） |
| **R-2** | 导入链路不受影响：跑一次 V6 报价数据导入，结果与改造前一致 |
| **R-3** | 核价侧渲染不受影响（本任务只动报价侧 `ds_quote_*`） |

---

## 5. 本期明确不做

1. **存量 V6 数据迁移到新表** —— 实测有 2 个料号存在真正的跨客户 BOM 差异（`3120011203` 在 `CUST-0002` 下是 `3110520422+00144`、在 `_GLOBAL_` 下是 `VS-RM01/02/05`，组成完全不同），而新表无 `customer_no` 只能存一份。归「新料号数据规则」那条线处理。
2. **「审批通过后回写基础数据并升版」** —— 实查核价审批 5 个类（`CostingOrderResource` / `CostingFreezeService` 等）回写调用 **0 处**，整条链路不存在。它还要回答「回写哪些字段 / 多张报价单引用同一料号时谁的编辑生效 / 回写失败算不算审批通过」，是独立任务。
3. **工序侧迁移** —— `quotation_line_process` / `unit_price` / `capacity` 仍走 V6。新体系的 `ds_quote_self_process_fee` 是「按料号导入的基础费率」，与「报价单行的工序单价」语义不同，硬套会错位。
4. **报价单单据层迁移** —— `quotation_line_item` 等新体系没有对应表。
5. **核价侧（`ds_cost_*`）** —— 本任务只动报价侧。

---

## 6. 前置依赖

| 依赖 | 状态 | 影响 |
|---|---|---|
| **V409 迁移落地**（`ds_quote_material` 加 `material_type`） | 🚩 **未落地**，实测 `flyway max = V408`、该列 0 列 | A-AC-7 阻塞。V409 落地前不得写 `material_type` |
| `output_material_type` 保留 | ✅ 已确认不在 V409 删除名单 | 投影维度可用 |
| 新表那条线的 Excel 改造完成 | 🚩 进行中 | V409 合并的前置 |
| **`QuotePendingRewriter` 白名单需加 `v_compat_*`** | 🚩 **B-3 的前置，实测发现** | 该改写器靠**白名单表 token** 做四步改写（表替换 / 遮蔽 / 锚点注入 `__v6_id` / 库函数补参）。实测 **128/150 段命中白名单，其中 48 段只靠 `material_bom_item`/`element_bom_item` 命中** —— 改表名后锚点全丢，而 `anchorInjected=false` 是它的**安全降级设计、不报错**（`QuoteViewValidationService` 返回「不适用」）⇒ **48 段静默失去 pending 与回填能力**。<br>⚠️ 这是在碰 `task-0721` 的核心机制，白名单加 3 项本身很小但影响面是「所有报价单的 pending 版本升级」，须单独提交、单独验收 |

---

## 7. 关键风险

| 风险 | 说明 | 缓解 |
|---|---|---|
| 🔴 **改的是渲染主链路** | 135 个组件 SQL 属 `docs/三大核心模块基线.md` 明列的核心契约 | B 阶段先上、V6 仍在写，此时系统行为零变化，可充分验证后再切 A |
| 🟡 **性能（已实测定性，判通过）** | 根因：**PG 无法穿过 `UNION ALL` 做列裁剪**。实测同一段 `material_bom_item` 扫描 `width=28 → 1987`（原 SQL 只要几列，兼容视图把 58 列全物化）⇒ **每行搬运量 70 倍**。小结果集无感（单段 13.6→16.8ms），24920 行则灾难 | 生产档绝对劣化 100ms，判通过；**登记为技术债 BL**：代价随数据量**线性放大**，共享库现在 `is_current` 数据少所以看不出，数据长起来会变真问题 |
| 🟡 **无过渡期** | 用户裁决不双写，A 上线即停写 V6 | A、B 必须同一次发布；回退方案 = 回退 A（B 可独立留存） |
| 🟡 **跨会话协调** | 依赖另一条线的 V409 | 已建立沟通；开工前重新确认 |
