id,sql_template
01122940-d931-407c-b5df-ad73e57185d2,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
03855937-b3f5-4e65-9a01-27848eb1277b,"-- 其他费用(成品其他费用; price_type='FINISHED_MATERIAL_OTHER'; 料号在 code 列)
-- 本页签是「本成品自身的费用项」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  up.code AS hf_part_no,
  up.code AS _销售料号,
  up.seq_no AS _项次,
  up.cost_type AS _类别,
  up.pricing_price AS _费用,
  up.unit AS _单位
FROM unit_price up
WHERE up.system_type = 'QUOTE' AND up.price_type = 'FINISHED_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no"
05266048-6195-473e-87b8-c0af966b3730,"-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  mbi.scrap_rate AS _损耗率
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
0659329e-7766-4211-8db6-4849bd944a4c,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
06962c8d-414c-4c2c-ac71-90d73f85f733,"-- ============================================================================
-- 组件：物料（施耐德-v3）   SQL 视图名：bom_view   驱动路径：$bom_view
-- requiredVariables: [""total_material_no""]   scope: COMPONENT
-- 页签类型 tabType: BOM   partNameField: 料件   rowKeyFields: [""料件""]
--
-- 2026-07-31 修订：新增 `_材料占比`（material_bom_item.material_ratio，V365 新增列），
--                  UNION ALL 根分支同步补 NULL::numeric 占位。其余逐字未动。
--                  规则见 dev-docs/rule-0724-组件模板配置/报价侧.md §7.2.2。
--
-- 【改这个文件后怎么生效】本文件是「可编辑副本」，不是运行时来源。改完二选一：
--   ① UI：组件管理 →「物料」组件 → SQL 视图 → 粘贴保存（保存即 dry-run 校验）
--   ② API：PUT /api/cpq/components/{componentId}/sql-views/{viewId}
--          body: {""sqlViewName"":""bom_view"",""sqlTemplate"":""<本文件内容>"",
--                 ""scope"":""COMPONENT"",""requiredVariables"":[""total_material_no""]}
--      （componentId/viewId 在你导入 bundle 后才确定；GET /components?directoryId=<你的目录> 查）
--   改完记得把 bundle JSON 也重导一次，否则 JSON 与实际配置漂移。
--
-- 【改之前必看两条坑】
--   1. 本视图是 UNION ALL 两分支（主分支=BOM 边 / 根分支=无父边的成品自身）。
--      **加删任何一列，两个分支都要同步改**，根分支用 NULL::<类型> 占位。列数不等 PG 直接报错。
--   2. 验证别靠肉眼数列。直连 PG 跑一次最可靠（把命名参数换成字面量）：
--      sed 's/:total_material_no/ARRAY[\'S-80011\']::text[]/g' 本文件 | psql -h 10.177.152.12 -U postgres -d cpq_db_0724 -f -
-- ============================================================================
-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  -- 【产出类型】= material_bom_item.component_usage_type（v2 由「材料成本」移入本页签）。
  --   写入路径已核实：MaterialBomMergeHandler:148 ← Excel「产出料号类型」(另有 P06/ConfigureProductService)。
  --   ⚠️ 同名列陷阱(§3.5)：element_bom_item 上的同名列 0 写点、恒 NULL，只有本表这列有数据。
  mbi.component_usage_type AS _产出类型,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  -- 【材料占比】= material_bom_item.material_ratio（V365 新增列，2026-07-31）。
  --   写入路径已核实：MaterialBomMergeHandler ← 报价侧「物料BOM」sheet 可选列「材质占比」。
  --   仅材质行(characteristic='RECIPE')有值——零件/外购件行 handler 显式置 NULL，故本树页签的
  --   材质边出数、组装边留空，属预期。小数口径(0.3=30%)。
  --   ⚠️ 本页签 FROM material_bom_item 可直接绑；若页签主表是 element_bom_item(如 $mc_view)，
  --      该表**没有**这个列，必须走跨表标量子查询(报价侧.md §7.2.2)。
  mbi.material_ratio AS _材料占比,
  mbi.scrap_rate AS _损耗率,
  -- 【回收价格/回收比例】= 来料回收折扣(unit_price price_type='INCOMING_MATERIAL_RECYCLE')。
  --   落库口径见 Q09IncomingRecoveryHandler：groupKey=(code=投入料号, finished_material_no=成品料号)，
  --   content=[seq_no=项次, cost_ratio=回收折扣%, pricing_price=值, currency, unit]（task-0730 扩列后）。
  --   匹配维度 =【投入料号 + 直接父件】(2026-07-31 用户拍板)：code=本行子件、finished=本行父件。
  --   ⚠️ 已知取舍：三层以上 BOM 中孙件那行取不到值(它的父不是成品)。
  -- ⚠️ unit_price 是版本化表 → 禁写进带 is_current 的 LEFT JOIN(§4 硬约束②：参数错位、dry-run 照过、
  --    只在真实渲染炸)，故用相关标量子查询。
  -- ⚠️ 树契约(§5.6)不注入 :customerCode，本子查询靠 (成品 + 投入料号) 两维定位。
  -- ⚠️ LIMIT 1：task-0730 起同一 (成品,投入料号) 可按项次存多行，取项次最小的一条。
  (SELECT r.pricing_price FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收价格,
  (SELECT r.cost_ratio FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收比例
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::varchar,                                  -- 产出类型
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric, NULL::numeric,   -- 单位 / 材料占比 / 损耗率
  NULL::numeric, NULL::numeric                    -- 根分支(成品自身)无回收折扣
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)
"
0740e16a-c827-48e0-a807-e3af1a44f37f,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
09eb6429-ace6-4246-9f60-c74b327df939,"-- 其他费用(成品其他费用; price_type='FINISHED_MATERIAL_OTHER'; 料号在 code 列)
-- 本页签是「本成品自身的费用项」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  up.code AS hf_part_no,
  up.code AS _销售料号,
  up.seq_no AS _项次,
  up.cost_type AS _类别,
  up.pricing_price AS _费用,
  up.unit AS _单位
FROM unit_price up
WHERE up.system_type = 'QUOTE' AND up.price_type = 'FINISHED_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no"
0af85e5b-897a-45f8-b59a-1375211ea7e9,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  up.operation_no   as operation_no,
  pm.process_name   as process_name,
  up.pricing_price  as pricing_price,
  up.currency       as currency,
  up.unit           as unit,
  up.defect_rate    as defect_rate
from unit_price up
left join process_master pm on pm.process_no = up.operation_no
where up.system_type = 'PRICING'
  and up.price_type = 'SELF_PROCESS'
  and :versionFilter(up.is_current, up.version_no, up.code)
  and up.code = ANY(:total_material_no)
"
0cbd5249-a59f-448b-bd71-b599c35450c7,"-- 外购件成本(外购件, 平铺契约; 料号列=组成件料号 component_no(料号铁律); characteristic='OUTSOURCED')
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件下挂的外购件也进入本页签。
-- ⚠️ 铁律1: 白名单表(material_bom_item)必须在顶层 FROM（详见 mc_view 注释）。
-- ⚠️ 铁律2: LEFT JOIN + COALESCE 兜底无 BOM 的单料产品。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION ALL
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.component_no AS _料号,
  COALESCE(mm.material_name, mr.name) AS _外购件名称,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位,
  mbi.material_no AS _归属料号
FROM material_bom_item mbi
  LEFT JOIN bom_closure cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no"
0e4ba6c9-4820-4d3e-a8ff-a35dfe0e487e,"-- 物料（BOM 树页签；树契约：material_no=子 / parent_no=父 + :total_material_no；边式全子件 + 根分支）
-- 走 Pass2 树渲染（BomTreeRenderService, usage=QUOTE），禁用 :customerCode 平铺契约（报价侧.md §5.6）。
-- ⚠️ 加删任何一列，UNION ALL 两个分支都要同步改，根分支用 NULL::<类型> 占位；列数不等 PG 直接报错。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  -- 【产出类型】= material_bom_item.component_usage_type ← Excel「产出料号类型/产出类型」。
  --   ⚠️ 同名列陷阱：element_bom_item 上的同名列 0 写点、恒 NULL，只有本表这列有数据（§7.2.1）。
  mbi.component_usage_type AS _产出类型,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  -- 【材料占比】= material_bom_item.material_ratio（V365）← Excel「材质占比/材料占比」。
  --   本页签 FROM material_bom_item 可直接绑；材质边出数、组装边留空属预期（报价侧.md §7.2.2）。
  mbi.material_ratio AS _材料占比,
  mbi.scrap_rate AS _损耗率,
  mbi.defect_rate AS _不良率,
  -- 【回收价格/回收比例】= 来料回收折扣（unit_price price_type='INCOMING_MATERIAL_RECYCLE'）。
  --   Q09IncomingRecoveryHandler：groupKey=(code=投入料号, finished_material_no=成品料号)，
  --   content=[seq_no=项次, cost_ratio=回收折扣%, pricing_price=值, currency, unit]。
  --   匹配维度 =【投入料号 + 直接父件】：code=本行子件、finished=本行父件。
  --   ⚠️ 已知取舍：三层以上 BOM 中孙件那行取不到值（它的父不是成品）。
  -- ⚠️ unit_price 是版本化表 → 禁写进带 is_current 的 LEFT JOIN（§3.6：参数错位、dry-run 照过、
  --    只在真实渲染炸），故用相关标量子查询。树契约不注入 :customerCode，靠(成品+投入料号)两维定位。
  (SELECT r.pricing_price FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收价格,
  (SELECT r.cost_ratio FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收比例
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身（树根）
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::varchar,                                 -- 产出类型
  NULL::numeric, NULL::numeric, NULL::numeric,   -- 组成数量 / 材料毛重 / 材料净重
  NULL::varchar,                                 -- 单位
  NULL::numeric, NULL::numeric, NULL::numeric,   -- 材料占比 / 损耗率 / 不良率
  NULL::numeric, NULL::numeric                   -- 根分支（成品自身）无回收折扣
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
0f95f6cf-d8eb-448b-8ce3-8c3810b8c997,"-- 加工费(零件, 平铺契约; 料号列=零件料号 up.code(料号铁律); 工序=process_master.process_name)
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件自身的加工费也进入本页签。
-- ⚠️ 铁律1: 白名单表(unit_price)必须在顶层 FROM（详见 mc_view 注释）。
-- ⚠️ 铁律2: LEFT JOIN + COALESCE 兜底无 BOM 的单料产品。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION ALL
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.code AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位,
  up.finished_material_no AS _归属料号
FROM unit_price up
  LEFT JOIN bom_closure cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
104be544-fb93-4b0a-a1d8-56d7f96aa536,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  mm.material_name  as material_name,
  mm.specification  as specification,
  mm.dimension      as dimension,
  up.seq_no         as seq_no,
  up.cost_type      as cost_type,
  up.pricing_price  as pricing_price,
  up.currency       as currency,
  up.unit           as unit
from unit_price up
left join material_master mm on mm.material_no = up.code
where up.system_type = 'PRICING'
  and up.price_type = 'FINISHED_OTHER'
  and up.cost_ratio is null
  and :versionFilter(up.is_current, up.version_no, up.code)
order by up.seq_no"
1095bcc1-82ee-4236-8a45-17b9776ea7dc,"-- 来料其他费用(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_OTHER')
-- 落库口径见 Q07IncomingOtherFeeHandler:
--   groupKey=(cost_type=要素名称, code=投入料号, finished_material_no=销售料号)
--   content =[seq_no=项次, pricing_price=值, cost_ratio=比例, currency=货币, unit=计价单位]
-- 「销售料号」= 所属成品(up.finished_material_no)；「料号/料件」= 投入料件(up.code 及其名称)。
-- 模板未勾「料号列」→ partNoField 留空(§3.4)；tabType 空属费用类，不受""料号/名称至少一个""约束。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的来料其他费用也进入本页签
--   (§3.8.1 判据: 本页签是整个产品结构的成本构成, 与兄弟页签「来料固定加工费」同族)。
-- ⚠️ 铁律1(§3.8.3): 白名单表 unit_price 必须在顶层 FROM，不能写成 FROM bom_closure JOIN unit_price，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
-- ⚠️ 铁律3: 递归用 UNION(非 UNION ALL) + bom_closure_d 对 (root,node) 去重，否则行成倍复制、页签小计翻倍。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code                 AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no               AS _项次,
  up.cost_type            AS _要素,
  up.pricing_price        AS _费用,
  up.cost_ratio           AS _比例,
  up.unit                 AS _计价单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
1113d143-9e94-420a-99bb-31c0bbdd31ff,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
1180bdb0-3657-4f38-a147-f203d461d627,"-- ============================================================================
-- 组件：材料成本（施耐德-1）   SQL 视图名：mc_view   驱动路径：$mc_view
-- requiredVariables: [""customerCode"", ""priceBaseDate""]   scope: COMPONENT
--
-- 2026-07-28 修订：只改 `_产出类型` 一处（原绑 ebi.component_usage_type 恒为空），
--                  其余逐字未动。详见 dev-docs/rule-0724-组件模板配置/报价侧.md §7.2.1。
-- ============================================================================
-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  -- 「产出类型」= 物料BOM 的「产出料号类型」(MaterialBomMergeHandler:148 → material_bom_item.component_usage_type)。
  -- ⚠️ 不在 element_bom_item 上：该表的同名列无任何写入路径，绑它恒为空。
  -- ⚠️ 必须用相关标量子查询而非 LEFT JOIN：material_bom_item 是版本化表(§3.6)，
  --    且同一对父子常并存 RECIPE+ASSEMBLY 多条边(§3.8.3 铁律3) → JOIN 会让材料成本行倍增。
  (SELECT t.component_usage_type
     FROM material_bom_item t
    WHERE t.system_type  = 'QUOTE' AND t.is_current
      AND t.customer_no  = :customerCode
      AND t.material_no  = ebi.material_no
      AND t.component_no = ebi.material_part_no
    ORDER BY CASE WHEN t.characteristic = 'RECIPE' THEN 0 ELSE 1 END, t.characteristic
    LIMIT 1) AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
1503a3e1-2870-474d-b44c-9bb5ce8f11b5,"-- ============================================================================
-- 组件：材料成本（施耐德-1）   SQL 视图名：mc_view   驱动路径：$mc_view
-- requiredVariables: [""customerCode"", ""priceBaseDate""]   scope: COMPONENT
--
-- 2026-07-28 修订：只改 `_产出类型` 一处（原绑 ebi.component_usage_type 恒为空），
--                  其余逐字未动。详见 dev-docs/rule-0724-组件模板配置/报价侧.md §7.2.1。
-- ============================================================================
-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  -- 「产出类型」= 物料BOM 的「产出料号类型」(MaterialBomMergeHandler:148 → material_bom_item.component_usage_type)。
  -- ⚠️ 不在 element_bom_item 上：该表的同名列无任何写入路径，绑它恒为空。
  -- ⚠️ 必须用相关标量子查询而非 LEFT JOIN：material_bom_item 是版本化表(§3.6)，
  --    且同一对父子常并存 RECIPE+ASSEMBLY 多条边(§3.8.3 铁律3) → JOIN 会让材料成本行倍增。
  (SELECT t.component_usage_type
     FROM material_bom_item t
    WHERE t.system_type  = 'QUOTE' AND t.is_current
      AND t.customer_no  = :customerCode
      AND t.material_no  = ebi.material_no
      AND t.component_no = ebi.material_part_no
    ORDER BY CASE WHEN t.characteristic = 'RECIPE' THEN 0 ELSE 1 END, t.characteristic
    LIMIT 1) AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
16d7c6ae-0a63-4583-bc1f-e104f2b011f2,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
19e15f21-0446-4e61-a724-da524e470f54,"select
  up.finished_material_no as material_no,
  up.finished_material_no as hf_part_no,
  up.version_no           as view_version,
  up.production_no        as production_no,
  up.code                 as incoming_material_no,
  coalesce(mi.material_name, mr.name) as material_name,
  mi.specification        as specification,
  mi.dimension            as dimension,
  up.seq_no               as seq_no,
  up.cost_type            as cost_type,
  up.cost_ratio           as cost_ratio
from unit_price up
left join material_master mi on mi.material_no = up.code
left join material_recipe mr on mr.code = up.code
where up.system_type = 'PRICING'
  and up.price_type = 'INCOMING_OTHER'
  and up.cost_ratio is not null
  and :versionFilter(up.is_current, up.version_no, up.finished_material_no)
order by up.seq_no"
1ae89d0f-c5a8-48c5-94ba-94bd67b1e549,"-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  ebi.component_usage_type AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no"
1c34bc4f-344f-4cef-b28a-4dd443d65498,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
1d76aaee-5252-4753-b8df-e60fa4687167,"-- 外购件成本(外购件, 平铺契约; component_no 只作取名 JOIN 键, 不出可见列)
SELECT
  mbi.material_no AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _组成件名称,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY mbi.material_no, mbi.seq_no"
1dea38d1-2d76-425f-9973-0719e344fdc4,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
22c40821-ac46-4f76-b3d4-04cd3de97da1,"-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  mbi.scrap_rate AS _损耗率
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
22efecaf-a805-4223-9a20-6bb72ec2f7d4,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
23fba63e-c729-4cbb-99fd-383f9f7a6ab5,"-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  ebi.component_usage_type AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no"
24fafcad-1ad6-4999-afe4-b177438e253d,"select
  pe.material_no      as material_no,
  pe.material_no      as hf_part_no,
  pe.calc_version     as view_version,
  pe.production_no    as production_no,
  coalesce(pe.material_name, mm.material_name)  as material_name,
  coalesce(pe.specification, mm.specification)  as specification,
  coalesce(pe.dimension, mm.dimension)          as dimension,
  pe.process_no       as process_no,
  coalesce(pm.process_name, pe.process_name)    as process_name,
  pe.unit_price       as unit_price,
  pe.currency         as currency,
  pe.unit             as unit
from production_energy pe
left join material_master mm on mm.material_no = pe.material_no
left join process_master pm on pm.process_no = pe.process_no
where pe.system_type = 'PRICING'
  and pe.price_type = 'DEPRECIATION'
  and :versionFilter(pe.is_current, pe.calc_version, pe.material_no)"
276a68c5-9f5e-4050-b31d-81c3654bf4dc,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
287bd627-f58c-4d2b-8f01-b12e14145589,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  up.operation_no   as operation_no,
  pm.process_name   as process_name,
  up.pricing_price  as pricing_price,
  up.currency       as currency,
  up.unit           as unit
from unit_price up
left join process_master pm on pm.process_no = up.operation_no
where up.system_type = 'PRICING'
  and up.price_type = 'OUTSOURCE_PROCESS'
  and :versionFilter(up.is_current, up.version_no, up.code)"
294f353e-70cb-4563-ab62-cd6d6552d7bd,"-- 来料其他费用(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_OTHER')
-- 落库口径见 Q07IncomingOtherFeeHandler:
--   groupKey=(cost_type=要素名称, code=投入料号, finished_material_no=销售料号)
--   content =[seq_no=项次, pricing_price=值, cost_ratio=比例, currency=货币, unit=计价单位]
-- 「销售料号」= 所属成品(up.finished_material_no)；「料号/料件」= 投入料件(up.code 及其名称)。
-- 模板未勾「料号列」→ partNoField 留空(§3.4)；tabType 空属费用类，不受""料号/名称至少一个""约束。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的来料其他费用也进入本页签
--   (§3.8.1 判据: 本页签是整个产品结构的成本构成, 与兄弟页签「来料固定加工费」同族)。
-- ⚠️ 铁律1(§3.8.3): 白名单表 unit_price 必须在顶层 FROM，不能写成 FROM bom_closure JOIN unit_price，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
-- ⚠️ 铁律3: 递归用 UNION(非 UNION ALL) + bom_closure_d 对 (root,node) 去重，否则行成倍复制、页签小计翻倍。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code                 AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no               AS _项次,
  up.cost_type            AS _要素,
  up.pricing_price        AS _费用,
  up.cost_ratio           AS _比例,
  up.unit                 AS _计价单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
2ca97604-5b07-4613-9c32-86f59fbc7fb1,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
2cef58ee-6def-4720-a91e-c5a34a7e1466,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
30de1ae4-9820-4c87-9149-20efccb73302,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  up.operation_no   as operation_no,
  pm.process_name   as process_name,
  up.pricing_price  as pricing_price,
  up.currency       as currency,
  up.unit           as unit,
  up.defect_rate    as defect_rate
from unit_price up
left join process_master pm on pm.process_no = up.operation_no
where up.system_type = 'PRICING'
  and up.price_type = 'SELF_PROCESS'
  and :versionFilter(up.is_current, up.version_no, up.code)
  and up.code = ANY(:total_material_no)
"
3657cfb6-16c9-4c6a-af6a-848fc4be53b9,"-- 其他费用（成品其他费用；price_type='FINISHED_MATERIAL_OTHER'；料号在 code 列）
-- 本页签是「本成品自身的费用项」→ 不做 BOM 闭包（§3.8.1 判据）。
-- ⚠️ 现网口径：固定金额项写 pricing_price，按比例登记的项（材料管理费/利润/税率…）值在 cost_ratio、
--    pricing_price 为空 —— 故「费用」「比例」两列并存，各取各的，不互相兜底。
SELECT
  up.code AS hf_part_no,
  up.code AS _销售料号,
  up.seq_no AS _项次,
  up.cost_type AS _要素,
  up.pricing_price AS _费用,
  up.cost_ratio AS _比例,
  up.unit AS _单位
FROM unit_price up
WHERE up.system_type = 'QUOTE' AND up.price_type = 'FINISHED_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no"
3782deba-5ec3-4a03-960e-a5c53ae4d2e7,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
38633460-fa3b-45b9-86ec-fd4c8e007ea7,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
39dd9f01-730f-44db-b2e8-a8462af89570,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
3a5b6d48-5874-4964-ba41-019495c50921,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  mm.material_name  as material_name,
  mm.specification  as specification,
  mm.dimension      as dimension,
  up.seq_no         as seq_no,
  up.cost_type      as cost_type,
  up.cost_ratio     as cost_ratio
from unit_price up
left join material_master mm on mm.material_no = up.code
where up.system_type = 'PRICING'
  and up.price_type = 'FINISHED_OTHER'
  and up.cost_ratio is not null
  and :versionFilter(up.is_current, up.version_no, up.code)
order by up.seq_no"
3b5ef529-e047-43bb-84a2-ac95805dc78d,"-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  ebi.component_usage_type AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_material_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
         AND cep.material_no = COALESCE(cl.root_no, ebi.material_no)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
3bbb89de-5582-49ec-9807-74d7c440553d,"select
  mbt.material_no        as parent_no,
  mbt.component_no       as material_no,
  mbt.component_no       as hf_part_no,
  mbt.bom_version        as view_version,
  mbt.production_no      as production_no,
  mbt.seq_no             as seq_no,
  coalesce(mm.material_name, mr.name) as material_name,
  mm.specification       as specification,
  mm.dimension           as dimension,
  mbt.operation_no       as operation_no,
  pm.process_name        as process_name,
  mbt.feature_mgmt       as feature_mgmt,
  mbt.composition_qty    as composition_qty,
  mbt.issue_unit         as issue_unit,
  mbt.base_qty           as base_qty,
  mbt.issue_unit         as base_unit,
  mbt.scrap_rate         as scrap_rate,
  mbt.fixed_scrap        as fixed_scrap,
  mbt.defect_rate        as defect_rate,
  mbt.calc_type          as calc_type,
  mm.unit_weight         as unit_weight
from material_bom_item mbt
left join material_master mm on mm.material_no = mbt.component_no
left join material_recipe mr on mr.code = mbt.component_no
left join process_master pm on pm.process_no = mbt.operation_no
where mbt.system_type = 'PRICING'
  and mbt.customer_no = '_GLOBAL_'
  and :versionFilter(mbt.is_current, mbt.bom_version, mbt.material_no)
  and mbt.component_no = ANY(:total_material_no)
order by mbt.seq_no
"
3c80d767-37f4-42d3-bef7-758d39746251,"-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  mbi.scrap_rate AS _损耗率
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
3f0b908d-4645-44a4-8bb8-d0549d21f829,"-- BOM 树页签(树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.scrap_rate AS _损耗率,
  mbi.net_weight AS _净重,
  mbi.rough_weight AS _毛重,
  mbi.weight_unit AS _用量单位,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric, mm.standard_unit
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
3f706b7b-06b3-4371-b03a-56886b73a572,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名 JOIN 键(料号铁律 §3.4)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no"
3f86a092-52b4-4ccd-a593-9cfd0bc06f8d,"-- 加工费(零件, 平铺契约; 料号=up.code 零件料号, hf_part_no=finished_material_no 按成品收窄; 工序=process_master.process_name)
SELECT
  up.finished_material_no AS hf_part_no,
  up.code AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no"
4062da36-19b2-421a-9a5d-c8b7e0638f8a,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
41693c8b-cf13-4e8e-b299-4389e51611b1,"-- ============================================================================
-- 组件：物料（施耐德-v3）   SQL 视图名：bom_view   驱动路径：$bom_view
-- requiredVariables: [""total_material_no""]   scope: COMPONENT
-- 页签类型 tabType: BOM   partNameField: 料件   rowKeyFields: [""料件""]
--
-- 2026-07-31 修订：新增 `_材料占比`（material_bom_item.material_ratio，V365 新增列），
--                  UNION ALL 根分支同步补 NULL::numeric 占位。其余逐字未动。
--                  规则见 dev-docs/rule-0724-组件模板配置/报价侧.md §7.2.2。
--
-- 【改这个文件后怎么生效】本文件是「可编辑副本」，不是运行时来源。改完二选一：
--   ① UI：组件管理 →「物料」组件 → SQL 视图 → 粘贴保存（保存即 dry-run 校验）
--   ② API：PUT /api/cpq/components/{componentId}/sql-views/{viewId}
--          body: {""sqlViewName"":""bom_view"",""sqlTemplate"":""<本文件内容>"",
--                 ""scope"":""COMPONENT"",""requiredVariables"":[""total_material_no""]}
--      （componentId/viewId 在你导入 bundle 后才确定；GET /components?directoryId=<你的目录> 查）
--   改完记得把 bundle JSON 也重导一次，否则 JSON 与实际配置漂移。
--
-- 【改之前必看两条坑】
--   1. 本视图是 UNION ALL 两分支（主分支=BOM 边 / 根分支=无父边的成品自身）。
--      **加删任何一列，两个分支都要同步改**，根分支用 NULL::<类型> 占位。列数不等 PG 直接报错。
--   2. 验证别靠肉眼数列。直连 PG 跑一次最可靠（把命名参数换成字面量）：
--      sed 's/:total_material_no/ARRAY[\'S-80011\']::text[]/g' 本文件 | psql -h 10.177.152.12 -U postgres -d cpq_db_0724 -f -
-- ============================================================================
-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  -- 【产出类型】= material_bom_item.component_usage_type（v2 由「材料成本」移入本页签）。
  --   写入路径已核实：MaterialBomMergeHandler:148 ← Excel「产出料号类型」(另有 P06/ConfigureProductService)。
  --   ⚠️ 同名列陷阱(§3.5)：element_bom_item 上的同名列 0 写点、恒 NULL，只有本表这列有数据。
  mbi.component_usage_type AS _产出类型,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  -- 【材料占比】= material_bom_item.material_ratio（V365 新增列，2026-07-31）。
  --   写入路径已核实：MaterialBomMergeHandler ← 报价侧「物料BOM」sheet 可选列「材质占比」。
  --   仅材质行(characteristic='RECIPE')有值——零件/外购件行 handler 显式置 NULL，故本树页签的
  --   材质边出数、组装边留空，属预期。小数口径(0.3=30%)。
  --   ⚠️ 本页签 FROM material_bom_item 可直接绑；若页签主表是 element_bom_item(如 $mc_view)，
  --      该表**没有**这个列，必须走跨表标量子查询(报价侧.md §7.2.2)。
  mbi.material_ratio AS _材料占比,
  mbi.scrap_rate AS _损耗率,
  -- 【回收价格/回收比例】= 来料回收折扣(unit_price price_type='INCOMING_MATERIAL_RECYCLE')。
  --   落库口径见 Q09IncomingRecoveryHandler：groupKey=(code=投入料号, finished_material_no=成品料号)，
  --   content=[seq_no=项次, cost_ratio=回收折扣%, pricing_price=值, currency, unit]（task-0730 扩列后）。
  --   匹配维度 =【投入料号 + 直接父件】(2026-07-31 用户拍板)：code=本行子件、finished=本行父件。
  --   ⚠️ 已知取舍：三层以上 BOM 中孙件那行取不到值(它的父不是成品)。
  -- ⚠️ unit_price 是版本化表 → 禁写进带 is_current 的 LEFT JOIN(§4 硬约束②：参数错位、dry-run 照过、
  --    只在真实渲染炸)，故用相关标量子查询。
  -- ⚠️ 树契约(§5.6)不注入 :customerCode，本子查询靠 (成品 + 投入料号) 两维定位。
  -- ⚠️ LIMIT 1：task-0730 起同一 (成品,投入料号) 可按项次存多行，取项次最小的一条。
  (SELECT r.pricing_price FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收价格,
  (SELECT r.cost_ratio FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收比例
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::varchar,                                  -- 产出类型
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric, NULL::numeric,   -- 单位 / 材料占比 / 损耗率
  NULL::numeric, NULL::numeric                    -- 根分支(成品自身)无回收折扣
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)
"
416a97cf-cbf2-464f-aac5-a9379f5ae34f,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名/取价 JOIN 键(料号铁律 §3.4)。
--
-- 【要素/费用/单位/货币】= 组成件其他费用(unit_price price_type='COMPONENT_OTHER')，2026-07-27 用户拍板。
--   ⚠️ 匹配维度必须是【闭包根(成品) × 组成件】，不是【直接父件 × 组成件】：
--      Q13ComponentOtherFeeHandler 的 groupKey =(cost_type=要素名称, code=组成件料号,
--      finished_material_no=**销售料号/成品料号**, operation_no, supplier_no)，费用登记在成品维度。
--      实测 W-1001 同时直挂成品 S-3120014539(数量2) 与挂在子装配 S-80011 下(数量3)；若按
--      mbi.material_no(直接父件) 匹配，后者取不到价。故 ON 用 COALESCE(cl.root_no, mbi.material_no)。
--   注：「单位」= 计价单位(up.unit)，与已有的「组成单位」(mbi.issue_unit = BOM 发料单位) 语义不同，勿混。
--
-- ⚠️ §3.6 pending 改写坑：版本化表(unit_price)**禁**写进带 is_current 的 LEFT JOIN ON(参数错位，
--    dry-run 照过、只在真实渲染炸)。此处改用【派生表】——is_current 落在子查询 WHERE(文档明列的安全位置)，
--    JOIN ON 内不含 is_current。为什么不用文档推荐的标量子查询：cost_type 是 groupKey 的一维，
--    同一组成件可挂多条费用要素，必须真连接产生多行；行键已含「要素」防撞键。
--
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件下挂的外购件也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
--          (本视图主位表 = 顶层 FROM 的 material_bom_item mbi；派生表里的 unit_price 在 depth>0，不夺主位。)
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位,
  co.cost_type AS _要素,
  co.pricing_price AS _费用,
  co.unit AS _单位,
  co.currency AS _货币
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
  LEFT JOIN (
    SELECT up.code, up.finished_material_no, up.cost_type,
           up.pricing_price, up.currency, up.unit
    FROM unit_price up
    WHERE up.system_type = 'QUOTE' AND up.price_type = 'COMPONENT_OTHER' AND up.is_current
      AND up.customer_no = :customerCode
  ) co ON co.code = mbi.component_no
      AND co.finished_material_no = COALESCE(cl.root_no, mbi.material_no)
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no
"
421a9d55-27a7-4402-b565-1ed69f04dda3,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名 JOIN 键(料号铁律 §3.4)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no"
438baa89-f567-4502-9f9b-75be2ebb19fa,"-- ============================================================================
-- 组件：材料成本（施耐德-1）   SQL 视图名：mc_view   驱动路径：$mc_view
-- requiredVariables: [""customerCode"", ""priceBaseDate""]   scope: COMPONENT
--
-- 2026-07-28 修订：只改 `_产出类型` 一处（原绑 ebi.component_usage_type 恒为空），
--                  其余逐字未动。详见 dev-docs/rule-0724-组件模板配置/报价侧.md §7.2.1。
-- ============================================================================
-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  -- 「产出类型」= 物料BOM 的「产出料号类型」(MaterialBomMergeHandler:148 → material_bom_item.component_usage_type)。
  -- ⚠️ 不在 element_bom_item 上：该表的同名列无任何写入路径，绑它恒为空。
  -- ⚠️ 必须用相关标量子查询而非 LEFT JOIN：material_bom_item 是版本化表(§3.6)，
  --    且同一对父子常并存 RECIPE+ASSEMBLY 多条边(§3.8.3 铁律3) → JOIN 会让材料成本行倍增。
  (SELECT t.component_usage_type
     FROM material_bom_item t
    WHERE t.system_type  = 'QUOTE' AND t.is_current
      AND t.customer_no  = :customerCode
      AND t.material_no  = ebi.material_no
      AND t.component_no = ebi.material_part_no
    ORDER BY CASE WHEN t.characteristic = 'RECIPE' THEN 0 ELSE 1 END, t.characteristic
    LIMIT 1) AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_material_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
         AND cep.material_no = COALESCE(cl.root_no, ebi.material_no)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
43cc1b6d-499e-45d2-b84d-4cb851721bb6,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  mm.material_name  as material_name,
  mm.specification  as specification,
  mm.dimension      as dimension,
  up.operation_no   as operation_no,
  pm.process_name   as process_name,
  up.pricing_price  as pricing_price,
  up.currency       as currency,
  up.unit           as unit
from unit_price up
left join material_master mm on mm.material_no = up.code
left join process_master pm on pm.process_no = up.operation_no
where up.system_type = 'PRICING'
  and up.price_type = 'CONSUMABLE'
  and :versionFilter(up.is_current, up.version_no, up.code)"
43e07bb2-f62e-466f-bec1-3ec5ffae04da,"select
  pe.material_no      as material_no,
  pe.material_no      as hf_part_no,
  pe.calc_version     as view_version,
  pe.production_no    as production_no,
  coalesce(pe.material_name, mm.material_name)  as material_name,
  coalesce(pe.specification, mm.specification)  as specification,
  coalesce(pe.dimension, mm.dimension)          as dimension,
  pe.process_no       as process_no,
  coalesce(pm.process_name, pe.process_name)    as process_name,
  pe.unit_price       as unit_price,
  pe.currency         as currency,
  pe.unit             as unit
from production_energy pe
left join material_master mm on mm.material_no = pe.material_no
left join process_master pm on pm.process_no = pe.process_no
where pe.system_type = 'PRICING'
  and pe.price_type = 'ENERGY'
  and :versionFilter(pe.is_current, pe.calc_version, pe.material_no)"
46245d0e-8d3e-407f-a1bf-58f362de7934,"-- 物料（BOM 树页签；树契约：material_no=子 / parent_no=父 + :total_material_no；边式全子件 + 根分支）
-- 走 Pass2 树渲染（BomTreeRenderService, usage=QUOTE），禁用 :customerCode 平铺契约（报价侧.md §5.6）。
-- ⚠️ 加删任何一列，UNION ALL 两个分支都要同步改，根分支用 NULL::<类型> 占位；列数不等 PG 直接报错。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  -- 【产出类型】= material_bom_item.component_usage_type ← Excel「产出料号类型/产出类型」。
  --   ⚠️ 同名列陷阱：element_bom_item 上的同名列 0 写点、恒 NULL，只有本表这列有数据（§7.2.1）。
  mbi.component_usage_type AS _产出类型,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  -- 【材料占比】= material_bom_item.material_ratio（V365）← Excel「材质占比/材料占比」。
  --   本页签 FROM material_bom_item 可直接绑；材质边出数、组装边留空属预期（报价侧.md §7.2.2）。
  mbi.material_ratio AS _材料占比,
  mbi.scrap_rate AS _损耗率,
  mbi.defect_rate AS _不良率,
  -- 【回收价格/回收比例】= 来料回收折扣（unit_price price_type='INCOMING_MATERIAL_RECYCLE'）。
  --   Q09IncomingRecoveryHandler：groupKey=(code=投入料号, finished_material_no=成品料号)，
  --   content=[seq_no=项次, cost_ratio=回收折扣%, pricing_price=值, currency, unit]。
  --   匹配维度 =【投入料号 + 直接父件】：code=本行子件、finished=本行父件。
  --   ⚠️ 已知取舍：三层以上 BOM 中孙件那行取不到值（它的父不是成品）。
  -- ⚠️ unit_price 是版本化表 → 禁写进带 is_current 的 LEFT JOIN（§3.6：参数错位、dry-run 照过、
  --    只在真实渲染炸），故用相关标量子查询。树契约不注入 :customerCode，靠(成品+投入料号)两维定位。
  (SELECT r.pricing_price FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收价格,
  (SELECT r.cost_ratio FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收比例
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身（树根）
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::varchar,                                 -- 产出类型
  NULL::numeric, NULL::numeric, NULL::numeric,   -- 组成数量 / 材料毛重 / 材料净重
  NULL::varchar,                                 -- 单位
  NULL::numeric, NULL::numeric, NULL::numeric,   -- 材料占比 / 损耗率 / 不良率
  NULL::numeric, NULL::numeric                   -- 根分支（成品自身）无回收折扣
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
47e8cb4d-6eab-4d2f-8b4e-98417d10b495,"select
  mbt.material_no        as parent_no,
  mbt.component_no       as material_no,
  mbt.component_no       as hf_part_no,
  mbt.bom_version        as view_version,
  mbt.production_no      as production_no,
  mbt.seq_no             as seq_no,
  coalesce(mm.material_name, mr.name) as material_name,
  mm.specification       as specification,
  mm.dimension           as dimension,
  mbt.operation_no       as operation_no,
  pm.process_name        as process_name,
  mbt.feature_mgmt       as feature_mgmt,
  mbt.composition_qty    as composition_qty,
  mbt.issue_unit         as issue_unit,
  mbt.base_qty           as base_qty,
  mbt.issue_unit         as base_unit,
  mbt.scrap_rate         as scrap_rate,
  mbt.fixed_scrap        as fixed_scrap,
  mbt.defect_rate        as defect_rate,
  mbt.calc_type          as calc_type,
  mm.unit_weight         as unit_weight
from material_bom_item mbt
left join material_master mm on mm.material_no = mbt.component_no
left join material_recipe mr on mr.code = mbt.component_no
left join process_master pm on pm.process_no = mbt.operation_no
where mbt.system_type = 'PRICING'
  and mbt.customer_no = '_GLOBAL_'
  and :versionFilter(mbt.is_current, mbt.bom_version, mbt.material_no)
  and mbt.component_no = ANY(:total_material_no)
order by mbt.seq_no"
48379ca8-5280-40cf-acc9-d5ad82bd3e3b,"-- BOM 树页签(树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.scrap_rate AS _损耗率,
  mbi.net_weight AS _净重,
  mbi.rough_weight AS _毛重,
  mbi.weight_unit AS _用量单位,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric, mm.standard_unit
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
48720977-4070-4055-9e0e-dc151ab3f5e8,"-- ============================================================================
-- 组件：物料（施耐德-v3）   SQL 视图名：bom_view   驱动路径：$bom_view
-- requiredVariables: [""total_material_no""]   scope: COMPONENT
-- 页签类型 tabType: BOM   partNameField: 料件   rowKeyFields: [""料件""]
--
-- 2026-07-31 修订：新增 `_材料占比`（material_bom_item.material_ratio，V365 新增列），
--                  UNION ALL 根分支同步补 NULL::numeric 占位。其余逐字未动。
--                  规则见 dev-docs/rule-0724-组件模板配置/报价侧.md §7.2.2。
--
-- 【改这个文件后怎么生效】本文件是「可编辑副本」，不是运行时来源。改完二选一：
--   ① UI：组件管理 →「物料」组件 → SQL 视图 → 粘贴保存（保存即 dry-run 校验）
--   ② API：PUT /api/cpq/components/{componentId}/sql-views/{viewId}
--          body: {""sqlViewName"":""bom_view"",""sqlTemplate"":""<本文件内容>"",
--                 ""scope"":""COMPONENT"",""requiredVariables"":[""total_material_no""]}
--      （componentId/viewId 在你导入 bundle 后才确定；GET /components?directoryId=<你的目录> 查）
--   改完记得把 bundle JSON 也重导一次，否则 JSON 与实际配置漂移。
--
-- 【改之前必看两条坑】
--   1. 本视图是 UNION ALL 两分支（主分支=BOM 边 / 根分支=无父边的成品自身）。
--      **加删任何一列，两个分支都要同步改**，根分支用 NULL::<类型> 占位。列数不等 PG 直接报错。
--   2. 验证别靠肉眼数列。直连 PG 跑一次最可靠（把命名参数换成字面量）：
--      sed 's/:total_material_no/ARRAY[\'S-80011\']::text[]/g' 本文件 | psql -h 10.177.152.12 -U postgres -d cpq_db_0724 -f -
-- ============================================================================
-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  -- 【产出类型】= material_bom_item.component_usage_type（v2 由「材料成本」移入本页签）。
  --   写入路径已核实：MaterialBomMergeHandler:148 ← Excel「产出料号类型」(另有 P06/ConfigureProductService)。
  --   ⚠️ 同名列陷阱(§3.5)：element_bom_item 上的同名列 0 写点、恒 NULL，只有本表这列有数据。
  mbi.component_usage_type AS _产出类型,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  -- 【材料占比】= material_bom_item.material_ratio（V365 新增列，2026-07-31）。
  --   写入路径已核实：MaterialBomMergeHandler ← 报价侧「物料BOM」sheet 可选列「材质占比」。
  --   仅材质行(characteristic='RECIPE')有值——零件/外购件行 handler 显式置 NULL，故本树页签的
  --   材质边出数、组装边留空，属预期。小数口径(0.3=30%)。
  --   ⚠️ 本页签 FROM material_bom_item 可直接绑；若页签主表是 element_bom_item(如 $mc_view)，
  --      该表**没有**这个列，必须走跨表标量子查询(报价侧.md §7.2.2)。
  mbi.material_ratio AS _材料占比,
  mbi.scrap_rate AS _损耗率,
  -- 【回收价格/回收比例】= 来料回收折扣(unit_price price_type='INCOMING_MATERIAL_RECYCLE')。
  --   落库口径见 Q09IncomingRecoveryHandler：groupKey=(code=投入料号, finished_material_no=成品料号)，
  --   content=[seq_no=项次, cost_ratio=回收折扣%, pricing_price=值, currency, unit]（task-0730 扩列后）。
  --   匹配维度 =【投入料号 + 直接父件】(2026-07-31 用户拍板)：code=本行子件、finished=本行父件。
  --   ⚠️ 已知取舍：三层以上 BOM 中孙件那行取不到值(它的父不是成品)。
  -- ⚠️ unit_price 是版本化表 → 禁写进带 is_current 的 LEFT JOIN(§4 硬约束②：参数错位、dry-run 照过、
  --    只在真实渲染炸)，故用相关标量子查询。
  -- ⚠️ 树契约(§5.6)不注入 :customerCode，本子查询靠 (成品 + 投入料号) 两维定位。
  -- ⚠️ LIMIT 1：task-0730 起同一 (成品,投入料号) 可按项次存多行，取项次最小的一条。
  (SELECT r.pricing_price FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收价格,
  (SELECT r.cost_ratio FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收比例
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::varchar,                                  -- 产出类型
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric, NULL::numeric,   -- 单位 / 材料占比 / 损耗率
  NULL::numeric, NULL::numeric                    -- 根分支(成品自身)无回收折扣
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)
"
4fd071a4-69d8-46d5-9ba0-5571b20170ec,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
545ab346-7081-403d-83ba-0ea0881ce3fa,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名 JOIN 键(料号铁律 §3.4)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no"
54a346af-4237-443a-b500-07a474355b56,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
564e7686-2d9b-423c-97b4-2b82bc33d1dc,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
56e91553-d36f-4754-97c6-b9d48c082b32,"select
  up.finished_material_no as material_no,
  up.finished_material_no as hf_part_no,
  up.version_no           as view_version,
  up.production_no        as production_no,
  up.code                 as incoming_material_no,
  coalesce(mi.material_name, mr.name) as material_name,
  mi.specification        as specification,
  mi.dimension            as dimension,
  up.seq_no               as seq_no,
  up.cost_type            as cost_type,
  up.pricing_price        as pricing_price,
  up.currency             as currency,
  up.unit                 as unit
from unit_price up
left join material_master mi on mi.material_no = up.code
left join material_recipe mr on mr.code = up.code
where up.system_type = 'PRICING'
  and up.price_type = 'INCOMING_OTHER'
  and up.cost_ratio is null
  and :versionFilter(up.is_current, up.version_no, up.finished_material_no)
order by up.seq_no"
5a585595-b801-4cee-a54b-9015d66ee349,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
5c00c09e-8874-4d65-8e00-540186c96943,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  up.seq_no         as seq_no,
  up.cost_type      as cost_type,
  up.cost_ratio     as cost_ratio
from unit_price up
where up.system_type = 'PRICING'
  and up.price_type = 'FINISHED_OTHER'
  and up.cost_ratio is not null
  and :versionFilter(up.is_current, up.version_no, up.code)
  and up.code = ANY(:total_material_no)
order by up.seq_no
"
6355154f-de0b-4c6c-9fec-1c6d637d77b8,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名 JOIN 键(料号铁律 §3.4)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no"
6362e9f8-3b8c-406f-8c08-a5b1319f55ba,"select
  mbt.material_no        as parent_no,
  mbt.component_no       as material_no,
  mbt.component_no       as hf_part_no,
  mbt.bom_version        as view_version,
  mbt.production_no      as production_no,
  mbt.seq_no             as seq_no,
  coalesce(mm.material_name, mr.name) as material_name,
  mm.specification       as specification,
  mm.dimension           as dimension,
  mbt.operation_no       as operation_no,
  pm.process_name        as process_name,
  mbt.feature_mgmt       as feature_mgmt,
  mbt.composition_qty    as composition_qty,
  mbt.issue_unit         as issue_unit,
  mbt.base_qty           as base_qty,
  mbt.issue_unit         as base_unit,
  mbt.scrap_rate         as scrap_rate,
  mbt.fixed_scrap        as fixed_scrap,
  mbt.defect_rate        as defect_rate,
  mbt.calc_type          as calc_type,
  mm.unit_weight         as unit_weight
from material_bom_item mbt
left join material_master mm on mm.material_no = mbt.component_no
left join material_recipe mr on mr.code = mbt.component_no
left join process_master pm on pm.process_no = mbt.operation_no
where mbt.system_type = 'PRICING'
  and mbt.customer_no = '_GLOBAL_'
  and :versionFilter(mbt.is_current, mbt.bom_version, mbt.material_no)
  and mbt.component_no = ANY(:total_material_no)
order by mbt.seq_no"
6380735d-b826-4220-b113-4f1b5a079f59,"with recursive bom_closure as (
  select distinct b.material_no as root_no, b.material_no as node_no, 0 as lvl
  from material_bom_item b
  where b.system_type = 'PRICING' and b.is_current and b.customer_no = '_GLOBAL_'
  union all
  select c.root_no, b.component_no, c.lvl + 1
  from bom_closure c
    join material_bom_item b on b.material_no = c.node_no
  where b.system_type = 'PRICING' and b.is_current and b.customer_no = '_GLOBAL_'
    and c.lvl < 10
)
select
  ebi.material_no        as material_no,
  ebi.material_no        as hf_part_no,
  ebi.material_part_no   as material_part_no,
  coalesce(mr.name, mi.material_name) as material_name,
  mi.specification       as specification,
  mi.dimension           as dimension,
  ebi.seq_no             as seq_no,
  ebi.component_no       as component_no,
  ebi.content            as content,
  ebi.scrap_rate         as scrap_rate,
  cep.unit_price         as 元素单价
from element_bom_item ebi
left join material_master mi on mi.material_no = ebi.material_part_no
left join material_recipe mr on mr.code = ebi.material_part_no
left join lateral (
  select root_no from bom_closure where node_no = ebi.material_no
  order by (case when lvl = 0 then 1 else 0 end) asc, lvl asc, root_no asc limit 1
) cl on true
left join f_material_element_price(:customerCode, :priceBaseDate) cep
       on cep.element_code = ebi.component_no
      and cep.material_no  = coalesce(cl.root_no, ebi.material_no)
where ebi.system_type = 'PRICING'
  and ebi.is_current = true
order by ebi.seq_no
"
6b62ec3e-8747-49dc-ad6d-94a4c1afe28b,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  up.seq_no         as seq_no,
  up.cost_type      as cost_type,
  up.cost_ratio     as cost_ratio
from unit_price up
where up.system_type = 'PRICING'
  and up.price_type = 'FINISHED_OTHER'
  and up.cost_ratio is not null
  and :versionFilter(up.is_current, up.version_no, up.code)
  and up.code = ANY(:total_material_no)
order by up.seq_no
"
6e251fe8-31d5-4f6e-840f-fbc9d896fd62,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  up.operation_no   as operation_no,
  pm.process_name   as process_name,
  up.pricing_price  as pricing_price,
  up.currency       as currency,
  up.unit           as unit,
  up.defect_rate    as defect_rate
from unit_price up
left join process_master pm on pm.process_no = up.operation_no
where up.system_type = 'PRICING'
  and up.price_type = 'SELF_PROCESS'
  and :versionFilter(up.is_current, up.version_no, up.code)"
6ecbad48-4b14-443a-813e-adb97285d98f,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
6ed82e46-18ba-49a0-b322-868a57e8bd5c,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
72dd9878-d471-41a9-ae01-a6ff8bd1b4d3,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名 JOIN 键(料号铁律 §3.4)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no"
749b573e-7bbb-423f-889c-afbac394bbad,"-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  ebi.component_usage_type AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no"
75370563-676e-480b-9ff0-f42948f9ac63,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
7a71c82b-bdf7-46a9-b5f5-89bdf00c0cb9,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
7de3a374-c4ab-4409-b37d-252b8147eb59,"with recursive bom_closure as (
  select distinct b.material_no as root_no, b.material_no as node_no, 0 as lvl
  from material_bom_item b
  where b.system_type = 'PRICING' and b.is_current and b.customer_no = '_GLOBAL_'
  union all
  select c.root_no, b.component_no, c.lvl + 1
  from bom_closure c
    join material_bom_item b on b.material_no = c.node_no
  where b.system_type = 'PRICING' and b.is_current and b.customer_no = '_GLOBAL_'
    and c.lvl < 10
)
select
  ebi.material_no        as material_no,
  ebi.material_no        as hf_part_no,
  ebi.material_part_no   as material_part_no,
  ebi.seq_no             as seq_no,
  ebi.component_no       as component_no,
  ebi.content            as content,
  ebi.scrap_rate         as scrap_rate,
  cep.unit_price         as 元素单价
from element_bom_item ebi
left join lateral (
  select root_no from bom_closure where node_no = ebi.material_no
  order by (case when lvl = 0 then 1 else 0 end) asc, lvl asc, root_no asc limit 1
) cl on true
left join f_material_element_price(:customerCode, :priceBaseDate) cep
       on cep.element_code = ebi.component_no
      and cep.material_no  = coalesce(cl.root_no, ebi.material_no)
where ebi.system_type = 'PRICING'
  and ebi.is_current = true
  and ebi.material_no = ANY(:total_material_no)
order by ebi.seq_no
"
80481ac4-0bd9-4c80-9eb2-9b51fa533738,"-- ============================================================================
-- 组件：材料成本（施耐德-1）   SQL 视图名：mc_view   驱动路径：$mc_view
-- requiredVariables: [""customerCode"", ""priceBaseDate""]   scope: COMPONENT
--
-- 2026-07-28 修订：只改 `_产出类型` 一处（原绑 ebi.component_usage_type 恒为空），
--                  其余逐字未动。详见 dev-docs/rule-0724-组件模板配置/报价侧.md §7.2.1。
-- ============================================================================
-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  -- 「产出类型」= 物料BOM 的「产出料号类型」(MaterialBomMergeHandler:148 → material_bom_item.component_usage_type)。
  -- ⚠️ 不在 element_bom_item 上：该表的同名列无任何写入路径，绑它恒为空。
  -- ⚠️ 必须用相关标量子查询而非 LEFT JOIN：material_bom_item 是版本化表(§3.6)，
  --    且同一对父子常并存 RECIPE+ASSEMBLY 多条边(§3.8.3 铁律3) → JOIN 会让材料成本行倍增。
  (SELECT t.component_usage_type
     FROM material_bom_item t
    WHERE t.system_type  = 'QUOTE' AND t.is_current
      AND t.customer_no  = :customerCode
      AND t.material_no  = ebi.material_no
      AND t.component_no = ebi.material_part_no
    ORDER BY CASE WHEN t.characteristic = 'RECIPE' THEN 0 ELSE 1 END, t.characteristic
    LIMIT 1) AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
80975961-b600-46df-a7ed-ad2e01d69db9,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名/取价 JOIN 键(料号铁律 §3.4)。
--
-- 【要素/费用/单位/货币】= 组成件其他费用(unit_price price_type='COMPONENT_OTHER')，2026-07-27 用户拍板。
--   ⚠️ 匹配维度必须是【闭包根(成品) × 组成件】，不是【直接父件 × 组成件】：
--      Q13ComponentOtherFeeHandler 的 groupKey =(cost_type=要素名称, code=组成件料号,
--      finished_material_no=**销售料号/成品料号**, operation_no, supplier_no)，费用登记在成品维度。
--      实测 W-1001 同时直挂成品 S-3120014539(数量2) 与挂在子装配 S-80011 下(数量3)；若按
--      mbi.material_no(直接父件) 匹配，后者取不到价。故 ON 用 COALESCE(cl.root_no, mbi.material_no)。
--   注：「单位」= 计价单位(up.unit)，与已有的「组成单位」(mbi.issue_unit = BOM 发料单位) 语义不同，勿混。
--
-- ⚠️ §3.6 pending 改写坑：版本化表(unit_price)**禁**写进带 is_current 的 LEFT JOIN ON(参数错位，
--    dry-run 照过、只在真实渲染炸)。此处改用【派生表】——is_current 落在子查询 WHERE(文档明列的安全位置)，
--    JOIN ON 内不含 is_current。为什么不用文档推荐的标量子查询：cost_type 是 groupKey 的一维，
--    同一组成件可挂多条费用要素，必须真连接产生多行；行键已含「要素」防撞键。
--
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件下挂的外购件也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
--          (本视图主位表 = 顶层 FROM 的 material_bom_item mbi；派生表里的 unit_price 在 depth>0，不夺主位。)
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位,
  co.cost_type AS _要素,
  co.pricing_price AS _费用,
  co.unit AS _单位,
  co.currency AS _货币
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
  LEFT JOIN (
    SELECT up.code, up.finished_material_no, up.cost_type,
           up.pricing_price, up.currency, up.unit
    FROM unit_price up
    WHERE up.system_type = 'QUOTE' AND up.price_type = 'COMPONENT_OTHER' AND up.is_current
      AND up.customer_no = :customerCode
  ) co ON co.code = mbi.component_no
      AND co.finished_material_no = COALESCE(cl.root_no, mbi.material_no)
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no
"
8102800e-5f1c-4419-8ca6-4afcf5ad2174,"select
  up.finished_material_no as material_no,
  up.finished_material_no as hf_part_no,
  up.version_no           as view_version,
  up.production_no        as production_no,
  up.code                 as incoming_material_no,
  coalesce(mi.material_name, mr.name) as material_name,
  mi.specification        as specification,
  mi.dimension            as dimension,
  up.pricing_price        as pricing_price,
  up.currency             as currency,
  up.unit                 as unit,
  up.defect_rate          as defect_rate
from unit_price up
left join material_master mi on mi.material_no = up.code
left join material_recipe mr on mr.code = up.code
where up.system_type = 'PRICING'
  and up.price_type = 'INCOMING_PROCESS'
  and :versionFilter(up.is_current, up.version_no, up.finished_material_no)"
83daf105-565d-40ff-bee3-fd028e4976ba,"-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  ebi.component_usage_type AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_material_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
         AND cep.material_no = COALESCE(cl.root_no, ebi.material_no)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
84ab0a78-881e-42c6-a912-2507fa531319,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名 JOIN 键(料号铁律 §3.4)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no"
88959a40-b4bc-4f7c-b3d3-26fa2d546868,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
894c5cd2-ee40-4fb8-8bd0-1d3235b37947,"-- 来料固定加工费（费用页签，tabType 空不参与树；price_type='INCOMING_MATERIAL_PROCESS'）
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名（经 up.code 取名）。
-- ⚠️ 该 price_type 的金额落在 base_value、pricing_price 恒空 → 绑 COALESCE(pricing_price, base_value)。
-- task-0726 BOM 闭包：hf_part_no 输出「闭包根料号」，使子件的数据也进入本页签（§3.8.1 判据）。
-- ⚠️ 铁律1：白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，否则
--          QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2：必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 铁律3：用 UNION 去重，不能 UNION ALL（同一对父子常并存 RECIPE+ASSEMBLY 两条边）
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只留一行（取最浅层）。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍（§3.8.3 铁律3）。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.cost_ratio AS _比例,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
8a5b245c-c0e7-409b-8d4a-57ce190e144d,"-- ============================================================================
-- 组件：物料（施耐德-v3）   SQL 视图名：bom_view   驱动路径：$bom_view
-- requiredVariables: [""total_material_no""]   scope: COMPONENT
-- 页签类型 tabType: BOM   partNameField: 料件   rowKeyFields: [""料件""]
--
-- 2026-07-31 修订：新增 `_材料占比`（material_bom_item.material_ratio，V365 新增列），
--                  UNION ALL 根分支同步补 NULL::numeric 占位。其余逐字未动。
--                  规则见 dev-docs/rule-0724-组件模板配置/报价侧.md §7.2.2。
--
-- 【改这个文件后怎么生效】本文件是「可编辑副本」，不是运行时来源。改完二选一：
--   ① UI：组件管理 →「物料」组件 → SQL 视图 → 粘贴保存（保存即 dry-run 校验）
--   ② API：PUT /api/cpq/components/{componentId}/sql-views/{viewId}
--          body: {""sqlViewName"":""bom_view"",""sqlTemplate"":""<本文件内容>"",
--                 ""scope"":""COMPONENT"",""requiredVariables"":[""total_material_no""]}
--      （componentId/viewId 在你导入 bundle 后才确定；GET /components?directoryId=<你的目录> 查）
--   改完记得把 bundle JSON 也重导一次，否则 JSON 与实际配置漂移。
--
-- 【改之前必看两条坑】
--   1. 本视图是 UNION ALL 两分支（主分支=BOM 边 / 根分支=无父边的成品自身）。
--      **加删任何一列，两个分支都要同步改**，根分支用 NULL::<类型> 占位。列数不等 PG 直接报错。
--   2. 验证别靠肉眼数列。直连 PG 跑一次最可靠（把命名参数换成字面量）：
--      sed 's/:total_material_no/ARRAY[\'S-80011\']::text[]/g' 本文件 | psql -h 10.177.152.12 -U postgres -d cpq_db_0724 -f -
-- ============================================================================
-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  -- 【产出类型】= material_bom_item.component_usage_type（v2 由「材料成本」移入本页签）。
  --   写入路径已核实：MaterialBomMergeHandler:148 ← Excel「产出料号类型」(另有 P06/ConfigureProductService)。
  --   ⚠️ 同名列陷阱(§3.5)：element_bom_item 上的同名列 0 写点、恒 NULL，只有本表这列有数据。
  mbi.component_usage_type AS _产出类型,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  -- 【材料占比】= material_bom_item.material_ratio（V365 新增列，2026-07-31）。
  --   写入路径已核实：MaterialBomMergeHandler ← 报价侧「物料BOM」sheet 可选列「材质占比」。
  --   仅材质行(characteristic='RECIPE')有值——零件/外购件行 handler 显式置 NULL，故本树页签的
  --   材质边出数、组装边留空，属预期。小数口径(0.3=30%)。
  --   ⚠️ 本页签 FROM material_bom_item 可直接绑；若页签主表是 element_bom_item(如 $mc_view)，
  --      该表**没有**这个列，必须走跨表标量子查询(报价侧.md §7.2.2)。
  mbi.material_ratio AS _材料占比,
  mbi.scrap_rate AS _损耗率,
  -- 【回收价格/回收比例】= 来料回收折扣(unit_price price_type='INCOMING_MATERIAL_RECYCLE')。
  --   落库口径见 Q09IncomingRecoveryHandler：groupKey=(code=投入料号, finished_material_no=成品料号)，
  --   content=[seq_no=项次, cost_ratio=回收折扣%, pricing_price=值, currency, unit]（task-0730 扩列后）。
  --   匹配维度 =【投入料号 + 直接父件】(2026-07-31 用户拍板)：code=本行子件、finished=本行父件。
  --   ⚠️ 已知取舍：三层以上 BOM 中孙件那行取不到值(它的父不是成品)。
  -- ⚠️ unit_price 是版本化表 → 禁写进带 is_current 的 LEFT JOIN(§4 硬约束②：参数错位、dry-run 照过、
  --    只在真实渲染炸)，故用相关标量子查询。
  -- ⚠️ 树契约(§5.6)不注入 :customerCode，本子查询靠 (成品 + 投入料号) 两维定位。
  -- ⚠️ LIMIT 1：task-0730 起同一 (成品,投入料号) 可按项次存多行，取项次最小的一条。
  (SELECT r.pricing_price FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收价格,
  (SELECT r.cost_ratio FROM unit_price r
    WHERE r.system_type = 'QUOTE' AND r.price_type = 'INCOMING_MATERIAL_RECYCLE' AND r.is_current
      AND r.code = mbi.component_no AND r.finished_material_no = mbi.material_no
    ORDER BY COALESCE(r.seq_no, 0) LIMIT 1) AS _回收比例
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::varchar,                                  -- 产出类型
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric, NULL::numeric,   -- 单位 / 材料占比 / 损耗率
  NULL::numeric, NULL::numeric                    -- 根分支(成品自身)无回收折扣
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)
"
8aaafad3-fe68-4e27-bd8c-d5482da1bfeb,"select
  mbt.material_no        as parent_no,
  mbt.component_no       as material_no,
  mbt.component_no       as hf_part_no,
  mbt.bom_version        as view_version,
  mbt.production_no      as production_no,
  mbt.seq_no             as seq_no,
  coalesce(mm.material_name, mr.name) as material_name,
  mm.specification       as specification,
  mm.dimension           as dimension,
  mbt.operation_no       as operation_no,
  pm.process_name        as process_name,
  mbt.feature_mgmt       as feature_mgmt,
  mbt.composition_qty    as composition_qty,
  mbt.issue_unit         as issue_unit,
  mbt.base_qty           as base_qty,
  mbt.issue_unit         as base_unit,
  mbt.scrap_rate         as scrap_rate,
  mbt.fixed_scrap        as fixed_scrap,
  mbt.defect_rate        as defect_rate,
  mbt.calc_type          as calc_type,
  mm.unit_weight         as unit_weight
from material_bom_item mbt
left join material_master mm on mm.material_no = mbt.component_no
left join material_recipe mr on mr.code = mbt.component_no
left join process_master pm on pm.process_no = mbt.operation_no
where mbt.system_type = 'PRICING'
  and mbt.customer_no = '_GLOBAL_'
  and :versionFilter(mbt.is_current, mbt.bom_version, mbt.material_no)
  and mbt.component_no = ANY(:total_material_no)
order by mbt.seq_no
"
8ba51b69-2ab8-4be9-a371-5c673c0dacb0,"select
  lr.material_no          as material_no,
  lr.material_no          as hf_part_no,
  lr.version_no           as view_version,
  lr.production_no        as production_no,
  mm.material_name        as material_name,
  mm.specification        as specification,
  mm.dimension            as dimension,
  lr.process_no           as process_no,
  coalesce(pm.process_name, lr.process_name) as process_name,
  lr.standard_labor_rate  as standard_labor_rate,
  lr.currency             as currency,
  lr.unit                 as unit
from labor_rate lr
left join material_master mm on mm.material_no = lr.material_no
left join process_master pm on pm.process_no = lr.process_no
where lr.system_type = 'PRICING'
  and :versionFilter(lr.is_current, lr.version_no, lr.material_no)"
8bb7cfd4-a226-4028-93fa-b2184da5967b,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
8ca940dd-c62f-4b0f-851e-265d24f1ca92,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
-- repair-0727：工序名称口径与 jg_view 统一——主数据(process_master)优先 → 库内冗余名(process_name)次之 → 编号兜底。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(pm.process_name, NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
  LEFT JOIN process_master pm ON pm.process_no = c.process_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
8d7a7e81-de85-455b-b65b-49e12eaa1039,"-- 材料成本（材质元素，平铺契约：hf_part_no + :customerCode；元素单价接客户价格策略）
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (§3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号（§3.4 第二步）。
-- 「产出类型」不在本模板的本页签（该列在【物料】BOM 页签直接取 mbi.component_usage_type）。
-- task-0726 BOM 闭包：hf_part_no 输出「闭包根料号」，使子件的数据也进入本页签（§3.8.1 判据）。
-- ⚠️ 铁律1：白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，否则
--          QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2：必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 铁律3：用 UNION 去重，不能 UNION ALL（同一对父子常并存 RECIPE+ASSEMBLY 两条边）
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只留一行（取最浅层）。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍（§3.8.3 铁律3）。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号（partNoField）
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  -- 【净用量】= element_bom_item.base_qty ←「物料与元素BOM」sheet 的「净用量」(Q04:72)。
  ebi.base_qty AS _净用量,
  -- 【净用量单位】⚠️ V6 只有一个 issue_unit 列：Q04 的口径是「净用量单位非空则存它，否则存毛用量单位」，
  --   即毛/净两个单位在库里共用同一列。2026-08-02 用户拍板：两列都绑 issue_unit（显示同值）。
  ebi.issue_unit AS _净用量单位,
  -- 【元素回收折扣】= element_bom_item.recovery_discount ←「元素回收折扣」sheet（Q05 按
  --   (销售料号, 材质料号, 元素) 三维 UPDATE 到本行上），故直接取本行列、无需子查询。
  ebi.recovery_discount AS _元素回收折扣,
  -- 价格策略列：别名逐字、不加 _（硬约束6）。本模板无「货币」列 → element_currency_field 留空。
  cep.unit_price AS 元素单价
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  -- 🔴 task-0729 / V369 起：料号级取价函数（报价侧.md §5.2.2）。旧的 f_customer_element_price
  --    继续存在、但已降级为本函数内部的 fallback —— 新配/改配一律用 f_material_element_price，
  --    否则该组件永远吃不到「客户价格调整策略」产出的价格版本（静默按实时价，不报错）。
  -- 🔴 JOIN 键铁律：cep.material_no 必须与本视图 hf_part_no 的输出表达式**逐字一致**。
  --    本视图是闭包形态（hf_part_no = COALESCE(cl.root_no, ebi.material_no)），故照抄该表达式；
  --    平铺形态的视图写 ON cep.material_no = ebi.material_no。写错 = 静默取不到版本价。
  LEFT JOIN f_material_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键（硬约束 1/2）
        AND cep.material_no = COALESCE(cl.root_no, ebi.material_no)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
8e055034-f6d0-4e60-b3cb-e8a7fefd3a19,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
8efd733b-ba87-410c-b630-4ac56aff8168,"-- 来料其他费用(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_OTHER')
-- 落库口径见 Q07IncomingOtherFeeHandler:
--   groupKey=(cost_type=要素名称, code=投入料号, finished_material_no=销售料号)
--   content =[seq_no=项次, pricing_price=值, cost_ratio=比例, currency=货币, unit=计价单位]
-- 「销售料号」= 所属成品(up.finished_material_no)；「料号/料件」= 投入料件(up.code 及其名称)。
-- 模板未勾「料号列」→ partNoField 留空(§3.4)；tabType 空属费用类，不受""料号/名称至少一个""约束。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的来料其他费用也进入本页签
--   (§3.8.1 判据: 本页签是整个产品结构的成本构成, 与兄弟页签「来料固定加工费」同族)。
-- ⚠️ 铁律1(§3.8.3): 白名单表 unit_price 必须在顶层 FROM，不能写成 FROM bom_closure JOIN unit_price，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
-- ⚠️ 铁律3: 递归用 UNION(非 UNION ALL) + bom_closure_d 对 (root,node) 去重，否则行成倍复制、页签小计翻倍。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code                 AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no               AS _项次,
  up.cost_type            AS _要素,
  up.pricing_price        AS _费用,
  up.cost_ratio           AS _比例,
  up.unit                 AS _计价单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
8fb00326-8ee2-4731-beae-8a3c051341cf,"-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「料号」列 = 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 材质料号(material_part_no) 模板无此列 → 只作 JOIN 键 + ORDER BY 键，不出可见列(料号铁律 §3.4)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件(如 S-80011)的材质也进入本页签。
-- ⚠️ 铁律1(task-0726 §3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入致
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(实测 4 行→8 行)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _料号,
  COALESCE(mr.name, mm2.material_name) AS _材质,   -- 逐行取自本行 material_part_no(一号多材质铁律)
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价                        -- 价格策略列: 别名逐字、不加 _
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_material_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键
         AND cep.material_no = COALESCE(cl.root_no, ebi.material_no)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
92ff3b24-928d-466c-8759-f36c6a60431b,"-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  mbi.scrap_rate AS _损耗率
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
9a3f5974-0d54-4b11-87fb-14ca4c631a14,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
9aeb1d15-d1e5-4625-8ef5-9dc65a29d60a,"-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  mbi.scrap_rate AS _损耗率
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
9b2b67d4-d76c-4e88-a690-c4547cfb1a60,"-- 来料其他费用（费用页签，tabType 空不参与树；price_type='INCOMING_MATERIAL_OTHER'）
-- Q07 落库口径：groupKey=(cost_type=要素, code=投入料号, finished_material_no=销售料号)，
--   content=[seq_no=项次, pricing_price=值, cost_ratio=比例, currency=货币, unit=单位]。
-- 模板未勾「料号列」→ partNoField 留空（§3.4）；tabType 空属费用类，不受「料号/名称至少一个」约束。
-- task-0726 BOM 闭包：hf_part_no 输出「闭包根料号」，使子件的数据也进入本页签（§3.8.1 判据）。
-- ⚠️ 铁律1：白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，否则
--          QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2：必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 铁律3：用 UNION 去重，不能 UNION ALL（同一对父子常并存 RECIPE+ASSEMBLY 两条边）
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只留一行（取最浅层）。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍（§3.8.3 铁律3）。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code                 AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no               AS _项次,
  up.cost_type            AS _要素,
  up.pricing_price        AS _费用,
  up.cost_ratio           AS _比例,
  up.unit                 AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
9c99c7cd-678f-45ed-ba69-896fed55c30e,"-- 其他费用(成品其他费用; price_type='FINISHED_MATERIAL_OTHER'; 料号在 code 列)
-- 本页签是「本成品自身的费用项」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  up.code AS hf_part_no,
  up.code AS _销售料号,
  up.seq_no AS _项次,
  up.cost_type AS _类别,
  up.pricing_price AS _费用,
  up.unit AS _单位
FROM unit_price up
WHERE up.system_type = 'QUOTE' AND up.price_type = 'FINISHED_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no"
a00c0c42-82fc-4738-8c40-5e9b5b380ed1,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
a0d4e4a1-cdcc-4485-95d9-e462d7ba76d4,"-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  mbi.scrap_rate AS _损耗率
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
a15f4d77-1bd7-4baf-b50a-8c707f269604,"-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  ebi.component_usage_type AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_material_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
         AND cep.material_no = COALESCE(cl.root_no, ebi.material_no)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
a1e0087b-113f-4f92-8ea2-dbbc14a2211c,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
a53d40b1-1d3f-4f3d-9e58-bd13859ea652,"select
  up.finished_material_no as material_no,
  up.finished_material_no as hf_part_no,
  up.version_no        as view_version,
  max(up.production_no) as production_no,
  up.code              as input_part_no,
  max(coalesce(mm.material_name, mr.name)) as input_part_name,
  up.plating_scheme_no as plating_scheme_no,
  max(case when up.cost_type = '电镀加工费' then up.pricing_price end) as plating_proc_fee,
  max(case when up.cost_type = '电镀材料费' then up.pricing_price end) as plating_mat_fee,
  max(up.currency)     as currency,
  max(up.unit)         as unit,
  max(up.defect_rate)  as defect_rate
from unit_price up
  left join material_master mm on mm.material_no = up.code
  left join material_recipe mr on mr.code = up.code
where up.system_type = 'PRICING'
  and up.price_type = 'PLATING'
  and :versionFilter(up.is_current, up.version_no, up.finished_material_no)
group by up.finished_material_no, up.code, up.version_no, up.plating_scheme_no"
a994f8d7-5f92-4290-9b53-575ae4200273,"SELECT
  ebi.material_no AS hf_part_no,
  COALESCE(mr.name, mm.material_name) AS ""_材质库_材质名称""
FROM element_bom_item ebi
  LEFT JOIN material_recipe mr ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm ON mm.material_no = ebi.material_part_no
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode AND ebi.material_no = ANY(:total_material_no)
ORDER BY ebi.material_no, ebi.material_part_no, ebi.component_no, ebi.seq_no"
aa96fe8e-9003-4546-bfa6-b786ad9183c7,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
ab1889bf-07cd-4efd-bf9f-03498d2719e8,"SELECT
  ebi.material_no AS hf_part_no,
  COALESCE(mr.name, mm.material_name) AS ""_材质库_材质名称""
FROM element_bom_item ebi
  LEFT JOIN material_recipe mr ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm ON mm.material_no = ebi.material_part_no
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode AND ebi.material_no = ANY(:total_material_no)
ORDER BY ebi.material_no, ebi.material_part_no, ebi.component_no, ebi.seq_no"
ac0f98b1-1fa9-4d54-a9b4-f7d05b1b6448,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  mm.material_name  as material_name,
  mm.specification  as specification,
  mm.dimension      as dimension,
  up.operation_no   as operation_no,
  pm.process_name   as process_name,
  up.pricing_price  as pricing_price,
  up.currency       as currency,
  up.unit           as unit
from unit_price up
left join material_master mm on mm.material_no = up.code
left join process_master pm on pm.process_no = up.operation_no
where up.system_type = 'PRICING'
  and up.price_type = 'PACKAGING'
  and :versionFilter(up.is_current, up.version_no, up.code)"
acad5ea4-a882-4698-83b4-10a3e1097f3e,"with recursive bom_closure as (
  select distinct b.material_no as root_no, b.material_no as node_no, 0 as lvl
  from material_bom_item b
  where b.system_type = 'PRICING' and b.is_current and b.customer_no = '_GLOBAL_'
  union all
  select c.root_no, b.component_no, c.lvl + 1
  from bom_closure c
    join material_bom_item b on b.material_no = c.node_no
  where b.system_type = 'PRICING' and b.is_current and b.customer_no = '_GLOBAL_'
    and c.lvl < 10
)
select
  ebi.material_no        as material_no,
  ebi.material_no        as hf_part_no,
  ebi.material_part_no   as material_part_no,
  ebi.seq_no             as seq_no,
  ebi.component_no       as component_no,
  ebi.content            as content,
  ebi.scrap_rate         as scrap_rate,
  cep.unit_price         as 元素单价
from element_bom_item ebi
left join lateral (
  select root_no from bom_closure where node_no = ebi.material_no
  order by (case when lvl = 0 then 1 else 0 end) asc, lvl asc, root_no asc limit 1
) cl on true
left join f_material_element_price(:customerCode, :priceBaseDate) cep
       on cep.element_code = ebi.component_no
      and cep.material_no  = coalesce(cl.root_no, ebi.material_no)
where ebi.system_type = 'PRICING'
  and ebi.is_current = true
  and ebi.material_no = ANY(:total_material_no)
order by ebi.seq_no
"
aed86b65-14fe-4917-abaa-f7350f6e7498,"select
  ae.material_no    as material_no,
  ae.material_no    as hf_part_no,
  ae.calc_version   as view_version,
  ae.production_no  as production_no,
  coalesce(ae.material_name, mm.material_name)  as material_name,
  coalesce(ae.specification, mm.specification)  as specification,
  coalesce(ae.dimension, mm.dimension)          as dimension,
  ae.process_no     as process_no,
  coalesce(pm.process_name, ae.process_name)    as process_name,
  ae.non_production_energy_price as non_production_energy_price,
  ae.currency       as currency,
  ae.unit           as unit
from auxiliary_energy ae
left join material_master mm on mm.material_no = ae.material_no
left join process_master pm on pm.process_no = ae.process_no
where ae.system_type = 'PRICING'
  and :versionFilter(ae.is_current, ae.calc_version, ae.material_no)"
b03519c3-f361-4ea9-b0da-ad982674824e,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
b125698c-c4b2-420b-9ec2-1c9522492506,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
b137a7e1-5816-4870-8534-5ad3bb3d6488,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
b23bae8b-ab29-459c-a91c-8d121f98dfa1,"-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  ebi.component_usage_type AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no"
b311785e-3876-40e7-9fa2-554cddc794fe,"-- 其他费用（成品其他费用；price_type='FINISHED_MATERIAL_OTHER'；料号在 code 列）
-- 本页签是「本成品自身的费用项」→ 不做 BOM 闭包（§3.8.1 判据）。
-- ⚠️ 现网口径：固定金额项写 pricing_price，按比例登记的项（材料管理费/利润/税率…）值在 cost_ratio、
--    pricing_price 为空 —— 故「费用」「比例」两列并存，各取各的，不互相兜底。
SELECT
  up.code AS hf_part_no,
  up.code AS _销售料号,
  up.seq_no AS _项次,
  up.cost_type AS _要素,
  up.pricing_price AS _费用,
  up.cost_ratio AS _比例,
  up.unit AS _单位
FROM unit_price up
WHERE up.system_type = 'QUOTE' AND up.price_type = 'FINISHED_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no"
b5092b32-1a25-4a2c-a5f7-a5d217c27f8f,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
b6530668-c863-457c-a457-11285830586e,"-- 其他费用(成品其他费用; price_type='FINISHED_MATERIAL_OTHER'; 料号在 code 列)
-- 本页签是「本成品自身的费用项」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  up.code AS hf_part_no,
  up.code AS _销售料号,
  up.seq_no AS _项次,
  up.cost_type AS _类别,
  up.pricing_price AS _费用,
  up.unit AS _单位
FROM unit_price up
WHERE up.system_type = 'QUOTE' AND up.price_type = 'FINISHED_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no"
bb99db8a-0158-49be-8239-c90573f4f3bb,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
bd28230e-5f26-4740-bf13-c8a4daaa8726,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
bf04d6f7-bccb-419b-8515-20c9238156a6,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
bfa29413-dbe6-4201-9747-d8c5ef00bdf0,"-- 来料其他费用(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_OTHER')
-- 落库口径见 Q07IncomingOtherFeeHandler:
--   groupKey=(cost_type=要素名称, code=投入料号, finished_material_no=销售料号)
--   content =[seq_no=项次, pricing_price=值, cost_ratio=比例, currency=货币, unit=计价单位]
-- 「销售料号」= 所属成品(up.finished_material_no)；「料号/料件」= 投入料件(up.code 及其名称)。
-- 模板未勾「料号列」→ partNoField 留空(§3.4)；tabType 空属费用类，不受""料号/名称至少一个""约束。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的来料其他费用也进入本页签
--   (§3.8.1 判据: 本页签是整个产品结构的成本构成, 与兄弟页签「来料固定加工费」同族)。
-- ⚠️ 铁律1(§3.8.3): 白名单表 unit_price 必须在顶层 FROM，不能写成 FROM bom_closure JOIN unit_price，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
-- ⚠️ 铁律3: 递归用 UNION(非 UNION ALL) + bom_closure_d 对 (root,node) 去重，否则行成倍复制、页签小计翻倍。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code                 AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no               AS _项次,
  up.cost_type            AS _要素,
  up.pricing_price        AS _费用,
  up.cost_ratio           AS _比例,
  up.unit                 AS _计价单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
c035c2eb-cc07-4a4b-837d-9c94dc35076f,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
c31e2643-53e6-4abd-9a21-05d534657690,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
c3486542-f4fc-49c5-93b3-38dbca0e672e,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名/取价 JOIN 键(料号铁律 §3.4)。
--
-- 【要素/费用/单位/货币】= 组成件其他费用(unit_price price_type='COMPONENT_OTHER')，2026-07-27 用户拍板。
--   ⚠️ 匹配维度必须是【闭包根(成品) × 组成件】，不是【直接父件 × 组成件】：
--      Q13ComponentOtherFeeHandler 的 groupKey =(cost_type=要素名称, code=组成件料号,
--      finished_material_no=**销售料号/成品料号**, operation_no, supplier_no)，费用登记在成品维度。
--      实测 W-1001 同时直挂成品 S-3120014539(数量2) 与挂在子装配 S-80011 下(数量3)；若按
--      mbi.material_no(直接父件) 匹配，后者取不到价。故 ON 用 COALESCE(cl.root_no, mbi.material_no)。
--   注：「单位」= 计价单位(up.unit)，与已有的「组成单位」(mbi.issue_unit = BOM 发料单位) 语义不同，勿混。
--
-- ⚠️ §3.6 pending 改写坑：版本化表(unit_price)**禁**写进带 is_current 的 LEFT JOIN ON(参数错位，
--    dry-run 照过、只在真实渲染炸)。此处改用【派生表】——is_current 落在子查询 WHERE(文档明列的安全位置)，
--    JOIN ON 内不含 is_current。为什么不用文档推荐的标量子查询：cost_type 是 groupKey 的一维，
--    同一组成件可挂多条费用要素，必须真连接产生多行；行键已含「要素」防撞键。
--
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件下挂的外购件也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
--          (本视图主位表 = 顶层 FROM 的 material_bom_item mbi；派生表里的 unit_price 在 depth>0，不夺主位。)
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位,
  co.cost_type AS _要素,
  co.pricing_price AS _费用,
  co.unit AS _单位,
  co.currency AS _货币
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
  LEFT JOIN (
    SELECT up.code, up.finished_material_no, up.cost_type,
           up.pricing_price, up.currency, up.unit
    FROM unit_price up
    WHERE up.system_type = 'QUOTE' AND up.price_type = 'COMPONENT_OTHER' AND up.is_current
      AND up.customer_no = :customerCode
  ) co ON co.code = mbi.component_no
      AND co.finished_material_no = COALESCE(cl.root_no, mbi.material_no)
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no
"
c4b32561-aff7-480f-b62a-44ad2406996a,"-- 材料成本(材质元素, 平铺契约; 材质逐行取本行 material_part_no; 元素单价接客户价格策略)
SELECT
  ebi.material_no AS hf_part_no,
  ebi.material_no AS _销售料号,
  COALESCE(mr.name, mm2.material_name) AS _材质,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价
FROM element_bom_item ebi
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_material_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no
         AND cep.material_no = ebi.material_no
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY ebi.material_no, ebi.material_part_no, ebi.seq_no
"
c59dc22f-3a1f-43c1-8a6f-44a13df339dc,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
ca132a8b-0cc8-4084-bdff-d9afc6b4d8f4,"-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  mbi.scrap_rate AS _损耗率
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
cd84a865-5e90-432f-950d-4ef81ee580e0,"-- 产品（主件，平铺契约：hf_part_no + :customerCode）
-- 本页签展示「产品自身属性」→ 按 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
-- 「税率」无源手填，故本视图不输出该列（§6 自检 0：视图输出列与字段一一对应，无没人绑的死列）。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
ceef2a9c-146d-4ad5-a95d-ec430774fdcc,"-- 其他费用(成品其他费用; price_type='FINISHED_MATERIAL_OTHER'; 料号在 code 列)
-- 本页签是「本成品自身的费用项」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  up.code AS hf_part_no,
  up.code AS _销售料号,
  up.seq_no AS _项次,
  up.cost_type AS _类别,
  up.pricing_price AS _费用,
  up.unit AS _单位
FROM unit_price up
WHERE up.system_type = 'QUOTE' AND up.price_type = 'FINISHED_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no"
d3ad2150-bfc3-4303-b7fd-a33faba42876,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
d42a46e7-5886-43cb-92c0-b6215b411e2c,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
d4a6edc7-f6b5-49e0-9067-5aa0080a0134,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「料号」列 = 所属销售料号(up.finished_material_no)，见 task-0726 §3.8.4；零件料号(up.code) 模板无此列
--   → 只作取名 JOIN 键，不出可见列(料号铁律 §3.4)。partNoField 留空由「料件」担标识。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件自身的加工费也进入本页签。
-- ⚠️ 铁律1(task-0726 §3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入致
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(实测 4 行→8 行)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
d5cd9b6a-760b-4a65-922c-f834d6ad64d5,"select
  up.code           as material_no,
  up.code           as hf_part_no,
  up.version_no     as view_version,
  up.production_no  as production_no,
  mm.material_name  as material_name,
  mm.specification  as specification,
  mm.dimension      as dimension,
  up.operation_no   as operation_no,
  pm.process_name   as process_name,
  up.pricing_price  as pricing_price,
  up.currency       as currency,
  up.unit           as unit,
  up.defect_rate    as defect_rate
from unit_price up
left join material_master mm on mm.material_no = up.code
left join process_master pm on pm.process_no = up.operation_no
where up.system_type = 'PRICING'
  and up.price_type = 'SELF_PROCESS'
  and :versionFilter(up.is_current, up.version_no, up.code)"
d601f042-fc1f-458b-a263-317f5855e8ac,SELECT :priceBaseDate::text AS pricebasedate
d6a907f3-be33-413f-8e5b-d269b4ae64ef,"-- 物料(BOM 树页签; 树契约: material_no=子 / parent_no=父 + :total_material_no; 边式全子件 + 根分支)
-- 走 Pass2 树渲染(BomTreeRenderService, usage=QUOTE)，禁用 :customerCode 平铺契约(报价侧.md §5.6)。
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.rough_weight AS _材料毛重,
  mbi.net_weight AS _材料净重,
  mbi.weight_unit AS _单位,
  mbi.scrap_rate AS _损耗率
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
-- 根分支：无父边的成品自身(树根)
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
d9792e6d-81c7-43e8-bc16-4b63315a8e81,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
e071b722-b906-4985-b23d-a2e34e2697e2,"-- 组装加工费(主件=成品自身的组装工序; 源 capacity, QUOTE 口径)
-- capacity 无 customer_no 列 → 不加 :customerCode 过滤，靠 hf_part_no=material_no 按本行成品收窄。
-- 本页签是「产品自身的组装工序」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  c.material_no AS hf_part_no,
  c.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  c.seq_no AS _项次,
  COALESCE(NULLIF(c.process_name, ''), c.process_no) AS _工序,
  c.fixed_cost AS _加工费,
  c.capacity_unit AS _单位
FROM capacity c
  LEFT JOIN material_master mm ON mm.material_no = c.material_no
  LEFT JOIN material_recipe mr ON mr.code = c.material_no
WHERE c.system_type = 'QUOTE' AND c.is_current
ORDER BY c.seq_no"
e1ad454a-07cd-4922-a0b3-59eb6919826c,"-- 来料其他费用(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_OTHER')
-- 落库口径见 Q07IncomingOtherFeeHandler:
--   groupKey=(cost_type=要素名称, code=投入料号, finished_material_no=销售料号)
--   content =[seq_no=项次, pricing_price=值, cost_ratio=比例, currency=货币, unit=计价单位]
-- 「销售料号」= 所属成品(up.finished_material_no)；「料号/料件」= 投入料件(up.code 及其名称)。
-- 模板未勾「料号列」→ partNoField 留空(§3.4)；tabType 空属费用类，不受""料号/名称至少一个""约束。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的来料其他费用也进入本页签
--   (§3.8.1 判据: 本页签是整个产品结构的成本构成, 与兄弟页签「来料固定加工费」同族)。
-- ⚠️ 铁律1(§3.8.3): 白名单表 unit_price 必须在顶层 FROM，不能写成 FROM bom_closure JOIN unit_price，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
-- ⚠️ 铁律3: 递归用 UNION(非 UNION ALL) + bom_closure_d 对 (root,node) 去重，否则行成倍复制、页签小计翻倍。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code                 AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no               AS _项次,
  up.cost_type            AS _要素,
  up.pricing_price        AS _费用,
  up.cost_ratio           AS _比例,
  up.unit                 AS _计价单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
e4dee25a-c1d2-42c8-bcb9-1aab722b46f8,"-- ============================================================================
-- 组件：材料成本（施耐德-1）   SQL 视图名：mc_view   驱动路径：$mc_view
-- requiredVariables: [""customerCode"", ""priceBaseDate""]   scope: COMPONENT
--
-- 2026-07-28 修订：只改 `_产出类型` 一处（原绑 ebi.component_usage_type 恒为空），
--                  其余逐字未动。详见 dev-docs/rule-0724-组件模板配置/报价侧.md §7.2.1。
-- ============================================================================
-- 材料成本(材质元素, 平铺契约: hf_part_no + :customerCode; 元素单价接客户价格策略)
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (task-0726 §3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号(§3.4 第二步)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号(partNoField)
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  -- 「产出类型」= 物料BOM 的「产出料号类型」(MaterialBomMergeHandler:148 → material_bom_item.component_usage_type)。
  -- ⚠️ 不在 element_bom_item 上：该表的同名列无任何写入路径，绑它恒为空。
  -- ⚠️ 必须用相关标量子查询而非 LEFT JOIN：material_bom_item 是版本化表(§3.6)，
  --    且同一对父子常并存 RECIPE+ASSEMBLY 多条边(§3.8.3 铁律3) → JOIN 会让材料成本行倍增。
  (SELECT t.component_usage_type
     FROM material_bom_item t
    WHERE t.system_type  = 'QUOTE' AND t.is_current
      AND t.customer_no  = :customerCode
      AND t.material_no  = ebi.material_no
      AND t.component_no = ebi.material_part_no
    ORDER BY CASE WHEN t.characteristic = 'RECIPE' THEN 0 ELSE 1 END, t.characteristic
    LIMIT 1) AS _产出类型,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 元素单价,                       -- 价格策略列：别名逐字、不加 _
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键(硬约束 1/2)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
e62a91bc-105e-40bd-95b8-7aa4b0a6af82,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名/取价 JOIN 键(料号铁律 §3.4)。
--
-- 【要素/费用/单位/货币】= 组成件其他费用(unit_price price_type='COMPONENT_OTHER')，2026-07-27 用户拍板。
--   ⚠️ 匹配维度必须是【闭包根(成品) × 组成件】，不是【直接父件 × 组成件】：
--      Q13ComponentOtherFeeHandler 的 groupKey =(cost_type=要素名称, code=组成件料号,
--      finished_material_no=**销售料号/成品料号**, operation_no, supplier_no)，费用登记在成品维度。
--      实测 W-1001 同时直挂成品 S-3120014539(数量2) 与挂在子装配 S-80011 下(数量3)；若按
--      mbi.material_no(直接父件) 匹配，后者取不到价。故 ON 用 COALESCE(cl.root_no, mbi.material_no)。
--   注：「单位」= 计价单位(up.unit)，与已有的「组成单位」(mbi.issue_unit = BOM 发料单位) 语义不同，勿混。
--
-- ⚠️ §3.6 pending 改写坑：版本化表(unit_price)**禁**写进带 is_current 的 LEFT JOIN ON(参数错位，
--    dry-run 照过、只在真实渲染炸)。此处改用【派生表】——is_current 落在子查询 WHERE(文档明列的安全位置)，
--    JOIN ON 内不含 is_current。为什么不用文档推荐的标量子查询：cost_type 是 groupKey 的一维，
--    同一组成件可挂多条费用要素，必须真连接产生多行；行键已含「要素」防撞键。
--
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件下挂的外购件也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
--          (本视图主位表 = 顶层 FROM 的 material_bom_item mbi；派生表里的 unit_price 在 depth>0，不夺主位。)
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位,
  co.cost_type AS _要素,
  co.pricing_price AS _费用,
  co.unit AS _单位,
  co.currency AS _货币
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
  LEFT JOIN (
    SELECT up.code, up.finished_material_no, up.cost_type,
           up.pricing_price, up.currency, up.unit
    FROM unit_price up
    WHERE up.system_type = 'QUOTE' AND up.price_type = 'COMPONENT_OTHER' AND up.is_current
      AND up.customer_no = :customerCode
  ) co ON co.code = mbi.component_no
      AND co.finished_material_no = COALESCE(cl.root_no, mbi.material_no)
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no
"
e73388ce-c268-4533-88b8-d4224567f404,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
ec71687d-d88b-49b6-a910-303505b45f5a,"select
  tc.material_no        as material_no,
  tc.material_no        as hf_part_no,
  tc.calc_version       as view_version,
  tc.production_no      as production_no,
  coalesce(tc.material_name, mm.material_name)  as material_name,
  coalesce(tc.specification, mm.specification)  as specification,
  coalesce(tc.dimension, mm.dimension)          as dimension,
  tc.process_no         as process_no,
  coalesce(pm.process_name, tc.process_name)    as process_name,
  tc.seq_no             as seq_no,
  tc.tooling_no         as tooling_no,
  tc.tooling_unit_cost  as tooling_unit_cost,
  tc.tool_life          as tool_life,
  tc.cycle_output       as cycle_output,
  tc.tooling_unit_price as tooling_unit_price,
  tc.currency           as currency,
  tc.unit               as unit,
  tc.is_effective       as is_effective
from tooling_cost tc
left join material_master mm on mm.material_no = tc.material_no
left join process_master pm on pm.process_no = tc.process_no
where tc.system_type = 'PRICING'
  and :versionFilter(tc.is_current, tc.calc_version, tc.material_no)
order by tc.seq_no"
f0e01022-19d3-4170-bd6b-74a25f6a3a47,"-- BOM 树页签(树契约: material_no=子/parent_no=父 + :total_material_no; 边式全子件 + 根分支)
SELECT
  mbi.component_no AS material_no,
  mbi.material_no  AS parent_no,
  COALESCE(mm.material_name, mr.name) AS _料件名称,
  mbi.scrap_rate AS _损耗率,
  mbi.net_weight AS _净重,
  mbi.rough_weight AS _毛重,
  mbi.weight_unit AS _用量单位,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.component_no = ANY(:total_material_no)
UNION ALL
SELECT
  mm.material_no, NULL::text, mm.material_name,
  NULL::numeric, NULL::numeric, NULL::numeric,
  NULL::varchar, NULL::numeric, mm.standard_unit
FROM material_master mm
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no
                    AND x.system_type = 'QUOTE' AND x.is_current)"
f228a7f8-3b13-4b7c-9671-1e9dc0d3e81e,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名/取价 JOIN 键(料号铁律 §3.4)。
--
-- 【要素/费用/单位/货币】= 组成件其他费用(unit_price price_type='COMPONENT_OTHER')，2026-07-27 用户拍板。
--   ⚠️ 匹配维度必须是【闭包根(成品) × 组成件】，不是【直接父件 × 组成件】：
--      Q13ComponentOtherFeeHandler 的 groupKey =(cost_type=要素名称, code=组成件料号,
--      finished_material_no=**销售料号/成品料号**, operation_no, supplier_no)，费用登记在成品维度。
--      实测 W-1001 同时直挂成品 S-3120014539(数量2) 与挂在子装配 S-80011 下(数量3)；若按
--      mbi.material_no(直接父件) 匹配，后者取不到价。故 ON 用 COALESCE(cl.root_no, mbi.material_no)。
--   注：「单位」= 计价单位(up.unit)，与已有的「组成单位」(mbi.issue_unit = BOM 发料单位) 语义不同，勿混。
--
-- ⚠️ §3.6 pending 改写坑：版本化表(unit_price)**禁**写进带 is_current 的 LEFT JOIN ON(参数错位，
--    dry-run 照过、只在真实渲染炸)。此处改用【派生表】——is_current 落在子查询 WHERE(文档明列的安全位置)，
--    JOIN ON 内不含 is_current。为什么不用文档推荐的标量子查询：cost_type 是 groupKey 的一维，
--    同一组成件可挂多条费用要素，必须真连接产生多行；行键已含「要素」防撞键。
--
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件下挂的外购件也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
--          (本视图主位表 = 顶层 FROM 的 material_bom_item mbi；派生表里的 unit_price 在 depth>0，不夺主位。)
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位,
  co.cost_type AS _要素,
  co.pricing_price AS _费用,
  co.unit AS _单位,
  co.currency AS _货币
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
  LEFT JOIN (
    SELECT up.code, up.finished_material_no, up.cost_type,
           up.pricing_price, up.currency, up.unit
    FROM unit_price up
    WHERE up.system_type = 'QUOTE' AND up.price_type = 'COMPONENT_OTHER' AND up.is_current
      AND up.customer_no = :customerCode
  ) co ON co.code = mbi.component_no
      AND co.finished_material_no = COALESCE(cl.root_no, mbi.material_no)
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no
"
f271737b-e313-456e-ab29-41d48797337f,"-- 材料成本(材质元素, 平铺契约; 料号列=材质料号 material_part_no(料号铁律); 元素单价接客户价格策略)
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件(如 S-80011)的材质也进入本页签。
-- ⚠️ 铁律1: 白名单表(element_bom_item)必须在顶层 FROM。写成 FROM bom_closure JOIN element_bom_item
--          会让 QuotePendingRewriter 主位表探测退化选中 CTE base case 里的表, 锚点注入致
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里, 否则页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION ALL
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_part_no AS _料号,
  COALESCE(mr.name, mm2.material_name) AS _材质,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  ebi.material_no AS _归属料号,
  cep.unit_price AS 元素单价
FROM element_bom_item ebi
  LEFT JOIN bom_closure cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_material_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no
         AND cep.material_no = COALESCE(cl.root_no, ebi.material_no)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
f4930cc8-2c84-490d-b60d-5d6729d8da2e,"-- 其他费用(成品其他费用; price_type='FINISHED_MATERIAL_OTHER'; 料号在 code 列)
-- 本页签是「本成品自身的费用项」→ 不做 BOM 闭包(§3.8.1 判据)。
SELECT
  up.code AS hf_part_no,
  up.code AS _销售料号,
  up.seq_no AS _项次,
  up.cost_type AS _类别,
  up.pricing_price AS _费用,
  up.unit AS _单位
FROM unit_price up
WHERE up.system_type = 'QUOTE' AND up.price_type = 'FINISHED_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no"
f6b6fb4f-3a70-4b4f-89f7-74b6f0a3d53b,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 模板无料号列 → partNoField 留空，组成件料号(component_no) 只作取名 JOIN 键(料号铁律 §3.4)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no"
f77f0ca6-dec5-4d62-b30f-4ecf23048c30,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
fbcbd795-fd29-4e2c-92e7-6d36456594ed,"-- 材料成本（材质元素，平铺契约：hf_part_no + :customerCode；元素单价接客户价格策略）
-- 「销售料号」= 所属销售料号(ebi.material_no)：闭包后主件与子件的材质行同页签共存，靠本列区分归属
--   (§3.8.4)；它必须同时在 rowKeyFields 里，否则撞键 → 编辑串行/末值塌缩。
-- 「料号」= 材质料号(ebi.material_part_no) = 本页签语义层级的料号（§3.4 第二步）。
-- 「产出类型」不在本模板的本页签（该列在【物料】BOM 页签直接取 mbi.component_usage_type）。
-- task-0726 BOM 闭包：hf_part_no 输出「闭包根料号」，使子件的数据也进入本页签（§3.8.1 判据）。
-- ⚠️ 铁律1：白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，否则
--          QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2：必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 铁律3：用 UNION 去重，不能 UNION ALL（同一对父子常并存 RECIPE+ASSEMBLY 两条边）
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只留一行（取最浅层）。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍（§3.8.3 铁律3）。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, ebi.material_no) AS hf_part_no,
  ebi.material_no AS _销售料号,
  ebi.material_part_no AS _料号,                   -- 材质料号（partNoField）
  COALESCE(mr.name, mm2.material_name) AS _料件,   -- 材质名：逐行取自本行 material_part_no
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  -- 【净用量】= element_bom_item.base_qty ←「物料与元素BOM」sheet 的「净用量」(Q04:72)。
  ebi.base_qty AS _净用量,
  -- 【净用量单位】⚠️ V6 只有一个 issue_unit 列：Q04 的口径是「净用量单位非空则存它，否则存毛用量单位」，
  --   即毛/净两个单位在库里共用同一列。2026-08-02 用户拍板：两列都绑 issue_unit（显示同值）。
  ebi.issue_unit AS _净用量单位,
  -- 【元素回收折扣】= element_bom_item.recovery_discount ←「元素回收折扣」sheet（Q05 按
  --   (销售料号, 材质料号, 元素) 三维 UPDATE 到本行上），故直接取本行列、无需子查询。
  ebi.recovery_discount AS _元素回收折扣,
  -- 价格策略列：别名逐字、不加 _（硬约束6）。本模板无「货币」列 → element_currency_field 留空。
  cep.unit_price AS 元素单价
FROM element_bom_item ebi
  LEFT JOIN bom_closure_d cl ON cl.node_no = ebi.material_no
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  -- 🔴 task-0729 / V369 起：料号级取价函数（报价侧.md §5.2.2）。旧的 f_customer_element_price
  --    继续存在、但已降级为本函数内部的 fallback —— 新配/改配一律用 f_material_element_price，
  --    否则该组件永远吃不到「客户价格调整策略」产出的价格版本（静默按实时价，不报错）。
  -- 🔴 JOIN 键铁律：cep.material_no 必须与本视图 hf_part_no 的输出表达式**逐字一致**。
  --    本视图是闭包形态（hf_part_no = COALESCE(cl.root_no, ebi.material_no)），故照抄该表达式；
  --    平铺形态的视图写 ON cep.material_no = ebi.material_no。写错 = 静默取不到版本价。
  LEFT JOIN f_material_element_price(:customerCode, :priceBaseDate) cep
         ON cep.element_code = ebi.component_no     -- LEFT JOIN + 元素符号键（硬约束 1/2）
        AND cep.material_no = COALESCE(cl.root_no, ebi.material_no)
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), ebi.material_no, ebi.material_part_no, ebi.seq_no
"
fc2c62b0-c6f9-411d-bf11-4488fc9b26b5,"-- 产品(主件, 平铺契约: hf_part_no + :customerCode)
-- 本页签展示「产品自身属性」→ 按 task-0726 §3.8.1 判据【不做 BOM 闭包】，否则会串入子件信息。
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode"
fc68247d-635f-461b-9d04-18818b62df3f,"-- 外购件成本(外购件, 平铺契约; characteristic='OUTSOURCED')
-- 「料号」列 = 所属销售料号(mbi.material_no)，见 task-0726 §3.8.4；组成件料号(component_no) 模板无此列
--   → 只作取名 JOIN 键，不出可见列(料号铁律 §3.4)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件下挂的外购件也进入本页签。
-- ⚠️ 铁律1(task-0726 §3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入致
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(实测 4 行→8 行)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, mbi.material_no) AS hf_part_no,
  mbi.material_no AS _料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN bom_closure_d cl ON cl.node_no = mbi.material_no
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current
  AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), mbi.material_no, mbi.seq_no"
fcfd825c-7d28-4213-a702-4dac38507882,"with recursive bom_closure as (
  select distinct b.material_no as root_no, b.material_no as node_no, 0 as lvl
  from material_bom_item b
  where b.system_type = 'PRICING' and b.is_current and b.customer_no = '_GLOBAL_'
  union all
  select c.root_no, b.component_no, c.lvl + 1
  from bom_closure c
    join material_bom_item b on b.material_no = c.node_no
  where b.system_type = 'PRICING' and b.is_current and b.customer_no = '_GLOBAL_'
    and c.lvl < 10
)
select
  ebi.material_no        as material_no,
  ebi.material_no        as hf_part_no,
  ebi.material_part_no   as material_part_no,
  ebi.seq_no             as seq_no,
  ebi.component_no       as component_no,
  ebi.content            as content,
  ebi.scrap_rate         as scrap_rate,
  cep.unit_price         as 元素单价
from element_bom_item ebi
left join lateral (
  select root_no from bom_closure where node_no = ebi.material_no
  order by (case when lvl = 0 then 1 else 0 end) asc, lvl asc, root_no asc limit 1
) cl on true
left join f_material_element_price(:customerCode, :priceBaseDate) cep
       on cep.element_code = ebi.component_no
      and cep.material_no  = coalesce(cl.root_no, ebi.material_no)
where ebi.system_type = 'PRICING'
  and ebi.is_current = true
order by ebi.seq_no"
fefc75c9-895d-4b08-a347-776e0c07ad4c,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
ff375aaa-b3bc-44e2-bebb-d24a66d09da1,"-- 来料固定加工费(费用页签, tabType 空不参与树; price_type='INCOMING_MATERIAL_PROCESS')
-- 「销售料号」= 所属销售料号(up.finished_material_no)；「料件」= 来料件名(经 up.code 取名)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  COALESCE(mm.material_name, mr.name) AS _料件,
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  COALESCE(up.pricing_price, up.base_value) AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'INCOMING_MATERIAL_PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
ffae2879-bf48-47b7-86d5-92df6592cd3c,"-- 加工费(零件, 平铺契约; price_type='PROCESS'; 工序=process_master.process_name)
-- 「销售料号」= 所属销售料号(up.finished_material_no)，「料号」= 零件料号(up.code)。
-- task-0726 BOM 闭包: hf_part_no 输出「闭包根料号」, 使子件的数据也进入本页签。
-- ⚠️ 铁律1(§3.8.3): 白名单表必须在顶层 FROM，不能写成 FROM bom_closure JOIN <白名单表>，
--          否则 QuotePendingRewriter 主位表探测退化选中 CTE base case 的表 → 锚点注入报
--          ""each UNION query must have the same number of columns""。
-- ⚠️ 铁律2: 必须 LEFT JOIN bom_closure_d + COALESCE 兜底 —— 无任何 BOM 行的单料产品不在闭包里，
--          写成 INNER JOIN 或直取 cl.root_no 会让这类产品的页签整个空白。
WITH RECURSIVE bom_closure AS (
  SELECT DISTINCT b.material_no AS root_no, b.material_no AS node_no, 0 AS lvl
  FROM material_bom_item b
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
  UNION            -- ⚠️ 用 UNION 去重，不能用 UNION ALL：同一对父子常并存多条 BOM 边
                   --    (RECIPE + ASSEMBLY 各一条) → UNION ALL 会生出重复路径
  SELECT c.root_no, b.component_no, c.lvl + 1
  FROM bom_closure c
    JOIN material_bom_item b ON b.material_no = c.node_no
  WHERE b.system_type = 'QUOTE' AND b.is_current AND b.customer_no = :customerCode
    AND c.lvl < 10                          -- 深度上限，防环
), bom_closure_d AS (
  -- 🔒 (root,node) 唯一化：多条边/多层路径到达同一子件时只保留一行(取最浅层)。
  --    漏这层 → 平铺页签行数成倍复制、页签小计翻倍(task-0726 §3.8.3 铁律3)。
  SELECT root_no, node_no, MIN(lvl) AS lvl FROM bom_closure GROUP BY root_no, node_no
)
SELECT
  COALESCE(cl.root_no, up.finished_material_no) AS hf_part_no,
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                                 -- 零件料号(partNoField)
  COALESCE(mm.material_name, mr.name) AS _料件,     -- 该费用项针对的零件名(经 up.code)
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _工序,
  up.pricing_price AS _加工费,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN bom_closure_d cl ON cl.node_no = up.finished_material_no
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY COALESCE(cl.lvl, 0), up.finished_material_no, up.seq_no"
