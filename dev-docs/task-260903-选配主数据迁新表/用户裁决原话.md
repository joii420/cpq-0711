# 用户裁决原话（2026-09-03）

> 📌 归档规范：`task-docs.md` 要求用户原话进任务目录，不能只写「见聊天记录」。

---

## 裁决一 · 迁往新表

> 你向 会话[新料号数据规则] 获取了解最新的产品数据存储规则，询问报价侧的数据存储，
> 你分析选配逻辑要如何存储到新规则表中，**选配不需要向 v6 旧表存储**。
> 你分析好后给我一份落库方案让我能清楚知道你的存储逻辑

## 裁决二 · 版本升级的时机

> **选配本质是创建产品**，如果真的是选配出结果，就保存到库中，**使用初始版本号**。
> 如果在选配中发现要选择的料号已存在，改为使用了之前的销售产品，**只是做引用**。
>
> 选配创建到产品卡片后，用户可以在报价单中进行编辑，关于版本升级，
> 是在这个报价单提交给核价审批，**在审批通过后才进行版本升级**。
> **在选配阶段不会出现版本升级**

## 裁决三 · 不双写

> **不要双写，只落新表**

## 裁决四 · 立项方式

> **合成一个任务立项，B 先行 A 后到**

---

## 由裁决直接推导出的设计约束

| 裁决 | 推导出的约束 | 落在哪 |
|---|---|---|
| 只落新表、不双写 | 没有过渡期 ⇒ A、B 必须同一次发布；135 个视图必须同期改 | `需求文档.md §2` |
| 命中即引用 | 指纹命中路径保持不变，`ds_quote_*` 一行不写 | `A-AC-4` / `A-8` |
| 初始版本号 | 选配只走 CREATED 分支（`version_no=1`），不走 UPGRADED | `A-AC-5` / `A-9` |
| 审批通过后才升版 | 该链路**现在不存在**（核价审批 5 个类回写调用 0 处）⇒ 本期不做，单独立项 | `需求文档.md §5.2` |
| B 先行 A 后到 | B 上线时 V6 仍在写，系统行为零变化，可充分验证后再切 A | `需求文档.md §2` |
