初始未筛选列表共 = -1
切换前：筛选框="(未找到, 共0个select)" 列表共=1 表格行=1 导出=1 行
切换后：筛选框="(未找到, 共0个select)" 列表共=263 表格行=20 导出=630 行

判定依据：导出是否 == 页面所见
  切换前 导出1 vs 列表1 → 一致
  切换后 导出630 vs 列表263 → 见下
  筛选框是否被重置：(未找到, 共0个select) → (未找到, 共0个select)