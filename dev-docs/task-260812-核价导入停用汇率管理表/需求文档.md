# 核价导入追加停用「汇率管理表」（P03）

- 立项日期：2026-08-12
- 类型：task
- 状态：开发中
- 父任务：`dev-docs/task-260812-核价导入停用四个Sheet/`（已合 master `9477223b`）—— 本任务是它的**同型追加**，一切未在此处重述的规则、纪律、坑位**一律以父任务文档为准**
- 关联：分支 `feat/task-0812-停用汇率管理表`（基于合并后 master）

---

## 1. 背景与目标

父任务已停用 P01/P02/P04/P05 四个 Sheet，导入与模板口径由 24 收敛为 20。用户追加要求：**「汇率管理表」（P03）按完全相同的手法一并停用**。

停用后：导入与模板口径 **20 → 19 Sheet**。

**影响面实测（2026-08-12，`cpq_db_0724`）—— 结论与前四个同级，无活跃消费方**：

| 检查项 | 结果 |
|---|---|
| Java 代码读 `exchange_rate_v6` | **无**。仅 P03 写入；`VersionedV6Writer:62` 是版本化表泛型登记（不是读取）；`ExchangeRateV6.java` 是实体映射 |
| `component_sql_view`（库内实配） | **0 命中** |
| `template_sql_view`（库内实配） | **0 命中** |
| 依赖该表的 PG 视图 | **0 个** |
| 组件字段 `fields::text`（`basic_data_path` 等） | **0 命中** |
| 存量数据 | 3 行，**不动**（停用只停增量） |

**唯一的代码引用**在 `V257__v12_create_template_sql_views.sql:296-302`（`summary_material` 视图取 `CNY→USD` 汇率），但**该视图在当前库里不存在**（`template_sql_view` 对 `exchange_rate_v6` 0 命中）—— 与父任务里 P01 元素价格的情形完全一致，属未落地的历史模板定义。

> ⚠️ **排查时的一个易误判点，记录备查**：`component_sql_view` 对 `%exchange_rate%` 有 **15 处命中**，但逐条核对后全部是 `mcm.exchange_rate`（`material_customer_map` 上的**列**），**不是** `exchange_rate_v6` **表**。用宽松 LIKE 会得出"汇率被 15 个视图依赖、不能停"的错误结论。

---

## 2. 范围

### 2.1 本期做

1. `PricingHandlerCatalog.all()` 摘除 `p03`（20 → 19 项）
2. `PricingImportService.orderedHandlers()` 摘除 `p03`（16 → 15 项）
3. `P03ExchangeRateHandler` 类**原样保留**，零改动
4. 前端 `PricingBasicDataImportDrawer.tsx` 文案 20 → 19 Sheet
5. `PricingTemplateServiceTest`：数量 20 → 19，`DISABLED_HANDLERS` 增加 P03
6. **`Task0812DisabledSheetsTest` 的正对照必须改换而非删除**（见 §3 D-3，本任务唯一的技术难点）

### 2.2 本期明确不做

| 不做项 | 原因 |
|---|---|
| 删除 `P03ExchangeRateHandler` | 同父任务 D-2：保留落库逻辑，恢复=加回两个 List |
| 清理 `exchange_rate_v6` 存量 3 行 | 停用只停增量；无人读、无害 |
| 重新生成 `docs/table/核价测试数据/核价数据模板.xlsx` | 该文件的「汇率管理表」页数据停用后会被忽略。**交付后由主线重新生成为 19 Sheet 版本**（不属代码改动，收尾时处理） |
| 任何 Flyway 迁移 | 不动 schema、不动数据 |

---

## 3. 澄清结论与决策记录

| # | 决策项 | 结论 | 理由 |
|---|---|---|---|
| D-1 | 停用手法 | **与父任务四个 Sheet 完全一致**：摘调用点、留 Handler 类、模板同步收敛、结果条目不出现 | 用户原话「以之前四个相同的修改操作」 |
| D-2 | 停用后口径 | 模板 **19 Sheet**；`sheetResults` 条目数 **18 → 17** | 条目数 = 15 循环项 + 2 合并项 = 17（合并 bean 口径见父任务 api.md） |
| D-3 | `Task0812DisabledSheetsTest` 的正对照如何处置 | **换成 P24 单重 → `material_master.unit_weight`，严禁直接删除** | 该测试第 273 行「P03 汇率行应正常写入」是**全文件唯一的正对照**，作用是证明「停用没有误伤其余 Sheet 的正常写入路径」。若删掉，测试退化为只能证明"什么都没写"——**一个彻底坏掉的导入也满足这个条件**，用例就此丧失鉴别力。选 P24 还因为它能与已停用的 P05 构成**同表反向对照**：同一张 `material_master`，P05 料号必须查不到、P24 料号必须查得到 |
| D-4 | 是否重走完整闸门 A | 否 | 用户明确指定「与前四个相同」，方案由已交付并验收的父任务定义，无待决项 |

---

## 4. 功能需求

- **FR-1** 核价导入不再解析、不再写库「汇率管理表」；上传文件含该页也不产生任何 `exchange_rate_v6` 写入
- **FR-2** `sheetResults` 不再包含「汇率管理表」条目（条目数 18 → 17）
- **FR-3** 「下载空模板」生成 **19 个 Sheet**，不含「汇率管理表」，其余 19 个的名称/顺序/表头与停用前逐字一致
- **FR-4** `P03ExchangeRateHandler` 类文件零改动，其落库逻辑完整保留
- **FR-5** 前端抽屉标题与上传提示显示 19 Sheet
- **FR-6** 其余 19 个 Sheet 的导入行为、版本升级语义、结果统计口径零变化

---

## 5. 数据与规则

无 schema 变更、无 Flyway 迁移、存量数据不动。

**停用后两个 List 的期望序列（逐字，只删不排）**：

```java
// PricingHandlerCatalog.all() —— 19 项
p06, p07, p08, p09, p10, p11, p12, p13, p14, p15, p16, p17, p18, p19, p20, p21, p22, p23, p24

// PricingImportService.orderedHandlers() —— 15 项
p24, p06, p07, p08, p09, p10, p11, p12, p13, p14, p15, p18, p21, p22, p23
```

---

## 6. 验收标准

- **AC-1**（FR-3）模板 `getNumberOfSheets() == 19`；Sheet 名与顺序逐字等于 §5 第一个序列；不含「汇率管理表」
- **AC-2**（FR-3）19 个保留 Sheet 的表头逐列与停用前一致（Handler 类零 diff 即为证明）
- **AC-3**（FR-1/FR-2）含「汇率管理表」且有非空数据行的 Excel 导入后：`sheetResults` 条目数 = **17**，且不含「汇率管理表」
- **AC-4**（FR-1）AC-3 导入前后 `SELECT count(*) FROM exchange_rate_v6` 不变（当前 3 行）；夹具专属汇率对（base/target）在表中查不到
- **AC-5**（D-3）`Task0812DisabledSheetsTest` 的正对照已换为 P24 单重且**断言为"应存在"**；与 P05 料号的"应不存在"构成同表反向对照；7 个测试方法全绿
- **AC-6**（FR-6）全量 `mvn test` 相对本次改动前**不新增** failure/error（用例名级 diff，口径见父任务）
- **AC-7**（FR-4）`git diff --stat` 不含 `P03ExchangeRateHandler.java`
- **AC-8**（FR-5）前端显示 19 Sheet，`tsc --noEmit` 0 错误，无 "20 Sheet" 残留
- **AC-9** 无新增 Flyway 迁移（`git diff --name-only -- db/migration` 为空）

---

## 7. 影响面与风险

改动文件：`PricingHandlerCatalog.java` / `PricingImportService.java` / `PricingTemplateServiceTest.java` / `Task0812DisabledSheetsTest.java` / `PricingBasicDataImportDrawer.tsx`。`P03ExchangeRateHandler.java` 零改动（AC-7 守卫）。

- **R-1（低）** 汇率数据今后无导入入口。经实测无任何消费方，且存量 3 行保留。若将来需要汇率，恢复成本 = 往两个 List 加回 `p03`。
- **R-2（已在 D-3 处置）** 正对照丢失导致回归测试丧失鉴别力 —— 本任务最需要小心的一点。

**反模式检查**：AP-44 未命中（不改 `field_type`）；`PricingSheetRegistry` 不含 P03（无镜像同步义务）；N+1 不适用（纯删除）。

---

## 8. 已知坑位

全部继承父任务 §8，特别重申：
1. 两个 List 是**写入依赖序**，只删不排
2. `PricingTemplateServiceTest` 的 `catalogCoversEveryPricingHandlerBean` 守卫**不许降级** —— bean 数仍是 24，必须把 P03 加进 `DISABLED_HANDLERS` 过滤，而不是把期望数字改小
3. 提交只 `git add` 明确改动文件，严禁 `git add -A`（有并发会话）
4. 搜代码用 `/usr/bin/grep -a`（本机 `grep` 是 `ugrep -I`）
