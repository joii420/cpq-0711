# 本目录原型的身份说明

> 本文件由 `task-260902-选配流程重构` 于 2026-09-02 补建，用于标注本目录各原型的身份。
> 🚫 **原 HTML 一字未改** —— 按 `docs/rules/frontend.md §1.3`，历史快照是那次交付的验收记录，不回改。

| 原型 | 身份 | 说明 |
|---|---|---|
| `原型-报价单-选配添加.html` | 🕰️ **历史快照** | 已被 `dev-docs/task-260902-选配流程重构/原型图/` 取代。偏差清单见该目录 `index.html` 的「与上一版原型的关系」一节 |
| `原型-报价单-从已有产品添加.html` | 📌 **仍为当前基准** | 「从产品库添加」入口本次不改 |
| `原型-选配模板管理.html` | 📌 **仍为当前基准** | 选配模板管理页本次不改 |
| `原型-配置中心-3D模型配置.html` | 📌 **仍为当前基准** | 3D 模型配置本次不改 |

## 为什么「选配添加」那份成了历史快照

`task-260902` 把选配从**两层模型**（产品 → 配件≡一个材质料号）改成**三层**
（产品 → 配件〔零件/外购件〕→ 零件挂 1~N 个材质 → 每材质选含量配置），
并把客户产品编号前置到流程第一步。五处结构性偏差逐条列在新目录的 `index.html` 里。

**照哪份做**：`dev-docs/task-260902-选配流程重构/原型图/`（挂 📌 当前基准）。
