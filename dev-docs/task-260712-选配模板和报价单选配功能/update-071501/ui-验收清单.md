# task-0712 update-071501 · 前端 UI 验收清单

> 用途：换轴（三类模板统一到「客户产品分类」）交付后的**人工 UI 点击验收**留档。
> 对应交付：merge `b53f348`（已合入 master）。前后端已通过两阶段评审 + 后端 API 直验；本清单覆盖 agent 无法执行的**真实浏览器交互**。
> 登录真实 UI（`http://localhost:5174`）逐项走一遍，勾选记录。
>
> **前置说明**：迁移已生效——所有客户已 backfill「默认分类」，`sel_template` 已被 V338 清空（测 D 段前需先新配一套选配模板）。测试完记得清理测试数据。

---

## A. 客户管理（F1｜客户绑定产品分类）

- [ ] **A1 新建客户**：客户管理 → 新建 → 表单出现**「产品分类」下拉**，且**默认已选中「默认分类」**（不是空）
- [ ] **A2 必填拦截**：清空产品分类 → 提交 → 报「请选择产品分类」，无法提交
- [ ] **A3 带过滤**：产品分类下拉可输入文字过滤（showSearch）
- [ ] **A4 编辑回填**：打开一个已有客户 → 产品分类**正确回填**，不是空白
- [ ] **A5 可改绑（D4）**：把某客户产品分类改成另一个 → 保存成功
- [ ] **A6 列表不变**：客户列表**没有**多出「产品分类」列，也**没有**产品分类筛选（D5 刻意不加）

## B. 选配模板管理（F2｜归属换产品分类）

- [ ] **B1 归属字段**：配置中心 → 选配模板管理 → 新建 → 归属项是**「产品分类」**（不再「归属行业」），下拉带过滤
- [ ] **B2 无行业哨兵**：下拉**没有**「默认模板 / 通用组合工艺」旧行业哨兵（`RESERVED_INDUSTRIES` 已移除）
- [ ] **B3 一分类一套**：给某产品分类建一套模板后再新建 → 该分类**不再出现**在可选下拉里
- [ ] **B4 列表列**：列表首列是**「产品分类」**（显示分类名）
- [ ] **B5 编辑/停用/删除**：编辑回填正常；停用/启用切换正常；删除确认文案是「该产品分类」

## C. 报价单创建（F3｜产品分类客户带出、只读锁定）⭐ 重点

> 本次补齐了 **3 个创建入口**，逐个验：

- [ ] **C1 入口①「从基础数据导入」向导** Step3：产品分类**只读展示**客户绑定值、**不可改**；报价/核价模板据此自动匹配
- [ ] **C2 入口②手动新建** `/quotations/new`（QuotationWizard）：产品分类只读带出客户值
- [ ] **C3 入口③ QuoteBasicDataImportV6Drawer**（QuotationList「基础数据导入」按钮）：只读带出
- [ ] **C4 ⭐自动报价单名不丢**（必修1 竞态）：选客户后「报价单名称」应**自动带出「<客户名> 报价单」且不被清空**（改前 bug：主流程下名字被产品分类锁定 effect 冲掉、字段显红）——**本次最高优先回归项**
- [ ] **C5 编辑态不追溯（D4）**：先改某客户产品分类 → 打开该客户**已生成的旧报价单**编辑 → 产品分类/模板**保持原样、不被新分类覆盖**

## D. 选配抽屉回归（F4 + 文案）

- [ ] **D1 按客户分类带模板**：给某客户所属产品分类配好一套选配模板 → 用该客户草稿报价单打开「选配添加」→ 抽屉正确带出该分类的材质/元素/工序候选
- [ ] **D2 缺模板文案**：用一个产品分类下**没配**选配模板的客户 → 打开选配添加 → 空态提示为**「…为该客户所属产品分类或默认分类配置选配参数」**（不再说「行业」）
- [ ] **D3 选配落库不回归**：选配添加走完确认加入 → 卡片正常渲染

## E. 客户表单竞态（必修2）

- [ ] **E1 默认分类不闪失**：连续快速开关「新建客户」抽屉几次 → 产品分类始终稳定显示「默认分类」，不空白闪烁
- [ ] **E2 不覆盖已选**：新建时先手动改成别的分类 → 不被「默认分类」种子逻辑覆盖回去

---

## 判定标准

- **全绿** → 交付达标，可关单。
- **C4 / C5 是最高优先级**——竞态修复 + D4 编辑态是本次唯一有真实用户可见风险的点，且属**静态走查修复、未在真实浏览器复现验证**过。这两项 OK 则核心风险清零。
- 任一项异常 → 记录**页面 + 操作步骤 + 现象**反馈技术总监，走同样的 worktree + 两阶段评审流程修复。

---

## 已由技术总监完成的验证（无需重复）

| 项 | 证据 |
|---|---|
| 迁移 | 事务 dry-run + 主仓 Flyway V337/V338 `success` |
| 数据 | customer backfill 82/0 空、sel_template 换轴 + UNIQUE、默认分类在、pcid 可空 |
| 后端业务 API | 临时 rbac=false 实例 curl 直验：CustomerDTO 带 pcid、EffectiveTemplateDTO.resolvedCategoryId、upsert 400 校验、模板匹配命中 |
| 前端编译 | tsc 0 错误 + 改动文件 Vite transform 200 |
| 代码质量 | 后端/前端各两阶段独立评审（spec + 质量）+ 技术总监多轮亲验 |
