# 前端任务 · 核价导入数据版本升级与版本维护

> 负责人：cpq-frontend｜优先级：P0

## 结论：本次前端**无改动**

- 需求 §4.2 明确"本次前端无修改内容"；本任务为纯后端落库/升版规则改造，导入入口、页面、交互均不变。
- 导入触发页面（主数据维护 → 导入核价数据）与既有一致，不涉及任何 UI/表单/向导/抽屉调整。

## 前端侧只需回归验证（不改代码）

1. 主数据维护 → 导入核价数据 → 选核价 6.0 测试文件 → 导入流程正常、结果提示成功（沿用既有导入进度/结果 UI）。
2. 导入后核价单渲染/核价树页签取数正常（后端 `production_energy` 列由两列合并为 `unit_price`+`price_type`，若前端某处**直接**引用过 `energy_unit_price`/`depreciation_unit_price` 字段名——正常应经后端视图，不直连——需一并核查；预期无前端命中）。

> 若回归中发现前端确有直连上述被合并字段的地方，回报技术总监评估（预期无）。

## 交付物

- [ ] 回归验证记录：核价导入流程正常、核价渲染正常（截图/说明）
- [ ] 确认无前端代码改动（或如有命中被合并字段，列出并上报）
