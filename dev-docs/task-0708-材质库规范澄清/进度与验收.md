# 进度与验收看板 — 材质库规范化（task-0708）

> 技术总监维护 · 由我（技术总监）跟踪开发进度并对交付验收把关。
> 开发人员由甲方安排；本文是双方对齐**进度**与**验收结果**的单一入口。
> 状态图例：`⬜ 未开始`｜`🟡 开发中`｜`🔵 待验收`｜`✅ 验收通过`｜`❌ 打回`

---

## 一、总体进度

| 阶段 | 状态 | 说明 |
|------|------|------|
| 需求澄清（8 项决策锁定） | ✅ 完成 | 见 backtask.md「零、澄清结论」 |
| 任务与接口文档下发 | ✅ 完成 | backtask.md / fronttask.md / api.md |
| 后端开发 | 🔵 待验收 | B1–B6 代码就绪，`mvnw compile+test-compile` EXIT=0；运行时测试待受控验收 |
| 前端开发 | 🔵 待验收 | F1–F5 代码就绪，`tsc --noEmit` EXIT=0 |
| 联调 | ⬜ | 待后端上运行时环境后做导入端到端（8081 现跑 master 无导入端点） |
| 终验收 | ⬜ | 见「四、终验收清单」 |

**进度口径**：`11 子任务；代码就绪 11 / 编译·类型已过 / 运行时+端到端验收 待 / 终验收 0`。（2026-07-09 我方状态核实见文末「附录·状态核实记录」）

---

## 二、后端任务跟踪

| 任务 | 标题 | 状态 | 验收把关点（我逐条实测，不看口头汇报） |
|------|------|------|------------------------------------------|
| B1 | 元素主表 `element` + 中文字典 seed | 🔵 待验收 | V317 已建表+30 中文 seed；**运行时待**：迁移 success=t、`count(element)≥28`、`Ag→银` |
| B2 | 删 12 条 demo seed 材质 | 🔵 待验收 | V318 DELETE 12 code + name/spec_label 改可空；**运行时待**：AgCu85 等全删=0 |
| B3 | 列表全状态+搜索+排序+时间字段 | 🔵 待验收 | 代码已到位(新 `list(keyword,withCount)` ILIKE code/symbol/元素 + `ORDER BY status DESC,updated_at,created_at` + 时间字段；保留 listActive 给选配)；**运行时待**：`?keyword=银` 命中 |
| B4 | 导入服务(校验/×100/Upsert/批量) | 🔵 待验收 | ImportService+ReportDTO 已到位、编译过；**运行时待**：真文件 upsert 计数/9 数字元素跳过/×100/性能<3s |
| B5 | 导入接口 + 干净模板下载 | 🔵 待验收 | Resource `POST /import`(FileUpload)+`GET /import/template` 已到位、编译过；**运行时待**：端点实调 |
| B6 | 测试（校验/×100/Upsert/性能） | 🔵 待验收 | `MaterialRecipeImportServiceTest`+`MaterialRecipeListSearchTest` 已到位、test-compile 过；**运行时待**：worktree `./mvnw test` 亲跑全绿 |

**后端提交须附**：一行「已自检」声明（TS/编译? 端点 200/401、迁移 success=t、导入报告截图）。

---

## 三、前端任务跟踪

| 任务 | 标题 | 状态 | 验收把关点 |
|------|------|------|-----------|
| F1 | service：list keyword + import/template + 时间字段 | 🔵 待验收 | 代码到位；tsc EXIT=0 ✅；**联调待**：真调 import/template |
| F2 | 列表：时间列/排序/搜索/导入按钮/去料号列 | 🔵 待验收 | 代码到位；**联调待**：页面实测两时间列/搜索/导入按钮/无绑定料号列 |
| F3 | 导入抽屉（上传/报告/模板下载） | 🔵 待验收 | 代码到位；**联调待**：上传 xlsx 走通后端 → 报告；脏数据走 200 |
| F4 | 编辑抽屉：材质名称/隐藏字段/隐藏 tab/只读编号/默认 locked | 🔵 待验收 | 代码到位；**联调待**：页面实测标签/隐藏项/默认 locked |
| F5 | 自检 | 🔵 待验收 | tsc EXIT=0 ✅（我方 worktree 复验）；各 tsx Vite 200 待联调时补 |

**前端提交须附**：一行「已自检」声明（tsc 0 错 + 各 tsx Vite 200）。

---

## 四、终验收清单（对齐需求第 8 节 + 8 项锁定决策）

上线前我逐条实测，全绿才算交付：

- [ ] **导入正确**：主数据维护→材质 上传 `材质库.xlsx`，254 种材质入库，报告列出跳过明细
- [ ] **只读两 sheet**：`材质对应料号` 与隐藏草稿表未被读入（无脏材质）
- [ ] **含量 ×100**：抽查某材质 `Σdefault_pct=100`（如 AgC3：Ag97+C3）
- [ ] **Upsert 语义**：二次导入同文件无重复、Σ稳定；手工新建材质不被导入清掉
- [ ] **删 seed**：12 条 demo 材质已清
- [ ] **元素主表**：`element` 表有数据、符号接得上定价键、中文名不被符号覆盖
- [ ] **CRUD**：新建/编辑/停用可用；编辑时材质编号只读
- [ ] **搜索**：材质编号 / 材质名称 / 元素（符号+中文）均可过滤
- [ ] **列表**：有创建/修改时间列；排序=启用优先→改时倒序→建时倒序
- [ ] **编辑抽屉**：化学式已改「材质名称」；关联料号 tab 已隐藏；无 name/配比
- [ ] **模板下载**：得到干净两 sheet 模板（非 12-sheet 脏文件）
- [ ] **性能**：1000 行导入 <3s（B6 用例 + 真文件实测）
- [ ] **无回归**：选配材质 Tab / `ConfigureSearchResource` 正常（name 走 COALESCE 不报错）

---

## 五、风险与雷区（验收时重点盯）

| 风险 | 后果 | 验收盯点 |
|------|------|----------|
| 含量未 ×100 | 触发"和=100"校验被拒 / 流入定价 100 倍错价 | 抽查 default_pct 是 97 不是 0.97 |
| element_code 存了数字编号非符号 | 接不上 `costing_element_price`，定价断链 | `element_code='Ag'` 不是 `'10001'` |
| 导入用了 truncate 而非 Upsert | 手工/存量材质被误删 | 造一条手工材质→导入后仍在 |
| 删了 name/spec_label DB 列 | 下游 `COALESCE(mr.name…)` 报错 | 列仍在，仅 UI 隐藏 |
| 导入嵌套循环连库 | 1000 行超 3s | 看 durationMs + 代码无 per-row DB 调用 |

---

## 六、后续（本任务完成后才启动）

- **BL-0039** 材质↔料号关联功能（P1，前置＝本任务）
- **BL-0040** 元素主表管理 UI（P2，前置＝本任务）

---

## 附录·状态核实记录（2026-07-09 技术总监实测）

**触发**：前端会话上报「后端导入未实现，与主仓某乐观版看板『后端 B1–B6 全完成』矛盾」。据实独立核实（不信任何口头/乐观报告），结论如下。

**核实到的真相（均已实测）**：
1. 主仓那份「后端✅全完成」是**并发会话的乐观版**，非事实——本分支看板本就全 ⬜。
2. 前端的「后端 B4/B5 完全不存在」也**已过时**——实为**后端 B1–B6 文件全部到位**（`MaterialRecipeImportService`/`MaterialImportReportDTO`/`Element`/`V317`/`V318`/Resource `POST /import`+`GET /import/template`/`MaterialRecipeImportServiceTest`+`MaterialRecipeListSearchTest`），只是当时**全未提交**。核实中还观察到后端 dev 正实时新增测试文件（`MaterialRecipeListSearchTest.java` 中途冒出）。
3. `feat/quote-material-no`、`feat/task-0708-repair-1-production-no` 是**别的任务**分支，非竞争的材质库实现。
4. **后端编译验证**：worktree 内 `./mvnw compile test-compile` **EXIT=0**（含一处必要附带修复：`ElementRecoveryBatchUpdateEquivTest` 适配 3 维料号键，HEAD 处已破，非本任务）。
5. **前端类型验证**：worktree 内 `tsc --noEmit` **EXIT=0**。

**我方处置**：
- 把散落未提交成果按流打包 checkpoint 提交（`e52cc3a` 后端 / `ef4efbf` 前端+QA），**保护并发多会话易丢失的工作**；`材质库.xlsx`(1.9MB 测试数据)不入库。
- **未擅自跑后端运行时测试**：`@QuarkusTest` 会触发 Flyway 对**共享 dev 库**跑 V317/V318（删 seed/建表/改列），有跨会话副作用；留到「受控验收」统一跑（见下）。

**下一步验收路线（受控）**：
1. 后端 dev 收尾 `MaterialRecipeListSearchTest` 并声明提测。
2. 受控跑 `./mvnw test`（worktree 内）→ 导入/列表/性能用例全绿；用真 `材质库.xlsx` 核**真实**计数（不照抄文档 254/11/620 占位值，遵 test.md 铁律）。
3. 端到端联调：backend 需带本分支改动跑起来（worktree 起临时端口 或 合并入 master 由共享 8081 热加载）——见主线答复的方案权衡。
4. 全绿后走终验收清单 → finishing-a-development-branch。
