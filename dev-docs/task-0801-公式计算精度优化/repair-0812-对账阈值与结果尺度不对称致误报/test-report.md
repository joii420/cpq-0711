# test-report.md — repair-0812 对账阈值与结果尺度不对称致误报

- **报告日期**：2026-08-13
- **执行环境**：分支 `repair/0812-reconcile-scale`（已合 master）｜库 `10.177.152.12:5432/cpq_db_0724`｜后端复用主工作区 `8081`｜前端 worktree 内临时 Vite `5211`（验完已 kill）
- **合并**：已合入 master（前端 4 文件，后端 0 文件）
- **结论**：**单测层达成，真机层未验证** —— 详见 §6「未验证项」。按用户 2026-08-13 决定：**先合并，真机验收由用户在闸门 B 亲自执行**。

---

## 1. 汇总

| 类别 | 通过 | 失败 | 未验证 / 阻塞 |
|---|---|---|---|
| 单元测试（TC-00~TC-06c） | **15** | 0 | 0 |
| 全量前端回归 | **97 文件 / 1169 用例** | 0 | 0 |
| 编译自检 | tsc EXIT=0 | 0 | 0 |
| 真机（TC-07/07b/08/10/11/12/13/16） | 0 | 0 | **8（harness 缺陷，见 §5）** |
| E2E（TC-18b / precision-flow TC-076·076b） | 0 | 0 | **BLOCKED（`BL-0167` 种子缺失）** |
| 后端零改动核验 | **7（B-R6/B-R7 + 复核 A-1~A-3 + N+1）** | 0 | B-R1~B-R5 依赖真机，未执行 |

---

## 2. 决定性证据：单测级 A/B（用真实故障值）

**这是本次交付唯一具备鉴别力的运行级证据。** 手法：把 `valuesReconcile` 的归一还原成修复前口径，跑同一组用例。

| 臂 | 代码 | 结果 |
|---|---|---|
| **A（修复前口径）** | `isWithinTolerance(an, bn)` | **4 failed / 11 passed** |
| **B（修复版）** | `isWithinTolerance(formatFormulaResult(an), formatFormulaResult(bn))` | **15 passed** |

A 臂失败的 4 条：

```
× TC-01（P0，负例，补齐 task-0806 TC-121）：仅第 10~12 位不同 → 判一致（本 BUG 现场值）
× TC-01b（P0，负例，第二组真实现场值·材料价格）
× TC-03（P0，边界·进位）
× TC-06b（P1，回归·数组包裹）
```

**TC-01 用的就是故障单里的原始数值**（前端 `63.211125158028` / 后端 `63.211125158`），A 臂精确复现了用户截图里的误报。
⇒ 这组用例**不是"改完还是绿"的空验证**，它真的能区分修没修。

> 📌 TC-01 即 `task-0806/test.md` 的 **TC-121**（阈值负例），当初标「未执行」——它是唯一能拦住本类 BUG 的探针，本次补齐。

## 3. 自检原始输出（主线亲跑，非采信子代理）

```
$ npx tsc --noEmit -p tsconfig.app.json          # 合并后主工作区
TSC_EXIT=0

$ npm test                                        # 合并后主工作区
 Test Files  97 passed (97)
      Tests  1169 passed (1169)

$ curl --noproxy '*' http://localhost:5174/                       → 200
$ curl --noproxy '*' http://localhost:8081/api/cpq/components      → 401（后端健康）
$ curl .../5174/src/pages/quotation/QuotationStep2.tsx | grep -c "formatFormulaResult(an), formatFormulaResult(bn)"
1                                                 # 共享 dev server 已服务修复版
```

## 4. 后端零改动核验

- `git diff --stat master...repair/0812` 对 `cpq-backend/` → **0 文件**；`db/migration/` → **0 文件**
- **N+1 自检：后端零改动，无新增循环，SQL 条数不变 ✅**
- 独立复核（后端工程师）结论「零改动判定成立」，三条依据：
  1. `formulaResults` **唯一写入源**是 `FormulaCalculator.calculate()`（5 个重载全部收敛到 `computeRows`/`computeRowsCellTopo`）；`engine.formula.FormulaCalculationService` 是**无调用方的死代码**，`formula.FormulaEngine` 服务的是驱动预览/核价预览，都不落这份 JSON
  2. 两个 `roundFormulaResult` 调用点（`:1113` 普通行 / `:1244` BOM 树单元格拓扑）**逐字同构**，无既有分叉
  3. 报价侧 `buildCardValues` 与核价侧 `buildCostingCardValues` 共用同一 `assembleTabsWithFormulaResults` 重载链，**两侧无精度分叉**
- **数值一致性判定 100% 在前端**：`SubmitGateService` 只判 `diffs.isEmpty()`，`ReconcileDiffStore` 只做 put/remove，`reportReconcileDiff` 只做类型校验（`rejectNumericTokens`）+ 落日志

## 5. ⚠️ 真机验证为什么没做成（如实记录，不粉饰）

主线搭了 Playwright harness 跑 AC-1/AC-2，**首次运行 ⚠=0、diffs=[] 看似通过**。
**但随即做的 A/B 对照推翻了它**：把修复还原掉重跑，**⚠ 仍然是 0** —— 两臂都是空绿，该 PASS 不成立。

补日志查明根因：

```
活动页签 = []                                                    ← tab 选择器未命中
表头 = 销售料号|客户料号名称|客户产品编号|报价货币|汇率|管理费|税率   ← 停在「产品」页签
编辑的列 = 销售料号[0]                                            ← 编辑的是文本列
```

**harness 从未切到「物料」页签**，编辑的是文本列，根本不会触发公式比较。

> **教训**：若不做这次 A/B，就会拿一个假绿宣布「AC-1 通过」——与本次 BUG 同源的错误（验了个空 / 只验一侧）。
> **该 harness 已删除，未入库** —— 一个会给出假绿的验证脚本比没有更危险。

## 6. 未验证项（交付口径的硬边界，禁止在别处放大）

| AC | 状态 | 原因 |
|---|---|---|
| **AC-1** 真机 ⚠=0 | ❌ **未验证**（非未通过） | harness 缺陷，见 §5 |
| **AC-2** 提交不被 409 | ❌ **未验证** | 同上。且**故意未真跑 submit** —— 会把 DRAFT 单永久推成 SUBMITTED，不可逆，本环境无回退手段 |
| **AC-3** 阳性能力（E2E） | ⚠️ **BLOCKED** | `precision-flow.spec.ts` 缺 `PW_PRECISION_SEED_QUOTATION_NO` 种子（`BL-0167`）。TC-076 已按新语义改写、TC-076b 已新增，但**从未运行过** —— 阳性能力目前只有代码推理 + 单测 TC-02/TC-02b 支撑，**无运行级证据** |
| **AC-5** 落库逐字节不变 | ❌ **未验证** | 幂等回环依赖 harness 走到物料页签 |
| **AC-6** 真差异普查 | — | **用户裁决本次不做，另立任务** |
| B-R1~B-R5 | ❌ 未执行 | 均依赖真机 |

### 🚨 本次交付的准确口径（禁止放大）

> **只能声明「结构性误报的判定口径已修正，且该修正经真实故障值的单测 A/B 证明有效」。**
> **不得声明「前后端已一致」**（AC-6 普查未做）、**不得声明「⚠ 已在真机消失」**（AC-1 未验证）、
> **不得声明「提交闸门已恢复放行」**（AC-2 未验证）。

## 7. 已知限制（`问题说明.md` §5.1，须随交付一并周知）

| # | 限制 |
|---|---|
| **L-1** | 归一后两侧都是精确 9 位，最小非零差 `1e-9` > 容差 `1e-12` ⇒ **`isWithinTolerance` 的容差在本调用点已成死代码**，判定实际等价于「9 位精确相等」 |
| **L-2** | 若前后端 12 位工作值存在 1 ulp 差异且恰落在 9 位舍入中点附近 → 仍误报且仍不可消解。**前端单侧堵不上**（手上没有后端的 12 位工作值），只有方案 C 能闭合 |
| **L-3** | 两侧 12 位工作值「必然逐位相同」**无法被证明**：除法口径逐字对称，但乘加减有效数字上限不对称（后端 DECIMAL128 34 位 vs 前端 Decimal.js 80 位）；另有 JEXL3 内部实现、前后端语法树分组等价性两点静态读码无法验证 |
| **L-4** | 该风险**当前不可观测**：后端 12 位 `working` 只在局部变量与内存态 `ctx.fieldValues`，从不写日志/进 DTO/落库 |

## 8. 契约与索引

- **本次无契约变更，无需回写 `dev-docs/main-api.md`**（`任务平台规则.md` §2.4）
- `dev-docs/INDEX.md` 已回写；`docs/RECORD.md` 已追加

## 9. 交付期发现的缺陷 / 遗留

| # | 内容 | 处置 |
|---|---|---|
| D-1 | **FR-12 的可执行形态被漏改** —— 作废条款时只改了 3 处文档，漏了 `e2e/precision-flow.spec.ts` TC-076 把「第 10~12 位差异必须阻断」写死成 P0 断言 | ✅ 已改写 TC-076 + 新增 TC-076b；教训沉淀为 **`AP-65`** |
| D-2 | `precision-flow.spec.ts` 种子零文档，task-0810 的「5/5 通过」今天无人能复现 | ✅ 登记 **`BL-0167`**（P1，非本次引入） |
| D-3 | `问题说明.md` §4.2 证据 1 是 **n=1 外推成全称判断**（扩样 200 单实测 `max_dp=17`，421 个值中 50 个 >9 位） | ✅ 已划掉并订正；教训与 task-0813 合并沉淀为 **`AP-64`** |
| D-4 | 测试用例 TC-00b 原写法**只验一侧**（抓得到收紧、抓不到放宽），而它声称防的正是放宽 | ✅ 主线审核时订正为双侧断言 |
| D-5 | 共享库 `cpq_db_0724` 在验收窗口内被第三方改写两次（夹具 DRAFT→SUBMITTED、`quote_card_values` 29909→23532 字节） | 已在 `test.md` §0 登记「共享库漂移警告」+ 取证纪律 |
| D-6 | 亲验时对夹具 `QT-20260811-0169` 做了一次编辑，触发 `editCardValue` 全量重建，其 `formulaResults` 由 12 位遗留值变为 9 位新口径 | 报备：`editCardValue` 既有行为，非代码改动所致。**副作用是正向的** —— bug 场景现已 armed，下次真机可直接复现 |

## 10. 闸门 B 真机验收步骤（交给用户）

主工作区已合并，**共享 dev server `5174` 服务的就是修复版**（已 curl 校验命中改动代码）。

1. 打开任一 **DRAFT** 报价单 → 编辑 → 步骤 2 → 展开产品卡片
2. **切到「物料」页签**（关键：不是默认的「产品」页签）
3. 改任一行「材料净重」等数字列 → 失焦 → 等重算回灌
4. **期望**：该页签 **⚠ 数量 = 0**（修复前此处至少 3 个）
5. 点「提交审批」→ **期望不再返回 `409 前后端算值不一致`**
6. 推荐夹具：`QT-20260811-0169`（lineItem `b7077e71-…`，料号 `3120011203`）—— 其 `formulaResults` 已在亲验中被重建为 9 位新口径，bug 场景已 armed
   > ⚠️ 原故障单 `QT-20260811-0170` **已是 SUBMITTED**（冻结态不可编辑），不能用它验
