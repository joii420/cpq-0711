# test.md — repair-0812 对账阈值与结果尺度不对称致误报（测试用例）

- **归属**：`dev-docs/task-0801-公式计算精度优化/repair-0812-对账阈值与结果尺度不对称致误报/`
- **被测改动**：`QuotationStep2.tsx#valuesReconcile` 比较前按 `FORMULA_RESULT_SCALE = 9` 归一（后端零改动、契约零变更）
- **AC 来源**：`问题说明.md` §6（AC-1~AC-5、AC-7、AC-8；**AC-6 已裁决本次不做**）
- **版本**：v2（测试工程师审校版，见文末 §7「用例评审记录」）——v1 为技术总监起草稿

## 0. 环境

| 项 | 值 |
|---|---|
| 分支 | `repair/0812-reconcile-scale`（worktree 内） |
| 库 | `10.177.152.12:5432/cpq_db_0724`（dev 默认 profile） |
| 后端 | 复用主工作区 `8081`（**本次后端零改动，不需重启、不需另起**） |
| 前端 | worktree 内**临时 Vite（另开端口）**，不用共享 5174（它服务主工作区代码，SPA fallback 会给假 200） |
| **代码现状核实**（本轮审校时已读源码确认，非猜测） | `valuesReconcile` 已被前端实现挪到 `QuotationStep2.tsx` 模块作用域（约 `:493`）并 `export`（`:4618`，注释「供单测直接校验生产逻辑」）。**单测应直接 `import { valuesReconcile } from '.../QuotationStep2'`**，不必再用 `fronttask.md §2` 说的"等价复现"兜底——除非执行时发现该 import 因 `QuotationStep2.tsx` 是重量级页面组件（大量 antd/hooks 依赖）在 vitest 环境里报错或过慢，此时才退回等价复现方案（见 §2 前言，此风险已计入 §7 与我的最终风险提示） |
| 数据 | 报价单 `QT-20260811-0170`（lineItem `f57bfe0c-ba0c-4850-afdf-f23b81a3aa7e`）/ 料号 `3120011203` / 物料页签 |
| ⚠️ **夹具状态漂移（本轮审校时发现，原稿未覆盖）** | 截至本次审校（2026-08-13），`QT-20260811-0170` 的状态经 SQL 核实已是 **`SUBMITTED`**，不再是问题说明.md 报告时（2026-08-12）的 `DRAFT`。`quote_card_values` 内容本身未变（`quote_values_at=2026-08-13 06:52:18.769245+00`、`row_version=0`，与 B0 基线记录一致，md5 现查为 `5eea1cedd49e9192f6186e2b756b5400`——**这不是本次改动造成的，是基线采集之后有人把它提交审批了**）。**这意味着 TC-07~TC-10（要求 DRAFT + 可编辑）在执行时必须先做状态自检，见 TC-ENV-1**；反过来它现在恰好是 TC-13（非 DRAFT 分支）的现成夹具，见该条 |
| **DRAFT 备用夹具**（本轮审校时 SQL 查得，同模板同「物料」组件 `457603c5-1fc6-40d9-b877-fafbf04bd681`） | `QT-20260811-0169`（lineItem `b7077e71-9f7c-4035-9c47-24339a348b4c`）/ `QT-20260811-0168`（`a206c914-a107-4dac-989f-820199f613e5`）/ `QT-20260809-0167`（`37e836ac-cc94-40ec-bb3a-9bd9b7a6408c`）——若原夹具在执行时仍非 DRAFT 或已被他人占用，按此顺序取用；取用前同样先跑 TC-ENV-1 自检 |
| **字段级夹具核实**（本轮审校时 SQL 读取组件定义确认，供 TC-07 精确断言用） | `物料` 组件（`457603c5-…`）：`component_type=NORMAL`、`tab_type=BOM`、`bom_recursive_expand=true`（走 `FormulaCalculator.java:1244` BOM 树单元格拓扑分支）；字段「材料成本」`field_type=FORMULA` 且带 `conditional_formula`（`rules[]`：非银点类走「非银点类材料成本公式」、`是否叶子=0` 走「父子公式测试」、否则 `default` 走「银点材料成本公式」——**即问题说明.md 现场原文的条件公式**，其 `默认` 分支本身又是 `cross_tab_ref` 引用「材料成本」组件 `f04d1881-…`）；「材料损耗成本」「原材料成本」「公式10」等同页签另有 6 个纯 `FORMULA` 字段。**同一个页签一次性覆盖了"普通 FORMULA + 条件公式 + cross_tab_ref + BOM 树拓扑"四类中的三类**（唯独 LIST_FORMULA 缺席，见 §3 TC-09b） |
| **跨页签夹具核实** | 「产品」组件（`91d73900-…`）的「管理费」字段是 `cross_tab_ref` 公式，`SUM` 聚合分别引用「其他费用」组件（`d0df815c-…`）与「物料」组件（`457603c5-…`）——三个不同页签，天然适合验证「reconcileTab 只对账活动页签、但跨页签累积不丢」这条已知限制未被放大（TC-12） |
| **LIST_FORMULA 现网普查**（本轮审校时 SQL 核实） | `SELECT f->>'field_type', count(*) FROM component CROSS JOIN LATERAL jsonb_array_elements(fields) f GROUP BY 1` 结果只有 `INPUT_TEXT / INPUT_NUMBER / BASIC_DATA / FORMULA` 四种，**全库 0 条 LIST_FORMULA 字段**——该字段类型无法真机验证，见 TC-09b |
| **核价卡片核实** | 同一 lineItem `f57bfe0c-…` 的 `costing_card_values IS NOT NULL = true`，可直接作为 TC-11「核价单视图」的真实夹具，无需另找 |
| **AC-5 基线** | 🔴 **原基线已作废，不得使用**。主线 2026-08-13 采集的 `B0-quote_card_values.json`（`md5=92ff887dd51126a519ae2e2f6e274b49` / 29909 bytes）对应的 lineItem `f57bfe0c-…` 内容**已在同日被第三方改写**（主线实测现为 `md5=5eea1cedd49e9192f6186e2b756b5400` / 23532 bytes），该单据亦已 `SUBMITTED`。**执行 TC-10 前必须对 DRAFT 主夹具自采新基线 B0′**，见 TC-10 |
| 🚨 **共享库漂移警告（主线实测，最高优先级）** | `cpq_db_0724` 是**多会话共享**开发库。本任务期间该夹具**被第三方改写了两次**（DRAFT→SUBMITTED；`quote_card_values` 29909→23532 字节），均非本任务所为。⇒ **任何「改动前 vs 改动后」对比都必须在尽可能短的窗口内完成，且对比前后各校验一次内容指纹**；指纹在窗口内变过 = 本次结果作废重跑，**不得强行判定** |
| 🔬 **DRAFT 主夹具的特殊性（主线实测，直接影响复现步骤）** | `QT-20260811-0169`（`b7077e71-…`）的 `formulaResults` 存的是 **12 位遗留值**（实测 `材料成本=63.211125158028`、`材料价格=127.342371193912`、`公式10=2654.867256637168`），是 `a3285cc0`（2026-08-11 20:36 引入 `FORMULA_RESULT_SCALE=9`）**之前**写入的存量。<br>**后果**：打开页面**不编辑**时前端 12 位 vs 后端 12 位遗留值本就相等 → **看不到 ⚠**，这不代表 bug 不存在。**必须先编辑一格**触发 `editCardValue` 全量重建（重建后落 9 位新口径），bug 才会在**修复前**显现；修复后同样操作应 ⚠=0。这一前一后才是有效对比 |

## 1. 覆盖矩阵

| 维度 | 用例 | 说明 |
|---|---|---|
| 正常流 | TC-07、TC-07b、TC-08 | 编辑不再误报（含跨页签字段）、提交放行 |
| 边界值 | TC-00、TC-01、TC-01b、TC-02、TC-02b、TC-03、TC-04、TC-05、TC-05b~d | 归一/幂等边界（第 6/9/10 位、进位、借位、尾零、极大/极小/零值） |
| 异常/错误码 | TC-09、TC-14 | 真差异仍 409；裸 number 仍 400 |
| 权限 | TC-15 | **不适用说明**：本改动在渲染层判定函数内，不涉及鉴权分支；非 DRAFT 走 `useSnapEdit` 另一分支（TC-13 覆盖） |
| 并发/重复提交 | TC-16 | 连续快速编辑多格 → 上报以最后一次为准（整份替换语义不变） |
| 回归面 | TC-06、TC-06b、TC-06c、TC-10~TC-13、TC-17、TC-18 | 空值/非数字/JS number 分支、跨页签累积、三视图（含小计）与落库不变、E2E |
| 已知不可执行 | TC-09b | LIST_FORMULA 现网无夹具，理由见 §0 |
| 文档 | TC-19 | FR-12 / D4 改写留痕 |
| 环境自检 | TC-ENV-1 | 夹具 DRAFT 状态门禁（本轮新增，阻断 TC-07~TC-10） |

---

## 2. 单元测试（vitest，`npm test`）

> 被测函数 = `QuotationStep2.tsx` 内已导出的 `valuesReconcile`（见 §0「代码现状核实」）。**优先直接 import 真实生产函数**，不要再用"等价复现"抄一份逻辑——那样只能证明"我抄的逻辑对"，证明不了"生产代码对"，是本 BUG 教训（§4.6）明确要避免的模式。仅当 import 因页面组件重依赖在 vitest 环境跑不起来时，才退回等价复现，并**在测试文件顶部注释注明"等价复现，源函数改动须同步本文件"**。
>
> **不适用说明（科学计数法）**：`precisionValue`/`isDecimalString` 的正则 `^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$` 不接受 `e`/`E` 指数形式；且前端产物统一经 `Decimal.js` 的 `toFixed()`（已配置 `toExpNeg/toExpPos` 为极值防止输出指数记法），后端 `BigDecimal.toString()` 在本系统数值量级下也不会产出科学计数法字符串。故 `precisionValue` 实际不会收到科学计数法输入，本次不构造该用例（构造了也只是验证既有的"非数字→null→按字符串比"兜底分支，与本次改动无关）。

| 编号 | 对应 AC | 前置 | 步骤 / 输入 | 期望结果 | 实际 | 优先级 |
|---|---|---|---|---|---|---|
| **TC-00** | **AC-4（幂等性，方案成立的前提，原稿缺失）** | — | 直接调用 `formatFormulaResult('63.211125158')`（已是 9 位的后端典型产物），**不经过 `valuesReconcile`，单独断言这一个函数** | 返回值**逐字符等于** `'63.211125158'`（幂等：对已是 9 位的值再归一一次不改变字符串）。这是方案成立的前提——若这条不成立，TC-01 即便通过也可能是"两边同时被错误移动到同一个错值"而非"正确对齐"，TC-01 单独不能排除这种可能 | | **P0** |
| **TC-00b** | AC-4（回归·防止误改） | — | **两侧都断言**（主线审核订正）：① `isWithinTolerance('1','1.0000000000005')`（差 5e-13 < 容差）→ **true**；② `isWithinTolerance('1','1.000000000002')`（差 2e-12 > 容差）→ **false** | 默认容差仍恰为 `1e-12`。⚠️ **主线审核订正**：原用例只给了 ① 这一侧（5e-13 → true），而 ① 在容差被**放宽**（如改成 1e-9）时**仍然返回 true**，抓不到放宽 —— 恰恰放宽才是本用例声称要防的事。必须补 ② 这条上侧断言（2e-12 → false）才真正把默认值**双向**钉死。**教训与本 BUG 同源：只验一侧的断言 = 没有断言。** | | P1 |
| **TC-01** | AC-4（**负例，补齐 task-0806 TC-121**） | — | `a='63.211125158028'`（前端 12 位）、`b='63.211125158'`（后端 9 位） | **判一致**（true）——这正是本 BUG 的现场值 | | **P0** |
| **TC-01b** | AC-4（负例·负号镜像） | — | `a='-63.211125158028'`、`b='-63.211125158'` | **判一致**（true）——防止归一逻辑在负数分支有符号处理 bug（`toDecimalPlaces` 对负数的舍入方向容易出错） | | P1 |
| **TC-02** | AC-4（正例，⚠️**原稿此处小数位描述有误，已订正**） | — | `a='63.211125158'`、`b='63.211126158'` | **判不一致**（false）——阳性能力不丢。<br>⚠️ **订正**：原稿写"第 8 位差"，实际逐位比对 `211125158` vs `211126158` 差异在**第 6 位**（`5`→`6`），量级 `1e-6`。位置描述影响的是执行者能否无歧义复核，数值本身与预期结果不受影响，但必须订正，否则执行者按"第 8 位"去核对会发现对不上而误判用例本身有问题 | | **P0** |
| **TC-02b** | AC-4（正例·**边界，原稿缺失，本轮新增**） | — | `a='63.211125158'`、`b='63.211125159'`——两者归一到 9 位后仍在**最后一位（第 9 位）**相差 1，量级 `1e-9` | **判不一致**（false）——这是"必须抓住的最小真差异"边界：比它还小（第 10 位起）按方案设计应判一致（TC-01 已验），比它相等或更大（第 9 位起）必须判不一致。没有这条，TC-01（负例）和 TC-02（正例，差 1e-6，量级远离边界）之间存在一段没人验证的地带——万一实现有偏差（比如归一 scale 写成了 8 而不是 9），TC-01/TC-02 仍会全过，只有卡在第 9 位的用例才能抓到 | | **P0** |
| **TC-03** | AC-4（边界·进位） | — | `a='1.0000000005'`（第 10 位为 5）、`b='1.000000001'` | **判一致**（HALF_UP 归一后相等，`1.0000000005` → `1.000000001`） | | P0 |
| **TC-04** | AC-4（边界·进位反向/借位） | — | `a='1.0000000005'`、`b='1.000000000'` | **判不一致**（归一后 `1.000000001` ≠ `1`，差值 `1e-9` > 容差） | | P0 |
| **TC-05** | AC-4（边界·尾零/等值异形） | — | `a='63.2111251580'`（10 位，末位 0）、`b='63.211125158'` | 判一致（`formatFormulaResult` 去尾零幂等） | | P1 |
| **TC-05b** | AC-4（边界·极大值，亿级金额，原稿缺失） | — | `a='123456789.123456789012'`（9 位整数 + 12 位小数，约 1.2 亿量级）、`b='123456789.123456789'` | 判一致——验证 `Decimal.js` 在大整数部分下 `toDecimalPlaces/toFixed` 不因位数长而丢精度或切换成指数记法（库已配置 `toExpNeg/toExpPos` 极值防呆，本用例是对该配置的行为验证，不是纸面假设） | | P1 |
| **TC-05c** | AC-4（边界·极小值，接近§5接受的误差上界，原稿缺失） | — | `a='0.000000000499'`（4.99×10⁻¹⁰，略低于问题说明 §5 载明的结构性差值上界 `5×10⁻¹⁰`）、`b='0'` | 判一致——**这条正是方案裁决书 §5「已知风险与接受依据」描述的那类被吸收的真差异**（<5e-10 时接受判一致），用例存在的意义不是找 bug，是把"接受的代价"钉成可回归的断言，防止将来有人把它当 bug 修掉后反而破坏方案前提 | | P1 |
| **TC-05d** | AC-4（边界·零值多形态） | — | ①`a='0', b='0'` ②`a='0.000000000', b='0'` ③`a='-0.000000000001', b='0'` | 均判一致（各种零的字符串形态经归一后收敛到同一个 `'0'`） | | P2 |
| **TC-06** | 回归 | — | ① `a=null,b=null` ② `a='1',b=null` ③ `a='abc',b='abc'` ④ `a='abc',b='abd'` | ① true ② false ③ true ④ false（非数字/空值分支与改动前一致，`precisionValue` 对这些输入本就返回 `null`，不进入本次改动的归一分支） | | P0 |
| **TC-06b** | 回归 | — | `a=['63.211125158028']`（数组包裹，`precisionValue` 取 `[0]`） | 与 TC-01 同结果（true）——数组解包分支未被破坏 | | P1 |
| **TC-06c** | 回归（防御性，原稿缺失） | — | `a=63.211125158028`（**JS number 类型**，非字符串）、`b='63.211125158'` | `precisionValue(a)` 命中 `typeof scalar === 'number' → return null` 分支（此分支本次未改动）→ 落到 `String(a) === String(b)` 分支 → `'63.211125158028' !== '63.211125158'` → **判不一致**（false）。**本用例价值**：确认"裸 JS number 输入"这条既有防线没被本次改动意外接管进归一分支——若归一分支疏忽接纳了 number 类型，会掩盖上游本该在 `DecimalRequestValidator.rejectNumericTokens` 就被拒绝的契约违规，形成新的隐蔽退化 | | P1 |

---

## 3. 前端真机 / 集成

| 编号 | 对应 AC | 前置 | 步骤 | 期望结果 | 实际 | 优先级 |
|---|---|---|---|---|---|---|
| **TC-ENV-1** | 阻断门禁（本轮新增，非 AC 本身，但 TC-07~TC-10 的前置条件） | — | `SELECT status FROM quotation WHERE quotation_number='QT-20260811-0170'` | ⚠️ **主线已实测定案：`QT-20260811-0170` 已是 `SUBMITTED`，原夹具作废 —— 直接用 §0 的 DRAFT 主夹具 `QT-20260811-0169`（`b7077e71-…`）**，并在执行前再跑一次本 SQL 确认它**仍是** `DRAFT`（共享库会漂移，见 §0 漂移警告）。若它也已非 DRAFT：若三条备用也都不是 DRAFT，走「复制单」功能克隆 `QT-20260811-0170` 得到一份新 DRAFT。**不得跳过本步直接假设夹具是 DRAFT**——这正是本轮审校发现原稿漏掉的坑：夹具状态会随时间/他人操作漂移，写死一个 ID 不做状态自检，执行时大概率卡在"编辑请求 400 被前端静默吞掉"（`BL-0152`）却误判为"改动引入新 bug” | | **P0** |
| **TC-07** | **AC-1** | 承 TC-ENV-1 确认的 DRAFT 夹具；后端**未重启**（沿用现网 pending 状态）；打开该报价单编辑页 → 物料页签 | 修改任一行「材料净重」（或任意会牵动「材料成本」重算的字段）→ 失焦 → 等重算回灌 | 该页签 **⚠ 数量 = 0**（改动前至少 3 个：材料成本/材料损耗成本/原材料成本）；**逐项确认这三个具体字段而非只看总数**（总数=0 有可能是巧合地两个新⚠抵消一个消失的假象，逐项确认才是硬证据）；截图存证。<br>**覆盖说明**：本页签 `bom_recursive_expand=true`（BOM 树拓扑路径）+「材料成本」带 `conditional_formula`（条件公式）+ 该条件公式默认分支是 `cross_tab_ref`——一次操作覆盖普通 FORMULA / 条件公式 / BOM 树拓扑三类 | | **P0** |
| **TC-07b** | **AC-1（跨页签，原稿缺失）** | 承 TC-07 | 承 TC-07 的编辑动作后，切到「产品」页签查看「管理费」字段（`cross_tab_ref` 聚合「其他费用」+「物料」两个页签） | ⚠ = 0——验证跨页签 `cross_tab_ref` 公式同样吃到了归一后的比较口径，没有因为聚合链路更长而漏判/多判 | | P0 |
| **TC-08** | **AC-2** | 承 TC-07，**全程未重启后端** | 点「提交审批」 | **不再返回 409 `RECONCILE_PENDING`**；F12 看到 `reconcile-report` 请求体 `diffs: []` | | **P0** |
| **TC-09** | **AC-3 + E-4（原稿只映射 AC-3，本轮补 E-4）** | Playwright `page.route` 拦截 `quote-card-edit` 响应，把某 FORMULA 字段的 `formulaResults` 值篡改**第 8 位小数**（>1e-9 量级，确保归一到 9 位后仍是真差异） | ① 注入后编辑一格 ② **移除注入后**再编辑一格 | ① 出现 ⚠ **且**提交被 409 阻断（证明 AC-3：阳性能力没被本次改动连带削弱）；② ⚠ 消失、提交放行（证明 **E-4**：差异一旦解除，存在真实可达的消解路径，不需要重启后端——① ② 合起来是"注入真差异 → 用户侧动作使差异消失 → 恢复提交"的完整闭环，而不只是"能报警"这一半） | | **P0** |
| **TC-09b** | **AC-1/E-1（LIST_FORMULA，判定为无法执行）** | — | **不执行**。原因：`§0` SQL 已核实现网 `cpq_db_0724` 全库 0 条 `LIST_FORMULA` 字段，无真机夹具可用 | 需在 `test-report.md` 如实登记为"未覆盖"，**不得**用"理论上 `valuesReconcile` 对字段类型无分支判断，所以行为一致"这句话替代真机验证——那是**推断**不是**证据**。若之后有客户模板配了 LIST_FORMULA 字段，应作为本 repair 的补充回归项 | 已知不可执行 | — |
| **TC-10** | **AC-5** | 🔴 **原基线作废**（主线实测：`f57bfe0c-…` 已被第三方改写为 `md5=5eea1ced…`/23532 bytes，单据亦已 SUBMITTED）。**执行者须对 §0 的 DRAFT 主夹具 `b7077e71-…` 自采基线 B0′**：`psql -h 10.177.152.12 -U postgres -d cpq_db_0724 -tAc "SELECT md5(quote_card_values::text), length(quote_card_values::text), row_version FROM quotation_line_item WHERE id='b7077e71-9f7c-4035-9c47-24339a348b4c';"`（`PGPASSWORD=joii5231`） | **幂等回环验证，三步必须紧邻执行**：① 采 B0′ 指纹 → ② 把该 lineItem 某个已有单元格的**原值重新输入一遍**（no-op 编辑，触发 `quote-card-edit` 全量重建）→ ③ 立刻再采一次指纹。<br>⚠️ **不要用「改动前编辑一次 vs 改动后编辑一次」的写法** —— TC-07 会真的改「材料净重」的值，两次落库本就该不同，比不出任何结论。后端零改动、写库路径没变，**同输入必须同输出**，no-op 回环才是有效证法。<br>⚠️ **若 ①③ 之间 `row_version` / `quote_values_at` 出现本次操作无法解释的跳变**（＝第三方写入），本次结果作废、重跑 | ③ 的 md5 与 ① **完全一致**（`quote_values_at` 允许变，jsonb 内容不得变）。⚠️ **首次 no-op 编辑可能例外**：若该夹具仍存 12 位遗留值（见 §0 特殊性），第一次编辑会把整份重建成 9 位口径，md5 **必然改变**且属预期；此时以**第二次 no-op 编辑**（此时两侧都已是 9 位口径）的前后指纹作为 AC-5 判据 | | **P0** |
| **TC-11** | AC-5 | 承 TC-ENV-1；同一 `lineItem`（原夹具或备用夹具皆可，三视图须用同一份数据） | ① 报价单编辑页（`QuotationStep2`）「物料」页签 + SUBTOTAL 页签「报价」小计金额 ② 核价单视图（该 lineItem 已确认 `costing_card_values IS NOT NULL`，无需另找夹具）③ 详情页（`ReadonlyProductCard`） | 三处显示值与修复前截图逐字符一致，**含 SUBTOTAL「报价」页签的小计金额**（小计走 `formatProductCardSubtotal`，与 `valuesReconcile` 是不同函数，本条同时兼做"小计路径未被连带影响"的回归证据，不必再开一条专门用例） | | P1 |
| **TC-12** | 回归（跨页签累积，原稿描述笼统，本轮具体化） | 承 TC-ENV-1；用 TC-09 手法先在「产品」页签的「管理费」字段（componentId `91d73900-…`）制造一处**未移除**的真实差异 | 不移除该注入，切到「物料」页签（componentId `457603c5-…`）编辑任意字段触发一次对账上报 | 上报报文里「产品」页签的那条差异**仍在**（`reconcileDiffs` 按 `componentId::rowKey::fieldName` 前缀替换，只替换 `457603c5-…::` 开头的 key，不动 `91d73900-…::` 开头的）——验证"整页签替换、跨页签累积"语义未被本次改动破坏。可用 F12 Network 直接比对两次 `reconcile-report` 请求体的 `diffs[]` 长度不减反增 | | P1 |
| **TC-13** | 回归（非 DRAFT） | **现成夹具**：`QT-20260811-0170`（`f57bfe0c-…`）**当前恰好就是 `SUBMITTED`**（本轮审校 SQL 实测），可直接用，无需另找。若执行时它被重置回 DRAFT，改用 `QT-20260807-0156`（lineItem `a863b34b-5f3a-4ee0-8089-3a8dce4cb385`）等任一 SUBMITTED 单据 | 打开该单据详情/编辑页查看 FORMULA 列 | 走快照读取分支，值正常、无 ⚠、不触发对账（`useSnapEdit && !isDraft`） | | P1 |
| **TC-14** | 回归·错误码 | — | 构造上报体把 `frontendValue` 传成裸 JSON number | 后端仍返 **400**（`rejectNumericTokens` 契约未破；本次归一产物必须仍是 string——`formatFormulaResult` 返回值类型是 `DecimalString`，天然满足） | | P1 |
| **TC-15** | 权限 | — | **不适用**：改动位于前端判定函数，无鉴权分支；记录判定依据即可，不构造用例 | 文档说明 | | P2 |
| **TC-16** | 并发 | 承 TC-ENV-1 | 连续快速编辑同页签 3 格（不等上一次回灌） | 无异常；最终 pending 状态以最后一次上报为准；提交行为与最后一次一致 | | P1 |

---

## 4. 回归套件

| 编号 | 对应 AC | 命令 | 期望 | 实际 | 优先级 |
|---|---|---|---|---|---|
| **TC-17** | **AC-8** | `npx tsc --noEmit -p tsconfig.app.json` | **0 错误**（注意用 `tsconfig.app.json`，见 `BL-0085`） | | **P0** |
| **TC-17b** | AC-8 | 临时 Vite `curl /src/pages/quotation/QuotationStep2.tsx` | **200**，且返回内容**确含本次改动文本**（防 SPA fallback 假阳性；具体 grep `formatFormulaResult` 出现在响应体里） | | P0 |
| **TC-18** | AC-8 | `npm test`（`vitest run src`） | 全绿，**无新增失败**；特别关注 `precision.test.ts` / `formulaEngine.test.ts` / `formulaReconcile.test.ts` / 本次新增的 `valuesReconcile` 单测文件 | | **P0** |
| **TC-18b** | **AC-8** | `npx playwright test --config=e2e/playwright.config.ts e2e/quotation-flow.spec.ts --reporter=list` | 通过且 `'加载中' final count = 0`。<br>⚠️ **夹具风险已知**（`BL-0078`/`BL-0158`：该 spec 硬编码客户"西门子"/模板"报价模板0608"在现库可能 0 命中）。**若卡在选客户步骤，必须在干净 master 同环境跑一次做 A/B 对照**，确认是夹具枯竭而非本次回归，并在报告中**如实标注 BLOCKED**，不得含糊标"通过"。**不接受**只跑一次挂了就归因夹具——A/B 对照是强制动作，不是可选项 | | **P0** |
| **TC-19** | **AC-7** | `git diff` 三份文档 | `task-0810/需求文档.md:82` FR-12 已改写为「按结果精度 `FORMULA_RESULT_SCALE` 判定」；`task-0810/fronttask.md:69` 同步；`task-0806/需求文档.md` D4 对齐；三处均有演进史说明推翻原因 | | P1 |
| **TC-20** | 后端零改动 | `git diff --stat` 看 `cpq-backend/` | **0 文件改动**，`db/migration/` 0 文件 | | P0 |

---

## 5. P0 红线（任一不过即不得合并）

1. **TC-00 幂等 + TC-01 判一致** —— 方案成立的前提 + 本 BUG 的现场值不再误报（两条缺一，另一条单独过关都不能证明修复正确）
2. **TC-02b 第 9 位差判不一致** —— 阳性能力的最小边界不丢（比 TC-02 的粗粒度正例更硬，是本次审校新增的关键红线）
3. **TC-09 ①②两态** —— 阳性能力在 + 存在真实消解路径（E-4），不是靠重启绕过
4. **TC-08 未重启后端即可提交** —— 证明差异走的是正常消解链路
5. **TC-10 落库值逐字节不变**（须用正确的对应基线比对，见 TC-ENV-1 联动） —— 本次不改数
6. **TC-17 / TC-18 / TC-18b** —— 编译 + 单测 + E2E 三项自检齐全（E2E 若 BLOCKED 必须有 A/B 对照证据）
7. **TC-20 后端零改动** —— 与 `backtask.md` 结论一致

## 6. 已知不覆盖（须原样写进 `test-report.md`）

| 项 | 原因 |
|---|---|
| **真差异普查**（原 AC-6） | 用户裁决本次不做，另立任务。**因此本 repair 交付只能声明「结构性误报已消除」，不得声明「前后端已一致」** |
| 第 10~12 位真分歧的检出能力 | 本方案按设计不再检出（<5e-10，低于显示精度），属**已接受的代价**，非缺陷，见 TC-05c |
| **LIST_FORMULA 字段的真机验证**（本轮新增披露） | 现网 `cpq_db_0724` 全库 0 条该类型字段，无夹具，见 TC-09b。风险不为零：`valuesReconcile` 虽无字段类型分支，但未被真机证实过 |
| 多实例部署下的闸门有效性 | `ReconcileDiffStore` 是进程内 Map，task-0806 D8 已知限制，与本次无关 |
| `BL-0152`（非 DRAFT 编辑 400 被前端静默吞） | 既有缺陷，不在本次范围；但本轮因夹具漂移撞见了它的触发条件，测试时须能分辨"这是已知缺陷"而非新问题 |

---

## 7. 用例评审记录（测试工程师，2026-08-13）

### 7.1 订正的问题

1. **TC-02 小数位描述错误**：原文写"第 8 位差"，逐位核算后实际是第 6 位（`63.211125158` vs `63.211126158`，差异在小数点后第 6 位，量级 1e-6）。已订正描述并保留原数值——这是标注错误，不是用例设计错误，但如果不订正，执行者按位置核对会对不上，可能误判"用例本身有问题"而浪费排查时间，或者更糟——按照错误的位置描述去构造别的数据，导致真正没验证到"位置 6~9 之间"这段区域。
2. **TC-10 前一版已由技术总监订正为幂等回环写法**（不再是"改动前 vs 改动后各编辑一次"的比较，那种写法证不出任何结论，因为两次编辑本来就会因为写入了不同的"材料净重"值而产生不同的落库结果）——本轮审校确认这个订正是对的，未再改动，只是把口径说明写得更完整（"本条须用同一份先采集自己的 B0'"这句是我加的，覆盖"夹具漂移后 TC-10 怎么办"这个原稿没考虑到的分支）。

### 7.2 新增的用例，各自补的盲区

| 新增用例 | 补的盲区 |
|---|---|
| TC-ENV-1 | 原稿假设 `QT-20260811-0170` 恒为 DRAFT，未做状态自检。本轮 SQL 实测它已变成 `SUBMITTED`，若不查直接执行 TC-07 会在编辑请求上先撞 `BL-0152`（400 被静默吞），执行者大概率会把"编辑没反应"误判成新 bug，而不是"夹具选错了" |
| TC-00 | 方案成立的前提（`formatFormulaResult` 幂等）此前只被"隐含"在 TC-01/TC-05 的推导里，从未被单独断言。TC-01 通过不能排除"两侧被同时错误移动到同一个错值"这种巧合性通过 |
| TC-00b | 防止实施时把 F1（归一）和 F3（只加注释不改默认容差）搞混，手滑把 `isWithinTolerance` 默认容差也改了——那样会波及系统里所有其它调用方，是`fronttask.md`明确禁止的 |
| TC-01b | 负数镜像，`toDecimalPlaces` 对负数舍入方向是历史上容易出错的地方，原稿全部用例都是正数 |
| TC-02b | **本轮认为最关键的新增**：原稿在"能判一致"（TC-01，第 10~12 位差）和"能判不一致"（TC-02，第 6 位差）之间留了一大段没人测过的区域，唯独没测**最紧邻边界的第 9 位差**。如果实现时 scale 参数误写成 8（而非 9），TC-01/TC-02 仍然全过，只有卡在第 9 位的用例才能抓出这类"差一个数量级"的实现错误 |
| TC-05b/c/d | 原稿完全没有极大值/极小值/零值覆盖。TC-05c 尤其重要——它把"方案裁决书 §5 接受的误差代价（<5e-10 判一致）"钉成了一条可回归的断言，而不是只停留在文档里的一句话，防止将来有人把这个"已知且接受的行为"当 bug 误修 |
| TC-06c | 原稿"非数字"分支只测了字符串 `'abc'`，没测过 JS number 类型输入。`precisionValue` 对 number 类型有专门的 `return null` 分支，这条防线如果被本次改动意外接管，会掩盖上游 `rejectNumericTokens` 本该拦住的契约违规 |
| TC-07b | 原稿 TC-07 只测了「物料」页签本身，未测跨页签 cross_tab_ref 字段（「产品」页签「管理费」）。核实后发现这份夹具天然自带一个真实的跨页签公式，不用另找数据，成本很低但原稿漏了 |
| TC-12 | 原稿这条（当时编号也是 TC-12）只写了"上报报文仍是跨页签全量"这句结论性描述，没给出可执行的具体步骤（用什么字段制造差异、怎么验证没被冲掉）。本轮用「产品」的管理费 + 「物料」的任意字段这组具体夹具把它坐实 |
| TC-09b | 原稿完全没提 LIST_FORMULA。问题说明.md 明确要求"每类都要在真机上确认 ⚠=0"，但现网这个类型 0 条数据，只能诚实标记"无法执行"，不能假装测过 |
| TC-11 的 SUBTOTAL 部分 | 原稿的"回归面 6"提到过"小计/合计列走另一条求值路径"，但没有落成任何具体用例。本轮把它并入 TC-11（同一次截图顺手覆盖），没有另开一条用例造成冗余 |

### 7.3 AC → 用例覆盖矩阵

| AC | 覆盖用例 | 备注 |
|---|---|---|
| **AC-1**（复现路径 ⚠=0） | TC-07（物料页签，逐字段核对）、TC-07b（跨页签管理费字段） | TC-07 同时覆盖普通 FORMULA / 条件公式 / BOM 树拓扑三类 |
| **AC-2**（提交不再 409，未重启） | TC-08 | |
| **AC-3**（真差异仍阻断，阳性能力不丢） | TC-02（单测粗粒度正例）、TC-02b（单测边界正例，红线级）、TC-09①（真机注入） | |
| **AC-4**（单测锁边界） | TC-00、TC-00b、TC-01、TC-01b、TC-02、TC-02b、TC-03、TC-04、TC-05、TC-05b、TC-05c、TC-05d、TC-06、TC-06b、TC-06c | |
| **AC-5**（三视图 + 落库逐字节不变） | TC-10（落库 md5 幂等回环）、TC-11（三视图+小计截图） | 均依赖 TC-ENV-1 先确认夹具 |
| ~~AC-6~~ | — | 用户裁决不做，另立任务，§6 已登记 |
| **AC-7**（文档改写留痕） | TC-19 | |
| **AC-8**（编译/单测/E2E 自检） | TC-17、TC-17b、TC-18、TC-18b | |
| **E-4**（可消解路径，AC 未单独编号但 §3 需求结果硬性要求） | TC-09②（承 TC-09①，同一条用例两态） | 原稿只把 TC-09 映射到 AC-3，本轮补上 E-4 映射 |

### 7.4 判断为无法执行的用例

| 用例 | 原因 | 处置 |
|---|---|---|
| TC-09b（LIST_FORMULA 真机验证） | 现网 `cpq_db_0724` 全库 0 条 LIST_FORMULA 字段（SQL 已核实，见 §0），无夹具可用 | 保留在用例清单中但标"已知不可执行"，`test-report.md` 须原样登记，不得用"同一函数无类型分支所以理论一致"代替真机证据 |
| `quotation-flow.spec.ts`（TC-18b）**若**卡在选客户步骤 | `BL-0078`/`BL-0158` 已知夹具枯竭风险（硬编码客户"西门子"/模板"报价模板0608"，现库可能 0 命中）——这是**条件性**不可执行，不是确定不可执行，需执行时先跑一次才知道 | 若命中，必须在干净 `master` 同环境跑一次 A/B 对照后才能定性为"夹具问题、非本次回归"，不得直接标 BLOCKED 了事 |

---

