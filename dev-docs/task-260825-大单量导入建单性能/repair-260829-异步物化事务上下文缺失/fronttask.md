# fronttask · repair-260829 —— **前端零改动**

## 为什么不改

本缺陷全部发生在后端建单物化的后台线程内。前端行为**已经是正确的**：

| 前端环节 | 现状 | 判定 |
|---|---|---|
| `POST /v6/quote/create-quotation` | 收到 `materializing=true` 后轮询 `ensure-card-values` / `materialize-status` | ✅ 正确，无需改 |
| 轮询终止条件 | `ready == total` → `done` | ✅ 空数据下 `ready` 也会达标（卡片值 JSON 存在、只是内容空），**这不是前端 bug**——它信任后端的就绪信号，而后端给了错误的信号 |
| 编辑页渲染 | `baseRows` 为空数组 → 渲染空表格 | ✅ 正确响应空数据 |
| 金额显示 | Σ 空数据 = 0 | ✅ 正确 |

**结论**：修好后端后，前端无需任何改动即可正确显示。

## 回归确认清单（前端不改代码，但必须确认这些没坏）

| 项 | 服务的 AC | 确认方式 |
|---|---|---|
| 建单后进度条与轮询 | AC-1 | 建单流程走完，抽屉正常跳转编辑页，无卡死 |
| 编辑页 5 个页签渲染 | AC-1 | 各 driver 页签有非空行，小计与总价非 0 |
| 分页与料号查询 | AC-6 | `task-260825-报价单大单量分页与料号查询` 的成果不受影响（1845 行仍分页渲染，默认 10/页） |
| 编辑—保存—刷新 | AC-9 | 序列 AC，前端参与但不改代码 |

## 二期触发条件（本次不做，满足则另立）

- 若 B-4 的 `warnings` 需要在**前端显式提示**（当前只进 `CommitResult.warnings`，前端是否展示未确认）→ 届时补一条前端任务。
  本次不做的理由：`warnings` 的前端消费链路属 `task-260825` 既有设计，**改它需先确认现状**，不在返修范围。
