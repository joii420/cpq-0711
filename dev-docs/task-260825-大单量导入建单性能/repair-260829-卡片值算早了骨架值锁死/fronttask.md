# fronttask · repair-260829 卡片值算早了 —— **本线前端零改动**

## 为什么不改

前端确实是**触发源**（自愈误判 + 四处无条件 warm），但那部分归**并发会话** `修复draft超时问题` 的 F-4，不在本线范围。两线分工：

| 改动 | 归属 | 说明 |
|---|---|---|
| 自愈判据加 `!materializeInProgress`（`quotationService.ts:329`） | **对方 F-4** | 需消费本线 B-4 的 `MaterializeRegistry` |
| `materializeStatus` 响应加字段暴露 registry | **对方 F-4** | 后端 `QuotationResource`，也归对方 |
| 四处无条件 warm（`QuotationWizard.tsx:626/633/816/821`） | **待定** | 对方尚未表态；**本线 B-1 的产物自检能兜住它们**，故不阻塞本次交付 |
| ③步落库前产物自检 | **本线 B-1** | 纯后端 |

**结论**：本线交付后，前端**无需任何改动**即可消除骨架值锁死 —— B-1 从落库这一层堵死，无论谁在什么时机触发计算。对方的 F-4 是**更靠前一层**的优化（少一次白触发的计算），两者互补但不互相阻塞。

## 回归确认清单（前端不改代码，但必须确认这些没坏）

| 项 | 服务的 AC | 确认方式 |
|---|---|---|
| 导入建单 → 自动跳转 → Step2 各页签有数据 | AC-1 | 真机走一遍完整导入建单流程 |
| 轮询语义不变（进度条正常推进、不卡死） | AC-9 | 观察抽屉进度条，`ready/total` 正常递增至完成 |
| 打开已就绪的单不触发全量重算 | AC-7 | 打开一张卡片值已就绪的 1845 行单，确认无长时间等待 |
| 序列：建单→跳转→立即打开→等完成→切走切回→刷新 | AC-11 | 真机序列操作 |

## 二期触发条件（本次不做，满足则另立）

- 若对方 F-4 落地后仍有骨架值产生（说明还有第五个触发点），需回来加前端防护
- 四处无条件 warm 是否也该加 `materializeInProgress` 判断 —— 等对方 F-4 的实测数据再定；本线 B-1 已兜住正确性，那属于**性能优化**（省掉物化期间的无效计算）而非正确性修复
