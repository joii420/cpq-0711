# 大单量导入建单性能 —— 建单后置物化 N+1 致事务超时

> **任务性质**：新建主任务（用户 2026-08-25 裁决）。本任务内容 100% 为缺陷收敛，故立项文档采用
> `docs/rules/task-docs.md §5` 的返修六节格式，不另写 `需求文档.md`。
>
> **为什么新建主任务而不是挂 repair-**：本缺陷跨两个既有主任务且不属于其中任一 ——
> 症状发生在 `task-260712-报价单中的核价单数据展示问题修复` 引入的建单物化编排
> （`CreateQuotationMaterializer`，提交 `5b4ceca7`）上，但缺陷本体由 `repair-0803-snapshot`
> （提交 `daa95eaf`）引入，**而后者走的是路径 B、没有任务目录**。
>
> **关联主任务**：`task-260712-报价单中的核价单数据展示问题修复`（建单物化链路的所有者）、
> `task-260708` / `task-260709`（V6 导入落库与版本升级，触发场景的上游）
>
> **本次范围**：**D-1 + D-3 两处 N+1 + D-4 事务拆批 + D-5 建单异步化**（三次扩范围，均经用户裁决）。
>
> 🔴 **第三次扩范围（2026-08-26，用户真机测试后）**：后端已修好（1845 行建单跑通、卡片值 1845/1845 全落库、
> ARJUNA 0 次），**但用户实测体感毫无改善** —— 前端 axios 在 **30.01s** cancel 掉请求，
> 而整单要 **132s**。用户原话：「创建报价单依然 30 秒超时失败」。**这个判断是对的**：
> 修好的是「数据会丢」，没修「界面会报超时」。故纳入 **D-5：建单改异步 + 轮询**。
> ⚠️ 本次首度触及**接口契约变更**与**前端改动**（前两次扩范围都只在后端内部）。
>
> 🔴 **第二次扩范围（2026-08-25 亲验后）**：D-1+D-3 修复后实测通过（AC-1①②/AC-2 全绿），
> **但 AC-9 四步埋点暴露 ③ `ensureCardValues` = 58,679ms，距 Narayana 60s 硬上限仅剩 1.3s（余量 2%）**。
> 评审预估的「阈值推到 ≈3700 行」**被实测证伪** —— ③ 每行实际 31.8ms（58679÷1845），
> 真实阈值 ≈**1887 行**。本单 1845 行用掉 **97.7%** 事务预算，下一单再多 40 行即可能重新撞穿，
> 且失败模式是**静默数据损坏**（卡片值全量作废）。故纳入 **D-4：把 ③ 拆成分批独立事务**。
> D-2（导入 sheet 27.2s CPU 热点）**根因未定位，不进本次实现范围**，见 §④ 附记与 `BL-0182`。
>
> 🔴 **重要更正（2026-08-25，独立评审 `cpq-architect` 触发）**：
> 本文档初稿把根因单独归给 D-1，并称其「吃掉 Narayana 60s 事务预算的 45%」。**该表述已被代码证伪** ——
> `snapshotLines` **不带事务**，D-1 的耗时只烧墙钟、不占任何事务预算。真正被 reaper 杀掉的是
> **第 ③ 步 `ensureCardValues`**，其中另有一处**同型且更大**的 N+1（D-3）。
> 只修 D-1 **不会**让 AC-1/AC-2 变绿。详见 §④。

---

## ① 问题描述

**用户原话**（2026-08-25）：

> 「我马上要导入一个大笔订单，可能会超时」
> 「`http://localhost:5174/api/cpq/basic-data-import/v6/quote/create-quotation` 接口直接 30 秒超时」

**客观现象**（三者同时成立，缺一会误判）：

1. 浏览器侧：请求在 **30 秒**整被掐断，前端报超时。
2. 后端侧：**不报 500、不报业务异常**，接口最终返回 `200`，body 里带
   `"cardValuesReady": false, "warnings": ["卡片值物化失败: Error invoking subclass method"]`。
3. 数据侧：报价单与 1845 条明细行**已落库**（不丢单），但
   `quotation_line_item.quote_card_values` **1845 行全部为 NULL**，且**不会自愈** ——
   重试多少次都是同样结果。

> ⚠️ **这个组合极易被误判成「还在后台跑，等等就好」**。实际是跑到 60s 被事务 reaper 强杀、
> 整段计算作废。本次排查一开始也做过这个错误判断，靠 `ARJUNA012117` 日志才纠正。

---

## ② 复现 / 发现方式

**谁怎么发现的**：用户在导入一笔大订单前预告可能超时，要求监控后台日志；导入后报接口 30s 超时。

**环境**：
- 后端：本地 Quarkus dev，`8081`，**默认 profile** → `10.177.152.12:5432/cpq_db_0724`
- 前端：Vite dev，`5174`，`/api` 代理到 `localhost:8081`
- ⚠️ **DB 不在本机**，`ping 10.177.152.12` RTT 均值 **15.763 ms**（min 8.8 / max 29.3）—— 这是放大因子

**前置数据**：

| 对象 | 值 |
|---|---|
| `import_record` | `f1bb5b7d-065c-4c0c-98ae-f40ac04ea40e`（`1800笔产品订单.xlsx`，`total_rows=11536`，`SUCCESS`） |
| `customer_id` | `1f5818d8-b934-44be-b1c3-47df7053cff4` |
| 产出 `quotation` | `1ed36b62-cb35-4f88-82e8-9456e496544d`，**1845 条 line item** |
| 组件数据 | `quotation_line_component_data` **7380 行**（1845 × 4 组件），`row_data` 全部非空，合计 **1902 kB** |

**操作步骤**：

1. 走 V6 报价基础数据导入，导入上述 xlsx（Step 1，该步已是异步 + 轮询，**不是本缺陷**）
2. 导入完成后触发 Step 2 建单 → `POST /api/cpq/basic-data-import/v6/quote/create-quotation`
3. 观察：30s 前端超时；60s 后端日志出现 `ARJUNA012117 TransactionReaper`

**命令行等价复现**（本次实测用的就是这条，幂等、不重复建单）：

```bash
curl -s --noproxy '*' -b cookie.txt -H 'Content-Type: application/json' \
  -d '{"importRecordId":"f1bb5b7d-065c-4c0c-98ae-f40ac04ea40e",
       "customerId":"1f5818d8-b934-44be-b1c3-47df7053cff4",
       "name":"1800笔产品订单"}' \
  http://localhost:8081/api/cpq/basic-data-import/v6/quote/create-quotation
```

**出现频率**：**必现**。行数够多即触发，与数据内容无关。

**触发阈值（估算，未逐点实测）**：单次 `loadRowDataByComp` 往返实测 ≈14.6 ms
（200 次 2.91 s），物化其余步骤约占 20~30 s，故 line item 数超过约 **1200~1400** 即有超 60s 风险。

---

## ③ 需求结果

| 编号 | 期望 | 对照 |
|---|---|---|
| **E-1** | 1845 行建单时，**第 ③ 步 `ensureCardValues` 的事务在 60s 预算内完成**，不触发 `TransactionReaper` | AC-1 |
| **E-2** | 物化完成后 `quote_card_values` 与 `costing_card_values` **两侧均 1845 行非 NULL**，接口返回 `cardValuesReady: true`、`warnings` 为空 | AC-2 |
| **E-3** | **两处**逐行查询（D-1 `loadRowDataByComp` / D-3 `loadFrozenQuoteTabs`）的执行次数与 line item 数**无关**，满足 `backend.md` N+1 硬指标 | AC-3 |
| **E-8** | 第 ④ 步 `ensureExcelValues`（同为单事务包住全部行，**评审标注未测**）在修复后**不成为新的墙** | AC-9 |
| **E-10** | 🔴 **③ 的单个事务时长与总行数解耦** —— 分批提交，单批时长恒定，不再随 N 线性增长撑向 60s 上限 | AC-11 |
| **E-11** | 🔴 分批带来的**部分失败可恢复**：中途失败时已提交批的卡片值保留，未完成行仍为 NULL，重跑靠 `IS NULL` 谓词只补未完成行（自愈），不重算已完成行 | AC-12 |

**反向期望（防止「修成把告警关了」）**：

| 编号 | 仍必须成立 | 对照 |
|---|---|---|
| **E-4** | `repair-0803-snapshot`（`daa95eaf`）修的行为不许回退 —— 重展开路径落库前仍须盖回既有 INPUT 键（含用户显式清空的 `""`） | AC-4 |
| **E-5** | 落库的 `row_data` / `snapshot_rows` / 两侧卡片值与改动前**逐位等价**（本次只改「怎么取数」，不改「取到什么」） | AC-6 |
| **E-6** | 物化真失败时**仍要报**：`cardValuesReady=false` + `warnings` 带原文的降级路径保持不变 | AC-5 |
| **E-7** | 单行 / 小单量路径（加产品、编辑失焦同步、非 `wholeBatchEnabled` 分支）行为不变 | AC-7 |
| **E-9** | `b6e86a18`（卡片值按冻结结构算）修的行为不许回退 —— 仍须**优先用冻结结构**算值，三级降级链（冻结结构 → prefetch 模板快照 → 模板表）语义不变，不得退回「同卡双值」 | AC-10 |

## ④ 问题原因 / 报告

### 根因：两处独立的 N+1，各自作用于不同的失败面

**这不是一个缺陷，是两个。** 它们叠在同一条链路上，但**只有第二个会导致失败**：

| | **D-1**（初稿唯一认定的） | **D-3**（评审发现的真凶） |
|---|---|---|
| 位置 | `ConfigureSnapshotService.loadRowDataByComp`（`:1144`，SQL 在 `:1152`） | `CardSnapshotService.loadFrozenQuoteTabs`（`:1784`，SQL 在 `:1789`） |
| 循环体 | `snapshotLines` 逐行循环（`:610` → `computeRowDataFromSnap:1181`） | `snapshotNewLinesCardValues` 的 **Pass1 逐行循环**（`:603` → `buildCardValues:1634`） |
| 所在步骤 | ① `snapshotQuotation` | ③ `ensureCardValues` |
| **事务** | 🟢 **无事务** —— `ConfigureSnapshotService` 类 javadoc 原文：<br>「**本协调方法 `snapshotLines` 不带事务**」；`:141`/`:246`/`:1144` 均无 `@Transactional`；`CreateQuotationMaterializer` 类 javadoc 亦明写「**本类不加 @Transactional**」 | 🔴 **`@Transactional`（`CardSnapshotService:1055`），单个事务包住全部 1845 行** |
| 单次往返实测 | 200 次 **2.91 s / 3.19 s**（两次测量） | 200 次 **3.34 s / 3.49 s**（评审与主线各测一次） |
| 1845 行外推 | ≈ **27~29 s** | ≈ **31~32 s** |
| **后果** | 只烧**墙钟**，用户等得久 | **撑爆 60s 事务预算 → reaper 强杀 → 卡片值全量作废** |
| 取数性质 | 按 `lineItemId` 取，**逐行确实不同** | 按 `quotationId` 取，**整单恒定值**，逐行查纯属浪费 |

**一句话根因**：`ensureCardValues` 在一个事务内逐行调用 `loadFrozenQuoteTabs(li.quotationId)` ——
一个**整单恒定**的查询被放进逐行循环，1845 次往返 ≈31 s，叠加逐行 `buildCardValues` /
`buildCostingCardValues` 的 CPU 与树渲染后超出 Narayana 60s 预算，事务被 reaper 强杀
（`ARJUNA012117`），`quote_card_values` 与 `costing_card_values` **两侧同时**全量作废且不自愈。

D-1 是同一族的另一处浪费，**不致命但同样违反 `backend.md` 的 N+1 硬指标**，本次一并修。

> 🔑 **两者是同一个模式的两次发作**：都是**一次正确性修复，把 per-row 读加进（或换掉）一个已经优化过的循环**。
> D-3 尤其典型 —— 它的 diff 是把既有的整单 prefetch **直接替换**成了逐行查库：
> ```diff
> - JsonNode snapshot = (prefetch != null) ? prefetch.templateSnapshotById.get(templateId) : null;
> + JsonNode snapshot = loadFrozenQuoteTabs(li.quotationId);
> ```
> 副作用：`prefetch.templateSnapshotById` 这个整单预取被彻底架空，等于把上一轮的优化白做了。

### 两条调用链

**D-3（真凶，在事务内）**
```
BasicDataImportV6Resource.createQuotation(:152)
  └ CreateQuotationMaterializer.materialize(:43)          ← 本类不加 @Transactional
      └ CardSnapshotService.ensureCardValues(:1056)       🔴 @Transactional(:1055) 单事务包住 1845 行
          └ snapshotNewLinesCardValues(:571)
              └ Pass1 逐行循环(:603)
                  └ buildCardValues(:1634)
                      └ loadFrozenQuoteTabs(quotationId)  ← 每行一次，取的却是整单恒定值
                          └ SELECT structure FROM quotation_view_structure
                            WHERE quotation_id=:qid AND view_kind='QUOTE_CARD'   (:1789)
```

**D-1（同族浪费，无事务）**
```
CreateQuotationMaterializer.materialize(:41)
  └ ConfigureSnapshotService.snapshotQuotation(:134 → :145)   🟢 无 @Transactional
      └ snapshotLines(:610)                                    🟢 类 javadoc 明写「不带事务」
          └ computeRowDataFromSnap(:1168)
              └ overlayExistingInputKeys(..., loadRowDataByComp(lineItemId))   ← :1181 循环内查库
                  └ SELECT component_id, row_data FROM quotation_line_component_data
                    WHERE line_item_id=:lid AND row_data IS NOT NULL            (:1152)
```

### 证据链（全部可复跑）

**证据 1 —— DB 监控直接抓到「事务里正在跑 D-3」（本案最硬的一条）**

排查期间挂着的 `pg_stat_activity` 轮询监控，在两次不同的运行里各推出一条：

```
[DB] 事务中空闲 42s pid=2138930 :: SELECT structure FROM quotation_view_structure WHERE quotation_id = $1 AND view_kind = $2
[DB] 事务中空闲 60s pid=2139149 :: SELECT structure FROM quotation_view_structure WHERE quotation_id = $1 AND view_kind = $2
```

该 SQL **逐字就是 `loadFrozenQuoteTabs:1789`**，且出现在一个**已存活 42s / 60s 的事务**里
（`idle in transaction` = 该刻 PG 未在执行，应用正在 Java 侧算值 → 正是逐行 `buildCardValues`）。

**证据 2 —— 事务被砍后的失败刷屏，点名同一个方法**

```
[card-snapshot] loadFrozenQuoteTabs failed q=1ed36b62-...: Unable to acquire JDBC Connection
                [Failing fast as the transaction has rolled back]
```
以 ≈1 ms 间隔连续刷屏（数千条）。这是 reaper 杀掉事务后 Pass1 循环**仍在逐行往下跑**、
每行再撞一次已死事务的直接证据 —— **同时坐实「该调用位于逐行循环内」**。

**证据 3 —— 事务被强杀的日志原文**（`17:46:33`）

```
ARJUNA012117: TransactionReaper::check processing TX 0:ffff7f000101:b36f:6a8e35c1:12b in state RUN
ARJUNA012095: Abort of action ... invoked while multiple threads active within it
ERROR [CreateQuotationMaterializer] [create-quotation] 后置物化失败 quotation=1ed36b62-...（不丢单，前端 warm 兜底）
Caused by: jakarta.transaction.RollbackException: ARJUNA016102: The transaction is not active!
```
堆栈顶帧为 `CardSnapshotService_Subclass.ensureCardValues` → `CreateQuotationMaterializer.materialize(:43)`
—— **抛点在 ③，不在 ①**。

**证据 4 —— A/B 基准实测（同库、同数据、各 200 次）**

| 复刻对象 | 200 次耗时 | 单次 | 1845 行外推 |
|---|---|---|---|
| `loadRowDataByComp:1152`（D-1） | 2.91 s / 3.19 s | ≈14.6 ms | ≈27~29 s |
| `loadFrozenQuoteTabs:1789`（D-3） | 3.34 s / 3.49 s | ≈17 ms | ≈31~32 s |
| 合并成 1 次 `IN`（D-1 对照组） | **0.16 s** | — | ≈1.5 s |

两组由评审与主线**各自独立测过一次**，控制组数字互相校准通过（2.91 vs 2.90）。

**证据 5 —— jstack 采样（⚠️ 附重要限定）**

6 次采样中 5 次落在 `loadRowDataByComp:1152`，阻塞于 `sun.nio.ch.Net.poll`。

🚩 **这只证明 D-1 是采样时刻的墙钟热点，不证明它是失败原因。** 初稿据此把根因单独归给 D-1，
是本次排查最大的方法论失误：**jstack 找的是「时间花在哪」，失败原因要看「哪个事务超了预算」** ——
两者可以落在完全不同的步骤上，本例正是如此（D-1 无事务）。

**证据 6 —— 两侧卡片值同时全空，只能由「③ 整体作废」解释**

实测 `quote_card_values` 与 `costing_card_values` **均为 1845 行全 NULL**。
若瓶颈在 ①，则 ②③④ 会照常执行并写入卡片值；只有 ③ 的事务被整体回滚才会两列同时全空。

**证据 7 —— 30s 与 60s 是两道独立的墙**

- 30s：`cpq-frontend/src/services/api.ts:5` 的 `timeout: 30000`（axios 全局）。Vite proxy 与后端均未设超时。
- 60s：Narayana 默认事务超时（`application.properties` 未显式配置）。

> 🚩 **只放宽前端 30s 是无效修法** —— 60s 那道墙照撞，且撞穿后数据作废。本次不动 `api.ts`。

> ⚠️ **初稿「证据 4」的全称否定已撤销。** 初稿曾以「物化窗口内 `com.cpq` 日志仅 23 行、
> `SqlViewExecutor` 仅 4 次」推出「**除本条外该链路不存在 N+1**」。
> **该推论不成立** —— 这台仪器只看得见带日志的调用，而**裸 `em.createNativeQuery` 完全不打日志**；
> D-1 与 D-3 恰恰都是裸 native query，两者都在它的盲区里。
> 🔑 **教训**：不要用「日志里没看到」证明「不存在」。

### 引入时间线

| 时间 | 提交 | 事件 |
|---|---|---|
| task-0712 | `5b4ceca7` | 引入 `CreateQuotationMaterializer`，建立建单后置物化四步编排 |
| —— | —— | 期间两个循环**分别**被批量化：`snapshotLines` 写侧收口为 `writeSnapshotBatchAllLines`/`writeRowDataBatchAllLines`（注释原文「替代每行的 **N×REQUIRES_NEW**」）；`snapshotNewLinesCardValues` 建立 `prefetch` 整单预取 + 两遍 build-then-assign |
| **2026-07-31** | **`b6e86a18`** | **`fix(card-values): 卡片值改按冻结结构算 —— 消除「卡片小计 214 / 报价总额 14」同卡双值`** → 引入 **D-3**。**该提交把既有的整单 prefetch 查询直接换成了逐行 `loadFrozenQuoteTabs`**，`prefetch.templateSnapshotById` 从此被架空 |
| **2026-08-03** | **`daa95eaf`** | **`fix(repair-0803-snapshot): 重展开路径落库前盖回既有 INPUT 键`** → 引入 **D-1**。为 `overlayExistingInputKeys` 新增配套取数，取数被放进循环体内 |

**之前为什么不犯**：两次提交后的实际单据量级远小于 1845 行。库内**第二大的报价单仅 2 行** ——
1845 行是**孤例**，本次是该规模第一次出现，两处 N+1 同时显形。

> 🔑 **可复用教训（结案时按 `change-protocol.md §6` 提议升格为规则）**：
> `b6e86a18` 与 `daa95eaf` **都是正确性修复，逻辑本身都没错**。共同失误是
> **把 per-row 读放进了已经被专门批量化过的循环**，其中 `b6e86a18` 更是**直接替换掉既有的整单预取**。
> **规范建议**：在带有「批量化 / prefetch / 整单一次」字样注释的循环里新增或替换任何取数时，
> 必须先回答两个问题 —— ① **这个读的入参是逐行变化的吗？**（`loadFrozenQuoteTabs` 收 `quotationId`，
> 整单恒定，本就不该在循环里）② **它能不能提到循环外？**

### 影响面

| 面 | 说明 |
|---|---|
| **直接受害** | 大单量建单：报价 + 核价两侧卡片值全 NULL 且不自愈 |
| **D-3 额外受害面** | `ensureCardValues` 被多个入口调用（不止建单），任何触发整单重算卡片值的操作都吃这 ≈31 s |
| **D-1 额外受害面** | `snapshotQuotation`/`snapshotLines` 有 **5 个生产入口**（清单见 `backtask.md`），含 **saveDraft 热路径** |
| **数据安全** | **不丢单** —— 报价单、明细行、`snapshot_rows`、4 份结构快照均在更早的独立事务中已提交 |
| **金额正确性** | 卡片值为 NULL 时前端走 warm 兜底重算，**不会算出错误金额**，但用户侧表现为等待/空白 |
| **非受害** | Step 1 导入（`POST /v6/quote`）已是异步 + 轮询，不受影响 |

### 为什么测试没拦住

1. **等价性测试只守「算得对」，不守「算得快」** —— `OverlayExistingInputKeysTest`、
   `GoldenCardValuesEquivTest`、`CardValuesBatchPersistEquivTest` 全是**值等价**断言；
   N+1 不改变任何返回值，这些测试**必然全绿**。
2. **全工程没有任何「SQL 条数与行数无关」的断言** —— `backend.md` 的 N+1 硬指标目前**纯靠人工 review**。
   本次 AC-3 + T-1 就是来补这个洞的。
3. **测试数据量级不足** —— 既有夹具均为几十行，跑不出 60s 事务超时；且 `mvnw test` 走 `cpq_db`，
   1845 行的单在 `cpq_db_0724`，测试根本看不见它。
4. **本地 DB 若与应用同机则几乎无症状** —— 两处缺陷的严重度都与 DB RTT 线性相关（本环境 15.76 ms）。

### 附记：D-2，本次不修的另一个热点

同批导入日志中另有一条**独立**的性能问题，**根因未定位，按 §5「根因未定位不许进 A0」不进本次范围**：

```
[v6import] QUOTE sheet=客户料号与宏丰料号的关系 rows=1845 handle=27200ms writer{groups=0 dbCalls=0 | ...}
```

**27.2 秒，且 `dbCalls=0`（一次库都没打，纯 Java CPU）**。同批其余 sheet 对照：
`物料BOM` 1845 行 → 833 ms；`物料与元素BOM` 4153 行 → 1132 ms；`成品其他费用` 1845 行 → 305 ms。
量级差 **30~90 倍**，高度疑似该 sheet 的 handler 内含 O(N²)。属**导入步骤**（`QuoteImportService`），
与本任务的建单物化是两条独立链路。已登记 `BACKLOG.md`。

---

## ⑤ 修复方案

> 本节只记裁决通过的方案：**2026-08-25 闸门 A0 裁决方案甲（D-1）+ 同日独立评审后用户裁决扩范围纳入 D-3**。

**共同思路一句话**：两处都是「**把一个不该逐行做的读，提到循环外做一次**」。零契约变更、零 schema 变更、零迁移。

---

### 修复一 · D-3（优先级更高 —— 它才是让 AC-1/AC-2 变红的那个）

**做法**：把 `loadFrozenQuoteTabs(li.quotationId)` 从 `buildCardValues` 的逐行调用中提出来。
该查询的入参是 **`quotationId`，整单恒定** —— 循环内每次都查回同一份数据。

- 在 `snapshotNewLinesCardValues` 的 **Pass1 循环之前**（`CardSnapshotService:603` 之前）查一次，
  结果沿现有的 `prefetch` 通道或新增参数传入 `buildCardValues`。
- 🔒 **位置纪律（这次是真的适用）**：该文件 `:625-631` 的「Pass1.5 位置纪律」注释写明 ——
  Pass1 阶段实体必须保持**干净**，否则 native query 会触发 Hibernate `flush-before-query`。
  **提到 Pass1 之前天然满足该约束**（此刻尚未给任何托管实体赋值）。
- **保留回退语义**：`loadFrozenQuoteTabs` 返回 `null` 时回退 `prefetch.templateSnapshotById`
  再回退模板表查询（`:1635-1643`）的三级降级链**必须原样保留**，只是「取一次」而非「取 N 次」。
- ⚠️ **顺带修复一个被架空的优化**：`b6e86a18` 之后 `prefetch.templateSnapshotById` 实际已失效
  （被逐行查询短路）。本次提出循环后该预取重新生效。

**核价侧同查**：`buildCostingCardValues` 是否有同型的逐行整单查询（如 `COSTING_CARD` 对应的
`loadFrozenCostingTabs` 之类），**实现前必须确认**。⚠️ 本次评审**未覆盖核价侧**，
不要假定它干净 —— 若存在同型问题，一并提出（属同一 AC-3 覆盖，不算扩范围）。

---

### 修复二 · D-1

**做法**：把 `loadRowDataByComp` 的 per-row 读提到 `snapshotLines` 循环外，改为批量预载。

1. **新增**批量加载方法（暂名 `loadRowDataByLineComp(Collection<UUID> lineItemIds)`）：
   ```sql
   SELECT line_item_id, component_id, row_data
   FROM quotation_line_component_data
   WHERE line_item_id IN (:ids) AND row_data IS NOT NULL
   ORDER BY line_item_id, component_id, id
   ```
   **分块 200**（同文件 `writeRowDataBatchAllLines` 的 `CHUNK = 200` 范式）。
2. `computeRowDataFromSnap` 增加入参接收本行预载结果，内部不再调用 `loadRowDataByComp`；
   随后**删除** `loadRowDataByComp`（唯一调用点 `:1181`，已 `codegraph_impact` + `/usr/bin/grep -a` 双向确认无残留引用）。
3. **降级语义保持不变**：原 `try/catch` 记 warn 不中止整份快照的行为必须保留。

**🔒 六条强制落地约束（均来自 2026-08-25 独立评审的实测发现，缺一条即视为未完成）**：

| # | 约束 | 为什么 |
|---|---|---|
| C-1 | **预载集合收窄到 `lineNeedsExpand` 命中的行** | `snapshotLines` 的 `anyNeedsExpand` 段（`:319-332`）已逐行判过；`QuotationResource:154` 的 **saveDraft 增量热路径**下绝大多数行会在 `:447-455` 被 `continue` 掉、**根本走不到** `computeRowDataFromSnap`。不收窄 = 给热路径凭空加一次全量预载（本单 1902 kB）。**逐行懒查时这些行成本为 0，改批量后反而变贵 —— 这是本次改动唯一可能的性能倒退面** |
| C-2 | **IN 列表必须过滤 `null` 元素** | 现状 `:1145` 靠 `lineItemId == null` 提前返空 map 兜住；批量版没有这层保护，含 `null` 的集合绑定行为不确定 |
| C-3 | **SQL 必须带 `ORDER BY line_item_id, component_id, id`** | 表上**没有** `(line_item_id, component_id)` 唯一约束（只有 `pkey(id)` + `idx_qlcd_line`）。评审实测**全库存在 6 组重复键**。两侧都是 `Map.put` 覆盖 =「最后一行赢」，无 `ORDER BY` 时物理返回序不定 → 理论上可取到不同那行。加 `ORDER BY` 成本为零，把这条不确定性钉死 |
| C-4 | **预载只存原始 `String`，循环内对当前行 `readTree` 一次即弃** | 🚩 **撤销初稿「分块 200 后峰值更低」的错误说法** —— 分块只限制单次结果集，累积的 Map 要活到循环结束，**不降驻留峰值**。存 `JsonNode` 全量驻留约 **15~40 MB**（1902 kB 文本 × Jackson 8~20× 膨胀）；存 String 约 **3.8 MB**，降一个数量级 |
| C-5 | **预载结果视为只读，禁止喂给任何会原地改的下游** | 批量预载会让同一 `JsonNode` 实例被预载 Map 与 `freshRowData` **同时引用**（逐行查询天然每次新对象，无此问题）。当前无害（`overlayExistingInputKeys` 只读 `oldRow` 写 `freshRow`，且 INPUT 值是不可变 `ValueNode`），但这是逐行→批量转换最经典的 bug 面。**采纳 C-4 后该问题自动消失** |
| C-6 | **事务注解必须是有意识的选择，并在注释里写明理由** | `test.md T-4` 要求对标 `loadSnapshotRowsByLines`，而那个方法带 `@Transactional(REQUIRES_NEW)` + `self.` 调用。**照抄会把一个原本无事务的读变成开真事务的读**（`snapshotLines` 无事务，见 §④）。此处外层无事务、行为无害，但多借一次连接 + 多一次 begin/commit，**不能是复制粘贴的副产品** |

**实现者可选（不需要重新审批，属「循环前批量预载」范畴内）**：
`snapshotLines` 在进入循环前**已经**对同一张表、同一批 line id 打过整单 IN 查询
（`:308` 增量路径 / `:344` 非增量路径，建单走 `:344`）。可以给 `loadSnapshotRowsByLines`
加一个**姊妹方法**（多带 `row_data` 一列），复用这一次查询而非新增第二条。
- 收益：省 ≈10 次往返 ≈0.17 s —— **在 27 s 面前不值一提，真价值是结构性的**（少一处会与 `:308/:344` 漂移的 id 收集代码）。
- 🚫 **不得修改 `loadSnapshotRowsByLines` 自身的签名** —— `LoadSnapshotRowsByLinesEquivTest` 守着它。

---

---

### 修复三 · D-4（2026-08-25 亲验后扩范围）

**为什么必须做**：D-1+D-3 修完后 ③ 仍占 58,679ms / 60,000ms 预算。
**这 58 秒不是 N+1** —— SQL 条数护栏（T-1）已证明查询次数与行数无关。它是 ③ 内部
`buildCardValues` / `buildCostingCardValues` / `BomTreeRenderService.render` 在
1845 行 × 1845 distinct 料号下的**真实计算量**。

**做法**：把 `ensureCardValues` 从「单事务包住全部 N 行」改为「**按行分批，每批独立事务**」。

1. `ensureCardValues` 拿到待算行集合后**按 chunk 切分**（建议 chunk 默认 **300**，
   按实测 31.8ms/行 → 单批 ≈9.5s，余量 84%；chunk 应可配以便调优）。
2. 每批调用 `snapshotNewLinesCardValues` 时走**独立事务**（`REQUIRES_NEW`），批内提交、批间不共享事务。
3. **`prefetch` 保持整单一次**，在分批**之外**计算（它是只读预取，不应被拆进批事务）——
   ⚠️ 拆错位置会把 D-3 刚修好的「整单查一次」重新打回「每批查一次」。
4. 新增分批埋点：批次序号 / 每批行数 / 每批耗时，供 AC-11 断言。

**部分失败语义（明确接受，且比现状更好）**：
现状是 all-or-nothing —— 一处超时全单作废。分批后中途失败只损失未提交的批。
`ensureCardValues` 本身按 `IS NULL` 谓词选行（文档原注：「下次不再重选该行(自愈、不无限重算)」），
故重跑天然只补未完成行。**这是升级不是降级。**

**不做的部分**：🚫 **不改接口契约、不转异步轮询**（那是 `BL-0183` 的完整方案丙，本次只取「拆批事务」这一半）。

---

### 改哪些文件

| 文件 | 改动 | 对应 |
|---|---|---|
| `cpq-backend/.../quotation/service/CardSnapshotService.java` | `loadFrozenQuoteTabs` 提出 Pass1 循环；核价侧同型排查 | **D-3** |
| `cpq-backend/.../configure/service/ConfigureSnapshotService.java` | 新增批量加载；循环外调用；`computeRowDataFromSnap` 加参；删 `loadRowDataByComp` | **D-1** |
| `cpq-backend/.../quotation/service/CardSnapshotService.java` | `ensureCardValues` 按 chunk 分批 + 每批 `REQUIRES_NEW`；prefetch 留在分批外；分批埋点 | **D-4** |

**两个生产文件。** 无 API 变更、无 DB schema 变更、无迁移、无前端改动。

### 影响面

- `overlayExistingInputKeys` **签名与实现均不变**（静态纯函数），`OverlayExistingInputKeysTest` 6 例**不需改**且必须继续全绿 —— E-4 的守卫。
- `loadFrozenQuoteTabs` 的**三级降级链**（冻结结构 → prefetch 模板快照 → 模板表查询）语义不变。
- 落库内容**逐位不变**：本次只改「从哪里、查几次拿到数据」，不改「拿到之后怎么用」。
- ⚠️ **上层入口不封闭**：`snapshotQuotation`/`snapshotLines` 有 **5 个生产入口**（清单见 `backtask.md`），
  含 saveDraft 热路径 —— 这正是 C-1 存在的原因。
- **不碰** `materializeRowData` 非批量分支（`snapshotLines:613`，`wholeBatchEnabled=false`）。
- **不碰** `api.ts` 的 30s、**不碰** Narayana 60s 配置。
- 内存：采纳 C-4 后新增驻留约 **3.8 MB**。

### 是否构成协议级改动

**否。** 不涉及 `field_type` / 枚举 / 跨端契约，不触发 AP-44。改动全部在两个服务类的私有方法内，
无 API 变更、无 DB schema 变更、无迁移。**无需走 `change-protocol.md` 的 17 检查点。**

### 已否决备选

- **已否决：乙（谓词收窄，按需才查）** —— 在 saveDraft/刷新路径下不成立（那里的行确实已有 `row_data`），
  且 C-1 的「收窄到 `lineNeedsExpand` 命中行」已用更精确的方式拿到了乙想要的收益。
  ⚠️ **初稿对乙的证伪理由需修正时点**：初稿称「实测 7380 行 `row_data` 全部非空」——
  那是**跑挂之后**的库状态；`V6QuotationCommitService` 全文无 `quotation_line_component_data` 引用，
  **首次建单跑到该循环时这些行还没建**（由循环后的 `writeSnapshotBatchAllLines` INSERT），
  首跑时 1845 次查询**全部返 0 行**。结论（否决乙）不变，理由需按此修正。
- **丙（物化拆批 + 移出请求）—— 🔴 部分采纳（2026-08-25 亲验后改判）**：
  评审曾据估算认为不必做（「修完 D-1+D-3 阈值推到 ≈3700 行」），**该估算已被实测证伪**
  （③ 每行实际 31.8ms，真实阈值 ≈1887 行，本单已用掉 97.7% 预算）。
  故本次**取丙的前一半 = 拆批事务（D-4，见「修复三」）**，
  **不取后一半 = 移出请求线程 / 改异步轮询**（那涉及接口契约变更，仍留 `BL-0183`）。
- **已否决：只放宽前端 `api.ts` 的 30s** —— 60s 事务墙照撞且数据作废，属无效修法（见 §④ 证据 7）。

### ⚠️ 已知未消除的风险（诚实标注）

1. **④ `ensureExcelValues` 未测**。它同样 `@Transactional`（`CardSnapshotService:948`）、同样单事务包住 1845 行。
   D-3 修好后它可能成为**下一堵墙**。评审明确标注「未测」。**T-1 与亲验必须覆盖到它**。
2. **③ 的 CPU 侧未 profile**。两处 N+1 修完后实测 ③ 仍需 **58,679ms**（1845 行，每行 31.8ms），
   而 SQL 条数护栏（T-1）已证明查询次数与行数无关 → **那 58 秒是纯计算**，
   落在 `buildCardValues` / `buildCostingCardValues` / `BomTreeRenderService.render`。
   本次**不追它的来源**（范围内只拆 60s 悬崖），但下面两条线索已排查/取得，留给后续：

   **✅ 已排除**：`task-260819` 的 **B-19**（`BomTreeVarsContext` 注入 + BOM 闭包递归 SQL）
   **不是原因**。① 该改动已在 master 基线里（我们的 worktree 基于 `6629675f`，
   `ConfigureSnapshotService` 内已含 `// task-260819 B-19` 注释）→ 它是常量不是变量；
   ② 经该任务会话逐点核查，`collectTotalMaterialNoUnion` 在渲染链路的**全部 5 个调用点均为整单一次**
   （`CardSnapshotService:3484/3363/4176`、`QuotationService:1636`、`ConfigureSnapshotService:375`），
   且 `snapshotNewLinesCardValues` 的两个逐行循环体里调的是 `buildCardValues`/`buildCostingCardValues`，
   **完全不碰** `collectTotalMaterialNoUnion` → 那 58 秒里**没有 1845 次递归 CTE**。

   🔍 **待查线索（跨会话取得，2026-08-25）**：`task-260819` 的 **`D-58`** 改了
   `DataLoader` 的 outer 过滤器口径 —— `WHERE inner_q.hf_part_no = ANY(:hfPartNos)` 的绑定值
   由「根成品列表」**加宽为「整单料号池」**（合桶路径 `expandMulti`）。
   **SQL 条数未变（仍是常数），但单次取回的行数变多**，再由 Java 侧 `rootsByMaterial` 回分。
   该会话原话：当时裁决是在**常规单量**下评估的，「1845 行的极端单量不在当时的评估视野里」，
   并主动表示「如果 profile 确认是它，那是我们要认的账，会开返修」。
   **→ 后续若 profile 这 58 秒，第一个该看的是 `DataLoader.loadByPath` 的返回行数。**
3. **「被杀的是 ③ 的事务」有观测支撑但非逐帧证明**。证据 1/2/3/6 四条独立指向 ③，
   但没有对四步各打时间戳的埋点。**若开工后 D-3 修完 AC-1/AC-2 仍红，第一件事是补埋点重新定位，不要盲目继续优化。**

## ⑥ 验收标准

> 亲验一律在**用户实际使用的环境**（dev server + `cpq_db_0724` + 上述真实数据）、走 **UI 完整路径**执行。
> 🚫 每条 AC 必须附**可复核证据**，禁止只写「✅ 通过」；空列表 / 0 行 / `—` 一律不算通过。

### 阴性 —— 原路径不再复现

**AC-1（单点 · 判据先测后定）** 前置：`import_record = f1bb5b7d-...`、`customer = 1f5818d8-...` 的 1845 行数据。
操作：触发 `POST /quote/create-quotation`。
断言（**硬判据，必须成立**）：
① 后端日志**不出现** `ARJUNA012117` / `ARJUNA016102`；
② 响应体 `cardValuesReady: true` 且 `warnings` 为空数组；
③ 记录并公布本次**实测总墙钟**。

> 🚦 **判据先测后定（2026-08-25 用户裁决）**：初稿写的「30 秒内返回」经评审判为**高风险不可达** ——
> 即使 D-1+D-3 全修，总墙钟估算仍在 **≈32 s** 上下。故本条**不预设秒数阈值**：
> 先由 ③ 拿到实测值，再由主线与用户**共同确定**写死的阈值（并据此决定要不要单独给该调用放宽前端超时）。
> 🚫 **不许拿一个拍脑袋的数当验收线**，也🚫**不许因为达不到 30s 就把 ①② 判为通过**——①② 是硬的。

**AC-2（单点）** 同上请求完成后查库：
```sql
SELECT count(*) FILTER (WHERE quote_card_values IS NULL)   AS 报价空,
       count(*) FILTER (WHERE costing_card_values IS NULL) AS 核价空,
       count(*) AS 总行数
FROM quotation_line_item WHERE quotation_id = '1ed36b62-cb35-4f88-82e8-9456e496544d';
```
断言：**报价空 = 0，核价空 = 0，总行数 = 1845**。（改动前实测为 1845 / 1845 / 1845）

**AC-3（单点 · N+1 硬指标 · 覆盖两处）** 前置：**N=5** 与 **N=50** 行的报价单各一张
（`cpq_db` 测试库内动态取，不硬依赖固定 ID）。
操作：对每张分别跑整单物化路径，用 Hibernate `Statistics` 分别统计：
（a）对 `quotation_line_component_data` 的读取条数（**D-1**）；
（b）对 `quotation_view_structure` 的读取条数（**D-3**）。
断言：
① （a）≤ ⌈N/200⌉；（b）**≤ 4**（四类 `view_kind` 各一次，与 N 无关）；
② **N 从 5 增至 50，两个计数均不增长** —— 这一条才是区分「常数条」与「每行一条」的真判据；
③ 同口径下改动前应分别为 **5/50**（a）与 **5/50**（b） —— **该对照必须实际跑出来**，作为测试有效性的证明。

### 阳性 —— 真问题仍能被抓住

**AC-4（序列 · 守 E-4，最关键的一条）** 前置：任一含 `INPUT_*` 列的报价单，某行手工填入值 `X`，
另一行的 INPUT 列被用户**显式清空**（存 `""`）。
操作：保存 → 触发一次会走重展开路径的整单物化 → 刷新页面 → 切走页签再切回。
断言：① 手工值仍为 `X`，**未被重物化值冲掉**；② 显式清空的那格仍为空，**未被自动填回默认值**；
③ 同页签的 `BASIC_DATA` / `DATA_SOURCE` / `FORMULA` 列**确实被刷新为新算值**（证明不是靠「整个不刷」蒙混过关）。

**AC-5（单点 · 守 E-6）** 构造物化必失败的场景（如临时置一个必用的 `$view` 为报错态）。
断言：接口仍返回 `cardValuesReady: false` 且 `warnings` 含失败原文，**降级路径未被吞掉**。

### 无副作用 —— 相关值 / 显示 / 导出不变

**AC-6（单点 · 守 E-5 逐位等价）** 对同一张报价单，取改动前后的
`row_data` / `snapshot_rows` / `quote_card_values` / `costing_card_values` 做**逐位比对**。
断言：**除时间戳字段外完全一致**。证据形式：背靠背 diff 的输出。

**AC-7（边界 · 守 E-7）** 前置：单产品小单（1~3 行）。
操作：加产品 / 编辑失焦同步 / 非 `wholeBatchEnabled` 分支各走一遍。
断言：行为与改动前一致，卡片值正确落库，无新增 warn。

**AC-8（边界）** 前置：0 行 / 1 行的极端单据，以及 `row_data` 全为 NULL 的新建单。
断言：批量预载返回空 Map 时不抛异常、不 NPE，物化正常完成。

**AC-9（边界 · 守 E-8 · 评审标注的未测面）** 前置：同 AC-1 的 1845 行单。
操作：在 D-1+D-3 修复后重跑建单，**对四步各自的耗时分别计时**。
断言：① 第 ④ 步 `ensureExcelValues` **不触发 reaper**；② 四步耗时逐一记录并写入 `test-report.md`
（**这是「下一堵墙在哪」的唯一依据**，不许省）。
🚩 若 ④ 成为新瓶颈 → **不属本次范围**，按 `CLAUDE.md §4.3` 回来问用户，不许自行扩范围继续改。

> ✅ **该旗子已触发并已闭合（2026-08-28）**：B-28 的受控实验顺带证实 ④ **确实是新瓶颈**，
> 且与 ③ 修复前**结构同构**（单事务包 1845 行，被堵即整步回滚，Excel 值 0/1845）。
> 主线**未自行扩范围**，按本旗子要求回问用户 → **用户裁决纳入本任务**，立 **B-29**，新增 **AC-15**。
> 详见 §⑨ 与 `backtask.md` B-29。

**AC-15（边界 · 守 E-8 · B-29 的核心断言 · 2026-08-28 扩范围新增）** 前置：同 AC-1 的 1845 行单，
并用独立连接人为持有某一批的行锁 130 秒（装置见 §⑧／§⑨，**必须先验证锁真的持有再往下走**）。
操作：走完整物化，读 ④ 的分批埋点。
断言：① 被堵那批**快速失败**（远早于 60s），日志含 `batch=%d/%d` 与 `idxRange=[start,end)`；
② **其余批照常完成** —— 终值应为 `1845 − 被堵批行数`，**而不是 0/1845**（这是与修复前的判别点）；
③ ④ 链路 `ARJUNA012117` 出现 **0 次**；
④ 提交路径（`QuotationResource:362`）失败时**有 ERROR 日志**，不再被 `catch (Exception ignore)` 静默吞掉；
⑤ 释放锁后调一次 `POST /{id}/ensure-excel-values` 能**自愈补齐至 1845/1845**（幂等性未被拆批破坏）。
🚩 **判据依据（实测基线，非估算）**：④ 无争用时 33~41s，60s 预算下**余量仅 31~44%**；
受控实验中被堵 35s 后整步回滚，Excel 值 **0/1845**。

**AC-16（单点 · 守 B-29-5 · 金额路径不许静默 · 2026-08-28 新增）** 前置：一张 DRAFT 单，
用独立连接持住其中一批的行锁（装置同 AC-15）。
操作：在持锁期间**提交**该报价单。
断言：① 提交**被拒绝**并返回可理解的错误（**不是**静默成功）；
② 报价单状态**仍为 DRAFT**，`total_amount` **未被改写**；
③ 释放锁后重新提交能正常成功，金额正确。
🚩 **为什么必须有这条**：B-28 把批失败从「异常冒泡」改成「按批 catch 不 rethrow」，
而 submit 走 `force=true` 时失败批的行**保留旧值而非 NULL** —— 下游 `lineDiscountService.recompute`
照旧汇总，**冻结出用陈旧卡片值算的金额且零报错**。这是 B-28 引入的行为回退，
`EnsureResult` javadoc 里「其余 5 个既有调用点不受影响」那句话**只在签名层面成立**。

**AC-10（单点 · 守 E-9）** 前置：一张卡片小计与报价总额曾因「配置源不一致」而不等的单
（`b6e86a18` 修的就是这个）。
操作：走完整物化。
断言：卡片小计与报价总额**一致**，即卡片值仍按**冻结结构**算 —— 证明 D-3 的提出循环
没有把三级降级链的优先级改掉、没有退回用模板快照算值。

**AC-11（单点 · 守 E-10 · D-4 的核心断言）** 前置：同 AC-1 的 1845 行单。
操作：跑完整物化，读 B-10 四步埋点 + 新增的分批埋点。
断言：① ③ 的**单个事务**最长耗时 **≤ 20,000ms**（即余量 ≥ 66%，对比改动前的 58,679ms / 余量 2%）；
② 分批数 = ⌈N/chunk⌉，且**单批耗时不随 N 增长**（用 N=1845 与一个更小的 N 对照）；
③ 全程 `ARJUNA012117` / `ARJUNA016102` 出现 **0 次**。
🚩 **判据依据（实测基线，非估算）**：改动前 ③ = **58,679ms**，每行 **31.8ms**，真实阈值 ≈**1887 行**。

**AC-12（边界 · 守 E-11 · 部分失败可恢复）** 前置：构造第 k 批必失败的场景。
断言：① 已提交批的 `quote_card_values` **保留**（不被回滚）；② 未完成行仍为 NULL；
③ 重跑时 `IS NULL` 谓词**只补未完成行**，已完成行不重算（自愈，不无限重算）；
④ 接口仍按既有降级语义返回 `cardValuesReady=false` + `warnings`（不许因为"大部分成功"就报 true）。

**AC-13（单点 · D-5 · 用户可见的成败判据）** 前置：1845 行导入记录。
操作：从 **UI** 走导入 → 建单完整路径。
断言：① **浏览器不再出现任何超时/取消**（F12 Network 里 `create-quotation` 不再有「已取消 30.01 秒」）；
② 建单 POST 在 **5 秒内**返回（只做建单+建行，不等物化）；
③ 前端显示「正在计算」类进行中状态，轮询直到完成后进入编辑页；
④ 完成后卡片值可见，**不依赖用户手动刷新**。

**AC-14（边界 · D-5 · 后台任务的失败面）** 断言：
① 物化在后台失败时，前端轮询能**拿到失败态并显式提示**，不许无限转圈；
② 服务端**中途重启**导致后台任务丢失时，下一次轮询能**自动重新触发**补算（靠 `IS NULL` 谓词自愈），
不留下永久「算不完」的单；
③ 用户中途关闭页面不影响后台完成（数据仍落库）。

### 自检证据（闸门 B 汇报必须同时给出，不可与亲验证据互相替代）

- 后端：`./mvnw test` 全绿，**必须点名** `OverlayExistingInputKeysTest`（**6 例**）、
  `GoldenCardValuesEquivTest`、`CardValuesBatchPersistEquivTest` 三者的通过数
- 后端启动自检：`GET /api/cpq/components` → `401`（应用在跑、鉴权正常）
- 本次**无迁移、无 DDL、无前端改动**，故不含 Flyway 与 `tsc` 自检项 —— 见 `fronttask.md` / `api.md`

---

## ⑦ 与在途功能的边界（2026-08-25 追加）

用户同期在规划**报价单产品分页**功能，特此划清边界，避免两边互相误认为对方已覆盖：

| | 本任务（D-1） | 报价单分页 |
|---|---|---|
| 作用侧 | **写侧** —— 建单那一刻的整单物化 | **读侧** —— 详情页/编辑页取数与渲染 |
| 解决什么 | 27s 的无谓往返 + 60s 事务被强杀 | 单次响应体积过大 |
| 能否互相替代 | **不能。** 建单时 1845 行必须全部物化完，与前端如何翻页无关 | 同左 |

**🚫 分页不能作为本缺陷的替代修法**，反之本次修复也不解决读侧体积问题。

**实测：该单读侧体积（卡片值尚为 NULL 时）**

| 项 | 大小 |
|---|---|
| `quotation_line_component_data.row_data` | 1902 kB |
| `quotation_line_component_data.snapshot_rows` | 5764 kB |
| 合计（7380 行） | **7666 kB** |

AC-2 达成后 `quote_card_values` / `costing_card_values` 补齐，该体积会进一步显著上升。

### ⚠️ 移交给分页功能的已知风险（本任务不修，但必须传达）

1. **小计/合计是跨全量行的聚合，而本项目行内值读后端快照、小计读前端实时重算（双源）。**
   分页后前端只持有当前页的行，直接重算会得到「本页小计」。
   **该症状表现为「行内值全对、只有小计不对」，与 `repair-260808-前端页签算序假环致列小计归零`
   的已知症状族外观一致，极易被误判为旧缺陷复发。**
2. **cross_tab 跨页签公式**：源页签行被分页截断时会**静默取少**，不报错。
3. **压力样本**：`quotation 1ed36b62-...`（1845 行）是库内**孤例**，第二大的报价单仅 **2 行**。
   建议分页功能直接以该单为开发与验收夹具。

---

## ⑧ 物化批事务被 reaper 杀的根因（2026-08-27 受控实验证实）

> 该问题从 2026-08-25 起反复出现、两次被主线错误归因，最终由受控实验闭合。**结论非推断。**

### 症状

大单量建单后，物化**停在批次边界**（实测 900 / 1200 / 1500 —— 全是 chunk=300 的整数倍），
日志 `ARJUNA012117 TransactionReaper` + `后置物化失败`，**报价/核价哨兵均为 0、`RequestScoped` 为 0**
（即：不是 D-3/D-5 那两个已修的缺陷）。

### 根因（完整链条）

```
用户在编辑页操作（保存草稿 / 自动保存）
  → saveDraft 按 id UPSERT 全部 1845 行（QuotationService:397「按 id UPSERT 报价行」）
    → 持有 quotation_line_item 的行锁
  → 页面被 batch-evaluate 风暴（BL-0184 / task-260819 守卫缺口）拖慢，事务迟迟不提交
       ↓
物化的批事务 UPDATE 同一批行的卡片值 → 被行锁堵住
  → 内层批事务是 @Transactional(REQUIRES_NEW) + Narayana 默认 60s
  → 堵满 60s → reaper 强杀 → 整个 materialize() 失败
  → 停在批次边界
```

### 受控实验（第 5 次尝试才做成，前 4 次装置失误见下）

**干预**：建单后立刻用 psql 持有批 4 目标行的写锁 130 秒
```sql
BEGIN;
SELECT id FROM quotation_line_item
  WHERE quotation_id='e3809233-...' AND sort_order>=900 AND sort_order<1200 FOR UPDATE;
SELECT pg_sleep(130);
ROLLBACK;
```

**实时抓到的阻塞链**：
```
🔒 pid=2186708（物化批 4）被阻塞于 pid=2186840（人为持锁）
   update quotation_line_item set card_snapshot_at=$1, costing_card_values=$2,
   quote_card_values=$3, quote_values_at=$4 where id=$5
```

**结果**：
```
批 1  13,855ms  ✅      批 2  12,226ms  ✅      批 3  13,569ms  ✅
批 4  被堵 51s+ → ARJUNA012117 → 后置物化失败
最终  900 / 1845   ← 精确停在批 3 结束处
```

**自愈验证**：释放锁后触发一次 `ensure-card-values`，**945 行 39 秒补齐**，最终 1845/1845、哨兵 0。
→ **数据从不真丢，只是需要再触发一次。**

### 它解释了此前全部观测

| 观测 | 解释 |
|---|---|
| 主线单跑三轮全过（0186/0187/0190） | 没人碰那些行 |
| 用户一开页面就挂（0188/0192/bb118cc6） | saveDraft 持锁 |
| 每次都停在 300 的整数倍 | 批次边界 |
| **纯读风暴实验（38,301 次守卫报错）杀不死它** | **读不锁行** —— 这一条曾让主线误判「假设不成立」 |

### 🚨 暴露的设计缺陷（比这个 bug 本身更值得修）

1. **一个批被堵，整个 materialize 就死** —— 但批与批是 `REQUIRES_NEW` 独立事务，
   前面成功的批本该保住、后面的批本该继续。现在是一荣俱荣一损俱损。
2. **堵满 60 秒才死，这 60 秒纯属白等** —— 与其等 reaper 杀，不如几秒拿不到锁就跳过该批、留给自愈。

### 🔑 主线的方法论教训（4 次实验装置失误）

| # | 失误 | 后果 |
|---|---|---|
| 1 | 变量取空 → `until` 死循环 | 10 分钟超时，锁一次没加上 |
| 2 | `SELECT count(*) ... FOR UPDATE` | PG 报 `FOR UPDATE is not allowed with aggregate functions`，事务直接退出、零持锁 |
| 3 | 目标单已算完，无 NULL 行可锁 | 空跑 |
| 4 | 只造**读**负载（batch-expand），没造**写**负载 | 杀不死物化 → **差点据此判定「假设不成立」** |

🔑 **每一次「失败的实验」都曾被当成「假设被证伪」的证据。**
第 5 次成功的唯一区别是**加了一步「先验证干预真的生效，再往下走」**。
**规范建议：任何「证伪型」实验，必须先证明干预条件成立，否则阴性结果无意义。**
