# fronttask.md —— 前端任务分解

## 结论：本任务前端零改动

**判定依据**（不是「暂时不改」，是「按方案甲不需要改」）：

1. 缺陷位于后端 `ConfigureSnapshotService` 的服务端物化路径，**不经过任何前端代码**。
2. 方案甲只改「从哪里取既有 `row_data`」，**不改数据形态、不改字段、不改接口响应结构** ——
   前端收到的 JSON 与改动前逐位一致（见 `问题说明.md` AC-6）。
3. `POST /quote/create-quotation` 的**请求与响应契约完全不变**（见 `api.md`）。
4. 🔴 **2026-08-25 扩范围复核**：纳入 D-3（`CardSnapshotService`）后，前端仍**零改动** —— D-3 同样只改
   服务端私有方法的调用位置，响应结构一字不变。
5. 🚫 **`api.ts` 的 `timeout: 30000` 本次明确不动** —— 见 `问题说明.md §④ 证据 5`：
   前端 30s 与 Narayana 60s 是两道独立的墙，只放宽前端会撞穿 60s 且数据作废，属无效修法。
   本次靠把物化拉回 60s 预算内来解决。
   ⚠️ **但 AC-1 的秒数判据已按用户裁决改为「先测后定」** —— 评审估算两处全修后总墙钟仍在 ≈32 s 上下，
   **可能仍会撞穿前端 30s**。若实测确认，「是否单独给该调用放宽 timeout」将作为独立决策回到用户面前，
   **不在本次范围内自行决定**。

---

## 回归确认清单（前端不改代码，但必须确认这些没坏）

| 编号 | 服务的 AC | 确认内容 |
|---|---|---|
| **F-1** | AC-1 | 走 UI 完整路径（导入 → 建单）确认不再出现 30s 超时提示 |
| **F-2** | AC-2 | 建单完成后进编辑页，卡片值直接可见，**不再走 warm 兜底重算**（F12 Network 应无兜底重算请求风暴） |
| **F-3** | AC-4 | 手工填写的 INPUT 值与显式清空的空格，在触发重展开物化后仍保持原状；同页签 `BASIC_DATA`/`DATA_SOURCE`/`FORMULA` 列确实刷新 |
| **F-4** | AC-7 | 小单量路径（加产品 / 编辑失焦同步）行为不变，无新增「加载中…」永久占位 |
| **F-5** | AC-10 | **卡片小计与报价总额一致**，未退回 `b6e86a18` 修掉的「同卡双值」（卡片小计 214 / 报价总额 14 那类）。⚠️ 本项因 2026-08-25 扩范围纳入 D-3 而新增 |

> ⚠️ F-2 / F-4 的「加载中…」相关观察点见 `docs/反模式.md` AP-31 / AP-38。

---

## 二期触发条件（出现任一即需新开前端任务）

- 若最终选择实施备选丙（物化异步化 + 轮询），则 `POST /quote/create-quotation` 契约变更，
  前端需改为「立即返回 + 轮询」模式（可复用 `basicDataImportV6Service.pollImportResult` 的现成范式）
- **报价单产品分页功能**（用户在途规划）—— 见 `问题说明.md §⑦`，含移交的三条已知风险
