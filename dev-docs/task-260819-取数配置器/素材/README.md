# 素材目录（只读输入，**不是本任务的验收依据**）

> 建立日期：2026-08-19。来源：`dev-docs/task-260728-组件SQL视图的用户便捷化配置功能/`（已留碑，仅作历史追溯）。

🚨 **效力声明**：本目录下的文件是**讨论输入素材**，不是本任务的需求或验收标准。
本任务的唯一验收依据是 `../需求文档.md`。素材与 `需求文档.md` 冲突时，**以 `需求文档.md` 为准**。

| 文件 | 是什么 | 怎么用 |
|---|---|---|
| `需求说明-v8-承接自task260728.md` | task-260728 的 **v8 正式版**（1294 行，八轮澄清 45 个决策，闸门 A0 已通过，AC 38 条，含 §12 派工计划）。**停在闸门 A，从未开工，无分支无代码** | 讨论的主素材。其中的**实测数据**（表维度、sheet 真实导入列、JOIN 形态普查、粒度实测行数）是硬资产，复核后可直接复用；其中的**范围与 AC** 需重新裁决 |
| `需求说明-v7-历史存档.md` | v7（739 行）。v8 的前身，配方模型为「6 段页签硬配方」 | 仅追溯「为什么 v8 改成语义图」。**不要按它实现** |
| `原型-v8语义模型.html` | v8 定稿原型：配置器主界面 + 字段名/列名双列 + 价格策略原子组 + 粒度条与体检 + 真实预览 + L1 只读语义图谱（4 视图，含「声明源码」）。已做 96 项行为验证 | 讨论时的界面参照。**范围定稿后**才移入 `../原型图/` 并成为 1:1 还原基准（`frontend.md §1.3`） |

## ⚠️ 引用素材结论前必须复核的四项

素材里的实测结论取自 **`10.177.152.12:5432/cpq_db_0724`，时间 2026-07-27 / 07-28 / 08-19**，此后 master 有大量提交：

| # | 待复核 | 为什么 |
|---|---|---|
| 1 | 当前 8081 实际连的哪个库 | 库切过 3 次；带具体行数的结论换库即失效 |
| 2 | 价格策略取价函数是否仍是 `f_material_element_price`（V369） | `task-0729` 在 2026-08-05 后仍有 `#58`/`#59` 续修，规范可能又动 |
| 3 | `TemplateService.java:364` 是否仍是 `fieldNode.put("label", f.path("name").asText(""))` | 素材标为「开工必带的最高危单点」，行号是 2026-08-19 的快照 |
| 4 | golden 基准组件 `COMP-0027`（材料成本）是否仍存在且渲染正常 | AC-21 逐行等值是总把关，基准没了则该 AC 落空 |

## v7 的两份旧原型

未拷入本目录。仍在 `../../task-260728-组件SQL视图的用户便捷化配置功能/原型-历史存档/`，含一份说明其过期点的 README。**不要使用**（价格策略开关与取价函数均已过期）。
