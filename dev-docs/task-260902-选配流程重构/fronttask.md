# task-260902 · 前端任务分解

> 前端**只按本文件做**。接口契约以 `api.md` 为准，AC 原文在 `需求文档.md`（本文件只标编号）。
> 🎨 **视觉基准 = `原型图/` 下的 6 份独立原型，按它们 1:1 还原**（`frontend.md §1.3`）。
> `原型图/选配流程-交互原型.html` 是**汇报展示用**，不是验收基准；两者不一致时以 6 份独立原型为准。

**主战场**：`cpq-frontend/src/pages/quotation/ConfigureProductDrawer.tsx`（349 行）+ `configure/AddPartSubDrawer.tsx`（391 行）
⚠️ 后者刚被 `task-260901` 从 3 段并为 2 段（① 材质与含量 → ② 工序），本任务在此基础上**整体重做**。

---

## A · 向导框架与步骤 1

| 编号 | 服务的 AC | 原型 | 任务内容 |
|---|---|---|---|
| **F-1** | AC-1, AC-2 | `1-客户产品编号.html` 状态 A/B/C/D | 新增步骤 1「客户产品编号」：输入框 + **debounce 400ms** 调 `GET /check-product-no`（`api.md` §2.1）。<br>未占用 → 绿边框 + 「✓ 该编号未被占用，可以继续」；已占用 → 红边框 + 错误条（显示已对应的销售料号与创建日期）+ **「→ 打开『从产品库添加』并定位到该产品」按钮**，且「下一步」**禁用但可见** + tooltip「客户产品编号已存在，请从产品库添加」。<br>🚫 校验请求**不得阻塞输入**（不要 await 后再更新 value）。超长编号/产品名**单行省略 + hover 全文**，不换行、不撑高抽屉（状态 D）。 |
| **F-2** | AC-11 | 各原型顶部步骤条 | 向导框架改 4 步（客户产品编号 → 添加配件 → 组合工序 → 确认并添加）。已完成步骤可点击回跳；当前步不可跳未完成步。每步的「下一步」禁用态都要有 tooltip 写明原因（§1.2）。 |

## B · 配件层（新增的中间层）

| 编号 | 服务的 AC | 原型 | 任务内容 |
|---|---|---|---|
| **F-3** | AC-5 | `2-配件类型与来源.html` 状态 A/B/C/D | 「+ 添加配件」→ 两级选择：① 配件类型（**零件 / 外购件**并排卡片，不用下拉）；② 零件来源（**新建 / 已有**）。已添加配件以卡片列表展示，每卡显示构成摘要（材质+占比 / 外购件料号 / 工序）。<br>空态：「还没有配件」+「+ 添加第一个配件」，「下一步」禁用 + tooltip「请至少添加一个配件」。<br>📌 卡片行内的「编辑/移除」适用 `frontend.md §1.2` **例外白名单的「抽屉内部子表」**条款，**不是违规**，不要上提到工具栏。 |

## C · 新建零件（核心）

| 编号 | 服务的 AC | 原型 | 任务内容 |
|---|---|---|---|
| **F-4** | AC-3, AC-13, AC-14 | `3-新建零件与多材质.html` 状态 A/E | 零件表单：**品名(必填) / 规格 / 尺寸 / 总重(必填,克)**；材质表格（材质 / 含量配置 / 占比% / 折合克重 / 操作）。<br>「折合克重」= 总重 × 占比 ÷ 100，**实时计算、只读展示**。<br>零材质时表格区显示空态「还没有材质」，「确定」禁用 + tooltip「请至少添加一个材质」。<br>单材质（占比 100）必须与旧行为等价（AC-13 是不回归判据）。 |
| **F-5** | AC-4, AC-15a, AC-15b | 状态 B / F / F-b | 占比合计实时校验：合计 = 100 → 绿 tag；≠100 → **红 tag + 行级红底 + 消息写出实际合计值**（如「材质占比合计为 **90%**，需要正好 100%（还差 10%）」），「确定」禁用 + 同文案 tooltip。<br>🚨 **合计必须用定点整数运算**（把占比按 12 位小数转成整数再累加），🚫 **不许用 `Number` 累加**。<br>⚠️ AC-15a 那组（`33.333333333333×2+33.333333333334`）在浮点下**恰好等于 100**，**它拦不住浮点实现**；AC-15b（`0.000000000001+99.999999999998+0.000000000001`）浮点下 = `99.99999999999999` 会被错误拒绝 —— **两条都要通过才算对**。 |
| **F-6** | AC-6, AC-17, AC-18 | 状态 A2 / A2-b / A2-c / C / D / G | **材质选择器（可搜索）**：点「+ 添加材质」展开面板，输入框按**材质编号或材质名**过滤（大小写不敏感、中文名可搜），列显示 编号/名称/首个配置的含量/含量配置组数/是否支持自定义。<br>🚫 **不可选的材质仍要列出并灰显 + tooltip 写明原因**，不许过滤掉（§1.2）：① 含量配置 **0 组** → 「该材质尚未配置含量」；② **本零件已添加过** → 「该材质已添加」。<br>无匹配时显示空态 + 提示可用编号或名称搜索。<br>含量配置下拉（选标准配方）+ 「切换到自定义含量」：`allowCustomContent=false` 时**禁用但可见** + tooltip「该材质不支持自定义含量」；为 true 时展开元素含量编辑（**只能改含量、不能增删元素**）。<br>⚠️ 实测材质库 **258 条**，走虚拟滚动，**必须先输入过滤再点选**。 |
| **F-11** | AC-19, AC-20 | 状态 H + 3/4/5 三份的工序区 | **工序选择器 + 有序列表**（与材质同构）：点「+ 添加工序」展开可搜索面板（按工序编号/名称过滤），选中**加入列表**。<br>列表带 `#` 序号 + **↑↓ 调序**（首行 ↑ / 末行 ↓ 禁用 + tooltip「已是第一道」「已是最后一道」）+ 移除。<br>🔄 **与材质选择器的唯一差别：工序允许重复加入**（面板里显示「已加 N 次」而不是灰显禁用），因为「粗车→热处理→精车」是常态。<br>📌 提交时 `processNos` **按列表顺序原样发送**，不排序 —— 顺序不影响料号复用（后端指纹会排序），但影响 `unit_price.seq_no` 与报价单显示顺序，两者是两件事。<br>**同一套组件复用三处**：新建零件工序 / 已有零件工序 / 外购件工序；组合工序（步骤 3）也用它。 |

## D · 已有零件 / 外购件

| 编号 | 服务的 AC | 原型 | 任务内容 |
|---|---|---|---|
| **F-8** | AC-11 | `4-已有零件与工序.html` 状态 A/B/C | 从产品列表选零件（搜索 + 单选）+ 选工序。列表「材质构成」列须能显示 **N 个材质标签**（多材质零件在本次重构后才会出现），不能只显示第一个。<br>搜索无结果 → 空态 +「← 改为新建零件」；未选中 → 「确定」禁用 + tooltip「请先选择一个零件」。 |
| **F-7** | AC-5, AC-16 | `5-外购件与工序.html` 状态 A/B/C | 外购件选择（调 `GET /outsourced-parts`）+ 选工序。<br>🚨 **空态必须是空态，绝不能显示「加载中…」永久占位**（AP-31 族）：「料号库里还没有外购件」+ 说明「需先在料号维护里录入并把料号类型设为外购件」+「→ 打开料号维护」「← 改为添加零件」两个出口。<br>⚠️ 实测该条件当前**只命中 1 条**，**返回 0 条是正常业务状态**，不是错误。超长品名/规格**省略 + hover 全文**。 |

## E · 组合工序与提交

| 编号 | 服务的 AC | 原型 | 任务内容 |
|---|---|---|---|
| **F-9** | AC-7, AC-8, AC-9, AC-10 | `6-组合工序与指纹结果.html` 状态 A/B/C/D | 步骤 3 组合工序（复用 F-11 的选择器 + 有序列表，保留既有「参与配件」与「参数」列）；步骤 4 确认页展示全部配件摘要。<br>提交响应 `fingerprintMatched=false` → 绿色「产品库里没有相同配置的产品，将新建一个销售料号」；`=true` → 蓝色「已有相同配置，将直接复用」+ **展开 `reusedProductInfo`**（销售料号/品名规格尺寸/单重/材质构成/历史报价）。<br>失败（如 409 `RECIPE_HAS_NO_CONFIG`）→ 红色错误条 + 错误码 + 指路按钮。 |
| **F-10** | AC-11 | — | 保存草稿 / 刷新 / 重新打开后，多材质与占比、外购件、工序顺序、组合工序**全部正确回填**；占比显示 `70` 而非 `70.000000000000`。 |

## F · 公共

| 编号 | 服务的 AC | 任务内容 |
|---|---|---|
| **F-13** | AC-25, AC-26 | **选配模板功能下线（本期不控制）**：<br>① **移除 `hasTemplate` 门禁** —— `ConfigureProductDrawer.tsx:250-253` 现在是 `if (effectiveLoading \|\| !hasTemplate) return <只有取消的 footer>`，去掉 `!hasTemplate` 分支，**无模板也能正常提交**；同时清理 `types/configure.ts:143` 那句「false = 前端渲染『缺少选配模板』空态」的注释与对应空态渲染。<br>② **隐藏菜单**：注释掉 `MainLayout.tsx:101` 的 `{ key: '/config/sel-templates', label: '选配模板管理', … }`。<br>🚫 **路由 `router/index.tsx:143` 与页面 `SelTemplateManagement.tsx` 保留不删**（直接访问 URL 仍可打开）。<br>⚠️ `effective` 的其余用法（材质/工序候选 `effectiveValues`）**先保持不动** —— 实测 `allowedValues` 为空时是**全放行**（`if (allowed.isEmpty() \|\| …)`），且 `sel_template_item_value` 现网 0 行，所以留着不影响行为；本期不做候选源改造，避免与 F-6/F-11 的选择器改动缠在一起。<br>📌 依据见 `需求文档.md §4.3c`：模板三个参数开关与值域限定**全部空转**，唯一生效的就是这道门禁。 |
| **F-12** | AC-15a, AC-3 | **去尾随零的共享工具函数**（放 `utils/precision.ts` 旁，与既有 decimal 工具同处）：`70.000000000000 → "70"`、`0.150000000000 → "0.15"`、`12.345678901200 → "12.3456789012"`。<br>🚫 **必须走字符串正则** `s.replace(/(\.\d*?)0+$/,"$1").replace(/\.$/,"")`，**禁止 `Number(s).toString()`** —— 12 位小数过 JS `number` 会丢尾数。<br>作用范围：占比列、折合克重、含量配置下拉文案、确认页摘要、**以及输入框回填值**。提交时把输入框字符串**原样发送**，不补零不做数值转换。<br>📌 与 `task-260901` AC-30 同一口径，**优先复用它已交付的实现**，不要重写。 |

---

## 🚫 明确不做（防超范围）

| 不做 | 理由 |
|---|---|
| 改「从产品库添加」入口页 | 另一个入口，本次只新增一条从 F-1 阻止态跳转过去的链接 |
| 改**选配模板管理页本身**（页面内容/表单/校验） | 本期只**隐藏入口 + 移除门禁**（F-13），页面代码原样保留。将来重新引入时再改 |
| 3D 模型配置页 | 不在本次范围（`task-260712` 的原型仍是当前基准） |
| 删除 `sel_template` 系列表 / 路由 / 页面文件 | 🚫 **只停用不删除** —— 将来要按新模型重新引入（管步骤可见性/值域/必填性），删了等于把历史设计一并丢掉 |
| 改造材质/工序候选的取数来源（现走 `effective.effectiveValues`） | 实测 `allowedValues` 为空时**全放行**且现网 0 行配置 ⇒ 留着不影响行为。本期不动，避免与 F-6/F-11 的选择器改动缠在一起 |
| 把配件列表/材质表的行内操作上提到工具栏 | §1.2 例外白名单明列「抽屉内部子表」，上提反而违反原型 |
| 用 Modal | §1.1：一律 Drawer |
| 自己实现材质/工序主数据的增删改 | 那是主数据维护页的职责，选配侧只读 |

---

## 派工提示（给前端工程师）

- **不要读后端实现代码**去推断契约，一律以 `api.md` 为准；对不上就报主线，不要自己改契约。
- `AddPartSubDrawer.tsx` 刚被 `task-260901` 改过（3 段并 2 段），**从 master 最新代码起步**（`f5adfccc` 之后）。
- 完成后按 `frontend.md` 自检：`tsc --noEmit` 0 错误 + 改动文件模块地址 200 + **逐屏比对 6 份独立原型**，偏差逐条列出。
