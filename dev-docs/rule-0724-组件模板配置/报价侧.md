# 报价侧配置 delta

> **来源**：`docs/rule/报价模板生成规则.md`（§0 映射心智 / §5 V6 表映射 5.1~5.6 含 §5.2.1 元素单价接价格策略 / §7 组件配方大全 7.0~7.7）+ 记忆 `customer-quotation-component-templates`。
> **先读共性**：R1~R6 用户硬约束 + 4 问工作流 + 料号铁律 → `1-总则与工作流.md`；字段结构（`fields[]`、`default_source`、C1~C7）→ `2-组件与字段.md`；SQL 视图编写规则（`_` 前缀 / pending 改写坑 / 中文别名）→ `3-SQL视图.md`；页签属性 / `sort_field` / 树契约 → `4-页签属性与树.md`；公式 / Excel 列 → `5-公式与Excel列.md`。
> **本文只写报价侧特有的**：客户口径契约 + §5 V6 表字段来源 + §7 组件配方大全（每类页签完整 SQL，照抄即可）。

---

## 契约要点（报价侧特有）

- **客户口径**：`:customerCode` = 报价单客户的 `customer_no`，框架运行时注入，按客户收窄；dry-run 时按 NULL 兜底（返 0 行不报错）。
- **料号键**：报价侧「料号」= **销售料号** = `material_no`。视图把 `material_no`（或 `finished_material_no`）`AS hf_part_no` 接隐式 JOIN 做料号收窄。`hf_part_no` 是唯一不带 `_` 前缀的别名（R4-例外，改名则视图返全表）。`production_no`（生产料号）报价场景不用作键。
- **版本**：无——**提交即冻结**（frozen 逐字节隔离），不可切版本。报价单一旦提交，快照即定格，与后续 V6 基础数据变化解耦。
- **渲染服务**：`ConfigureSnapshotService.snapshotLines`，**两趟路由，两趟注入的绑定变量完全不同**（§5.6 铁律）：
  - **Pass 2 树渲染**：**仅 `tab_type='BOM'`**（判据 `BomTreeRenderService.isQuoteTreeTabType`），走 `BomTreeRenderService.render(usage=QUOTE)`，注入 **`:total_material_no`**（整棵 spine 料号数组）+ `versionFilter` 宏；视图用 `WHERE <料号列> = ANY(:total_material_no)` + 输出 `material_no`(子)/`parent_no`(父) 边键。
  - **Pass 1 平铺展开**：**其余所有数据页签**（材质元素/零件/外购件/费用页签），走 `componentDriverService.expand`，注入 **`:customerCode`** + 行料号（隐式 JOIN 注入 `hf_part_no`）。
  - 🚨 **数据页签禁用 `:total_material_no`**：Pass 1 永不注入该变量 → 数据页签视图若写 `= ANY(:total_material_no)` 则运行时无绑定 → **整表静默 0 行**（不报错，正是 QT-2085「只有产品页签有数据、材料/加工全空」根因）。

---

## 0. 客户模板 → 组件的映射心智

一份客户模板（`页签——<客户>.xls` 或 `组件模板 - <客户>.xlsx`）：

- **每个 sheet（页签）= 一个组件**（`component`，`componentType=NORMAL`；小计性质的 sheet 建 `SUBTOTAL`）。
- **每个列（表头字段）= 一个字段**（`fields[]` 里一项），**严格 1:1，不多不少**。🚫 **不得为了填 `partNoField` 而新增模板没有的「料号列」字段**（~~材料成本+材质料号、外购件成本+组成件料号~~ 2026-07-24 废止）——模板不显示料号是真实场景，`partNoField` 留空、名称列担标识、料号只进 SQL JOIN（§5.5）。
- **每个组件配一个组件 SQL 视图**（`component_sql_view`，`$name`）当取数源；多行组件用它当 `data_driver_path` 驱动。
- **组件按页签语义配 `tabType`**（主件/材质元素/外购件…，费用页签留空，§5.5）。
- **模板目录 = 客户名称**（`component_directory.name = <客户名>`）。

模板只给「结构」（组件数 + 字段名），**数据来源由 §5 的 V6 表映射规则补齐；每种页签的完整配方见 §7**。字段类型统一 `INPUT_*`、`default_source` 绑基础数据、SQL 列 `_` 前缀等共性规则见 `1-总则与工作流.md`（R1~R6）与 `2-组件与字段.md`。

---

## 5. V6 数据表映射规则（字段来源权威表）

> 料号口径：**报价侧「料号」= 销售料号 = `material_no`**（`material_master`/`material_customer_map`/`element_bom_item`/`material_bom_item` 的 `material_no`）。`production_no` 为生产料号，本报价场景不用作键。

### 5.1 产品信息（单行/成品级）——源 `material_customer_map` + `material_master`

| 模板字段 | V6 来源 |
|---|---|
| 销售料号 / hf_part_no | `material_master.material_no`（或 `material_customer_map.material_no`） |
| 客户料号名称 | `material_customer_map.customer_material_name` |
| 客户产品编号 | `material_customer_map.customer_product_no` |
| 客户图号 | `material_customer_map.customer_drawing_no` |
| 报价货币 | `material_customer_map.quote_currency` |
| 基础货币 / 汇率 / 付款方式 | `material_customer_map.base_currency / exchange_rate / payment_method` |
| 品名 / 规格 / 单重 | `material_master.material_name / specification / unit_weight` |

JOIN：`material_customer_map.material_no = material_master.material_no AND material_customer_map.customer_no = :customerCode`。

### 5.2 材料成本（多行/逐元素）——源 `element_bom_item`（材质经**本行 `material_part_no`** → `material_recipe` + 元素单价接价格策略）

**层级结构（务必理解）**：
- `element_bom_item` 是元素行驱动表：`.material_no` = **销售料号（成品）本身**，`.component_no` = **元素**（Ag/Cu/Ni…），`.seq_no` = 项次。
- `element_bom_item.material_part_no` = **本行元素所属的材质料号**（= 材质库配方码，如 991/99901）。
- `material_recipe.code` = 材质料号，`.name` = **材质名**（如 `H65带`/`Cu/Ag/Cu带`）；`material_master.material_no`（=material_part_no）的 `.material_name` 可作兜底名源。

🚨 **「一号多材质」铁律（森萨塔 QT-20260716-2046 事故沉淀 → COMP-0063/0066/0070 修复）**：
- **一个销售料号在同一客户下可合法对应多个 `material_part_no`**（多材质链，各一条独立 element BOM 版本链、各留一个 `is_current=TRUE`；导入按材质料号分链是正确行为，**勿改升版/Q04ElementBomHandler**）。
- **材质名必须逐行取自本行 `material_part_no`**（`LEFT JOIN material_recipe mr ON mr.code = ebi.material_part_no`）。**严禁**用 `material_bom_item` 子查询把整单聚合成一个材质值——那样多材质链的元素行会变成「值相同、无从区分的匿名重复行」，且客户 `material_bom` 为空时材质恒空（旧方案的双重坑）。
- **`material_part_no` 进 driver 排序**（`ORDER BY material_no, material_part_no, seq_no`）让同材质连续成组。
- **🔑「材质」字段必须进 `rowKeyFields`**（如 `["销售料号","元素","材质"]`）。否则多材质链下 `(项次,元素)` 撞键 → 可编辑行 editRows 串行 / 末值×行数塌缩（见 `2-组件与字段.md` 行键唯一化；记忆 `cpq-rowkey-uniqueness-disambiguation`）。这是纯改视图 SQL **不够**、必须连组件 `rowKeyFields` 一起改的点。
- **不要** DISTINCT/GROUP BY 压成一行（丢一种真实材质成本），也**不要**随便取一个材质料号只留一行（两种材质都是有效数据）。
- 「只显示客户实际报价的那一种材质」需另加锚点（母件 BOM `material_bom_item` INNER JOIN `ON child.component_no = ebi.material_part_no`，或报价行加「材质选择」字段），非改视图 SQL 能解决 → **另立需求**。

> 📌 `material_bom_item.characteristic` 有 `RECIPE`/`ASSEMBLY`/`(空)` 三态，其中 `RECIPE` 只是 task-0708 repair-2 开发期临时代码遗留脏数据（当前 `MaterialBomMergeHandler` 只写 `null`/`ASSEMBLY`），**任何按 characteristic 过滤都不可取**。

| 模板字段 | V6 来源 |
|---|---|
| 销售料号 / hf_part_no | `element_bom_item.material_no` |
| **材质** | `COALESCE(mr.name, mm2.material_name)`，`mr.code = ebi.material_part_no` / `mm2.material_no = ebi.material_part_no`（**逐行取，见铁律**） |
| （材质料号）| `element_bom_item.material_part_no` —— **默认只作 JOIN 键 + `ORDER BY` 键，不出可见列**；仅当客户模板确有「材质料号」列时才出 `_材质料号` 并配成 `partNoField` |
| 项次 | `element_bom_item.seq_no` |
| 元素 | `element_bom_item.component_no` |
| 损耗率% | `element_bom_item.scrap_rate` |
| 毛重（毛用量） | `element_bom_item.composition_qty`（与「毛用量单位」= `issue_unit` 成对） |
| 毛用量单位 | `element_bom_item.issue_unit` |
| 元素单价 | **接客户价格策略**：`LEFT JOIN f_customer_element_price(...)` 表函数按元素符号取价（见 §5.2.1）。 |

材质取数范式（逐行、经 material_part_no）：
```sql
COALESCE(mr.name, mm2.material_name) AS _材质
-- ...
FROM element_bom_item ebi
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
-- 组件 rowKeyFields 必须含「材质」：["销售料号","元素","材质"]
```

#### 5.2.1 元素单价接入客户价格策略（`f_customer_element_price`，task-0722）

**目标**：给元素类组件的「单价」列接客户价格策略，报价/核价渲染时**按客户自动取元素单价**（不让用户手填）。**只改组件 `$view` 的 `sql_template` + 字段取数配置，不改字段类型**（不触发 AP-44）。

**核心机制**：价格逻辑封装在 PG 表函数（**已落库**）：
```
f_customer_element_price(客户编号 TEXT, 基准日 DATE)
  → TABLE(element_code 元素符号, unit_price 最终单价(含系数加价), currency 货币, price_unit 价格计价单位)
```
一次返回该客户全部有价元素（非逐行调用，无 N+1）。视图里 `LEFT JOIN` 它，用**元素符号**关联。

**三步配置**：
1. 驱动视图 `FROM` 加 JOIN（**两侧客户参数不同**）：
   ```sql
   -- 报价侧(system_type='QUOTE')：传运行时真实客户
   LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep ON cep.element_code = ebi.component_no
   -- 核价侧(system_type='PRICING')：传字面量 '_GLOBAL_'（见 核价侧.md）
   LEFT JOIN f_customer_element_price('_GLOBAL_', :priceBaseDate) cep ON cep.element_code = ebi.component_no
   ```
2. `SELECT` 加输出列，中文别名与组件字段名**逐字一致**：`cep.unit_price AS 单价, cep.currency AS 货币`（⛔ 不输出「计价单位」，见硬约束 4）。字段实际名叫「元素单价」就 `AS 元素单价`。
3. `component_sql_view.required_variables` 声明：报价侧 `["customerCode","priceBaseDate"]`；核价侧 `["priceBaseDate"]`（不含 customerCode，传的是字面量）。字段取数：报价侧字段 `default_source` 指 `$view.单价`/`$view.货币`（类型仍 `INPUT_*`）；核价侧 `basic_data_path` 指 `$view.单价`（类型 `BASIC_DATA`）。**字段类型一个字都不改**。

**🚨 硬约束（违反即出 bug，逐条自检）**：
1. **必须 `LEFT JOIN`，禁 `INNER JOIN`** —— 否则无价元素整行消失、行数变少、页签小计错。
2. **JOIN 键用元素符号列** `element_bom_item.component_no`（值如 Ag/Cu/301），**不是元素编号** —— 用错恒不命中，全表无价。
3. **禁 `COALESCE(cep.unit_price, 0)` 兜底** —— 无价必须留 `NULL` 触发「留空手填」；填 0 会静默算 0 元成本、不报错。
4. **⛔ 不要绑「计价单位」字段**。报价单「计价单位」列是 **BOM 发料单位** `ebi.issue_unit`（g/KG），被「毛用量/净用量」以 `unit_source_field` 引用做 g→KG 换算；改指价格 `price_unit`(元/kg) 会让换算读到非法 token、静默算错。**本期只绑「单价」+「货币」两字段**。
5. **核价侧传字面量 `'_GLOBAL_'`，且视图 WHERE 里不要加 `customer_no=:customerCode`** —— 核价基础数据 `customer_no` 恒 `'_GLOBAL_'`，加客户过滤一行查不到。
6. **别名与字段名逐字一致**（含全半角/空格）—— 否则路径解析不到、渲染空白。
7. **不为「源名/命中日期/取值方式」加额外输出列** —— 无消费方，徒增复杂度。

**前提与边界**：
- 客户没配价格策略 → 表函数返 0 行 → 元素行照常显示、单价留空手填（**设计如此，不报错**）。*（当前现网实测 `_GLOBAL_`/`CUST-1269` 均返 0 行 = 尚无策略数据，接上后暂全留空，待业务配策略后自动出价。）*
- `:priceBaseDate` 后端自动注入（=报价单创建日期；配置期预览回退今天），视图直接写占位符，不用自己算。已在 `SqlViewExecutor`（task-0722 §11.2）接好。
- 字段类型不变 → **不触发 AP-44**。

**自检**：`expand-driver` 端点或直接 SQL 实测——有价元素出数、无价元素返 `NULL`（不是 0、不是整行消失）；换一个没配策略的客户，元素行数不变、单价全空。

### 5.3 加工费（多行/逐要素）——源 `unit_price` price_type='PROCESS'（要素名称=工序名称）

| 模板字段 | V6 来源 |
|---|---|
| 销售料号 / hf_part_no | `unit_price.finished_material_no`（按成品收窄本报价行） |
| 料号列（零件料号，partNoField） | `unit_price.code`（该费用项针对的零件，非 `finished_material_no`，见 §5.5 料号铁律） |
| 项次 | `unit_price.seq_no` |
| **要素名称** | **`process_master.process_name`（工序名称），经 `process_master.process_no = unit_price.operation_no` 关联；兜底 `COALESCE(pm.process_name, up.operation_no)`** |
| 单价 | `unit_price.pricing_price` |
| 单位 | `unit_price.unit` |

过滤：`system_type='QUOTE' AND price_type='PROCESS' AND is_current AND customer_no=:customerCode`。

🔧 **要素名称=工序名称规则（2026-07-19 用户明确）**：
- **不要**用 `unit_price.cost_type` 当要素名称——它是统称（恒为「自制加工费」），无法区分工序。
- **要素名称 = 工序名称 = `process_master.process_name`**（`process_master.process_no = unit_price.operation_no`）。`process_master` 是 V6 工序主表（非废弃，可引；`mat_process`/`costing_part_process_cost` 是 V44/V76 废弃，禁用）。兜底 `COALESCE(pm.process_name, up.operation_no)`，缺工序名时至少显工序编号。
- **rowKeyFields 必须含「项次」**（如 `["销售料号","项次","要素名称"]`）：改前 cost_type 恒定→整料号加工费行同键撞键；改后要素名称变工序名，但同料号仍可能同工序重复，`项次`(seq_no) 才是料号内唯一保证。

### 5.4 其他常见来源（扩展页签时参考罗克韦尔母版）

| 页签类型 | 源 | 关键过滤 |
|---|---|---|
| 电镀费 | `unit_price` `price_type='PLATING'`（**行转列，见下**）| **零件料号在 `code`、销售料号在 `finished_material_no`**（repair-0802 起，见下方铁律） |
| 成品其他费用 | `unit_price` `price_type='FINISHED_MATERIAL_OTHER'` | **料号在 `code` 列**；`要素名称=cost_type`（材料管理费/外购件管理费/利润/税率，非统称，**不走 process_master**）；`值=pricing_price`；每料号多行，rk=`[销售料号,项次,要素名称]` |
| 来料其他费用 | `unit_price` | `price_type='INCOMING_MATERIAL_OTHER'` |
| 外购件成本（组成件）| `material_bom_item`（`characteristic='OUTSOURCED'`，见 §7.5）+ `material_master`（组成件名称）| rk 按模板实有列：有料号列→`[销售料号,组成件料号]`，无→`[销售料号,组成件名称]`；tabType=外购件 |
| 组装/工序 | `labor_rate` / `production_energy` / `auxiliary_energy` / `tooling_cost` | 工序级 |

> **`material_bom_item.characteristic` 三态语义（task-0720 起）**：`RECIPE`=材质（材质页签走 `material_part_no`，不靠它过滤，见 §5.2）/ `ASSEMBLY`=组成件（零件，tabType=零件）/ `OUTSOURCED`=外购件（tabType=外购件）。现网实查：RECIPE / ASSEMBLY 有数据，`OUTSOURCED` 暂 0 行。

🔧 **电镀费「费用列」= 行转列 PIVOT（2026-07-20 森萨塔事故）**：
- 现网**绝大多数客户**的电镀数据在 `unit_price` `price_type='PLATING'`，且是**行式**：每（销售料号 × 投入料号）两行（`cost_type='电镀加工费'` / `'电镀材料费'`，值在 `pricing_price`）。`plating_fee`（费用列式）只有个别客户（如 8000143）有数据。
- 模板要「电镀加工费 / 电镀材料费」作**两列** → 必须 **CASE-WHEN 行转列 + GROUP BY**（`plating_fee` 直取列的写法对绝大多数客户返 0 行，是错的，见 §7.4 完整 SQL）。
- 电镀元素名称/镀层厚度在 `unit_price` 无源（保留字段绑 NULL 列，待数据模型补 `plating_scheme` 链路）。**注意「投入料号名称」≠「电镀元素名称」**——前者是被电镀零件的物料名，后者是镀层元素（Ag/Ni…），别拿前者顶替后者。个别用 `plating_fee`+`plating_scheme` 的客户可另走「`plating_fee` JOIN customer(id→code) LEFT JOIN plating_scheme(scheme_no=plating_plan_code)」逐元素式，但那是少数。

🚨 **repair-0802（2026-08-02）改了电镀的两列语义 —— 上面这条铁律的 GROUP BY / 行键 / 收窄键全部随之变化，配电镀页签前必读**：

电镀 sheet 增加了**非必填**的【投入料号】【投入料号名称】两列，`Q17PlatingCostHandler` 落库口径归队到与 Q06/Q07/Q13 一致：

| 列 | 改造前 | 改造后（现行） |
|---|---|---|
| `code` | 销售料号 | **投入料号（零件料号）**；两列皆空时回退为销售料号 |
| `finished_material_no` | 恒 NULL | **销售料号（成品）**，必有值 |
| groupKey | (…, cost_type, code) | (…, cost_type, **code, finished_material_no**) |

配置侧四条随动规则：

1. **`hf_part_no`（驱动收窄键）必须绑 `finished_material_no`**，不再是 `code`。改造前 `code` 就是销售料号故绑 `code` 恰好正确；现在 `code` 是零件料号，**继续绑 `code` 会按零件号去匹配本报价行的成品料号 → 整页签 0 行**。
2. **`GROUP BY` 必须含 `up.code`**（连同 `up.finished_material_no`）：同一销售料号下可以有**多个投入料号**（各自独立成行、互不覆盖），只按销售料号分组会把多个零件的电镀费用 `MAX()` 成一行、静默丢数据。
3. **PIVOT 后不再保证「每料号一行」**：模板若有投入料号列 → `rowKeyFields=["销售料号","投入料号"]`；模板**没有**投入料号列（老模板）→ 数据侧 `code` 回退为销售料号，仍是每料号一行，`rowKeyFields=["销售料号"]` 照旧可用。
4. **电镀费页签现在可以是零件类型页签**（这正是加这两列的初衷，见 §5.5 与 `4-页签属性与树.md`）：模板有投入料号列 → `tabType=零件` + `partNoField=投入料号`（绑 `code`，**禁绑销售料号**，否则树把成品误判成零件节点）；模板没有 → 仍按费用页签处理，`tabType` 留空。

> 存量提醒：改造前落的 18 行旧语义数据（`finished_material_no IS NULL`）已由 `V372` 清理并需重新导入 —— 旧行 groupKey 少一维，重导不会下线它们，两组 `is_current=true` 并存会让视图出重复行。

### 5.5 页签类型属性（tabType / partNoField / partNameField，task-0721）

task-0721（报价侧 BOM 树）给**组件（页签）级**新增 3 个配置项，用于树的类型判定 + 加叶子候选采集。完整规则见 `4-页签属性与树.md`；此处列报价侧要点：

| 配置（JSON 键 / 请求体 camelCase） | 含义 | 规则 |
|---|---|---|
| `tabType` | 页签类型 | `BOM`/`材质元素`/`零件`/`外购件`/`主件`（5 类，非法值 400；可空） |
| `partNoField` | 料号列字段名 | 该组件某字段 `name`，**可空**——客户模板无料号列时留空（见下方 🚫 段） |
| `partNameField` | 料号名称列字段名 | 该组件某字段 `name`，可空 |

> 🚩 4 类物料页签的硬门槛是**「料号列与名称列至少配一个」**（`ComponentService.assertPartNoFieldRequirement`，2026-07-23 放宽），两者皆空才 400；类型判定 `QuotationTreeService` **优先 `partNoField`、为空回落 `partNameField`**。
>
> 🚫 **模板没有料号列（2026-07-24 用户定）**：`partNoField` 留空，名称列担标识，料号**只在 SQL 里做 JOIN 键**（`AS hf_part_no` / 取名 JOIN / `ORDER BY`），**禁新增模板没有的可见料号字段**。完整正文 `1-总则与工作流.md §1.3.1`。

- **`tabType→characteristic`**：材质元素→`RECIPE`、零件→`ASSEMBLY`、外购件→`OUTSOURCED`；主件=成品/树根；`BOM`=树页签。
- `tabType='BOM'` 保存时后端**自动同步 `bomRecursiveExpand=true`**，且若组件已被 COSTING 模板引用会 **400** → **报价侧树页签必须新建专用组件，不得复用核价树组件**。
- 类型判定/候选料号**严格按 `partNoField` 显式取值，禁按字段名启发式猜**。
- 费用类页签（成品其他费用等）无物料语义 → `tabType` 留空，不参与树。**电镀费自 repair-0802 起是例外**：sheet 有了【投入料号】列，模板配了该列时 `tabType=零件` + `partNoField=投入料号`（绑 `unit_price.code`）；模板没有该列才留空（见 §5.4 随动规则 4）。

> 🚨 **料号列绑定值铁律（2026-07-23 用户定，仅在「模板确有料号列」时适用）：`partNoField` 绑的列必须是该 `tabType` 语义层级的料号，禁绑成品/主件料号。** 完整表见 `1-总则与工作流.md §1.3.2`。报价侧对照：
>
> | `tabType` | 料号列语义 | 应绑的列（V6） |
> |---|---|---|
> | **主件** | 成品/产品料号 | `material_master.material_no`（产品自身） |
> | **材质元素** | 材质料号 | `element_bom_item.material_part_no` |
> | **零件** | 组成件料号 | `material_bom_item.component_no`（组成件）；费用类源（`unit_price`）取 **`code`**（该费用项针对的料号），**不是 `finished_material_no`（成品）** |
> | **外购件** | 外购组成件料号 | `material_bom_item.component_no`（`characteristic='OUTSOURCED'`） |
>
> ✅ **`unit_price` 的列语义**：`code` = **零件料号**（该费用项针对的零件，如自制加工费=零件的加工费），`finished_material_no` = **成品料号**（该费用上卷到的成品）。故 `unit_price` 源的零件页签**料号列绑 `code`**（非 `finished_material_no`）。二者常同值（零件=成品自身时），但语义不同、别处会分叉——按**语义**绑 `code`。`hf_part_no`（驱动键，隐式 JOIN 按本行成品收窄）另说：费用类通常仍绑 `finished_material_no`，与料号列绑 `code` 不冲突。

**本项目组件映射（2026-07-24 修订：按模板实有列，不再造料号字段）**：

| 组件 | tabType | partNoField | partNameField | 备注 |
|---|---|---|---|---|
| 产品 | 主件 | 销售料号 | 客户料号名称 | 成品/树根；模板本就有销售料号列 |
| 材料成本 | 材质元素 | *(空)*；模板确有料号列才配 | 材质 | `element_bom_item.material_part_no` **只作 JOIN 键（逐行取材质名）+ `ORDER BY` 键**；~~加「材质料号」字段~~ **已废止** |
| 外购件成本 | 外购件 | *(空)*；模板确有料号列才配 | 组成件名称 | 源 `characteristic='OUTSOURCED'`；`component_no` **只作取名 JOIN 键**；~~加「组成件料号」字段~~ **已废止** |
| 加工费 | 零件 | 料号（模板确有此列） | 料件 | 绑 `unit_price.code`（零件料号）；`hf_part_no` 绑 `finished_material_no` |
| 成品其他费用 | *(空)* | — | — | 费用页签，不参与树 |
| 电镀费 | 零件（模板有投入料号列时）<br>否则 *(空)* | 投入料号 | 投入料号名称 | repair-0802：绑 `unit_price.code`（零件料号），`hf_part_no` 绑 `finished_material_no`；无该列则退回费用页签 |

> ⚠️ `tabType`/`partNoField`/`partNameField` 已于 2026-07-21 补进导出/导入 bundle（同 `rowKeyFields`）。老 bundle 无此字段导入后为 null。

### 5.6 🚨 报价树渲染的**两种页签契约**（树页签 vs 数据页签，绑定变量不同，不可混——2026-07-22 QT-2085 事故）

配了 `tabType=BOM` 树页签的模板，报价侧渲染（`ConfigureSnapshotService.snapshotLines`）**分两趟路由，两趟注入的绑定变量完全不同**：

| Pass | 覆盖的页签 | 引擎 | 框架注入的绑定变量 | 视图必须用的过滤契约 |
|---|---|---|---|---|
| **Pass 2 树渲染** | **仅 `tab_type='BOM'`**（判据 `BomTreeRenderService.isQuoteTreeTabType`） | `BomTreeRenderService.render(usage=QUOTE)` | **`:total_material_no`**（整棵 spine 的料号数组）+ `versionFilter` 宏 | `WHERE <料号列> = ANY(:total_material_no)`，且输出 `material_no`(本节点子件号) + `parent_no`(父号) 做边键匹配 |
| **Pass 1 普通平铺展开** | **其余所有数据页签**（材质元素/零件/外购件/费用页签…） | `componentDriverService.expand`（既有报价平铺路径） | **`:customerCode`** + 行料号（隐式 JOIN 注入 `hf_part_no`） | `WHERE ... customer_no = :customerCode`，输出 `<表>.material_no AS hf_part_no`（驱动键，接隐式 JOIN） |

**铁律**：
1. **`:total_material_no` 只在 Pass 2（BOM 树页签）注入**。数据页签走 Pass 1，`:total_material_no` **永不注入** → 若数据页签视图写 `= ANY(:total_material_no)`，运行时该变量无绑定 → **整表 0 行**（不报错、静默空）。这正是 QT-2085「只有产品页签有数据、材料成本/加工费全空」的根因。
2. **树页签**（产品/BOM）：视图用 `:total_material_no` + `material_no`/`parent_no` 边契约（见 §7.0）。
3. **数据页签**（材质元素/零件/外购件/费用）：视图**照常用 §4/§7 的平铺契约**（`hf_part_no` + `:customerCode`），**不要**因为「模板里有树」就改成 `:total_material_no`。它们在报价侧渲染为**独立平铺页签**（每行项目一份数据），**不挂到树节点上**。
4. 树的深度由 `costing_bom_tree_config` 的递归 SQL 决定（递归 `material_bom_item` 子件）。若产品的子件是 `characteristic='RECIPE'`（材质），递归条件**不能限死 `='ASSEMBLY'`**，否则树只剩根节点。默认递归全子件（RECIPE 材质 + ASSEMBLY 组成件都成节点）。

> 一句话：**只有 BOM 树页签换契约（`:total_material_no`），其余页签一律保持 `:customerCode` 平铺契约。** 把数据页签也改成树契约 = 静默返 0 行。

---

## 7. 组件配方大全（每种页签的完整配置，照抄即可）

> **用法**：拿到客户模板，按 sheet 名对号入座；每个配方给全 `tabType` / `driver=$view` / `rowKeyFields` / 逐字段（类型+绑定）/ 完整 SQL 视图。
> **字段类型判定**：标识/名称/单位/货币/料号 → `INPUT_TEXT`；量/率/金额/厚度/汇率 → `INPUT_NUMBER`；金额性质加 `is_amount=true`（数字列 `content="0"`，汇率 `"1"`）。详见 `2-组件与字段.md`。
> **🚫 配方里的字段清单是「最全形态」，要按客户模板实有列裁剪**——模板没有的列（尤其料号列）**一律不建**：`partNoField` 留空、名称列担标识、该料号只在 SQL 里当 JOIN 键（§5.5 🚫 段 / `1-总则与工作流.md §1.3.1`）。~~"要加模板里没有的料号列字段"~~ 的旧写法 2026-07-24 已废止。
> 全部视图 `_` 前缀别名（`hf_part_no` + 价格策略列除外）、`FROM` V6 表、带 `system_type='QUOTE'`/`is_current`/`:customerCode`。SQL 视图编写通则见 `3-SQL视图.md`。
> **例外——§7.0 树页签视图**：`tabType=BOM` 的树页签走另一套契约（`:total_material_no` + `material_no`/`parent_no`），**不用** `hf_part_no`/`:customerCode`（详见 §5.6）。其余所有数据页签（§7.1~§7.7）一律用平铺契约。

### 7.0 🌳 产品（BOM 树页签，`tabType=BOM`）——树契约，非平铺

> **仅当产品页签要渲染成 BOM 树时用本配方**（否则产品用 §7.1 平铺主件）。走 Pass 2 树渲染，绑定变量是 `:total_material_no`（**不是** `:customerCode`），输出 `material_no`(本节点子件号)/`parent_no`(父号) 做树边匹配（§5.6）。
>
> 前置：① 该组件 `tabType=BOM`（保存自动 `bomRecursiveExpand=true`；被 COSTING 引用会 400 → 须新建报价专用组件）；② 建一条 `costing_bom_tree_config`（`usage=QUOTE`, active）的递归 SQL，递归 `material_bom_item` **全子件**（RECIPE 材质 + ASSEMBLY 组成件都成节点，别限死 ASSEMBLY），输出 `root_no/material_no/bom_version/parent_no/node_path`，入参 `:production_part_nos` + 客户传播列 `_cust`。

`driver=$cp_view` · `rowKeyFields=["销售料号"]` · `partNoField=销售料号` · 字段绑定同 §7.1（`$cp_view._销售料号` 等）。

```sql
-- $cp_view（树契约：边式全子件 + 根分支；material_no=子, parent_no=父）
SELECT
  mbt.component_no AS material_no,          -- 本节点（子件号）
  mbt.material_no  AS parent_no,            -- 父节点
  mbt.component_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称, mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币, mm.standard_unit AS _单位, mcm.exchange_rate AS _汇率
FROM material_bom_item mbt
LEFT JOIN material_master mm        ON mm.material_no  = mbt.component_no
LEFT JOIN material_customer_map mcm ON mcm.material_no = mbt.component_no
WHERE mbt.system_type='QUOTE' AND mbt.is_current
  AND mbt.component_no = ANY(:total_material_no)
UNION ALL   -- 根分支：无 ASSEMBLY/子件父边的成品自身（树根）
SELECT mm.material_no, NULL::text,
  mm.material_no, mcm.customer_material_name, mcm.customer_product_no,
  mcm.quote_currency, mm.standard_unit, mcm.exchange_rate
FROM material_master mm
LEFT JOIN material_customer_map mcm ON mcm.material_no = mm.material_no
WHERE mm.material_no = ANY(:total_material_no)
  AND NOT EXISTS (SELECT 1 FROM material_bom_item x
                  WHERE x.component_no = mm.material_no AND x.system_type='QUOTE' AND x.is_current)
```

> ⚠️ 树页签只出树结构 + 主件表头；材质/工序等**数据仍在各自数据页签（§7.2/§7.6）平铺渲染**，不挂到树节点上。

### 7.1 产品（`tabType=主件`，单行/每料号一行）
`driver=$cp_view` · `rowKeyFields=["销售料号"]` · `partNoField=销售料号` · `partNameField=客户料号名称`

| 字段 | 类型 | 绑定 |
|---|---|---|
| 客户料号名称 | INPUT_TEXT | `$cp_view._客户料号名称` |
| 客户产品编号 | INPUT_TEXT | `$cp_view._客户产品编号` |
| 销售料号 | INPUT_TEXT | `$cp_view._销售料号` |
| 报价货币 | INPUT_TEXT | `$cp_view._报价货币` |
| 单位 | INPUT_TEXT | `$cp_view._单位` |
| 汇率 | INPUT_NUMBER | `$cp_view._汇率`（content="1"） |
| 报价 | INPUT_NUMBER(¥) | *(无源，销售手填，content="0")* |

```sql
-- $cp_view
SELECT
  mm.material_no AS hf_part_no,
  mm.material_no AS _销售料号,
  mcm.customer_material_name AS _客户料号名称,
  mcm.customer_product_no AS _客户产品编号,
  mcm.quote_currency AS _报价货币,
  mm.standard_unit AS _单位,
  mcm.exchange_rate AS _汇率
FROM material_master mm
LEFT JOIN material_customer_map mcm
  ON mcm.material_no = mm.material_no AND mcm.customer_no = :customerCode
```

### 7.2 材料成本（`tabType=材质元素`，多行/逐元素）
`driver=$mc_view` · `rowKeyFields=["销售料号","元素","材质"]` · `partNameField=材质` · `partNoField`：**模板有「材质料号」列才配成它，没有就留空**（🚫 不新建该字段）
材质逐行取自 `material_part_no`（§5.2 一号多材质铁律）；元素单价接价格策略（§5.2.1）。

| 字段 | 类型 | 绑定 |
|---|---|---|
| 销售料号 | INPUT_TEXT | `$mc_view._销售料号` |
| 材质 | INPUT_TEXT | `$mc_view._材质` |
| 材质料号 *(仅模板确有此列时才建)* | INPUT_TEXT | `$mc_view._材质料号`（不建字段时视图也不出这列，`material_part_no` 只当 JOIN/ORDER BY 键） |
| 产出类型 | INPUT_TEXT | `$mc_view._产出类型` — 🚨 源是 **`material_bom_item.component_usage_type`**（物料BOM 的「产出料号类型」），**不是 `element_bom_item` 的同名列**。后者无任何写入路径，绑了**恒为空**（2026-07-28 实证，见配方注释与 §7.2.1） |
| 材质占比 *(仅模板确有此列时才建)* | INPUT_NUMBER（非金额） | `$mc_view._材质占比` — 源是 **`material_bom_item.material_ratio`**（物料BOM「材质占比」，V365 新增）。`element_bom_item` 上**没有**这个列，绑 `ebi.*` 直接 SQL 报错。同款跨表标量子查询，但**多一个 `AND t.characteristic='RECIPE'`**；小数口径（`0.3`=30%），见 §7.2.2 |
| 组成数量 | INPUT_NUMBER | `$mc_view._组成数量` |
| 项次 | INPUT_NUMBER | `$mc_view._项次` |
| 元素 | INPUT_TEXT | `$mc_view._元素` |
| 组成含量（%） | INPUT_NUMBER | `$mc_view._组成含量` |
| 损耗率% | INPUT_NUMBER | `$mc_view._损耗率` |
| 毛重 | INPUT_NUMBER | `$mc_view._毛重` |
| 毛用量单位 | INPUT_TEXT | `$mc_view._毛用量单位` |
| 元素单价 | INPUT_NUMBER(¥) | `$mc_view.单价`（价格策略列，别名逐字、不加 `_`） |
| 货币 | INPUT_TEXT | `$mc_view.货币`（价格策略列） |

```sql
-- $mc_view（材质逐行取 material_part_no + 元素单价接价格策略 f_customer_element_price）
SELECT
  ebi.material_no AS hf_part_no,
  ebi.material_no AS _销售料号,
  COALESCE(mr.name, mm2.material_name) AS _材质,   -- 逐行取自本行 material_part_no
  ebi.material_part_no AS _材质料号,               -- ⚠️ 仅模板确有「材质料号」列时才输出；否则删掉本行（该列只做 JOIN/ORDER BY 键）
  -- 「产出类型」= 物料BOM 的「产出料号类型」（`MaterialBomMergeHandler:148` 经 labelOnly() 写入
  --   material_bom_item.component_usage_type，取值如 触点/铜件/AgNi10触点/银点类）。
  -- 🚨 禁写 `ebi.component_usage_type`：element_bom_item 上虽有同名列，但**无任何写入路径**
  --    （「物料与元素BOM」sheet 没有这一列，全工程 3 个写点都写 material_bom_item）→ 绑了恒为空。
  -- 🔒 必须用相关标量子查询而非 LEFT JOIN：① material_bom_item 是版本化表（§3.6 纪律）
  --    ② 同一对父子常并存 RECIPE+ASSEMBLY 多条边（§3.8.3 铁律 3）→ JOIN 会让材料成本行倍增。
  -- 🔒 ORDER BY 优先 RECIPE 边：材质料号对应的是材质边，取错边会显示成「组装件」。
  (SELECT t.component_usage_type
     FROM material_bom_item t
    WHERE t.system_type  = 'QUOTE' AND t.is_current
      AND t.customer_no  = :customerCode
      AND t.material_no  = ebi.material_no
      AND t.component_no = ebi.material_part_no
    ORDER BY CASE WHEN t.characteristic = 'RECIPE' THEN 0 ELSE 1 END, t.characteristic
    LIMIT 1) AS _产出类型,
  -- 「材质占比」= 物料BOM 的同名列（`MaterialBomMergeHandler` 写 material_bom_item.material_ratio，
  --   V365 新增；小数口径 0.3=30%）。⚠️ 仅模板确有「材质占比」列时才输出，否则删掉本段。
  -- 🚨 element_bom_item 上**没有**这个列 —— 绑 `ebi.material_ratio` 不是"恒为空"而是**直接 SQL 报错**。
  -- 🔒 与 _产出类型 同样必须用相关标量子查询（版本化表 + 一对父子多条边，LEFT JOIN 会让行倍增）。
  -- 🔒 但这里用 `AND t.characteristic='RECIPE'` 硬过滤，而不是 ORDER BY 优先：该列只有材质边写值
  --    （零件/外购件行 handler 显式置 NULL），退化到 ASSEMBLY 边取到的必然是 NULL，不如直接限定。
  (SELECT t.material_ratio
     FROM material_bom_item t
    WHERE t.system_type  = 'QUOTE' AND t.is_current
      AND t.customer_no  = :customerCode
      AND t.material_no  = ebi.material_no
      AND t.component_no = ebi.material_part_no
      AND t.characteristic = 'RECIPE'
    LIMIT 1) AS _材质占比,
  ebi.base_qty AS _组成数量,
  ebi.seq_no AS _项次,
  ebi.component_no AS _元素,
  ebi.content AS _组成含量,
  ebi.scrap_rate AS _损耗率,
  ebi.composition_qty AS _毛重,
  ebi.issue_unit AS _毛用量单位,
  cep.unit_price AS 单价,        -- 价格策略列：别名逐字、不加 _（硬约束 6）
  cep.currency AS 货币
FROM element_bom_item ebi
  LEFT JOIN material_recipe mr  ON mr.code = ebi.material_part_no
  LEFT JOIN material_master mm2 ON mm2.material_no = ebi.material_part_no
  LEFT JOIN f_customer_element_price(:customerCode, :priceBaseDate) cep
       ON cep.element_code = ebi.component_no          -- LEFT JOIN + 元素符号键（硬约束 1/2）
WHERE ebi.system_type = 'QUOTE' AND ebi.is_current AND ebi.customer_no = :customerCode
ORDER BY ebi.material_no, ebi.material_part_no, ebi.seq_no
```
> `required_variables=["customerCode","priceBaseDate"]`。若本客户模板的单价列不接价格策略（旧模型），删 `f_customer_element_price` JOIN + 单价/货币两输出列，改回手填 `INPUT_NUMBER` 无源。**禁 `COALESCE(cep.unit_price,0)`**（硬约束 3）。

#### 7.2.1 🚨「产出类型」踩坑实录（2026-07-28，施耐德-1 返修）

**症状**：材料成本页签「产出类型」列**恒为空**，不报错、dry-run 通过、`expand-driver` 也正常，纯静默。

**根因**：绑成了 `ebi.component_usage_type`。`element_bom_item` 上**确实有**这个列（`ElementBomItem.java:64`），列名也跟业务叫法对得上——但它**没有任何写入路径**：

- 报价侧「物料与元素BOM」sheet 表头 = `销售料号|材质料号|材质料号名称|项次|元素|组成含量(%)|损耗率%|毛用量|毛用量单位|净用量|净用量单位`，**根本没有产出类型这一列**；
- 全工程写 `component_usage_type` 的只有 3 处，**全部写 `material_bom_item`**：`MaterialBomMergeHandler.java:148`（报价侧「产出料号类型」）/ `P06MaterialBomHandler.java:95`（核价侧「使用特性」）/ `ConfigureProductService.java:724`（选配）；
- 库里实证：`element_bom_item.component_usage_type` QUOTE 83 行 + PRICING 14 行 **100% NULL**。

**A/B 实证**（`cpq_db_0724`，单事务造数 + `ROLLBACK`）：

| 场景 | 行数 | `_产出类型` |
|---|---|---|
| 旧绑定 `ebi.component_usage_type`，**给 mbi 造数后** | 4 | 仍全 NULL ← 铁证绑错表 |
| 新绑定（标量子查询） | 4 | `(S-80011,00002)` 出「触点」，无对应 BOM 边的行留 NULL **且不掉行** |
| 反例：改用 `LEFT JOIN material_bom_item` + 造第二条 ASSEMBLY 边 | **6**（倍增）| 还被 ASSEMBLY 边的「组装件」污染 |
| 采用的标量子查询 + 同样两条边 | 4（不变）| 「触点」← ORDER BY 优先 RECIPE 生效 |

**取值长什么样**（真实客户文件的「产出料号类型」列，是产出物品名/类型，不是枚举）：罗克韦尔 `铜件`/`触点`；伊顿 `AgNi10触点`/`AgSnO2(12)-H65接触板`；森萨塔、西安中熔 `Cu/Ag/Cu带`；老版 V2 `1.银点类`/`2.非银点类`（`labelOnly()` 会剥掉前导编号 → `银点类`）。

**它是逐行的，不是 per 销售料号**：罗克韦尔文件同一个 `S-3120014539` 下 `991/H65带→铜件`、`992/AgNi11#-Ⅰ→触点`。所以 JOIN 键必须是 `(material_no, component_no=ebi.material_part_no)` 两维。

⚠️ **数据侧另一层**：施耐德那份 `报价系统模板0723.xlsx` 的物料BOM「产出料号类型」**整列空白**，且本库 QUOTE 侧 `material_bom_item` 全部 146 行该列也 100% NULL。所以**绑定修好后该列在当前数据下仍显示空**——要出数得先让业务把导入文件那列填上。别把"改完还是空"误判成没修好。

🔤 **表头写法（2026-08-01）**：导入 sheet 的这列，写「产出料号类型」或「产出类型」都认（`MaterialBomMergeHandler.USAGE_TYPE_HEADERS`）。实测填了值就能落库：`1.银点类 → 银点类`、`组成件 → 组成件`（前导"N."被 `labelOnly()` 剥掉）。

#### 7.2.2 「材质占比」绑定规则（2026-07-31，V365 新增列）

**它在哪**：`material_bom_item.material_ratio`，由报价侧「物料BOM」sheet 的**可选列**「材质占比」写入（`MaterialBomMergeHandler`）。`numeric(18,6)`、**非必填**、**小数口径**（`0.3` = 30%，与 `element_bom_item.content` 同口径，**不是** `scrap_rate`/`defect_rate` 那种百分数值）。

**只有材质边有值**：物料BOM 是三态单表，handler 按 `characteristic='RECIPE' ? 取值 : NULL` 写入——零件（ASSEMBLY）/外购件（OUTSOURCED）行即使业务在 Excel 里误填了也落 NULL。核价侧 `P06MaterialBomHandler` 不写该列，PRICING 行恒 NULL。

**绑法按页签主表分两种**：

| 页签驱动视图 `FROM` | 绑法 |
|---|---|
| `element_bom_item`（材料成本 `$mc_view`） | **相关标量子查询跨表取**（见 §7.2 配方），键 `(t.material_no=ebi.material_no, t.component_no=ebi.material_part_no)` + `t.characteristic='RECIPE'` |
| `material_bom_item` **且不按 characteristic 过滤**（BOM 树页签 `$bom_view`） | ✅ **直接 `mbi.material_ratio`**——树页签的行是三态全部边，其中材质边正好带值。**材质边出数、组装边留空是预期**，不是漏配（施耐德-v3「物料」页签实证：`00002←S-80011 (AgC3)` 取到 `0.350000`） |
| `material_bom_item` **但 WHERE 限定了非材质边**（外购件 `$wg_view` 的 `characteristic='OUTSOURCED'`、零件类） | 绑了**恒 NULL**——这些页签按定义就取不到材质边。模板真要这列，说明该列该放在别的页签上 |

**与 §7.2.1「产出类型」的三点差别**（别照抄）：

1. **`element_bom_item` 上没有 `material_ratio` 这个列** —— 产出类型绑错表是"恒为空"的静默失败，材质占比绑错表是 `column ebi.material_ratio does not exist` **直接报错**，dry-run 当场拦下。这反而是好事：不会静默。
2. **用 `AND t.characteristic='RECIPE'` 硬过滤，不用 `ORDER BY` 优先** —— 产出类型三态都有值，取错边会显示成「组装件」，所以要 ORDER BY 择优；材质占比只有 RECIPE 边有值，退化到别的边取到的必然是 NULL，直接限定更清晰。
3. **字段类型 `INPUT_NUMBER` 且 `is_amount=false`** —— 它是比率不是钱，不进金额小计（同 §7.2「损耗率」「组成含量」口径）。

**A/B 实证**（`cpq_db_0724`，单事务造数 + `ROLLBACK`，2026-07-31）：

| 场景 | 行数 | `_材质占比` |
|---|---|---|
| 现状（存量数据该列全 NULL） | 4 | 全 NULL，但**键是对的**——`(CUST-0001, S-80011, 00002)` 的 RECIPE 边确实存在，只是没值 |
| 给该 RECIPE 边 `UPDATE material_ratio=0.3` | 4 | `(S-80011,00002)` 两行出 `0.300000`，其余留 NULL **且不掉行** |
| 再造一条同父子的 ASSEMBLY 边（倍增反例场景） | **4（不变）** | 仍 `0.300000` ← `characteristic='RECIPE'` 过滤生效，ASSEMBLY 边的 NULL 没盖掉它 |
| 反例：绑 `ebi.material_ratio` | — | `ERROR: column ebi.material_ratio does not exist` ← 当场报错，非静默 |

🔤 **表头写法（2026-08-01 修）**：导入 sheet 的这列，写「材质占比」或「材料占比」都认（`MaterialBomMergeHandler.MATERIAL_RATIO_HEADERS`）。**V365 刚落地那版只认「材质占比」**——客户按卡片列名/视图别名 `_材料占比` 的叫法加列时，`SheetRow.getStr` 的 contains 匹配不上，**静默按"没填"处理**（不报错、不计失败行、整列丢数），A/B 实测：同一份数据只改表头，「材料占比」全 NULL、「材质占比」正常落 0.3/0.7/0.35。

⚠️ **"改完还是空"的排查顺序**（同 §7.2.1 的数据侧陷阱）：① 客户导入文件的「物料BOM」sheet 有没有这一列、填没填（表头两种写法都行，但**必须是这两种之一**）② 该行是不是材质行（投入料号是材质编码、`characteristic='RECIPE'`）③ 才轮到看视图绑定。列是 2026-07-31 才加的，**在此之前导入的存量数据该列全 NULL，重导才有值**。

### 7.3 成品其他费用（`tabType` 空，多行/逐费用项）
`driver=$qt_view` · `rowKeyFields=["销售料号","项次","要素名称"]` · 费用页签不参与树。
> sheet 名叫「成品其他费用」用本配方；叫「加工费」用 §7.6。

| 字段 | 类型 | 绑定 |
|---|---|---|
| 销售料号 | INPUT_TEXT | `$qt_view._销售料号` |
| 项次 | INPUT_NUMBER | `$qt_view._项次` |
| 要素名称 | INPUT_TEXT | `$qt_view._要素名称`（=`cost_type`：材料管理费/利润/税率…） |
| 值 | INPUT_NUMBER(¥) | `$qt_view._值` |
| 货币 | INPUT_TEXT | `$qt_view._货币`（仅模板有「货币」列时加） |
| 单位 | INPUT_TEXT | `$qt_view._单位` |

```sql
-- $qt_view（有「货币」列的模板保留 _货币 行，否则删）
SELECT
  up.code AS hf_part_no,
  up.code AS _销售料号,
  up.seq_no AS _项次,
  up.cost_type AS _要素名称,
  up.pricing_price AS _值,
  up.currency AS _货币,
  up.unit AS _单位
FROM unit_price up
WHERE up.system_type = 'QUOTE' AND up.price_type = 'FINISHED_MATERIAL_OTHER' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no
```
> 建议配 `sort_field='项次'`（多行页签数字正序；视图 ORDER BY 对 DRAFT 无效，见 `4-页签属性与树.md`）。

### 7.4 电镀费（行转列 PIVOT；repair-0802 后按「销售料号 × 投入料号」出行）
`driver=$dp_view` · 见 §5.4 电镀费行转列铁律（含 repair-0802 四条随动规则）。

**两套配法，按客户模板有没有「投入料号」列二选一：**

| | 模板**有**投入料号列（推荐，新口径） | 模板**没有**（老模板兼容） |
|---|---|---|
| `tabType` | `零件` | *(空)*，费用页签不参与树 |
| `partNoField` | `投入料号`（绑 `up.code`，🚫 禁绑销售料号） | — |
| `partNameField` | `投入料号名称`（有该列时） | — |
| `rowKeyFields` | `["销售料号","投入料号"]` | `["销售料号"]` |
| 出行粒度 | 每（销售料号 × 投入料号）一行，可多行 | 每料号一行（`code` 回退=销售料号） |

| 字段 | 类型 | 绑定 |
|---|---|---|
| 销售料号 | INPUT_TEXT | `$dp_view._销售料号`（= `finished_material_no`） |
| 投入料号 | INPUT_TEXT | `$dp_view._投入料号`（= `code`；模板无此列则不建，见 🚫 铁律） |
| 投入料号名称 | INPUT_TEXT | `$dp_view._投入料号名称`（JOIN `material_master`/`material_recipe` 取名） |
| 电镀元素名称 | INPUT_TEXT | `$dp_view._电镀元素名称`（unit_price 无源→NULL；**≠ 投入料号名称**） |
| 镀层厚度（μm） | INPUT_NUMBER | `$dp_view._镀层厚度`（无源→NULL） |
| 电镀加工费 | INPUT_NUMBER(¥) | `$dp_view._电镀加工费` |
| 电镀材料费 | INPUT_NUMBER(¥) | `$dp_view._电镀材料费` |
| 货币 | INPUT_TEXT | `$dp_view._货币` |
| 计价单位 | INPUT_TEXT | `$dp_view._计价单位` |

```sql
-- $dp_view（repair-0802 新口径：hf_part_no 绑 finished_material_no、GROUP BY 含 code）
SELECT
  up.finished_material_no AS hf_part_no,          -- 收窄键=成品，禁用 up.code（零件号匹配不上本行成品→0 行）
  up.finished_material_no AS _销售料号,
  up.code                 AS _投入料号,           -- 零件料号；两列皆空导入时回退=销售料号
  MAX(COALESCE(mm.material_name, mr.name)) AS _投入料号名称,
  NULL::varchar AS _电镀元素名称,                  -- 镀层元素仍无源，别拿投入料号名称顶替
  NULL::numeric AS _镀层厚度,
  MAX(CASE WHEN up.cost_type = '电镀加工费' THEN up.pricing_price END) AS _电镀加工费,
  MAX(CASE WHEN up.cost_type = '电镀材料费' THEN up.pricing_price END) AS _电镀材料费,
  MAX(up.currency) AS _货币,
  MAX(up.unit) AS _计价单位
FROM unit_price up
  LEFT JOIN material_master mm ON mm.material_no = up.code
  LEFT JOIN material_recipe mr ON mr.code = up.code
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PLATING' AND up.is_current = true
  AND up.customer_no = :customerCode
GROUP BY up.finished_material_no, up.code        -- 少 up.code 会把同成品下多个零件 MAX() 成一行，静默丢数
ORDER BY up.finished_material_no, up.code
```

> 老模板兼容说明：不建投入料号字段时，上面的 SQL 原样可用（`_投入料号` 列不被任何字段引用即可），`GROUP BY` 仍须保留 `up.code` —— 否则一旦客户开始填投入料号就静默丢数据。

### 7.5 外购件成本（`tabType=外购件`，多行/逐组成件）
`driver=$wg_view` · `partNameField=组成件名称` · `partNoField`/`rowKeyFields` **按模板实有列二选一**：
- 模板**有**「组成件料号」列 → `partNoField=组成件料号`、`rowKeyFields=["销售料号","组成件料号"]`
- 模板**没有**（常见）→ `partNoField` 留空、`rowKeyFields=["销售料号","组成件名称"]`，`component_no` 只当取名 JOIN 键（🚫 不新建料号字段）

数据源 `characteristic='OUTSOURCED'`（当前 0 行，待数据）。

| 字段 | 类型 | 绑定 |
|---|---|---|
| 销售料号 | INPUT_TEXT | `$wg_view._销售料号` |
| 组成件料号 *(仅模板确有此列时才建)* | INPUT_TEXT | `$wg_view._组成件料号` |
| 组成件名称 | INPUT_TEXT | `$wg_view._组成件名称` |
| 组成数量 | INPUT_NUMBER | `$wg_view._组成数量` |
| 组成单位 | INPUT_TEXT | `$wg_view._组成单位` |

```sql
-- $wg_view
SELECT
  mbi.material_no AS hf_part_no,
  mbi.material_no AS _销售料号,
  mbi.component_no AS _组成件料号,          -- ⚠️ 仅模板确有「组成件料号」列时才输出；否则删本行（只当取名 JOIN 键）
  COALESCE(mm.material_name, mr.name) AS _组成件名称,   -- material_recipe 兜底（V341/V342 口径）
  mbi.composition_qty AS _组成数量,
  mbi.issue_unit AS _组成单位
FROM material_bom_item mbi
  LEFT JOIN material_master mm ON mm.material_no = mbi.component_no
  LEFT JOIN material_recipe mr ON mr.code = mbi.component_no
WHERE mbi.system_type = 'QUOTE' AND mbi.is_current AND mbi.characteristic = 'OUTSOURCED'
  AND mbi.customer_no = :customerCode
ORDER BY mbi.material_no, mbi.seq_no
```
> ⚠️ 无料号列时行键靠「组成件名称」，**同名多行会撞键**（editRows 塌缩）；真遇到再找用户确认是否把料号列显出来。

### 7.6 加工费（legacy——sheet 名叫「加工费」时用；`tabType` 空或零件）
`driver=$pf_view` · `rowKeyFields=["销售料号","项次","要素名称"]` · 要素名称=工序名（§5.3）。
> ⚠️ 与 §7.3 成品其他费用区别：加工费=`price_type='PROCESS'` + 要素名称走 `process_master.process_name`；成品其他费用=`price_type='FINISHED_MATERIAL_OTHER'` + 要素名称=`cost_type`。**按 sheet 名选**。
> ⚠️ 若本页签设 `tabType=零件`，`partNoField` 绑「零件料号」列（视图加 `up.code AS _料号`），**非 `finished_material_no`**（§5.5 料号铁律）。`hf_part_no` 仍绑 `finished_material_no` 按成品收窄。

字段：销售料号/[零件料号]/项次/要素名称/值(¥)/[货币]/单位。
```sql
-- $pf_view
SELECT
  up.finished_material_no AS hf_part_no,     -- 驱动键：按成品收窄本报价行
  up.finished_material_no AS _销售料号,
  up.code AS _料号,                          -- 零件料号（partNoField，零件页签绑此列）
  up.seq_no AS _项次,
  COALESCE(pm.process_name, up.operation_no) AS _要素名称,
  up.pricing_price AS _值,
  up.unit AS _单位
FROM unit_price up
  LEFT JOIN process_master pm ON pm.process_no = up.operation_no
WHERE up.system_type = 'QUOTE' AND up.price_type = 'PROCESS' AND up.is_current = true
  AND up.customer_no = :customerCode
ORDER BY up.seq_no
```

### 7.7 小计（`componentType=SUBTOTAL`）
sheet 名含「小计」时建 SUBTOTAL 组件：**无字段、无 driver、无 SQL 视图**。成本聚合靠模板/组件的 `component_subtotal` 公式 token 引用各 NORMAL 组件的 `is_subtotal` 列，见 `5-公式与Excel列.md`，本文不展开。

> **数据现状提示**：客户未登记 / V6 无数据时，视图 dry-run 通过但返 0 行，字段仍可输入（三级兜底见 `2-组件与字段.md`），数据导入后自动显值。`OUTSOURCED` 当前全表 0 行。

---

## 已交付实例

| 客户 | 目录 / 版本 | 组件构成 | 备注 |
|---|---|---|---|
| **西安中熔** | dir `acf3f8a2` / v2 `e90deab5` | 产品(COMP-0062,$cp_view,rk=[销售料号]) / 材料成本(COMP-0063,$mc_view,rk=**[销售料号,元素,材质]**) / 加工费(COMP-0064,$pf_view,rk=[销售料号,项次,要素名称]) + 小计 / 材料费 / 小计 | 首份落地（2026-07-16 沉淀规则）；材质=逐行 `material_part_no`（一号多材质铁律 §5.2）；加工费要素名=`process_master.process_name`（§5.3）；v2 组件 COMP-0306 产品(cp_view 树契约)/COMP-0307 材料成本(mc_view 平铺)/COMP-0308 加工费(pf_view 平铺)，树配置 `5251e427`(usage=QUOTE 全子件) |
| **罗克韦尔** | dir `f4e2bca1`（`__imp1`，10 组件） | 产品=主件 / 元素=材质元素(+材质料号字段，元素单价接价格策略 ys_view) / **来料=BOM 树** / 其他费用=空 / 外购件=空 / 电镀费用=零件 / 组装加工费=空 / 自制加工费=零件 | 权威客户模型；逐个页签问用户定 tabType（勿猜——「来料」是 BOM 非零件）；配 5 个 `sort_field`（其他费用/电镀/自制加工/组装加工=项次、元素=序号）；自制加工费料号列绑 `code`（非 finished）；live 验 QT-20260717-2050（CUST-1269/S-3120014539）8 页签全渲染、来料真树 4 节点 |
| **罗克韦尔（2026-07-24 · 新库 `cpq_db_0724`）**<br>⚠️ **JSON 已被 2026-07-26 版覆盖（见下一行）；本行保留是因为它确立的「模板没有料号列就不造字段」口径依然有效** | JSON `客户组件模板/组件导入-罗克韦尔.json`（bundle **5 组件**，用户自建目录后导入；2026-07-24 晚模板加 BOM sheet 后重出，覆盖原 4 组件版） | 产品(主件,$cp_view,rk=[销售料号]) / **BOM(BOM 树,$bom_view,partNoField 空,partName=料件名称,rk=[料件名称])** / 材料成本(材质元素,$mc_view,**partNoField 空**,partName=材质,rk=[销售料号,材质,元素],sort=项次,元素单价接价格策略) / 外购件成本(外购件,$wg_view,**partNoField 空**,partName=组成件名称,rk=[销售料号,组成件名称]) / 加工费(零件,$jg_view,partNo=料号=`up.code`,partName=料件,rk=[项次,工序],sort=项次) | **本条确立「模板没有料号列就不造字段」口径**（§5.5 🚫 段）：`material_part_no`/`component_no` 只作 SQL JOIN+ORDER BY 键；字段与模板列严格 1:1；旧库 `cpq_db`(CUST-1269 真数据)背靠背验 JOIN 正确（材质出 AgC3/AgNi11#-Ⅰ/银镍合金、工序出 酸洗/电镀）。<br>⚠️ **BOM 树页签有 bundle 带不走的前置**：`costing_bom_tree_config`(usage=QUOTE, is_active) 递归 SQL 是 **DB 行、不进 bundle**，`cpq_db_0724` 当前**一条都没有** → 导入后 BOM 页签仍不出树，须另建（老库 `cpq_db` 有可抄的「报价BOM树-QUOTE口径v1」）。BOM 字段绑 `material_bom_item`：净重/毛重/用量单位=`net_weight`/`rough_weight`/`weight_unit`(重量口径，实测值 `g/kg`)，组成数量/组成单位=`composition_qty`/`issue_unit`(发料口径) |
| **罗克韦尔（2026-07-26 晚 · 「料号列」改语义为「所属销售料号」+ BOM 闭包，当前有效版）** | JSON `客户组件模板/组件导入-罗克韦尔.json`（bundle **5 组件**，checksum `1a78965e…`；覆盖当日早版同名文件） | 产品(主件,$cp_view,partNo=销售料号,partName=客户料号名称,rk=[销售料号],**不做闭包**) / BOM(BOM 树,$bom_view,partNo 空,partName=料件,rk=[料件]) / 材料成本(材质元素,$mc_view,**partNo 空**,partName=材质,rk=[**料号**,材质,元素],sort=项次,元素单价接价格策略) / 外购件成本(外购件,$wg_view,**partNo 空**,partName=**料件**,rk=[料号,料件]) / 加工费(零件,$jg_view,**partNo 空**,partName=料件,rk=[料号,料件,项次,工序],sort=项次) | 模板把三个数据页签的「料号」列**重新定义为「所属销售料号」**（= task-0726 §3.8.4 的 `_归属料号`，用户在模板备注里写死）→ ①三视图 `_料号` 改绑 `ebi.material_no`/`mbi.material_no`/`up.finished_material_no`；②`material_part_no`/`component_no`/`up.code` 退回**只作 JOIN + ORDER BY 键**；③三页签 `partNoField` **全部留空**（绑成品料号给零件/材质页签＝§5.5 铁律硬错），由名称列担标识；④「料号」已按 §3.8.4 进 `rowKeyFields` 防撞键。加工费 rk 在模板勾选基础上**补了「项次」**（§5.3 硬规则：同料号同工序可重复，seq_no 才是唯一保证）。<br>🔧 **本版修了 §3.8.2 闭包写法的行翻倍缺陷**（新增铁律 3：递归 `UNION` + `bom_closure_d` 去重）——旧库 `cpq_db` CUST-1269 实测 `0363-2607000009` 材料成本 8 行（每行重复 2 次，因 RECIPE/ASSEMBLY 双边）→ **4 行** ✅。<br>验证：5 个 `$view` 保存期 dry-run 全过；`expand-driver` 真管道实跑无报错、谓词正常包成 `inner_q WHERE hf_part_no = ANY(?)`（铁律 1 未破）；旧库背靠背——材料成本出子件 …07/…08 的 Ag/Cu/Ni 且 `_料号` 正确显归属、加工费出「分条/长管质量处理」、闭包为空的单料产品仍出自己的行（铁律 2）；导入预演 `checksumValid=true / canCommit=true / 5×CREATE / 0 blocker`。<br>✅ **pending 开域实测（`cpq_db_0724` 真数据）**：该库 QUOTE 基础数据 `is_current` 全为 false 但**每行都带 `pending_quotation_id`** —— 这是「导入后待审核、尚未转正」的**正常态**，不是坏数据（task-0725 已把 `QuotePendingScope` 接进报价驱动链路，DRAFT 报价单渲染时 `QuotePendingRewriter` 把白名单表整表替换成 `(t.is_current OR t.pending_quotation_id = :pq)` 开域）。按该规则原样模拟改写后实跑 DRAFT 单 `2168c574`（料号 `S-3120014539`）：**材料成本 4 行**（主件 H65/Cu + AgNi11#-Ⅰ/Ag，**子件 `S-80011` 的 AgC3 的 Ag/C**，元素单价出 1850 / 118478，无价元素 C 留 NULL 不掉行）、**加工费 2 行**（主件 Z380 + 子件 Z381）、**外购件成本 2 行**（同名「组成件1」数量 2/3，**靠新的「料号」归属列才分得清**——正是 §3.8.4 举的那个案例）。闭包 CTE 内的 `material_bom_item` 同样被 pending 改写（改写器对**任意深度**的白名单表 token 都替换），故子件闭包在 pending 数据下照常成立。 |
| **罗克韦尔（2026-07-26 早 · 模板补勾料号列后重出，已被上一行覆盖）** | JSON `客户组件模板/组件导入-罗克韦尔.json`（bundle **5 组件**，checksum `5386af25…`；覆盖 0724 版同名文件） | 产品(主件,$cp_view,partNo=销售料号,partName=客户料号名称,rk=[销售料号]) / BOM(BOM 树,$bom_view,partNo 空,partName=**料件**,rk=[料件]) / 材料成本(材质元素,$mc_view,**partNo=料号 → `ebi.material_part_no`(材质料号)**,partName=材质,rk=[料号,材质,元素],sort=项次,元素单价接价格策略) / 外购件成本(外购件,$wg_view,**partNo=料号 → `mbi.component_no`**,partName=外购件名称,rk=[料号],**新增手填「单价」列**(无源)) / 加工费(零件,$jg_view,partNo=料号 → `up.code`,partName=料件,rk=[项次,工序],sort=项次) | 用户在模板里给**材质元素 / 外购件补勾了「料号列」** → 按 §5.5 / `1-总则与工作流.md §1.3.2` 铁律绑**本页签语义层级**的料号（材质料号 / 组成件料号），**不是销售料号**；这两个页签因此不再出 `_销售料号` 可见列（`hf_part_no` 仍绑 `material_no` 做驱动键）。模板同批改名：BOM「料件名称」→「料件」、外购件「组成件名称」→「外购件名称」。<br>✅ **0724 版那条「`cpq_db_0724` 一条 `costing_bom_tree_config` 都没有」的前置已消除**——V363 迁移把 QUOTE/COSTING 两条递归 SQL 纳入版本控制（新库自带，实测各 1 条 active）。<br>验证：5 个 `$view` 保存期 dry-run 全过；旧库 `cpq_db`(CUST-1269 真数据)背靠背实跑——材质料号出 `00002/992/AgNi90/991`、工序出 电镀/酸洗/分条/正火退火、BOM 树出 12 条边(含根分支)、元素单价无策略返 **NULL 且 12 行不掉行**；`characteristic='OUTSOURCED'` 全库 0 行 → 外购件页签暂 0 行（**数据未导，非配置错**，去掉该过滤同 SQL 出数）。bundle 导入预演 `checksumValid=true / canCommit=true / 5×CREATE / 0 blocker`。 |
| **施耐德-1（2026-07-27 · 首出，8 组件；新增 3 类页签配方）** | JSON `客户组件模板/组件导入-施耐德-1.json`（bundle **8 组件**，checksum `5d49d3b5…`）；输入 `客户组件模板/组件模板-施耐德-1.xlsx` | 产品(主件,$cp_view,partNo=销售料号,partName=客户料号名称,rk=[销售料号],+「报价货币」列) / **物料(BOM 树,$bom_view,partNo 空,partName=料件,rk=[料件])** / 材料成本(材质元素,$mc_view,**partNo=料号 → `ebi.material_part_no`**,partName=料件,rk=[销售料号,料号,元素],sort=项次,元素单价+货币接价格策略,+「产出类型」=`component_usage_type`,+ 手填「材料成本/材料损耗成本」两列) / 外购件成本(外购件,$wg_view,partNo 空,partName=料件,rk=[销售料号,料件]) / 加工费(零件,$jg_view,**partNo=料号 → `up.code`**,partName=料件,rk=[销售料号,料号,项次,工序],sort=项次) / **组装加工费(主件,$zz_view,源 `capacity`)** / **来料固定加工费(tabType 空,$ll_view,`INCOMING_MATERIAL_PROCESS`)** / **其他费用(主件,$qt_view,`FINISHED_MATERIAL_OTHER`,partNo=销售料号,rk=[项次,类别])** | **三条新配方（罗克韦尔母版没有，可复用）**：<br>① **组装加工费 → `capacity`（QUOTE）**，非 `unit_price`：`c.material_no AS hf_part_no` 按成品收窄（该表**无 `customer_no` 列**，不加 `:customerCode`）、`fixed_cost`=加工费、`capacity_unit`=单位；现网工序名写在 **`process_no`**（`process_name` 空、新库 `process_master` 0 行）→ 工序绑 `COALESCE(NULLIF(c.process_name,''), c.process_no)`，实测出「焊接/铆接」。<br>② **来料固定加工费 → `price_type='INCOMING_MATERIAL_PROCESS'`**：该类型**金额落在 `base_value`、`pricing_price` 恒空** → 绑 `COALESCE(pricing_price, base_value)`（实测 0.043260/0.034140）；`operation_no` 全空 → 工序列留空手填。<br>③ **其他费用**：料号在 `up.code`（`finished_material_no` 空），按 §7.3；用户定「费用」列**只取 `pricing_price`**，故按比例登记的项（材料管理费 4.5/利润 4/税率 13 在 `cost_ratio`）此列为空 = 数据口径非配置错。<br>**闭包口径**（§3.8.1 判据）：材料成本/外购件成本/加工费/来料固定加工费**做闭包**（含铁律 1/2/3）；产品/组装加工费/其他费用**不做**（本产品自身属性/费用）。<br>**用户拍板**：报价侧；材料成本+材料损耗成本**两列手填**（不配公式）；物料「单位」= **重量单位 `weight_unit`**（非 `issue_unit`）。<br>验证：字段↔模板列 1:1 脚本比对 8/8 PASS；8 个 `$view` dry-run 全过；`expand-driver` 真管道 7 个平铺组件全通、谓词正常包 `inner_q ... hf_part_no = ANY(?)`（物料是树页签走 Pass2，平铺 expand 报 `inner_q.hf_part_no does not exist` 属**契约不适用**非缺陷）；`cpq_db_0724` 真数据实跑——材料成本出 H65/AgNi11#-Ⅰ + 元素单价 1850/118478（无价元素 NULL 不掉行）、BOM 树 5 边+根分支、pending 开域下子件 `S-80011` 的 AgC3(Ag/C) 与 `W-1001`(数量 3) 进页签；导入预演 `checksumValid=true / canCommit=true / 0 blocker`。⚠️ 数据缺口：外购件严格态 0 行（OUTSOURCED 仅 pending）、来料工序空、产品客户料号名称/单位空。 |
| **海格 / 伊顿 / 圣衡斯 / 森萨塔** | JSON（`docs/table/报价测试数据/客户报价模板/组件导入-<客户>.json`） | 产品 / 材料成本 / 成品其他费用（原加工费改版）/ 电镀费(PIVOT)；罗克韦尔 JSON 另含外购件成本 | 伊顿/圣衡斯/森萨塔结构同构 → 组件与客户无关（`:customerCode` 运行时注入），建一次导出存多份；森萨塔 QT-20260716-2046 触发一号多材质铁律修复（COMP-0070）；电镀费 unit_price PLATING 行转列（森萨塔 1111122 实测加工费11/材料费22 成两列） |

**权威母版**：目录「报价模板V2」(`4007d593`) / 「罗克韦尔模板」(`f4e2bca1`)。落库走 REST API（`admin/Admin@2026`）或 JSON 导入（暂存目录 export → 用户建目录 import/commit），流程见 `AGENT-配置入口.md §5`。
