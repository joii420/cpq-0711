# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# 会话开始时的动作顺序（按此顺序，不要跳，也不要重排）

本文件里有多处写着「必读」「先做」。**它们的唯一执行顺序是这里定义的**：

| # | 动作 | 何时做 |
|---|---|---|
| 1 | 若 §1 仍是「待探测」，或 `docs/` / `dev-docs/` 缺 §2 表中的文件 → 执行 **§0 初始化** | 仅首次；只探测和建骨架，不动代码 |
| 2 | 读 `docs/RECORD.md`（历史上下文与已知问题） | 每个新会话一次 |
| 3 | 读 `dev-docs/INDEX.md` 的「当前项目态势」（在途工作，避免撞车） | 每个新会话一次 |
| 4 | 读 `docs/BACKLOG.md`，列出待开发条目（§9 规则二） | 每个新会话一次 |
| 5 | 收到代码改动请求 → 走 **§4.0 路径确认**，等用户拍板 | **每个新请求都要走** |

第 1~4 步是只读的，同一会话内**只做一次**，不要每轮重复。第 5 步每来一个新的改动请求就走一次。

---

# 0. 首次进入项目：初始化（Claude 自动执行，一次性）

**触发条件**：新会话开始时，发现「§1 项目速览」仍带「待探测」字样，或 `docs/` / `dev-docs/` 下缺少 §2 表中的文件。

> 目录分工：**`docs/` = 项目文档目录**（长期知识：需求、开发记录、待办）；**`dev-docs/` = 项目任务目录**（每个开发任务一个子目录 + 任务索引）。

**必须先做完这一节，再执行用户的任务。**

### 0.1 探测并回写 §1

自行探测，**不要问用户能自己查到的东西**：

| 要探测的 | 从哪查 |
|---|---|
| 语言 / 框架 / 版本 | `README*`、`package.json`、`pom.xml`、`build.gradle`、`go.mod`、`pyproject.toml`、`*.csproj` |
| 前后端子项目目录 | 仓库根目录下含独立构建文件的子目录 |
| dev server 端口 | `vite.config.*`、`webpack.config.*`、`application*.properties`、`application*.yml`、`.env*` |
| 数据库连接 / profile 列表 | 同上；`application-<profile>.*` 的文件名即 profile 名 |
| 已在跑的服务 | `ss -ltnp 2>/dev/null \| grep -E 'node\|java\|python'` |
| 核心模块与数据流 | 顶层目录结构 + 路由/控制器清单 + README |

探测完把结论**直接改写进 §1**，删掉「待探测」标记。

**顺带按探测结果裁剪本文件**（不要留给用户做）：探测到前端不是组件化 SPA → **Claude 自己把 §6 UI 交互规范整节替换成本项目的等效约定或直接删除**，并在 §0.3 汇报里说明。凡本文件里写着「按项目替换 / 视情况选择」的地方，一律由 Claude 在此步定下来，不留悬空条件句。

**探测不到的**（典型：业务定位、模块职责的业务含义）——向用户提**一个**集中的问题问清，**不要臆造**。

### 0.2 创建缺失的文档骨架

只建骨架，不编内容：

```bash
mkdir -p docs dev-docs
[ -f docs/PRD.md ]            || printf '# PRD\n\n> 功能交付的唯一标准。需求变更必须同步本文件。\n\n## 1. 产品定位\n\n## 2. 功能需求\n\n## 9. 演进史\n\n| 日期 | 变更 | 原因 |\n|---|---|---|\n' > docs/PRD.md
[ -f docs/RECORD.md ]         || printf '# 开发记录（多 Agent 共享记忆）\n\n> 开始工作前必读；完成工作后必须追加。\n> 格式：[日期] 模块 - 描述 | 涉及文件 | 关键决策\n\n' > docs/RECORD.md
[ -f dev-docs/INDEX.md ]          || printf '# 任务索引\n\n## 0. 当前项目态势\n\n- 进行中的任务：\n- 未闭合缺陷：\n- 未合并分支：\n- Backlog 概况：\n\n## 0.1 按症状反查\n\n| 症状 | 归属任务 | 根因 |\n|---|---|---|\n\n## 0.2 按代码文件反查\n\n| 文件 | 被哪些任务动过 | 压着的契约 |\n|---|---|---|\n\n## 1. 任务清单\n\n| 目录 | 状态 | 一句话 |\n|---|---|---|\n' > dev-docs/INDEX.md
[ -f docs/BACKLOG.md ]        || printf '# Project Backlog\n\n> 自动维护文件，spec 评审后写入，开发完成后更新。\n> 开始新任务前请先阅读本文件。\n\n## 状态说明\n- [ ] 待开发\n- [~] 进行中\n- [x] 已完成\n- [-] 已废弃（需注明原因）\n\n---\n\n## 待开发条目\n\n---\n\n## 变更记录\n\n| 日期 | 操作 | 条目 | 备注 |\n|---|---|---|---|\n' > docs/BACKLOG.md
```

### 0.3 汇报

向用户说明：探测到什么、回写了 §1 哪些内容、创建了哪些文件、有哪些项探测不到需要他回答。

---

# 1. 项目速览

> 🔎 **待探测** —— 本节由 Claude 在首次会话时按 §0.1 自动回写。

### 项目定位

`待探测`

**Status**: `待探测`（pre-implementation / active development / maintenance）

### 技术栈

| 层 | 技术 |
|---|---|
| Backend | `待探测` |
| Frontend | `待探测` |
| Database | `待探测` |

### 架构

```
待探测：核心数据流向
```

### 本地开发服务

> dev server 是**全会话 / 多 worktree 共享**的：先探端口，**已在跑就直接复用，不要重复起**。
> 热重载遇大范围文件变化（如切分支）会重编译，期间服务短暂无响应属正常。

| 服务 | 启动命令 | 端口 |
|---|---|---|
| 后端 | `待探测` | `待探测` |
| 前端 | `待探测` | `待探测` |

### 多环境 profile

各 profile 可能指向**不同的库**。库选错的症状是「改动/数据看不到效果」，且可能污染他人环境。

| profile | 主机 / 库 | 用途 |
|---|---|---|
| `待探测` | | |

**不要凭配置文件推断服务实际连的哪个库**——配置会被 profile / 环境变量覆盖。可靠做法是**比对数据**：取一个业务端点返回的总数，与各库 `SELECT count(*)` 对照，数字对上就是那个库。

### 启动 / 存活自检

```bash
# ⚠️ 坑1: shell 常设了 http_proxy，curl 访问 localhost 会走代理返 502。
#        探本机服务一律加 --noproxy '*'。
# ⚠️ 坑2: 未装健康检查扩展时 /health 返 404 —— 它不是健康探针！
#        判后端健康看业务端点返 401（应用在跑、鉴权正常），不要看 /health。
# ⚠️ 坑3: 探活必须验明正身。只验「有东西在响应」不等于「响应的是我要的那个」——
#        并行跑测试时极易探到别人的实例，然后把断言打在别人的库上。
#        探活要带身份校验（如启动日志里的唯一库路径 / 实例 ID），不能只看 200。
# ⚠️ 坑4: EADDRINUSE 但 ss 看不到监听者、curl 是连接被拒 = 系统保留端口
#        （WSL2/Hyper-V 会保留 3000-3003 等段），不是僵尸进程，杀进程没用，换端口。
ss -ltnp 2>/dev/null | grep -E 'node|java'          # 先确认端口在跑
PORT=<填本节「本地开发服务」表里的端口>              # §0.1 回写后此处应为具体数字
curl -s --noproxy '*' -o /dev/null -w '%{http_code}\n' "http://localhost:$PORT/"
```

### Language

文档、UI、原型使用中文；代码产物（变量、函数、API、注释）使用英文。

---

# 2. 关键文档体系

每类知识有唯一权威出处。**缺失的按 §0.2 创建，不要另起炉灶新建同类文件。**

| 角色 | 文件 | 约束 |
|---|---|---|
| **产品级唯一标准** | `docs/PRD.md` | 功能验收的最终依据，跨任务长期有效。需求变更必须同步回写，含演进史章节。与任务文档的层级关系见 §4.1 |
| **多 Agent 共享记忆** | `docs/RECORD.md` | **开始工作前必读**；完成工作后必须追加。格式：`[日期] 模块 - 描述 \| 涉及文件 \| 关键决策` |
| **历史任务索引** | `dev-docs/INDEX.md` | 含「当前项目态势」「按症状反查」「按代码文件反查」三张表 |
| **待办清单** | `docs/BACKLOG.md` | 见 §9「Backlog 自动管理规则」 |
| **任务文档目录** | `dev-docs/` | 每个任务一个子目录，见 §4.1 |

**归档纪律**：文档废弃时**移入 `docs/archive/` 并在索引里划掉（`~~删除线~~`）+ 注明取代者**，不要直接删。Agent 会引用旧路径，必须让它撞到「已废弃，请看 X」而不是撞到空。

**禁止全量扫大目录**：`dev-docs/` 体量变大后，一律先过 `dev-docs/INDEX.md` 再定向深读。

**风险警告的放置原则**：**警告要放在会触发它的人的必经之路上，不是放在报告里。**
判据：谁最可能破坏这条约束？他动手前会打开哪个文件？警告就该在那儿——写进 `dev-docs/INDEX.md` 的「按代码文件反查」表 + 该文件的行内注释。

> 反例：把「`workers: 1` 不能调大，否则并行 spec 会互相清库」只写进测试报告。报告会被归档，而**下一个想让测试跑快点的人只会打开 `playwright.config.ts`** —— 他在那里看不到任何警告，那行 `workers: 1` 看起来就只是个性能参数。
> 这正是「按代码文件反查」表的真正价值：**它不是索引，是把契约压在文件上。**

---

# 3. 开发规范

## 3.1 需求变更同步

1. 需求变更时必须同步更新 `docs/PRD.md` 的对应章节
2. 在演进史章节记录所有调整
3. 不要修改 `docs/archive/` 下已归档的历史版本

## 3.2 🚫 后端严禁 N+1 查库（强制，无商量余地）

**定义**：查询次数随数据量线性增长 —— 「先查一批 N 条，再对每条各查一次」。

> 下列示例用 Java + JPA 写法，**规则适用于任何语言的任何 ORM / 查询层**（ActiveRecord、Sequelize、GORM、SQLAlchemy…），形态一致：循环体里出现了查询。

```java
// ❌ 循环里查库
for (Id id : ids)      { repo.findById(id); }
// ❌ 懒加载在循环里触发（隐式 N+1，不出现 SQL 字样，最难发现）
for (Order o : orders) { o.getItems().size(); }
// ❌ Stream 里藏循环查库
ids.stream().map(repo::findById).toList();
```

**必须改成批量**，四选一：

| 手法 | 用法 |
|---|---|
| `IN` 批量 | `repo.list("id in ?1", ids)` → 回内存建 Map 分发 |
| 元组 `IN` | 复合键用 `(col1, col2) IN ((?,?),(?,?)…)` |
| `JOIN FETCH` | 关联对象一次带出，杜绝懒加载触发 |
| 数组参数 | 如 PG `ANY(:arr)` |

**硬指标**：**单个业务操作的 SQL 条数必须是常数**，与 N 无关。推荐口径：每张表最多 2 条 SQL。

**唯一例外路径**（不是自己说了算）：必须**同时**满足三条 —— ① 代码处写明 `// N+1 例外：具体原因` ② 在 `docs/BACKLOG.md` 登记并标 P1 以上 ③ **向用户报备并获批**。三条缺一即视为违规。

**自检**（后端改动结束前必跑，写进 PR）：
1. 人工过一遍新增/改动代码里所有 `for` / `forEach` / `stream()` 循环体，确认**没有 repository 调用、没有触发懒加载的 getter**
2. 有条件就开 SQL 计数断言单测
3. 关键链路补日志证据：`[perf] 操作名 N=数据量 sql=条数` —— **N 翻倍而 sql 不变**才算过

---

# 4. 开发流程规范（路径确认 → 闸门 → worktree 隔离）

**强制**：**走路径 A（任务立项）的改动**，必须先创建**隔离 worktree 分支**，在该隔离工作区里开发，**不直接在主工作区或 master 上改**。
走路径 B（直接修复）的小改动不开分支——路径怎么定见 §4.0，**由用户拍板，不由 Claude 自行判断**。

## 术语：先统一三个词

| 词 | 含义 | 不要混淆 |
|---|---|---|
| **主线** | **当前这个主会话（你自己）**，相对于你派出去的子代理 | ≠ master 分支 |
| **亲验** | 主线自己动手复验，不采信子代理的汇报 | ≠ 用户验收 |
| **验收** | **用户**在已合并环境真机确认 | ≠ 亲验 |

## 术语：什么是「闸门」（读本章前先看这一节）

**闸门 = 必须停下来等用户回话的地方。不是自检清单，不是走形式 —— 是「用户不点头，下一步就不许做」。**

全流程共 4 个停止点，各问一件不同的事：

| 停止点 | 在问什么 | 用户未回答时，Claude 该做什么 |
|---|---|---|
| **路径确认**（§4.0） | 这事走完整流程，还是直接改？ | 停。可以继续读代码 / 查根因，但**不许建任务目录、不许改代码** |
| 🚦 **A0 方案裁决** | 原因查清了，有几种改法，选哪个？ | 停。**不许把候选方案写进文档**，更不许按自己偏好开写 |
| 🚦 **A 开工确认** | 文档写完了，就照这个干？ | 停。**不许建分支、不许写代码** |
| 🚦 **B 验收** | 做完并已合并，你去点一遍，对吗？ | 停。任务状态停在「待验收」，**不许自行标「已交付」** |

**三条判定规则**：

1. 🚫 **「通过」只能由用户给出，Claude 不能自我宣布通过。自己检查一遍 ≠ 闸门通过。**
2. **用户没明确表态时一律视为未通过。** 含糊回应（「嗯」「看看」「你先弄」）要追问一句确认，不要当默许。
3. 闸门可被用户**显式豁免**（「不用问了，直接做」），但**豁免只对当次有效，不跨会话**。

## 4.0 🚦 开工前必须与用户确认路径（最高优先级，先于本章所有规则）

**收到任何代码改动请求时（新功能 / BUG / 优化 / 重构），禁止自行决定走哪条路径，也禁止直接动手。必须先停下来问用户，拿到答复后才开始。**

> **不适用本节的情况**（这些直接做，不用问）：纯文档改动（`docs/` / `dev-docs/` / README / 注释）、流程本身要求的回写动作（`RECORD.md` / `INDEX.md` / `BACKLOG.md`）、只读的探查（读代码、查根因、跑查询）。
> 判据：**没改到会进构建产物的代码，就不用问。**

两条路径二选一：

| 路径 | 含义 | 产出 |
|---|---|---|
| **A. 任务立项** | 走 §4.1~§4.3 完整流程：建任务目录 → 写文档 → 闸门 A0 → 闸门 A → worktree → 开发 → 亲验 → 合并 → 闸门 B → 结案 | 完整任务文档 + 特性分支 |
| **B. 直接修复** | 不建任务目录、不开分支，直接在主工作区改 | 代码改动 + §8 自检 + 一行 `docs/RECORD.md` |

**提问格式**：一句话说清改动性质与影响面 → 给出**推荐路径**和理由 → 等用户拍板。不要罗列长篇选项。

**推荐时的参考判据**（这只是给用户的建议依据，**不是自动豁免的许可**）：

| 倾向 A（立项） | 倾向 B（直接修） |
|---|---|
| 新功能 / 新页面 / 新接口 | 纯文案、样式微调、注释与文档 |
| 跨多个文件，或影响面不清 | 单文件单点，根因明确 |
| 涉及数据契约、DB 迁移、公式、权限 | 依赖版本号、配置项 |
| 需要回归验证既有功能 | 改动 ≤ 2 个文件且不碰上述任一项 |

三条硬约束：

1. 🚫 **用户答复前，不许建任务目录，也不许改代码。** 探测/读代码/定位根因可以先做，动手不行。
2. ⚠️ **走 B 豁免的只是流程文档和分支隔离，不豁免验证。** 具体：§8「修改后强制自检」照跑；改完必须**主线亲自确认改动真的生效**（不是「应该没问题」）；若 B 路径下派了子代理，§4.4 的假绿清单同样适用。
3. 用户在本次会话已明确表示「直接改，不用问」时，按 B 执行，并在回复里注明「按你上条指示走直接修复」——**该授权不跨会话延续**。

### 决策权归属（贯穿全流程）

**路径选择（A/B）、技术方案选型、需求取舍、需求范围增减 —— 一律由用户裁决。**
Claude 的职责是给出**分析、候选方案、明确推荐和推荐理由**，不是替用户做选择。

⚠️ **「范围变更」和「方案变更」同样要回来问。** 开发中发现需求有缺口、要加一条 AC，那是**扩范围**——用户在闸门 A 批的是 N 条 AC，不是 N+3 条。追溯链维护得再好也替代不了重新点头。

🚫 **禁止把尚未裁决的选择写成既成事实**（写进文档、写进代码、写进提交）。
凡是「有两种以上合理做法」的岔路，**先在对话里问，拿到答复再落文档**。文档记录的是**裁决结果**，不是 Claude 的提案。

## 4.1 任务文档形态

**目录约定**：`dev-docs/task-YYMMDD-<任务名>/`，子任务与返修任务（`repair-YYMMDD-<任务名>`）挂在主任务目录下。

> 📅 一律用 **6 位 `YYMMDD`**（`task-260813-库存盘点`）。4 位 `MMDD` 跨年必撞号，而「要不要加年份」不该成为一个需要判断的分支——多两个字符换掉一个决策点。

🚫 **目录层级上限 = 2 层，禁止第三层。**

```
✅ dev-docs/task-260801-公式计算精度优化/
   ├── task-260810-公式计算12位显示9位/
   └── repair-260812-对账阈值与结果尺度不对称致误报/   ← 平铺在主任务下

❌ dev-docs/task-260801-公式计算精度优化/
   └── task-260810-公式计算12位显示9位/
       └── repair-260812-对账阈值与结果尺度不对称致误报/   ← 第三层，禁止
```

**主任务下的所有子任务和返修任务一律平铺**。即使这次返修修的是某个子任务，也仍然建在主任务目录下，只在文档开头注明一行「**返修对象**：`task-260810-公式计算12位显示9位`」。

**什么时候建子任务、什么时候另起主任务**（判不准就问用户，不要自己定）：

| 情况 | 放哪 |
|---|---|
| 同一需求的延续、细化、二期 | 该主任务下的**子任务** |
| 修既有任务交付物的缺陷 | 该主任务下的 **`repair-`** |
| 独立需求，与既有任务无共享验收标准 | **新主任务** |
| 跨多个既有主任务 | **新主任务**，在文档里列出关联的主任务 |

> 理由：嵌套超过两层后，`dev-docs/INDEX.md` 的反查表和路径引用都会失准，且同一主线的返修散落在不同深度，无法一眼看全。归属关系靠文档里的「返修对象」字段表达，不靠目录深度。

| 文档 | 作用 |
|---|---|
| `需求文档.md` | 该任务的验收标准（含 AC 验收条款）。**常规任务必写** |
| `问题说明.md` | **返修任务专用**，取代 `需求文档.md` 作为立项文档。格式见下文「BUG 返修任务的立项流程」 |
| `fronttask.md` / `backtask.md` | 前 / 后端任务分解，**每项必须标「服务的 AC」**。格式见下文规范 |
| `api.md` | 接口契约 |
| `test.md` | **测试方案 + AC 可追溯矩阵**。闸门 A 的**前置产物**，开工前写完 |
| `test-report.md` | 测试**执行**结果。开工后测试进场执行完才产出 |

**前端或接口零改动的任务也要写 `fronttask.md` / `api.md`**，内容是「为什么不改」的判定依据 + 回归确认清单 + 二期触发条件。**宁可写细，不可留空槽**——留空槽 = 该文档未完成。

**与 PRD 的层级关系**（避免两个「唯一标准」打架）：

- `docs/PRD.md` 是**产品级唯一标准**，跨任务、长期有效
- 任务的 `需求文档.md` / `问题说明.md` 是**该任务的验收切片**，只对本任务有效
- 🚫 **任务文档不得与 PRD 冲突。** 发现冲突时按 §3.1 **先更新 PRD，再改任务文档**，不许两边各写各的
- 冲突未消解前，**以 PRD 为准**

### 开工后 AC / 契约变更（并行开发下最危险的一件事）

开发中发现需求有缺口、要加 AC 或改 `api.md`，**四步缺一不可**：

1. 🚦 **回用户确认** —— 加 AC = 扩范围，见 §4.0「决策权归属」。不能主线自己裁完继续走
2. **同步四处**：`需求文档.md` 的 AC → `api.md` → `fronttask.md` / `backtask.md` 的 AC 映射 → `test.md` 的追溯矩阵。**漏一处链就断了**
3. ⚠️ **主动通知所有在跑的子代理** —— 用 SendMessage 续跑，或杀掉重派
4. 变更后重跑闸门 A 的三项自检（AC 质量 / 正向覆盖 / 反向覆盖）

> 🚨 **为什么第 3 步必须显式做**：子代理在派出时读过一次 `api.md`，那份内容就**冻结在它的上下文里**。之后文件改了**它不会知道**，也不会主动重读。
> 典型后果：前端按新契约写完了，后端还在按派出时的旧契约写，联调才发现。
> 配套归因纪律：**联调失败先查契约版本，再查实现质量**（见 §4.4 归因表）。

### BUG 返修任务的立项流程

前提：§4.0 已与用户确认走**路径 A（任务立项）**。确认走路径 B 的直接修复不适用本流程。

用户提出 BUG 后，**不要直接改代码**，按序走：

1. **定位归属任务** —— 先过 `dev-docs/INDEX.md` 的「按症状反查」和「按代码文件反查」，确认这个 BUG 属于哪个**主任务**。查得到就挂过去；确实无归属才新建主任务目录。**判不准就问用户，不要猜。**
2. **建返修目录** —— 在该主任务目录下建 `repair-YYMMDD-<问题简述>/`（**平铺，遵守两层上限**）。
3. **只写 ①~④ 四节** —— 问题描述 / 复现方式 / 需求结果 / 问题原因。**写到根因为止就停下，不要往下写修复方案。**
4. 🚦 **闸门 A0：方案裁决（不可跳）** —— 在**对话里**向用户呈报，等裁决：
   - 一句话根因
   - 2~3 个候选方案，每个标注：改动面 / 代价 / 风险 / 是否触发协议级改动
   - **明确推荐哪一个 + 为什么**
   - 🚫 **此时不许把候选方案写进 `问题说明.md`** —— 文档记录裁决结果，不记录提案过程
5. **补写 ⑤⑥** —— ⑤ **只填用户裁决通过的那一个方案**；⑥ 写验收标准。
6. **其余文档照写** —— `fronttask.md` / `backtask.md` / `api.md` / `test.md` 按 §4.1 表补齐；零改动的也要写「为什么不改」，不许留空槽。
7. **走闸门 A** —— 完整的 `问题说明.md` 经用户确认后，才建 worktree 动代码。
   （A0 已裁决方案，闸门 A 是对整份文档的最终确认，通常很快）

> **为什么拆成 A0 和 A**：根因定位是 Claude 的活，方案选型是用户的活。合在一起写完再给用户看，用户拿到的是**既成事实**而不是**待裁决项** —— 会默认顺着已写好的方案往下走，选择权实际被架空。

`问题说明.md` 的六节：

| 节 | 要求 |
|---|---|
| **① 问题描述** | 用户原话 + 客观现象 / 报错文案。**用户提供了截图必须嵌进 md**：图片文件拷进本返修目录，md 内用相对路径引用 `![现象描述](./图片名.png)`，不许只写「见聊天记录」 |
| **② 复现 / 发现方式** | 谁怎么发现的（用户报 / 监控 / 测试）+ 前置数据（哪个单据、哪个客户、哪条记录）+ 可执行的操作步骤 + 环境/库 + 出现频率（必现 / 偶现）。偶现必须写明已知触发条件 |
| **③ 需求结果** | 期望行为逐条列（`E-1`、`E-2`…），并注明对照 `需求文档.md` / `docs/PRD.md` 的哪条 AC。**必须同时写反向期望**（「该报的仍要报」「该拦的仍要拦」），防止修成「把告警关了」。找不到对应 AC = 需求有缺口，先补 AC |
| **④ 问题原因 / 报告** | 根因 + 证据链（SQL / 代码行 / 实测数据，须可复跑）+ 引入时间线（哪个提交引入、之前为什么不犯）+ 影响面 + **为什么测试没拦住**。🚫 **根因未定位前不许进入 A0** —— 拿不出根因就讨论方案，讨论的是猜测。**本节写完必须停下走 A0** |
| **⑤ 修复方案** | ⚠️ **只写用户在 A0 裁决通过的那一个方案**，不罗列候选、不写「建议采用」。内容：改哪些文件、怎么改、影响面、是否构成协议级改动（触发 §8.1 的 E2E 要求）；若要推翻既有需求条款，必须点名是哪份文档哪一条。<br>末尾可加一行 `已否决备选：<方案> —— <否决理由>`，防止后续会话重新提同一个已被否掉的方案 |
| **⑥ 验收标准** | 按下文「AC 编写规范」写可执行 AC，**至少含四类**：阴性（原路径不再复现）、阳性（真问题仍能被抓住）、无副作用（相关值 / 显示 / 导出不变）、以及 §8 要求的自检证据 |

### AC 编写规范（`需求文档.md` / `问题说明.md`）

每条 AC 独立编号（`AC-1`、`AC-2`…），且**必须可执行**——三要素缺一不可：

| 要素 | 含义 | 反例 → 正例 |
|---|---|---|
| **前置状态** | 什么数据、什么角色、什么入口 | ~~"打开页面"~~ → "以销售角色打开客户 A 的报价单 QT-001（含 3 个产品）" |
| **操作步骤** | 点哪里、填什么 | ~~"编辑数量"~~ → "在页签「投料」第 2 行数量列填 5，失焦" |
| **可观测断言** | 具体数值 / 具体文案 / 具体行数 | ~~"金额正确"~~ → "该行小计显示 1250.00，页签合计从 3000.00 变为 4250.00" |

🚫 **禁止形容词 AC**：「正确显示」「性能良好」「体验流畅」「符合预期」。
写不出可观测断言的条目不是 AC，是愿望 —— 打回需求文档重写。

**AC 集合必须覆盖三类，缺类即视为需求文档未完成**：

1. **单点 AC** —— 单次操作的正确性
2. **序列 AC** —— 用户真实使用是**有状态的连续操作**。至少一条 AC 描述「做 A → 做 B → 切走再切回 → 刷新页面」这类多步路径，并断言**中间态与最终态**。只写单点 AC = 只覆盖 happy path，串起来用必翻车
3. **边界 AC** —— 空数据 / 超长输入 / 并发编辑 / 权限不足

### `fronttask.md` / `backtask.md` 规范

**前端只按 `fronttask.md` 做，后端只按 `backtask.md` 做**，两份各自独立成篇——因为并行开发的两个子代理**互相看不见**，它们之间唯一的协调物是 `api.md`。

**① 必须分条式，每项独立编号**（前端 `F-1`、`F-2`…，后端 `B-1`、`B-2`…）。大段叙述不合格——没法标 AC，也没法逐项验收。

**② 每项必须标「服务的 AC」**：

| 编号 | 服务的 AC | 任务内容 |
|---|---|---|
| F-1 | AC-1, AC-3 | 数量列输入框 + 失焦触发重算 |
| B-2 | AC-1 | 行小计计算接口 |

**③ 只标 AC 编号，不复制 AC 原文。** 单一事实源留在 `需求文档.md` / `问题说明.md`。复制会双写漂移——改了 AC 忘了改任务文档，比不标更糟。

**④ 双向覆盖，两个方向各拦一种病**（闸门 A 检查）：

| 检查方向 | 拦什么 |
|---|---|
| 每条 AC **至少被一个** `F-x` 或 `B-x` 覆盖 | **交付缺口** —— 写了 AC 但没人实现 |
| 每个 `F-x` / `B-x` **至少指回一条** AC | **超范围** —— 没人要的功能，Agent 自己加的戏。它不在 AC 里，测试也不会覆盖它，往往是下一个 BUG 的来源 |

> **为什么开发侧也要挂 AC**：开发子代理只读它那份任务文档，**不知道 AC 是什么**。拿到的是「实现一个输入框」这种任务分解，不是「小计应显示 1250.00」这种验收标准 —— 它只能按自己的理解填细节。需求 → AC → 任务分解 → 子代理理解，**三次转译，中间两次你都看不见**。挂上 AC 编号 + 派工时要求它读 AC 原文（见 §4.3「开工编排」），才能把这条链接回去。

### `test.md` 规范

**① 必须在闸门 A 之前写完，且从需求文档原文派生，不得从实现代码派生。**
写 `test.md` 时只读 `需求文档.md` / `问题说明.md` 的 AC 章节，**不读实现代码**；派子代理写测试时只给 AC，不给实现。

> ⏱ **时点是这条规则能否落地的关键**：闸门 A 之前实现代码还不存在，「不读实现」是天然成立的。一旦拖到开发中期再补 `test.md`，代码就在眼前，这条规则事实上无法遵守 —— 所以**它是闸门 A 的前置产物，不是开发期的补充材料**。

> 理由：测试若从代码反推，它与实现**共享同一个理解偏差** —— 实现理解错了，测试就跟着错，结果是永远全绿，永远发现不了需求理解错了。（测试与 AC 各自能证明什么，见 §4.3 步骤 4）

**② 必须含 AC 可追溯矩阵**，缺行即视为文档未完成：

| AC 编号 | 覆盖它的测试 | 层级（单元/接口/E2E/真机） | 验收证据形式 |
|---|---|---|---|
| AC-1 | T1.2 | E2E | 截图 |

三条硬规则：
- **没有任何测试覆盖的 AC = 交付缺口**，必须在闸门 B 汇报时显式列出，不许沉默
- **只有单元测试覆盖、没有 E2E 或真机验证的 AC，不算已验收**（单测与实现同源，见 ①）
- 🚨 **矩阵里声明的「验收证据形式」必须真的留得下来。** 判据：**下一轮跑测试会不会把它删掉？会 → 不算证据。**
  测试框架默认会在每轮开跑前清空输出目录（Playwright 的 `test-results/` 就是），所以声明「截图」却把截图留在那里，等于**没有证据**。
  正确做法：把要当证据的产物**复制到任务目录下的归档位置并随任务提交**，不要留在工具的临时输出目录里。

## 4.2 与技能包（superpowers 等）的优先级裁决

技能包里的技能**硬编码了自己的产物形态和默认落盘路径**。不显式 override，Agent 会照搬技能默认值，产出游离在任务目录体系之外的文档，且**不报错**——只会在查索引查不到时才发现文档裂成两套体系。

**通则：任何技能与本 `CLAUDE.md` 冲突时，一律以 `CLAUDE.md` 为准。**（多数技能自身也写明「User preferences override this default」。）

| 技能 | 裁决 |
|---|---|
| `writing-plans` | 🚫 **禁用**。**不产出 `实现计划.md`**，开发文档形态以 §4.1 为准 |
| `brainstorming` | ✅ **保留且必用** —— 它是生命周期**步骤 -1「澄清需求」**的承担者（§4.3）。但**覆盖其落盘路径**：其检查清单硬写的 `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md` **不要照搬**，一律落 `dev-docs/` |
| `subagent-driven-development` | ✅ 保留，用于**单个 Task 内部**推进（每个 Task 派全新子代理；Task 间两阶段评审：先 spec 合规、再代码质量，连续执行不中途请示）。与 §4.3 不冲突：§4.3 管**外层流程**，本技能管**单任务内部执行** |
| `using-git-worktrees` | ✅ 保留，闸门 A 通过后调用（§4.3 步骤 2） |
| `finishing-a-development-branch` | ✅ 保留，走「合并并清理」路径（§4.3 步骤 5） |

**废弃路径要留碑**：技能默认路径若曾被使用过，标注「已停用，仅保留历史存档，不再新增」而不是直接删目录。

### 三个确认阶段的分工（防止让用户点三次头）

`brainstorming` 自带「分节呈报、逐节确认」，与 A0、闸门 A 容易叠成三道重复确认。分工钉死：

| 阶段 | 只管 | **不管** |
|---|---|---|
| **brainstorming**（步骤 -1） | **需求侧**：做什么、给谁、什么场景、边界在哪 | 不定技术方案 |
| 🚦 **A0**（步骤 0.5） | **实现侧**：技术路线选型 | 不重开需求讨论 |
| 🚦 **A**（步骤 1） | 整份文档**定稿放行** | 不重开前两项，只做最终确认 |

**出口**：若 brainstorming 阶段已经把技术方案一并讨论定了（它天然会聊到实现），**A0 压缩成一句「确认采用方案 X」**，不重复呈报候选。

## 4.3 生命周期（闸门不可跳）

```
-1. 澄清需求 → 调 superpowers:brainstorming，一次一问把模糊点问清
              （做什么 / 给谁 / 什么场景 / 边界在哪），产出写 AC 的素材
              ⚠️ 需求没问清就动笔写需求文档 = 写出来的 AC 全是猜的
              这一步只管需求侧，不定技术方案（分工见 §4.2）
0. 立项    → 建 dev-docs/task-YYMMDD-xxx/ + 写需求文档（先只写需求与 AC）
             同步在 docs/BACKLOG.md 登记条目（编号 BL-NNNN）
             ⚠️ 这一步在建 worktree 之前，不要反过来
             （BUG 返修走 §4.1「BUG 返修任务的立项流程」，产出 问题说明.md）
0.5 🚦闸门A0 → 方案裁决：技术方案存在两种以上合理做法时，先在**对话里**呈报
              候选（改动面/代价/风险）+ 明确推荐，等用户拍板；
              裁决结果才写进 fronttask.md / backtask.md
              🚫 不许把未裁决的方案先写进文档（见 §4.0「决策权归属」）
              方案唯一、无可选项时可直接进闸门 A，但要在汇报里说明「无岔路」
1. 🚦闸门A → 用户确认完整文档。未过闸门 A 严禁建分支写代码，更不许合并
             进闸门 A 前的自检三项，任一不过就打回重写，不要拿去呈报：
             ① AC 质量：逐条过，须满足「可执行三要素」且三类覆盖齐全
                （单点/序列/边界）
             ② 正向覆盖：每条 AC 至少被一个 F-x / B-x 认领，且至少被一条
                测试用例覆盖 —— 没人认领 = 交付缺口
             ③ 反向覆盖：每个 F-x / B-x 都能指回至少一条 AC
                —— 指不回 = 超范围，砍掉或补 AC
             ⚠️ 自检通过只是取得「呈报资格」，是否放行仍由用户裁定
2. 起步    → 用户说「开工」= 闸门 A 放行信号，触发下文「『开工』触发什么」的全套编排
             ⚠️ 先提交任务文档，再建 worktree（顺序不能反，理由见下）
             建独立 worktree + 特性分支，后续所有编码/提交都在该 worktree 内
3. 开发    → 前后端并行实现 + 测试工程师写用例 + 主线审用例 + 测试执行
             自检照跑；协议级改动跑 E2E（何为协议级见 §8.1 第 4 条）
4. 主线亲验 → 逐条对照 AC 验收，四条同时满足才算打勾：
             a. 在用户实际使用的环境跑（dev server + dev 库 + 真实数据），
                不是测试环境 / 测试夹具
             b. 走用户视角的完整路径（从 UI 入口进，不是从 API 或单测进）
             c. 每条 AC 附可复核证据：实际输出值 / 截图 / 接口原始响应
                —— 禁止只写「✅ 通过」，那不是证据
             d. 证据必须是非空正向结果。空列表 / 0 行 / "—" 一律不算通过
                （数据为空会让断言根本没执行，测试照样报绿）
             🚫 测试全绿不构成 AC 达成的证据，二者不可互相替代：
                测试证明「代码按实现者的理解工作」，AC 证明「功能符合需求文档」
             ⚠️ 不采信子代理的"已完成"（四类假绿见 §4.4）。这一步不可省
5. 收尾合并 → 亲验通过即自动执行，不等用户许可：
             切回 master → merge → 跑测试确认
             ⚠️ 删 worktree 前，把 worktree 里装的 node_modules 移到主工作区
                （git worktree remove 会连它一起销毁，否则主工作区要重装一遍）
                mv <worktree>/<子项目>/node_modules <主工作区>/<子项目>/
             再删 worktree 目录 + 删特性分支
             🚦 冷启动验证（见 §8.8）—— 合并后的 master 必须能从零跑通
             回写 docs/RECORD.md 与 dev-docs/INDEX.md
6. 🚦闸门B → 汇报"AC 逐条达成 + 亲验证据 + 已合并"，由用户在已合并环境真机验收
7. 结案    → 用户验收通过后：dev-docs/INDEX.md 任务状态改「已交付」
             + docs/BACKLOG.md 对应条目打 [x] + 变更记录追加一行
             验收未通过 → 本任务状态保持「待验收」，按配套第 2 条开返修任务；
             返修任务自己走一遍完整流程，**返修验收通过后本任务才转「已交付」**
```

> ⚠️ **步骤 5 的合并不是闸门。** 全流程只有路径确认、A0、A、B 四处需要等用户点头（见本章「术语：什么是闸门」），合并不在其中 —— 它由「亲验通过」这个客观条件触发，理由见下。

### 「开工」触发什么（步骤 2~4 的执行编排）

闸门 A 通过后，用户说「**开工**」「开始吧」「可以了」——**等同于下达以下全套指令，用户不需要每次重复交代**：

```
① 先提交任务文档，再建 worktree + 特性分支（顺序不能反）
   git add dev-docs/task-YYMMDD-xxx/ docs/BACKLOG.md dev-docs/INDEX.md
   git commit -- <上述路径>      # docs-only commit，遵守 §4.6 只提交指定路径
   然后才 git worktree add

② 并行派三类子代理（不要串行，也不要一个代理包揽）
   ├─ 后端工程师 → 只按 backtask.md 逐项实现
   ├─ 前端工程师 → 只按 fronttask.md 逐项实现
   └─ 测试工程师 → 按 test.md + AC 编写测试用例
   派工 prompt 必含六段，缺一段就会踩一个已知的坑：
   ┌ a. 工作区坐标：worktree 绝对路径 + 分支名，「所有读写/构建/测试
   │     都在此路径内，不要 cd 出去，不要动主工作区的任何文件」
   │     + 「**你不要执行 git commit，提交由主线统一做**」
   │     └ 不写 → 子代理在主仓动手，提交打进 master、测的是旧代码
   │              （§4.4 假绿第 2、3 类的成因）
   ├ b. 必读文档 + AC 原文：路径**一律给 worktree 内的绝对路径**，
   │     不许指向主工作区（两边可能不同版本）
   │     「先读 <worktree>/…/需求文档.md 的 AC-x、AC-y 原文，再按
   │      <F-1/B-2> 实现」—— 子代理不会主动去读它
   │     + 冲突裁决：「任务分解是分解结果，**AC 原文才是验收标准**，
   │       两者有出入以 AC 原文为准并向主线报告」
   ├ c. 可读范围：测试工程师**禁止读任何实现代码**（点名禁止的目录），
   │     只能读需求文档 / test.md / api.md；信息不足就停下来报告缺什么，
   │     不许翻实现补齐
   │     └ 上下文隔离 ≠ 访问隔离。不显式禁止，它自己就去把 src/ 读了，
   │        读完又和实现同源，「测试独立于实现」这条就白设了
   ├ d. 越界纪律：只做自己那份任务，**不许改别人的产出**。
   │     发现别人的代码/测试有 bug → **只报根因 + 修法，不动那个文件**，
   │     由主线裁决谁来修。发现契约有问题 → 停下来报告，不许自行改契约
   ├ e. 回报格式：做了什么 / 证据（实际输出值、命令输出）/ 哪些没做到
   │     + 「过程中你规避掉的坑」（差点做错但被你挡住的）
   └ f. 自检口径（§4.5）：共享 dev server 服务的是**主工作区代码**，
         看不到你在 worktree 里的改动 —— **拿它验证自己的改动 = 假绿**
         · 端口避让：起服务一律用临时端口（如 3099/5199），
           **声明端口（如 3001/5173）保留给主线亲验**，你不要占
         · 依赖：按 §4.5 三分支判断该软链还是该装；装了要在回报里说明
         · 前端：靠 tsc / lint + grep 确证文件内容；确需跑起来就用
                 临时端口，用完即停。不要 curl 主工作区端口
         · 后端：同上不要 curl 主工作区端口；构建与测试必须在 worktree
                 内执行；worktree 里的 DB 迁移**不会**被主仓 dev server 跑
         · 测试：所有测试命令都在 worktree 路径内执行，不要 cd 出去
         → 验证不了就在回报里**明说「未验证」**，不许写「应该没问题」

③ 🔍 主线审核测试用例（用例一产出就审，不等开发完成，不可省）
   逐条对照需求文档的 AC 核四项：
   a. 每条 AC 都有用例覆盖 —— AC 可追溯矩阵无缺行
   b. 用例断言的是 AC 里的可观测断言，不是实现细节
      （断言实现细节 = 又变成与实现同源，白写）
   c. 三类覆盖齐全：单点 / 序列 / 边界
   d. 用例的前置数据是真实业务数据，不是能让断言空跑的空夹具
   ❌ 任一项不合格 → 打回测试工程师重写，不进入 ④

④ 前后端都完成后 → 测试工程师进场执行测试，产出 test-report.md
   失败项回流给对应工程师修，修完重跑，直到全绿
   ⚠️ 回流时要带上原始 AC 原文，不要只丢一句「T1.2 失败」——
      子代理是全新上下文，它不记得这个用例在验什么

⑤ 转入步骤 4「主线亲验」——⚠️ 测试全绿只是进入亲验的前提，不是亲验的结论
```

> 🚨 **为什么必须先提交再建 worktree（实测确认过）**：`git worktree add` 是**从某个 commit 检出**的，主工作区里**未提交/未跟踪的文件不会带过去**。任务文档是在闸门 A 之前于主工作区写的，此时若没提交，**新建的 worktree 里根本没有 `dev-docs/task-xxx/`** —— 子代理进去按路径找 `fronttask.md` 会扑空，然后大概率不是停下来报错，而是**自己猜该实现什么**，整条 AC 追溯链当场作废。
>
> 顺带两个好处：立项对其他并发会话可见（避免撞车）；`INDEX.md` / `BACKLOG.md` 的登记同时落地。
> 开工后对任务文档的更新（如 `test-report.md`）直接写在 worktree 内，随特性分支走，合并时一起回来。

**三个必须说清的边界**：

1. **`test.md` ≠ 测试用例代码。** `test.md`（测试方案 + AC 追溯矩阵）是**闸门 A 的前置产物**，开工前就该写完；测试用例**代码**才是开工后测试工程师依据 `test.md` 和 AC 写的。两者都不读实现代码。
2. **「开工」不豁免闸门 B。** 它只是闸门 A 的放行信号，覆盖步骤 2~4；步骤 5 合并、步骤 6 用户验收照走。
3. **子代理报「已完成」一律不采信** —— 按 §4.4 的四类假绿逐项复验。

**任务状态**（写进 `dev-docs/INDEX.md` 的状态列，全项目统一这五个值）：

| 状态 | 含义 | 由谁在何时改 |
|---|---|---|
| `立项` | 目录已建、文档在写，未过闸门 A | 步骤 0 |
| `开发中` | 闸门 A 已过，worktree 内开发 | 步骤 1 → 2 |
| `待验收` | 已合并 master，等用户真机验收 | 步骤 5 |
| `已交付` | 用户验收通过 | 步骤 7 |
| `已废弃` | 需求取消或被其他任务取代（**须注明原因和取代者**） | 任意阶段 |

**为什么合并在验收之前**：dev server 服务的是**主工作区已合并代码**，特性分支代码在 worktree 里，不先合并用户根本没法真机验收。

配套三条：
1. **自动合并跳过的是"等用户点头"，不是"亲验"** —— 没有亲验证据就合并 = 拿 master 当测试环境
2. 验收不达标走 **fix-forward**（新开返修任务），不默认 revert，除非缺陷会污染数据或破坏在途单据
3. **出现「测试全绿但用户验收不符」时，先归因再修**。必须归到下列四类之一并写进 `docs/RECORD.md`，否则同样的事下次照犯：

   | 归因 | 判据 | 对应修法 |
   |---|---|---|
   | AC 不可执行 | AC 含形容词，双方各自解读 | 重写 AC，补可观测断言 |
   | 测试与 AC 不对齐 | 可追溯矩阵有缺行，或测试从代码派生 | 按 AC 重写测试 |
   | 环境/数据不一致 | 测试库 ≠ dev 库、夹具 ≠ 真实数据、mock 掉了真链路 | 在真实环境重跑 |
   | **契约版本不一致** | 并行子代理各自对着**不同版本**的 `api.md` 写（开工后契约改过，在跑的代理不知道） | 对齐契约版本，重派受影响的代理 |
   | **测试 harness 不可信** | 症状是「随机挂一条、每次不一样、数据看着合理但对不上」 | 先修 harness，见 §8.7 |
   | 实现确实错了 | 前五项都排除 | 才是改代码 |

   ⚠️ 跳过归因直接改代码，等于默认最后一类 —— 而经验上前五类才是多数。
   ⚠️ **联调失败先查契约版本，再查实现质量**；两边各自「按自己那份契约」都写对了，也会对不上。

## 4.4 子代理交付的验收纪律

子代理**会虚报完成**。派出子代理后，主线必须亲自复验，且注意三类假绿：

| 假绿 | 表现 | 复验手段 |
|---|---|---|
| 虚报完成 | 报告"已实现/已通过"但代码或测试并不存在 | 亲跑测试、亲查 DB、亲跑 E2E |
| 提交跑错分支 | 子代理在 worktree 开发，却 `cd` 主仓把提交打进 master | `git branch --contains <commit>` 逐个确认 |
| 测错代码树 | 子代理在主仓而非 worktree 跑测试，测的是未改动的旧代码 | 主线在 **worktree 内**亲跑一遍构建/测试 |
| 断言从未执行 | 数据为空 → 循环 0 次 / 分支没走到 → 断言压根没跑，测试照样报绿 | 要求测试打印实际值；断言前先断言「结果非空」 |

子代理报"这是既有问题、非本次引入"时，也要自己在基线分支跑一次 A/B 对照确认——两类误判（假阳性阻断、假阴性放行）代价对称。

## 4.5 worktree 共享约束

worktree 只隔离 **git 工作区 + 分支**。以下仍是**共享**的：前后端 dev server、远程 DB、`node_modules/` 及各类构建缓存。

**默认不在 worktree 里另起 dev server**——复用主工作区已运行的实例。

**依赖（`node_modules` 等）按三分支判断，不是一刀切禁止安装**：

| 情况 | 做法 |
|---|---|
| 主工作区**已有**该子项目的依赖目录 | **软链复用**，不重装 |
| **绿地 / 新建子项目**，主工作区还没有 | **在 worktree 内装**（没有东西可软链，不可避免）；回报里说明装了什么 |
| `package.json` 等清单**有增删** | 在 worktree 内装；**合并后主工作区补装一次** |

⚠️ 无论哪种，**删 worktree 前把装好的依赖目录移到主工作区**（§4.3 步骤 5）——`git worktree remove` 会连它一起销毁。

复用共享 dev server 还带来一个自检陷阱：

> ⚠️ **共享 dev server 服务的是主工作区代码，看不到 worktree 里尚未合并的改动。**
> 拿共享 dev server 去"验证" worktree 内的改动，得到的是**主仓代码的结果**，是假绿。
>
> worktree 内改动的自检手段，**按此顺序**降级使用：
> 1. 静态检查（类型检查 / lint）—— 这类工具按路径读真实文件，可信，优先用
> 2. 确证文件内容而非只看接口响应（`grep` 改动特征串）
> 3. 前两步不足以确证时，**才**软链 `node_modules` + 用**另一个临时端口**短暂起服务，**用完即停**（这是上面「默认不另起」的唯一例外）
> 4. 数据库迁移：共享 dev server 不会执行 worktree 里的迁移文件，需复制到主仓触发，**合并前记得删掉副本**

## 4.6 并发纪律

- 多会话各用自己的 worktree 分支并行，避免污染主工作区
- **提交只 `git add` 本次明确改动的文件，严禁 `git add -A`**（同分支并发提交会交错，会把别人的改动夹带进你的提交）
- ⚠️ **`git commit` 提交的是整个暂存区，不是你刚 add 的那几个路径。** 暂存区若有他人/前序遗留的改动会被一并带走。必须用 `git commit -- <paths>` 限定路径，并在提交后 `git show --stat` 自查有无夹带
- **已应用到共享库的迁移文件，禁止改名、改版本号、删除**。迁移工具按 checksum 对账，改了会让所有人的服务启动失败

---

# 5. 代码探索规范（代码索引优先）

项目若建有代码知识图谱 / 符号索引（如 codegraph MCP），涉及代码结构、调用链、符号定位、改动影响面时，**优先用索引工具，而非 grep/rg/find 或盲目派子代理探索**：

| 意图 | 手段 |
|---|---|
| 找符号（"X 在哪 / 叫什么"） | 符号搜索 |
| 理解某块（起手首选） | 上下文查询（一发返回入口 + 相关符号 + 代码） |
| 追调用链 | trace（含 grep 跟不到的动态分发 / 回调 / 前端 fetch → 后端 endpoint 跳转） |
| 谁调它 / 它调谁 | callers / callees |
| **改它会波及谁**（重构前必跑） | impact |

**边界**：代码索引只索引**代码符号**，不索引 Markdown。业务为什么这么设计、进度、踩坑教训、决策仍须读 `docs/` 与 `dev-docs/`。理想分工 = **索引管代码骨架，文档管业务血肉**。

**grep 结果为空 ≠ 不存在**：某些环境的 `grep` 是别名（如 `ugrep -I`），会把含大量非 ASCII 的源文件**静默判为二进制并返空**。据 grep 空结果下「无引用 / 无写点 / 影响面为零」结论前，必须用 `/usr/bin/grep -a` 复核。

---

# 6. UI 交互规范

> **适用前提**：本节假定前端是**组件化 SPA**，示例语汇取自 React + Ant Design。技术栈不匹配时**整节替换**为本项目的等效约定 —— 但保留「规则 + 理由 + 例外白名单 + PR 自检项」这个写法，只写规则不写例外，会被在不该用的地方硬套。

- **统一使用抽屉（Drawer）替代弹窗（Modal）**：所有弹出式交互（新建/编辑表单、详情查看、多步骤向导、批量导入）一律用抽屉从右侧滑出
  - **例外**：轻量即时反馈类组件（message / notification / 简单二次确认）不属于"弹窗功能"范畴
  - **适用范围**：新增需求、旧页面重构、代码审查均须遵循；发现遗留 Modal 表单应顺手迁移

- **列表操作统一走「表格 + 顶部工具栏动作」模式**
  - **行内不放动作按钮**：变更/状态切换/危险动作一律上提到顶部工具栏，行内只保留主入口链接
  - **选择驱动启用**：动作声明 `enabledWhen(selected): true | reason 字符串`，禁用时 hover tooltip 显示原因，**禁止用 `if (...) return null` 隐藏不可用按钮**
  - **统一多选**：单选场景用 `enabledWhen` 限定 `length === 1`，不要单独搞 radio
  - **危险动作走弹窗列出所选项二次确认**，不用零散行内确认
  - **批量操作的"部分失败"必须聚合**并列出失败明细
  - **何时不用**：纯查看页、Master-Detail 双栏、树 + 编辑器、抽屉内子表 —— 需维护「例外白名单」

---

# 7. 质量保证规范

1. **严格单元测试**：每次编码完成后进行代码审核和单元自测，发现问题即修复，然后重复审核 + 自测直到全绿。
   ⚠️ **单元自测全绿是必要条件，不是充分条件。** 全绿之后仍必须走 §4.3 步骤 4 的 AC 亲验，二者不可互相替代（理由见 §4.3 步骤 4）
2. **需求文档驱动交付**：严格按 `docs/PRD.md` 交付，它是产品级唯一标准；具体到单个任务，以该任务的 `需求文档.md` / `问题说明.md` 为验收切片，**该切片须与 PRD 一致**（层级关系见 §4.1）
3. **需求变更沟通**：用户要求的修改与 `docs/PRD.md` 预期不符时，**必须先与用户沟通确认方案**，确认后同步更新文档
4. **开发记录（多 Agent 共享记忆）**：
   - **开始工作前**：必须先读 `docs/RECORD.md` 了解历史上下文和已知问题
   - **排查 bug 时**：先过 `dev-docs/INDEX.md` 的「按症状反查」定位历史任务 → 读该任务的根因/决策 → 再动手复现。**禁止跳过本步直接读代码**（重查已定位过的根因 = 纯浪费 + 大概率得出与既有结论不一致的判断）
   - **新会话进场时**：读 `dev-docs/INDEX.md` 的「当前项目态势」——进行中的任务 / 未闭合缺陷 / 未合并分支 / Backlog 概况，避免与在途工作撞车或重复已交付的东西
   - **要改热点代码文件时**：先查「按代码文件反查」表（某些文件压着多条历史契约），再跑影响面分析确认精确波及面
   - **完成工作后**：必须把核心内容或修复的核心问题追加到 `docs/RECORD.md`；新建任务目录或任务状态变化时同步回写 `dev-docs/INDEX.md`
   - **收尾强制输出**：每次开发任务结束，回复里必须包含一行
     `任务索引：新增 N 条 / 更新 M 条状态 / 无变化`
     没有这一行 = 没检查过索引，等同未完成

---

# 8. 修改后强制自检（每次代码改动结束前必须跑一遍）

**背景**：类型检查（`tsc --noEmit`）**不覆盖**打包器的解析阶段错误（字符串嵌套引号、JSX 解析、syntax 报错）。因此每次改完必须**亲自跑一遍编译 / transform 验证**，再向用户报告"完成"。

## 8.1 前端改动

1. 在前端子项目目录执行 `npx tsc --noEmit` → 必须 0 错误
2. **对每个改动的文件**请求其在 dev server 上的模块地址 → 必须 200
   ```bash
   PORT=<§1「本地开发服务」表里的前端端口>
   curl -s --noproxy '*' -o /dev/null -w '%{http_code}\n' "http://localhost:$PORT/src/<改动文件相对路径>"
   ```
   （这一步才抓得到打包器解析错误，类型检查抓不到）
   > 该写法是 **Vite 系**特有（dev server 按源码路径直出转译后模块）。其他打包器（webpack / rspack 等）改用等效的「单模块编译验证」手段，目的一致：**在浏览器之外触发一次真实 transform**。
3. 用户描述的故障路径含具体页面时，必须**真的把那个 URL 拿到 200** 才能宣布修复
4. **协议级改动必须跑 E2E**（不是可选）。判定为协议级的四类改动：
   - 跨前后端的数据契约（字段类型、枚举、DTO 结构）
   - 渲染主链路文件（列表/编辑/详情共用的取数、缓存、展开逻辑）
   - 数据库迁移
   - 任何「改一处、多处静默失效」的强联动改动（见 §8.4）

   PR 必须附截图或日志作为渲染证据。**跳过 E2E 等于跳过自检** —— 协议类 bug 只在 E2E 暴露，API / 类型检查看不到。
   > 项目里具体哪些文件属于「渲染主链路」，随经验登记进 `dev-docs/INDEX.md` 的「按代码文件反查」表，并在此处列名。

## 8.2 后端改动

1. 强制服务重启（如 `touch` 一个源文件触发热重载）→ 等重启完成
2. `curl` 具体 endpoint → 期望 200/401（**不要 500**）
3. 有数据迁移时：查迁移历史表确认 `success = true`
4. **不要手工执行迁移 SQL**！让框架启动时自动跑。手工跑会导致重启时 checksum 对账不符报 `Migration checksum mismatch`，或碰到"对象已存在"导致启动失败
5. **N+1 自检**（见 §3.2）。声明格式示例：
   > `N+1 自检：本次改动 3 处循环，均为纯内存运算，无查库 ✅`
   > `批量化验证：数据量 5→20 条，SQL 条数恒为 4 ✅`

## 8.3 schema DDL（DROP CASCADE / 视图重建）后必须重启服务

1. **触发场景**：任何 `DROP VIEW/TABLE ... CASCADE`、视图列结构变更、视图重建
2. **必须**强制重启（不是只跑完迁移就够）
3. **原因**：进程级缓存（表结构缓存 / 编译后 SQL 缓存 / 解析器缓存）会在对象被 CASCADE 临时删除的瞬间**缓存空集**并永久残留，导致后续查询行为错乱（典型症状：本该返单值的地方返全表）
4. **自检**：用一个依赖该视图的 endpoint 验证，期望返单值而非数组

## 8.4 强联动改动的 SOP

某些改动会跨多个文件传播协议（如新增字段类型、改枚举、改数据契约），**漏一处必有静默失败**——不报编译错、不报运行时错，只是行为不对。

1. **写代码前**：全工程 grep + 跑影响面分析，列出全部检查点清单，写进任务文档
2. **写代码中**：对照清单逐项勾掉
3. **写代码后**：跑 E2E，并在**所有受影响视图**（列表 / 编辑 / 详情）分别验证
4. **PR 必含**：清单的 grep 命中输出 + E2E 通过证据 + 各视图截图（修复前 vs 后）

发现新的强联动关系时：检查点清单留在该任务目录下，并在 `dev-docs/INDEX.md` 的「按代码文件反查」表登记「这些文件压着什么契约」，同时在 `docs/RECORD.md` 追加一条根因记录。

## 8.5 当出现"看到了 500 / 红色报错遮罩"反馈时

1. 不要立刻假设"是浏览器缓存"—— 先 `curl` 拉一次原始响应，看错误是不是已经修了
2. 文件确实是好的而遮罩仍在 → 让用户**强刷（Ctrl+Shift+R）** 清 HMR overlay
3. 文件本身有问题 → 立即修，再跑完整自检后通报

## 8.6 "完成"宣告的格式要求

**任何"完成"宣告必须包含一行"已自检"声明**，例如：

> TS 0 错误 ✅；`XxxPage.tsx` → dev server 200 ✅；后端 `/api/xxx` → 401（auth 正常）✅；迁移 V77 success=t ✅

**没有这行声明的"完成" = 未完成。**

## 8.7 测试结果的可信前提

**测试报告能当验收证据，前提是测试基础设施本身可信。** 这一节管的是「怎么知道那个绿是真的」。

### ① 出现不稳定失败，先查测试基础设施，再查业务代码

症状特征：**随机挂一条、每次挂的还不一样、数据看着合理但对不上**。

这类症状的高频根因**不在业务代码**，而在测试基础设施：并发、端口分配、临时目录、共享库、全局状态。

⚠️ 它最危险的地方是**两个方向的证据同时失效**：绿可能只是没撞上，红可能是被污染而非真缺陷。**在 harness 被证明可信之前，测试报告不作为验收证据**，也不要急着修业务代码。

### ② 🚫 禁止以「偶发，重跑一下就好」结案

必须定位到根因。宣称修好时要给出**重复次数**，且次数要与**修法的性质**匹配：

| 修法性质 | 含义 | 需要的重复次数 |
|---|---|---|
| **结构性修复** | 失败模式被**构造性消除**（例如探活改成验明正身，撞端口也不会误判） | 少量即可，作用是「确认没引入新问题」 |
| **概率性修复** | 只是把碰撞概率压小，失败模式还在 | 按 `p^n` 算，通常远超直觉 |

概率性修复的置信度参考（`p` = 修复前每轮的通过率）：

| p | 2 轮全绿是运气的概率 | 8 轮 | 要压到 5% 以下需要 |
|---|---|---|---|
| 0.83 | **69%** | 23% | 约 17 轮 |
| 0.90 | 81% | 43% | 约 29 轮 |

**跑两次全绿证明不了任何事** —— 这是最容易被骗过去的地方。

### ③ 并行测试的临时资源必须唯一化

端口、库文件、临时目录都要唯一化，且 **获取与占用之间不能有「无主窗口」**。

> 典型 TOCTOU：`freePort()` 先 `close()` 拿到端口号，再交给子进程去绑——中间这段时间端口是无主的，OS 会把它分给别人。
> 而且它**不以「启动失败」的形式暴露**：B 的子进程绑不上直接退了，B 的探活却探到 A 那个活得好好的服务，探活成功，然后 B 把全部断言打在 A 的库上。

配套：**探活必须验明正身**（见 §1 坑 3），只看 HTTP 200 挡不住这条路径。

### ④ 新加的守卫 / 断言必须做一次证伪实验

**首次 PASS 不能证明守卫有效** —— 可能它压根没接上。

- 故意破坏它保护的条件，确认它**硬失败**（例：把身份匹配串改坏，确认探活会失败而不是退回「200 就算数」）
- 断言「某事没发生」时（如「一个请求都没发」），必须配一个**阳性对照**，证明观察手段能抓到该事件
- 🚨 **自己写的验证脚本同样适用**：先确认它「能输出东西」。命令写错导致**全程空输出**，看起来和「全部通过」一模一样

## 8.8 冷启动验证（闸门 B 之前必做）

**假装自己是刚 clone 这个仓库的人**，只按文档从零跑一遍：装依赖 → 起服务 → 跑测试 → 打开页面。**跑不通的每一步，都是文档缺口。**

**为什么这一步不能省**：开发期为了隔离而替换掉的东西，真实值**从头到尾没被验证过**——

| 开发期的替身 | 从没被验证的真实值 |
|---|---|
| 临时端口 3099 / 5199 | **契约里声明的端口**（可能压根绑不上） |
| worktree 里的依赖目录 | 主工作区能不能装起来（worktree 一删就没了） |
| 每用例一个临时库 | 真实库上的迁移 |
| mock 掉的外部依赖 | 真实依赖的连通性 |
| 工具默认输出目录里的截图 | 下一轮就被清空，等于没有证据 |

**所有测试全绿，都证明不了「应用能在它自己声称的配置下启动」。**

两条配套：
- **前移一个 5 秒的冒烟**：开发早期就用**声明端口**绑一次，别等亲验才发现绑不上
- 冷启动跑不通的步骤，**补进 README 或任务文档**，不要只在自己会话里记着

---

# 9. Backlog 自动管理规则

## 核心原则

所有在 spec 评审中被建议推迟的功能，必须持久化到 `docs/BACKLOG.md`。
**禁止只在对话中口头提及二期任务，上下文丢失不能成为功能遗漏的理由。**

## 规则一：评审 Spec 时的强制行为

评审任何 spec / 需求文档时必须：

1. 完成评审分析后，明确列出**建议推迟的功能点**及推迟理由
2. 询问用户是否采纳（**等待确认，不要自动写入**）
3. 收到确认后，立即更新 `docs/BACKLOG.md`
4. 每个条目必须包含：功能描述（来源章节）/ 推迟原因 / 优先级（P0/P1/P2）/ 前置条件 / 预估规模（S=1-2天 / M=3-5天 / L=1周以上）
5. 写入后输出摘要确认，并告知总计推迟条目数

## 规则二：新会话首个开发指令前的强制行为

1. 读取 `docs/BACKLOG.md`
2. 列出所有 `[ ]`（待开发）条目及优先级
3. 判断本次任务是否与任何待开发条目相关，并告知用户
4. 相关则询问是否一并处理，等待决策
5. 不相关则简短提示当前有 N 个待开发条目，然后继续执行

## 规则三：完成开发任务时的强制行为

1. 检查本次实现是否覆盖了任何 Backlog 条目 → 覆盖则更新为 `[x]` 并在变更记录追加一行
2. 检查是否满足了某些条目的「前置条件」→ 是则更新备注为「✅ 已就绪」
3. 输出收尾摘要：
   「Backlog 状态：共 N 条，已完成 X 条，待开发 Y 条，其中 P0 级 Z 条。」

## 条目标准格式

```markdown
### [模块名称]

- [ ] **功能描述**
  - 来源：spec §X.X / [章节名]
  - 推迟原因：
  - 优先级：P0 / P1 / P2
  - 前置条件：
  - 预估规模：S / M / L
```
